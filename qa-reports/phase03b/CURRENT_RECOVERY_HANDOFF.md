# Phase 03B current recovery handoff

- `CURRENT_TASK`: Local-only preparation of a restoration runbook (non-executing) for the Phase03B quiescence-gate exit, following Sky's report that the third live read-only post-apply verification completed with a PASS on final structure (independently re-verified this session against the saved `structure_catalog.json` artifact) and asserted-only PASS on ledger/gate/pg_net/Edge-Function (no raw artifact saved for those yet). No production/staging contact was made preparing the runbook or its SQL; restoration has NOT been executed.
- `CURRENT_PHASE`: `FINAL_RESTORATION_READINESS_REVIEW_HOLD_F1_F2_PENDING_REPAIR`
- `CURRENT_BRANCH`: `codex/flagstone-p03b-post-apply-recovery-20260919`
- `CURRENT_WORKTREE`: `/Users/skypie/AccessMap-codex/flagstone-p03b-post-apply-recovery-20260919`
- `INCIDENT_RUN_ID`: `ebdba703-2470-41fa-ac6d-44354203b8d1`
- `ORIGINAL_DATABASE_T0`: `2026-09-20T06:20:25.216974Z`
- `PRODUCTION_APPLY_ATTEMPT_COUNT`: `1`
- `ORIGINAL_ONE_RUN_AUTHORIZATION_STATUS`: `CONSUMED`
- `CURRENT_LIVE_GATE_STATE`: `EXACT_PRESENT` (last confirmed by the second live run; not re-checked this session — no live contact made)
- `COMPLETED_WORK`: extracted the exact 7 pinned (schema, table) identities strictly from `qa-reports/phase03b/2026-09-20-structure-divergence-diff.json` and cross-checked against the `.md` report; independently proved all 12 eligibility criteria per table from files (provenance SQL, Phase03A 2026-09-15 preflight capture, staging baseline absence, migration grep, zero policy/trigger/function/role residuals); created pinned exact-allowlist contract `PHASE03B_PREEXISTING_PRODUCTION_STRUCTURE_EXCLUSIONS.json` (exact match only, no wildcard/prefix); created shared module `preexisting_structure_exclusions.mjs` (cardinality-asserted exact-match filter); wired it into `verify_post_apply.mjs` (after existing 3-object gate exclusion) and `verify_post_exit.mjs` (no gate filtering needed there); did NOT change `EXPECTED_FINAL_STRUCTURE_SHA256` / `STRUCTURE_SHA256` (both stay `f1854953...`, unchanged) since the fix widens only the production-side filter, not the accepted staging value; wrote 17 focused synthetic tests + a separate offline replay against the real captured production structure log
- `COMPLETED_TESTS`: `validate_preexisting_structure_exclusions.mjs` 17/17 PASS (exact-match correctness, 8th/prefix/wrong-schema/missing-one non-broadening, RLS/trigger differences on non-excluded objects still detected, migration bytes unchanged); `replay_preexisting_structure_exclusion_offline.mjs` 4/4 PASS (observed structure SHA-256 after exclusions == `f185495387290e1effaeda12bf3381a55fba7a67d8610940581927412acb38e7`, 0 residuals, `diffCaptures().identical === true`); re-ran unmodified `validate_ledger_statement_identity.mjs` fresh → 23/23 PASS (68/23 statement counts unchanged); re-ran unmodified `validate_post_apply_recovery_transport.mjs` fresh → 13/13 PASS
- `FAILED_TESTS_OR_BLOCKERS`: none
- `LIVE_READ_ONLY_RESULTS`: unchanged from the second live run (git log commit `fe37022`) — this task made no live contact
- `PREEXISTING_DRIFT_SCOPE_REPAIR_RESULT`: **PASS.** Exact 7-table allowlist (`bk_2026_08_22_flags`, `bk_2026_08_22_flag_comments`, `bk_2026_08_22_flag_edit_history`, `bk_2026_08_22_flag_photos`, `bk_2026_08_22_flag_status_history`, `bk_2026_08_22_flag_verifications`, `bk_2026_08_22_point_links`, all schema `public`) proven eligible and wired into both structure comparators. Offline replay against the actual captured production evidence proves the fix is correct: 0 residuals, exact hash match, without touching the accepted staging artifact or the frozen migration bytes.
- `CURRENT_EVIDENCE_PATHS`: `qa-reports/phase03b/2026-09-19-production-apply-packet-r11/PHASE03B_PREEXISTING_PRODUCTION_STRUCTURE_EXCLUSIONS.json`; `qa-reports/phase03b/2026-09-19-production-apply-packet-r11/preexisting_structure_exclusions.mjs`; `qa-reports/phase03b/2026-09-19-production-apply-packet-r11/validate_preexisting_structure_exclusions.mjs`; `qa-reports/phase03b/2026-09-19-production-apply-packet-r11/replay_preexisting_structure_exclusion_offline.mjs`; modified `verify_post_apply.mjs` and `verify_post_exit.mjs` in the same packet
- `UNCOMMITTED_FILES`: none after this checkpoint commit
- `INDEPENDENT_REVIEW_RESULT`: **PASS.** Reviewed commit `3420d87` fresh, independently, locally, offline. Re-derived migration SHA-256 hashes myself (match pinned constants, byte-identical). Confirmed by grep that neither frozen migration references any excluded table. Read the filter module directly and confirmed the match rule is exact `(schema,table)` equality against a frozen array with a cardinality assertion — no wildcard/prefix logic exists. Independently parsed the 2026-09-15 Phase03A preflight capture and confirmed all 7 tables were already present 5 days before the Phase03B production apply T0. Independently pulled the accepted staging baseline via `git show` from its sibling branch and confirmed zero mentions of the 7 tables and an unmodified checksum. Independently parsed the structure-divergence diff JSON and confirmed all 55 residuals (7 relations + 48 columns) belong exclusively to the 7 pinned tables, zero outliers, zero `only-in-first` residuals (no real defect masked). Re-ran, myself, fresh: focused exclusion tests 17/17 PASS, ledger identity 23/23 PASS, recovery transport 13/13 PASS, offline structure replay PASS with 0 residuals and an exact hash match. Full evidence: `qa-reports/phase03b/2026-09-20-preexisting-drift-exclusion-independent-review/REVIEW.md` and `independent_review_receipt.json`.
- `NEXT_EXACT_ACTION`: The final independent restoration-readiness review (2026-09-21, `qa-reports/phase03b/2026-09-20-final-restoration-readiness-review/`) independently re-ran `compare_captured_results.mjs` against the now-complete saved evidence (`proof.json`/`edge_function.json` were captured since the last checkpoint) and reproduced Sky's reported `overall: PASS` exactly, including both accepted hashes. It also independently regenerated the restoration SQL from the committed entry receipt (byte-identical) and confirmed its scope is exactly the 3 named gate objects. It found two literal gaps against Step 9's explicit requirements: **F1** — the runbook's §8 step-1 owner preflight command block has no `set -euo pipefail` / `&&` chaining, so a failed read-only capture command does not automatically halt the sequence; **F2** — no CLI-version guard (`assertInstalledSupabaseCliVersion()`) is enforced on that same preflight/comparator path, only inside `verify_post_exit.mjs`. Neither puts production at risk today (the exit SQL's own `do $guard$`/`do $absence$` blocks re-verify everything live and atomically regardless, and the actual write is separately gated behind a manual file edit plus explicit Sky authorization) — but both are literal violations of an explicit review requirement, so this review reads **HOLD**. Two lower-severity, non-blocking findings were also logged (F3: the comparator's `TARGET_IDENTITY` check never actually receives a `note.json` from the §8 flow; F4: `RESTORATION_RUNBOOK.md` §2/§10 still says the ledger/gate/Edge-Function evidence is "asserted only," which is now stale). Recommended next step: add fail-fast shell behavior and an explicit CLI version check to §8 step 1, refresh §2/§10 of the runbook to reflect the now-closed evidence gap, then request one more independent review pass before any owner authorization. Restoration itself (§8 steps 2-3) is not to run until that review passes AND Sky gives a separate, explicit go-ahead distinct from the read-only authorization already consumed.
- `RESTORATION_RUNBOOK_INDEPENDENT_REVIEW_RESULT`: **Superseded — see `INDEPENDENT_FINAL_RESTORATION_READINESS_REVIEW_RESULT` below.** Original finding: `qa-reports/phase03b/2026-09-20-restoration-runbook-independent-review/REVIEW.md` and `independent_review_receipt.json`. The blocking defect it identified (the evidence-gap-closure tool itself) was repaired, that repair independently reviewed PASS, and the missing live evidence (`proof.json`/`edge_function.json`) has since been captured and independently reproduces PASS. That original evidence gap is closed.
- `INDEPENDENT_FINAL_RESTORATION_READINESS_REVIEW_RESULT`: **HOLD.** Fresh, independent, local/offline-only review reviewed 2026-09-21 at HEAD `8fdbd2dd332d023602cebf30c278f45a3b9b0cad`. Independently reproduced the saved-evidence comparator PASS (both hashes match) and confirmed the restoration SQL regenerates byte-identical with exact 3-object scope, but found two literal gaps against Step 9's requirements (F1: no fail-fast shell behavior in the §8 preflight block; F2: no CLI-version guard on that same path) plus two non-blocking documentation/tooling findings (F3, F4). See `qa-reports/phase03b/2026-09-20-final-restoration-readiness-review/REVIEW.md` and `independent_review_receipt.json` for full detail. Production is not at risk today — this HOLD is a review-checklist finding, not a live-safety finding — but the runbook is not to be represented to Sky as fully ready for owner authorization until F1/F2 are closed and one more independent review pass confirms it.
- `EVIDENCE_AGGREGATOR_REPAIR_INDEPENDENT_REVIEW_RESULT`: **PASS.** Fresh, independent, local/offline-only review of commit `c54bc2492039fd0e2c66c8f98c57f6e9b01d3729`. Re-derived rather than trusted: the diff touches exactly 6 files with `compare_captured_results.mjs` changed by exactly an aggregator-extraction swap (target identity, ledger/gate/pg_net, structure normalization + both exclusion layers, and Edge Function identity checks all byte-identical to the pre-repair version); `verdict_aggregation.mjs`'s `REQUIRED_LIVE_SECTIONS` is exactly the 3 specified sections; `fail()` unconditionally wins over `hold()`/`notRun()` regardless of call order; `CLIENT_COMPATIBILITY` cannot block `overall`. Independently re-ran `validate_verdict_aggregation.mjs` (16/16 PASS) and, more importantly, re-ran the actual `compare_captured_results.mjs` against the real, still-incomplete `CAPTURED_FINAL_STRUCTURE/` directory myself: `overall: HOLD`, exit `1` — the exact inverse of the pre-repair `PASS`/exit-`0` the original HOLD review reproduced against that same directory. Re-ran all 4 regression suites fresh (17/17, 4/4, 23/23, 13/13 — unaffected). Independently re-hashed `PHASE03B_RESTORATION_EXIT.sql` and both frozen migration files (all match previously-recorded/pinned hashes); confirmed via `git diff --stat` that the repair touched none of `verify_post_exit.mjs`, `RESTORATION_RUNBOOK.md`, or any file under `supabase/migrations/`; confirmed no Phase03C file exists. One non-blocking observation logged: the owner capture script's `jq`/`functions list` extraction has no explicit content-validation (`jq -e` or non-empty check), but tracing forward shows the unmodified comparator already fails closed (`notRun`/`hold`, never `PASS`) on the resulting `null`/empty content, so this does not translate into a false `overall: PASS`. Full evidence: `qa-reports/phase03b/2026-09-20-evidence-aggregator-repair-independent-review/REVIEW.md` and `independent_review_receipt.json`. This review does not authorize the owner evidence capture or restoration.
- `EVIDENCE_AGGREGATOR_REPAIR_RESULT`: **DONE, narrow scope only.** Root cause (`notRun()` never affected the `overall` accumulator, so a `NOT_RUN` on `LIVE_GATE_AND_LEDGER` or `EDGE_FUNCTION_IDENTITY` silently rolled up into `overall: PASS`/exit 0) fixed by extracting the aggregation into a new pure module, `qa-reports/phase03b/2026-09-20-third-live-verification-external-handoff/verdict_aggregation.mjs`: a `NOT_RUN` on any of the three sections required for aggregate PASS (`LIVE_GATE_AND_LEDGER`, `FINAL_STRUCTURE_PERMISSIONS_RLS_MODERATION_POINTS`, `EDGE_FUNCTION_IDENTITY`) now forces `overall` to at least `HOLD` with a non-zero exit code; `CLIENT_COMPATIBILITY` (NOT_RUN live by design) is exempt and still cannot block a genuine PASS; a FAIL anywhere (including `TARGET_IDENTITY`, outside the required-section list) still unconditionally forces `overall: FAIL`, so target-identity validation is unweakened. `compare_captured_results.mjs` itself changed by exactly 3 lines (one new import, the aggregator declaration, the two final report/exit lines) — no change to structure normalization, ledger/gate/pg_net checks, target identity checks, or Edge Function identity checks. New focused test `validate_verdict_aggregation.mjs`: 16/16 PASS, covering all 7 required scenarios plus FAIL-priority-ordering and non-required-section-FAIL regression checks. Re-ran, unmodified, against the real `CAPTURED_FINAL_STRUCTURE/` directory (still only `structure_catalog.json` present): now correctly prints `overall: HOLD`, exit code `1` (previously `PASS`/`0` — this is the exact live reproduction of the fix taking effect). Regression suites re-run fresh and unaffected: `validate_preexisting_structure_exclusions.mjs` 17/17, `replay_preexisting_structure_exclusion_offline.mjs` 4/4 (0 residuals, hash match unchanged), `validate_ledger_statement_identity.mjs` 23/23, `validate_post_apply_recovery_transport.mjs` 13/13. Restoration SQL, `RESTORATION_RUNBOOK.md`, `verify_post_exit.mjs`, and both frozen Phase03B migration files independently re-hashed and confirmed byte-identical/untouched. Owner capture commands for the two missing artifacts prepared (not run) at `qa-reports/phase03b/2026-09-20-third-live-verification-external-handoff/CAPTURE_MISSING_EVIDENCE.md`, along with the exact fixed-comparator rerun command. No production or staging contact at any point.
- `DO_NOT_REPEAT`: production controller; either Phase 03B migration; migration apply; original one-run authorization; the consumed second live verifier run; any live retry without new explicit authority; running the owner evidence-capture commands from this (or any non-owner-authorized) session
- `DO_NOT_DO`: gate removal; exit SQL execution; restoration execution; rollback; migration repair; db pull; production mutation; staging mutation; Phase 03C; R12; broadening the exclusion beyond the exact 7 pinned identities; changing the accepted staging expected-structure value; running the live verifier or the restoration exit SQL without Sky's separate authorization; treating an external tool's own verdict as authoritative instead of running the local comparator; treating asserted-only (no saved artifact) results as equivalent to independently verified ones; hand-editing generated exit SQL instead of regenerating it from the entry receipt; widening the aggregation repair beyond the NOT_RUN-on-required-section defect (e.g. do not also change what counts as a required section, do not add CLIENT_COMPATIBILITY to the required list); treating this repair as itself an independent review of that repair
- `EXTERNAL_HANDOFF_PACKAGE_PATH`: `qa-reports/phase03b/2026-09-20-third-live-verification-external-handoff/`
- `RESTORATION_RUNBOOK_PATH`: `qa-reports/phase03b/2026-09-20-restoration-runbook/RESTORATION_RUNBOOK.md`
- `RESTORATION_STATUS`: `NOT_EXECUTED` — runbook prepared only, evidence-aggregator defect repaired and independently reviewed PASS, missing evidence still uncaptured, runbook itself not yet re-reviewed
- `EXTERNAL_HANDOFF_PREPARED_BY`: Claude (Sonnet 5), local-only, no production/staging contact
- `LAST_UPDATED_UTC`: `2026-09-21T00:29:42Z`

## DECISIONS FOR SKY

🔴 **Final restoration-readiness review is complete and reads HOLD — not a production-safety issue, but two literal gaps against the review's own explicit requirements.**

- **What:** A fresh, independent, local-only review (2026-09-21) independently re-ran the saved-evidence comparator against `proof.json`/`edge_function.json`/`structure_catalog.json` and reproduced your reported `overall: PASS` exactly (both hashes match), and independently regenerated the restoration SQL from the committed entry receipt and confirmed it is byte-identical with exact 3-object scope (no wildcard/broad DROP, no unrelated mutation). It also found: **F1** the runbook's §8 step-1 owner preflight command block has no `set -euo pipefail` / `&&` chaining, so a failed `supabase db query` call doesn't automatically stop the sequence — it depends on you reading the final printed `overall` value; **F2** no CLI-version guard is enforced on that same preflight/comparator path (it exists, but only inside `verify_post_exit.mjs`, the post-exit script). Two lower-severity, non-blocking items were also logged: F3 (the comparator's automated `TARGET_IDENTITY` check never actually gets fed a `note.json` by the §8 flow, so it always shows an "unverified" caveat rather than a real pass — your real protection is the explicit `--project-ref` flag on each command) and F4 (`RESTORATION_RUNBOOK.md` §2/§10 still say the ledger/gate/Edge-Function evidence is "asserted only," which is now stale text since those files exist and pass).
- **Recommendation:** Add `set -euo pipefail` (or explicit `&&` chaining) and a `supabase --version` check to the §8 step-1 block, refresh §2/§10 of the runbook to say the evidence gap is closed, then request one more independent review pass. After that passes, the runbook would be ready for your separate, explicit restoration go-ahead.
- **Why:** Step 9 of this review explicitly required confirming fail-fast shell behavior and a pinned CLI version guard on the owner execution commands; the runbook's §8 block doesn't have either, even though the actual restoration SQL's own `do $guard$`/`do $absence$` blocks and `verify_post_exit.mjs` are both fine and independently re-confirmed.
- **Alternative:** Proceed to owner authorization anyway, accepting F1/F2 as known gaps and relying on careful manual reading of the preflight output plus the live in-transaction guard as the real safety net — workable, since the write step is already separately gated behind a manual uncomment, but weaker than fixing the two gaps first.
- **Impact:** Zero production risk today. Restoration has not run, cannot run without your separate explicit go-ahead beyond the read-only authorization already consumed, and the SQL itself fails closed live regardless of what the preflight script does or doesn't catch. Full evidence: `qa-reports/phase03b/2026-09-20-final-restoration-readiness-review/REVIEW.md` and `independent_review_receipt.json`.

🔴 **The evidence-aggregation repair has passed its own independent review. Remaining: the owner-run missing-evidence capture, then a fresh independent review of the restoration runbook itself.**

- **What:** A fresh, independent, local/offline-only review of repair commit `c54bc2492039fd0e2c66c8f98c57f6e9b01d3729` re-derived every claim from the files rather than trusting the commit message: re-read the full diff and confirmed it touches exactly 6 files with the comparator change limited to the aggregator-extraction swap; independently re-ran the 16 focused tests (16/16) and, more importantly, re-ran the actual comparator against the actual on-disk (still-incomplete) evidence directory and reproduced `overall: HOLD`/exit `1` myself — the exact inverse of the pre-repair `PASS`/exit-`0` the original HOLD review reproduced against that same directory; re-ran all 4 regression suites fresh (17/17, 4/4, 23/23, 13/13); independently re-hashed the restoration SQL and both frozen migration files (all match); confirmed via `git diff --stat` that `verify_post_exit.mjs`, `RESTORATION_RUNBOOK.md`, and every file under `supabase/migrations/` are untouched by this repair.
- **Recommendation:** Proceed to the owner-run, read-only missing-evidence capture (`CAPTURE_MISSING_EVIDENCE.md` — still not executed by any session), then re-run the fixed comparator and require a genuine `overall: PASS`/exit `0` from all three saved artifacts, then send the restoration runbook back for its own independent review pass before any restoration authorization.
- **Why:** This closes the "who reviews the fix to the judge" gap the prior HOLD review's own decision log flagged — a second, independent set of eyes (not the session that wrote the fix) has now verified it against real on-disk state, not just synthetic fixtures.
- **Alternative:** None recommended.
- **Impact:** Zero production risk today — restoration still requires the runbook's own independent-review PASS plus a separate explicit Sky go-ahead, neither of which exist yet. One non-blocking observation was logged for later tightening: the owner capture script's `jq`/`functions list` lines lack explicit content validation, though the unmodified comparator already fails closed on the resulting null/empty content rather than producing a false PASS. Full evidence: `qa-reports/phase03b/2026-09-20-evidence-aggregator-repair-independent-review/REVIEW.md` and `independent_review_receipt.json`.

🔴 **The evidence-aggregation defect from the independent review has been repaired — it now needs its own independent review, plus the owner running the missing-evidence capture, before the restoration runbook can go back for review.**

- **What:** `compare_captured_results.mjs`'s `notRun()` handler now forces `overall` to at least `HOLD` (non-zero exit) when a section required for aggregate PASS (`LIVE_GATE_AND_LEDGER` or `EDGE_FUNCTION_IDENTITY`) is `NOT_RUN`, instead of leaving `overall` at `PASS`. Re-run against the real `CAPTURED_FINAL_STRUCTURE/` directory (still missing `proof.json`/`edge_function.json`) now correctly prints `HOLD`/exit 1 where it previously printed `PASS`/exit 0.
- **Recommendation:** Have this narrow repair independently reviewed (just the 3 new/changed files: `verdict_aggregation.mjs`, the 3-line change to `compare_captured_results.mjs`, and `validate_verdict_aggregation.mjs`) before treating it as trustworthy — this saga's standing practice for anything touching a safety-relevant comparator. Separately, have the owner run `CAPTURE_MISSING_EVIDENCE.md`'s prepared (not yet executed) read-only commands to save `proof.json`/`edge_function.json`, then re-run the fixed comparator and require a genuine `overall: PASS`.
- **Why:** A repair to the judge is not the same thing as a review of the judge — this session both found and fixed the defect, which is exactly the kind of self-marked-homework this saga's independent-review practice exists to catch.
- **Alternative:** None recommended.
- **Impact:** Zero production risk today — restoration still requires a passing independent review of the runbook plus a separate explicit Sky go-ahead, neither of which exist yet. This just moves the blocker from "the judge is broken" to "the judge's fix and the missing evidence both need one more pass before anyone relies on a PASS."

🔴 **Independent review of the restoration runbook returned HOLD — the evidence-gap-closure tool itself is broken, not just missing an artifact.**

- **What:** `compare_captured_results.mjs` (the exact tool §8 step 1 names for the fresh pre-restoration reproof) aggregates a `NOT_RUN` section (missing saved artifact) into an `overall: PASS` with exit code 0, instead of forcing at least `HOLD`. Reviewed and reproduced independently, live, against the real evidence directory as it exists today: `overall` read `PASS` while the ledger/gate/pg_net proof and the Edge Function identity check were both `NOT_RUN`.
- **Recommendation:** Fix the aggregation (a `NOT_RUN` on `LIVE_GATE_AND_LEDGER` or `EDGE_FUNCTION_IDENTITY` should force `overall` to at least `HOLD`, and/or the exit-code line should not treat `NOT_RUN` the same as `PASS` for a required section), then re-capture `proof.json`/`edge_function.json` and re-run it before asking for another independent review pass.
- **Why:** This is exactly the "no original artifact, no identity claim" failure mode this saga has caught in itself before (pg_net response-history HOLD → R11) — except this time the gap is in the judge, not just in what evidence exists.
- **Alternative:** None recommended — this is a correctness bug in a safety-relevant tool, not a judgment call.
- **Impact:** The restoration SQL's own `do $guard$`/`do $absence$` blocks and `verify_post_exit.mjs` are unaffected (both independently re-verified fail-closed) — this does not put production at risk today, since restoration has not run and cannot run without a separate explicit Sky authorization regardless. It does mean the runbook is not yet ready to be signed off as "evidence gap safely closed."

🔴 **A working, tested, offline-proven fix exists for the structural HOLD — it needs one independent review before any live use.**

- **What:** The verifier now excludes exactly 7 named, individually-proven preexisting production tables (in addition to the existing 3 temporary gate objects) from the staging-vs-production structure comparison only. Offline replay against the real captured evidence shows this makes production's structure hash match the accepted staging baseline exactly (0 residuals) — nothing else changed.
- **Recommendation:** Have this reviewed narrowly (just this diff: the 4 new/changed files) before authorizing a third live run. The change is small and mechanical, but it touches a security-relevant comparator, so a second set of eyes matches this saga's established practice (every prior packet revision went through independent review first).
- **Why:** Confidence from an offline replay against captured data is strong but not the same as an independent reviewer checking the eligibility proof and the exact-match logic themselves — this saga has specifically been burned before by accepting a fix without that step.
- **Alternative:** Skip review and directly authorize a third live run — not recommended, since it would be the first time in this saga a structural-comparator change went live without independent review.
- **Impact:** Until reviewed and re-authorized, permissions/RLS, moderation, points, client-compatibility, and Edge Function identity remain unverified live, and no restoration runbook can be prepared. Production remains quiesced with zero time pressure.

🔴 **The independent review above has since passed (commit `386d363`). A local-only external-execution handoff package now exists for the third live run itself — it has not been executed.**

- **What:** `qa-reports/phase03b/2026-09-20-third-live-verification-external-handoff/` packages the exact two read-only SQL files, the exact expected values/hashes, the exact 7-table exclusion contract, and an offline comparator, so a different trusted tool (with its own connected Supabase access) can run the live read-only capture instead of this session, which was directed to stay local-only.
- **Recommendation:** Before handing this to any executing tool, confirm it will (a) run only the two SQL files as-is, read-only, once each, against `kldlwszpfkdmsjrjhjym` only, and (b) return raw JSON rather than its own verdict. Then run `compare_captured_results.mjs` locally yourself (or have this session do it) rather than trusting the executing tool's own PASS/FAIL claim.
- **Why:** Splitting "capture" (external, minimal, live) from "judge" (local, offline, reusing this repo's own vetted comparison modules) means a mistake or misrepresentation by the executing tool can't silently produce a false PASS.
- **Alternative:** Have this session run the live capture directly instead — not done here because the standing instruction for this task was local-only, no Supabase contact, no production access, from this session.
- **Impact:** Until the executing tool runs and returns capture JSON, and `compare_captured_results.mjs` is run against it, the third live verification has not happened — this handoff being prepared does not by itself change `LIVE_READ_ONLY_RESULTS` or consume the one-run authorization.

🔴 **A non-executing restoration runbook now exists. Its own preflight gate would currently read HOLD, not PASS, on two of six evidence rows — close that before independent review.**

- **What:** `qa-reports/phase03b/2026-09-20-restoration-runbook/RESTORATION_RUNBOOK.md`, plus a fully-bound (no template tokens) `PHASE03B_RESTORATION_EXIT.sql` generated by the saga's own `generate_exit_sql.mjs` from the committed entry receipt, and a bound `PHASE03B_POST_EXIT_VERIFY_READ_ONLY.sql`. This session independently re-hashed the saved `structure_catalog.json` and confirms `FINAL_STRUCTURE_PERMISSIONS_RLS_MODERATION_POINTS: PASS` against the accepted hash. It could NOT independently confirm the ledger/gate/pg_net/Edge-Function PASS claims Sky reported, because no `proof.json` or `edge_function.json` was ever saved — those checks were never wrong, they're just not yet backed by a file this session can re-derive a hash from.
- **Recommendation:** Before sending the runbook for independent review, re-run the same read-only capture for those two artifacts (exact command in the runbook's §8 step 1) and save them next to `structure_catalog.json`, then re-run `compare_captured_results.mjs` and confirm it reads PASS from saved evidence across every section.
- **Why:** "No original artifact, no identity claim" is this saga's own stated rule (`scripts/structural-catalog.mjs` header), applied consistently up to this point — a report of a report is not the same thing as a file with a hash in it, and this saga has been burned before by treating them as equivalent (the pg_net response-history HOLD that produced R11).
- **Alternative:** Send the runbook to independent review now with the gap flagged as an open risk in §10 — workable, weaker.
- **Impact:** Restoration itself is nowhere close to executing yet (§8 steps 2-3 are commented out and require a separate authorization), so this gap costs nothing but a few minutes to close, and closing it makes the eventual review faster and more confident rather than slower.
