# R4 three-defect traceability matrix

Source review: `f77555b2b4a3ce7e1939ad63ae1a9bef4e82890c`, `qa-reports/2026-09-17_Codex_Phase03BProductionApplyPacketR3IndependentReview.md`.

| ID | Exact source finding | Executable failure and root cause | Downstream HOLD fields | Machine-verifiable R4 acceptance | Repair and local proof | Candidate change |
|---|---|---|---|---|---|---|
| R4-D1 | Lines 15–22: exit generator ignores the controller envelope's `entry` member; R3 replay bypasses that envelope. | The successful path cannot materialize exit SQL. Producer/consumer JSON contracts differ. | `R3_B3_EXECUTABLE_COMPARATORS`, `PARTIAL_APPLY_FAILURE_MATRIX`, `QUIESCENCE_EXIT_ATOMICITY`, `WRITE_RESTORATION_VERIFICATION`, `EVIDENCE_PACKAGE_R3`, transient risk. | Only one schema-validated R4 envelope is accepted; the exact controller envelope generates exit SQL; malformed/UNKNOWN/comparator-failed states cannot exit; post-exit failure is HOLD. | Fix `generate_exit_sql.mjs`; make the controller own exit and both comparators; replay the exact envelope end to end. | NO |
| R4-D2 | Lines 24–30: 87-file/tail checks do not authenticate historical entries; a substituted unapplied migration can become a third `--include-all` apply. | More than the frozen pair can be discovered. The workspace mixes applied history with pending files and proves shape rather than set equality. | `R3_B3_EXECUTABLE_COMPARATORS`, `EXACT_TWO_MIGRATIONS_ONLY`, `TEMP_WORKDIR_DESIGN`, failure matrix, evidence package, transient risk. | Workspace SQL inventory is exactly the two frozen paths/hashes/sizes; seed/roles/helper SQL are absent; target-explicit dry-run JSON proposes exactly the ordered pair before entry. | Hermetic builder + inventory receipt + dry-run plan guard; inject a third migration and require failure. | NO |
| R4-D3 | Lines 32–40: monitor/immediate proof are captured but not enforced; cadence gaps, full gate identity, ledger/HTTP proof, and ambiguous server state are untested. | Bad evidence can coexist with progression and ambiguous client death cannot be classified. Capture has no enforcing state-machine contract. | `R3_B2_MONOTONIC_CONTROLLER`, cadence, max-quiescence, unknown/client-exit policies, failure matrix, entry receipt, exit/restoration, evidence package, transient risk. | One origin and immutable 30/180/480/600-second deadlines; anchored T+5n samples; every result parsed and required; 600 seconds escalates without exit; ambiguous outcomes invoke read-only classification; no blind retry. | Enforcing controller, expanded monitor, adjudicator, executable matrix, raw-derived receipts, recursive manifest; replay cadence, UNKNOWN, classifications, escalation, comparator/exit behavior. | NO |

Exactly three root defects were present. R3 controls already accepted by the review remain unchanged unless required by one of these contract repairs.
