# Phase 03B production-apply packet R5 four-defect traceability

Source packet: `71ec54f6bbebf9934dbb6ea6cd8dacf2305d670a`

Independent review: `675503927b44c51d6c5b21447d7bc9d9203c52fc`
Root-defect count: **4**

This matrix was the first R5 packet artifact. It maps the four independent-review blockers before any R4 packet implementation was copied or changed. `CANDIDATE_CHANGE_REQUIRED` is `NO` for all four repairs; application code and both frozen migration files remain out of scope.

| ID | Source review artifact and exact location | Exact finding / executable failure / root cause | Downstream HOLD fields | R5 repair / acceptance | Local replay cases | Candidate change |
|---|---|---|---|---|---|---|
| R5-D1 | Review commit `67550392…`, report lines 62–77; R4 `verify_post_apply.mjs:27-28`, `verify_post_exit.mjs:19-20`, `validate_r4_controls.mjs:147-163` | **Finding:** comparator consumers accept `envelope.entry`, a legacy alias, or a bare object. **Failure:** incompatible entry material can reach comparison instead of failing before use. **Root cause:** no shared exact-key, current-version schema validator. | `STRICT_R5_ENVELOPE_SCHEMA`; `LEGACY_BARE_ENVELOPE_REJECTION`; `ENTRY_RECEIPT_DESIGN`; `POST_APPLY_VERIFICATION_WHILE_QUIESCED`; `WRITE_RESTORATION_VERIFICATION`; `EVIDENCE_PACKAGE_R5` | One fail-closed R5 validator shared by every consumer, with exact R5 packet/candidate/tree/target/run/controller/monotonic/phase/check/expected/observed/status/source/numeric-exit identity. Accept only the exact current schema; reject bare, legacy, incomplete, extra-key, wrong-identity, null-identity, and malformed values. | 1–7, 11–15, 24, 27, 28, 30 | NO |
| R5-D2 | Review commit `67550392…`, report lines 21–42; R4 controller lines 222–235, 270–277, 369–391 | **Finding:** `entryCommitted` and adjudication depend on trustworthy client output/full envelope. **Failure:** a committed entry with lost output can be labeled `HOLD_NOT_ENTERED` with no server adjudication. **Root cause:** absence of client receipt is equated with non-commit, and the classifier depends on that receipt. | `ENTRY_AMBIGUITY_SERVER_ADJUDICATION`; `ENTRY_COMMITTED_LOST_OUTPUT_CASE`; `SERVER_SIDE_UNKNOWN_STATE_POLICY`; `AMBIGUOUS_CLIENT_EXIT_POLICY`; `PARTIAL_APPLY_FAILURE_MATRIX`; `ENTRY_RECEIPT_DESIGN`; `MONOTONIC_CONTROLLER` | Generate immutable `runId` before entry, mark entry begun before spawn, and route every untrustworthy result through receipt-independent read-only classification of exact gates, relationships, ledger, invariants, HTTP baseline, and backends. Exact present = committed; proven absence after explicit rollback = not committed; partial/mismatch/unknown = owner-required fail-closed. | 8–13, 25, 29, 30 | NO |
| R5-D3 | Review commit `67550392…`, report lines 44–60; R4 controller lines 101–110, 263–268, 323–346 | **Finding:** exit eligibility is checked once and expired deadlines still spawn. **Failure:** restoration can launch after the 600-second fail-closed boundary. **Root cause:** no irreversible dispatch-level latch; expired deadline is reduced to a one-millisecond timeout. | `MAX_QUIESCENCE_HARD_LATCH`; `RESTORATION_AFTER_600S`; `MONOTONIC_CONTROLLER`; `MONITORING_CADENCE`; `PARTIAL_APPLY_FAILURE_MATRIX`; `QUIESCENCE_EXIT_ATOMICITY` | Add irreversible `ESCALATED_FAIL_CLOSED_OWNER_REQUIRED`, refuse expired-deadline spawn, and recheck all exit predicates at dispatch. Crossing 600 seconds permanently blocks new automation for the run; a transaction already dispatched below 600 seconds may complete atomically. | 14–18, 26, 30 | NO |
| R5-D4 | Review commit `67550392…`, report lines 79–90; R4 `SERVER_STATE_ADJUDICATE.sql:14-21`, `adjudicate_server_state.mjs:69-76`, `r4_control_lib.mjs:281-297` | **Finding:** ambiguous-state classification omits normalized definition hashes and complete trigger relationships. **Failure:** altered reserved-name objects can be called exact. **Root cause:** incomplete capture plus no committed expected-object identity manifest. | `GATE_EXPECTED_IDENTITY_MANIFEST`; `SERVER_STATE_CLASSIFIER_HASH_PINNING`; `FUNCTION_HASH_PINNING`; `TRIGGER_HASH_PINNING`; `SERVER_SIDE_UNKNOWN_STATE_POLICY`; `PARTIAL_APPLY_FAILURE_MATRIX` | Commit the expected identity manifest and compare every schema/name/OID relationship/owner/enable-state/hash/table/referenced-function/event/timing/level field. Only a complete match is exact; any mismatch is owner-required fail-closed and never automatically dropped. | 12, 13, 19–23, 29, 30 | NO |

The numbered replay cases refer to the 30-case minimum in the R5 owner packet. The machine-readable file contains the full field-by-field wording required for each entry.
