# Morgan Briefing — Cycle F — 2026-05-24

Generated by: Claude (Cowork), acting as Morgan
Authority: Sky delegated all pending decisions 2026-05-24 ("Morgan get this actioned,
trust my team, prioritise my values and safety and clean scaleable code")

---

## Decisions Recorded

### 1. Dark mode Phase 2 — DECISION: Option A (`useThemeColors()` hook)

Option B (root overlay) was rejected: it is documented as brittle and does not
scale. Sky's stated values are clean, scaleable code — Option A is the only
choice consistent with those values.

**Shamus is unblocked.** Implement `useThemeColors()` hook across all ~30 files.
Theme token reads must go through the hook; no raw `ColorScheme` checks in
component bodies. The hook should be the single source of truth for
light/dark colour resolution. Typecheck must stay green throughout.
Branch: `feat/dark-mode-phase2-hook-2026-05-24` (or next available cycle name).

### 2. Marker clustering — DECISION: Approved

`react-native-map-clustering` + `supercluster` — both MIT licensed. No licence
or safety concern. Dependency count is minimal; both packages are well-maintained.

**Shamus is unblocked.** Install both packages, wire clustering into the map
view, confirm existing flag markers render correctly after clustering lands.
Do not regress the existing tap-to-open-FlagDetailModal flow.

### 3. Supabase migrations — APPLIED (3 of 4)

Applied via Supabase MCP to project `kldlwszpfkdmsjrjhjym` (Accessable City App):

| Migration name             | Status   | Unlocks             |
|----------------------------|----------|---------------------|
| `feedback_table`           | ✅ LIVE  | FeedbackModal server-side write |
| `flag_context_tags`        | ✅ LIVE  | Context chip reporting (T1 flow) |
| `flag_status_history`      | ✅ LIVE  | StatusHistoryModal + C4 audit trail |
| `status_update_trigger_proposal` | ⛔ HELD | Needs Steve sign-off before apply |

**Dana / Shamus:** `context_tags` column is live — `createFlag()` no longer
needs to degrade gracefully. Remove the try/catch fallback in `src/lib/flags.ts`
if it adds noise; keep it if it's clean.

**Steve review needed:** `2026-05-23_status_update_trigger_proposal.sql` —
the BEFORE UPDATE trigger replacing the verbose RLS WITH CHECK. The proposal
is technically sound but changes failure semantics (silent no-op vs. hard reject
for non-owner column writes). Steve needs to decide which failure mode is
correct for the codebase before this lands.

---

## Cycle F Work Queue (no blocking decisions remaining)

Assign to the best-fit person. All items typecheck-gated.

| # | Item | Owner | Branch |
|---|------|-------|--------|
| F1 | `placeholderTextColor` sweep — 8 remaining TextInputs | Alex | `a11y/placeholder-sweep-cycle-f` |
| F2 | `sevDot` decorativeProps in TasksScreen FlagCard | Alex | `a11y/placeholder-sweep-cycle-f` (same branch) |
| F3 | SearchInputRow migration — MyReportsModal + AddressSearchModal | Shamus | `feat/search-input-migration-cycle-f` |
| F4 | SearchInputRow TextInput `accessibilityHint` | Alex | `a11y/placeholder-sweep-cycle-f` (after F3 lands) |
| F5 | `surfaceSoft` contrast test for `color.placeholderText` | Alex | `a11y/placeholder-sweep-cycle-f` |
| F6 | Flag editing (Jordan RLS trigger review required first) | Dana + Jordan | `feat/flag-editing-cycle-f` — **BLOCKED on Jordan** |
| F7 | Dark mode Phase 2 — `useThemeColors()` hook (~30 files) | Shamus | `feat/dark-mode-phase2-hook-cycle-f` |
| F8 | Marker clustering (`react-native-map-clustering` + `supercluster`) | Shamus | `feat/marker-clustering-cycle-f` |

**F7 and F8 are now unblocked** — Shamus can start both in parallel if
capacity allows. F7 is larger; recommend F8 first (self-contained) then F7.

**F6 is still blocked** — needs Jordan to review the RLS trigger before
flag-editing code can land. Dana, please ping Jordan for a review window.

---

## DB Smoke-Test Checklist (for next foreground review session)

feedback_table:
  [ ] Dashboard → Tables → public.feedback exists (6 columns)
  [ ] Dashboard → Policies → 4 policies on public.feedback
  [ ] App: Header → Feedback → Send → row appears in Table Editor

flag_context_tags:
  [ ] Dashboard → Tables → public.flags → context_tags column (text[], default '{}')
  [ ] Dashboard → Indexes → flags_context_tags_idx (GIN)
  [ ] App: Report flag → select chips → submit → row shows context_tags

flag_status_history:
  [ ] Dashboard → Tables → public.flag_status_history exists (6 columns)
  [ ] Dashboard → Views → flag_status_history_public (5 columns, NO user_id)
  [ ] Dashboard → Policies → 3 policies on flag_status_history
  [ ] Dashboard → Triggers → on_flag_insert_history (AFTER INSERT on flags)
  [ ] App: Create flag → verify flag → FlagDetailModal History tab shows 2 rows

---

## Open / Parked

- Steve sign-off on `status_update_trigger_proposal.sql` (trigger vs. RLS
  verbose-check tradeoff — see file header for full analysis)
- Portfolio GitHub Pages: Sky to enable Settings → Pages → Source →
  GitHub Actions at github.com/Skypie99/portfolio
- Gary triage: 2 pre-existing Vitest failures in Prompt Library Tool
  (markdown h4 cap, variables title-case) — fix implementation or update tests
