# AccessMap — Tier 2 Visual Bug Fix (the 8 glaring singles) — Fix Report

**Date:** 2026-07-02
**Branch:** `fix/visual-sweep`
**Base:** `c18403b` (last Tier-1 commit) → **Head:** `b47c9e2`
**Scope:** the 8 remaining GLARING single-site bugs (G4–G10 + M4) from the 2026-07-01 app-wide visual sweep, stacked as one commit each on top of the 7 leveraged Tier-1 class-fixes.
**Status:** ✅ All 8 fixes landed. Gates green. Fence held. **NOT merged, NOT pushed, NOT built (no EAS credit spent).** Web export run as a bundling proof only (allowed by the plan).

---

## Result at a glance

| Gate | Baseline (`c18403b`) | After Tier-2 (`b47c9e2`) |
|---|---|---|
| `tsc --noEmit` | 0 errors | **0 errors** |
| `jest` | 1732 passed / 117 todo / 107 suites | **1732 passed / 117 todo / 107 suites** |
| `eslint src` | 87 problems (0 err, 87 warn) | **87 problems (0 err, 87 warn)** — no new warnings |
| `expo export --platform web` | — | **exit 0** (`dist/` generated) |

**Fence proof** (`git diff --stat c18403b..HEAD`): exactly 8 files, all `.tsx` source, **CommentBubble.tsx and HeatmapLegend.tsx NOT in the diff**, zero data/auth/EXIF/RLS/points/location/handler changes.

```
 src/components/FeedbackModal.tsx    | 295 +++++++++----------  (re-indent-heavy; see delta)
 src/components/FlagDetailModal.tsx  |   7 +-
 src/components/OnboardingCards.tsx  |  63 ++++---
 src/components/SavedPlacesModal.tsx | 115 ++++++------
 src/screens/MapScreen.tsx           |  69 ++++++-
 src/screens/OnboardingModal.tsx     |  47 ++++--
 src/screens/ProfileScreen.tsx       |   8 +-
 src/screens/TasksScreen.tsx         |  43 ++++--
 8 files changed, 413 insertions(+), 234 deletions(-)
```

### Pre-flight (all confirmed)
- Branch/commit/tree matched the plan exactly at start (8 Tier-1 commits `81a08f5..c18403b`, no reverts, tree clean except untracked `qa-reports/*`).
- Baseline gate green and matching the plan's recorded numbers (1732 tests, 87 lint warnings).
- A read-only **anchor-verification pass** (8 Sonnet agents, one per fix) confirmed every anchor line still matched the plan at `c18403b` before any edit — no drift beyond line-within-range imprecision.
- Item 8 prerequisite confirmed: both inner chip scrollers (`:1481`/`:1546`) carry `style={styles.filterScroll}` (`{flexGrow:0, flexShrink:0}`).

---

## The 8 fixes

### 1 — G4: bulk bar above tab bar + measured reserve — `TasksScreen.tsx` — `9a07505`
**Done:** Added `Platform` to the RN import. Renamed the seed const `BULK_BAR_HEIGHT = 88` → `BULK_BAR_FALLBACK_HEIGHT = 94`. Added `const [bulkBarHeight, setBulkBarHeight] = useState(BULK_BAR_FALLBACK_HEIGHT)`. Put `onLayout` on the bulk-bar `View` to feed the bar's real measured height into the list reserve. Removed `bottom: 0` from the `bulkBar` style factory and applied the offset **inline, platform-gated**: `{ bottom: Platform.OS === 'web' ? 0 : tabBarHeight }` (web tab bar is in-flow; native tab bar is `position:absolute`). Changed `paddingBottom: spacing.xxl → spacing.md` (the bar no longer owns the home-indicator inset — the tab bar it now sits above does). Rewrote the stale/false comment that claimed `react-native-safe-area-context` is "not in this project yet" (the file imports `useSafeAreaInsets` at `:78`).
**Evidence:** typecheck 0 · lint clean · **full jest 1732 pass** (dynamicTypeGuard + qaMergeConsolidation intact).
**Reality-vs-plan delta:** the plan seeded the fallback at **90** using `paddingBottom 8`; the real bar uses `paddingBottom: spacing.md (12)`, so the honest derivation sums to **94**. I used 94 so the comment's arithmetic is self-consistent (avoiding exactly the kind of stale-comment rot this commit is cleaning up). The value is **non-load-bearing** — `onLayout` overwrites it on first layout.

### 2 — G7: comment bubble bleed invariant — `FlagDetailModal.tsx` — `067725e`
**Done (choice made):** kept the bleed but **shrank it to the invariant** — `commentsList.marginHorizontal: -spacing.xl (-20) → -spacing.md (-12)`.
**Why it's correct:** verified `CommentBubble.tsx` `row.paddingHorizontal = spacing.md (12)`, so `|−12| == 12 == the row's own padding`. Each row's padding now pulls its bubble flush to the card content edge, and the bubble's `borderRadius.lg (16)` corners land inside the body `ScrollView`'s clip. One-token change; `CommentBubble.tsx` untouched (it is FlagDetailModal's only consumer — the sole other reference is its unit test).
**Evidence:** typecheck 0 · lint clean.
**Side effect (accepted):** bubble `maxWidth:'80%'` now measures against a row that is ~16px narrower → bubbles get marginally narrower, which is toward the correct column.

### 3 — M4: avatar pencil badge clip — `ProfileScreen.tsx` — `75b90e6`
**Done:** deleted `overflow:'hidden'` from `avatarBtn` so the bottom-right edit badge is no longer corner-clipped; **in the same commit** added `borderRadius: radius.circle` to `avatarOverlay` (the uploading scrim relied on the parent clip to be round).
**Why it's safe:** `avatarImg` and `avatarPlaceholder` each self-clip to a circle via their own `borderRadius: radius.circle`, so the photo/initials stay round without the parent clip. The avatar-picker handler (object-URL revoke, webResilience pin) and the realtime toggle (qaMerge pin) are **not** in this commit.
**Evidence:** typecheck 0 · lint clean.

### 4 — G8: SavedPlaces scrollable list — `SavedPlacesModal.tsx` — `4feafaa`
**Done:** replaced the `<View style={listWrap}>` + `.map()` with a `FlatList` (extracted `renderItem` `useCallback` typed `({ item: place }: { item: SavedPlace })`, `keyExtractor={(item) => item.id}`, `removeClippedSubviews`, `initialNumToRender={10}`, `ItemSeparatorComponent`, `contentContainerStyle`, `keyboardShouldPersistTaps="handled"`). Deleted `listWrap`; added `listContent: { paddingVertical: spacing.tight }` + `separator: { height: spacing.sm }` (verbatim from FilterPresetsModal). The loading/empty/list ternary is preserved.
**Why it bounds+scrolls:** identical structure to the proven-in-repo `FilterPresetsModal` (same backdrop + `card { maxHeight:'85%' }`, FlatList as last child, no FlatList style prop).
**Evidence:** typecheck 0 · lint clean · **full jest 1732 pass** · full `eslint src` still **87** (0 new).
**Reality-vs-plan delta:** the plan said "copy FilterPresetsModal's FlatList pattern **exactly**," but that reference's `renderItem` uses a *partial* dep array that carries a **baseline exhaustive-deps warning**. Copying it verbatim would have added a **new** warning and broken the "baseline 87 / no new warnings" gate. I copied the FlatList structure exactly but gave `renderItem` a **complete, lint-clean dep array** `[handleJump, handleRemove, styles, color]`. (`styles` is recomputed each render here — line 68, not memoized — so the `useCallback` memo is moot anyway; the change is purely to keep lint clean.) Vertical FlatList → dynamicTypeGuard Rule 4 (horizontal-only) exempt.

### 5 — G6: heatmap legend / FAB coexistence — `MapScreen.tsx` — `fe226c5`
**Done (choice made): a flex slot, not stacking.** Wrapped the legend branch in `<View style={styles.legendSlot}>{heatmapEnabled ? <HeatmapLegend /> : null}</View>` (empty branch is now `null`, was `<View />`). New `legendSlot: { flex: 1, marginRight: spacing.sm, alignItems: 'flex-start' }`.
**Why it's correct:** confirmed `HeatmapLegend`'s container has no fixed width (`alignSelf:'flex-start'`) and its row uses `flexWrap:'wrap'`. Giving the legend a `flex:1` bounding slot beside the intrinsic-width `fabColumn` forces its wrap to engage at the slot boundary instead of overflowing into the FABs. `bottomBar`'s `alignItems:'flex-end'` keeps both bottom-aligned. `HeatmapLegend.tsx` untouched.
**Evidence:** typecheck 0 · lint clean · qaMergeConsolidation + MapScreen suites pass (`iconLabelRow` count stays 3, Report-FAB strings untouched).

### 6 — G9: FeedbackModal bound + scroll + chip floor — `FeedbackModal.tsx` — `e9e92a5`
**Done:** added `KeyboardAvoidingView`, `Platform`, `ScrollView` imports. `card` gains `maxHeight: '90%'`. A `KeyboardAvoidingView` (`behavior={Platform.OS === 'ios' ? 'padding' : undefined}` `style={{ width: '100%' }}` — verbatim AddressSearchModal recipe) nests **inside** the backdrop. The body (subtitle → invalid-email error) is wrapped in `<ScrollView style={styles.body} contentContainerStyle={styles.bodyContent} keyboardShouldPersistTaps="handled">`; `headerRow` and `actionsRow` stay **outside** the ScrollView so the ✕ and Cancel/Send never scroll away. New `body: { flexShrink: 1 }`, `bodyContent: { gap: spacing.sm, paddingBottom: spacing.tight }`. **Named rider:** `categoryChip.minHeight: 36 → 44` (touch floor).
**Critical pin preserved:** `accessibilityViewIsModal` + `testID="feedbackModal-backdrop"` stay on the **backdrop** (NOT relocated to the card as AddressSearchModal does), because `sharedModalsContext.test.tsx` asserts the backdrop node carries `accessibilityViewIsModal===true`.
**Evidence:** typecheck 0 · lint clean · **sharedModalsContext canary 9/9** · **full jest 1732 pass**. Committed nesting verified: backdrop → KAV → card → headerRow → ScrollView(body) → actionsRow → close.
**Delta note:** the large line count (163+/132−) is almost entirely the **two-level re-indentation** from the new KAV + ScrollView nesting — the file was not prettier-clean, so I re-indented by hand rather than running `prettier --write` (which would have reformatted unrelated lines). Logic unchanged.

### 7 — G10: onboarding slide scroll at large type — `OnboardingCards.tsx` + `OnboardingModal.tsx` — `7a458e9`
**Done (both files):** reduced each slide style (`cardOuter` / `card`) to `{ flex: 1 }`; moved `alignItems/justifyContent/paddingHorizontal xxxl/paddingTop xxl/paddingBottom md/gap xl` into a new `cardScrollContent` with `flexGrow: 1` (centered when short, scrollable when tall); wrapped the slide content in `<ScrollView style={styles.cardScroll} contentContainerStyle={styles.cardScrollContent} showsVerticalScrollIndicator={false} nestedScrollEnabled>`.
**Gesture safety (verified):** the pagers are horizontal `pagingEnabled` ScrollViews whose paging math reads the pager ref's `contentOffset.x` on momentum end (Cards `:192`, Modal `:90`); the inner vertical scroller reports only `.y` and cannot corrupt it — orthogonal axes. `nestedScrollEnabled` is required on Android inside the horizontal pager. OnboardingModal's SR live-region mirror (reads `index`) and its inset-aware actions row stay outside the pager.
**Evidence:** typecheck 0 · lint clean · **dynamicTypeGuard 19/19** (new scrollers vertical + pagers `pagingEnabled` → Rule 4 exempt).

### 8 — G5: map filter panel bound + scroll — `MapScreen.tsx` — `b47c9e2`
**Done (choice made): two-layer bound.**
1. `flexShrink: 1` added to `filterPanel` — the overlay is `absoluteFillObject`, so Yoga shrinks the panel to the true leftover beside its variable-height siblings (this is the real guarantee the FABs stay visible).
2. An explicit inline ceiling at the JSX site: `maxHeight: windowHeight - insets.top - insets.bottom - OVERLAY_PADDING*2 - spacing.sm - PANEL_BRACKET_ALLOWANCE` with new module consts `OVERLAY_PADDING = 16` (mirrors `styles.overlay` padding) and `PANEL_BRACKET_ALLOWANCE = 160` (documented allowance for the action bar above + FAB bottomBar below).

Added imports `useWindowDimensions` (RN) + `SafeAreaInsetsContext` (safe-area-context); `const { height: windowHeight } = useWindowDimensions()` + `const insets = React.useContext(SafeAreaInsetsContext) ?? { top:0, bottom:0, left:0, right:0 }` (non-throwing context form). The 8 filter sections (previously a Fragment) are wrapped in a vertical `<ScrollView style={styles.filterPanelScroll /* flexShrink:1 */} contentContainerStyle={styles.filterPanelScrollContent /* gap: spacing.sm */} showsVerticalScrollIndicator keyboardShouldPersistTaps="handled" nestedScrollEnabled>`; `filterHeaderRow` (collapse toggle) stays **outside** so it's always visible. Nothing inside the sections restructured; `filterRow`, the overlay's `justifyContent` (M10), and the action tray/placesRow (M11) all untouched.
**Evidence:** typecheck 0 · lint clean · full `eslint src` **87** (0 new) · **full jest 1732 pass** (iconLabelRow count stays 3; the two inner horizontal `filterScroll` chip strips keep their Pattern-B pins and nest correctly with orthogonal axes under the new vertical scroll; Report-FAB untouched).
**Known non-issue:** on an absurdly small viewport the `maxHeight` arithmetic could theoretically go non-positive, but (a) real devices never approach it (smallest ≈ 568pt tall → maxHeight ≈ 290) and (b) `flexShrink:1` is the actual bound — `maxHeight` is a secondary ceiling. Left as the plan specified.

---

## Choices made where the plan offered options
- **G7:** shrink the bleed to `-spacing.md` (align to the row-padding invariant) rather than restructuring CommentBubble's insets.
- **G6:** a `flex:1` legend slot rather than stacking the legend above the FABs.
- **G5:** two-layer `flexShrink:1` (real bound) **+** explicit `maxHeight` (ceiling), rather than a single static reserve.

## Tier-1 leftovers
None. No Tier-1 revert/deferral touched any Tier-2 item; item 8's prerequisite was already satisfied.

## Verification honesty
- **Web-confirmed / code-proven (PROVEN):** typecheck, full jest (incl. every tripwire — dynamicTypeGuard, qaMergeConsolidation, sharedModalsContext, webResilience), lint parity, `expo export --platform web` exit 0, fence proof, and a main-loop self-review of all 8 committed diffs (the two heaviest — G9 and G5 — read back from the committed blobs to confirm nesting/structure).
- **Independent adversarial review — NOT completed:** the parallel 8-agent adversarial review workflow was launched but **all agents failed on a session/rate limit** (resets 4:20am America/Vancouver), so I performed a **main-loop self-review** instead. This is less independent than a fresh-eyes pass; a re-run of `tier2-adversarial-review` after the limit resets would strengthen confidence.
- **Pixel before/afters — deferred:** dev-server visual captures for each state were not produced this pass. Many target states are auth-gated or seeded (signed-in Saved Places with 10+ items, live upload scrim, feedback on a short viewport) and several behaviors are native-only; combined with the session limit, these fold into Sky's device pass below. The machine-checkable proofs above stand.

## NEEDS-SKY-DEVICE checklist (native-suspected, stay code-proven → verify on device)
- **G4** bulk-bar blur/shadow stacking above the frosted tab bar on a real iPhone (native tab bar is `position:absolute`).
- **G9** iOS keyboard avoidance (KAV `padding`) with the feedback body scrolling and Cancel/Send pinned on a short screen.
- **G10** onboarding at **real large Dynamic Type** + **Android nested vertical-in-horizontal scroll** feel.
- **G5** filter-panel scroll feel + FAB visibility on device across rotation.
- General: the whole set at ×1.6 type on a small device (the sweep's reconstruction condition).

## What remains after this pass
- **Tier 3 / Tier 4** sweep items (adjacent-temptation fences deliberately left: M16/M17 FlagCard `compactActions` gap + `bulkBtn/sortChip` padding, M11 map action tray, M10 overlay `space-between`, placesRow 50-chip wrap, M12 preset-row crush).
- **Tasks FlagCard design elevation** (the locked pass-3 build from `2026-06-27_FlagCard_Direction.md`).
- Then, and only then, the **one** EAS TestFlight build (which doubles as the deferred on-device EXIF + VoiceOver verification).

## Deliverable / stop state
On `fix/visual-sweep` with **8 Tier-2 commits** (`9a07505`→`b47c9e2`, none reverted) stacked on the 8 Tier-1 commits. Gates green, fence held. **DID NOT merge. DID NOT push. DID NOT build** (Sky merges, Sky builds).

**Win condition:** all 12 glaring sweep bugs fixed (Tier 1's classes + these 8 singles), code-proven, fence held, stacked cleanly for Tiers 3–4 — **zero build credits spent.**
