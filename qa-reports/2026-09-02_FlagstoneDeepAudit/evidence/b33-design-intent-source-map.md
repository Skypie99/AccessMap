# Build 33 Design-Intent Source Map (premium-UI lane)

Audit date: 2026-09-02 · Read-only · Lineages compared:
- **SUBMITTED_BUILD_33** = commit `f5594171e75bc5ec92a87d0392c361601ddedfba` (not in main)
- **CURRENT_MAIN** = worktree `/Users/skypie/AccessMap-deep-audit-20260902` @ `70b52a30`

Method: for each intended design-language statement, locate the implementing source in Build 33 (`git show f5594171:<path>`), then diff against the worktree file. Quotes are ≤4 lines. Status vocabulary for CURRENT_MAIN: **same** (identical implementing lines), **older** (a prior/different implementation exists), **absent** (no implementation).

_(File written incrementally; sections below are appended as evidence is gathered.)_

## Scope facts established first

- Shared primitives byte-identical in both lineages (`git diff --stat 70b52a30 f5594171 -- <file>` empty): `src/theme.ts`, `src/theme/ThemeContext.tsx`, `src/components/ui/ScreenHeader.tsx`, `src/components/ui/PressableScale.tsx`, `src/components/SearchInputRow.tsx`, `src/lib/accessibility.ts`, `src/components/ui/AppText.tsx`, `src/components/ui/TypeBlock.tsx`, `src/lib/a11yText.ts`, `src/components/SeverityDisc.tsx`, `src/components/ui/HeaderActions.tsx`, `src/components/ui/Input.tsx`. `src/components/ui/GlassSurface.tsx` differs by one comment line only (HeatmapLegend mention removed).
- Build-33-only source files (absent from CURRENT_MAIN): `src/navigation/TabBarGlass.tsx`, `src/navigation/tabBarGeometry.ts`, `src/hooks/useFocusedInputScroll.ts` (plus non-UI `postSignInPushGate.ts`, `accountDeletion*.ts`, `adminReports.ts`).
- CURRENT_MAIN-only UI file: `src/components/HeatmapLegend.tsx` (deleted in Build 33 Wave 2).
- **No `glassMode.ts` exists in either lineage.** The Reduce-Transparency switch lives in `src/lib/accessibility.ts:526-540` (`useReduceTransparency`) and is consumed by `GlassSurface.tsx:207-240`, `TabBarGlass.tsx` (B33) / inline `TabBarGlass` in `RootNavigator.tsx` (MAIN), `HamburgerDrawer.tsx`, `FeedbackModal.tsx`, `TasksScreen.tsx`.
- **No `keyboardAppearance` prop anywhere in either lineage** (`git grep keyboardAppearance` empty for both).
- Build 33 polish lineage (commit subjects, newest first): `76ee355 fix(final-polish)`, `8df6082 build 32 stabilization`, `441e2b1/1439854/93caa71 close visual a11y regressions`, `a1b40c2 address search above keyboard`, `92e0c2d visual freeze layout`, `ba0f2ca scale report severity at xxxl`, `da1570a scale remaining report modal controls`, `1ceeaa6 activity feed xxxl`, `f94e7a1 recover map overlay space`, `70de4e0 contain map heat notices at large type`, `2690d44 VP1 fix3 grounded nav`, `cdf0b69 VP1 fix2 selected chip + legend`, `e1689d5 VP1 selection wash`.

## Source map

| Intent | Build 33 evidence (file:line + quote) | CURRENT_MAIN state (same / older / absent + file:line) | Sim check needed (Y/N) + what to look for |
|---|---|---|---|
| **(a) Bottom navigation hugs the physical bottom** | `src/navigation/TabBarGlass.tsx:14-17` — "VP1 fix3: the capsule now spans the FULL bar container — the 68pt control band AND the bottom safe-area strip below it — so the material is grounded flush to the device edge instead of floating above a transparent gap." Geometry: `TabBarGlass.tsx:58-61` `capsule: { position: 'absolute', top: 0, bottom: 0, …}`; `src/navigation/RootNavigator.tsx:410-411` `height: FLOATING_TAB_BAR_CAPSULE_HEIGHT + insets.bottom, paddingBottom: FLOATING_TAB_BAR_CONTROL_BOTTOM_PADDING + insets.bottom`. | **older** — `src/navigation/RootNavigator.tsx:155-166` inline `TabBarGlass()` is a full-width `StyleSheet.absoluteFill` BlurView + `tabBarGlassFloor`; `:359-360` `height: 68 + insets.bottom, paddingBottom: 8 + insets.bottom`. It reaches the bottom edge, but as a legacy full-band bar, not the grounded capsule. | **Y** — on iOS (normal transparency) confirm no transparent strip between capsule bottom and home indicator; confirm labels sit inside the top 68pt band, not under the indicator. |
| **(b) Three primary nav actions do not look like floating islands** | ONE capsule wraps all three tabs: `TabBarGlass.tsx:41-53` renders a single `View style={tabBarGlassStyles.capsule}` containing one `BlurView`, one `LinearGradient`, one specular hairline; `src/navigation/tabBarGeometry.ts:9-10` `FLOATING_TAB_BAR_CAPSULE_SIDE_INSET = spacing.md; …RADIUS = radius.xl`; tabs are `flex:1` segments inside it (`TabBarButton.tsx:112` `segment: { flex: 1, position: 'relative' }`). | **older** — MAIN has no capsule at all (single full-width bar, `RootNavigator.tsx:155-166`); no islands, but not the intended material. | **Y** — verify one continuous capsule (not three pills), side inset ≈16pt, radius consistent with sheet corners. |
| **(d) Crystal / liquid-glass divider between tabs** | `src/navigation/TabBarButton.tsx:99-106` `{showDivider ? (<View pointerEvents="none" style={[styles.divider, dividerInk ? { borderRightColor: dividerInk } : null]} {...decorativeProps} testID="tab-segment-divider" />) : null}`; `:114-121` `divider: { position:'absolute', top:12, bottom:12, right:0, width:0, borderRightWidth: StyleSheet.hairlineWidth }`; wired at `RootNavigator.tsx:385-390` `dividerInk={color.navBorder} … showDivider={route.name === 'Home' \|\| route.name === 'Tasks'}`. | **absent** — `src/navigation/TabBarButton.tsx:36-47` is a bare `PlatformPressable` wrapper; `RootNavigator.tsx:349` `tabBarButton: (props) => <TabBarButton {...props} />` passes no divider props. | **Y** — two hairline dividers (after Home, after Tasks) visible in light and dark; not announced by VoiceOver. |
| **(e) Selected tab subtly illuminated with Flagstone blue, readable in both themes** | Wash: `TabBarButton.tsx:79-86` `{selected ? (<View pointerEvents="none" style={[styles.selectedFill, selectedFill ? { backgroundColor: selectedFill } : null]} … testID="tab-segment-fill" />) : null}`; `:128-136` fixed 68pt chip (`left:'50%', marginLeft:-(…/2), width: FLOATING_TAB_BAR_SELECTED_FILL_WIDTH, borderRadius: radius.lg`); token `RootNavigator.tsx:388` `selectedFill={color.glassSelectedTint}`. Ink: `TabBarGlass.tsx:77-85` `liquidTabInk … active: usesLiquidTabBar ? color.scheme === 'light' ? color.brandTextAlt : color.inkSelect : color.tabBarActiveTint, inactive: … color.textStrong`; consumed `RootNavigator.tsx:288, 379-380`. Design note `TabBarButton.tsx:51-56` (third signal — 2pt underline — removed in VP1 fix3). | **absent** — MAIN selection is the OS tint only: `RootNavigator.tsx:344-345` `tabBarActiveTintColor: color.tabBarActiveTint, tabBarInactiveTintColor: color.tabBarInactiveTint`; no fill, no per-scheme ink fork. | **Y** — selected chip visible but restrained in light AND dark; active label ≥4.5:1 over map detail beneath the crystal capsule; chip width stays a chip (not a band) on wide devices. |
| **(f) Separators optically crisp** | Hairlines everywhere on the nav: `TabBarButton.tsx:120` `borderRightWidth: StyleSheet.hairlineWidth`; `TabBarGlass.tsx:66` capsule `borderWidth: StyleSheet.hairlineWidth`; `:68-74` specular `height: StyleSheet.hairlineWidth`; `RootNavigator.tsx:396-399` `// The native edge lives on the capsule; a full-width line would turn the transparent safe-area remainder into a second visual bar. borderTopWidth: Platform.OS === 'web' \|\| !usesLiquidTabBar ? 1 : 0`. Divider ink token `src/theme.ts:294` `navBorder: 'rgba(22,33,58,0.10)'` (identical in both). | **older** — `RootNavigator.tsx:354-355` `borderTopWidth: 1, borderTopColor: color.navBorder` (1pt, not hairline); no inter-tab separators. Tokens (`theme.ts:286,294`) same. | **Y** — check at 3x that dividers/edges are 1px, not 3px; no doubled line at the safe-area strip. |
| **(g) Liquid-glass overlays share one transparency system** | Tokens identical in both: `src/theme.ts:636-641` `export const glass = { … intensity: { row: 12, chrome: 24, banner: 12, bulk: … 24 } }`; material tokens `theme.ts:316-402` (`glassRowFloor`, `glassChromeFloor`, `glassBulkFloor`, `glassMapCrystal0/1`, `glassChipFill`, `glassSelectedTint`, …); variants in `src/components/ui/GlassSurface.tsx` (`variant="row" \| "chrome" \| "banner" \| "bulk"`, legacy path). B33 extends the same crystal tokens to the nav: `TabBarGlass.tsx:44-52` `colors={[color.glassMapCrystal0, color.glassMapCrystal1]} … backgroundColor: color.glassRowSpecular` — same pair MapScreen's command bar/FABs use (`MapScreen.tsx:1968, 2880, 2928`). | **same** for tokens + GlassSurface (diff empty / 1 comment line). Tab bar is the outlier in MAIN: `RootNavigator.tsx:164` uses `tabBarGlassFloor` (legacy 0.82 floor) rather than the crystal pair. | **Y** — compare capsule vs map command bar vs Legend pill: same tint family; sheets (bulk) denser than chrome/crystal; under Reduce Transparency everything opaque. |
| **(m) Expanded Watch Flags / Leaderboard / Legend use available vertical space** | Primitive: `src/components/ui/Sheet.tsx:201-207` `presentation?: 'standard' \| 'expanded'` ("fills from the safe area's top edge (plus the smallest shared gutter) to the existing bottom-safe-area pad"); `:320-321` `const expanded = presentation === 'expanded'; const expandedTopMargin = insets.top + spacing.sm;`; `:491-503` `pullExpanded: { width:'100%', flexGrow:1 }, kavExpanded: { maxHeight:'100%', flexGrow:1 }, cardExpanded/cardShadowExpanded: { maxHeight:'100%', flexShrink:1 }`. Opt-ins (`git grep presentation="expanded"`): `MyWatchedModal.tsx:373`, `LeaderboardScreen.tsx:367`, `MyReportsModal.tsx:316`, `ActivityFeedModal.tsx:278`, `AchievementsModal.tsx:181`, `AddressSearchModal.tsx:212`, `ChangelogModal.tsx:131`, `FeedbackModal.tsx:215`, `HelpModal.tsx:117`, `MyFeedbackModal.tsx:139`, `NotificationPrefsModal.tsx:182`, `ReportContentModal.tsx:305`, `SavedPlacesModal.tsx:287`, `StatusHistoryModal.tsx:177`, `MapScreen.tsx:2211` (Filter sheet), `TasksScreen.tsx:1199` (Filter & Sort). Legend is hand-built but mirrors it: `src/screens/LegendModal.tsx:397, 405-409` `pullExpanded: { width:'100%', flexGrow:1 } … cardShell: { …, maxHeight:'100%', flexGrow:1, flexShrink:1 }` with `marginTop: insets.top + spacing.sm` at `:117`. Leaderboard list owns the viewport: `LeaderboardScreen.tsx:503` `list: { flexGrow: 1, flexShrink: 1, minHeight: 0 }`. | **absent** — MAIN `Sheet.tsx` has no `presentation` prop (diff shows it added); no consumer passes it. Watched: MAIN `MyWatchedModal.tsx` `kav: { minHeight:'55%', maxHeight:'85%' }` (removed in B33 diff); Leaderboard: MAIN `shrinkStyle={styles.cap}` `cap: { maxHeight:'90%' }`; Legend: MAIN `cardShell: { maxHeight: '85%' }`. | **Y** — Watched Flags / Leaderboard / Legend top edge sits at `insets.top + 8`; no dead band above short lists; Legend's last row ("Anonymous report") reachable above the nav. |
| **(o) Close controls clear + swipe-down has an accessible alternative** | Shared close: `Sheet.tsx:171-185` labelled `Pressable … accessibilityLabel={closeLabel ?? \`Close ${title}\`}` on a 44×44 circle (`:529-539`); escape scrub `Sheet.tsx:415-418` `onAccessibilityEscape={onClose}`. Gesture contract: `src/components/ui/SheetPull.tsx:34-39` "It never becomes the only way out. Every adopting sheet keeps its visible Close/Cancel … Under VoiceOver the gesture stands aside entirely". Grabber `Sheet.tsx:110-119` `SheetGrabber` (visible `inkGlassMuted` pill, hidden from AT). Legend moved its Close to a top-right X: `LegendModal.tsx:181-190` `accessibilityLabel="Close legend" hitSlop={8}` on a 32pt circle (`:420-427`, "32 + hitSlop 8 = an effective 48pt target"). B33 also adds the one-gesture-one-close latch: `SheetPull.tsx:257-261` `if (dismissingRef.current) return; dismissingRef.current = true;` and scrim/slide lifecycle `SheetPull.tsx:132-166` `useSheetPullDismissLifecycle`. | **same** for the shared `Sheet` close button, escape, grabber and the SheetPull contract text (MAIN `Sheet.tsx` / `SheetPull.tsx` carry the identical lines minus the lifecycle additions). **older** for Legend: MAIN `LegendModal.tsx` renders a full-width bottom "Close" button (`closeBtn: { … minHeight: 44 }`, removed in the B33 diff) and no top-right X. **absent**: `useSheetPullDismissLifecycle`, `SheetPullHandle.resetAfterDismiss`, `onDismissStart`, `visible` prop. | **Y** — every sheet shows a labelled X; one real swipe → exactly one close, scrim fades with the card, no flash-back on reopen (B33 lifecycle); Legend X 32pt visual / 48pt target. |
| **(p) Details reaches its intended bottom boundary / no clipped sheet content** | `src/components/FlagDetailModal.tsx:1480-1487` `style={[styles.card, { marginTop: insets.top + spacing.sm, paddingBottom: Math.max(spacing.xl, insets.bottom) }]}`; `:2598-2604` `card: { …, maxHeight: '100%', flexGrow: 1, flexShrink: 1 }, pullExpanded: { width: '100%', flexGrow: 1 }`; `:1460-1468` `<SheetPull ref={pullRef} … style={styles.pullExpanded} visible={visible}>`; body scroller is RNGH `ScrollView` (`:19-22`) so pull + scroll coexist. Guarded by `src/screens/__tests__/Wave2ScreenGeometry.test.ts:20-28`. | **older** — MAIN `FlagDetailModal.tsx` card is `maxHeight: '90%'` with no top margin, plain RN `ScrollView`, `useReducedMotion`-driven `animationType` (all replaced in the diff). | **Y** — Details opens to `insets.top + 8`; pinned verbs/footer never cover the last body row at XXXL; bottom pad ≥ safe inset. |
| **(t) Dark-mode tokens + Reduce Transparency fallback** | Dark mirror: `src/theme/ThemeContext.tsx:24-28` "Dark palette — same token names as lightColor, dark-mode values … `const darkColor = {`" with nav tokens `:147-155` ("Navigation chrome — always-dark … `tabBarBlurTint: 'dark'`"); light source `src/theme.ts:130` `export const color = {`, `:308` `scheme: 'light' as 'light' \| 'dark'`. RT fallback (no `glassMode.ts` — see Scope facts): `src/lib/accessibility.ts:526-540` `useReduceTransparency()` polling `AccessibilityInfo.isReduceTransparencyEnabled` + `reduceTransparencyChanged`; `GlassSurface.tsx:207-240` `const material = reduceTransparency ? 'opaque' : …`; nav: `TabBarGlass.tsx:27-31` `if (reduceTransparency) { // Preserve the established full-band opaque fallback … return <View style={[StyleSheet.absoluteFill, { backgroundColor: color.tabBarBg }]} />; }`; ink fork `TabBarGlass.tsx:78-83` falls back to `tabBarActiveTint/InactiveTint` under RT. | **same** for `ThemeContext.tsx`, `theme.ts`, `lib/accessibility.ts`, `GlassSurface.tsx` (byte-identical). Nav RT branch **same behaviour, older location**: `RootNavigator.tsx:158-160`. | **Y** — toggle Reduce Transparency: capsule becomes full-band opaque `tabBarBg`, tab inks revert to `tabBarActiveTint`; dark mode: capsule/gradient read as dark crystal, selected chip still visible. |
