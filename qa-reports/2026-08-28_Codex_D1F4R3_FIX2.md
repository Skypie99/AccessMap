# C-D1F4R3-FIX2 — local source-repair QA handoff

**Date:** 2026-08-28
**Scope:** Only the two independently established W-D1F4R3-FINAL source blockers: review resume/replay truthfulness and `public.flags` direct-DELETE authority containment.
**Outcome:** Source repair is complete locally and requires fresh independent acceptance. This is not a deployed, catalog, Storage, Auth, or physical-runtime verdict.

## What changed

### 1. Review resume/replay repair

- Added forward-only migration `supabase/migrations/20260828020000_d1f4r3_fix2_review_replay_and_flag_delete.sql`.
- Replaced only the review resolver function with a `jsonb` durable-state response. It preserves the existing service-role-only grant, review-secret route boundary, exact object evidence rules, foreign-object preservation guard, upload-intent handling, audit write, and terminal redaction behavior.
- The resolver maps every valid stored `review_resume_from` value without collapsing it to `CLEANING`:
  - `LOCK_DRAIN` -> `REQUESTED`
  - `CLEANING` -> `CLEANING`
  - `VERIFYING` -> `VERIFYING`
  - `AUTH_DELETE` -> `READY_FOR_AUTH_DELETE`
  - `AUTH_RECONCILIATION` -> `RETRY_REQUIRED` with `resume_from = AUTH_RECONCILIATION`
- It checks a resolved replay before requiring `subject_id`, so an identical replay after `COMPLETE` returns `{ status: "complete", operation_status: "COMPLETE" }` after the subject has been redacted. An action conflict or unrecognized durable state fails closed.
- Added `supabase/functions/_shared/accountDeletionReviewCore.ts` and updated the narrow review Edge route to validate and return the structured durable status; malformed RPC results remain a 503 rather than a false success.

### 2. Direct `public.flags` DELETE containment

- The same forward-only migration revokes `DELETE` on `public.flags` from `public`, `anon`, and `authenticated`; it retains `SELECT`, `INSERT`, and `UPDATE` for authenticated users and retains service-role authority for the canonical server route.
- It drops all known client-authorizing `DELETE`/`ALL` policies: `flags_user_scoped`, `flags delete own`, and `admin delete any flag`.
- Updated `supabase/schema.sql` with the same idempotent baseline containment so a future baseline replay cannot reintroduce the owner DELETE policy.
- The existing `delete-flag` Edge function remains the only ordinary deletion path: it verifies the bearer-derived caller, derives the exact plan server-side, verifies Storage ownership, removes exact keys, proves exact absence, and only then finalizes the relational delete.

## Files changed

- `supabase/migrations/20260828020000_d1f4r3_fix2_review_replay_and_flag_delete.sql`
- `supabase/functions/_shared/accountDeletionReviewCore.ts`
- `supabase/functions/account-deletion-review/index.ts`
- `supabase/schema.sql`
- `supabase/tests/d1f4r3_fix2_flags_delete_rls.test.sql`
- `src/__tests__/d1f4r3Fix2ReviewReplay.test.ts`
- `src/__tests__/d1f4r3SourceClosure.guard.test.ts`

## Branch + SHA

- Branch: `codex/d1f4r3-fix2`
- Integration base: `b4c11a3e4eda646fa99313165f1527972e935adb` (`docs(qa): record d1f4 r3 local gate verification`)
- Source implementation: `d0da58321d0bf17508e39ae95907ec38f82a0736` (`fix(deletion): repair review replay and flag delete authority`)
- At the time of source commit, `main...origin/main` was `0 0`; the primary checkout was not changed.

## Self-attack results

### Review replay

- Passed the actual production worker core through every legal repair destination. `LOCK_DRAIN` reacquires Transaction B and proves the writer drain before any Storage work; `CLEANING` resumes after a fresh drain assertion; `VERIFYING` skips Storage/purge; `AUTH_DELETE` skips cleanup; and `AUTH_RECONCILIATION` stays Auth-last.
- Verified source order: the corrupt-final-resume guard occurs before `review_items.resolution` is changed.
- Verified replay logic precedes the `subject_id` guard, allows an identical replay to report later durable progress or `COMPLETE`, and rejects a conflicting action.
- Verified the Edge route validates the RPC shape and cannot translate stale `resolved_item` text or an invalid status/state pair into success.

### Direct DELETE authority

- Source scan found no application client path chaining `from('flags').delete()` outside migrations/tests.
- Verified that the client invokes `delete-flag` with only `{ flagId }`, while the Edge route derives the caller from `caller.auth.getUser()`.
- Verified no `[functions.delete-flag] verify_jwt = false` configuration and no service-role key reference under `src`.
- Added staging-only pgTAP assertions for effective `anon`/`authenticated` denial, retained service-role authority, absence of `DELETE`/`ALL` policies, owner/admin direct-DML denial (`42501`), and retained authenticated read/create/update table grants.

## Gates actually run

All commands below ran locally in `/Users/skypie/AccessMap-codex/d1f4r3-fix2` against the source implementation before the documentation-only handoff commit.

| Gate | Result |
| --- | --- |
| `node_modules/.bin/jest --ci --watchman=false -w 3 src/__tests__/d1f4r3Fix2ReviewReplay.test.ts src/__tests__/d1f4r3SourceClosure.guard.test.ts src/__tests__/d1f4r2Adversarial.test.ts src/lib/__tests__/sr050DeleteFlagPhotos.test.ts src/lib/__tests__/d1f4r3CanonicalReportDelete.test.ts src/lib/__tests__/flags.supabase.test.ts` | PASS — 6 suites, 88 tests |
| D1 focused Jest gate including Option A and async account-deletion guards | PASS — 8 suites, 162 tests |
| `npm run typecheck` | PASS — `tsc --noEmit` |
| `npm run lint` | PASS — 0 errors; 90 existing warnings |
| `git diff --check` | PASS |
| Full canonical `node_modules/.bin/jest --ci --watchman=false -w 3` | PASS — 262 suites passed; 3,818 tests passed; 32 todo; 3,850 total; 17.186 s |

The full Jest process emitted its non-failing existing teardown warning about a worker forced to exit gracefully after completion. It did not change the zero exit status or passing totals; open-handle diagnosis is outside this narrowly scoped repair.

## Static and privacy checks

- Frozen migration SHA-256 values are unchanged:
  - `2026-08-27_d1sa_deployed_security_containment.sql`: `d131d76929bae33051b7a3fcacb8852d58b38fda951f1c57b95aac227e85c68d`
  - `2026-08-27_d1_option_a_account_deletion.sql`: `a01142702609c2c32cce252f979e2ffc3ee6aa90b91030332fe1ceb287c83e01`
- No credential files, `.env` files, credentials, remote catalog, Storage, Auth, or deployment controls were accessed or changed.
- No migration was applied; no `supabase db push`, migration repair, deployment, EAS, TestFlight, push, or merge was run.
- The local Supabase CLI was unavailable (`supabase --version` / migration helper exited 137), so the timestamped forward-only migration was created manually and is covered by source tests. No local Deno, PostgreSQL, Docker, or gitleaks binary was available.

## Remaining staging-only proof

The following are deliberately **NOT VERIFIED**, not failures and not release approval:

- Applying the migration to an authorized non-production catalog, confirming effective grants and RLS policy state, and executing `supabase/tests/d1f4r3_fix2_flags_delete_rls.test.sql`.
- Actual authenticated owner and administrator Data API denial against that catalog, followed by successful canonical `delete-flag` behavior and retained create/owner-edit/community-status flows.
- Database-function compilation and RPC JSON serialization in the real Supabase/PostgREST version.
- Actual review-route invocation with its deployment-provided secret, replay after an interrupted HTTP response, and durable operation observation during the worker race window.
- Storage, Auth, concurrent writer drain, and remote migration-ledger behavior.

## What's left

Fresh independent source acceptance is required. The reviewer should re-derive both original blockers from `d0da583` rather than relying on this report, then decide separately whether the staging-only migration/catalog evidence may be authorized. No source blocker remains in this local candidate.

## DECISIONS FOR SKY

1. **Decision:** Whether to authorize a fresh independent source acceptance of `d0da583` before any staging action.
   **Recommendation:** Authorize the independent source re-derivation first.
   **Why:** The candidate changes the exact P0 resume mapping and direct-DELETE authority boundaries, so independent adversarial review is the required closure evidence.
   **Alternative:** Proceed directly to staging verification.
   **Impact:** Skipping independent source acceptance would leave the controlling W-D1F4R3-FINAL process incomplete even if staging checks later pass.

2. **Decision:** Whether to separately authorize non-production migration application and pgTAP/catalog verification after fresh source acceptance.
   **Recommendation:** Keep it separate and require explicit authorization.
   **Why:** This task was intentionally source-only and made no remote changes.
   **Alternative:** Treat the local source gates as deployed proof.
   **Impact:** That would overstate what Jest/static analysis can prove about grants, RLS, PostgREST, Storage, or Edge runtime behavior.
