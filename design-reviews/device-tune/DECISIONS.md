# Device-Tune Train — DECISIONS

Convention: mirrors the R2 build-plan ledger. Untracked working-tree governance (never `git clean -fd`). Sky's picks land in §A; phase handshakes in §P.

## §S Standing decisions

- **S-1 · String fence (BP16 seam):** the R2 BP16 copy-gate is STILL PENDING and owns all drawer-surface strings, including the `settings-retitle` taste row (R2 `build-plan/DECISIONS.md:154`, S-8 at :14, O-5 at :25). **No device-tune phase ships a user-facing string change on Settings / Resources / How To Help / About.** Byte-identical reuse of already-shipped strings is permitted (it introduces no new copy and cannot collide with a BP16 pick).
- **S-2 · Function/material split:** Phase 1 = function only. No glass, no ink, no floor, no token change → no arbiter run required (R2 discipline: "NO arbiter — no ink/color/floor change"). Phase 2 owns material.
- **S-3 · No paid builds:** simulator proof via local `npx expo run:ios` only. EAS builds are Sky-initiated.
- **S-4 · The always-dark drawer ratification is SUPERSEDED (Phase 2, 2026-07-25).** The dark drawer was a *recorded design*, not an accident: R2 ratified it as an "always-dark navigation rail" (M-45 — `design-reviews/r2-audit/04_material_migration_spec.md:131-146`; R2 `DECISIONS.md` S-5; `GLASS.md:188-190`), and `DESIGN.md:257-259` lists "the always-dark nav chrome" among the fixed-background exceptions. **Sky's D2 phase prompt (2026-07-25) supersedes all of it** — Sky's intent outranks a recorded ratification (Const. authority order). Phase 2 therefore rebinds the drawer to scheme-bound tokens and **amends the living law docs** (`GLASS.md` §8 drawer entry, `DESIGN.md` §7 fixed-background list, the stale `theme.ts:168-176` comment) so the next audit reads the new law instead of reverting the fix. **The historical R2 record is NOT edited** — `04_material_migration_spec.md` and R2's `DECISIONS.md` keep saying what was true when written; this file is where the supersession lives.
  - Scope of the supersession: **appearance beyond the plain scheme rebind is PROVISIONAL** pending the D2b mockup gate. C2 ships the drawer at the *incumbent tier* (engineered chrome-Lite, zero blur-budget cost). If Sky picks live glass at the gate, C4 applies it; if she picks A or declines to pick, the drawer stays correct as built.
- **S-5 · "Select multiple" joins the search row. Pre-decided by Sky in the D3/D4 phase prompt (2026-07-26) — NOT a fork, not gated.** The control leaves its own dedicated row (`selectEntryRow`, TasksScreen.tsx:916-937 at base) and mounts inside the search row after the clear button; the full height of the vacated row returns to the list. Constraints carried verbatim: the target stays **≥44pt**, the label stays **byte-identical** (`Select multiple`), and it stays **reachable in VoiceOver order**. This ships pre-gate as **D3-C1** and applies to every D3 candidate — each candidate's measured numbers are quoted *on top of* C1, never instead of it.

## §F Findings of record (Phase 1 STEP 0)

- **F-1 · R2 coordination seam (verified read-only 2026-07-24/25):** the R2 train is **fully merged** — `main` @ `d43f867` is byte-identical to `r2/bp17-hygiene-deploy`. R2's `DECISIONS.md` §P is **stale**: all 17 phase entries still read "NOT merged/pushed/built — STOPPED on `<branch>` (Sky's hands)" (e.g. :52, :54), which git disproves. **No unmerged R2 branch claims the drawer or the Resources / How To Help / About / Settings code** — R2's drawer work was BP15 (T12+T10), closed and merged (`94f4171`, `9fb637d`, `a5535f8`, `c0ee449`). `r2/bp11-press-vocab` @ `8acb184` is a content-duplicate of `eafd20e` (already in main); nothing unique.
- **F-2 · bp12 rebase superseded:** the documented `git rebase --onto 7de1f34 eafd20e r2/bp12-status-ledge` (R2 DECISIONS.md:48/:49/:138) is **unsafe and moot** — DEPLOY-CHECKLIST §1 step 3 records `eafd20e` is in mainline and a naïve rebase would DROP content. Never run it. Re-verified 2026-07-24 (`git branch --contains eafd20e` includes `main`).
- **F-3 · Pending critical fix on a sibling branch:** `fix/photo-privacy-sanitize` @ `64342e1` (CRITICAL — photo/avatar uploads dead on iOS device) is unmerged, off the same `main` @ `d43f867`. This train does not touch it; **merge order is Sky's call** (no conflict expected — different files).
- **F-4 · Doors-to-empty-rooms fork: MOOT.** All four destinations have real, shipped screens with real content (`ResourcesScreen.tsx` 6 info cards + honesty footnote, `HowToHelpScreen.tsx` 4 steps, `AboutScreen.tsx` version + 5 sections, `SettingsScreen.tsx` full surface). No hollow door to hide → no per-destination Sky pick required. FYI only: Resources' cards carry Sky's own shipped `TODO(Sky)` for real links (ResourcesScreen.tsx:9-10) — link content remains hers; nothing invented here.
- **F-5 · D1 mechanism (evidence):** static wiring is fully intact (census in the phase plan/report). The device failure is a **runtime iOS dismiss/present collision introduced by BP15's exit latch**: the drawer Modal now stays presented until t≈180 ms (`rendered` latch, HamburgerDrawer.tsx:80/:118-125) while the sub-screen Modal presents at t≈180 ms (`setTimeout(..., motion.duration.base)`, :132-149; base=180, theme.ts:478) — same-frame native transactions; iOS drops the presentation or tears the sub-screen down with the drawer's VC. Pre-BP15 there was ~220 ms of clearance (dismissal began at tap; present at t=220). Reduce-motion path collides the same way (snap + `setTimeout(...,0)`). Web cannot reproduce (no UIKit transactions) — why every web gate passed. Settings hypothesis set (S1 collateral of wedged sub-screen state / S2 poisoned-session appearance / S3 independent defect) resolves on simulator evidence before any Settings-specific change.

## §P Phase tips + handshake record (append per phase: base SHA → tip SHA, date, gate one-liner)

- **Phase 1 (D1, `devicetune/1-drawer-function`): base `d43f867` → tip `067864e` · 2026-07-25 · typecheck 0 · lint 0 err/77 warn (baseline exact) · jest 2074 pass / 148 suites, the 2 documented load-flakes only (MyReportsModal + StatusHistoryModal — 10/10 green isolated in 6 s vs 260 s timeouts under load) · 25 drawer tests green · falsification: guards fail 7/16 against pre-fix source. D1 CLOSED pending Sky's device pass. NOT merged/pushed/built — STOPPED on `devicetune/1-drawer-function` (Sky's hands). Report: `qa-reports/2026-07-25_DeviceTune_Phase1_Drawer.md`.**
  - Two mechanisms, both shipped in BP15: (1) the sub-screen present raced the exit latch's Modal dismissal (same-frame UIKit transactions → iOS drops the presentation); (2) an interrupted exit stranded `rendered=true` — an invisible full-screen backdrop that ate every tap app-wide, which explains Settings reading "dead" too. Fixes: event-driven handoff on `onDismiss` (iOS) / latch-release (elsewhere), and release gated on live open intent (`openRef`) instead of the stale closure + `finished`.
  - **F-6 · A guard test had pinned the bug.** `HamburgerDrawer.exitLatch.test.tsx` asserted "interrupted exit ⇒ Modal stays mounted" while never reopening — encoding the wedge as intended behavior. Corrected + a true-reopen case added. Lesson for the train: a green suite is not proof the contract is right.
  - **F-8 · Why `271e8ec` broke light mode — and why this rebind does not (Phase 2 STEP 0, verified read-only 2026-07-25).** The shipped comments at base `067864e` (`HamburgerDrawer.tsx:360-363`, `:387-392`, `:436-438`, `:499`) encode the revert as law: *"theme tokens would go invisible in light mode."* That is a correct description of what happened and a **wrong diagnosis of why**. `271e8ec` was a **partial binding** — it tokenized three *inks* while leaving the panel a hardcoded near-black. Light-mode inks (`#222`-family) then landed on a still-dark surface, which is exactly "invisible". The failure was incoherence between surface and ink, not the tokens themselves. Phase 2 binds **surfaces AND inks together**, which is why the identical token set now passes the arbiter 32/32.
- **F-9 · The rebind is byte-stable in dark mode (verified token-by-token).** The drawer's dark literals are byte-identical to the dark glass tokens, so dark mode barely moves: panel `rgba(13,18,32,0.94)` === `glassChromeLite0` dark (`ThemeContext.tsx:195`); right edge `rgba(168,192,224,0.18)` === `glassChromeEdge` (`:162`); inner lip `rgba(168,192,224,0.14)` === `glassChromeLip` (`:163`); wordmark/label `#f5f5f5` === `textStrong` (`:42`); row icons `#4E89EF` === `brand` (`:51`). **Recorded dark deltas (5, all small and all improvements):** scrim `0.5 → 0.6` (`color.scrim`); muted label `rgba(255,255,255,0.48)` @400 → `inkGlassMuted #B8BEC9` @500 (a knife-edge 4.97:1 becomes 9.57:1, and satisfies the GLASS §2 type law "text on glass ≥500"); close-button + pressed-row wash `0.08/0.06 → glassNeutralBtn 0.10`; header border + divider → `color.border` / `color.divider`; muted icon/chevron/X `rgba(255,255,255,0.7)` → `inkGlassMuted` (≈byte).
- **F-10 · The two-blues split (found during the rebind, fixed by it).** `DrawerItem` hardcoded `#4E89EF` — the **dark**-palette brand — for row icons (`:363`), while `logoMini` twelve lines away already used `color.brand` (`:428`, the one place the drawer read the theme at all). In light mode the drawer therefore rendered **two different brand blues at once** (`#1466E0` tile vs `#4E89EF` icons). Binding the icons to `color.brand` retires it; no separate fix needed.
- **F-11 · Drawer strings and the S-1 fence.** The footer string `AccessMap · Made with ♥ in Canada` is the phase's only string change and it is a **removal**, pre-recorded by Sky in the D2 phase prompt (logged in §A). Nothing else on Settings / Resources / How To Help / About moves. The a11y additions in C3 are non-visual (`accessible={false}` on the scrim, `accessibilityRole="header"` on the wordmark) and introduce no user-facing copy, so S-1 stays intact.
- **F-12 · The disputed jest baseline is RESOLVED — both prior records were wrong.** Phase 1 recorded `2074 pass / 148 suites`; another record said 2072. Measured directly at `067864e` on a quiet machine (2026-07-25): **148 suites · 2076 passed · 84 todo · 2160 total**. At the Phase-2 tip: **151 suites · 2113 passed · 84 todo · 2197 total** — a delta of exactly **+3 suites / +37 tests**, which is exactly the three suites this phase added (`HamburgerDrawer.material.guard` 16 · `HamburgerDrawer.scheme` 6 · `HamburgerDrawer.focus` 15). Conservation is exact; no pre-existing test changed state. Use **2076** as the base number from here on.
- **F-13 · The Phase-1 "load-flake" list is a machine-state artifact, not a suite defect.** MyReportsModal / StatusHistoryModal / ReportFlagModal / flagsStoreSwr all passed in-run at both base and tip with zero retries, in a **64-second** full run. During the first half of this phase the same suites were unrunnable: the machine was at load 84–98 with **16.2 GB of 17.4 GB swap consumed and ~69 MB free RAM**, because a *different* task's iOS Release build (worktree `.claude/worktrees/angry-kepler-721d75`, the `fmt`/Xcode-26.6 fix) had been running 2.5 h with 16 concurrent `clang` jobs. Under that pressure `tsc --noEmit` took **8 minutes** and `eslint` **50**; afterwards they take ~40 s and ~60 s. **Before recording a flake, check `uptime` and `sysctl vm.swapusage`** — and never bump `--maxWorkers` to "fix" it.
- **F-14 · A green jest run did not stop C3 shipping a dead drawer on web — the browser capture did.** C3's `useDrawerTrigger.register()` runs inside the hamburger's `onPress`, immediately before `setOpen(true)`. On react-native-web `findNodeHandle` **throws** (`"findNodeHandle is not supported on web"`), so the press handler aborted and **the drawer never opened at all on the web build** — the entire menu was inert. All 67 tests stayed green: `react-test-renderer` implements `findNodeHandle` perfectly well, so the failing path did not exist under jest. It surfaced only because the candidate-A captures came back showing a plain Home screen with no drawer, and a Playwright `pageerror` probe named the cause. **Rule adopted:** an accessibility *enhancement* must never sit upstream of the primary action in a handler — C4 skips the registration on web by design *and* wraps it in try/catch, with a suite (`src/lib/__tests__/drawerTrigger.test.tsx`) that forces `findNodeHandle` to throw and asserts the drawer still opens, plus a counter-test that the native path still records a real handle so the guard cannot silently disable the feature. **Lesson for the train (companion to F-6): a green suite is not proof the contract is right, and it is not proof the code runs on every platform it ships to.**
- **F-15 · The duplicate "Close menu" is source-verified, NOT web-verified.** At base, two elements carried `accessibilityLabel="Close menu"` — the scrim `Pressable` (`:224`) and the close button (`:252`). The banked BEFORE ARIA snapshot (`assets/a11y-tree/drawer__light__390.txt`) lists only **one**, because RNW renders the scrim as a generic `div` that Playwright's `ariaSnapshot` omits. So "VoiceOver announced two identical buttons" is a **reasonable inference from the source, not something web proved** — on iOS a `Pressable` carrying an `accessibilityLabel` *is* exposed to VoiceOver, but only a device pass can confirm it. The C3 fix is right either way (the scrim has no business in the a11y tree), but the evidence level is `code-inferred` → **NEEDS-SKY-DEVICE**, and it is on the device list as such.
- **F-16 · Every web frame of Tasks is a SIGNED-OUT frame — the 60pt mine/All row cannot appear in one (Phase 3 P5, 2026-07-26).** `TasksScreen` gates that row on `userId = user?.id` (`:182`, `:973`), and the capture rig is read-only by law — it clicks "Browse without an account" and never authenticates. So the row is *structurally absent* from every banked Tasks capture and from every web measurement, at base and for every candidate. It is added back analytically (`MINE_ROW_HEIGHT = 60`, fully determined by shipped style values: `mineToggleRow` paddingVertical 8×2 `:2250` + `mineChip` minHeight 44 `:2261`) and tagged `code-inferred`, never silently merged into a measured number. **Consequence for the gate board:** the header Sky sees on device is 60pt taller than the header in every capture on the board, and the board must say so.
- **F-17 · The phase plan's card-pitch constant (179) is outside the measured range — the pane arithmetic is confirmed, the "cards visible" numbers were not (Phase 3 P5, 2026-07-26).** `tools/measure-header.mjs` measured the base at 390×844: chrome pane **451** device-adjusted vs the plan's 453 (Δ −2) and first card top **553** vs 557 (Δ −4) — both inside the P5 gate, so the row table (ScreenHeader 98 · select-entry 52 · search 60 · mine 60 · category 62 · sort 64) is confirmed measured truth. But **card pitch is content-dependent, not a constant**: the live list measures `139, 139, 139, 159, 159, 159, 175, 159, 159` (slot heights, each carrying its own bottom margin). 179 appears nowhere. The three cards at the top of the list — the ones that decide what Sky sees — are **139**.
  - Re-derived baseline at the measured reference pitch: **rest 1.36 cards** (plan said 1.03) and **scrolled 2.09** (plan said 1.61). The diagnosis is unchanged and still damning in points — 451pt of chrome on an 844pt screen is **53% of the display**, and the first card starts **65% down** — but every candidate estimate in the plan is quoted at the wrong pitch and is superseded.
  - **Rule adopted for this phase:** no candidate's row count is *estimated*. Each candidate is built, exported, and measured with the same tool, so the gate board's cost table is measured truth (`web-approximated`) rather than arithmetic. Points-reclaimed is quoted alongside cards-visible, because points are pitch-invariant and cards-visible is not.
  - Two measurement traps found and fixed in the tool, recorded so the next reader does not re-hit them: the card's `aria-label` sits on an element **16pt inside** the card (measuring it overstates `firstCardTop` by 16 and mis-reads pitch), and `[role="tablist"]` on Tasks matches the **sort row**, not the tab bar (`:1048`).
- **F-7 · Verification-environment truths** (see also `~/.claude/.../memory`): the in-app preview pane runs its tab hidden → rAF suspended → RN animations freeze mid-flight (harness artifact; use a real browser tab). Local iOS sim builds fail on the `fmt` pod under Xcode 26.6 (5× consteval errors) — fails on untouched `main` too; separate task filed. `pod` needs `LANG=en_US.UTF-8`. Never trust `cmd | tail` exit codes — a piped `expo run:ios` reported success over a failed xcodebuild.

- **Phase 2 (D2, `devicetune/2-drawer-material`): base `067864e` → tip `8cded0b` · 2026-07-25 · typecheck 0 · lint 0 err / 77 warn (baseline exact) · jest 152 suites / 2116 pass / 0 fail (base 2076 + exactly the 40 tests this phase added — F-12) · arbiter exit 0, 32/32 PASS, declared == shipped verified token-by-token · 13/13 pre-existing stacks files 0-diff · PROTECT untouched · 30/30 browser captures banked against fresh per-candidate static exports. D2a CLOSED; **D2b mockup gate OPEN — awaiting Sky's pick**. NOT merged/pushed/built — STOPPED on `devicetune/2-drawer-material` (Sky's hands). Report: `qa-reports/2026-07-25_DeviceTune_Phase2_DrawerMaterial.md`. Board: `design-reviews/device-tune/D2b_mockup_gate.html`.**
  - `c30f062` C1 — the footer retires; its `paddingBottom` becomes a real safe-area inset on the panel (it was the panel's only bottom clearance).
  - `737ce8d` C2 — the scheme rebind, surfaces AND inks together. Living law amended (GLASS §8, DESIGN §7, `theme.ts` nav-chrome comment); historical R2 record untouched.
  - `32c4987` C3 — scrim de-duplicated, focus-on-open, close-reason-aware focus return.
  - `ee8821d` C4 — **fixes a regression C3 shipped** (see F-14).
  - `8cded0b` C5 — lint hygiene (duplicate import took the baseline 77 → 79).
  - **Provenance: ran on Opus 5, NOT the Fable 5 the phase prompt specified** — a session cannot re-point its own model. Tagged in the report §0; re-fire from `067864e` in a Fable-5 window if that matters to the record.

## §A Sky's picks land below this line

*(none owed for Phase 1 — F-4 fork is moot; sign-out confirm reuses shipped Settings strings byte-for-byte under S-1)*

### Phase 2 (D2 — drawer material)

- **A-1 · Drawer footer REMOVED. Pre-recorded by Sky in the D2 phase prompt (2026-07-25).** The row `AccessMap · Made with ♥ in Canada` (`HamburgerDrawer.tsx:311-314` at base) and its `drawerFooter` / `footerText` styles are deleted. The row held nothing else — the version string lives in `AboutScreen`, not here. **This moots the BP16 copy-table row for this one string** (a deleted string cannot collide with a pending copy pick); the S-1 fence is otherwise fully intact for every other drawer-surface string. The footer's `paddingBottom: 36/20` was the panel's ONLY bottom clearance, so that clearance moves onto the panel itself as a real safe-area inset (`SafeAreaInsetsContext`, the `MyReportsModal.tsx:66` idiom — `useSafeAreaInsets()` throws in the drawer suites' bare renders). Net effect on a home-indicator device: the last row gains real clearance instead of the old hardcoded guess.
- **A-2 · D2b mockup gate — OPEN, awaiting Sky's pick.** Candidates BEFORE / **A** (scheme-bound engineered chrome-Lite, as built in C2) / **B** (live glass, `variant="bulk"` + the Sheet outer-shadow recipe). Recommendation is stated on the board *after* the blinded squint strip, per material-lab convention. **Gates ONLY the treatment beyond C2** — the scheme rebind itself is a bug fix and is not gated. No pick ⇒ the phase closes with the gate open and the drawer correct at A.
  - **B's honesty block (must be read before picking B):** (1) B adds one live blur pane, which **transiently breaches `glass.maxLivePanes = 12`** at Tasks' worst case *while the drawer is open* — picking B requires Sky to record an explicit budget amendment; (2) **Android-B ≈ A** (Android is all-engineered by ratified strategy, GLASS §5), so B buys nothing on Android; (3) blur *feel* is a Chromium proxy on web → **NEEDS-SKY-DEVICE**.
- **A-3 · RT asymmetry (implementer's call, recorded for ratification).** In Reduce Transparency the panel goes opaque: **dark keeps the shipped flattened `#0D1220` byte-for-byte**; **light takes `color.overlay`** (the house RT chrome fill, GLASS §6). Symmetric `color.overlay` was rejected because dark `color.overlay` is a *neutral* `rgba(20,20,20,0.97)` and would visibly shift the drawer off its navy deep-field identity — breaking the "dark mode does not move" promise for no gain. Both states are arbiter-proved.

### Phase 3 (D3 — Tasks header reclaim · D4 — the map comes home)

- **A-4 · D3 mockup gate — OPEN, awaiting Sky's pick.** Candidates **BEFORE** / **A** (collapse-on-scroll) / **B** (consolidated control row — *recommended*) / **C** (density pass). Every candidate inherits **D3-C1** (S-5, pre-decided, ships pre-gate); the gate decides only what happens *on top of* C1. Board: `design-reviews/device-tune/Phase3_mockup_gate.html` §1 — blinded squint strip first, recommendation after it, per material-lab convention.
  - Cost tables on the board are **measured, not estimated** (F-17): each candidate was built, exported, and measured with `tools/measure-header.mjs`.
  - **No pick ⇒ the phase closes with the gate open and C1 + all of D4 shipped correct** (the D2b precedent). Picking a different candidate later costs a revert of the winner's commits plus one build commit — bounded, and stated on the board.
- **A-5 · D4 empty-local moment — OPEN, awaiting Sky's pick.** Two questions, one gate:
  1. **Placement** — **A** (a quiet caption line under the peek; *recommended*, and what ships) or **B** (a glass chip on the peek; rendered working-tree-only for the board, never committed).
  2. **Copy ratification** — three options; **option 1 ships tagged PROVISIONAL** and is a one-line swap (`EMPTY_LOCAL_INVITE`, a single const feeding both the visible caption and the peek's composed `accessibilityLabel`).
  - Register rule applied to all three options: they claim **report-absence, never barrier-absence** — the app cannot know a place has no barriers, only that nobody has reported one.
  - Not gated, because it is deterministic and Sky pre-decided the defect: **D4-C1…C4** (the peek honoring a post-mount center, honest probe states, the empty-local predicate, the standing geo-privacy fence).

- **F-18 · A capture disproved a claim the source review had accepted (Phase 3 D3/C1, 2026-07-26).** The C1 commit message first asserted "VoiceOver order is untouched", and a source-level guard appeared to back it — the guard's *name* said "stays the first focusable control after the banners" while its *body* only compared row-container order, which C1 genuinely does not change. The banked ARIA trees said otherwise: `Select multiple` and the search textbox **trade places** (before: subtitle → button → textbox; after: subtitle → textbox → button), because S-5 places the control after the clear button. The behaviour is correct and intended; the claim about it was not. An intermediate `diff` of the two trees also reported "identical" because the grep normalizing it required a colon, which silently dropped the one line that had moved.
  - Corrected: the guard now asserts the real four-control order (`Search flags` → `Select multiple` → `Show all flags` → `Show all categories`) and a second test covers row order separately; the commit message states the one-position move and points at both trees. Reachability is genuinely unchanged — it is still the 4th interactive control and still precedes every filter.
  - **Lesson, and it is the third of its kind on this train (with F-6 and F-14): a passing guard is only as true as its assertion.** F-6 was a guard that pinned a bug; F-14 was a green suite that missed a dead screen; this is a guard whose name overclaimed what its body checked. The capture caught all three. Read the artifact, not the checkmark — and never let a test's name carry a claim its body does not make.

- **F-19 · The empty-local message reaches VoiceOver twice, by construction (Phase 3 D4/C3, 2026-07-26).** The banked tree shows both channels live at once: the peek button's name became `"Open the full map. No barriers reported here yet — be the first."` AND the visible caption is exposed as its own text node. A linear VoiceOver swipe therefore hears the sentence twice in succession.
  - **Kept as built, deliberately, but flagged rather than hidden.** It is not the F-15 case (two identical *buttons*, a genuinely confusing double control): this is a button and a caption that describes it, and the composition earns its place — the peek is a MAP, and a blind user focusing "Open the full map" otherwise has no way to know it is empty. Removing the caption from the tree would also cost the polite live-region announcement.
  - Evidence level is `web-approximated`: RN-web ARIA is not iOS VoiceOver, and `accessibilityRole="text"` + `accessibilityLiveRegion` behave differently there. **On the device list as an explicit VO item.** If Sky finds it chatty, the remedy is one line — `accessibilityElementsHidden` + `importantForAccessibility="no-hide-descendants"` on the caption, keeping the composed button name as the single channel.

- **Phase 3 (D3 + D4, `devicetune/3-header-map-polish`): base `8cded0b` → tip `c2d7a0c` · 2026-07-26 · typecheck 0 · lint 0 err / 77 warn (baseline exact) · jest 157 suites / 2186 pass / 0 fail / 84 todo (base 152/2116 + exactly the 5 suites and 70 tests this phase added — F-12 conservation exact, declared == shipped) · NO arbiter owed (no ink/floor/edge/pair change, no new composite) · 14/14 pre-existing stacks files untouched · PROTECT (`PlatformMap.tsx`, `.web.tsx`, `GlassSurface.tsx`) 0 files changed · S-1 empty diff on all four fenced surfaces · 34 PNGs + 6 ARIA trees banked against fresh per-candidate static exports, 1 FAILED row kept as honest history · working tree clean of all candidate diffs. **D4 CLOSED pending device; D3-C1 shipped; gates A-4 + A-5 OPEN.** NOT merged/pushed/built — STOPPED on `devicetune/3-header-map-polish` (Sky's hands). Report: `qa-reports/2026-07-26_DeviceTune_Phase3_HeaderMapPolish.md`. Board: `design-reviews/device-tune/Phase3_mockup_gate.html`.**
  - `8c48c60` D4/C1 — the peek camera honors a post-mount center (keyed remount; `snapToRegion` rejected because it no-ops pre-ready and neither half exposes a ready signal, so it would have meant editing PROTECT-adjacent code).
  - `2d47655` D4/C2 — honest probe states; the 300 ms reveal delay is load-bearing (a denied check returns in ms and would otherwise flash a false claim).
  - `3639eb2` D4/C3 — the empty-local moment; five honesty gates; copy PROVISIONAL pending A-5.
  - `b259cca` D4/C4 — standing geo-privacy fence; not falsifiable by design, proved to bite by injection.
  - `c2d7a0c` D3/C1 — select-multiple joins the search row (S-5). Measured: chrome pane 344 → 292 web px, exactly the predicted 52pt.
  - **Headline:** cards visible at rest **1.36 → 1.73 shipped**, up to **2.87 if Sky picks B**. Chrome 451 → 399 shipped → 243 at B.
  - **Provenance: ran on Opus 5, NOT the Fable 5 the phase prompt preferred** — a session cannot re-point its own model. No sub-agents. Re-fire from `8cded0b` in a Fable-5 window if that matters to the record.

- **A-4 · RESOLVED 2026-07-26 — Sky picks candidate B, with the `TASKS` eyebrow KEPT.** She caught that the rendered B dropped both the eyebrow and the subtitle, and chose the variant that keeps the editorial eyebrow: **B + `TASKS`**. Measured cost of the amendment: the eyebrow is 18pt, taking the chrome pane from 243 → 261 and rest-visible cards from 2.87 → ~2.74 — about a tenth of a card, against C1's 1.73 and BEFORE's 1.36.
  - **Rationale recorded, because it is a design law and not just a preference:** the all-caps eyebrow is the app's editorial voice (Home carries `NEARBY` / `LATEST` above its title). Dropping it on Tasks alone would have made that one screen a different family member. The subtitle ("Verify and resolve reports") does NOT carry that role and stays dropped — the 30pt title says the same thing.
  - Ships as D3-C2 (header) + D3-C3 (the consolidation) + D3-C4 (banner slim, isolated so it can be vetoed on its own without disturbing the rest).

- **F-20 · D3/C4 (the banner slim) was BUILT, then REVERTED before shipping — it silently breaks a documented colour-not-sole-signal contract (Phase 3, 2026-07-26).** The plan carried the banner slim as candidate B's last commit, deliberately isolated as "most-likely-vetoed", and the rendered B on the gate board included it. Building it exposed why it should not ship: dropping the `Nearest open barrier · ` prefix leaves the visible row reading `No ramp · Severity 3 · 639 m`, and the shipped style comment (`TasksScreen.tsx:2096-2097`) states the contract it violates verbatim — *"Color is never the sole signal — the 'Nearest open barrier' text states it plainly."* With the prefix gone, the fact that this row is the nearest OPEN barrier is carried only by position, a brand tint, and a decorative (a11y-hidden) pin icon. The `accessibilityLabel` is unaffected, so **screen-reader users keep the meaning and sighted users lose it** — the inverse of the usual failure, and easy to miss precisely because the a11y tree still reads correctly.
  - Reverted, not shipped. The isolation the plan required is what made that a one-command decision.
  - **Sky's call, framed as a real choice:** (a) leave the banner as shipped — it wraps to two lines and costs ~14pt; (b) a shorter prefix that keeps the meaning, e.g. `Nearest · No ramp · Severity 3 · 639 m`, which fits one line and preserves the stated-in-text signal; (c) ship the slim and amend the colour-not-sole-signal comment to admit the tint now carries it — **not recommended**, it trades an accessibility property for 14pt.
  - Nothing else in candidate B depended on it: C2 and C3 shipped without it, and the measured gains (chrome 399 → 261, cards 1.73 → ~2.74) are C2+C3's alone.

- **Phase 3 — A-4 RESOLVED AND BUILT (continuation entry): tip `c2d7a0c` → `fa8c0d8` · 2026-07-26.** Sky picked candidate **B with the `TASKS` eyebrow kept**; built same session.
  - `a623235` D3/C2 — the subtitle retires; the eyebrow stays (props-only).
  - `fa8c0d8` D3/C3 — the three filter rows move into an opaque `Sheet` behind one trigger; ScrollView/OverflowFade/overflow-fade hook and their styles removed as orphans; `CHROME_FALLBACK_HEIGHT` 350 → 210 (every term measured).
  - **D3/C4 (banner slim) BUILT THEN REVERTED — see F-20.** Not shipped. It silently breaks a documented colour-not-sole-signal contract, and Sky owes a choice there (options a/b/c in F-20).
  - **Measured shipped result:** chrome pane **451 → 257pt device**, first card 553 → 359, rest-visible cards **1.36 → 2.76**. The eyebrow amendment cost 4pt against the rendered candidate (243 → 257) — Sky was quoted ~261 and the measurement came in at 257.
  - **Verified end-to-end against the export, not just by source contract:** picking a category from inside the sheet cuts the list 9 → 5, the trigger's background goes `rgba(255,255,255,0.6)` → `rgb(20,102,224)`, and the Clear-filters chip mounts 0 → 1 → 0 across filter-and-clear.
  - Gates at `fa8c0d8`: typecheck 0 · lint 0 err / **77 warn** (baseline exact) · jest **158 suites / 2205 pass / 0 fail / 84 todo** · conservation **+6 suites / +89 tests** vs base, matching every per-commit declaration · PROTECT 0 files changed · S-1 empty diff · no arbiter owed · **42 PNGs + 8 ARIA trees** banked.
  - **A-5 remains OPEN** (D4 empty-local placement + copy). D4 itself is shipped and closed pending device.

- **A-2 · RESOLVED 2026-07-26 — Sky picks A.** The D2b drawer-material gate closes on **A = the drawer exactly as Phase 2 built it** (scheme-bound engineered chrome-Lite). **No further work**: live glass (B) is not taken, so the `maxLivePanes` budget amendment it would have required is moot and the Phase-2 blur budget stands unchanged. **Phase 2 is now fully closed** pending only Sky's device pass.
- **A-5 · RESOLVED 2026-07-26 (copy) — Sky picks option 3:** `No reports here yet. You could add the first.` Shipped in `f41def4`; the code comment now records the ratification and keeps the two rejected options on the record. It is the softest of the three and states the register rule most plainly — it speaks of REPORTS, never of barriers, so it cannot be read as a claim that a place is free of them.
  - **Placement: A (the caption line) stands, as built.** Sky's message said "option B" while quoting option 3's wording, so the copy is unambiguous and the placement reference is not. **Placement B — a glass chip on the peek — was never built** (it would add a live blur pane over the map for one sentence, and the caption costs nothing against the budget). Nothing is blocked either way; if she does want the chip, say so and it gets built and captured on its own. Flagged rather than assumed.
- **F-21 · The banner-slim decision (F-20) is smaller than it looked, and the recommendation is to leave the banner alone.** The whole rationale for slimming it was to reclaim header space — but the banner is the `SectionList`'s `ListHeaderComponent`, so it is **not part of the fixed chrome at all**. It scrolls away with the list. The measurement confirms this independently: `measure-header.mjs` finds it among the LIST's slots (58pt), not among the chrome pane's rows. So its ~14pt is a one-time cost at the top of the list, not a permanent tax on every screenful — against a header that already went 451 → 257pt. **Recommendation: option (a), leave it as shipped.** It keeps a documented accessibility property at a cost that is now close to irrelevant, and it is the only option requiring no further work or re-measurement.

## §M Merge record

- **2026-07-26 · THE TRAIN IS MERGED TO LOCAL `main`, on Sky's explicit approval. NOT PUSHED.**
  - **Authority:** Sky said "I approve" in session. Recorded plainly because the Constitution makes this Sky-only: Art. 17's delegated merge+push grant covers the **Prompt Library Tool** and nothing else ("every other agent, project, and surface remains Sky-only merge"). This merge rests on her spoken intent outranking the Constitution (authority order), **not** on any standing grant. No precedent is set for AccessMap.
  - **Order (chosen because it minimises rebase risk):** the 14-commit train fast-forwarded first, then the 1-commit photo fix was rebased onto the new `main` and fast-forwarded. Rebasing 1 commit beats rebasing 14, and the two branches touch **zero files in common**.
  - `d43f867` → **`f41def4`** (train: Phases 1+2+3, 17 commits) → **`7887ce3`** (photo fix, rebased `64342e1` → `7887ce3`).
  - **`origin/main` is UNCHANGED at `d43f867`. Nothing was pushed** — a push is an external side effect the Constitution reserves to Sky (Prompt-Library carve-out aside). **No build, no deploy** (S-3: EAS builds are Sky-initiated).
  - **Rollback:** `git reset --hard d43f867` on `main`. Both source branches still exist unmodified except the photo fix's rebase (`64342e1` is still reachable and recorded here).
  - **Gates on merged `main`:** typecheck **0** · jest **158 suites / 2222 pass / 0 fail / 84 todo** · lint **0 err / 79 warn**.
  - **The 79 is correct, not a regression.** The train alone is exactly 77 (baseline). The photo-fix branch measured **79 on its own, pre-merge** — verified by checking out `64342e1` and running lint there. So 79 = 77 + that branch's own 2 warnings, with no merge interaction. Those 2 belong to the photo fix's author decision, not to this train. Jest likewise: 2205 (train) + 17 (photo fix, in existing suites) = 2222.
  - **Still Sky's:** the push · the build · the 20-item device list (report §8) · the F-20 banner decision (recommendation: leave it, F-21).
