# Adversarial verification — skeptic verdicts

## ⚠ MODEL-TRANSITION RECORD (governance — required by the audit SAFETY BLOCK)

- **Stages 0–5-partial ran on Claude Fable 5** at max effort per Sky's standing directive: the full arbiter re-arbitration, all EIGHT lens agents (L1–L8), the Part-1 evidence base (Part 1, prior session), the probe pass, and the FIRST THREE completed skeptic verdicts below (L5-01, L6-02, L2-1).
- **Fable 5 hit credit exhaustion mid-skeptic-fan-out** (2026-07-04). 3 of 20 dispatched skeptics completed with real verdicts; 17 died returning only "out of usage credits" (no adjudication). Per the SAFETY BLOCK, this halt is reported here and in the §Sky-decision notes; the fallback model is Sky's call, never the audit's.
- **Sky made the fallback call directly:** switched the session to **Opus 4.8** (`/model claude-opus-4-8`) and instructed "resume." This satisfies both the audit's Fable-halt protocol (Sky chose the fallback) and the global CLAUDE.md Opus HARD RULE (Sky started Opus directly, in an interactive session). The temporary-surge override is expired and irrelevant — the direct-start exception is what authorizes it.
- **Consequence for provenance:** skeptic verdicts marked **[Fable]** were adjudicated on Fable 5; those marked **[Opus 4.8]** were adjudicated on Opus 4.8 after the transition. The eight lens findings themselves are all Fable-authored. No finding's *authorship* changed; only the *verification* model changed mid-pass, and only for the 32 findings not yet Fable-verified. This is disclosed, not silent.

---

## Completed BEFORE the transition [Fable]

### L5-01 (canonical B — web/native zoom lockout) — **ADJUSTED · keep CRITICAL** [Fable]
Not artifact, not stale (16 consistent captures + code at HEAD). **Mechanism is STRONGER than the finding wrote:** `overlay` + `overlayTopGroup` are `box-none` (`MapScreen.tsx:1259/:1265`) but **`topRow` is not** — its full-width div blocks DOM hit-testing across the whole strip, so on web even the *visible* zoom-button pixels (incl. 834's fully-visible "−") are pointer-dead except a ~6px fringe. `leaflet.css:288/:324` = 26×26 / 30×30 touch; no `zoomControl` prop on `MapContainer` (`PlatformMap.web.tsx:638`); zero leaflet overrides in `src` (grep clean).
Per-width occlusion (verified in captures): **375/390** "+" majority-buried under the pill, "−" bottom third–half under the action bar; **430** same; **834** single-row, "+" mostly under pill, "−" fully visible **but still click-dead**.
CORRECTIONS folded: (1) taps die on the un-guarded `topRow` wrapper, not the pill (the pill only paints the occlusion; even uncovered pixels inert). (2) Native: **iOS** ships no zoom buttons (MapKit none, app adds none) but double-tap zooms IN single-fingered — the real gap is single-pointer **zoom-OUT**; **Android** `zoomControlEnabled` defaults **true** (`react-native-maps`) → would show Google buttons, so restrict "no buttons" to iOS. (3) 834 "−" fully visible, not a sliver. (4) Surviving paths: buttons keyboard/Tab-focusable; scroll-wheel + double-click still zoom — the lockout is pointer/touch-specific = exactly R4's population. CRITICAL holds.

### L6-02 (announcement/focus layer silent on web) — **CONFIRMED · keep HIGH** [Fable]
RN-web@0.21.2 `dist/exports/AccessibilityInfo/index.js:75,79` — `setAccessibilityFocus(){}` and `announceForAccessibility(){}` are **literally empty**; `isScreenReaderEnabled` hard-resolves `true` (:15, ledger #15 confirmed). `useFocusOnOpen` (`accessibility.ts:42`) calls exactly this dead API. **liveRegion DOES translate** (`createDOMProps/index.js:460-462` → `aria-live`) — BUT the app's live-region twins (FlashBanner, UpdateBanner, Tasks flash-pill, locating banner) mount *with* their content (`if(!rendered) return null`), and mount-timed live regions don't reliably fire on web. Blast radius **undercounted**: ~50 announce sites / 15 files (+FlagDetailModal ×8, ProfileScreen ×5, FeedbackModal, UpdateBanner, OnboardingModal, flagsStore). Reliable survivors = the finding's three persistent-mount cases (status pill `MapScreen:1275`, severity hint `ReportFlagModal:649`, `role=alert` incl. SignIn error `:175-183`). Focus half real but trap-mitigated (RN-web `ModalContent` emits `role=dialog`+`aria-modal`, `ModalFocusTrap` moves DOM focus — entry works, title-focus/context dies; overlaps L6-16). HIGH (not CRITICAL) — the finding's own tempering is correct. No corrections; two enrichments (larger radius; Android twins fail via mount-timing not missing translation).

### L2-1 / L6-08 (canonical C — white digit on severity fills) — **CONFIRMED · keep CRITICAL** [Fable]
**7 render sites across 6 components (one MORE than claimed):**
- `NearbyFlagsModal.tsx:140,144` (+styles :376-387) — white `textOnBrand` bold 13 on 32px `severityColor` disc — **guest, auto-opened**
- `LegendModal.tsx:66,70` (+:192-203) — white bold 14 on 32px disc — **guest**
- `ReportFlagModal.tsx:607` fill / `:620-628` white Check-11 / `:637`,`:1081,1084` `sevTextActive` white bold 16 — **guest**, probe PNG shows white "3"+check on orange
- `ActivityFeedModal.tsx:156,162` (+:458-461) — white bold 12, 28px disc — auth-gated
- `RecentlyViewedRow.tsx:139,145` (+:201-204) — white bold 12, 24px dot — auth-gated (parked item ①)
- `FlagDetailModal.tsx:1066,1076` (+:1797-1798) edit radio — auth-gated
- **UNDERCOUNT** `FlagDetailModal.tsx:834,839` (+:1595) view-mode "Severity {n}" chip, white bold 12 — unlisted (site 6b)
None adopt `textOnColor` (the fork exists `theme.ts:543-547`; correct adopters = SeverityBadge, `MapScreen:1364/:1702`, `AdminScreen:165`). Arbiter re-run exit 1: sevDot1–4 = **1.57 / 2.15 / 2.78 / 3.61** vs 4.5, both themes, verbatim; fills opaque so ratios transfer. 12–13 bold < 18.66px-bold threshold → 4.5 floor correct; sev1–3 fail even the lenient 3.0. Guest-reachability code-verified (RN-web `isScreenReaderEnabled()`→true → `MapScreen:352-357` auto-opens Nearby for every web guest; Report entry ungated; Legend toolbar ungated `:1316-1319`). Not stale/artifact/web-only (white digit ships both platforms). Bonus: `ReportFlagModal:1082-1084`'s comment calls the white fill "legible," contradicting `theme.ts:538-542`. CRITICAL stands. Optional enrichment: add site 6b.

---

## Re-run AFTER the transition [Opus 4.8]
*(banked below as the re-launched skeptics complete)*

### L3-2 (canonical A — lying arrival) — **CONFIRMED · keep CRITICAL** [Opus 4.8]
Every sub-claim survives at HEAD. Mount effect `MapScreen.tsx:1043-1061`: `getForegroundPermissionsAsync().then` → `initialLocationAction(status)==='fetch'` ? `requestLocation()` : `setLocating(false)` — the else branch clears the spinner and **never** `setPermissionDenied`. `permissionDenied=true` set at exactly ONE site (`:999`, inside `requestLocation`); banner gated on it `:2004-2014`. `DEFAULT_REGION={37.7749,-122.4194}` SF `:123-128` via `initialRegion :1190`. Pill `:1277-1283` = `` `${flags.length} flag(s) nearby` ``, count from `listFlagsPage` (`flags.ts:652-671`) — **no lat/lng/bounds clause anywhere** (grep clean); the `:454` viewport gate only discards out-of-bounds *realtime events*, never bounds the counted `flags`. NO region persistence (grep clean) → SF recurs every denied/undetermined arrival, repeat users NOT softened. Facet-b independently proven by the panned-empty probe (empty viewport, pill still "5 flags nearby"). **DEDUP: ONE canonical + facet-b confirmed** — the mount-hole and global-count pill are the same "map lies about proximity" failure, causally entangled (the denied state is the very evidence that exposes the pill's lie). Native: already-denied = platform-independent logic (confirmed); iOS first-run *feel* = NEEDS-SKY-DEVICE. No corrections.

### L3-1 (canonical D — guest CONTRIBUTE dead-end) — **CONFIRMED · keep CRITICAL** [Opus 4.8]
Could not kill it; every attack confirmed. Mechanism: Home pill `openReport:true` handler (`MapScreen.tsx:1093-1097`) does only `setReportOpen(true)` — never `requestLocation()`; the sheet's `location` prop is `dropLocation ?? location` (`:2103`), both MapScreen-local `useState(null)` (`:257`,`:281`) never written for a no-grant guest → `ReportFlagModal` shows "Waiting for location…" (`:465-467`) with `disabled={submitting || !location}` (`:975`) forever. **WORKAROUNDS all dead-traced:** (1) sheet has no own location hook (`location` is a pure prop `:78`); (2) the only guest geolocation asks are Home "Use my location" / Map "Recenter"/FAB — and the FAB's `requestLocation()` is auth-gated (`:2059`); onboarding "Allow Location" is a web no-op (`OnboardingCards.tsx:244-246`); (3) Home-first→Report **cannot** work — MapScreen `location` is screen-local `useState`, NO shared location store exists in `src/` (grep-confirmed); (4) web long-press/right-click drop-flag is auth-gated (`:1218` `if(!authUser) return`). Fresh probe decisive: byte-identical SHA-256 open vs t+25s (light `80ad9c3e…`, dark `b75835b3…`), persists past the 15s GPS timeout, submit faded throughout. The author's own comment (`:1240-1243`) claims "the FAB can never dead-end on web" — proving the dead-end was solved for the FAB but NOT the Home pill. CORRECTION (enrichment): PROBE-REQUEST 3 satisfied — the no-permission variant is now captured + hash-verified, not code-inferred.

### L6-01 (canonical, RN-web selection-state drop) — **CONFIRMED · keep CRITICAL · 2 corrections** [Opus 4.8]
Mechanism: RN-web 0.21.2 `createDOMProps` maps only FLAT a11y props (`accessibilitySelected/Checked/Expanded/Busy` + `aria-*` twins) — it never reads the nested `accessibilityState={{…}}` object, so `selected/checked/expanded/busy` are dropped. Trees confirm zero `[selected]/[checked]` inside the report dialog (`report:71-93`); the ONLY `[selected]` across all 8 trees is the react-nav tab bar, which uses `aria-selected` directly (`BottomTabItem.tsx:353`) — validating both the diagnosis AND the prescribed fix on this exact version. Call sites cross-checked stateless: `ReportFlagModal:578,611`, `NearbyFlagsModal:236,252`, `MapScreen:1330,1661,1723`.
**CORRECTION 1 (required):** the "disabled is dropped too" slice is WRONG — plain `disabled` DOES translate (`Pressable/index.js:125` emits `aria-disabled`), and the buttons pass a plain `disabled={submitting}` alongside the nested state (`ReportFlagModal:574,603,965,975`), so the disabled STATE survives on web; only the redundant nested copy drops. Amend L6-01 to: "selected/checked/expanded/busy dropped; disabled survives via the parallel plain prop." Does NOT weaken CRITICAL (selected is load-bearing).
**CORRECTION 2 (framing):** the severity echo line IS a real `accessibilityLiveRegion="polite"` (`ReportFlagModal:649` → RN-web maps to `aria-live`, `createDOMProps:460-463`), so changing SEVERITY announces on web — but there is NO equivalent for CATEGORY. State the asymmetry explicitly: severity confirmable, category unconfirmable + stateless. CRITICAL holds (web is the only guest surface; SR path force-enabled for all).

### L1-4 / L3-9 (canonical E — web "Open full map" → auto-list) — **CONFIRMED · keep HIGH · wording tighten** [Opus 4.8]
`MapScreen.tsx:351-357` gates auto-open on `hasAutoOpenedListRef` (mount-scoped `useRef`) → fires once per COLD MOUNT, not per re-entry; explicit Close respected. But `isScreenReaderEnabled()→true` unconditionally on web (ledger #15) defeats the ref for **every** web visitor's first map view. Screenshot + tree confirm a **full-viewport pageSheet** (`NearbyFlagsModal.tsx:182`) covering the map edge-to-edge, with a labeled "Close nearby flags list" 2nd in traversal (one obvious tap). Good-direction intact: the list delivers distance/severity/status/age/description — the app's best surface. The finding already protects the SR benefit (fix the trigger, not the content) — no regression risk. **CORRECTION:** tighten "every web arrival" → "every web cold-mount." HIGH honest (fires on the entire web cohort + entry-promise break on the flagship).

### L3-5 / L6-03 (canonical F — silent submit) — **CONFIRMED · keep HIGH** [Opus 4.8]
Both paths re-traced at HEAD. Anon `:314-327`: `createAnonFlag→recordAnonSubmit→track→hapticNotify('success')→reset→onCreated→onClose`. Auth `:355-416`: success → `hapticNotify→announceForAccessibility('Report filed.')→reset/onCreated/onClose`. The only `notify()` (=`window.alert` on web) on either path is the FAILURE branch (`:329,:424`) — never success. `hapticNotify` native-only (`haptics.ts:5`); `announceForAccessibility` web no-op. `onCreated`(`MapScreen.tsx:2111`)=`refreshFlags()` only — no `animateTo`, user lands on the stale list. **Platform matrix — "confirms nothing" holds all 4 cells:** web-anon silent; web-auth silent; native-anon haptic-only; native-auth SR-hears-only (sighted native = haptic only). App.tsx FlashBanner fires only the cross-session "+N points while away" toast (`:73-79`), never on submit, never for guests. Orientation §3's "anon success alert" is CODE-INFERRED and does NOT exist at HEAD — settled. HIGH honest (silent finish → doubt-resubmits → 5/day anon cap punishes).

### L4-01 (canonical, web RM camera gate inverted) — **CONFIRMED · keep HIGH** [Opus 4.8]
Probe `rm-flight` (RM=reduce, Recenter tap): four-frame Leaflet flyTo curve — `panned` start, `t150` mid-transition (NOT settled → "instant" reading refuted), **`t700` a genuine intermediate zoom** (distinct from both endpoints), `t1600` settled. The flight ran AND ran >1s — longer than the 0.6s a non-RM user gets. Code `PlatformMap.web.tsx:624-627`: `flyTo(…,{duration: reducedMotion ? 0 : 0.6})` no `animate` key; Leaflet 1.9.4 `:3474-3475` short-circuits to instant only when `options.animate===false`; `:3522` `duration = options.duration ? 1000*duration : 1000*S*0.8` → `0` falsy → distance-based default. All 7 triggers route through this. It IS `flyTo` (not setView); only 4 `reducedMotion` refs exist, all on this path. Gate inverted, not merely ineffective. No corrections.

### L4-02 (canonical, cluster expansion ignores RM both platforms) — **CONFIRMED · keep HIGH** [Opus 4.8]
Probe `rm-cluster` (RM=reduce): a "5" cluster DID form (the "inconclusive at 5-flag scale" caveat does not apply) — `pre` single bubble, **`t120` zoomed-in intermediate** (proof the 0.4s flyTo animated under RM), `t620` settled. Web `PlatformMap.web.tsx:345` `flyTo(…,{duration:0.4})` hardcoded; `ClusteredMarkers` (`:252-258`,`:687-693`) never receives `reducedMotion` (zero refs inside it → gate unreachable). Native `PlatformMap.tsx:110-134` sets neither `preserveClusterPressBehavior` nor `animationEnabled={false}`; library `_onClusterPress`→`fitToCoordinates(…)` no `animated:false` (RNM default true). Native code-only (NEEDS-SKY-DEVICE, tagged). No corrections.

### L5-06 (canonical, Home peek is a live map) — **CONFIRMED · keep HIGH** [Opus 4.8]
Probe `home-peek`: `wheel-after` = surrounding page pixel-identical (scrollTop 0→0) while the peek's map **zoomed out to show all of San Francisco** — the peek ate the wheel/scroll gesture; `clustertap-after` = center tap opened FullMap (Pressable won on empty map). Code `HomeScreen.tsx:257-269`: live `<PlatformMap>` (`:264`) in a plain Pressable, only the hint pill `pointerEvents="none"` (`:265`); grep shows zero `scrollWheelZoom/dragging/zoomControl/pointerEvents` on the map (sole `interactive:false` is a decorative heat Rectangle). Wheel-hijack + interactive-peek core PROVEN; the cluster-fly-in-place half is code-inferred (tap landed on empty map). HIGH stands (scroll theft + app-exit-via-attribution on the landing surface). CORRECTION: mark the cluster-fly sub-claim code-inferred, not probe-proven.

### L3-8 (sorted-by-distance false claim) — **CONFIRMED (narrowed) · HIGH→borderline MEDIUM** [Opus 4.8]
False window real but NARROW: `NearbyFlagsModal.tsx:64-69` announces "Sorted by distance" UNCONDITIONALLY, while `:79-88` sorts by distance only `if (location)`, else recency. So the announcement is false only when `location==null` at announce-time (guest/denied/undetermined/first-open-pre-resolve). Granted context (`map-first-arrival__light__390.txt`) IS distance-sorted (297m→1.5km→1.6km) — announcement honest there. The `report-noperm` probe shows the no-location state has an HONEST *visible* banner (`:198-204` "Showing the most recent first") — so the SR announcement CONTRADICTS the visible notice in that one state. **CORRECTION:** the finding's "every web user's landing announces the falsehood" is WRONG — only when location is unresolved. Real but degraded-state-only → MEDIUM defensible; the SR-vs-visible contradiction keeps it arguably HIGH. **Ladder placement: HIGH, flagged as the softest HIGH (Part 3 may treat as MEDIUM).**

### L3-10 (location personality incoherent) — **CONFIRMED on web · REFUTED on native · keep HIGH (web-scoped)** [Opus 4.8]
Home gate `HomeScreen.tsx:113` `probeEnabled = Platform.OS!=='web' || askedForLocation`; `:174` `requireExistingPermission={!askedForLocation}` → **native Home DOES silently auto-detect an already-granted location** (no-prompt `getForegroundPermissionsAsync`). So "Home refuses a granted location" is TRUE on **web only**. Tasks (`TasksScreen.tsx:307` `useUserLocation()` → `requestForegroundPermissionsAsync`/web `getCurrentPosition` on mount) fires an uninvited prompt + consumes location independently — the "distances only after another surface resolves" counter dies (Tasks makes its own request). Same pre-granted web harness: `tasks__at-rest` shows "1.6 km · 19 min walk", `home__at-rest` shows "LATEST"/SF-peek/un-tapped button. No qa-report ratifies the split. **CORRECTION:** add "on web" to the Home half; native is coherent (silent grant pickup). Incoherence is web-cohort-real → keep HIGH (web).

### L3-11 (coords-only WHERE) — **CONFIRMED · steelman DEFEATED · keep HIGH** [Opus 4.8]
`ReportFlagModal.tsx:462-469` renders `at {lat.toFixed(5)}, {lng.toFixed(5)}` in a plain `<View>` (no onPress/adjust); coords = `MapScreen.tsx:2103 location={dropLocation ?? location}` = user's own GPS (or auth-only drop-pin), NOT map center. `reverseGeocode` EXISTS (`geocode.ts:108`) but is NOT wired to the sheet; `AddressSearchModal` not wired either. **Steelman "the map behind the sheet shows the location" is FALSE** for the guest FIND→report path: `report__open` + `report-noperm__open` both show the sheet stacked over the **Nearby-flags LIST modal** — no map, no pin visible behind. The coordinate string is the sole WHERE for everyone. No corrections; residue exactly as stated (can't verify/hear/adjust; SR gets a raw coord).

### L3-12 (callout cul-de-sac) — **CONFIRMED · platform-SPLIT REJECTED · keep HIGH** [Opus 4.8]
BOTH platforms dead-end. Web callout `PlatformMap.web.tsx:371` `alt` ends "Open for details." (slice lives at HEAD) but the Popup `:377-409` = category + "Severity N · status [· Anonymous]" + photo + description, no created_at, no handler (marker alt text, opens nothing). Native `PlatformMap.tsx:237` label ends "Tap to view details."; Callout `:239-277` same fields, no date; `PlatformMapHandle` exposes only `animateTo`+`showCallout` — no detail path, MapScreen imports NO FlagDetailModal. FlagDetailModal reachable only from Tasks/Profile → Tasks is the designed act-surface, so the missing map→detail LINK is the true defect. **CORRECTION:** do NOT split platforms — the SR-promise dishonesty ("Open for details"/"Tap to view details" opening nothing) is cross-platform. `created_at`/`relativeTime` absent from both callouts, present in list + Tasks. Keep HIGH.

### L5-02 (Home Report pill occlusion) — **CONFIRMED as real defect · DEMOTE CRITICAL→HIGH** [Opus 4.8]
Per-width: **375** pill overlaps row 3 chevron zone; **390** overlaps row 5 right edge/chevron; **430** pill in clear space below card, NO overlap; **834** pill far below in whitespace, NO overlap — width-dependence confirmed (list ends near the pill's fixed bottom at 375/390; taller viewports clear it). Mechanism real + code-confirmed: `reportPill` `position:absolute` genuine `PressableScale` (intercepts taps) anchored `bottom:bottomInset+12`; ScrollView `paddingBottom:bottomInset+96` clears it only at scroll-end, not the common short-content state (`HomeScreen.tsx:346-347,178,475,482-493`). **DEMOTE to HIGH — the rubric's "any occlusion at any size = CRITICAL" misfires here:** universal floating-FAB-over-list pattern; covers only the LAST visible row's right ~40% (chevron), never the tap-to-open left ~60% (`rowText: flex:1`); the whole row navigates identically to the chevron, so the covered chevron is redundant. Only true misfire zone = the pill footprint firing Report (recoverable by 1px scroll; absent at 2 of 4 widths). R4 mis-tap cost real but bounded. Trivial fix (reserve a bottom lane / apply paddingBottom at rest).

### L5-03 (web 200% zoom breaks CONTRIBUTE) — **CONFIRMED CRITICAL · one over-claim corrected** [Opus 4.8]
Two TRUE WCAG 1.4.4/1.4.10 failures at zoom-2.0 (RN-web renders real browser reflow; web IS guest mode so 200%-on-shipped-viewport is exactly the SC's test): (1) privacy banner shreds mid-word ("Reporti/ng anonym/ously") = loss of readable content; (2) header title occluded by the Feedback pill → "MapFeedback" (map dt-zoom-2) = loss of content. Submit-label overflow real (`submitBtn: overflow:'visible'`, `ReportFlagModal.tsx:1230`) — label escapes its pill's RIGHT edge. **CORRECTIONS:** (a) the finding's "bleeds toward Cancel — boundary dissolves" is OVERSTATED — Cancel/Submit are equal `flex:1` halves with a 12pt gap; the label overflows AWAY from Cancel (rightward), a clear gap remains, they stay separable. (b) submit stays REACHABLE (footer is a pinned sticky outside the ScrollView `:448-450`), so "form drops below fold" ≠ unsubmittable. Net: CRITICAL holds on the mid-word-shred + header-collision content failures, NOT on unreachability. (proxy caveat noted: `document.body.style.zoom`; the real fixes are unaffected.)

### L5-04 (filter "Clear" sub-44 target) — **CONFIRMED · keep HIGH** [Opus 4.8]
`clearLink` Pressable (`MapScreen.tsx:1544-1552`) has NO style/minHeight/hitSlop; text `variant="label"` `font.size.xs`=12pt, AppText sets no lineHeight (~1.2×≈14-16pt); "Clear" ≈34-38pt wide → ≈34-38 × ~15pt, both axes < 44, no slop rescue. Damning: the SIBLING `filterTitleRow` `:2515` got explicit `minHeight:32 + hitSlop:8` (→48pt, with a comment) — so the omission on Clear is a genuine oversight, not a pattern. HIGH (not CRITICAL) reflects the redundant 44pt "Reset all" empty-state path + per-chip deselect. No corrections.

### L5-05 (action-bar tools scroll out of reach) — **CONFIRMED · keep HIGH** [Opus 4.8]
Action bar `ScrollView horizontal showsHorizontalScrollIndicator={false}` (`MapScreen.tsx:1299-1303`). All 7 tools fit at 375/390/430/834 DEFAULT, but at zoom-1.3 Recenter clips off behind a bare divider (zero indicator); at zoom-2.0 only ~3.5 tools show (search/help/filter/"1-"), category+refresh+recenter gone with no affordance/gradient/count/peek. Truly horizontally scrollable + tools genuinely off-screen + NO indicator — confirmed. Fires only in conditional geometries (DT stress, code-inferred ~320pt Split View) but those are real and the failure is invisible-by-design; last tools drop first (Recenter = the documented locationless→CONTRIBUTE entry `:2085`). Keep HIGH. No corrections.

### L1-2 (native guest funnel) — **CONFIRMED · keep HIGH** [Opus 4.8]
Funnel order confirmed no-bypass (`App.tsx` Gate `:139-148`: native + signed-out + `guestMode===false` → `<SignInScreen>` after "Open the Map"). Guest copy verbatim (`SignInScreen.tsx:238-243`: "Browse without an account →" + hint "Reporting flags requires an account" + note "you'll need an account to report or verify"). Amnesia holds: `guestMode`=in-memory `useState(false)` (`App.tsx:110`), only AsyncStorage key is `@accessmap/onboarded_v1`, no guest flag anywhere; Supabase persists signed-in sessions only → returning guest re-hits the wall. **CORRECTIONS:** (1) copy is HALF-wrong — anon reporting IS live for native guests (`createAnonFlag` real `.insert()` `flags.ts:1270`; RLS `"flags anon insert"` policy from `migrations/2026-05-30_anon_flag_reporting_photo_fix.sql:100-106`, survived the 06-01 consolidation), so "report" is false BUT "verify" is TRUE (auth-only). State it half-wrong so a reviewer can't refute the whole leg on the true half. (2) provenance slip: L1-2 cites the anon policy in `2026-05-29_…` but it's the `2026-05-30_…` file. HIGH earned by compounding (walled CTA + wrong copy + amnesia).

### L1-3 (location consent slide) — **CONFIRMED · keep HIGH · reframe REJECTED** [Opus 4.8]
Probe-verified slide-3 (ungranted first-run) shows exactly three controls: "Skip" (top-right), "Back" (dimmed), "Allow Location" (primary) — **NO "Continue", NO "Maybe later"**. The `permission ? 'Continue' : 'Next'` recollection was wrong: "Continue" (`OnboardingCards.tsx:468,477`) renders ONLY in the else-branch, which for a permission slide needs `currentGranted===true` (a RETURNING granted user) — a first-run user never sees it. So "the only permission slide without a visible not-now" is CORRECT as stated for first-run; the reframe fails. "Maybe later" is notifications-only (`:216 showMaybeLater = permission==='notifications' && …`). Web theater CONFIRMED: `handlePermissionAction :243-247` `if (Platform.OS==='web' || currentGranted===true){goTo(index+1);return}` — advances with zero permission call. **CORRECTIONS:** (1) tighten headline to "in the ungranted first-run state, the only forward action is 'Allow Location'." (2) credit "Skip" as a real (costly, all-or-nothing) decline. (3) the two legs are severable + unequal — the web-theater leg is load-bearing; lead with it. HIGH defensible (MEDIUM defensible on the missing-decline leg alone).

### L2-2 / L8-6 (two header families) — **CONFIRMED · keep HIGH** [Opus 4.8]
Family map code+PNG verified: **Editorial** (Home/Tasks) = `headerShown:false` (`RootNavigator.tsx:328,335`) + in-body `ScreenHeader` = uppercase eyebrow + display-40 + 44px WHITE CIRCLE buttons + icon-only feedback. **Nav-header** (Profile/FullMap/Settings) = shared Tab.Navigator header `headerTitleAlign:'center'` bold-16 + 36px ROUNDED-SQUARE hamburger + "Feedback" TEXT pill. PNGs of Map/Settings/Profile-signedout pixel-identical in this family. **Double-header REAL at HEAD** (code-inferred, auth-gated): Profile route has headerLeft only (no `headerShown:false`) → centered "Profile"; ProfileScreen signed-in branch unconditionally renders `<ScreenHeader eyebrow="PROFILE">` (`:878-887`) → both fire (not in the signed-out capture, which is behind the `!user` return — partial labels this honestly). **RATIFICATION: NONE** — searched Phase 7a/7b, DESIGN.md, GLASS.md; the split is an INCOMPLETE Phase-7b migration (7b rolled ScreenHeader to Profile's body but never removed the nav header), not ratified tiering. Keep HIGH (double-title on a primary auth surface + seam on every tab switch). No corrections.

### L8-5 / L2-3 (flagship reads embedded-not-built) — **CONFIRMED (web-scoped) · keep HIGH** [Opus 4.8]
All three in-scope slices confirmed: **dark-tiles** — `OSM_URL='…/dark_all/…'` (`PlatformMap.web.tsx:531`) single unconditional const, no theme branch; light-mode map + Home peek render near-black. **attribution** — full-width gray strip (Ukraine glyph + "© CARTO" clipping) across the map bottom + through Home's peek; finding correctly says style/condense NOT remove (legally required). **chrome unstyled** — ZERO Leaflet CSS overrides in `src/` (grep clean), raw white zoom rects. **CURRENCY ATTACK answered:** the Map report's "basemap comment fixed" (line 86) was about `HeatmapLegend.tsx:61` (the pinned-light legend floor), NOT the tile URL — the `dark_all` comment "predating light mode" is still present + unconditional; the glass wave scoped web tile-theme OUT (fixed only app-owned chrome). Web-only but web IS the only guest surface (first-contact experience, not dev convenience); native renders none of this by construction. Keep HIGH (web-scoped). No corrections.

### L3-4 (points flash lies on anon triage) — **CONFIRMED · keep HIGH** [Opus 4.8]
SQL mechanism verified against the LIVE-authoritative migration (`migrations/2026-05-30_trust_score_system.sql:138-198`; `schema.sql:115-175` mirrors; the 05-29 + 06-01 migrations only `ALTER…SET search_path`, never the body). Actor guard (`:186-188` == `schema.sql:163-165`): `IF actor_bonus>0 AND auth.uid() IS NOT NULL AND auth.uid() <> NEW.user_id` — for anon flags `NEW.user_id IS NULL` → `auth.uid() <> NULL` = SQL-NULL → branch skipped → **0 actor points, no point_events row**. Contrast confirmed: `schema.sql:351` uses NULL-safe `is not distinct from` in a triage RLS policy — the codebase knows the operator, so the actor `<>` is a genuine defect. UI chain: `points.ts:11-18` static table (not DB-derived); `TasksScreen.tsx:760` `isOwn={item.user_id===userId}` → `null===UUID` → false → flash `+3/+7`; `HelpModal.tsx:37` repeats the promise. **Reachability PROVEN (finding under-proved it):** `mineOnly` defaults false (`TasksScreen:186`), `TRIAGE_STATUSES=['open','verified']` (`:95`) includes where anon flags land, anon INSERT live → a signed-in user's default queue renders anon flags with full Verify/Resolve buttons → +3/+7 shown, DB awards 0; Profile diff-toast fires only on increase → silently absorbs, never corrects. No repo evidence of drift toward a fix (`2026-06-01_Gary_TrustScore_AnonReporting_Gate.md` even noted "anon flags have user_id=NULL" yet missed this). Fix = `auth.uid() IS DISTINCT FROM NEW.user_id` (propose-only migration, Sky applies) OR suppress the actor-flash when `item.user_id===null`. Keep HIGH.

### L8-4 (guest↔auth cliff) — **CONFIRMED all 3 slices · keep HIGH · SPLIT recommended** [Opus 4.8]
**(a) guest triage → fabricated error:** FlagCard `actions[]` (`TasksScreen:1528-1567`) gated ONLY on `flag.status`, NOT on `user` (the `useAuth()` user at `:132` is unused in action construction) → guests see full Verify/Resolve/Reject. Path: `onSetStatus→setStatus(:672)→updateFlagStatus(flags.ts:938)` → anon UPDATE hits NO `to anon` UPDATE policy (grep = zero; both policies `to authenticated`) → 0 rows → `maybeSingle()` null → `throw FlagStatusConflictError` → `TasksScreen:697` shows **"This flag changed / It was updated by someone else just now — refreshing the list."** — FABRICATED (nothing changed; RLS refused). The F53 message (designed for real stale-snapshot conflicts, `flags.ts:930-937`) is mis-reused for authz denial. **(b) docs contradict shipped gates:** `HowToHelpScreen.tsx:35` "Tap + Report on the map" (FAB auth-only), `:41` "Verifying earns you points" (fails for guests); `HelpModal.tsx:33` same; `SignInScreen.tsx:239,243` "Reporting flags requires an account" (half-wrong — anon report lives). **(c) dead sign-in bridges:** three `onPress={onClose}` (`ReportFlagModal:492,705,307`); MapScreen `onClose` (`:2104-2110`) only closes, no sign-in nav; on web no sign-in screen exists to reach. **MERGE CALL: SPLIT** — (a) is a shipped correctness bug (fabricated error on guest verify) that survives even a perfectly-sold cliff → track as its own finding **L8-4a** (gate FlagCard actions on `user` + honest "Sign in to verify", as bulk-watch already does at `:558`). (b)+(c) = the communication-cliff (L8-4). Both HIGH. Not CRITICAL only because no data corrupts + the anon *report* path works.

### L7-01 (no data-layer timeout) — **CONFIRMED · keep HIGH · wording refine + new copy defect** [Opus 4.8]
Probes: on web both surfaces reach the terminal error within 5–30s — Home t5 = em-dash title + 4 message-less skeletons, **t30 = "Couldn't load reports." + Try again card** (t70 identical → stable); Map t5 = "Loading flags…" no banner, **t30 = loadError banner fired** (`MapScreen.tsx:1901`) (t70 identical). Code: `supabase.ts:25-33` no custom fetch/AbortSignal/timeout; grep `AbortController|AbortSignal|Promise.race|setTimeout` across supabase/flags/flagsStore = only unrelated hits, while `location.ts:44-58` has the 15s GPS race — the asymmetry is real. HIGH (not CRITICAL) stands: the undifferentiated-middle window on the danger path (empty map reads as "no barriers") justifies HIGH; an eventual honest state does arrive. **CORRECTIONS:** (1) NEW copy defect — the Map error banner rendered raw **"Unknown error"**, NOT the friendly `errors.ts` "Check your internet connection…" copy; the friendly mapping isn't reaching the offline-abort path (worth its own small finding/fix). (2) tighten "can take a minute-plus" → clean-abort window observed ~5–30s on web; the minute-plus ceiling is the poor-signal/OS-socket case (NEEDS-SKY-DEVICE).

### L7-02 (offline honesty gaps) — **CONFIRMED all 3 · keep HIGH** [Opus 4.8]
(1) Guest exclusion — `flagsStore.tsx:244` (`doSwrPaint = …&& !!currentUserId`), `:299-300` (failure fallback gated on `currentUserId`), key user-scoped `:31` → guests/web get no cache (deliberate privacy, Jordan Condition 2 `:30` — only the SILENCE is the defect). (2) Age discarded — `readFlagsCache` returns `entry.rows` only (`:97`); `cachedAt` written (`:47,67`) never read; `copy.ts:11` single static string. (3) Home hides refresh failures — `HomeScreen.tsx:283` gate `error && flags.length===0`; `:176` ScrollView no RefreshControl (Tasks has one `:1163`, Map banner `:1901` row-count-independent — Home is the odd one out). No corrections.

### L7-03 (global page, no viewport re-scope) — **CONFIRMED · keep HIGH** [Opus 4.8]
Probe `map-probe__panned-empty` decisive: panned ~4 screens west to Slocan Lake, **0 pins / 0 clusters in viewport, pill still "5 flags nearby."** No geo predicate — `flags.ts:606-615`(limit 500)/`:652-671`(limit 50) both `.in('status').order('created_at')` only. No region callback — `PlatformMapProps` (`PlatformMap.tsx:33-55`) exposes none; native `ClusteredMapView` gets no `onRegionChange`; web `useMapEvents({zoomend,moveend})` (`.web.tsx:313`) only drives internal recluster; repo-wide grep `onRegionChange|onRegionChangeComplete` = NONE. `currentRegionRef` (`:448`) updates only on location-resolve (`:1197-1200`); Refresh (`:1400`) re-fetches the same global page. Realtime inversion confirmed (`flagsStore.tsx:495-497` discards the current view's new-flag events as "outside viewport"). Latent at 5-flag scale but silent-degrades-with-growth on the FIND loop (pin-absence reads as barrier-absence) → HIGH honest; native `onRegionChange` is a genuine gap (real work, not a one-liner).

### L6-04 (Tasks card actions nested in accessible parent) — **CONFIRMED · keep HIGH (ceiling)** [Opus 4.8]
`TasksScreen.tsx:1595-1607` card root = bare `<Pressable>` (accessible-by-default, no `accessible={false}`) carrying role/label/hint, wrapping the 4 `PressableScale` action buttons (`:1528-1590`). iOS flattening (an `accessible` container → one VoiceOver leaf) is the documented RN pattern → NATIVE-only: the web tree `tasks__light__390.txt:30-59` shows Verify/Resolved/Reject/Details as reachable nested buttons (RN-web doesn't propagate `accessible` as a flattening barrier). Finding tags `NEEDS-SKY-DEVICE (single most important VoiceOver device-check in the audit)` — the "likely" hedge is correctly disclosed. HIGH ceiling / MEDIUM floor: IF flattening holds on iOS, blind users lose Verify/Details from the primary triage surface (bulk long-press is the only SR fallback; card-tap opens Map not details) = locked out of the trust engine. No corrections.

### L6-05 (accessible list's only action dead-ends) — **CONFIRMED · keep HIGH** [Opus 4.8]
`MapScreen.tsx:2166-2181` `onSelectFlag` = `setNearbyOpen(false)` → `setFocusedFlagId` → `animateTo` → `setTimeout(showCallout,350)` — no focus move, no detail sheet; the accessible list is destroyed and focus lands nowhere managed (`setAccessibilityFocus` is a web no-op per L6-02; the Leaflet popup / RN-maps Callout is not a focus target). Row hint (`NearbyFlagsModal.tsx:137`) honestly says "Closes the list and centers the map." The fix endpoint is REAL + available: `FlagDetailModal.tsx` is SR-complete (`useFocusOnOpen:125`, `severityA11y:837`, `accessibilityViewIsModal:729`) — route `onSelectFlag`→FlagDetailModal when `screenReaderOn`. Keep HIGH (the best accessible surface dead-ends its only verb).

### L6-07 (pin boundaries fail on light tiles) — **CONFIRMED · keep HIGH · numbers reproduced exact** [Opus 4.8]
Re-ran `contrast-check.mjs` on `audit-stacks.json` (exit 1): white ring vs white tile **1.00:1**, sev1 **1.57**, sev2 2.15, sev3 2.78, sev4 3.61 PASS, sev5 4.35 PASS, anon gray 2.54 — every sub-3.0 row FAILs, matching the finding to 2 decimals. Rows tagged `1.4.11` — correct classification (a pin IS an interactive map target conveying severity by fill, no per-pin visible text label → not decorative-exempt). iOS-light-mode ONLY: web tiles are `dark_all` always (ring = 21:1 on the real web surface), web-unprovable — the finding says exactly that + NEEDS-SKY-DEVICE. HIGH (not CRITICAL): device-conditional + the Nearby-list twin carries the data non-visually, but it IS a real 1.4.11 boundary failure on safety-critical FIND targets. No corrections. The ratified 1px `#0F1B2D` hairline-union fix already exists on heat badges/clusters — never applied to pins (GLASS §12.4 gap).

### L5-07 (native Dynamic Type walls) — **CONFIRMED · keep HIGH** [Opus 4.8]
Per cap at HEAD: (1) tab labels `tabBarAllowFontScaling:false` (`RootNavigator.tsx:311`) — hard freeze on primary nav, no LCV implemented (NEEDS-SKY-DEVICE); (2) **NearbyFlagsModal meta `maxFontSizeMultiplier={1.4}` (`:166`)** on "Severity {n} · {status} · {time}" — the DECISION DATA of the map's accessible twin, capped while the description above scales free → inverts hierarchy = genuine 1.4.4 fail (this alone clears "≥1 essential on a core flow"); (3) SignIn inputs 1.4 (`:153,170`) + Input primitive 1.5 (`:108`) — user's own typed text, borderline. The finding is honest — it does NOT lump: accepts label 1.6 / heading 1.5 / display 1.3 and the redundant caps (cluster 1.2, RV dot 1.3 — confirmed decorative: `accessibilityElementsHidden` + severity in the row label, `RecentlyViewedRow.tsx:139,146`) as justified. Keep HIGH.

### L8-2 ("Verified" never defined/counted/dated) — **CONFIRMED · keep HIGH · 2 corrections** [Opus 4.8]
`LegendModal.tsx` defines 5 severities + 6 categories but ZERO status vocabulary — the designated vocab surface never defines "verified." The only definition ("another person checked the spot and confirmed…") lives in `HelpModal.tsx:41`, behind the drawer. Callout shows "SEVERITY 4 · VERIFIED" bare (no count/date/tap-through). The trust ledger is real + map-unreachable: `flag_verifications`(verifier_id/weight/created_at) + `flag_status_history` tables exist; `StatusHistoryModal` ("Foundational for trust") composes only inside `FlagDetailModal`, which `MapScreen` does NOT import; `NearbyFlagsModal` tap → callout only. **CORRECTIONS:** (1) the parenthetical "FlagDetailModal imported only by TasksScreen/ProfileScreen" is FALSE — it's imported by ~20 modules; the load-bearing claim (none map-callout/Nearby-reachable) survives — tighten. (2) "never dated" is CALLOUT-only — the Nearby list DOES show age ("verified · 29d ago"); tighten to "the callout omits date; no surface shows verifier COUNT." Keep HIGH (undefined core-trust word on a civic safety product).

### L8-3 (untrusted content wears full confidence) — **CONFIRMED · keep HIGH** [Opus 4.8]
Grep for report-content/flag-inappropriate/hide across `src/` = nothing on callout or list; callout's only control is X, `NearbyFlagsModal` rows only `onSelectFlag`. Reject exists solely on Tasks/`AdminScreen.tsx:122`; nothing links an encountered flag to triage. "BUMBAKLOT · Severity 5 · verified" renders with a full red sev-5 disc = identical authority to real barriers; "verified" is a status enum + points, no display-time quality gate. Correctly scoped as IN-scope UI HANDLING of untrusted content (counter-affordance absent at discovery), not the out-of-scope DATA problem. Immune response parked on an auth-gated tab with a paid one-tap trigger. No material corrections.

### L8-7 (anon pins lose severity color) — **CONFIRMED · keep HIGH · STRENGTHENED (both renderers)** [Opus 4.8]
Web `PlatformMap.web.tsx:362-363` verbatim (anon→`#9CA3AF` else `severityColor`). **Native is the SAME defect, not different:** `PlatformMap.tsx:228` `pinColor={f.user_id===null ? '#9CA3AF' : severityColor(f.severity)}` + anon `opacity:0.7` (`:230`) = doubly quieter. Pin carries NO other sighted severity signal — `pinGlyphSvg(category, resolved)` takes no severity arg (glyph is category-only / checkmark when resolved) → color is the sole sighted severity channel → gray fully erases it. SR keeps the truth both platforms (`alt`/`title` `:371-372`, native `accessibilityLabel:237` announce severity + "anonymous"). Gray-as-provenance defensible; erasing SEVERITY to show it is the defect (a ring/badge could carry provenance while the fill keeps severity). **CORRECTION:** re-scope title "web pin renderer" → **both renderers**; add native `opacity:0.7` as a second severity-muting factor. Tagged code-inferred + NEEDS-SKY-DEVICE (no live anon pin in the demo viewport). Keep HIGH.

---

## SKEPTIC PASS COMPLETE
36 canonical CRITICAL/HIGH findings adjudicated (3 on Fable pre-transition, 33 on Opus 4.8 post-transition). **0 REFUTED · 34 CONFIRMED · 2 ADJUSTED (L5-01 mechanism; L5-02 CRITICAL→HIGH demotion).** Structural outcomes: dedup A kept-merged (mount-hole + global pill, one defect + facet-b); L8-4 SPLIT → L8-4a (guest-triage fabricated-error, own correctness finding) + L8-4 (comm-cliff); L3-8 flagged softest-HIGH (MEDIUM-defensible); L3-10 scoped web-only (native coherent); L8-7 strengthened to both renderers. New sub-defects surfaced by skeptics: raw "Unknown error" copy on the Map offline path (L7-01); L1-2 provenance slip; L8-2 two over-claims tightened. Full correction ledger → §Calibration.
