# Fable Audit — AccessMap — Part 3: The Canonical Improvement Slate (v2 — RECONCILED)

**Subject:** AccessMap @ `main` `82e738bc177f8a0b14ca0aa978c6ffb92bc5c54b` (post-glass-chain HEAD). **Date:** 2026-07-04.
**Source of record (v2).** This is the reconciled slate the judges and the report writer read. It applies the skeptics' set-level verdicts to the frozen v1 (`slate-proposals.md`): 12 KEEP untouched, 8 FIX tightened in place (stable IDs + all 7 fields preserved — a FIX narrows scope, never adds a feature), 0 KILL. **Floor status: 20 survivors (≥10 ✓), 7 SIGNATURE-tier (≥3 ✓) — no KILL breached either floor, so no bench promotion was required.** The bench (11 entries, 0 promoted) and the 6-row PARKED-RECONCILIATION table are carried forward verbatim.

WCAG 2.2 AA is a hard floor never traded for polish; GLASS.md law is respected (the arbiter `contrast-check.mjs` decides floors — never the eye; blur intensities are only 12 or 24; `GlassSurface.tsx` is DO-NOT-EDIT and is threaded, never forked; the map overlay `pointerEvents="box-none"` gesture law and the virtualization law are untouched); RN/Expo hard-facts are honored (`announceForAccessibility`/`setAccessibilityFocus` are no-ops on web — no proposal "announces on web"; RN-web drops `accessibilityState.selected`); color/floor/severity changes adopt an existing ratified token and name an arbiter re-run; privacy/backend/scope halves are FORKS-TO-SKY, and each such proposal scopes only its UI half.

**Provenance note.** Part-2 verdicts (`partials/verdicts.md`, `02_findings.md`) were authored on Claude Fable 5; 33 of 36 skeptic verifications ran on Opus 4.8 after Sky directly switched models at Fable's credit exhaustion (disclosed, governance-clean). This Part-3 synthesis inherits those canonical findings unchanged. The v1→v2 reconciliation records each applied FIX under a **`⟢ RECONCILED (S{n} FIX)`** note at the head of the affected proposal so the diff from v1 is auditable.

**Reconciliation ledger (v1 → v2).**

| Proposal | Skeptic verdict | Applied in v2 |
|---|---|---|
| S1, S2, S3, S5, S9, S12, S13, S15, S16, S17, S19, S20 | **KEEP** | verbatim from v1 |
| **S4** | FIX | denied/undetermined split (gate banner on `status==='denied'` only); guard test pins denied-banner AND first-run-undetermined-no-false-claim; banner re-word kept (finding-job + web "browser site permission"); VERIFICATION notes the L3-8 announce leg is native-SR-only |
| **S6** | FIX | field (7) corrected to a MANDATORY arbiter re-run (GLASS §12 LIVE-BACKDROP); adopt ctaFill+textOnBrand / forceEngineered row chrome (opaque, invent no token); non-blocking scope-honesty note on adding a zoom method to the handle |
| **S7** | FIX | arbiter re-run MANDATORY over the light CARTO tile family (white ring fails by construction); adopt the ratified paired light+dark ring UNION if it fails; PROTECT citation corrected to GLASS §12 always-light-overlay (not PROTECT-9); S6/S7 zoom-control seam reconciled (drop the redundant Leaflet-zoom restyle) |
| **S8** | FIX | L2-15 close-affordance convergence constrained (keep programmatic names; do NOT flatten PROTECT-1 Nearby "Close" pill or PROTECT-3 sheet close); box-none gesture invariant written into the FullMap header constraint |
| **S10** | FIX | deliver via a PERSISTENT-MOUNTED aria-live region (FlashBanner conditionally unmounts today); mount the live region on the web/guest path (App.tsx guest branch has no FlashBanner); optional camera-center gated on reduced-motion; PROTECT-7/8/3 + scope unchanged |
| **S11** | FIX | HARD timeout-abort scoped to READS only; WRITES escalate via overlay (or pair abort with idempotency/dedup) to avoid a duplicate-flag under the anon rate limit; read/write split explicit in (1)+(6); guard test asserts a post-threshold write does not double-insert |
| **S14** | FIX | effort re-tagged **M** (native arm is design, not replication — bare `pinColor` system marker has no face); PROTECT-15 added (`tracksViewChanges={false}` + content-derived key) + PROTECT-16 (mode-independent light literal); web half scoped as parity-only; arbiter re-run + `#0F1B2D`-only kept; sequence with S1 in ONE native pin-renderer pass |
| **S18** | FIX | Label-in-Name (2.5.3): align the a11y name so the visible text is contained (visible "Submit report" + a11y "Submit report anonymously"); header-collision leg (item ③) made a hard dependency on S8 (or apply an equivalent shrink/truncate on the nav header directly) — label + banner-wrap legs ship standalone |

---

## How the five drafts collapsed (dedup ledger for this slate)

Five drafters over-produced 44 candidates across the eight lenses. The cross-cluster collisions — where two or three drafters independently proposed the *same* fix — are collapsed to one canonical each, with the stable `S{n}` ID frozen below. The Part-2 dedup map (`partials/calibration.md`) folds facets to canonicals *before* this step (L8-1→L3-2, L6-08/L6-10→L2-1, L8-6→L2-2, L8-5/L5-16/L1-6→L2-3, L3-9→L1-4, L3-3/L6-20→L5-01, L1-1/L7-04→L3-2, L6-03→L3-5, badge family→L8-9, L4-06→L5-06).

| Canonical proposal | Collapsed drafter candidates | Rationale |
|---|---|---|
| **S1** severity-grammar signature | arrival-trust-1 + material-4 + access-5 | Three drafters proposed "make severity speak number+word everywhere + define verified"; one signature. The anon-pin ring (L8-7) rides here. |
| **S2** textOnColor adoption (CRITICAL) | material-1 | Mechanical AA fix; kept distinct from S1 because it is the *contrast* half (the ink), S1 is the *grammar* half (the word). S1 sequences after S2. |
| **S3** FlagDetailModal doorway (SIGNATURE) | arrival-trust-2 + flows-device-3 + access-2 | Three drafters proposed wiring the SR-complete FlagDetailModal to the callout + Nearby row; one canonical (read-side only). |
| **S4** honest arrival (CRITICAL) | flows-device-2 + motion-perf-4 | Both proposed pill-copy honesty + the denied banner; flows-device-2 is the superset (adds banner-gating + web-accurate copy). |
| **S5** location parity on the Report pill (CRITICAL) | flows-device-1 | Sole owner of the L3-1 dead-end fix. |
| **S6** honest zoom (SIGNATURE) | flows-device-5 | Sole owner of L5-01 (the interaction mechanism). |
| **S7** claim the flagship map (SIGNATURE) | material-3 | Sole owner of the tile-theme + chrome half of L8-5; forks the zoom mechanism to S6. |
| **S8** one editorial header family (SIGNATURE) | material-2 | Sole owner of L2-2. |
| **S9** mount the a11y engine on web (SIGNATURE) | access-1 + access-1a + access-6 | The meta-candidate + its two carved facets; one canonical "mount the engine" with the two facets as named sub-parts. |
| **S10** submit confirmation | flows-device-4 | Sole owner of L3-5/L6-03. |
| **S11** data-layer timeout + "still trying" (SIGNATURE) | motion-perf-2 | Sole owner of L7-01. |
| **S12** web reduce-motion camera parity | motion-perf-1 | Sole owner of L4-01/L4-02-web/L4-04. |
| **S13** free Tasks card actions (native VoiceOver #1) | access-3 | Sole owner of L6-04. |
| **S14** pin hairline boundary | access-4 | Sole owner of L6-07; overlaps S1's anon-pin ring — sequenced together, both arbiter-gated. |
| **S15** first-run honesty copy sweep | arrival-trust-3 | Copy half of L1-2 + the minute-one contradictions. |
| **S16** two worst map targets (Clear + overflow) | flows-device-7 | Sole owner of L5-04 + L5-05. |
| **S17** contain the Home map peek | flows-device-8 | Sole owner of L5-06. |
| **S18** "Submit report" label + 200% reflow guards (CRITICAL) | flows-device-6 | Sole owner of L5-03. |
| **S19** location consent "Not now" + de-theater web | arrival-trust-7 | Sole owner of L1-3. |
| **S20** trust-fallback surfaces (Help/changelog/About) | arrival-trust-6 | Sole owner of L8-12/L8-13/L8-14-About. |

**20 canonical proposals survive** (0 killed, 0 backfilled). Ambition mix: **6 quick-wins (S)**, **11 meaningful (M)**, **3 larger (L)**; **7 SIGNATURE-tagged** (S1, S3, S6, S7, S8, S9, S11) — deliberate headroom over the ≥3 floor; every one survived the skeptic pass with its signature status intact. The runner-up **BENCH** (11 entries, 0 promoted) and the **6-row PARKED-RECONCILIATION** table follow the proposals.

---

# THE SLATE

---

## S1 — Wear the severity grammar everywhere severity is spoken (and define "verified" in the same breath) · ★ SIGNATURE

**Resolves:** L8-2 (UI half — the callout omits the word), L8-7 (anon pins erase the severity color), L2-7 (four severity grammars), L6-14 (bare "severity 4" in the SR label), L8-10 (partial — the coverage-caveat home) · facets: copy-index L2 "raw status enum leaks on Home", L3 "callout speaks numbers only", L8 "define verified in the legend."
**Effort:** M · **Tier:** Signature · **Signature:** YES · **Needs device:** partial (the anon-pin ring visual on iOS light tiles is NEEDS-SKY-DEVICE; the arbiter re-run is not).
**FORKS-TO-SKY:** the *verifier-count* and *callout-date* half of L8-2 depends on Sky-decision note #5 (trust model: how much verification provenance to expose). S1 takes only the parts that need no data decision — the severity WORD, the legend status-line, the anon ring. Count/date live in S3.

**(1) WHAT — vividly, mockup-ready.** The audit's own verdict is that AccessMap's signature is not the glass — it is the **severity grammar**: the numbered amber→red disc + plain word + one-line stake ("4 · Significant — impassable, needs a detour"), praised independently by all six blinded readers. Today it is spoken in full in exactly one place (the Report form's inline definitions) and truncated at every *decision* surface. This proposal makes the grammar the app's real signature by repeating it identically at the four surfaces that currently reduce it, following the GLASS §9 rollout-recipe shape (mockup → arbiter → staged build → report):
- **Mockup:** compose (a) the pin callout and each Nearby row with the severity WORD beside the number ("Severity 4 of 5 · Significant · Verified" visually *and* in the SR label via the existing `severityA11y` helper) so the number is never naked; (b) Home's Recent rows gaining the *number* (they show word-only today, and drop it even from the SR label, `HomeScreen.tsx:320-334`) and routing the raw lowercase DB enum through `STATUS_LABELS` (Home prints `item.f.status` "Minor · open" where every other surface uses the product label — R6 read "open" as business hours); (c) the anonymous pin keeping its severity FILL and carrying provenance as a **ring or dashed border** instead of the gray swap that erases severity (`PlatformMap.web.tsx:362-363` + `PlatformMap.tsx:228` both replace `severityColor` with `#9CA3AF`, native adding `opacity:0.7`); (d) the Map legend — the designated vocabulary surface, which defines all 5 severities + 6 categories but zero status words — gaining a one-line **Status block** ("Open = reported · Verified = another person checked the spot and confirmed the issue is real · Resolved = fixed") reusing the sentence the FAQ *already wrote*, plus a legend entry for the anon ring and the resolved checkmark.
- **Arbiter → staged build → report:** any color/ring choice for the anon variant is proven against `contrast-check.mjs` + `tools/audit-stacks.json` over the map's worst-case tile bases before build; then the four label sites + the legend block + the anon-pin ring land as separate small commits; then the rollout report.

**(2) WHY + finding IDs + mission + taste anchor.** Severity is THE trust datum of a barrier map. Resolving L2-7 (theme.ts:526's own law "each color is ALWAYS paired with a number + a word" is honored only on Tasks), L6-14 (the accessible twin says "severity 4" with no scale or word — R2: "the plain-language scale only exists on Tasks and the report form"), L8-7 (a sighted user reads an anonymous *impassable* barrier as quieter than an authed trivial one — the safety encoding inverted), and the L8-2/L8-10 legend gaps in one move. It serves the mission because a civic safety product's core datum must read identically on every screen a user crosses; and it is Sky's taste anchor exactly — the "memorable, ownable, in-ethos" signature the audit named, executed to completion. R1: "I could judge risk from this alone." R6: "exactly what the rest of the app was missing." A signature that is *more* accessible because it stops a color-only failure is the ethos of this app made literal.

**(3) DISABLED-USER IMPACT by persona.** **R1 (wheelchair, low-severity discrimination):** the anon-pin fix ends the mission-betrayal where an impassable anonymous barrier renders quieter than a trivial authed one — the exact misread that could route them into a curb with no ramp. **R2 (blind, SR):** the Nearby/Tasks/Home labels finally speak "severity 4 of 5, Significant" instead of a bare number they must have memorized the legend to decode — the accessible twin speaks its own vocabulary law. **R6 (first-timer, cognitive load):** the four-grammar decode tax that delayed their severity comprehension until the report form collapses to one grammar everywhere. **R3 (low-vision):** number+word redundancy means the datum survives even when one channel is hard to resolve. Whose job becomes possible: R2 can judge risk from the list alone; whose betrayal ends: R1's "quiet impassable barrier."

**(4) TRUST + COHESION.** This is the single strongest cohesion win on the *content* layer — the safety vocabulary stops fragmenting across screens. It repairs the deepest trust wound (an undefined "Verified" on a civic product) by defining it on the legend where every other word is defined.

**(5) ACCESSIBILITY NOTE (mandatory).** Improves the floor: adopts the existing `severityA11y`/`statusA11y` single-source phrase (PROTECT-4/12) rather than inventing labels; the anon ring restores a 1.4.11 non-text-contrast channel that the gray swap erased; the legend Status block is the first place "Verified" is defined for a cognitively-loaded or blind user. No AA regression — the discs already carry the number in the SR label; the word is *added*.

**(6) DEPENDENCIES / CONFLICTS.** **Preserves PROTECT-4** (extends the severity grammar — the crown-jewel signature — never a new token), **PROTECT-1** (the Nearby one-breath row: the word is added, the SR content untouched; anon pins still announce "submitted anonymously" via `alt`), **PROTECT-5** (LegendModal rows — additive Status block), **PROTECT-16** (the CategoryIcon/Wayfinder house style). **Sequencing:** lands *after* S2 (the discs that gain a word must already carry the ratified `textOnColor` digit). **Shared-file coordination:** touches `PlatformMap.tsx` + `.web.tsx` (anon pin) alongside S14 (pin hairline) and S6/S7/S17 — coordinate one map-renderer pass; touches `LegendModal.tsx` alongside S9 (legend a11y) and S20; touches `NearbyFlagsModal.tsx` alongside S3/S9/S16.

**(7) VERIFICATION.** **Arbiter re-run (required):** `contrast-check.mjs` + `tools/audit-stacks.json` with a stack for the anon-pin ring over the five map tile bases + red heat cell → exit 0 (never eye-tuned). **A11y-tree re-walk:** a Nearby row + Tasks card + Home row show "severity N of 5, {word}"; the legend renders a Status section. **Render states:** callout + list + Home + legend across light/dark. Anon-pin-on-light-tile *visual* stays NEEDS-SKY-DEVICE; the ring's contrast is arbiter-proven regardless.

---

## S2 — Adopt the ratified `textOnColor` ink at the six un-forked severity-digit sites (CRITICAL)

**Resolves:** L2-1 / L6-08 / L6-10 (canonical C; + parked item ①; arbiter §D-1/§D-2).
**Effort:** S · **Tier:** QuickWin (must-ship — the audit's #3 CRITICAL, a live WCAG AA breach) · **Signature:** no (mechanical law-extension, but the single most important material fix on the slate) · **Needs device:** no.

**(1) WHAT — vividly, mockup-ready.** The system already retired white-on-severity-fill once (`92a2be6`) and ratified the fix as a token: `severity[n].textOnColor` (`theme.ts:543-547`) puts ink `#0F1B2D` on fills 1–4 (Minor/Mild/Moderate/Significant) and keeps white only on sev-5 Severe red, with the AA rationale written into the comment (`:538-542`, verbatim in the code: ink measures 8.05/6.21/4.79 on those fills). SeverityBadge, the Map sev pills (`MapScreen.tsx:1690-1700`), and the action-bar quick chip already obey it. Following the GLASS §9 recipe (this one is arbiter-first, no mockup needed — it is a token swap): **swap** the hardcoded `textOnBrand`/`sevTextActive` white for `severity[s].textOnColor` at the seven render sites the arbiter measured white-on-fill at **1.57 / 2.15 / 2.78 / 3.61** (sev1–4, both themes, vs the 4.5 floor) — and flip the paired white Check glyph in ReportFlagModal to the same ink. Sites: `NearbyFlagsModal.tsx:140,144` (the disc list that auto-opens for every web guest), `LegendModal.tsx:66,70` (the decoder ring itself), `ReportFlagModal.tsx:607/637/1081-1084` (the *selected* severity button in the core CONTRIBUTE flow — a white "3" + white check on orange, and its comment falsely calls the fill "legible"), `ActivityFeedModal.tsx:156,162`, `RecentlyViewedRow.tsx:139,145` (= arbiter D-1, parked ①), `FlagDetailModal.tsx:1066,1076` (edit radio) + the site-6b view-mode "Severity {n}" chip at `:834,839`. No new token, no floor tuning, no geometry change.

**(2) WHY + finding IDs + mission + taste anchor.** This is L2-1/L6-08/L6-10, the audit's #3 CRITICAL and a shipped WCAG 1.4.3 breach on the safety datum. Three sites are guest-reachable (NearbyFlagsModal, Report selected chip, Legend); a sev-1 disc currently reads as a blank yellow dot on the one list every web guest sees, and the number a user just picked on the Report form is at its least legible the moment it is chosen. It is a direct contradiction of DESIGN.md's "born accessible… color is never the only signal." Sky's taste anchor: the fix *extends the app's own ratified law* rather than weakening it — the cleanest possible move.

**(3) DISABLED-USER IMPACT by persona.** **R3 (low-vision):** the severity number becomes legible on every disc — today it is sub-AA everywhere the fork was skipped. **R1 (wheelchair, risk-reading):** the datum they base a detour on stops disappearing into its own fill. **R2 (blind):** unaffected visually but the fix is paired with the number already in the SR label, so nothing is lost. Whose betrayal ends: R3's "I can't read the number on the yellow one."

**(4) TRUST + COHESION.** Makes the six holdouts finally obey the fork the crown-jewel already ratifies — the material system stops contradicting its own documented AA rationale.

**(5) ACCESSIBILITY NOTE (mandatory).** This IS the floor fix — closes a live AA breach. Every site already keeps a passing text twin nearby (Nearby's meta line, Legend's labels, Report's live label) and all discs are a11y-hidden with the number in the SR label, so information is never *lost*; the primary visual mark is what fails, which is why the mechanical adoption is safe and complete.

**(6) DEPENDENCIES / CONFLICTS.** **Preserves PROTECT-4** (makes the holdouts obey the ratified fork) and **PROTECT-5** (the arbitration system — never eye-tunes a floor). **Sequencing:** ships *before* S1 (S1's word rides on top of the corrected digit). **Shared-file coordination:** `NearbyFlagsModal.tsx` (with S1/S3/S9/S16), `LegendModal.tsx` (with S1/S9/S20), `ReportFlagModal.tsx` (with S5/S18).

**(7) VERIFICATION.** **Arbiter re-run (required):** `tools/audit-stacks.json` through `contrast-check.mjs` with a stack per site (disc / selected-chip / edit-radio over each severity fill) → exit 0; the transferable arbiter pairs already prove ink `#0F1B2D` = 8.05/6.21/4.79 on sev2–4, so the swap is provably AA. Extends the law — touches no floor (GLASS §7.1).

---

## S3 — The map pin becomes a doorway: surface the trust ledger where trust is spent · ★ SIGNATURE

**Resolves:** L3-12 (callout cul-de-sac), L6-05 (the accessible list's only verb dead-ends), L8-2 (UI read-half — callout omits date, ledger unreachable from the map), L8-3 (partial — the read side).
**Effort:** L · **Tier:** Signature · **Signature:** YES · **Needs device:** partial (native focus traversal into FlagDetailModal is NEEDS-SKY-DEVICE; the routing is code-verifiable).
**FORKS-TO-SKY:** Sky-decision note #5 (trust-model scope). Two dependencies: (a) whether to *display* a verifier COUNT is a trust-model call — if yes, the count rides the callout; if not, the ledger still surfaces the status *history* (already shipped) without a raw count. (b) L8-3's "report this looks wrong" counter-affordance (can guests flag content?) is a product+privacy decision — S3 scopes only the *read* side (surface the receipt); the *write* side forks to Sky.

**(1) WHAT — vividly, mockup-ready.** The cruelest finding on the trust axis: the app *already built* a full trust ledger — `FlagDetailModal` (SR-complete: `useFocusOnOpen:125`, `accessibilityViewIsModal:729`, `severityA11y:837`, a real "Reported on {date}" line `:926`, the photo gallery, and `StatusHistoryModal` "Foundational for trust" `:53`) — and then hid every one of them three taps deep behind Tasks→Details / Profile→My Reports, *never* reachable from the callout or Nearby list where a user actually decides to bet an outing on a badge. So "Verified" arrives as a word with no receipt, and the instant R1 met "BUMBAKLOT · Severity 5 · verified" the whole badge economy collapsed. **The fix is one wiring change with two entry points** (MapScreen does not currently import FlagDetailModal — a real integration, not a one-liner): (1) the pin callout (`PlatformMap.web.tsx:377-409` / `PlatformMap.tsx:239-277`) gains a real **"Open details"** affordance that opens FlagDetailModal for that flag, *and* the callout itself gains the `created_at`/`relativeTime` line so even the quick-glance path shows freshness (a year-old "verified" blocked path may be long cleared — the callout hides the age the Nearby list already shows); (2) **when the screen reader is on**, route the Nearby list's terminal verb `onSelectFlag` (`MapScreen.tsx:2164-2181`, which today only closes the list + centers the map onto an unreachable callout) to open FlagDetailModal instead — turning the accessible twin's dead-end into a doorway to the one surface that carries verifier provenance and a managed focus target. The sighted fly-to path is retained (open detail under SR, keep the callout otherwise). This single move cashes the "Open for details" / "Tap to view details" over-promise (L3-12) that today opens nothing, ends the accessible dead-end (L6-05), and surfaces the ledger read-half (L8-2/L8-3).

**(2) WHY + finding IDs + mission + taste anchor.** It resolves a HIGH cul-de-sac (L3-12), a HIGH accessible-dead-end (L6-05), and the read-half of two HIGH trust gaps (L8-2/L8-3) by *adopting an asset the app already built and hides* — the most in-ethos move available (DESIGN.md rewards owning what you shipped). It serves the mission because it is the difference between "this app finds barriers" and "this app helps me act on them": R1: "severity 4, verified, 'wheelchair users have to detour' — and then nothing… a dead end exactly where I need a next step." Sky's taste anchor: the signature move is a civic one no venue-rater in the competitive meta-table makes — making the central promise word *inspectable*.

**(3) DISABLED-USER IMPACT by persona.** **R1 (wheelchair):** the FIND decision step finally has a next action — open the receipt, judge freshness, decide the detour. **R2 (blind):** the app's best surface (the Nearby twin) stops dead-ending its only verb; under SR the list row opens a focus-managed detail sheet with a real heading instead of silently centering a map they can't perceive. **R6 (first-timer):** "Verified 29d ago" gives the badge a receipt, restoring the trust that collapsed at BUMBAKLOT. Whose job becomes possible: R2 can now traverse from list → detail → status history without leaving the accessible path; whose betrayal ends: R1's "dead end exactly where I need a next step."

**(4) TRUST + COHESION.** The deepest trust repair on the slate — the badge economy's provenance becomes reachable at the point of decision. Cohesion: the callout and list stop being terminal and join the app's modal-detail idiom.

**(5) ACCESSIBILITY NOTE (mandatory).** Improves the floor: routes the accessible list into a surface with `useFocusOnOpen` + `accessibilityViewIsModal` + `severityA11y` — a managed focus target replacing a focus-nowhere dead-end (the Leaflet popup / RN-maps Callout is not an SR focus target and `setAccessibilityFocus` is a web no-op per L6-02). It is a Modal (not an `announceForAccessibility` call), so it works cross-platform — no web no-op trap.

**(6) DEPENDENCIES / CONFLICTS.** **Preserves PROTECT-1** (adds an *endpoint* to the Nearby list, never touches row content — exactly the "fixes touch its endpoints" clause), the map overlay `pointerEvents="box-none"` gesture law (the affordance lives inside the callout, not the overlay), **PROTECT-3** (unaffected). **Sequencing:** independent of S1/S2 but naturally pairs with S1 (both make the FIND decision honest); if S9 ships, the SR-branch uses the same `screenReaderOn` signal. **Shared-file coordination:** `MapScreen.tsx` (imports FlagDetailModal — coordinate with S4/S5/S6/S16), `PlatformMap.tsx` + `.web.tsx` (callout — with S1/S7/S14/S17).

**(7) VERIFICATION.** No arbiter (no color). **Manual:** tap a pin → callout shows a date + an "Open details" control → FlagDetailModal opens with focus on its title. **A11y-tree/SR re-walk:** with SR on (web forces it true), selecting a Nearby row opens the detail sheet rather than centering the map; focus lands on the sheet title. Native focus traversal stays NEEDS-SKY-DEVICE.

---

## S4 — Honest arrival: kill the "N flags nearby" lie and surface the denied-location banner (CRITICAL)

**⟢ RECONCILED (S4 FIX — skeptic: sound proposal, one concrete condition).** The v1 draft said "when `initialLocationAction` returns `'clear'` for a denied/undetermined status, set the state that renders the banner." Taken literally that is a NEW first-run lie: `initialLocationAction` (`location.ts:78`, pinned by `location.test.ts:20-27`) collapses `'undetermined'` (first run, prompt deliberately deferred to onboarding — `MapScreen.tsx:1039-1042`) and `'denied'` into the *same* `'clear'` result, so gating the banner on the whole `'clear'` branch would show "Location access is off. Turn it on…" to a user who has NEVER been asked. **Applied:** (1) gate the arrival banner on the raw `status === 'denied'` ONLY — the raw `status` IS in hand in the mount effect (`MapScreen.tsx:1045`, `.then(({ status }) =>`) — OR use a status-neutral, first-run-safe message that does not assert the user denied access. (2) the new guard test must pin the banner for `denied` AND pin that a first-run `undetermined` arrival does NOT assert a false "access is off" claim, so undetermined can't silently regress. (3) keep the banner re-word (finding-job framing + web-accurate "browser site permission," not "device Settings"). (4) VERIFICATION states the L3-8 announcement leg benefits NATIVE SR only (web routes the honest ordering claim through the pill live region, not the dead `announceForAccessibility`). All conditions are UI-local; no data-layer change; every cited PROTECT item preserved.

**Resolves:** L3-2 (CRITICAL), L7-03 (copy half — the pill's proximity claim), L3-13 (viewport under-report), L3-8 (the softest HIGH — the "Sorted by distance" false announce, folded here as the same honesty theme) · facets: copy-index L3/L5/L7 "N flags nearby is the most load-bearing dishonest word in the app."
**Effort:** M · **Tier:** Meaningful · **Signature:** no · **Needs device:** partial (native first-run deny→arrival *feel* is NEEDS-SKY-DEVICE; the logic is platform-independent).
**FORKS-TO-SKY:** Sky-decision note #1 (PROXIMITY ARCHITECTURE). S4 takes the "stop claiming 'nearby' until the query can back it" branch — the UI-only half. Whether AccessMap adds bounded/`ST_DWithin` spatial queries + an `onRegionChange` re-fetch is Sky's data-layer call; S4 does NOT touch `flags.ts` fetch scope or the realtime viewport-gate inversion.

**(1) WHAT — vividly, mockup-ready.** Arriving on the Map with location denied or undetermined shows a citywide **San Francisco** map (`DEFAULT_REGION` 37.7749,-122.4194), zero pins in frame, and a pill asserting **"5 flags nearby"** — with no banner, no explanation, no recovery. R1 named this the single worst state in the app ("the one that would strand me… a flat lie"); for a Canadian app with flags in Kelowna, the SF default makes the wrongness maximally visible. Two mechanisms, two fixes, **neither of which touches the query**: **(a) Truthful copy.** The pill's `flags.length` is a global, geo-unbounded count (`flags.ts:652-671`, no lat/lng clause — the panned-empty probe caught "5 flags nearby" over a viewport with zero pins), so "nearby" is false in *every* state. Change the label to what is true — **"N reports loaded"** / **"Showing most recent"** — and let the list's "Sorted by distance / Showing most recent first" honesty carry the ordering claim. This one word is the highest-frequency trust hit in the audit. Also make the L3-8 open-announcement conditional on `location` (say "Showing most recent first" when unresolved, matching the honest *visible* banner at `NearbyFlagsModal.tsx:198-204` — a string-selection fix, NOT an announce-on-web no-op; see the reconciliation note — the SR *announcement* leg lands on native only, the visible string carries web). **(b) Reachable denied banner.** The well-written banner ("Location access is off…") exists but is gated on `permissionDenied`, set at exactly one site (`:999`, inside `requestLocation`) — so it only appears if the user manually taps "Recenter." The mount 'clear' path (`:1043-1061`) clears the spinner but never sets `permissionDenied`. **Fix (reconciled):** on the mount path, gate the banner state on the raw **`status === 'denied'`** captured at `:1045` — NOT on the whole `'clear'` branch (which also carries first-run `undetermined`, where the prompt is deliberately deferred to onboarding and asserting "access is off" would manufacture a fresh first-run lie). For the `undetermined` arrival, either render nothing or a status-neutral first-run-safe line ("Location isn't on yet — turn it on to see barriers near you") that does not claim the user denied anything. Re-word the denied banner itself — today "Turn it on in your device Settings **to report barriers near you**" is wrong twice: the blocked job is FINDING, and on web it is the *browser's* site permission, not device Settings.

**(2) WHY + finding IDs + mission + taste anchor.** L3-2 is CRITICAL #2 — the FIND surface lies on arrival for any location-cautious user (this app's core audience). It serves the mission's deepest law (a map that maps *disability* must never lie about coverage) and Sky's taste anchor (Home's honesty law extended to the Map pill — PROTECT-10). L3-13's "one pin under a 5-count" is the same defect's picture-half, resolved by the copy without a fit-to-flags feature.

**(3) DISABLED-USER IMPACT by persona.** **R1 (wheelchair):** the mission-betrayal — trusting "5 nearby" and finding nothing, or concluding the app is broken and quitting — ends; arrival now explains itself and points to recovery. **R6 (first-timer, denies location as a cautious disabled user would):** meets an honest empty state, not a wrong city under a false claim — and, critically, a first-run user who was never asked is NOT told they "turned off" access. **R2 (blind):** the pill is a persistent live region (reliably announces on web), so the honest string reaches them too; the L3-8 fix stops the SR announcement contradicting the visible banner (native SR — see the reconciliation note). Whose betrayal ends: R1's #1 stranding friction.

**(4) TRUST + COHESION.** Repairs the single highest-frequency trust hit in the audit. Cohesion: aligns the pill to the list's already-honest voice.

**(5) ACCESSIBILITY NOTE (mandatory).** Improves the floor: the L3-8 leg stops a false SR announcement (native); the banner-on-arrival gives blind/low-vision users the recovery path sighted users get. The pill's honest string reaches SR via the existing persistent live region (not a no-op). No color change. The denied/undetermined split is itself an honesty-of-status improvement — a first-run user is never told a false "you denied access."

**(6) DEPENDENCIES / CONFLICTS.** **Preserves PROTECT-6** (the locating fix — the mount change only *adds* a `status`-gated `setPermissionDenied` in the existing 'clear' branch, reading the raw status already destructured at `:1045`; the spinner-clear that closed the prior CRITICAL hang, and `location.test.ts`, are untouched — the returned `'clear'`/`'fetch'` contract of `initialLocationAction` is NOT changed, the branch merely reads `status` alongside it), **PROTECT-1** (aligns the pill to the Nearby list's honest no-location notice, never regresses it), **PROTECT-10** (extends Home's honesty law). **Shared-file coordination:** `MapScreen.tsx` (pill + mount path — with S3/S5/S6/S16), `NearbyFlagsModal.tsx` (the L3-8 announcement — with S1/S2/S3/S9).

**(7) VERIFICATION.** No arbiter (copy + state gating, no color). **Render states:** `states/map__*__permission-denied.png` reproduction → the banner renders on arrival for a **denied** status and the pill no longer says "nearby"; a **first-run undetermined** arrival shows NO "access is off" claim (status-neutral line or nothing); confirm the granted path still reads correctly. **Guard test (reconciled):** a new unit test pins that the mount path sets the banner state for `status === 'denied'` AND asserts a first-run `undetermined` arrival does NOT set the "access is off" banner (so undetermined can't silently regress into a false claim) — the RM-test lesson from S12 (pin the invariant so it can't silently drop). **SR leg scope:** the L3-8 *announcement* benefits native SR only — on web the honest ordering claim rides the pill's persistent live region (the visible string), not the dead `announceForAccessibility`. Native first-run *feel* stays NEEDS-SKY-DEVICE.

---

## S5 — Make the Report pill actually start a report (location parity on the Home-pill path) (CRITICAL)

**Resolves:** L3-1 (CRITICAL) · touches L3-5 finish-line context.
**Effort:** S · **Tier:** QuickWin (mechanical parity — the single highest-leverage fix in the cluster) · **Signature:** no (a bug fix, not a moment) · **Needs device:** no.
**FORKS-TO-SKY:** Sky-decision note #3 (guest contract) — but *only* the "should web expose sign-in / request location at onboarding" half. S5's core (make the pill kick a location read) is a pure correctness fix that stands on its own and does not wait on the fork.

**(1) WHAT — vividly, mockup-ready.** Today a first-time web guest taps onboarding's "Allow Location" (a pure web no-op, `OnboardingCards.tsx:243-249`), lands on Home, taps the advertised "Report" pill, and gets a sheet reading "Waiting for location…" over a permanently-disabled "Report anonymously" button — a false-promise status line above a dead submit, forever (skeptic proved byte-identical open vs t+25s, past the 15s GPS timeout). The mechanism is a one-line asymmetry: the FAB path (`MapScreen.tsx:2072`) fires `void requestLocation()` *before* `setReportOpen(true)`, but the Home-pill `openReport` handler (`:1094-1096`) only does `setReportOpen(true)` — it never kicks a location read, and the sheet has no location hook of its own (`location` is a pure prop, `:78`). **The fix:** on `openReport`, `if (!dropLocation) void requestLocation()` then open the sheet — that single call turns "Waiting for location…" into a real in-flight request that resolves to the coordinate line and enables submit. Pair it with two cheap honesty upgrades so the wait is never a lie: (a) if the request *fails or is denied*, the sheet's location line switches from "Waiting for location…" to an actionable line with an in-sheet **"Use my location" retry** (mirroring Home's affordance), so recovery lives where the user is stranded; (b) the disabled submit gains an `accessibilityHint`/visible reason ("Add a location to submit") so R2 and R6 aren't left guessing. This is the CONTRIBUTE half of the entire product becoming reachable for the exact anonymous cohort it was built for.

**(2) WHY + finding IDs + mission + taste anchor.** L3-1 is CRITICAL #1 — a first-time web guest literally cannot file a report, the CONTRIBUTE half of the product's whole point, dead on arrival for the anonymous cohort it was built for. It serves the mission at its most fundamental (the app's promise is "anyone can flag a barrier"). Sky's taste anchor: the fix is a *parity* fix — the FAB already solved this ("the FAB can never dead-end on web," the author's own comment `:1240-1243`); the Home pill simply never got the same call.

**(3) DISABLED-USER IMPACT by persona.** **R6 (first-timer web guest):** the mission-betrayal — the advertised Report button leading to a permanently dead form — ends; the flow completes. **R2 (blind):** the disabled-submit reason is now announced, so they know *why* the button is dead and how to fix it, instead of acting into silence. **R4 (limited dexterity):** the in-sheet retry means recovery doesn't require abandoning the flow and hunting a different affordance. Whose job becomes possible: R6 (and every guest) can finally file a report on web; whose betrayal ends: the anonymous cohort's dead-on-arrival CONTRIBUTE.

**(4) TRUST + COHESION.** Closes the audit's #1 CRITICAL. Cohesion: the two report entry-points (FAB, pill) finally behave identically.

**(5) ACCESSIBILITY NOTE (mandatory).** Improves the floor: the disabled-submit `accessibilityHint`/visible reason is a WCAG 3.3.1/1.3.1 improvement (the control now explains its state); the retry inherits the 44pt grammar. No color change.

**(6) DEPENDENCIES / CONFLICTS.** **Preserves PROTECT-3** (sheet architecture — does not move the footer or replace the severity buttons; only adds a location kick + retry inside the existing body), **PROTECT-8** (anonymity honesty set — the anon banner alert and Sign-in-outside-it stay), **PROTECT-6** (reuses `getCurrentPositionWithTimeout` / the locating spine — the retry routes through the same `requestLocation`, no new watcher). **Sequencing:** independent; pairs with S10 (the submit confirmation is the finish line of the flow S5 unblocks) and S18 (both touch the Report sheet's submit). **Shared-file coordination:** `MapScreen.tsx` (openReport handler — with S3/S4/S6/S16), `ReportFlagModal.tsx` (location line + disabled reason — with S2/S10/S18).

**(7) VERIFICATION.** No arbiter (no color). **Manual:** fresh web context WITHOUT geolocation grant → Home → "Report" pill → the sheet resolves to a coordinate and submit enables; deny the browser prompt → the location line offers an in-sheet retry and the disabled submit states its reason. **Regression:** the FAB path and the auth flow are unchanged.

---

## S6 — Give the map an honest zoom: app-styled 44pt zoom buttons in the overlay bottom · ★ SIGNATURE

**⟢ RECONCILED (S6 FIX — skeptic: rails require an arbiter re-run + token-adopt correction).** Two corrections applied, no scope widened. (1) **Field (7) now requires a MANDATORY arbiter re-run.** The zoom buttons are tappable app-owned controls floating over live map tiles — the GLASS §12 LIVE-BACKDROP case, the same obligation as the pin boundaries at `audit-stacks.json:63-69`. v1's "No arbiter (button chrome)" was wrong. (2) **Adopt existing ratified tokens, invent none:** pin the buttons opaque and reuse `ctaFill` + `textOnBrand` (`GLASS.md:77`) and/or the `variant="row"`/`forceEngineered` chrome the FAB/actionBar/statusPill already ship (opaque ⇒ tiles unreachable per the audit-stacks COVERAGE LEDGER); declare that reuse in `audit-stacks.json`. (3) **Non-blocking scope-honesty note:** wiring the buttons to zoom needs ADDING a zoom method to `PlatformMapHandle` (or wiring the web buttons to the existing Leaflet `mapRef` from `setMapRef`, which exposes `zoomIn`/`zoomOut`/`setZoom`; native `MapView` exposes `animateCamera`/`getCamera`) — additive, NOT a `GlassSurface`/primitive fork.

**Resolves:** L5-01 (CRITICAL) · facets: copy-index "status pill occludes the zoom control", L3-3 flow-framing.
**Effort:** M · **Tier:** Signature · **Signature:** YES · **Needs device:** partial (iOS single-pointer zoom-out + the pinch/VoiceOver map story are NEEDS-SKY-DEVICE; the buttons are code-verifiable).

**(1) WHAT — vividly, mockup-ready.** The defining device-integrity defect of the audit: an interactive control occluded at *every* device size. On web, Leaflet's default zoom buttons (26–30 CSS px, already sub-44) render top-left and are covered by the "5 flags nearby" pill and the wrapped action bar ("+" ~85% buried at 375/390/430, still buried at 834), and — worse — the skeptic proved taps die on the un-guarded `topRow` wrapper (NOT `box-none`), so *even the visible pixels are pointer-dead* except a ~6px fringe. On iOS there are no zoom buttons at all: zoom is pinch/double-tap only, and double-tap zooms *in* single-fingered — the real gap is single-pointer **zoom-OUT**, a WCAG 2.5.7 gesture-alternative failure for R4's exact population. **The fix (per the L5-01 fix-shape):** on web, `zoomControl={false}` on the `MapContainer` and render app-styled **44pt** zoom +/− buttons inside the overlay's *bottom* zone (thumb-reachable, away from the top pill), wired to the map's imperative zoom (see the reconciliation note — add a `zoomIn`/`zoomOut` method to `PlatformMapHandle`, or drive the existing Leaflet `mapRef.zoomIn/zoomOut` on web and `animateCamera` on native; additive, no primitive fork); on iOS, add the same app-owned 44pt zoom buttons (react-native-maps has none), giving the single-pointer zoom-out pinch alone can't. The buttons are pinned **opaque** and reuse the ratified `ctaFill` + `textOnBrand` (or the `forceEngineered`/`variant="row"` chrome the FAB/actionBar/statusPill already ship) — so no new token is invented and, being opaque, the live tiles never composite through them. Both live in the app's own chrome, obey the 44pt floor written into `a11y.minTargetSize` (`theme.ts:490`), and — critically — are mounted so their hit region is not stolen by the same un-guarded wrapper (the buttons go in a `box-none`-respecting group mirroring `overlay`/`overlayTopGroup` at `:1259/:1265`). This turns "the least native surface in the app" (R5) into one a one-handed user can actually operate.

**(2) WHY + finding IDs + mission + taste anchor.** L5-01 is CRITICAL #4 — zoom is locked out on the flagship map for one-handed / low-dexterity users. Zoom is how a wheelchair user reads block-level detail (is the ramp on this corner or the next?), so this is the mission's FIND loop made operable. Sky's taste anchor: an ownable "this map was built, not embedded" moment — a real, reachable, one-thumb zoom on the namesake screen.

**(3) DISABLED-USER IMPACT by persona.** **R4 (limited dexterity / one-handed):** the mission-betrayal — no accessible zoom fallback, "slivers smaller than a fingertip" — ends; a 44pt one-thumb zoom appears in the reachable bottom zone. **R1 (wheelchair):** can zoom to read block-level detail to plan a route. **R3 (low-vision):** zoom-in to enlarge is finally a reliable button, not an occluded 26px target. Whose job becomes possible: R4 can operate the map one-handed; whose betrayal ends: the pointer-dead, occluded zoom.

**(4) TRUST + COHESION.** Co-signature with S7 on the "one product" win — the flagship map stops looking like a third-party widget with dead controls and gains chrome the same hands built.

**(5) ACCESSIBILITY NOTE (mandatory).** Improves the floor on two axes: WCAG 2.5.5 (target size — 44pt vs 26px) and 2.5.7 (dragging/gesture alternatives — a single-pointer zoom-out where only pinch existed). The buttons are keyboard/Tab-focusable (the surviving path Leaflet already offered) *and* pointer-operable (the path that died on `topRow`). The opaque `ctaFill`/`textOnBrand` glyph pairing is the ratified AA ink — proven by the arbiter re-run below, never eye-tuned.

**(6) DEPENDENCIES / CONFLICTS.** **Preserves the GLASS.md gesture law** — the map overlay's `pointerEvents="box-none"` is untouched; the new zoom group is itself `box-none` at the container with only the buttons intercepting, so map pans still pass through (this fix *respects* the law by repairing `topRow`, which already violated it). **`GlassSurface.tsx` DO-NOT-EDIT** (buttons thread the primitive; the zoom-method add on `PlatformMapHandle` is additive, not a primitive fork — see the reconciliation note). No blur-budget change (buttons are opaque chrome, not blur panes). Do not touch the Leaflet attribution (S7 handles that). **Sequencing:** coordinate with S7 (same map surface, same `topRow`/overlay region) — land S6's `topRow` fix and S7's chrome-restyle in one map-overlay pass to avoid conflicts; S6's `zoomControl={false}` + native 44pt buttons **supersede** the Leaflet zoom control, so S7 drops its redundant `.leaflet-control-zoom` restyle (reconciled with S7). **Shared-file coordination:** `MapScreen.tsx` overlay + `PlatformMap.web.tsx` MapContainer + `PlatformMap.tsx`/`PlatformMapHandle` zoom method (with S3/S4/S7/S17).

**(7) VERIFICATION.** **Arbiter re-run (required — reconciled from v1's "No arbiter"):** run `contrast-check.mjs` against `tools/audit-stacks.json` with a stack for (a) the button edge/hairline vs the tile extremes `#000`/`#FFF` (1.4.11, min 3.0 — same obligation as the pin boundaries at `audit-stacks.json:63-69`) and (b) the +/- glyph vs its fill (1.4.3, min 4.5) → exit 0; state the tile-extremes window per GLASS §12 rule 3. Because the buttons are pinned opaque with `ctaFill`+`textOnBrand`, the tiles are unreachable through them (COVERAGE LEDGER) and the glyph pairing is the ratified AA ink — so the re-run confirms, not tunes. **Manual (web):** the zoom buttons are 44pt, bottom-anchored, fully clickable, and map pans still work (gesture law intact). **DevTools:** `zoomControl={false}` present, no orphaned `.leaflet-control-zoom`. iOS single-pointer zoom-out + pinch/VoiceOver map story stay NEEDS-SKY-DEVICE.

---

## S7 — Claim the flagship map: theme the tiles, tame the third-party chrome · ★ SIGNATURE (web-scoped)

**⟢ RECONCILED (S7 FIX — skeptic: arbiter mandatory + PROTECT citation + S6 seam).** Four corrections, no scope widened. (1) **The arbiter re-run is MANDATORY, not "conditional,"** and scoped to the map-WORLD inks over the NEW light CARTO tile family: run `contrast-check.mjs` against `tools/audit-stacks.json` with the light tile family as a base regime covering (a) the 2.5px white pin ring, (b) each severity pin fill + the `#9CA3AF` anon fill, (c) cluster + heat-badge boundaries — all 1.4.11 min 3.0 vs the light-tile luminance window (GLASS §12 rules 3-4) → exit 0. The white pin ring (`PlatformMap.web.tsx:122`) fails on near-white tiles by construction (`audit-stacks:63` declares white ring vs `#FFFFFF` = 1.0:1), so this is **required, not optional** — v1's "conditional / overlay-ink" scope missed it. (2) **If the white ring fails on light tiles, adopt the already-ratified paired light+dark ring UNION** (the cluster/heat-badge union mechanism at `audit-stacks:13`; GLASS §12 rule 4) — never eye-tune, invent no new token (PROTECT-5 / GLASS §7.1). (3) **PROTECT citation corrected:** the preserved crown jewel is the **GLASS §12 always-light-overlay discipline** (legend/locating-banner/place-chip literals), NOT PROTECT-9 (which is web-as-guest-mode, per `protect-merged.md`); real PROTECT-9 is also untouched. (4) **S6/S7 seam reconciled:** S6's `zoomControl={false}` + native 44pt buttons supersede the Leaflet control, so S7 keeps ONLY the tile-theme + attribution restyle and **DROPS** the redundant `.leaflet-control-zoom` restyle (v1 item 2 targeted a control S6 removes).

**Resolves:** L8-5 / L2-3 (canonical I) · absorbs the dark-void-in-light-Home slice (L1-6) and the attribution slice (L5-16) · pairs with the Home peek (S17) for the peek instance.
**Effort:** M · **Tier:** Signature · **Signature:** YES (co-signature with S8) · **Needs device:** partial (the iOS light-tile visual is NEEDS-SKY-DEVICE; iOS uses Apple tiles that follow the OS and is unaffected by this web fix).
**FORKS-TO-SKY:** the *zoom-lockout mechanism itself* (taps dying on the un-guarded `topRow`, the iOS no-zoom-out gap) is canonical L5-01 = **S6**. S7 scopes only the *material/occlusion half* (theme + attribution restyle); the pointer-dead `topRow` fix and the zoom-control removal are S6's.

**(1) WHAT — vividly, mockup-ready.** The namesake screen is the one place the app "feels embedded, not built" (R5) and the *first* surface every web guest touches. The web-scoped defects, all real-and-shipped (native renders none by construction). Following the GLASS §9 recipe (mockup → arbiter → staged build → report): **Mockup** the fixes — (1) the CARTO basemap is `dark_all` **unconditionally** (`PlatformMap.web.tsx:531`, a single const, no theme branch), so in light mode the map — and Home's dark map-peek — render near-black (the "failed render / black hole" three personas hit; R6's session-ending #1): branch the tile URL on `color.scheme` so light mode gets a light-family CARTO basemap; (2) the full-width Leaflet/OSM/CARTO attribution strip (Ukraine-flag glyph + blue links) crosses the map bottom untouched: style/condense it to the app's hairline voice — **kept, not removed** (OSM/CARTO attribution is legally required; this is a restyle, never a deletion). **(Reconciled: the raw Leaflet zoom-button restyle is DROPPED — S6 removes that control via `zoomControl={false}` and ships app-styled 44pt buttons instead.)** **Arbiter → staged build → report:** the light-family tile choice triggers the MANDATORY re-run above — the pin ring/fills/cluster/heat boundaries are re-checked against the light-tile luminance window, and if the white pin ring fails (it does by construction on near-white tiles), the ratified paired light+dark ring UNION is adopted; the pinned-always-light overlays (legend, locating banner, place-chip literals) are AA-by-construction and stay; then the tile branch + attribution restyle land as separate commits; then the report. The result: the flagship stops looking like a third-party widget dropped into a premium shell.

**(2) WHY + finding IDs + mission + taste anchor.** L8-5/L2-3 is HIGH and web-scoped, but web IS the only guest surface — this is the first-contact experience, not dev convenience. R5 ranked it friction #1 ("passion project not audited product"); R6 nearly quit at the black hole. It serves the mission (a *map* product's map must read as owned) and Sky's taste anchor (the Deep Field system carried onto the one surface that escaped it).

**(3) DISABLED-USER IMPACT by persona.** **R3 (low-vision):** theming the tiles is what makes the light-mode map *legible at all* — a near-black map in light mode is a contrast catastrophe for them; and the arbiter re-run guarantees the pins stay visible on the newly-light tiles (the union ring if needed), so the theme change doesn't trade one contrast failure for another. **R6 (first-timer):** the black-hole first impression that nearly ended the session is gone. **R1 (wheelchair):** a legible, owned map is the surface they read routes on. Whose job becomes possible: R3 can actually see the light-mode map *and* the pins on it; whose betrayal ends: the "failed render" black hole.

**(4) TRUST + COHESION.** Co-signature with S8 — the strongest "one product" win on the FIND half (S8 owns the chrome half). The flagship reads as a screen the same hands built.

**(5) ACCESSIBILITY NOTE (mandatory).** Access-positive: theming the tiles restores light-mode map legibility (a de-facto contrast fix); the MANDATORY arbiter re-run over the light tile family is the floor guard — the pin ring/fills/cluster/heat inks are re-proven at ≥3.0 against the light-tile window, and the ratified union ring is adopted if the white ring melts (never eye-tuned). No AA floor is traded — the pinned-always-light overlays (legend, locating banner) are untouched and stay AA-by-construction over any tile (GLASS §12 always-light discipline).

**(6) DEPENDENCIES / CONFLICTS.** **Preserves the GLASS §12 always-light-overlay discipline** (the pinned always-light *overlay* strategy — legend/locating-banner/place-chip literals stay exactly as-is; this themes the *basemap*, never the app's own always-light overlays) — corrected from v1's mistaken PROTECT-9 citation; **real PROTECT-9 (web-as-guest-mode) is also untouched.** **Preserves PROTECT-5** (the arbitration system — the union ring is adopted only on the arbiter's word). **Sequencing:** coordinate with S6 (same map surface — one overlay pass; S6 owns the zoom control, so S7 does NOT restyle `.leaflet-control-zoom`), S1/S14 (the pin ring/anon-ring inks re-checked over light tiles compose with S1's anon ring + S14's severity hairline — one arbiter pass), and S17 (the Home peek instance inherits the light tile + suppressed attribution). **Shared-file coordination:** `PlatformMap.web.tsx` tile const + Leaflet attribution CSS (with S3/S6/S17), and the Home peek instance (S17).

**(7) VERIFICATION.** **Arbiter re-run (required — reconciled from v1's "conditional"):** `contrast-check.mjs` + `tools/audit-stacks.json` with the light CARTO tile family as a base regime covering the white pin ring, each severity fill + the `#9CA3AF` anon fill, and the cluster + heat-badge boundaries — all 1.4.11 min 3.0 vs the light-tile luminance window → exit 0; if the white ring fails (it does by construction), the paired light+dark ring UNION is adopted and re-checked. The always-light overlays need no re-run (unchanged). **Render states:** FullMap + Home peek in light and dark; confirm the near-black is gone in light, dark is unchanged, and pins remain visible on the light tiles. iOS light-tile visual is NEEDS-SKY-DEVICE (iOS unaffected by this web fix).

---

## S8 — One editorial header family across every tab · ★ SIGNATURE

**⟢ RECONCILED (S8 FIX — skeptic: constrain the L2-15 rider + write the box-none Map-header invariant).** Two constraints applied, no scope widened. (1) **The L2-15 close-affordance convergence is constrained:** every close control must keep a programmatic accessible name, converge on the labeled / circle-wash idiom, and NOT flatten **PROTECT-1**'s labeled Nearby "Close" pill (`NearbyFlagsModal.tsx:188-194`) or touch **PROTECT-3**'s ReportFlagModal sheet close — L2-15 is LOW and a rider; it must not become a lever that regresses a crown jewel. (2) **The box-none gesture invariant is written into the FullMap header constraint:** MapScreen is full-bleed `<PlatformMap>` (`:1247`) with a `box-none` overlay (`:1259`) and NO scroll body, so the editorial-header treatment on Map must either converge chrome grammar only (hamburger→circle, Feedback→icon) OR place any in-overlay title such that no `box-none` region becomes touch-opaque (map pan/zoom must still pass through) — resolve the exact Map layout in the mockup / Design-Compiler stage, NOT by dropping a display-40 scrolling ScreenHeader onto the map canvas.

**Resolves:** L2-2 / L8-6 (canonical H) · absorbs the "double title on signed-in Profile" facet · touches L2-15 (close-affordance grammar) as a sibling normalization (constrained — see the reconciliation note).
**Effort:** M · **Tier:** Signature · **Signature:** YES · **Needs device:** partial (the double-header is code-inferred/auth-gated — needs an authed session or a composited lab mockup to *see*; the family split itself is fully capture-proven).

**(1) WHAT — vividly, mockup-ready.** This is the seam a user crosses on *every* tab switch and the strongest single "two kits stitched together" signal in the app (R5 friction #2; skeptic CONFIRMED HIGH, no ratification — an incomplete Phase-7b migration). AccessMap ships two navigation architectures at once: Home/Tasks wear the **editorial** header (`headerShown:false` + in-body `ScreenHeader`: uppercase eyebrow + display-40 title + 44px WHITE-CIRCLE actions + icon-only Feedback), while Profile/FullMap/Settings wear the **shared nav** header (centered bold-16 title + 36px rounded-square hamburger + a "Feedback" TEXT pill). The same drawer trigger is drawn two shapes/sizes/finishes; the same Feedback action is an icon on two tabs and a labeled pill on the rest; and on **signed-in Profile the two families literally stack** (the nav bar's centered "Profile" directly above the in-body "PROFILE" eyebrow — a redundant double title on a primary auth surface). Following the GLASS §9 recipe: **Mockup** the editorial `ScreenHeader` on Profile/FullMap/Settings (the `ScreenHeader` docstring already declares the intent: "Profile / Leaderboard / future screens consume it for one consistent type rhythm," `:8-10`); **staged build:** roll `ScreenHeader` onto those three, set `headerShown:false` on those routes so the redundant nav bar disappears (killing the double title outright), unify the drawer trigger to ONE shape/size/finish and the Feedback affordance to ONE treatment (icon-in-header) everywhere, and while the family is unified, converge the modal close-affordance grammar (X-in-circle-wash vs bare-X vs bordered "Close" pill — L2-15) on a single close idiom **that keeps every close control's programmatic name and does NOT flatten the labeled Nearby "Close" pill (PROTECT-1) or the ReportFlagModal sheet close (PROTECT-3)** — see the reconciliation note. **On FullMap specifically:** the header treatment must respect the full-bleed map's `box-none` overlay — converge the chrome grammar (hamburger→circle, Feedback→icon) and, if any title is placed in-overlay, keep the map's pan/zoom passing through (no touch-opaque `box-none` region; no scrolling display-40 ScreenHeader on the map canvas). Resolve the exact Map layout in the mockup / Design-Compiler stage. **report.** The payoff: the app finally reads as ONE editorial product at the top of every screen.

**(2) WHY + finding IDs + mission + taste anchor.** L2-2/L8-6 is HIGH — the named strongest seam. It serves the mission indirectly (trust-through-polish — a coherent product reads as trustworthy) and is Sky's taste anchor precisely: the editorial eyebrow + display type is *AccessMap's chosen identity*, not a generic pattern, executed to completion instead of half-migrated (PROTECT-10 nominates the ScreenHeader auto-fit as the thing to extend, never replace).

**(3) DISABLED-USER IMPACT by persona.** **R2 (blind):** removing the double header removes a redundant, confusing title from the signed-in Profile traversal (heading-rotor users stop hitting "Profile" then "PROFILE"); the constrained close-affordance convergence keeps every close control's name, so no dialog loses its programmatic "Close." **R6 (cognitive load):** one header idiom means less to re-learn per screen. **R3 (low-vision):** consistent header type/size/position aids orientation. Whose job becomes easier: R2's Profile traversal; whose betrayal ends: the "two kits" incoherence a careful user reads as inattention.

**(4) TRUST + COHESION.** The single strongest cohesion win on the *chrome* layer (S7 owns the map half; S1 owns the content half). The seam on every tab switch closes.

**(5) ACCESSIBILITY NOTE (mandatory).** Improves the floor: carries **zero AA cost** (both header families' inks already pass the arbiter) while *improving* structure for SR users — removing the double header removes a redundant title; if S9's `accessibilityRole="header"` ships, the unified header is where it lands. The constrained L2-15 convergence is a **net a11y hold**: no close control may lose its programmatic name, and the labeled Nearby "Close" (PROTECT-1) + the ReportFlagModal sheet close (PROTECT-3) are explicitly out of scope. The FullMap `box-none` invariant guarantees the header unification never makes the map ungrabbable (WCAG 2.5.1 / gesture-law). A signature that regressed access would contradict this app; this one advances it.

**(6) DEPENDENCIES / CONFLICTS.** **Preserves PROTECT-10** (extends the ScreenHeader family exactly as the crown-jewel prescribes, using its auto-fit backstop, never forking a new header), **PROTECT-1** (the labeled Nearby "Close" pill is NOT flattened by the L2-15 convergence), **PROTECT-3** (the ReportFlagModal sheet close is untouched), and the **map overlay `pointerEvents="box-none"` gesture law** (the FullMap header treatment keeps the map's pan/zoom passing through). **Sequencing:** coordinate with S9 (the header-role a11y fix lands on the unified header) and S20 (the casing sweep touches the same titles); if S18's ScreenHeader auto-fit extension ships, it composes with this — and note that **S18's header-collision leg (item ③) is a hard dependency on this S8 migration** (the nav-header routes Map/Profile/Settings only get ScreenHeader's auto-fit once S8 moves them onto it). **Shared-file coordination:** `RootNavigator.tsx` (route `headerShown` + the nav-header `headerTitleStyle`/`headerRight` for the still-nav routes until migrated), `ScreenHeader.tsx` (with S9/S18), `ProfileScreen.tsx`/`SettingsScreen.tsx`/`FullMap` bodies, and the close-affordance sites (constrained — PROTECT-1/3 excluded).

**(7) VERIFICATION.** No arbiter (both inks already pass; named to show it wasn't disturbed). **Render states:** Home/Tasks/Profile/FullMap/Settings headers in light and dark read as one family; the signed-in Profile shows ONE title (device or composited-lab-mockup for the auth-gated double-header — PROBE-1); on FullMap, confirm the map still pans/zooms under the unified header (no touch-opaque `box-none` region). **A11y-tree:** the removed nav header no longer double-announces on signed-in Profile; every converged close control still exposes a programmatic name; the Nearby "Close" pill (PROTECT-1) and the sheet close (PROTECT-3) are unchanged.

---

## S9 — Mount the accessibility engine on web: adopt the modern RN ≥0.71 a11y dialect · ★ SIGNATURE

**Resolves:** L6-01 (CRITICAL), L6-02, L6-11, L6-16, L6-17 (page-heading half), L6-19 (SignIn modal containment — native, folded here as the a11y-correctness bundle), L6-21 (legend-backdrop flattening) · names the mechanism behind the web halves of L6-13, L6-18.
**Effort:** L (breadth — ~30 state sites + shim + shared-object edit + Modal labels; each edit mechanical/low-risk, but the surface is wide and every touched screen wants a re-verify) · **Tier:** Signature (contains a CRITICAL) · **Signature:** YES · **Needs device:** partial (native VoiceOver truth is unchanged by construction but NEEDS-SKY-DEVICE to confirm; the DOM/web behavior is in-harness verifiable).

**(1) WHAT — vividly, mockup-ready.** The app's accessibility layer is its single best-engineered subsystem — hooks, dual iOS/Android announce wiring, `accessibilityViewIsModal` across ~25 sheets, a centralized severity vocabulary — but it is written in the **pre-0.71 RN a11y dialect that `react-native-web@0.21.2` no longer translates.** Four of six a11y subsystems (selection-state, announcements, focus-move, decorative-hiding) are silently sheared off at the web bundler — and web is the ONLY surface a guest has. One **additive, seam-level** fix (four named facets, each independently shippable if the slate wants smaller bites):
- **(a) `aria-selected/checked/expanded/busy` at the ~30 state sites** [the L6-01 CRITICAL core]: the app sets `accessibilityState={{selected|checked|expanded|busy}}` (report category/severity chips, Tasks scope/category/sort, the whole map filter panel, the Nearby category tabs), but RN-web 0.21.2's `createDOMProps` reads only the *flat* modern props and drops the nested-object dialect — proven by the trees (the only `[selected]` anywhere is react-navigation's tab bar, which uses `aria-selected` directly, `BottomTabItem.tsx:353`). Add the flat `aria-*` prop **beside** the existing `accessibilityState` at each site (it maps to the same native state AND to the DOM). Note the L6-01 corrections: plain `disabled` DOES translate (leave it); the severity echo line IS a real live region so SEVERITY announces on web — only CATEGORY selection is unconfirmable, and it is what this fixes.
- **(b) the visually-hidden `aria-live` announce-shim** [L6-02]: `announceForAccessibility`/`setAccessibilityFocus` are *literally empty* in RN-web 0.21.2, so ~50 announce sites (auto-list count, "Report filed.", filter/sort/scope confirmations) are no-ops. Route the existing announce helper through one visually-hidden `aria-live="polite"` node on web — **call sites unchanged** (they already call the helper); the native dual-wiring stays.
- **(c) `aria-hidden` in `decorativeProps`** [L6-11]: the shared `decorativeProps` object (`accessibility.ts:14-18`) carries the OLD trio (`accessible:false` + `importantForAccessibility` + `accessibilityElementsHidden`) — none of which RN-web 0.21 honors — so every decorative image announces "image" (the Tasks screen *literally opens* on an anonymous "image"). Add `aria-hidden:true` to the shared object + let lucide icons take it — most sites fixed in one edit.
- **(d) `aria-label` per `<Modal>`** [L6-16] so dialogs stop landing SR users in anonymous containers.
- **Bundled native-correctness (one-line each, cheapest shipped together):** `accessibilityRole="header"` on ScreenHeader's title so heading-rotor navigation reaches the editorial screens (L6-17); `accessibilityViewIsModal` on the lone SignIn modal root (L6-19); the LegendModal backdrop made an absolute-positioned SIBLING (not a parent Pressable) so iOS can't flatten the decoder into one "Close" button (L6-21, same mechanism as S13).

**(2) WHY + finding IDs + mission + taste anchor.** L6-01 is CRITICAL #6 — a web SR user cannot confirm any category/filter selection on the only surface guests have. For a product whose promise is "born accessible," shipping the web app without its accessibility engine mounted is a *mission breach*, not a polish item — R2's "submitting blind in the worst sense." Sky's taste anchor: the best-engineered subsystem in the app is one bundler-dialect gap from working where guests live; making it *actually* accessible end-to-end on web is the most ownable, in-ethos signature this audit can produce.

**(3) DISABLED-USER IMPACT by persona.** **R2 (blind, web SR — the whole point of this proposal):** the mission-betrayal — submitting a report whose category they can't confirm, onto a defaulted form, acting into silence — ends; selection state, announcements, decorative-hiding, and dialog labels all mount. The Tasks screen stops opening on "image." **R6 (cognitive load, uses SR intermittently):** confirmations they rely on ("Report filed.", filter changes) finally speak. **R3 (low-vision + magnifier + SR):** heading-rotor reaches the editorial screens (L6-17). Whose job becomes possible: R2 can complete the CONTRIBUTE and FIND flows on web with confidence; whose betrayal ends: the four silently-sheared a11y subsystems.

**(4) TRUST + COHESION.** The deepest access repair on the slate — the app's promise made true on the surface that needs it most. Cohesion: the web and native a11y stories converge (same states, same announcements).

**(5) ACCESSIBILITY NOTE (mandatory).** This IS the floor fix at scale — WCAG 4.1.2 (name/role/value — selection state), 4.1.3 (status messages — the announce-shim), 1.1.1 (non-text — decorative-hiding), plus heading structure (2.4.6) and modal containment. No native regression: the flat props map to the same iOS/Android states the nested object already produced; the existing dual-wiring stays. RN/Expo hard-fact honored: this does NOT "announce on web" via the dead API — it routes through a *rendered* `aria-live` node (the mechanism that already makes the severity echo line work), not `announceForAccessibility`.

**(6) DEPENDENCIES / CONFLICTS.** **Preserves PROTECT-12** (this IS the "adoption, not redesign" the merged list nominates the hook suite for), **PROTECT-1** (touches the Nearby list's tab *state props* only, never row content), **PROTECT-3** (adds flat props beside the report vocabulary, never alters labels/liveRegion), **PROTECT-6** (the announce+liveRegion dual-wiring — the shim is the web leg, added not replaced). **Do NOT "fix" the web SR auto-open by removing it** (PROTECT-1 — keep the native auto-open). **Sequencing:** the S3 SR-branch and S13's flattening restructure use the same a11y model — coordinate; the ScreenHeader header-role (L6-17 half) lands on S8's unified header. **Shared-file coordination:** `accessibility.ts` shared object (with S13/access-adjacent), `ScreenHeader.tsx` (with S8), `LegendModal.tsx` (with S1/S2/S20), and the ~30 state-prop sites across `ReportFlagModal`/`TasksScreen`/`MapScreen`/`NearbyFlagsModal`.

**(7) VERIFICATION.** **A11y-tree/DOM re-walk (the L6 PROBE-REQUEST):** a shipped-DOM attribute dump (not ariaSnapshot) of the report form + filter panel for `aria-selected|checked|expanded|live|hidden|modal|label` per element, converting the code-verified claims to shipped-DOM proof; the Tasks tree no longer opens on a bare "image"; ScreenHeader title gains `heading`; the `aria-live` node updates on a filter change. **Arbiter:** touches no color token — the four glass proof-sets stay exit-0 (named to show contrast is undisturbed). Native VoiceOver (incl. SignIn containment, legend backdrop) stays NEEDS-SKY-DEVICE, unchanged by construction.

---

## S10 — Confirm the submit: a visible + live success banner on the CONTRIBUTE finish line

**⟢ RECONCILED (S10 FIX — skeptic: the live region must be persistent-mounted AND on the guest path).** Four conditions, no scope widened. (1) **Deliver the confirmation through a PERSISTENT-MOUNTED `aria-live` region, NOT the conditionally-unmounting FlashBanner as-is.** `FlashBanner.tsx:105` does `if (!rendered || !display) return null;` — it mounts into the DOM with the text already present, which is the exact case SRs frequently fail to announce and the opposite of S10's own "already mounted before its text changes" requirement. Either mirror the always-mounted severity-echo-line pattern (`ReportFlagModal.tsx:648-657`, the `accessibilityLiveRegion="polite"` node that stays mounted) or refactor FlashBanner so its `aria-live` wrapper `View` stays mounted (empty when idle) so **TEXT MUTATION, not node insertion**, is what the SR observes. (2) **Mount the live region on the web/guest path.** In `App.tsx` `Gate()`, only the signed-in `SignedInArea` mounts FlashBanner (`App.tsx:101`); the web/guest branch (`Platform.OS==='web' || guestMode → <RootNavigator/>`) has **NO FlashBanner** — so S10's declared audience (the anon-web cohort) never sees it. Lift the banner above the session branch (or mount an equivalent inside `RootNavigator`) or it never renders for its own audience. (3) **If the optional "center map on new pin" is taken, gate the camera move on reduced-motion** via the existing `animateTo` handle (respect PROTECT-7 map-camera RM posture); otherwise drop it. (4) Keep the verified PROTECT-7/8/3 commitments and the "No color / No arbiter" scope unchanged.

**Resolves:** L3-5 / L6-03 (HIGH, silent success on all four platform cells).
**Effort:** M · **Tier:** Meaningful (the emotional payoff of the flow; a strong supporting act to S5) · **Signature:** no · **Needs device:** partial (on-device VoiceOver announcement timing is NEEDS-SKY-DEVICE; the web live-region behavior is in-harness verifiable).

**(1) WHAT — vividly, mockup-ready.** The CONTRIBUTE flow's terminal step ends with the sheet simply closing. Skeptic confirmed "confirms nothing" holds all four cells: web-anon silent, web-auth silent, native-anon haptic-only, native-auth SR-hears-only. The only `notify()` on either path is the *failure* branch (`:329/:424`); `hapticNotify` is native-only; `announceForAccessibility('Report filed.')` is a web no-op; `onCreated` (`MapScreen.tsx:2111`) merely `refreshFlags()` and lands the user on the stale Nearby list — not even centered on their new pin. R6, holding the enabled button: "nothing tells me who sees it… or what happens next." Unconfirmed submits breed doubt-resubmits, which the 5/day anon rate limit (`anonRateLimit.ts:4-5`) then punishes. **The fix uses an idiom the app already owns, made SR-safe (reconciled):** surface a **visible** success confirmation on submit for everyone ("Report filed — thanks for flagging this barrier") that is *also* a **persistent-mounted `role=alert`/`aria-live` region** — the wrapper `View` stays mounted (empty when idle) so the SR observes a TEXT MUTATION, not a node insertion (the reason the always-mounted severity echo line works on web today, and the reason the current FlashBanner — which returns `null` until it has text — does NOT reliably announce). Mount that live region **above the session branch** (or inside `RootNavigator`) so the web/guest cohort — S10's whole audience — actually gets it (today FlashBanner mounts only in `SignedInArea`). Optionally center the map on the new pin so the user *sees* their contribution land — **but only with the camera move gated on reduced-motion** via the existing `animateTo` handle (PROTECT-7); otherwise drop the camera move. Because web IS guest mode, this is the first time the anonymous cohort gets any signal that the app's whole point just happened.

**(2) WHY + finding IDs + mission + taste anchor.** L3-5/L6-03 is HIGH — the CONTRIBUTE finish line confirms nothing. It serves the mission (the emotional close of "I helped" is what brings contributors back) and Sky's taste anchor (reuse the app's own FlashBanner/flash-pill idiom — refactored to the persistent-mount mechanism the severity echo line already proves — not a new pattern).

**(3) DISABLED-USER IMPACT by persona.** **R2 (blind, web):** the first time the anonymous cohort's SR gets any signal the submit succeeded — via a persistent-mounted live region on the guest path, not the dead API and not a node that inserts-with-text (which SRs miss). **R6 (first-timer):** the black-box submit ("nothing tells me what happens next") gets a visible, honest close. **R4 (dexterity):** the confirmation prevents the doubt-resubmit that the rate limit would punish. Whose job becomes possible: R2 hears the submit land; whose betrayal ends: the silent finish that breeds punished resubmits.

**(4) TRUST + COHESION.** Closes the loop on the app's central action. Cohesion: the submit joins the FlashBanner idiom (on the persistent-mount mechanism).

**(5) ACCESSIBILITY NOTE (mandatory).** Improves the floor: WCAG 4.1.3 (status message) via a *rendered, always-mounted* live region — the RN-web-safe mechanism, explicitly NOT `announceForAccessibility` and explicitly NOT a mount-with-text-present node (which SRs frequently drop). The banner announce is decoupled from motion (announce even when `reducedMotion`, exactly as FlashBanner already does — PROTECT-7); the optional camera-center is RM-gated. No color change.

**(6) DEPENDENCIES / CONFLICTS.** **Preserves PROTECT-7** (RM discipline — announce decoupled from motion; the optional camera-center is RM-gated via `animateTo`), **PROTECT-8** (anonymity honesty — the copy must not over-claim; "your report is public and others can verify it" is the honest frame), **PROTECT-3** (sheet architecture untouched — the banner fires after `onClose`). **Sequencing:** the finish line of the flow **S5** unblocks — ship after or with S5; if S9's announce-shim ships, this banner can also route through it, but it must stand alone on the persistent-mount mechanism (do not depend on S9). **Shared-file coordination:** `MapScreen.tsx` (`onCreated`) + `App.tsx`/FlashBanner (with S5 — includes lifting the live region above the session branch so the guest path renders it).

**(7) VERIFICATION.** No arbiter. **Manual (web):** submit anon **on the guest/web path** → a visible banner appears AND a screen reader hears it (persistent-mounted live region whose text mutates — not `announceForAccessibility`, not a node that mounts with its text); confirm the failure branch still `notify()`s. **DOM check:** the `aria-live` wrapper is present (empty) before submit and gains text on submit; the region is mounted in the guest branch, not only in `SignedInArea`. **RM check:** if the camera-center is taken, it is skipped/instant under reduced-motion. Native haptic + SR path unchanged; on-device VoiceOver timing stays NEEDS-SKY-DEVICE.

---

## S11 — Data-layer timeout + honest "still trying" escalation (the danger-path silence fix) · ★ SIGNATURE

**⟢ RECONCILED (S11 FIX — skeptic: scope the HARD abort to READS only).** One condition, no scope widened. For **WRITES** (`createFlag`/`createAnonFlag`/`uploadFlagPhoto` at `flags.ts:797/1270/536`), do NOT abort the in-flight insert at the threshold — a request that already committed the row server-side but is slow to respond would get its client promise aborted, surface a false "still trying," and invite a doubt-resubmit = a DUPLICATE flag, which the anon 5/day rate limit (`anonRateLimit.ts`, per S10) then punishes. **Instead:** surface the "still trying — check your signal" escalation as an OVERLAY while the write continues, OR if a write must be abortable, pair the abort with server-side idempotency/dedup (client-generated idempotency key or short-window duplicate guard). Make the **read/write split explicit** in fields (1) and (6). Extend the field (7) guard test to assert a write resolving AFTER the escalation threshold does NOT double-insert. Leave the read-half exactly as v1 wrote it — it is clean.

**Resolves:** L7-01 (primary) + the L7-01 rider (Map offline banner renders raw **"Unknown error"** instead of the friendly `errors.ts` copy) + the "Loading flags…" copy observation (reserve for cold load; "Updating…" over live data).
**Effort:** M (a timeout race in the data layer mirroring the GPS one, plus a "still trying" state and one copy-mapping fix) · **Tier:** Meaningful (co-signature) · **Signature:** YES · **Needs device:** partial (the poor-signal minute-plus ceiling is NEEDS-SKY-DEVICE; the ~5–30s clean-abort window and the code gap are probe-proven).

**(1) WHAT — vividly, mockup-ready.** The GPS layer got a 15s hard race *specifically because* an unbounded await is unacceptable on these journeys (`location.ts:44-58`); the Supabase data layer never did (`supabase.ts:25-33` — no custom fetch, no `AbortSignal`, no `Promise.race`; grep-confirmed). On clean offline the honest terminal states fire in ~5–30s (probe-verified: Home settles to "Couldn't load reports." + Try again; Map fires its error banner). But on **poor signal** — one bar, packet loss, captive portal, the actual field case — the request can pend until the OS socket gives up (~60s iOS, minutes on web), and for that entire window the Map pill says "Loading flags…", Home shows an em-dash title + message-less skeletons, and a report submit sits on its spinner, with no escalation, no cancel, no time bound. R1 converted exactly this into the mission's most dangerous misreading: "an empty map reads as 'no barriers.'" **The fix is a bounded, escalating middle — split by read vs write (reconciled):**
- **READS** (flag list, profile, etc.): race every data read against a timeout (same shape as the GPS race), and at a threshold surface a calm **"Still trying — check your signal"** state (a real, persistent-mounted live region so it announces) with a manual retry, *before* the OS gives up — so "still working" and "dead" stop looking identical. A read may be hard-aborted at the ceiling with no downside.
- **WRITES** (`createFlag`/`createAnonFlag`/`uploadFlagPhoto`, `flags.ts:797/1270/536`): do NOT hard-abort the in-flight insert at the threshold — an insert that already committed server-side but is slow to ACK would be falsely reported as failed and invite a duplicate resubmit (which the anon 5/day rate limit then punishes). Instead surface the same "Still trying — check your signal" escalation as an **overlay while the write continues**; if a write must be made abortable, pair the abort with server-side idempotency/dedup (a client-generated idempotency key or a short-window duplicate guard) so a retry can't double-insert.

Bundle the two small copy defects the skeptic surfaced: route the offline-abort path through the friendly `errors.ts` mapping (kill the raw "Unknown error"), and split **"Loading flags…"** (cold) from **"Updating…"** (revalidating over data) so loading-from-nothing and refreshing-something read differently.

**(2) WHY + finding IDs + mission + taste anchor.** L7-01 is HIGH and R1's stated #1 fear — a danger-path honesty gap on a safety product (the undifferentiated-middle window where an empty map reads as "no barriers"). It serves the mission by making the app's *own documented principle* ("expo-location can hang indefinitely… never leave the caller on a spinner forever") true of the data layer too — the temporal-honesty spine the whole L7 verdict is built around. Sky's taste anchor: mirror the shape the app already chose for GPS; do not invent a new pattern.

**(3) DISABLED-USER IMPACT by persona.** **R1 (wheelchair, field use on poor signal):** the mission-betrayal — an empty map on a stalled fetch reading as "no barriers here, safe to proceed" — ends; "Still trying — check your signal" makes the difference between working and dead legible. **R2 (blind):** the escalation is a persistent live region, so they hear "still trying" instead of an infinite silent spinner. **R6 (first-timer):** "Updating…" vs "Loading flags…" tells them whether they're seeing stale data or nothing; and a slow *submit* no longer risks a punished duplicate, because the write escalates without aborting a possibly-committed insert. Whose betrayal ends: R1's most dangerous misreading.

**(4) TRUST + COHESION.** The temporal-honesty spine — the app stops presenting "stalled" as "empty." Cohesion: the data layer inherits the GPS layer's honesty posture; "Loading" vs "Updating" clarifies every refresh. The read/write split keeps the fix from manufacturing a duplicate-flag failure on the CONTRIBUTE path.

**(5) ACCESSIBILITY NOTE (mandatory).** Improves the floor: the escalation state is a persistent-mount `role=alert`/`aria-live` (the L6-02 lesson — mount-timed live regions on web don't reliably fire; the reliable survivors are always-mounted like the status pill). No AA color traded; the retry inherits the 44pt grammar. The write-side overlay (rather than an abort) prevents a false-failure state that would push a rate-limited user into a punished resubmit.

**(6) DEPENDENCIES / CONFLICTS.** **Preserves PROTECT-8** (shaped honest loading/terminal states — fills the *unbounded middle* between them, never touches the terminal cards or shaped skeletons), **PROTECT-6** (mirrors the GPS race, adds zero pollers/intervals — still zero `watchPositionAsync`), **PROTECT-15** (store fetch discipline — `fetchSeqRef` stale-discard + SWR cache paint untouched; the timeout composes with them), **PROTECT-2** (the empty-filters recovery card as the template these states aspire to). **Read/write split (reconciled):** the **read** half may hard-abort at the timeout ceiling; the **write** half (`createFlag`/`createAnonFlag`/`uploadFlagPhoto`) must NOT abort a possibly-committed insert — it escalates via overlay, or pairs any abort with idempotency/dedup — so a slow-but-committed write can't become a duplicate flag under the anon rate limit. **Sequencing:** independent; the persistent-live-region mechanism is shared with S10/S12's escalation states — establish it once. **Shared-file coordination:** `supabase.ts`/`flags.ts`/`flagsStore.tsx` (timeout — read path abortable, write path escalate-not-abort), `MapScreen.tsx`/`HomeScreen.tsx` (states + copy).

**(7) VERIFICATION.** **Guard test (reconciled — two legs):** (a) a unit test pins the data **read** racing a timeout and surfacing the "still trying" state at the threshold (the RM-test lesson from S12); (b) a unit test asserts a **write** that resolves AFTER the escalation threshold does NOT double-insert (the write escalated via overlay / was idempotency-guarded, not aborted-and-resubmitted). **Manual:** probe the poor-signal path → the escalation state appears before the OS gives up, announces via the persistent region, and offers retry; the Map offline error shows the friendly `errors.ts` copy, not "Unknown error"; "Updating…" appears over live data, "Loading flags…" only cold; a slow submit shows the overlay and lands exactly one flag. Poor-signal minute-plus ceiling stays NEEDS-SKY-DEVICE.

---

## S12 — Bring the web map camera up to the native reduce-motion standard (kill the falsy-zero trap)

**Resolves:** L4-01 (primary), L4-02 (web cluster leg), L4-04 (Leaflet built-ins under RM), L4-09 (the stale doc-comment that seeded the hole).
**Effort:** S–M (a handful of one-line call-site changes + one RM-conditional prop block on `MapContainer`; no new abstraction) · **Tier:** Meaningful (a confirmed WCAG 2.3.3 failure on a core flow — the highest-value *correctness* fix in its cluster) · **Signature:** no · **Needs device:** partial (the felt result — a swooping arc vs an instant cut — is NEEDS-SKY-DEVICE; the correctness is probe-proven at HEAD).

**(1) WHAT — vividly, mockup-ready.** Today a user who asked their OS for less motion gets the **largest, curviest, longest** motion in the entire app — Leaflet's signature zoom-out-arc-zoom-in flight, ~1–4s for a cross-town jump — on the exact "tap a barrier, see it on the map" FIND payoff (probe `rm-flight` caught a genuine intermediate zoom at t700, the flight still running at t1600). The cause is a two-character trap: `flyTo(…, { duration: reducedMotion ? 0 : 0.6 })` (`PlatformMap.web.tsx:626`) passes `duration:0`, and Leaflet treats `0` as falsy → distance-based default (`leaflet-src.js:3522`). Native already does this right (`animateToRegion(…, reducedMotion ? 0 : 600)` genuinely lands instant). **The fix makes web behave like native:** under reduce-motion pass Leaflet **`{ animate: false }`** (short-circuits to instant `setView`, `:3474-3475`) instead of a falsy duration; apply the same to the cluster-expansion `flyTo` at `:345` (and thread `reducedMotion` into `ClusteredMarkers`, which never receives it, `:252-266`); and set `zoomAnimation={false}`/`fadeAnimation={false}` + popup `autoPan:false` on the `MapContainer`/callout under RM so a callout-open can't animated-pan the map either. Same PR fixes the `accessibility.ts:95` doc comment ("Web/unsupported platforms quietly resolve to `false`" — wrong for this RN-web version, the lie that rationalized "why gate what can't fire?") and rewrites the `:625` "Instant jump… (WCAG 2.3.3)" comment to name the falsy-zero trap so it never regresses.

**(2) WHY + finding IDs + mission + taste anchor.** L4-01/L4-02 are HIGH and a confirmed WCAG 2.3.3 (Animation from Interactions) failure on the core FIND flow — the app's *worst* motion inflicted on the users who explicitly asked for less. It serves the mission (vestibular/motion-sensitive users are part of the disabled audience this app serves) and Sky's taste anchor (bring web *up to* the native camera standard — PROTECT-7 — touch neither the native path nor its intent).

**(3) DISABLED-USER IMPACT by persona.** **R5 / vestibular & motion-sensitive users (the RM cohort):** the mission-betrayal — the biggest, longest swoop in the app fired at them specifically — ends; the camera cuts instantly. **R1 (wheelchair, uses RM for focus):** the FIND payoff stops being a nauseating arc. Whose betrayal ends: the RM user getting *more* motion than everyone else on the flagship interaction.

**(4) TRUST + COHESION.** Removes an outright contradiction of the app's praised motion discipline. Cohesion: web and native cameras finally behave identically under RM.

**(5) ACCESSIBILITY NOTE (mandatory).** This IS the WCAG 2.3.3 compliance — the whole point is the hard floor. Non-RM users are untouched (the 0.6s flight stays). Extends the reduce-motion discipline (PROTECT-7) to the last surface that escaped it.

**(6) DEPENDENCIES / CONFLICTS.** **Preserves PROTECT-7** (the native camera gate — this brings web up to it), the map overlay `pointerEvents="box-none"` gesture law (no hit-testing change), and the merged-list #7 RM discipline. **Sequencing:** coordinate with S17 (both touch `PlatformMap.web.tsx` map config); the RM guard test belongs with S8-bench's motion-hygiene test if that ships, but S12 should ship its own `flyTo(animate:false)` guard test regardless. **Shared-file coordination:** `PlatformMap.web.tsx` (with S3/S6/S7/S17), `accessibility.ts` comment (with S9).

**(7) VERIFICATION.** No arbiter. **Guard test (required — this is how the trap shipped):** mock `useReducedMotion` and assert `flyTo` is called with `animate:false` under RM (and the cluster `flyTo` + MapContainer props). **Manual/probe:** re-run `rm-flight` → an instant cut, no intermediate frame. On-device *feel* stays NEEDS-SKY-DEVICE (correctness is probe-proven).

---

## S13 — Free the Tasks card actions from the accessible-parent trap (native VoiceOver #1)

**Resolves:** L6-04 (HIGH — "the single most important VoiceOver device-check in the audit").
**Effort:** M (restructure the card's a11y tree — sibling action row or `accessible={false}` + summary node; the FlagCard is inline in TasksScreen, so a focused edit on one component) · **Tier:** Meaningful (HIGH; native-critical if flattening confirmed) · **Signature:** no · **Needs device:** YES (iOS VoiceOver is the confirmation gate — the audit's #1 device check; the web-tree nested-button fix is in-harness verifiable).

**(1) WHAT — vividly, mockup-ready.** Verification is the app's trust engine — the mechanism R1 and R6 both said decides whether any severity badge can be believed. On the primary triage surface, each Tasks card is a bare `<Pressable>` (accessible-by-default, `TasksScreen.tsx:1595-1607`) that WRAPS four `PressableScale` action buttons (`:1528-1590`). On iOS, `accessible` makes the card one VoiceOver leaf and its descendant buttons are not focusable — so a blind user's path to verifying a flag collapses to long-press → selection mode → the bulk bar (whose buttons are reachable siblings), or nothing, since the card tap opens the Map, not details. If confirmed on device, community moderation is sighted-only on the surface built for it. **The fix is structural and known:** lift the action row OUT of the accessible card wrapper as a sibling row, OR set `accessible={false}` on the card with a labeled inner summary node so both the summary and each action are independently focusable. (On web the buttons are reachable but as invalid nested `<button>`s with double-activation ambiguity — the same restructure fixes that too.) This is the cluster's highest-leverage *native* fix; device-gated for confirmation but the restructure stands on the documented RN flattening pattern.

**(2) WHY + finding IDs + mission + taste anchor.** L6-04 is HIGH and the audit's named #1 device check. It serves the mission at its core — the trust engine (verify/resolve/reject) must be operable by blind users on the surface built for it, or the badge economy is sighted-only. Sky's taste anchor: a structural a11y-tree fix that respects the card's visual layout entirely (touches only grouping).

**(3) DISABLED-USER IMPACT by persona.** **R2 (blind, iOS VoiceOver):** the mission-betrayal — Verify/Resolve/Reject/Details unreachable on the triage surface, forcing the bulk-select fallback or nothing — ends; each action becomes an independently focusable button. **R6 (uses SR intermittently):** the trust engine they rely on to believe badges becomes operable. Whose job becomes possible: R2 can run community moderation (the app's trust engine) on the surface designed for it; whose betrayal ends: sighted-only verification.

**(4) TRUST + COHESION.** Makes the trust engine accessible — the mechanism the whole badge economy rests on. Cohesion: the card's a11y grouping joins the app's otherwise-careful pattern.

**(5) ACCESSIBILITY NOTE (mandatory).** This IS the floor fix — WCAG 4.1.2 (name/role/value: the descendant buttons regain focusable roles) on the primary triage surface. The bulk-select flow stays the multi-select path; the fix touches only a11y grouping, no visual/token/gesture change.

**(6) DEPENDENCIES / CONFLICTS.** **Preserves** the bulk-select flow (the bulk bar stays the multi-select path) and the card's visual layout. Same flattening mechanism as S9's LegendModal-backdrop fix (L6-21) — coordinate the a11y-tree restructure approach. **Sequencing:** best sequenced behind the ONE EAS TestFlight build (the confirmation gate); the code fix can land before, verified on device after. **Shared-file coordination:** `TasksScreen.tsx` FlagCard (with S1's Home/Tasks severity label + S2's RV/Tasks digit — the card is a shared surface).

**(7) VERIFICATION.** **Device (the gate):** iOS VoiceOver — are Verify/Details focusable from a card? (the audit's #1 device check, NEEDS-SKY-DEVICE). **A11y-tree (web):** the nested-button invalidity is resolved (each action a valid focusable control, no double-activation). No arbiter (no color).

---

## S14 — Give map pins the ratified hairline boundary so low-severity barriers stop vanishing on light tiles

**⟢ RECONCILED (S14 FIX — skeptic: the native arm is design, not replication; re-tag M + name PROTECT-15/16).** Five corrections, no scope widened. (1) **Effort re-tagged S–M → M, and the "replication, not design" claim corrected.** On WEB the pin is a custom `DivIcon` (`PlatformMap.web.tsx:116-122`) and a one-line `box-shadow:0 0 0 1px #0F1B2D` add is trivial — but web pins already PASS (CARTO `dark_all` → ring 21:1; finding + arbiter explicitly exempt web). On NATIVE the pin is a bare `pinColor` system OS marker (`PlatformMap.tsx:228`) with NO custom face to attach a hairline to — the fix is a net-new custom child-View teardrop marker (severity fill + white ring + `#0F1B2D` outer-hairline wrapper View + counter-rotated glyph, preserving the Callout, opacity/focus dimming, and anon variant) = **design, not replication**. The failing arm (iOS Apple light tiles) is exactly where replication is impossible. (2) **Add PROTECT-15 to the preserves list explicitly:** the current native pin is a system marker with NO `tracksViewChanges={false}`; the new custom child-View marker MUST carry `tracksViewChanges={false}` + a content-derived key (mirroring the cluster's `cluster-${id}-${count}` at `PlatformMap.tsx:132,139`) or it regresses per-pan re-rasterization (PROTECT-15), and MUST be a mode-independent always-light literal (GLASS §12 rule 8 / **PROTECT-16**). (3) **Scope the web half as internal-consistency/parity only** (web already AA); the access win lands entirely on native iOS light tiles (NEEDS-SKY-DEVICE visual; arbiter re-run gates contrast in-harness). (4) Keep the arbiter re-run (`contrast-check.mjs` + `tools/audit-stacks.json`) and the `#0F1B2D`-only token adoption (no new token) — those rails are clean. (5) **Sequence with S1 in the ONE coordinated native pin-renderer pass** so the custom-teardrop rebuild happens ONCE and the anon ring (S1) + severity hairline (S14) compose without a second native rewrite.

**Resolves:** L6-07 (HIGH, arbiter-measured).
**Effort:** M · **Tier:** Meaningful (arbiter-measured HIGH) · **Signature:** no · **Needs device:** partial (the on-device Apple-light-tile visual is NEEDS-SKY-DEVICE; the arbiter re-run gates the fix in-harness).

**(1) WHAT — vividly, mockup-ready.** GLASS.md §12 rule 4 is the app's own law — "a white ring vanishes on white tiles… use regime-decomposed unions" — applied to clusters and heat badges but **never to pins**. The arbiter measured the gap head-on: the pin's 2.5px white ring reads **1.00:1** on Apple light tiles, and the sev1–3 fills read **1.57 / 2.15 / 2.78** (sev4 3.61, sev5 4.35 pass) — so an iOS light-mode low-vision user loses the yellows and ambers (the low-severity pins) into the tile background, and the ramp's low end disappearing first skews perceived risk *downward* on a safety map. Following the GLASS §9 recipe (arbiter-first): apply the ratified **1px `#0F1B2D` hairline-union** already shipping on clusters (`PlatformMap.web.tsx:175` is the working exemplar) to the pin marker — **but the two arms differ (reconciled):** on **web**, the pin is already a custom `DivIcon` (`:116-122`) that already passes on the dark CARTO tiles, so the hairline is a one-line internal-consistency/parity add (a `box-shadow` on the existing face); on **native**, the pin is a bare `pinColor` system marker (`:228`) with no face — so the fix is a **net-new custom child-View teardrop marker** (severity fill + white ring + `#0F1B2D` outer-hairline wrapper View + counter-rotated glyph) that preserves the Callout, the opacity/focus dimming, and the anon variant. That custom marker MUST carry `tracksViewChanges={false}` + a content-derived key (mirroring `cluster-${id}-${count}` at `:132,139`) so it doesn't re-rasterize every pan (PROTECT-15), and it is a mode-independent always-light literal (PROTECT-16 / GLASS §12 rule 8). **arbiter-run → staged build → report.** This EXTENDS an existing arbitrated pattern (the `#0F1B2D` ink), inventing no new token — but on native it is a marker *rebuild*, which is why it is design-tagged M and sequenced into S1's one native pin-renderer pass.

**(2) WHY + finding IDs + mission + taste anchor.** L6-07 is HIGH and arbiter-measured (numbers reproduced exact). It serves the mission (the severity ramp's low end vanishing first *downward*-skews risk on a safety map — the most dangerous direction for the error). Sky's taste anchor: replicate the ratified hairline-union the app already trusts on clusters — the arbiter decides, never the eye.

**(3) DISABLED-USER IMPACT by persona.** **R3 (low-vision, iOS light mode):** the mission-betrayal — low-severity pins dissolving into light tiles, skewing perceived risk downward — ends; the hairline restores the boundary on any tile. **R1 (wheelchair, risk-reading):** the pin they judge a route by stops disappearing. Whose betrayal ends: R3's "the yellow pins vanish on the light map." (Twin-mitigated: the Nearby list carries severity non-visually, which is why it's HIGH not CRITICAL — but it is a real 1.4.11 boundary failure on safety-critical FIND targets.)

**(4) TRUST + COHESION.** Completes the GLASS §12.4 union law across every color-bearing map element. Cohesion: pins finally match clusters and heat badges.

**(5) ACCESSIBILITY NOTE (mandatory).** This IS the floor fix — WCAG 1.4.11 (non-text contrast) on interactive map targets. Adopts the existing ink `#0F1B2D`, invents no new token. Extends the crown-jewel contrast-arbitration system rather than regressing it. The native marker rebuild preserves PROTECT-15 (`tracksViewChanges={false}` + content-derived key — no per-pan re-rasterization) and PROTECT-16 (mode-independent always-light literal).

**(6) DEPENDENCIES / CONFLICTS.** **Preserves** the GLASS §12.4 union law (PROTECT/L2·L6 arbitration system), **`GlassSurface.tsx` DO-NOT-EDIT**, **PROTECT-15** (the native custom marker MUST carry `tracksViewChanges={false}` + a content-derived key so it doesn't regress per-pan re-rasterization — a system marker has none today), and **PROTECT-16** (the marker is a mode-independent always-light literal). **Web half scoped as parity/internal-consistency only** (web already AA on dark tiles); the access win is native iOS light tiles. **Overlaps S1's anon-pin ring** (both touch the pin renderer's boundary) — sequence together in the ONE native pin-renderer pass so the custom-teardrop rebuild happens once and the anon ring (S1) + severity hairline (S14) compose without a second native rewrite; both arbiter-gated. **Shared-file coordination:** `PlatformMap.tsx` (native custom marker rebuild) + `.web.tsx` pin renderer (parity add) (with S1/S3/S6/S7/S17).

**(7) VERIFICATION.** **Arbiter re-run (required):** `contrast-check.mjs` + `tools/audit-stacks.json` (the §C tileExtremes rows) must show the pin ring/fill boundary clearing 3:1 on light tiles after the hairline; exit-0 on the four existing proof-sets confirms no regression. **Native marker regression check:** the custom child-View marker sets `tracksViewChanges={false}` + a content-derived key (no per-pan re-rasterization — PROTECT-15) and renders as a mode-independent light literal (PROTECT-16). On-device Apple-light-tile visual is NEEDS-SKY-DEVICE.

---

## S15 — First-run honesty sweep: retire the four promises the app can't keep in minute one

**Resolves:** L1-2 (HIGH, copy half), L1-8 (MED), L1-11 (LOW), L8-11 (MED), L8-14 (MED, submit-moment half) · facets: copy-index L1 "one thing four names" + "Open the Map never opens the map", L8 "submit-moment sentence missing" + "noun canon needed."
**Effort:** S · **Tier:** QuickWin (the cheapest high-signal win in the cluster — almost every fix is a string the app already contradicts itself on elsewhere) · **Signature:** no · **Needs device:** no.
**FORKS-TO-SKY:** Sky-decision note #3 (guest contract). The *structural* halves of L1-2 (the walled native CTA, guest-mode amnesia) and the L8-4/L8-4a correctness bugs belong to the guest-contract canonical — S15 takes only the copy, and cross-notes that the auth-wall architecture is Sky's call.

**(1) WHAT — vividly, mockup-ready.** The first five minutes are "two different products" (L1 lens verdict): the pitch is excellent, then it breaks its own promises. Four edits, all copy: (1) **Noun canon** (L8-11/L1-11) — pick *barrier* as the human display noun and *flag* as the system/object noun ("flag a barrier"), and stop the "barrier → flag → report → task" whiplash that delayed R6's severity comprehension; retire "reports/tasks" as display nouns; consider "open" → "unconfirmed" to break the open-for-business collision. (2) **The guest-contract copy that is factually wrong** (L1-2c) — SignInScreen's "you'll need an account to report" and the a11y hint "Reporting flags requires an account" are *false* (anonymous reporting is a shipped first-class flow, `createAnonFlag` + the `2026-05-30` RLS policy); correct them TO the truth the report sheet already tells ("Your anonymous report still counts"). (3) **The onboarding photo/tap promise** (L1-8) — slide 2's "Tap where the barrier is, snap a photo" describes an interaction guests don't have (no plain-tap placement; anon flow has no photo); soften to what's true for the reader being taught. (4) **The submit-moment sentence** (L8-14) — the report submit is a "black box" (R6): add one line under the CTA — "Your report appears on the map now; neighbours can verify it. AccessMap doesn't notify the city — see Resources." — converting the app's most anxious moment into its most honest one and cashing the onboarding's "so it gets fixed" promise.

**(2) WHY + finding IDs + mission + taste anchor.** L1-2 is HIGH; the bundle (L1-8/L1-11/L8-11/L8-14) are the minute-one contradictions three readers caught cold. These are trust withdrawals at the exact moments trust is decided. Sky's taste anchor: every edit is written in the privacy-forward trust voice (PROTECT-11); the exemplar (the anon-report sheet) is untouched — the funnel copy is corrected *toward* it.

**(3) DISABLED-USER IMPACT by persona.** **R6 (first-timer, cognitive load):** the four-name whiplash and the black-box submit — his #3 friction — resolve; the noun canon and the submit-moment sentence lower the decode tax. **R2 (blind, guest):** the false "need an account to report" (which would make them abandon the CONTRIBUTE flow they *can* use) is corrected. **R1 (privacy-cautious):** the submit-moment sentence states exactly what publishing a report does. Whose betrayal ends: the false guest-can't-report copy that turns away the anonymous cohort.

**(4) TRUST + COHESION.** Corrects the minute-one contradictions that make the first impression "two products." Cohesion: one noun canon across onboarding/Home/Map/Tasks.

**(5) ACCESSIBILITY NOTE (mandatory).** Pure copy — no AA/GLASS/RN surface touched. The corrected a11y hint ("Reporting flags requires an account" → the truth) *improves* the SR experience by removing a false statement; the noun canon reduces cognitive load (WCAG 3.1.5 spirit).

**(6) DEPENDENCIES / CONFLICTS.** **Preserves PROTECT-5** (the anon-report sheet trust block — funnel copy corrected toward it, exemplar untouched), **PROTECT-11** (privacy-forward voice), **PROTECT-3** (Home's honesty law — the noun canon touches labels, never the never-fabricate-distances logic). **Sequencing:** the noun canon touches many surfaces — coordinate with S1 (which routes Home's status enum through `STATUS_LABELS`) and S20 (casing). **Shared-file coordination:** `OnboardingCards.tsx`, `SignInScreen.tsx`, `ReportFlagModal.tsx` (submit-moment line — with S5/S10/S18), Home/Tasks labels (with S1).

**(7) VERIFICATION.** No arbiter (copy). **Render states:** onboarding slides + SignIn + the Report submit-moment line read the corrected copy in light/dark; the noun canon is consistent across surfaces. **A11y-tree:** the SignIn hint no longer states the false "requires an account."

---

## S16 — Fix the two worst map touch targets: the bare-text "Clear" and the invisible action-bar overflow

**Resolves:** L5-04 (HIGH, ~34×17pt "Clear"), L5-05 (HIGH, action-bar tools scroll out of reach with zero affordance).
**Effort:** S (Clear) + M (overflow affordance) · **Tier:** Meaningful (device-integrity hygiene, load-bearing for R4/R3) · **Signature:** no · **Needs device:** partial (Split View / true-320pt pane widths are NEEDS-SKY-DEVICE; the affordance + the 44pt Clear are code/inspector-verifiable).

**(1) WHAT — vividly, mockup-ready.** Two named offenders in the 44pt census, both on the core FIND surface, both hitting limited-dexterity and low-vision users hardest. **(a) The filter panel's "Clear all filters"** (`MapScreen.tsx:1544-1552`) is the only bare-text `Pressable` in the app — no style, no `minHeight`, no `hitSlop` → ~34×17pt, adjacent to the 48pt-effective collapse chevron. It is the *recovery* action for the "my filters hid everything" state (R1's most-praised flow), so a limited-dexterity user who filtered themselves into an empty map needs the panel's smallest target to escape — and a miss *collapses the panel* instead, reading as "my filters vanished." Fix: give "Clear" the same `minHeight:44` + `hitSlop` the sibling `filterTitleRow` (`:2515`) got, with the same WCAG breadcrumb comment. **(b) The 7-button action bar** is a `ScrollView horizontal showsHorizontalScrollIndicator={false}` (`:1299-1304`); when its viewport shrinks below content width (iPad Split View ~320pt, true 320pt devices, web zoom/text-size), the LAST buttons — Refresh and **Recenter** — silently vanish with no scroll indicator, no fade, no peeking half-button. Recenter is not a convenience: the Report-FAB's own a11y hint tells locationless users "Use the recenter button to turn on location, then report" (`:2085`) — so when the tray hides it, the documented path into CONTRIBUTE disappears (R3 lost it at 1.3×). Fix: add a visible overflow affordance (a fade/gradient edge or persistent "more" indicator) so hidden tools are discoverable, and/or guarantee a half-button peek at the scroll edge.

**(2) WHY + finding IDs + mission + taste anchor.** L5-04 and L5-05 are both HIGH. They serve the mission (the FIND surface's recovery and the documented CONTRIBUTE entry must stay reachable) and Sky's taste anchor (the fix is *already patterned* — the sibling `filterTitleRow` got exactly the `minHeight`+`hitSlop` treatment with a comment; "Clear"'s omission is a genuine oversight, not a pattern).

**(3) DISABLED-USER IMPACT by persona.** **R4 (limited dexterity):** the mission-betrayal — the 34×17pt recovery target where a miss collapses the panel, and the silently-vanishing Recenter — ends; both become 44pt/discoverable. **R3 (low-vision, DT/zoom stress):** at 1.3×/2.0× the action-bar overflow becomes visible instead of eating Recenter without a trace. **R1 (wheelchair):** the "my filters hid everything" recovery (R1's most-praised flow) stays escapable. Whose job becomes possible: R4 can reliably hit Clear and find Recenter; whose betrayal ends: the recovery-target-that-collapses-the-panel and the invisible-overflow.

**(4) TRUST + COHESION.** Hardens the two worst targets on the flagship surface. Cohesion: "Clear" joins the sibling's already-correct 44pt pattern.

**(5) ACCESSIBILITY NOTE (mandatory).** This IS the floor fix — WCAG 2.5.5 (target size, "Clear" to ≥44pt) and 1.4.10/1.3.1 (the overflow affordance makes hidden controls discoverable, not invisible-by-design). No color change.

**(6) DEPENDENCIES / CONFLICTS.** **Preserves PROTECT-2** (the empty-filters recovery card — "Clear"'s fix must not regress the redundant 44pt "Reset all" path, the belt to this suspenders), the action bar's 44×44 button geometry (the affordance is added *around* the ScrollView, buttons untouched), and the virtualization law (unrelated). **Sequencing:** independent; the action-bar affordance shares the "chip rail clips mid-word" pattern with the bench L5-11 consolidation — if that ships, coordinate the shared "more" affordance. **Shared-file coordination:** `MapScreen.tsx` filter panel + action bar (with S1/S3/S4/S6).

**(7) VERIFICATION.** No arbiter. **Manual:** "Clear" measures ≥44pt (code + inspector); at DT 1.3×/2.0× and ~320pt the action bar shows a discoverability affordance and Recenter is reachable. Split View / true-320pt device behavior stays NEEDS-SKY-DEVICE.

---

## S17 — Contain the Home map peek: one clean button, no tap-theft, no app-exit

**Resolves:** L5-06 (HIGH — live map inside the "Open the full map" button steals taps/scroll and can exit the app) · L4-06 (motion facet, deduped into L5-06).
**Effort:** S (a `pointerEvents` wrapper + peek-instance map config) · **Tier:** QuickWin (a tidy, high-confidence win) · **Signature:** no · **Needs device:** partial (the react-native-maps tap-swallow resolution is NEEDS-SKY-DEVICE; the `pointerEvents` wrapper + web behavior are code-verifiable).

**(1) WHAT — vividly, mockup-ready.** Home's map peek is announced to assistive tech as ONE button, "Open the full map" (`HomeScreen.tsx:257-269`) — but its interior is a *live* map with three conflicting behaviors. The skeptic probe was decisive: a wheel/scroll gesture over the peek zoomed the *preview* out to show all of San Francisco instead of scrolling the page or opening the map; and on web the peek embeds Leaflet's zoom buttons *and* the attribution anchors — so "+/−" zoom in place and **"Leaflet / OpenStreetMap / CARTO" are live links that navigate the browser away from the app entirely.** For a tremor user, "exit the app" from a mis-tap inside a *button* is the most disorienting failure available (R4 already treated the peek's zoom squares as reach targets). On native, react-native-maps' own gesture recognizers plausibly consume the tap before the parent Pressable sees it — the peek's tap-to-open contract may be a dead button on device. **The fix (per the L5-06 fix-shape):** wrap the peek's `<PlatformMap>` in a `pointerEvents="none"` layer so the whole 168pt tile region is inert and only the parent Pressable receives the tap → one clean "Open the full map" target with no tap-theft and no app-exit; on the web peek instance, also `zoomControl={false}` and relocate/suppress the attribution so no live third-party anchors sit inside a button (the *full* map keeps its legally-required attribution — this is only the decorative peek).

**(2) WHY + finding IDs + mission + taste anchor.** L5-06 is HIGH. It serves the mission (the landing surface must not exit the app on a mis-tap) and Sky's taste anchor (a few lines that remove an entire class of the most disorienting failure — leaving the app — from the first screen). It also removes the L4-06 motion facet (the peek's cluster-fly-in-place).

**(3) DISABLED-USER IMPACT by persona.** **R4 (tremor / limited dexterity):** the mission-betrayal — a mis-tap inside a button *leaving the app* via a live attribution link — ends; the peek becomes inert and only the parent button fires. **R2 (blind):** the peek is announced as one button and now *behaves* as one (no interior live links to strand focus). **R6 (first-timer):** the peek's tap-to-open contract becomes reliable (no gesture theft). Whose betrayal ends: the app-exit-from-a-button on the landing surface.

**(4) TRUST + COHESION.** Removes the most disorienting failure from the first screen. Cohesion: the peek becomes strictly non-interactive (the safe direction), matching its "one button" announcement.

**(5) ACCESSIBILITY NOTE (mandatory).** Improves the floor: the peek's announced role ("one button") finally matches its behavior (WCAG 4.1.2 / 2.5.1 — no hidden interior gestures). No color change; the full Map's attribution and gesture behavior are untouched (this scopes only the Home peek instance).

**(6) DEPENDENCIES / CONFLICTS.** **Preserves PROTECT-10** (Home's honesty law — the peek still shows the map, it just stops stealing gestures) and the full Map's attribution/gesture behavior (untouched — this is only the peek). **`GlassSurface.tsx` untouched.** **Sequencing:** coordinate with S7 (the peek instance inherits S7's light tile + suppressed attribution) and S12 (both touch `PlatformMap.web.tsx` config). **Shared-file coordination:** `HomeScreen.tsx` peek + `PlatformMap.web.tsx` peek-instance config (with S7/S12).

**(7) VERIFICATION.** No arbiter. **Manual (web):** wheel/scroll over the peek scrolls the page (not the mini-map); tapping the peek opens the full map; no Leaflet zoom buttons or live attribution links inside it. Native tap-to-open + drag behavior stays NEEDS-SKY-DEVICE.

---

## S18 — "Submit report" label + 200%-zoom reflow guards (CRITICAL)

**⟢ RECONCILED (S18 FIX — skeptic: align the a11y name for 2.5.3; scope item ③ to S8).** Two conditions, no scope widened. (1) **WCAG 2.5.3 Label-in-Name:** the visible label "Report anonymously" is already NOT a substring of the a11y name "Submit anonymous flag report" (`ReportFlagModal.tsx:995` vs `:982`), and the audit flags 2.5.3 as a real defect elsewhere (L1-25 / `02_findings.md:135`). v1 changed only the visible label to "Submit report" and left the a11y name as-is — so the visible words still would not be contained in the accessible name. **When renaming, align the accessible name so the visible text is contained in it** (prefer contiguous, e.g. visible "Submit report" + a11y "Submit report anonymously") — turning a pre-existing miss into a PASS on the app's most important button for one string. (2) **The header-collision leg (item ③ "MapFeedback"/"ProfiFeedback") is scoped to the wrong component today.** L5-03 item ③ is on the react-navigation nav header (`RootNavigator.tsx:271` `headerTitleStyle` + `:279` `headerRight` Feedback pill) used by Map/Profile/Settings — which are NOT `headerShown:false` and do NOT use the in-body ScreenHeader (only Home/Tasks do, `:328/:335`). So "extend ScreenHeader's deterministic auto-fit" does NOT fix item ③ as the app ships today — it only fits AFTER S8 migrates those routes onto ScreenHeader. **Either make the item-③ leg a HARD DEPENDENCY on S8, or apply an equivalent shrink/truncate directly on the nav header's `headerTitleStyle`/`headerTitle` for those routes.** The label + banner-wrap legs (items ①②, the bulk of the CRITICAL) are S8-independent and ship standalone.

**Resolves:** L5-03 (CRITICAL, WCAG 1.4.4 at 200%) · L3-27 (POLISH, title == button words) · the "Report anonymously as a button label" copy observation (L5).
**Effort:** S (copy/label change + a couple of reflow guards) · **Tier:** QuickWin (outsized reach) · **Signature:** no · **Needs device:** partial (whether the header collides at the native capped 1.5–1.6 DT is NEEDS-SKY-DEVICE; the web 200% behavior is the shipped SC test).

**(1) WHAT — vividly, mockup-ready.** AccessMap's web build is a first-class guest surface, and browser zoom is *the* low-vision mechanism on the web — so its WCAG 1.4.4 failures at 200% are shipped, not proxied. R3's reality is the 2.0 shots: the "Report anonymously" submit label overflows its pill (`submitBtn: overflow:'visible'`, `ReportFlagModal.tsx:1230`) and the header title collides with the Feedback pill into "MapFeedback"/"ProfiFeedback." **The single biggest lever is the label itself:** "Report anonymously" is 19 characters doing the work of 6. The sheet title already establishes anonymity, and — **reconciled** — when the *visible* button is renamed to **"Submit report,"** the **accessible name is aligned so the visible text is contained in it** (e.g. a11y "Submit report anonymously," visible "Submit report" — contiguous), which both buys ~40% width headroom on the app's most important button (directly relieving the pill overflow and the 1.3× two-line squeeze) AND turns the pre-existing 2.5.3 Label-in-Name miss (visible words not in the a11y name) into a PASS. It also fixes the L3-27 blur ("Report anonymously" is simultaneously the h1 and the button — at 2.0× they merge). Bundle two cheap reflow guards from the same finding: the anonymity banner must **wrap on word boundaries, never mid-word** (the "Reporti/ng/anonym/ously" shred is a `flex`/min-width bug, not an inevitability — proven by the graceful 1.3× degradation); and the header title × Feedback-pill collision (item ③) — **which today lives on the react-navigation nav header for Map/Profile/Settings, NOT the in-body ScreenHeader** — needs the title to truncate/shrink or the pill to yield rather than overprint. Because ScreenHeader's deterministic auto-fit only reaches those routes AFTER S8 migrates them, **item ③ is a hard dependency on S8** (or, if shipped standalone, applies an equivalent shrink/truncate directly on the nav header's `headerTitleStyle`/`headerTitle` for those routes). The label + banner-wrap legs (①②) are S8-independent. The submit stays reachable throughout (sticky footer outside the scroll, `:448-450` — so this is strictly legibility/boundary, not reachability, per the skeptic's correction).

**(2) WHY + finding IDs + mission + taste anchor.** L5-03 is CRITICAL #5 — the shipped web app fails WCAG 1.4.4 at 200% on the report flow (content shreds mid-word; the header collides). It serves the mission (low-vision users rely on browser zoom on the guest surface) and Sky's taste anchor (a copy change that *also* sharpens the button — "Submit report" is verb-forward and honest, anonymity still stated by the title + anon banner + the aligned a11y name).

**(3) DISABLED-USER IMPACT by persona.** **R3 (low-vision, browser zoom at 200%):** the mission-betrayal — the report flow's content shredding mid-word and the header colliding — ends; the label fits, the banner wraps cleanly, the header stops reading "MapFeedback." **R2 (blind):** the aligned accessible name now *contains* the visible label — so a voice-control user who says "Submit report" activates the button (2.5.3), which the v1 mismatch would have broken. **R6 (first-timer):** the h1/button no longer merge into one ambiguous string. Whose job becomes possible: R3 can complete the report flow at 200% zoom, and R2's voice-control target works; whose betrayal ends: the mid-word shred on the app's most important flow.

**(4) TRUST + COHESION.** Closes a shipped WCAG failure on the CONTRIBUTE flow AND a Label-in-Name miss on the app's most important button. Cohesion: extends ScreenHeader's deterministic auto-fit (PROTECT-4/10) — via S8 for the nav routes — rather than reaching for the RN-web-no-op `adjustsFontSizeToFit`.

**(5) ACCESSIBILITY NOTE (mandatory).** This IS the floor fix — WCAG 1.4.4 (resize text) and 1.4.10 (reflow) at 200%, PLUS 2.5.3 (Label in Name — the aligned accessible name now contains the visible "Submit report"). The header fix extends the deterministic auto-fit (via S8 for the nav routes), explicitly NOT `adjustsFontSizeToFit` (a RN-web no-op). No color change.

**(6) DEPENDENCIES / CONFLICTS.** **Preserves PROTECT-3** (sheet architecture — the sticky footer and severity buttons untouched; only the label string + its a11y name change), **PROTECT-4/10** (ScreenHeader auto-fit — the header fix *extends* its web-safe shrink), **PROTECT-8** (anonymity honesty — still stated by the title, the anon banner, and the aligned a11y name "Submit report anonymously"). **Sequencing:** **item ③ (header collision) is a HARD DEPENDENCY on S8** — the nav-header routes Map/Profile/Settings only gain ScreenHeader's auto-fit once S8 migrates them; if S18 ships before S8, item ③ applies an equivalent shrink/truncate directly on the nav header's `headerTitleStyle`/`headerTitle`. Items ①② (label + banner wrap) ship standalone. Coordinate with S5/S10 (all touch the Report sheet). **Shared-file coordination:** `ReportFlagModal.tsx` submit label + its `accessibilityLabel` + banner (with S2/S5/S10/S15), `ScreenHeader.tsx` auto-fit (with S8/S9) and/or `RootNavigator.tsx` `headerTitleStyle` for the still-nav routes (with S8).

**(7) VERIFICATION.** No arbiter. **Render states (the SC test):** web, 390 @ browser zoom 2.0 → the submit label sits inside its pill; the anonymity banner wraps on word boundaries; the header no longer reads "MapFeedback" (item ③ verified after S8, or via the direct nav-header shrink if shipped standalone). **A11y-tree (2.5.3):** the button's accessible name contains the visible "Submit report" (e.g. "Submit report anonymously"). Native per-variant caps make ①–③ unreproducible-as-shown; whether the header collides at the capped 1.5–1.6 stays NEEDS-SKY-DEVICE.

---

## S19 — Give the location consent slide a visible "Not now" (and stop the web permission theater)

**Resolves:** L1-3 (HIGH) · facets: R6 "consent is the toll to continue," copy-index L1 "two location-privacy contracts."
**Effort:** S · **Tier:** QuickWin (the correct pattern lives eight lines away in the same file) · **Signature:** no · **Needs device:** partial (native first-run slide *feel* is NEEDS-SKY-DEVICE; the missing-decline + web-theater are probe-verified).
**FORKS-TO-SKY:** Sky-decision note #3 (guest contract) — the "does the guest build request location / expose sign-in at all" question. S19 only adds the decline affordance and de-theaters the web button; the larger web-guest-location architecture is Sky's call.

**(1) WHAT — vividly, mockup-ready.** The location slide is the most sensitive consent moment in an app that maps *disability* — and it is the *only* permission slide with no visible decline. Slide 4 (notifications) models the respectful pattern perfectly: a big "Turn on Notifications" plus a visible "Maybe later" (`showMaybeLater`, `OnboardingCards.tsx:216`). Slide 3 (location) offers only "Allow Location," a dimmed Back, and the tiny Skip that abandons the whole tutorial (probe-verified: three controls, no "Continue"/"Maybe later" in the ungranted first-run state) — so a user who doesn't want to grant must either open the OS dialog just to decline it or quit onboarding. R6 hesitated here and nearly bailed. **The fix extends the pattern eight lines away** to the more sensitive of the two permissions: add a visible "Not now" to the location slide. **Second leg (web):** on web, tapping "Allow Location" performs *no permission action* and just advances (`:243-247`) — theater — then the real browser prompt appears later, unannounced, on Home's "Use my location." Make the first ask honest (wire it, or relabel it so it doesn't masquerade as the grant). The behavior is benign (denial never blocks — the handler advances regardless), but the UI hides that fact, which is the dark-pattern *shape*; adding "Not now" makes the benign truth visible.

**(2) WHY + finding IDs + mission + taste anchor.** L1-3 is HIGH. It serves the mission at its most values-load-bearing (consent in an app mapping disability must be genuinely optional and honestly presented) and Sky's taste anchor (extend the app's *own* respectful "Maybe later" pattern — PROTECT-1-of-L1's permission-priming architecture — to the location slide, exactly as L1-3 asks).

**(3) DISABLED-USER IMPACT by persona.** **R6 (first-timer, hesitated and nearly bailed):** the toll-to-continue framing ends; a visible "Not now" makes the decline a first-class choice. **R1 (privacy-cautious disabled user — the core audience):** the most sensitive consent moment gains an honest decline instead of forcing the OS dialog to say no. **R2 (blind):** the web theater (a button that announces "Allow Location" but does nothing, then an unannounced browser prompt later) is de-mystified. Whose betrayal ends: the location slide's dark-pattern *shape* on the app's most sensitive ask.

**(4) TRUST + COHESION.** Repairs the highest-stakes consent moment. Cohesion: both permission slides finally offer the same respectful decline.

**(5) ACCESSIBILITY NOTE (mandatory).** Improves the floor: a visible decline is a WCAG 3.3.4-spirit / consent-integrity improvement; de-theatering the web button removes a misleading control state (4.1.2). No AA/GLASS surface; the new button inherits the carousel's gated motion (PROTECT-7-of-L1). RN/Expo: a UI control, not an SR announce — no web no-op trap.

**(6) DEPENDENCIES / CONFLICTS.** **Preserves PROTECT-1-of-L1** (the permission-priming architecture — explain → check silently → prompt on tap → denial never blocks — extended by adding "Not now," never diluted) and **PROTECT-7-of-L1** (RM discipline in both carousels). **Sequencing:** independent; pairs thematically with S5 (both touch the guest location story — S5 the Report path, S19 the onboarding ask) and S15 (the "two location-privacy contracts" copy). **Shared-file coordination:** `OnboardingCards.tsx` (with S15).

**(7) VERIFICATION.** No arbiter. **Render states:** the location slide shows a visible "Not now" in the ungranted first-run state; the web "Allow Location" is wired or relabeled so it no longer masquerades as the grant. Native first-run slide *feel* stays NEEDS-SKY-DEVICE.

---

## S20 — Repair the trust-fallback surfaces (Help FAQ accuracy, stale changelog, About anchors, casing sweep)

**Resolves:** L8-12 (MED, Help FAQ), L8-13 (MED, stale changelog), L8-14 (MED, About half) · L2-7-adjacent casing (copy-index L2/L8 casing drift) · L2-15-adjacent (title/row agreement).
**Effort:** S · **Tier:** QuickWin (all copy/link) · **Signature:** no · **Needs device:** no.
**FORKS-TO-SKY:** none required — all copy/link. (If the repo is private, the "open source" claim is a factual/scope question for Sky, but *softening* the claim needs no decision.)

**(1) WHAT — vividly, mockup-ready.** These are the surfaces a user consults *at the exact moment trust is already strained* — and they're partially wrong about the shipped app, converting confusion into a verdict. Four copy repairs: (1) **Help & FAQ** (L8-12) — the one page that must be right, because it also holds the app's *only* definition of "verified" (until S1's legend Status block): fix "Open the Map tab" (there is no Map tab — tabs are Home/Tasks/Profile), fix "tap the '＋ Report' button" (auth-only, invisible to guests — the answer must name the guest path Home → Report a barrier and the auth note), fix "magnifying glass → filters" (the magnifier is address search; filters are the sliders icon), and "Resolved reports appear in a different color" (they keep severity color and gain a *checkmark*). (2) **Changelog** (L8-13) — `ChangelogModal.tsx` has a single entry dated 2026-05-23, six weeks and three visual eras stale (v3.0.0, editorial Home, the Deep Field system, heatmap, presets, onboarding — none logged); R5 explicitly *counted* the honest changelog toward trust, so a changelog that stops while the product visibly evolves reads as abandonment; add the v3-era entries. (3) **About anchors** (L8-14 About half) — About claims "The maps, icons, and database schema are open" under a heading titled SOURCE CODE with *no repo link or license*, and "Status changes are logged, visible to other users" with no map-side path (surfaced by S3); add the link/license or soften the claim. (4) **Casing sweep** — "How To Help" vs "About the App" vs "Resources"; modal "What's New" vs Settings row "What's new"; "My Feedback" modal vs "My feedback history" row — pick one casing rule (title-case is the dominant idiom) so every screen title and the row that opens it agree.

**(2) WHY + finding IDs + mission + taste anchor.** L8-12/L8-13/L8-14 are MEDIUM but load-bearing for trust — unverifiable/wrong copy reads as inattention exactly where the app is otherwise unusually honest. Sky's taste anchor: the changelog's candid maker-voiced bullets (PROTECT-10) are the model to extend; every edit stays in the privacy-forward trust voice (PROTECT-11).

**(3) DISABLED-USER IMPACT by persona.** **R6 (first-timer, consults Help when confused):** the FAQ that would misdirect them ("Map tab," wrong report path, magnifier-not-sliders) becomes accurate — the guest path into CONTRIBUTE is finally documented. **R2 (blind):** the FAQ's "verified" definition (the only one until S1) is corrected and reachable. **R3 (low-vision):** the casing sweep removes the papercut of a title rendered two ways. Whose betrayal ends: the Help page misdirecting a confused user to non-existent navigation.

**(4) TRUST + COHESION.** Repairs the surfaces consulted when trust is strained. Cohesion: the casing sweep makes every title agree with the row that opens it.

**(5) ACCESSIBILITY NOTE (mandatory).** Pure copy/link — no AA/GLASS/RN surface. The accurate FAQ *improves* the SR experience (correct navigation guidance for a blind user who can't visually cross-check). No contrast implication.

**(6) DEPENDENCIES / CONFLICTS.** **Preserves PROTECT-11** (privacy-forward voice), **PROTECT-10** (maker-voiced micro-copy — the changelog's candid bullets extended). **Sequencing:** the About "logged, visible to other users" claim is cashed by **S3** (which surfaces the ledger from the map) — land S20's About edit to reference the now-reachable path after S3, or soften it if S3 is deferred; the casing sweep coordinates with S8 (header titles) and S15 (noun canon). **Shared-file coordination:** `HelpModal.tsx`, `ChangelogModal.tsx`, `AboutScreen.tsx`, Settings/drawer titles (with S8/S15).

**(7) VERIFICATION.** No arbiter (copy). **Render states:** Help FAQ, Changelog, About, and the drifting titles read the corrected copy in light/dark; the FAQ names the guest report path; titles agree with their opening rows. **A11y-tree:** the FAQ's navigation guidance matches the real tab structure.

---

# RUNNER-UP BENCH (ranked, best first — for backfill; not discarded)

**Promotion status (v2): 0 promoted.** No KILL breached the ≥10-survivor or ≥3-signature floors (20 survive, 7 signature), so no bench candidate needed promoting. The bench is carried forward verbatim; B1 remains the highest-value entry for any future promotion.

1. **B1 — Points flash lies on anon triage (L3-4, HIGH).** The actor-bonus trigger `auth.uid() <> NEW.user_id` is SQL-NULL for anon flags, so triaging an anon report awards 0 while the UI flashes "+3/+7" (`TasksScreen.tsx:760`). **FORKS-TO-SKY (note #2):** the real fix is a one-line migration (`IS DISTINCT FROM`), Sky-applied, never auto-run; the UI half is a clean **S** flash-suppression (suppress the actor-flash when `item.user_id === null`). Benched not for weakness but because it is fundamentally a data-honesty fork — promote to the slate if Sky prefers the UI suppression over the migration. **Highest-value bench entry.**
2. **B2 — Retire the seven UI emoji for Lucide glyphs (L2-9, MED).** DESIGN.md §10 "SVG icons only — no emoji" is violated at 7 sites (Feedback/MyFeedback chips 🐛💡❤️💬, AddressSearch 🕘/⚠️, FlagDetail 💬); the W1 pass even restyled the chips but kept the emoji *on* the new material. One-for-one Lucide swap at the same sizes, all already `accessibilityElementsHidden`. Clean **S** quick-win closing a self-declared law violation; leave the drawer's "Made with ♥ in Canada" prose heart alone.
3. **B3 — Wear the Wayfinder mark (L8-8, MED).** Retire the three stock intro glyphs (onboarding compass, replay MapPin, drawer "A" placeholder) for the on-mission Wayfinder mark. Pure asset-swap **S**; supports the S1/S8 signature system (distinctiveness through repetition). **FORKS-TO-SKY (note #6):** the "AccessMap" name collision with accessmap.io is a separate naming-strategy call — NOT in this item.
4. **B4 — Unify the modal layer on one material (L2-5, MED).** Extend the ratified bulk-glass recipe to the opaque host siblings (Help/Changelog/MyFeedback) and the guest-critical Map sheets (Legend/Report) by threading `GlassSurface`. **M.** Benched below the header/map signatures; every newly-glassed sheet must arbiter-check its inks and keep its C-lite fallback (PROTECT-3). Overlaps B6's ghosting decision.
5. **B5 — Motion hygiene sweep + RM regression test (L4-05/07/08/10/11/12, POLISH→SHOULD).** Gate the dead 220ms/350ms RM delays (drawer, nearby-select) that only penalize RM users, tokenize the 600ms tier-fill + onboarding/Skeleton literals (add `motion.duration.pulse`), and — the load-bearing leg — add the RM guard tests that would have caught the falsy-zero trap (S12). **M** (each leg S). The RM-test leg pairs with S12's guard test; promote if the slate wants motion-discipline hardening.
6. **B6 — Light bulk-sheet ghosting decision (L2-6, device-gated).** High-contrast backdrop shapes ghost through About/Feedback body text over busy backdrops (perception defect — every ink already passes the arbiter). Carry into Part 3 *with rails attached*: raise the light bulk floor a step OR scrim the backdrop — **NEEDS-SKY-DEVICE**, and any floor change MUST re-run `contrast-check.mjs` (never eye-tune). **S–M.** Overlaps B4 (if Report/Legend join the bulk tier, apply the same treatment).
7. **B7 — Heat-map "no zones in view" companion + iOS cluster-spring gate (L7-11 + L4-03 + L4-02-native, SHOULD).** Add "No heat zones qualify in this view yet" when the heat layer is on but empty (closes the on/empty/broken ambiguity), and gate the un-gated iOS clustering `LayoutAnimation` spring + animated cluster-fit behind reduce-motion via additive props. **S–M**, iOS amplitude NEEDS-SKY-DEVICE.
8. **B8 — Photo pipeline: resize on ingest (L7-05, SHOULD).** Add a `resize` action to the `manipulateAsync` call that already strips EXIF (`flags.ts:108-111`) so 12–48MP originals are capped before being decoded at full-res into 64–120px thumbnails/callouts. **S**, pure client-side; the EXIF GPS strip is preserved (rides the same re-encode — PROTECT #9 / parked ④). Felt-performance win on the "can I get past this?" moment.
9. **B9 — Data-age on the offline banner + Home refresh-failure surfacing (L7-02, SHOULD).** Read the already-stored `cachedAt` and state it ("Showing saved data from 2 h ago"), and give Home the last-updated/refresh-failure affordance Map and Tasks already have. **S. FORKS-TO-SKY (note #4):** the guest-cache-scope leg (guests get no offline resilience) is a privacy-vs-utility ratification, not fixed here — only the age + Home-surfacing UI halves.
10. **B10 — Web locate-failure gets a visible/spoken outcome (L7-07, SHOULD).** Replace the web no-op `Alert.alert` catch on the Map locate flow (`MapScreen.tsx:1030-1033`) with a persistent `role=alert` "Couldn't find your location — check your browser's location permission" + retry, mirroring the report sheet's pattern. **S**, distinct from S4 (active locate failure, not passive denied arrival).
11. **B11 — Resurrection-trap + ctaFill + ≥500-weight hygiene sweep (L2-12/L2-13/L2-10, POLISH).** Delete seven dead styles carrying banned inks/text-glyphs, adopt the mode-independent `ctaFill` token at the two white-on-brand CTA stragglers (Map "Reset all", save-name "Save"), and bump the disclosed + undisclosed 400-weight on-glass text (`savedEmptyText`, `statusHint`×4, Tasks `emptyBody`) to ≥500. **S**, hardening against regression; ctaFill leg must adopt the existing token + re-run the arbiter; ≥500 leg is NEEDS-SKY-DEVICE for the haze *feel*.

---

# PARKED-RECONCILIATION (6 rows, per `partials/dispositions.md`)

| # | Parked item | Disposition | Where it lands / why |
|---|---|---|---|
| ① | **RecentlyViewedRow severity-dot white-digit** (`RecentlyViewedRow.tsx`) | **→ S2 (in-scope, CONFIRM)** | Arbiter measured it head-on (white digit 1.57/2.15/2.78/3.61 on sev1–4; boundary melt §D-2). It is site 5 of the seven white-digit render sites in canonical L2-1 — **resolved by S2** (the `textOnColor` adoption); auth-gated, so lower-priority than the guest sites but the same one-token fix. No longer parked. |
| ② | **Stage lower-right light pool `stagePoolB`** (`theme.ts:202`) | **Sky-fork (PARK — taste call)** | Sub-perceptual at 390 (0.06 alpha, card stack covers most of its footprint); serves restraint, costs nothing legible; killing it would be imperceptible at phone widths. A judgment is offered, not a finding — **the call remains Sky's**, not a slate entry. |
| ③ | **Deferred dark-themed saved-place-chips** (chips over live tiles) | **PARK-hold** | The chip row is auth-gated (guests never see it — no live capture). The SHIPPED always-light chips are AA-by-construction (arbiter exit 0). The deferred dark-over-dark-tiles variant is unbuilt and its "over LIGHT Apple tiles" read is device-only; **no audit evidence forces the decision** either way. NEEDS-SKY-DEVICE, not slated. |
| ④ | **EXIF-strip + VoiceOver device checks** | **PARK-hold → NEEDS-SKY-DEVICE** | The audit CODE-CONFIRMS the EXIF strip-by-re-encode exists but cannot verify on-device GPS removal (auth-only photo path, inside the never-signed-in fence). VoiceOver truth is device-only — most importantly **L6-04 (= S13)** flattening. Both stay on Sky's consolidated on-device list. **S13 codes the fix; the confirmation is the device pass.** (B8's resize preserves this contract.) |
| ⑤ | **`ui/Button` adopt-or-remove** (zero call sites) | **CONFIRM (in-scope, standing Sky decision)** | Grep at HEAD: zero `<Button` call sites app-wide — only the barrel re-export. Not a defect — a standing decision (adopt per the lab's recommendation, or delete). **Carried forward so it is not lost; it is a one-line Sky call, not a slate proposal** (no finding drives a UI change — it is a housekeeping decision). |
| ⑥ | **Map wave's deferred `bodyMedium` (≥500-weight-on-glass)** | **PARK-hold (+ B11 carries the mechanical half)** | The disclosed Map deferral (`savedEmptyText` + `statusHint`×4) + the undisclosed Tasks `emptyBody` sibling are the material-haze ≥500 law (GLASS §2), NOT a contrast breach (every ink passed the arbiter). **B11 carries the mechanical ≥500 bump** as a bench hygiene leg; the haze *feel* is **NEEDS-SKY-DEVICE**, so it does not ship on web-proxy evidence — parked for the device pass. |

**Reconciliation note:** ① and ⑤ are CONFIRM/in-scope per the brief (① resolved by S2; ⑤ a standing Sky housekeeping call carried forward); ②③④⑥ are PARK (② Sky's taste call, ③④ NEEDS-SKY-DEVICE with no forcing evidence, ⑥ device-gated haze with the mechanical half on the bench). **No parked row changed disposition in v2** — the skeptic pass produced no KILL and touched no parked item.

---

## Slate summary (for the assembler) — v2 reconciled

- **20 canonical proposals survive** (S1–S20), every one traced to Part-2 finding IDs. **0 killed, 0 backfilled.**
- **Skeptic outcome:** 12 KEEP (S1, S2, S3, S5, S9, S12, S13, S15, S16, S17, S19, S20), 8 FIX applied in place (S4, S6, S7, S8, S10, S11, S14, S18), 0 KILL. Every FIX tightened scope (a condition, a corrected rail, a preserved crown jewel) — none added a feature.
- **Floor status: PASS both floors.** Survivors = 20 (≥10 ✓). SIGNATURE-tier = 7 (S1, S3, S6, S7, S8, S9, S11) (≥3 ✓). No KILL, so no promotion was required; the bench (11 ranked, 0 promoted, B1 highest-value) is intact.
- **Ambition mix:** QuickWin (S) = S2, S5, S15, S17, S18, S19, S20 (7); Meaningful (M) = S4, S8, S10, S11, S12, S13, **S14 (re-tagged S–M→M per the S14 FIX)**, S16 (8); plus S1 (M), S6 (M), S7 (M) tagged Signature; Larger (L) = S3, S9. Effort tags on each header.
- **6 CRITICALs covered:** L3-1→S5, L3-2→S4, L2-1→S2, L5-01→S6, L5-03→S18, L6-01→S9.
- **FORKS-TO-SKY lines** on every proposal resolving L3-1 (S5), L3-2 (S4), L8-2 (S1, S3), L8-3 (S3), and the guest-contract/proximity/points/cache decisions (S15, S19, B1, B9) — each scopes only its UI half.
- **Every color/floor/severity proposal** (S1 anon ring, S2 digit, S6 zoom-button chrome [arbiter added in v2], S7 tile compositing [arbiter made mandatory in v2], S14 pin hairline) names an arbiter re-run (`contrast-check.mjs` + `tools/audit-stacks.json`) and adopts an existing ratified token; **`GlassSurface.tsx` is never edited**; blur intensities stay 12/24; the `box-none` gesture law and virtualization law are untouched.
- **New cross-proposal dependencies surfaced by the reconciliation:** S18 item ③ (header collision) is a hard dependency on S8 (or a direct nav-header shrink); S14's native marker rebuild sequences into S1's one native pin-renderer pass; S7 drops the Leaflet-zoom restyle that S6 supersedes.
- **BENCH:** 11 ranked runner-ups (B1 highest-value), 0 promoted. **PARKED:** 6-row reconciliation (① and ⑤ in-scope; ②③④⑥ park) — unchanged in v2.
