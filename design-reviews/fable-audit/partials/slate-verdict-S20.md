# Adversarial Skeptic Verdict — S20

**Proposal:** S20 — "Repair the trust-fallback surfaces (Help FAQ accuracy, stale changelog, About anchors, casing sweep)"
**Resolves:** L8-12 (MED, Help FAQ), L8-13 (MED, stale changelog), L8-14 (MED, **About half only**) · L2-7-adjacent casing · L2-15-adjacent title/row agreement · Effort S · Tier QuickWin
**Verdict: KEEP** (all 7 rails hold under verification)

---

## One-line

Pure copy/link repair; every cited finding is a code-confirmed, currently-shipping defect; every factual claim in the proposal (wrong tab name, magnifier-vs-sliders, single stale changelog entry, unanchored "open" claim, casing drift) verified true against source; trips no WCAG floor, no GLASS law, no blur budget, no `box-none` gesture law, no arbiter/token requirement, and no RN-web announce no-op; scoping is honest (submit-moment half of L8-14 correctly left to S15, repo-privacy factual question correctly flagged as Sky's while the copy fix stands independently). Clean.

## Per-rail (all 7 TRUE)

| Rail | Verdict | Evidence |
|---|---|---|
| tracesToFinding | ✅ | L8-12/L8-13/L8-14 all present in `02_findings.md:1565-1581` at MEDIUM. Every drift is verified at HEAD: **L8-12** — `HelpModal.tsx:33` says "Open the **Map tab**" (no such tab), ":33" says tap "＋ Report" (auth-only), ":57" says "magnifying glass icon → check Categories, Minimum severity" (magnifier is address search), ":41" says Resolved "appear in a different color" (they keep severity color + gain a check). **L8-13** — `ChangelogModal.tsx:28` is the *single* entry, dated 2026-05-23 (the only other `date:` at :16 is the type decl); title drift "What's New" (:76) vs Settings "What's new" (`SettingsScreen.tsx:536`). **L8-14 About half** — `AboutScreen.tsx:98-99` "maps, icons, and database schema are open" with no link/license; ":103" "Source code" heading, body :105-108 is tech-stack only, no repo link; ":118-121" "logged… visible to other users" with no map-side path (cashed by S3). **Casing** — "My Feedback" (`MyFeedbackModal.tsx:125`) vs "My feedback history" (`SettingsScreen.tsx:573`) is real drift. Each cited finding is genuinely resolved by the exact copy/link edit S20 prescribes. |
| wcagFloorHeld | ✅ (improved / neutral) | Pure copy/link — no contrast, target-size, focus, state, or reflow mechanism touched. Access-positive: a blind user who can't visually cross-check gets *correct* navigation guidance instead of being sent to a non-existent "Map tab" or told the magnifier opens filters (WCAG 3.3.2 spirit — the guidance the surface gives is now true). Nothing removed from the a11y tree. No floor traded for polish. |
| glassLawHeld | ✅ | Touches no color/floor/ink/severity token. `HelpModal.tsx` and `ChangelogModal.tsx` carry zero `BlurView`/`severity[`/`textOnColor`/`contrast` references. `AboutScreen.tsx` *consumes* `GlassSurface variant="bulk"` (:51) exactly as it does today — S20 edits the body **copy strings** (:98-99, :105-108, :118-121), it never edits or forks `GlassSurface.tsx`. Blur budget untouched (no BlurView added; still 12/24 elsewhere). `box-none` gesture law and virtualization law not in scope. |
| protectPreserved | ✅ (verified, not trusted) | **PROTECT-11** (privacy-forward trust voice): every edit corrects *toward* the honest voice — softening/anchoring an unverifiable "open" claim and correcting a false FAQ *strengthens* it. **PROTECT-10** (Home's honesty law): S20 does NOT touch Home's never-fabricate-distances logic — `AboutScreen`/`HelpModal`/`ChangelogModal` are unrelated files; the citation is for the changelog's maker-voiced candid bullets (extended, not regressed). **Note:** PROTECT-10 in `protect-merged.md` is literally "Home's distance-honesty law"; the changelog-voice intent S20 attaches to it is nearer PROTECT-11 — a cosmetic citation imprecision, not a regression (identical benign blemish the S15 verdict flagged as assembler cleanup). No crown jewel regressed. |
| rnExpoFeasible | ✅ | Static copy/link only. `grep` for `announceForAccessibility`/`setAccessibilityFocus` across all three target files → **zero hits**. No web-announcement dependency, no CSS-only trick, no expo-blur change. Nothing that RN-web 0.21 could silently shear. Trivially feasible. |
| accessNotTradedForPolish | ✅ | Net access GAIN (accurate SR navigation guidance; the FAQ "verified" definition — the app's only one until S1's legend Status block — corrected and kept reachable). No hidden access regression dressed as polish. |
| arbiterReRunPresent | ✅ (n/a) | Touches no color/floor/severity value; S20 correctly states "No arbiter (copy)." Rail set true per the no-color clause. |

## Attacks attempted (and why they failed)

1. **"S20 double-counts L8-14 with S15 — one finding, two slate entries claiming it."** Refuted. `slate-verdict-S15.md:4` confirms S15 (KEEP) owns L8-14's **submit-moment half**; S20 owns the **About half** and says so in its Resolves line and WHAT §(3). Clean partition, no dropped facet, no overlap.
2. **"The About 'open source under a heading literally titled SOURCE CODE' claim is fabricated."** Partially lands as a *finding-precision* nit, NOT a proposal defect. In source, the "…are open" text sits under the **Credits** heading (`AboutScreen.tsx:94-99`), while a *separate* **Source code** heading (:102-103) has a link-less tech-stack body (:105-108). The finding conflated the two headings. But S20's remedy — "add the link/license or soften the claim" — applies correctly either way: an unanchored openness claim exists AND a link-less Source-code heading exists. The fix is sound; the blemish is in L8-14's phrasing, not in S20.
3. **"Softening the 'open source' claim is a Sky product/scope decision smuggled into a copy slate."** Refuted. S20's FORKS-TO-SKY line explicitly states softening needs no decision, and the fix is "add link/license **OR** soften" — both paths safe and reversible. The repo public-vs-private *factual* question is correctly flagged as Sky's; the git remote is `github.com/Skypie99/AccessMap.git` (privacy not determinable from remote alone), which is exactly why S20 leaves the factual call to Sky while the UI copy fix stands on its own.
4. **"It hard-depends on S3, which could be killed, leaving a dangling About edit."** Refuted. `slate-verdict-S3.md` = **KEEP**, so the referenced map-side ledger path is real; and S20 provides an explicit fallback ("or soften it if S3 is deferred"). No hard dependency.
5. **"It touches GlassSurface / a color floor / the blur budget through AboutScreen."** Refuted. AboutScreen consumes `GlassSurface` unchanged; S20's edits are body copy strings only. No color/severity/BlurView reference in any of the three files.
6. **"RN-web announce-on-web no-op trap."** Refuted. Zero `announceForAccessibility`/`setAccessibilityFocus` in scope — it's static copy.
7. **"The FAQ 'corrections' are themselves wrong."** Refuted. Verified against source: magnifier = `Search`/`AddressSearchModal` (`MapScreen.tsx:1306-1312`), filters = `SlidersHorizontal` "Toggle filters" (:1329-1332); tabs = Home/Tasks/Profile (`RootNavigator.tsx:324/331/338`) with Map/others `tabBarButton:()=>null` hidden routes (:351/360/370). The current FAQ strings are genuinely inaccurate; S20's replacements are factually correct.

## fixConditions

None required for KEEP. Two non-blocking notes for the assembler:
- **PROTECT-10 citation imprecision** in field (6): the changelog maker-voice intent is nearer PROTECT-11 than PROTECT-10's Home-distance law. Cosmetic — no crown jewel is touched or regressed. Assembler may relabel.
- **L8-14 About-half evidence precision:** the "…are open" claim lives under the **Credits** heading, not the **Source code** heading, in shipped source (`AboutScreen.tsx:94-108`). S20's remedy is unaffected (it covers both the unanchored claim and the link-less Source-code heading). Worth reflecting in the fix note so the implementer edits the correct lines.

## Reasoning (why not FIX or KILL)

Not KILL: no rail is even close to unsatisfiable. There is no WCAG trade (access is *improved* by correcting false SR-navigation copy), no GLASS surface (no color/floor/token; GlassSurface consumed, never edited; no BlurView added), no RN no-op (static copy, zero announce API), and the sole product/scope question (repo privacy → "open source" wording) is correctly forked to Sky while the reversible copy fix stands independently.

Not FIX: every load-bearing factual claim is code-true at HEAD — the "Map tab" that doesn't exist (`HelpModal.tsx:33` vs `RootNavigator.tsx` tab set), the magnifier that opens address search not filters (`MapScreen.tsx:1306` vs the FAQ), the single six-week-stale changelog entry (`ChangelogModal.tsx:28`), the unanchored "open" claim and link-less Source-code heading (`AboutScreen.tsx:98-108`), and the "My Feedback"/"My feedback history" casing drift. Scoping is honest: the submit-moment half of L8-14 is explicitly S15's, the repo-privacy factual question is explicitly Sky's, and the About "logged/visible" edit correctly sequences behind (or softens without) S3. The only blemishes are two cosmetic imprecisions (a PROTECT number and one line-reference conflation) that are assembler cleanup, not defects in the fix. Clean, high-signal, low-risk, reversible copy repair on the exact surfaces a user consults when trust is already strained. KEEP.
