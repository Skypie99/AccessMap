# DEFECT LEDGER — Flagstone Sim Walk (Phase A)
All findings from build **sim-release @ main `bc91789`** unless tagged otherwise. Tiers: Blocker / High / Med / Low. Every row: repro + evidence + measured numbers where sizing.

| ID | Tier | Screen | Finding | Repro | Evidence | Notes |
|----|------|--------|---------|-------|----------|-------|
| SW-01 | **High** | SignInScreen | **Apple-1.2 Terms consent line invisible at rest** — consent button rect y 948..993 on 956pt (Pro Max): below the fold minus ~8pt under the home indicator. Code's own intent (SignInScreen.tsx:339) says consent "must be visible where the account is created". Will be worse on 17e (shorter screen). Privacy Policy button also ends y=929, 7pt past the 922 safe-area boundary. | Fresh install → complete onboarding → SignIn at rest | A2_signin_light.png + A2_signin_census.json (measured) | Store-review-relevant; fix likely = move consent above the fold or anchor above bottom inset |
| SW-02 | Low (class) | OnboardingCards (cards 3+4) + SignIn bottom links | **Bottom-anchored secondary controls intrude into the 34pt home-indicator inset**: 'Not now' + 'Maybe later' y 884..928 (6pt past 922); SignIn Privacy 929 (7pt); one padding root cause likely | Onboarding cards 3/4; SignIn bottom | censuses (measured) | Same family as SW-01's bottom-inset miss |
| SW-03 | Low | OnboardingCards | Paged carousel exposes duplicated scroll-bar a11y elements incl. 'Vertical scroll bar, 5 pages' on a horizontal pager → likely VoiceOver noise | Census the tree on any card | A1_onboarding_p1_census.json | PLAUSIBLE — may be WDA representation; device VoiceOver owns verdict → DEVICE-ONLY verify |
| SW-06 | Low | OnboardingCards card 5 | CTA a11y label 'Open the map' vs rendered text capitalization ('Open the Map'?) — WCAG 2.5.3 label-in-name nit; needs code read | Card 5 | A1_onboarding_5_allset.png | Phase B checks copy source |
| SW-07 | Obs | SignInScreen | No 'Forgot password' path anywhere on the auth surface (census-complete) | — | A2_signin_census.json | Product choice for Sky, not a defect; support email exists in Terms |

## Positives worth keeping (not findings)
- Onboarding a11y labels exemplary ("Back. Disabled on first card.", "Next. Card 2 of 5.", severity image alt).
- All measured hit targets ≥44pt so far (Show password + sheet closes exactly at 44 floor).
- Legal sheets open from SignIn (today's modal-ordering fix class OK here); both legal docs fully Flagstone-worded (BQ-1 RESOLVED; memory stale).
- Privacy gate honored: no OS location dialog fired by onboarding deny-path; no auto-prompt anywhere yet.
- Keyboard contract on SignIn email: correct type (@ key), field lifts above keyboard.

## Method/state notes
- Interaction = WebDriverAgent (idb impossible: needs CLT 27 admin install). Verified tap proof banked in 00_STEP0_BUILD.md.
- Deny-first permissions strategy: OS location dialog reserved for Map's user-initiated 'Use my location'; notifications for Settings prefs surface.

## Session-2 additions (Tasks/Map segment)
| ID | Tier | Screen | Finding |
|----|------|--------|---------|
| **SW-30** | **BLOCKER (sim-confirmed)** | FlagDetail (all parents) | **Re-opening the SAME flag's detail crashes the hosting screen.** Console: `Error: cannot add postgres_changes callbacks for realtime:flag_comments:<flagId> after subscribe()` → uncaught render error → ErrorBoundary. Repro: Tasks → flag X details → close → map callout (or any parent) → flag X details again → crash. **Blast radius: the open-detail state is shared, so EVERY tab that mounts FlagDetail for that flag dies as you visit it** (observed: FullMap, then Home). Guest comments are auth-gated yet the realtime channel still subscribes (side note for Phase B). Evidence: C3_from_map.png→A10_errorboundary.png, console-promax.log 04:36:07 |
| **SW-31** | High | ErrorBoundary fallback | 'Try again' re-crashes instantly for SW-30 (crashing state lives above the boundary) AND the fallback copy's "switch to another tab and come back" is false (other tabs die too). Only full app relaunch recovers. |
| **SW-28** | High | FlagDetail → View on Map | Opens FullMap WITHOUT focusing the flag (no animateTo, no callout; map stays at user location). The Tasks-card-title path focuses PERFECTLY (evidence pair: A6_map_focused2.png vs A6_map_cardtitle_focus.png) — so the param wiring differs between the two entry points. |
| SW-26 | High | FlagDetail | History + Report buttons silently dead (E2/E3 unreachable) — see A4 bank |
| SW-23 | High (PLAUSIBLE) | FlagDetail | Intermittent AX-tree void — see A4 bank |
| SW-25 | Med | FlagDetail | Copy-coordinates 21×24pt target |
| SW-22 | Med | Tasks cards | Title map-jump button 376×22pt |
| SW-29 | Low | FullMap | Flag markers 38×40pt (map-marker convention vs 44 floor — judgment: acceptable-tolerable, listed for completeness) |
| SW-27 | Low | Tasks meta | "3351 min walk" absurd at distance |

## Session-3 additions (map chrome, dark, AX-type, orientation)
| ID | Tier | Screen | Finding |
|----|------|--------|---------|
| SW-32 | Med | C2 'Name this filter' dialog | Save button absent from AX tree + sits under keyboard; VO users likely cannot reach it (return-key submits). **⚠ CORRECTED 2026-08-20 (Phase B Wave 3) — NEITHER HALF HOLDS. Do not write a fix from this row; see the correction row in the Wave-3 section below.** |
| SW-33 | Med | FullMap filter panel | 'Collapse filter panel' = 90×32pt (under 44 floor) |
| SW-34 | Low | FlagDetail | Reporter attribution inconsistent: "Another community member" (Steep grade) vs "Anonymous" (Broken/Blocked path) for the anonymized case |
| SW-35 | Low | FullMap More-tools | 'Dismiss heat map notice' + 'Collapse heat map legend' = 24-25px targets |

## Cross-appearance / sizing results (NOT defects — verification PASSES)
- DARK pass (OS dark): Home ✓, Tasks ✓ (memory-flagged dark STATUS_COLORS bug on status pills/severity chips = FIXED — all legible), FlagDetail ✓, Settings ✓ (toggle). No dark contrast failures found on walked surfaces.
- DYNAMIC TYPE (accessibility-extra-large): Tasks reflows correctly — cards grow, action pills stack (Verify full-width then Resolved/Reject/Details), no clip/overlap. Minor: "Broken sidewalk" wraps mid-word beside the "• Open" badge (SW-36, Low). Home/SignIn AX shots banked.
- ORIENTATION: app.json orientation=portrait; no rotation UI; lock honored (not separately forced-rotated in sim — DEVICE note).
- LOADING: skeletons glimpsed on list mounts (fast on sim). ERROR: SW-30/31 boundary is the real error-state finding (not induced — occurred naturally).

## Session-4 additions (17e small-screen pass)
| ID | Tier | Screen | Finding |
|----|------|--------|---------|
| **SW-37** | **High** | ReportFlagModal (no-location) | Anonymous report dead-ends: location denied → "Waiting for location…" forever, Submit DISABLED even fully filled, no manual pin-placement fallback. Core value flow blocked for privacy-conscious guest. Repro on 17e (location never granted). Same root as SW-11. |
| SW-01 (upgrade) | **High** | SignInScreen @ 390×844 | Consent line + Privacy button ENTIRELY below the fold (y933–978 / y869–914 on 844pt screen) — Apple-1.2 UGC consent fully off-screen at rest. Confirmed worse as screen shrinks. |
| SW-36 | Low | Tasks @ AX-XL | "Broken sidewalk" wraps mid-word ("sidewal/k") beside the Open badge |

## SEVERITY ROLLUP (Phase A ledger, de-duplicated)
- **BLOCKER (1):** SW-30 (re-open same flag → realtime-subscribe crash, cross-tab blast radius)
- **HIGH (7):** SW-01 (consent off-screen, worse on small), SW-23 (FlagDetail AX-tree void — PLAUSIBLE, device-VO verify), SW-26 (History+Report buttons dead), SW-28 (View-on-Map no focus), SW-31 (ErrorBoundary can't recover SW-30 + false copy), SW-37 (report dead-ends w/o location)
- **MED (9):** SW-08 (SF default vs data), SW-09 (clear-search 16×17), SW-11 (silent location deny dead-end), SW-12 (Report FAB 42pt), SW-20 (guest push switch silent-disabled), SW-22 (task title 376×22), SW-25 (copy-coords 21×24), ~~SW-32 (preset Save unreachable/AX-void)~~ **— premise did not hold, see correction row**, SW-33 (collapse-panel 32pt)
- **LOW (8):** SW-02, SW-03, SW-06, SW-16, SW-17, SW-19, SW-21, SW-27, SW-29, SW-34, SW-35, SW-36 (cluster; see rows)
- **OBS (non-defect):** SW-07 (no forgot-password), SW-14 (sparse guest profile)

---

# ═══ SESSION-5 ADDITIONS (AUTHED PASS) ═══
**All rows below:** build **sim-release @ main `bc91789`** (handshake re-verified at session start — main unmoved). Walked signed-in on the Pro Max (440×956) unless tagged 17e.

## ★★ SCOPING CAVEAT THAT GOVERNS EVERY ROW BELOW — READ FIRST
**The signed-in account is an ADMIN (`is_admin = true`), confirmed in-app.** Sky expected it was not. Evidence: drawer renders an **Admin** row (`shots/promax-authed/03_drawer_ADMIN_ROW.png`), and the tab bar count moved 5 (guest) → **6** (signed in) — `RootNavigator.tsx:409` registers the Admin route ONLY when `isAdmin === true`.
**Why she didn't know:** `src/lib/admin.ts` documents that `authenticated` held no SELECT grant on `users.is_admin` until **2026-08-18** — the hook returned 42501 and `?? false` laundered it into a clean-looking "not an admin" for months. The grant is now live, so the gate started honoring her flag two days before this walk. Her account did not change; the gate started working.
**Consequence for this pass:** authed surfaces are walked through an admin's eyes. Any affordance that is admin-only may be present here and ABSENT for a normal signed-in user. Rows below state explicitly when a control is admin-gated in code. **A normal-user authed pass remains uncovered** — logged as a coverage gap in the close-out, not silently absorbed.
**Consequence for safety:** admin reject carries a **−20 spam penalty against a real reporter** (CLAUDE.md points trigger). The Production Law is therefore tightened, not relaxed: destructive actions fire ONLY on this session's own `[SIMTEST]` row.

| ID | Tier | Screen | Finding | Repro | Evidence | Notes |
|----|------|--------|---------|-------|----------|-------|
| SW-38 | Med | Tab bar (all tabs, signed in) | **The tab-bar a11y count leaks account role and remains wrong for everyone.** VoiceOver announces "Home, tab, **1 of 6**" signed in as admin vs "1 of 5" signed in as non-admin vs "1 of 5" as guest — against a bar with **3 visible tabs**. The count enumerates hidden routes (FullMap, Settings, Admin) that have `tabBarButton: () => null`. Extends **SW-13** (banked: *"tabs announce 'tab N of 5' on a 3-tab bar"*, `screens/A3_home_guest.md`), which **explicitly predicted** *"signed-in admin would announce 'of 6'"* — this session **confirms that prediction on a live admin account**. The NEW fact is the consequence: the number is a **role oracle**, so a listener can hear whether the account is an admin. *(Correction: an earlier draft of this row cited SW-21, which is a different finding — a banner-preferences copy mismatch.)* | Sign in → census any tab button label | `shots/promax-authed/03_drawer_ADMIN_ROW.png` + census (both counts recorded this session) | Root: hidden `Tab.Screen`s still count toward the navigator's child total. Phase B fix likely `tabBarAccessibilityLabel` or excluding hidden routes from the count. |
| SW-39 | Med | A5b Profile (signed in) | **The "VERIFIED" headline stat contradicts the activity feed directly above it, and the three tiles don't reconcile.** Screen shows **6 REPORTED · 0 VERIFIED · 3 RESOLVED** while RECENT POINT ACTIVITY two inches above reads "Your report was verified · +10 pts" **twice**. Cause: the row mixes two different metrics — `reported` is a LIFETIME total (`Math.max(statusRows.length, statusSum)`, ProfileScreen.tsx:372) while `verified`/`resolved` are CURRENT-STATUS snapshots (`stats.byStatus.verified`, `:1162`; `resolved: byStatus.resolved`, `:373`). A report that was verified and then resolved leaves `verified`, so the count is technically true and reads as false. Compounding it, **rejected (3) is omitted from the row**, so 0 + 3 ≠ 6 with no visible remainder. | Sign in → Profile tab → compare the tile row against RECENT POINT ACTIVITY | `shots/promax-authed/A5b_profile_light.png` (both visible in one frame); `screens/A5b_profile_census.json` | Not a data bug — a semantics bug. Phase B decision: either relabel the tiles to current-status wording and add Rejected, or make Verified/Resolved lifetime counts to match the feed. Do NOT "fix" by changing the trigger. |
| SW-40 | Med | A5b Profile (signed in) | **Tier chip below the 44pt floor.** "Bronze tier. Tap to see all tiers." measures **87×33pt** — 11pt under. It is a real interactive control (opens the tier explainer), not decoration. Same family as SW-25/SW-33/SW-09. Secondary: the display-name TextField is **286×39** (5pt under). | Profile → census the tier chip rect | `screens/A5b_profile_census.json` (measured) | Adds two rows to the Phase B hit-target sweep; the chip is the actionable one. |
| SW-41 | Low | A5b Profile (signed in) | **Two stacked progress bars render as visual duplicates.** "Bronze tier, 90 of 100 points to Silver" (y385) and "Progress toward Engaged badge, 90 of 100 points" (y431) are both 90/100, both ~90% orange, stacked 46pt apart — they read as one bar accidentally drawn twice. Honest caveat: the Silver threshold and the Engaged-badge threshold merely COINCIDE at 100 for this user's tier; they may diverge elsewhere, so this is a presentation collision at this level, not necessarily a duplicated data source. | Profile at 90 points | `shots/promax-authed/A5b_profile_light.png` | Worth a Phase B look at whether the two tracks can ever differ; if rarely, collapse to one bar with two markers. |
| **SW-42** | **High** | C11 MyReportsModal + C12 MyWatchedModal (profile sheet family) | **The two profile sheets that wrap their card in a `KeyboardAvoidingView` render far too short and clip their own content, with the keyboard closed.** Measured card heights on a 956pt screen against the sheets' own `maxHeight:'85%'` (=813pt) cap: **C9 Achievements 692pt (72.4%) · C10 ActivityFeed 692pt (72.4%) · C11 MyReports 500pt (52.3%) · C12 MyWatched 352pt (36.8%)**. The two that land on the healthy 692/121 baseline (C9, C10) contain **no KAV**; the two that collapse (C11, C12) both wrap the card in `<KeyboardAvoidingView behavior="padding">` with **no `keyboardVerticalOffset`**, inside a `<Modal transparent>` presented over a bottom tab bar (MyReportsModal.tsx:288-289, MyWatchedModal.tsx:306-307). Because `card` also sets `overflow:'hidden'` (MyReportsModal.tsx:528), overflowing content is **clipped rather than scrolled**. Consequences: **C12 — "No watched flags yet" is sliced horizontally and its instruction line ("Open any flag… tap Watch to track it") is 100% invisible though present in the AX tree at y836; a user with no watched flags cannot read how to get one.** C11 — the list viewport is only **198pt**, showing ~1.5 of 6 report cards (it does scroll, so reachable but badly degraded). | Sign in → Profile → Watched Flags (worst) or My Reports | `shots/promax-authed/C12_mywatched.png` (clipped heading, unmistakable), `C11_myreports.png`, `C10_activityfeed.png`, `C9_achievements.png`; measurements via `tools/sheetgeom.py`, banked in `screens/C9-C12_profile_modals_authed.md` | Not an animation artifact — C11 re-measured identical at +6s, C12 at +7.5s. **Scoped honestly:** the residual 121pt gap shared by ALL FOUR sheets (card bottom y835 vs screen 956, clearing the tab bar at y861) is consistent and plausibly the intended "sheet sits above the tab bar" design — that part is NOT claimed as the defect. The defect is the KAV pair falling below that shared baseline. Phase B: add `keyboardVerticalOffset` / drop `behavior="padding"` for these tab-bar-hosted modals, and re-check that `overflow:'hidden'` isn't hiding overflow that should scroll. |
| SW-43 | Med | C10 ActivityFeed + C11 MyReports rows (and Tasks cards) | **The tappable row TITLE is ~21–29pt tall across three list surfaces** — C11 report titles **318×21**, C10 activity titles **320×29** — repeating the banked **SW-22** (Tasks card title 376×22). Now confirmed as a shared list-row pattern rather than one screen's slip, so the Phase B fix is one row component, not three. The adjacent "Show … on the map" buttons are correctly 44×45 in both, which is what makes the title's 21pt read as an oversight. | Open either modal → census any row title rect | `screens/C10_activityfeed_census.json`, `screens/C11_myreports_census.json` (measured) | Extends SW-22; cite together in Phase B. |
| SW-44 | Med | C14 Leaderboard | **Every anonymized contributor is given a "ME" avatar monogram, which reads as "me".** `LeaderboardScreen.tsx:158-159`: `const name = displayName ?? 'Member'` then `name.slice(0,2).toUpperCase()` → **"ME"**. On screen, 1st, 3rd and 4th place all wear a **ME** badge while the row that actually IS the signed-in user ("2nd, Jarvis Mckneil … you") wears **JA**. The one monogram that means "me" is the one place the user is not. | Sign in → Profile → See leaderboard → look at the avatar circles | `shots/promax-authed/C14_leaderboard.png` (three ME badges + one JA visible in one frame) | Cheap Phase B fix: give the anonymized fallback a non-initial glyph (person icon) or derive nothing when `displayName` is null. The `'Member'` label itself is correct and privacy-preserving — only the derived initials are the problem. |
| SW-45 | Med | C14 Leaderboard | **The leaderboard sheet overlaps the tab bar and paints list rows on top of it.** Card runs y548→956 (flush to bottom) while the tab bar occupies 861–914, so the 4th-place row renders directly over a ghosted "Home / Tasks / Profile" — including the red **9** Tasks badge showing through behind the row text. Also, with only 4 entries the 4th requires a scroll to reach despite the sheet appearing to have room. Sibling sheets (C9–C12) stop at y835 and never overlap, so this is an inconsistency inside one family, not a house style. | Profile → See leaderboard → scroll the list one notch → observe the bottom row over the tab bar | `shots/promax-authed/C14_leaderboard_scrolled.png` (row text over ghosted tab bar + badge) | Pair with SW-42 in Phase B — one decision for the whole sheet family: either all sheets clear the tab bar or all cover it. |
| **SW-46** | **BLOCKER** (supersedes/upgrades **SW-26**) | C3 FlagDetail → E2 ReportContentModal + E3 StatusHistoryModal | **ANSWER TO THE PHASE-A QUESTION: SW-26 is neither an auth-gate bug nor a wiring bug — it is an iOS modal-presentation conflict, and it affects EVERY user, signed in or out.** The handlers are correct, unconditional and DO fire; iOS then silently refuses to present. Proof — with the account signed in, tapping each button produced a UIKit console line at the exact tap second, with a distinct new modal address each time, while no sheet appeared and no in-app error surfaced:<br>• History (16:41:24): `Attempt to present <RCTModalHostViewController: 0x12f521900> on <UIViewController: 0x1051b8c00> … which is already presenting <RCTModalHostViewController: 0x1273b5e00>`<br>• Report (16:42:13): same line, `0x12f522300`.<br>`0x1273b5e00` is the already-presented FlagDetail Modal. Both sheets are mounted as **sibling `<Modal>`s after `</Modal>`** (FlagDetailModal.tsx:2161 & :2174) and iOS permits only one presented modal per view controller.<br>**Why this is a Blocker, not a High:** `ReportContentModal` is the **Apple Guideline 1.2(b) objectionable-content report path**, and it is mounted in exactly ONE place app-wide — FlagDetailModal — with both triggers inside it (`:1777` report a flag, `:1935` report a comment). There is no alternative route. **So a UGC app is shipping with its abuse-report mechanism 100% unreachable for all users, for both flags and comments** — the exact requirement App Review checks. The in-code comment at `:1753-1770` argues the control is guest-visible precisely so a reviewer signed out can find it; the control is findable and dead. | Sign in (or not) → any flag → View flag details → tap **History** or **Report** → nothing opens; console logs the refusal | `shots/promax-authed/E3_statushistory_ATTEMPT.png`; `logs/console-authed.log` lines at 16:41:24 and 16:42:13 (both quoted above); census before/after shows no new sheet, only `Close flag details` | **Phase B fix direction is already in this codebase:** `useLegalSheets()` exists because, in the project's own words, "iOS can't present the root pool over a presented modal" — the legal sheets got an in-Modal workaround and these two did not. Render E2/E3 as views INSIDE FlagDetail's Modal (or dismiss-then-present). **Also correct two false comments that cite this broken pattern as proven precedent:** FlagDetailModal.tsx:2169 ("StatusHistoryModal above is the shipped precedent for a payload-carrying sheet stacked over this one") and MapScreen.tsx:2808 ("already proven here (StatusHistoryModal stacks over FlagDetailModal)"). Neither was ever true; the second has already been used to justify another decision. |
| **SW-47** | **BLOCKER — re-test result for SW-30** | C3 FlagDetail (cross-parent) | **SW-30 reproduces IDENTICALLY when signed in — auth is irrelevant — and the trigger is now sharper than Phase A recorded.** Signed in, comments genuinely render (composer + "No comments yet — share what you know." both present), and the crash is byte-identical: `Error: cannot add \`postgres_changes\` callbacks for realtime:flag_comments:**29718d8c-59c4-4fb6-997b-6937b71924fa**  after \`subscribe()\`` → `[ErrorBoundary] uncaught render error` → fallback. **Refinement of the repro (this is the useful new part):** re-opening the same flag from the SAME parent does **NOT** crash — tested twice, including a deliberate fast close→reopen race with no settle. The crash needs a **SECOND FlagDetail host to mount for the same flagId**: Tasks → details → close → map callout → details = **crash**. Mechanism: `supabase.channel(topic)` is a singleton per topic, and `useComments` keys the topic on flagId alone (`useComments.ts:101`), so two hosts alive for one flag both call `.on('postgres_changes', …)` on one already-subscribed channel. The effect has **no auth condition** (deps `[flagId, tableNotReady, fetch]`, `useComments.ts:129`), which is exactly why signing in changes nothing. | Sign in → Tasks → View flag details → close → tap card title (map focuses flag) → tap callout → details → crash | `logs/console-authed.log` @ 16:47:39 (full error + boundary line); `shots/promax-authed/SW30_errorboundary_authed.png` | Phase B: the fix is per-topic channel ownership (reuse the existing channel and add no second listener, or scope the topic per host instance), NOT an auth gate and NOT "don't subscribe for guests". Verdict requested by the prompt: **IDENTICAL, not worse, not absent.** |
| SW-48 | Med — **correction to SW-31** | ErrorBoundary fallback | **SW-31's "Try again re-crashes instantly" did NOT hold under auth.** After the SW-47 crash, one tap on **Try again** recovered cleanly to the **Home** tab with **zero** re-crash lines in the console and a healthy tab bar. Phase A's claim that only a full relaunch recovers is therefore not universally true. Not asserting SW-31 was wrong in its original context (it was a guest, map-parented crash) — recording that the premise is state-dependent so Phase B does not "fix" a recovery path that already works here. The second half of SW-31 (the fallback copy telling users to "switch to another tab and come back", which is false during a live cross-tab crash) is untouched by this and still stands. | Trigger SW-47 → tap Try again once | `logs/console-authed.log` (no re-crash after the tap); census returned `Home, tab, 1 of 6` | Phase B: re-verify SW-31's premise before writing a fix for it. |
| SW-49 | Med | A7 Settings — Push notifications toggle | **The push toggle intermittently ignores taps right after Settings mounts, with no feedback of any kind.** Two consecutive attempts (coordinate tap at the switch centre, then a WDA element click) immediately after opening Settings produced **no state change, no alert, and zero `handlePushToggle error` lines in the whole console log**. A control experiment ruled out the driver: the "Notify on Open" switch in the banner-preferences sheet toggled reliably in the same session ('1'→'0'→'1'), and the push switch itself toggled normally on a later attempt. So the control is genuinely unresponsive for a window after mount while still rendering as an enabled, interactive switch. | Sign in → drawer → Settings → tap the Push notifications switch immediately on arrival | `logs/console-authed.log` (no handler error at any point); `shots/promax-authed/G3_push_switch_authed.png` (switch renders ON, undimmed, full opacity) | Mechanism NOT confirmed — stated as hypothesis: `handlePushToggle` opens with `if (!user || pushBusy) return;` (SettingsScreen.tsx:328), the only silent path in the handler, so a transient null `user` or `pushBusy===true` while the Switch is still rendered (rather than the intended `ActivityIndicator`) would produce exactly this. Phase B should confirm before fixing. Related to but distinct from **SW-20** (guest: switch disabled without dimming) — signed in the switch reports `enabled=1`, so SW-20's guest gating is resolved by auth. |
| **SW-46-A** | **Refinement of SW-46 — the Phase B fix, proven in-app** | C3 FlagDetail → E2/E3 | **A second RN `<Modal>` CAN present over a presented modal in this app — the failing pair are simply mounted in the wrong place.** Proof found this session: `PhotoLightboxModal` **is** a real `<Modal>` (`PhotoLightboxModal.tsx:45`) and it opened **successfully over the already-presented ReportFlagModal** (full-screen "Photo 1 of 1" + "Close photo" 44×44) — no UIKit refusal, no console warning. The difference is the mount point, and the UIKit error names it: *"Attempt to present … **on <UIViewController: 0x1051b8c00> (from <RNSScreen: …>)** which is already presenting …"* — the **screen's** view controller. E2/E3 are mounted as siblings **after `</Modal>`** (FlagDetailModal.tsx:2161, :2174), so they resolve to the screen's VC, which FlagDetail already occupies. `PhotoLightboxModal` is reached from `PhotoGallery`, rendered **inside** the presenting modal's own subtree, so it presents on that modal's VC — which is free. | Compare: report modal → Add photo → tap thumbnail → lightbox opens ✓ vs FlagDetail → History/Report → nothing ✗ | `shots/promax-authed/C8_lightbox_attempt.png` (lightbox over the report modal) vs `E3_statushistory_ATTEMPT.png` + the two refusal lines in `logs/console-authed.log` | **Phase B: move `<StatusHistoryModal>` and `<ReportContentModal>` INSIDE FlagDetail's `<Modal>`, exactly as `PhotoGallery` does with its lightbox.** The codebase already contains the working pattern twice (PhotoGallery's lightbox, and `useLegalSheets()`); only these two sheets missed it. This also invalidates the "sibling Modal pattern" comment at `PhotoGallery.tsx:189` as a general recipe — it works there only because of where it is mounted, not because siblings are fine. |
| SW-50 | Med | C7 ReportFlagModal — attached-photo controls | **"Remove photo 1" measures 28×29pt** — 15pt under the 44pt floor, and it sits overlapping the 96×97 thumbnail it belongs to, so a miss opens the full-screen lightbox instead of removing the photo. Destructive-adjacent control at half size. Same family as SW-25/SW-33/SW-40/SW-09. | Report a flag → Add photo → choose from library → census the remove badge | `screens/C7_report_authed_census.json` (measured); `shots/promax-authed/E1_photo_attached.png` | The sibling controls are correctly sized (thumbnail 96×97, Add photo 96×97, Close photo 44×44), which is what makes the 28×29 badge look like an oversight rather than a style. |
| SW-51 | Low | A5b Profile @ accessibility-extra-large | **Mid-word wrapping recurs on the Profile breakdown lists at AX-XL** — "Broken sidewal / k", "Modera / te" (Moderate), and "Signific…" clipped in the BY CATEGORY / BY SEVERITY rows. Extends the banked **SW-36** (Tasks: "sidewal/k") to a second surface, so the Phase B fix is a shared text/`numberOfLines` treatment rather than one screen's slip. The rest of the screen reflows correctly at this size (nav rows grow 67→145-266pt, buttons 44→51pt, no clipping or overlap). | `xcrun simctl ui <udid> content_size accessibility-extra-large` → Profile → scroll to BY CATEGORY | `shots/promax-authed/A5b_profile_AX_XL.png` | Cite with SW-36. |
| **SW-52** | **High (privacy)** | C7 ReportFlagModal | **A photo attached to a report you CANCEL is silently carried into your next report and published to the public map.** Observed live: I attached a library photo to a report, tapped **Cancel and close**, later opened a *fresh* report, filled it with unrelated content, and submitted — and the submitted flag carried the photo from the abandoned draft. Proof is independent of the screenshot: the points feed awarded **"Earned 3 points: Added a photo"** for a report in which I never opened the photo picker, and the created flag's detail rendered the earlier image. Cause, confirmed in code: `reset()` clears `photoUris`/`photoAlts`/description/category/tags but its own comment states it **"only runs after a successful submit"** (ReportFlagModal.tsx:225-236); the modal is a persistent `visible`-prop component that never unmounts, so Cancel leaves every field populated for the next session. | Report a flag → Add photo → choose any photo → **Cancel and close** → open Report a flag again → submit without touching the photo section → the abandoned photo is attached | `shots/promax-authed/W3_comment_composer_reach.png` (the flag detail showing a photo I never attached to it); Profile activity row "Earned 3 points: Added a photo" | **Privacy-sensitive, so it belongs in front of Sky before Phase B touches it** (Const. hard prohibition #5 — surface privacy changes first). A user can attach a personal photo, think better of it, cancel — and have it published on a public map attached to an unrelated report they file later. EXIF is stripped on upload, so coordinates don't leak, but the image itself does. Phase B: call `reset()` on cancel/dismiss as well as on success, or clear `photoUris` in the `!visible` branch. |
| SW-53 | Med (docs) | Points economy vs CLAUDE.md | **The live points trigger awards four things CLAUDE.md doesn't document, so the project's own reference under-states the economy by a factor of ~2 for a first report.** Measured end-to-end on a real account this session: **Reported a barrier +5 · Added a photo +3 · Added a comment +1 · Report verified +10 · Report resolved +15** — 90 → **124** across one flag, reconciling exactly. CLAUDE.md's "Points trigger" section documents only the verify/resolve awards (+10/+15 reporter, +3/+7 actor) and states "Forward-only", omitting +5/+3/+1 entirely. | Profile → RECENT POINT ACTIVITY after creating a flag with a photo and a comment | Profile census: the five "Earned N points" rows quoted above; arithmetic 90+5+3+1+10+15 = 124 ✓ | Load-bearing because CLAUDE.md explicitly warns the TasksScreen flash strings are coupled to the trigger and must be updated together — a doc that omits four award types will mis-guide exactly that edit. Phase B (or Will) should reconcile CLAUDE.md against `supabase/schema.sql`. |
| SW-42 (17e) | — | C12 MyWatched @ 390×844 | **SW-42 confirmed on the small screen with a diagnostic nuance:** the sheet card measures **354pt on the 17e vs 352pt on the Pro Max** — essentially *constant* despite a 112pt difference in screen height. The clipping is identical on both (empty-state heading sliced, instruction line entirely outside the card). Because the height does not scale with the screen, the cause looks like a **fixed/collapsed height**, not a mis-evaluated percentage — a useful narrowing for Phase B, since `maxHeight:'85%'` would have produced 813pt / 717pt respectively. | 17e → Profile → Watched Flags | `shots/17e-authed/C12_mywatched_17e.png`; measurements in `screens/17e_authed_smallscreen.md` | Not a new ID — evidence appended to SW-42. |
| **SW-32 (corrected)** | Med — **correction to SW-32** | C2 'Name this filter' dialog | **SW-32's two claims BOTH failed on re-walk, on the 17e — the smaller phone, where the keyboard half would be worst.** Censused with the dialog open: `Name this filter` header, `Filter set name` TextField, `Cancel` **149×45** and **`Save filter set` 149×45 — present, not absent.** The dialog also sits well clear of the keyboard (~135pt of gap below the button), so "sits under the keyboard" does not hold either. Save reports **`enabled=0`** with an empty draft, which is correct AND announced — so it is not a silent no-op, and Cluster E's rule is already satisfied here. Clicking it with an empty name did nothing and left the dialog open, as it should. **The observation was real and I reproduced it:** my first census, 5 s after the tap, contained NONE of the dialog — only the map and filter panel beneath it — and a re-census 3 s later had all four elements. It is a `transparent` Modal with a fade, and a snapshot taken across that transition reads the layer below. So this is a diagnosis error, not an observation error, exactly like SW-48's correction to SW-31. **No fix was written.** | FullMap → Toggle filters → "Save current filter" → census twice, ~3 s apart | `shots/wave3-verify/SW32_name_filter_dialog_clear_of_keyboard_17e.png` (dialog + keyboard in one frame); censuses banked in the Wave 3 run | The one REAL miss on that surface: `Filter set name` measured **308×43** — a bordered TextInput, same 1–2pt border-inset class as TasksScreen's search field and FeedbackModal's reply field. Fixed with them (`nameInput`, which serves both name dialogs); re-measured **308×45**. |
| SW-43 (17e) | — | A4 Tasks rows @ 390×844 | Row titles measure **326×22** on the 17e, matching the Pro Max (376×21 / 318×21 / 320×29). The adjacent action buttons all clear the floor at this width (100×45, 67×45, 69×45, 69×45), so the title is the sole offender at every size. | 17e → Tasks → census a row title | `screens/A4_tasks_17e_census.json` | Evidence appended to SW-43. |

---

# ═══ WAVE 4 CORRECTIONS (2026-08-21) ═══
**Full detail:** `PHASE_B_WAVE_4_RESULT.md`. Rows here so a later sweep does not "fix" something
that works, or re-report something already ruled on.

| ID | Correction |
|---|---|
| **SW-34 (corrected)** | ⛔ **The premise does not hold. Do not "pick one string".** "Anonymous" and "Another community member" are **different cases**, not one case rendered two ways: `FlagDetailModal.tsx:1411-1429` branches `user_id === null` → **Anonymous** (a pill; VoiceOver hears "Reported anonymously"), own flag → **You**, another account → **Another community member**. Showing "Another community member" for a null-`user_id` flag was **already reported and fixed** as semantically incorrect (`qa-reports/2026-05-30_Alex_AnonReporting_A11y.md:47-55`), and the contract is written at `src/lib/hiddenContent.ts:23-26`. Collapsing them walks that fix back. **The real drift, unfixed and worth Sky's eye:** `'Anonymous'` does double duty — anonymous-by-choice on flags, *no display name / deleted account* on comments — while the same no-display-name condition is `'Member'` on the leaderboard and `'(not set)'` in export. Three words, one condition. Unifying is new wording (copy gate) and two source-pinning tests must change with it. |
| **SW-41 (corrected + fixed)** | ⛔ **Do not collapse the two progress bars.** They genuinely diverge: tier cutoffs `0→100→500→1500`, badge cutoffs `25→100→500→1000`. They coincide **only in the 25–499 band**, which is where the walked account sat at 90 points — a collision, not a duplication. ✅ **The real defect beside it is fixed:** the badge bar filled from the previous milestone but announced `min: 0`, so at 300 points it was drawn at 50% and announced as 60% (WCAG 1.3.1). Now reports the range it draws. The tier bar was always correct and is pinned as must-not-regress. |
| **SW-36 (two passes)** | ✅ Fixed — **but the first attempt was not a fix, and only the device said so.** Freeing `cardTitle`'s flex basis + `flexWrap` on `cardHeader` left the title still shrinkable below its own longest word, so it wrapped to a second line and STILL rendered "sidewal / k". `minWidth: 130` is the floor that makes the wrap fire. The gate was green and four new suites passed against the broken build. **Verified after the floor on both devices, both appearances, at accessibility-extra-large.** |
| **SW-36/SW-51 (scope note)** | ⛔ **There is no shared list-row component.** Wave 3's "one shared row component, one fix" was wrong — `hitTargetFrame.guard.test.ts:112-117` enumerates four separate files. Four sibling rows share the basis-zero shape and are **deliberately not swept**: none renders a `SeverityBadge` (one call site app-wide), all three modal rows already carry `minWidth: 0`, none was reported defective, and all are behind auth. Reasons recorded in `flexBasisUnderLargeType.guard.test.ts`. |
| **SW-06 (corrected + fixed)** | ✅ Fixed at **both** sites (`OnboardingCards` card 5 and `OnboardingModal`'s finisher). ⛔ **But it is not a WCAG 2.5.3 failure**, contrary to the brief: `labelInName.guard.test.ts` enforces that criterion tree-wide and **passed before and after**, because its normalizer case-folds — correctly, since 2.5.3 exists for speech input and voice control is not case-sensitive. A consistency nit worth one character, not an accessibility defect. |
| **SW-17 (extended)** | ⛔ **Still live, and on a SECOND surface the brief did not scope.** Verified on device: the replay finisher labelled "Open the Map" lands on **Settings**, and the *first-launch* card-5 CTA of the same name lands on **SignInScreen**. Neither opens the map. An honest label is new wording → drafted for §A, not shipped. |
| **SW-03 (narrowed)** | ⛔ Still device-only, but **half of it is answered**: the *duplication* is a WebDriverAgent artifact, not an app defect. Every scroll-bar node in every Wave-4 census came in identical pairs — including on SignInScreen, which has a single ScrollView, and **inside iOS's own location-permission alert**, which this codebase does not author. The *counts* remain real (the pager nests five per-card vertical scrollers for large-type reachability). Whether VoiceOver announces a vertical scroll bar on a horizontal pager still needs a real device. |
| **SW-16 (confirmed deliberate)** | ⛔ **Not a defect — do not remove it.** Censused on device, the replay modal exposes exactly three elements, of which the 1×1pt node is one. The pager is hidden with `decorativeProps`, so that node is the **only** accessible copy on the surface. Removing it would silence the replay intro for screen-reader users entirely. |
| **N-2 (corrected)** | ⛔ **The premise does not hold, and the finding had been dropped from every rollup after Wave 1.** The Report sheet's "Terms & Community Guidelines" link reproduces at Wave 1's exact frame `[20,810,400,44]` on the Pro Max — and **one scroll of the sheet body moves it to `[20,682,400,45]`**, visible above the pinned Cancel/Send row. Same on the 17e. It is the last item in a `ScrollView` body; content below the fold in a scroller is not a defect. Wave 1's measurement was right, its diagnosis was not. **No fix written.** Third instance of this shape after SW-48→SW-31 and Wave 3→SW-32: *a census of a scrollable surface is a census of its scroll position.* |
| **SW-02 (verified)** | ✅ Confirmed fixed on device, both halves, both devices. 17e: "Not now" `[151,766,88,44]` and "Maybe later" `[139,766,112,44]` — both end at exactly **810**, the boundary. SignIn consent ends at **810** (17e) / **922** (Pro Max), at rest. **Residual, outside SW-02's wording:** `OnboardingModal.tsx:257` uses `insets.bottom + spacing.md` — additive, so it never intrudes — but with no floor and via the throwing `useSafeAreaInsets()`, unlike its two siblings. Sky's call. |

## Wave 4 — the copy pass (Sky ratified 2026-08-21)

| ID | Outcome |
|---|---|
| **SW-17 (fixed)** | ✅ **Labels changed, behaviour deliberately NOT.** First-launch card 5 → **"Continue"**; replay finisher → **"Done"**. Both destinations were already correct (a replay opened from Settings belongs back in Settings; the auth gate cannot be skipped), so changing behaviour would have made a real bug out of a cosmetic one. The two `accessibilityHint`s claiming the button "opens the map" were corrected with them. Pinned in `onboardingCoherence.guard.test.ts`. |
| **SW-21 (fixed)** | ✅ **The reported subtitle mismatch was the symptom; the defect was a name collision.** The sheet was titled **"Notifications"** while `SettingsScreen` separately offers **"Push notification types"** — two unrelated features, one word. Renamed to **"Updates"** across the sheet, the Settings row and the Profile row, after the banner's own word ("N updates since your last visit"); `UpdateBanner` renders only on Profile, so the sheet's existing purpose line was accurate and survives. "Push notification types" is pinned as must-not-regress. |
| **SW-34 (fixed, after the premise failed)** | ✅ Flags keep **all three** cases (Anonymous / You / Another community member) — three different facts, all correct. Comments' `display_name ?? 'Anonymous'` → **`?? 'Member'`** in `FlagDetailModal` and `HiddenCommentsModal`: a missing display name is not a privacy *choice* (no name set, or the account is gone under `ON DELETE SET NULL`), and "Anonymous" claimed a decision the person never made. `'Member'` is the leaderboard's already-ratified word for this case. **Two source-pinning tests changed deliberately** (`commentAuthor.test.ts:125`, `hiddenComments.test.ts:60`), plus `copy.ts`'s docblock and a `CommentBubble` note. ⚠️ **`dataExport.ts`'s `'(not set)'` is NOT part of this** — an earlier draft wrongly counted it as a third word for the same condition; it describes your OWN missing name in your OWN export, beside `email ?? '(no email on file)'`, and is correct as it stands. Pinned in `oneNameOneThing.guard.test.ts`. |
