# LENS 9 — THE CLAIMS CENSUS + VERDICT (banked 2026-07-31)

Law: the page and the product must tell the same true story. Every checkable published claim scored TRUE / FALSE / PARTIAL with evidence. Any FALSE claim is Blocker-class (fix the product or fix the page — **Sky words all page changes**).

| # | Claim | Source | Verdict | Evidence |
|---|---|---|---|---|
| C-1 | "**WCAG 2.1 AA — fully accessible from signup to reporting**" | `README.md:18` | **PARTIAL → Blocker-class as worded** | (a) "Fully" is falsified by this audit's own findings **inside the claimed path**: sign-in client-side validation errors are silent to iOS VoiceOver and web SR users (A11Y-203, `SignInScreen.tsx:45-52`) — the signup leg; focus-in absent on most dismissables incl. the report flow's neighbors (SR-070/A11Y-201). (b) The claim names **2.1** while the house standard is **2.2** (QA_PLAN_A11Y, this train) — the 2.2-six are not covered by the claimed standard. (c) Native SR behavior (focus return, escape, announcements-as-utterances) has zero binary/device evidence yet (SR-021; D-B/D-A rows open). The app is genuinely strong — but "fully accessible" is a conformance promise the evidence doesn't yet carry. Remedy: Sky rewords (e.g. "built to WCAG 2.2 AA, verified continuously") **or** Phase B closes A11Y-203 + the focus-in class and the device pass lands — then the sentence is honest at 2.2. programmatic. |
| C-2 | Hosted privacy policy = the app's current policy | `app.json` → `https://skypie99.github.io/AccessMap/privacy/` | **FALSE — Blocker-class** | The hosted page is the **pre-ratification May 30 v1.0 text**. It says account deletion lives at "**Settings > Account > Delete Account**"; the product has it on **Profile**, and the Run-3 [V] gate caught exactly this, Sky corrected the **in-app** policy (§SKY-9, `15_PRIVACY_POLICY_v1.md:30`: "delete your account any time from your **Profile**"). The hosted copy was never updated (hosting is Sky-physical, §SKY-8 P-3 — this is the reminder it's still pending). Two published statements of a legal right now disagree, and App Store Connect points reviewers/users at the stale one. Fix: Sky republishes the ratified text to GitHub Pages (or repoints `privacyPolicyUrl`). Evidence: WebFetch of the live page this session + `15_PRIVACY_POLICY_v1.md` + privacy.guard (in-app verbatim-pinned). programmatic. |
| C-3 | Heatmap is "privacy-safe k≥3 minimum" | `README.md:10` | **TRUE** | Code enforces it (`heatmap.ts` `DEFAULT_K_FLOOR = 3`, dropped in `bucketFlagsToCells`, guarded by tests); the in-app trust voice states it (`LegendModal.tsx:193-194`: "To protect reporters, heat zones only appear where at least 3 flags have been submitted." — the PROTECT-11 sentence, verified present at HEAD). README ↔ code ↔ product agree. programmatic. |
| C-4 | "Get notified when flags you reported are resolved" | `docs/APP_STORE_LISTING.md:40` | **PARTIAL (deploy-gated)** | Client infra real (`expo-notifications ~0.32.17` installed; prefs, token handling with no-log law); server halves exist in-repo (`supabase/functions/notify-flag-status`, `send-push-notification`) but edge-function deployment is Sky-physical and unverifiable here (R-1 precedent: written-undeployed). The in-app path is real regardless: UpdateBanner "N of your flags have status updates since your last visit" (announced). If push isn't deployed at submission time, soften to the in-app wording or deploy. NEEDS-SKY-DEVICE (deploy state). |
| C-5 | Screenshot alt text "for compliance with WCAG 2.1 AA" | `APP_STORE_LISTING.md:89-93,144` | NOTE | Alt-text drafts exist in the doc; execution happens in store consoles at listing time (Sky-physical). Wording nit: same 2.1-vs-2.2 drift as C-1. |
| C-6 | "Community-powered accessibility map" / "A crowdsourced map for accessibility issues." | `app.json` web manifest · `AboutScreen.tsx:102` | TRUE (mission voice, no conformance claim) | — |
| C-7 | Six categories ("ramps, elevators, parking, washrooms, entrances, and more") | listing + `README.md:7` | TRUE | `CATEGORY_LABELS` = 6 values; "and more" is the 6th bucket (other). |
| C-8 | Hosted policy makes no a11y-conformance claims | fetch | TRUE (nothing to score) | — |

## The verdict

**The product's a11y claims are structurally honest (k≥3 chain is exemplary) with two Blocker-class exceptions, both fixable only by Sky's hands:**

1. **C-2 — the hosted privacy page is stale** and misdirects the account-deletion right. Republish the ratified §SKY-9 text (the repo already pins it verbatim; the page is the only unsynced copy).
2. **C-1 — README's "fully accessible / WCAG 2.1 AA"** overclaims scope and cites the wrong standard version. Either reword (Sky's voice) or let Phase B + the device pass earn the sentence at 2.2 — this audit's close-out will say which gaps stand between the app and that sentence.

No claim was found that misrepresents a feature's existence. FINISHED.
