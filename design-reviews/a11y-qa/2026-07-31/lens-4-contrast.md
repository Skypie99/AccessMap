# LENS 4 — CONTRAST + COLOR, MEASURED (banked 2026-07-31)

Method: (1) all 18 arbiter proof sets re-run at HEAD via the Material Lab (`~/AccessMap-material-lab/2026-07-02/shared/contrast-check.mjs`); (2) full token matrix computed from `theme.ts` (light) + `ThemeContext.tsx` (dark) with WCAG relative-luminance math (script banked in session scratchpad, results below); (3) blast-radius greps for every failing pair. Spec-precision rules applied: disabled/decorative exempt; large text (≥18pt regular / ≥14pt bold-700) takes the 3:1 floor; icons take 1.4.11's 3:1.

## Gates re-verified (floors hold)

**All 13 ratified/shipped proof sets exit 0 at `5ab3f0c`** (devicetune ×2, r2 ×8, shipready-grabber-shipped, fable bench3 + p2). The 5 exit-1 files are all fail-by-design records (their `_doc` says so): `shipready-grabber-stacks` (arbitration record), `r2-trials` (spec-decision trials + M-52-class shipped-truth records), `audit-stacks`/`audit-stacks.pre-p2` (audit-owned shipped truth), and `shipready-comment-report-stacks` — whose "SHIPPED timestamp (THE FINDING)" FAIL rows are **still live** (see A11Y-230).

## Token matrix — verified sound (both themes)

Core text set light: text 12.63 · textMuted 5.74 · textSubtle 4.95 · textStrong 15.91 on surface. Dark: **byte-matches the recorded D-4 sweep** — text 12.23 · textMuted 7.15 · textSubtle 4.81 · textStrong 15.24. Severity ramp textOnColor reproduces the PROTECT-4 record exactly (11.03/8.05/6.21/4.79/4.83). textOnBrand on ctaFill 5.24 both themes · on ctaFillPressed 7.00 · on successStrong 4.72 · brandOnSoft on surface 7.00/10.48, on brandSofter 6.33/8.51 (Car-A record reproduced) · headerFg/headerBg 16.0/16.4 · tabBar tints on composited bar ≥6.24 both themes (T15 held) · grabber inkGlassMuted 8.8/8.9 vs 3.0 · brand-as-link on surface 5.24/4.86 · focus ring = `color.brand` ≥3:1 both palettes (a11y.focusRingWidth token doctrine, theme.ts:506-512) · placeholders documented-pass (light `#5b6470` ~4.7; dark `#9ca3af` ~6). **1.4.1 color-never-sole-signal:** carried by the severity grammar spine (number+word everywhere, PROTECT-4/25) and the F-20 contract — verified unregressed via guards.

## FINDINGS

- **A11Y-229 (High · 1.4.3 dark theme · programmatic, measured): the white-on-`brand` pair fails in dark mode (3.42:1) and is live at 5+ text sites.** Dark `brand` = `#4E89EF`; `textOnBrand` = white. Confirmed failing (13pt/600 or 13pt/700 — below the large-text line, 4.5 floor): NearbyFlagsModal active filter chips (`:478/:484`), ReportFlagModal active severity-pill labels (`:1269/:1271`), MyReportsModal active sort chips (`:616/:622`), HomeScreen `retryText` (`:723/:769` block), **CommentBubble own-bubble timestamp** (`:57` + `:114`; the bubble's body ink is the same pair — Phase B re-measures it per text size). Two more sites measure 3.42 but qualify as large text (≥14pt bold → 3:1 floor → thin pass, listed for judgment not as violations): ReportFlagModal `submitText` (default 14 + bold) and HomeScreen `reportPillText` (15 + bold). **This is SR-112 finally run** — the "two dark blues" arbitration routed twice and executed zero times: dark `brand` (3.42 w/ white) is not a text-fill token; dark `ctaFill` (5.24) is. The M-52 grammar (brand→ctaFill fill swap) is the ratified precedent and fixed this exact class in UpdateBanner/Leaderboard; these sites never adopted it. Phase B: per-site fill swap or ink fork, arbitrated via a new stacks file (PROTECT-5), Dani/mockup-gate where brand-critical (the submit CTA's look is signature). Light mode passes everywhere (5.24). Icon-only brand fills (Profile `:2122` 22px disc, Tasks `selectCheckOn`) pass 1.4.11 at 3.42 vs 3.0 — not violations. `MapScreen iconBtnActive` is a dead orphan style (Car D).
- **A11Y-230 (Medium · 1.4.3 both themes · programmatic, measured): `textSubtle` on `surfaceNeutral` fails both themes — 4.37:1 light / 3.69:1 dark.** Live pairing confirmed at HEAD: CommentBubble other-person timestamp (`:61` + `:114`) — exactly the "SHIPPED timestamp (THE FINDING)" rows the ship-ready comment-report proof set recorded and nobody disposed. Timestamp is AT-hidden (spoken via the composite label), so this is visual-only 1.4.3 — but 3.69 dark is a clear fail. One ink swap clears it (`textMuted` measures 5.44+ on the same family); Phase B greps for any sibling textSubtle-on-surfaceNeutral pairings before choosing.
- **A11Y-231 (Low · doc truth · SR-077 re-confirmed): `ctaFillPressed` documented "7.5:1" at both sites (`theme.ts:252`, `ThemeContext.tsx:188`) — computed 7.00:1.** Passes comfortably; the docs lie by 0.5. One-line doc fix, plus the stale "#111 surface" section header the ship-ready ledger noted.

## Notes

- The 4 web-only CSS `backdropFilter` sites and their floors are handled in lens 6 (SR-073 re-surface); GlassSurface translucent floors are arbiter-owned and untouched (0 changed lines — PROTECT).
- No new unarbitrated on-glass ink pairs found beyond the above; "already shipped elsewhere is an argument, not a measurement" was applied — every claim in this lens is a fresh computation.

**FINISHED** — 1 High, 1 Medium, 1 Low; everything else measured-and-passing with records reproduced exactly.
