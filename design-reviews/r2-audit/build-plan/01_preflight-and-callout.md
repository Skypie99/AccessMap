# BP1 — Pre-flight + the CRITICAL — the map payoff lands true

**Fire this file ALONE in a FRESH window.** Model law: **EXECUTE on Opus 4.8 ultracode MAX EFFORT, all sub-agents max effort** (Sky's direction for this train). Authored on Claude Fable 5 max effort, 2026-07-15, in the same window that authored the synthesis — provenance is disclosed, never silent; tag it in your phase report.

**Branch:** `r2/bp1-callout-true` — cut from the tip of `the Sky-designated base (DEFAULT: the audited tip `a8549ff` on `bench/4-quality`; if Sky merged bench→main first, main — S-9)` — NO prior phase exists: this is the train's FIRST window; DECISIONS §P is EMPTY at fire time and YOUR handshake (commit 1) writes its first entry. Record your base SHA as this phase's rollback anchor.
**Carries (conservation):** T1 — closes T1 (F2-01 CRITICAL + F3-04/05/06) in full.
**Effort:** M.
**Purpose:** Run the VERSION HANDSHAKE for the whole train, then fix the slate's only CRITICAL: every pin callout lands whole in clear map, in one designed rhythm — occlusion, races, dead beat, and theme-flip blank all closed.

## 0 · READ FIRST (in order — no edit before all of these)
1. `design-reviews/r2-audit/build-plan/DECISIONS.md` — apply every §S standing decision and §O/§A entry named in §1 below; §P is EMPTY until your handshake writes its first entry — your base comes from S-9 (inlined in §1 below).
2. **THE REPORT (design authority):** `/Users/skypie/AccessMap/design-reviews/r2-audit/2026-07-10_AccessMap_R2_Design_Review.md` — your inlined block(s) below are copied from it verbatim (`### #1 · T1 —`); also read §4 (the PROTECT list + fork registry + R2-D ledger) and §5's phase spine.
3. The master: `design-reviews/r2-audit/build-plan/00_master.md` — §6 ordering laws that name this phase, §13 PROTECT spot list.
4. **VERIFY-FIRST:** every `file:line` in the spec was true at plan-authoring (audit HEAD `a8549ff`). Re-grep each cited anchor BEFORE editing; small drift → adapt and note it in your evidence report; meaningful drift (a mechanism moved or is absent) → **STOP** and surface to Sky (evidence report + DECISIONS §P note). Never build against drift.

## 1 · STANDING DECISIONS THAT BIND THIS PHASE (inlined — self-containedness includes decisions)
- **S-9 (base designation):** default base = `a8549ff`. YOU record the actual designation + handshake result in DECISIONS §P as this train's first entry.
- **S-10:** model law (Opus 4.8 ultracode max; provenance tagged).

## 2 · THE WORK — the spec, inlined VERBATIM from the report (the "Skeptic verdict: FIX (absorbed)" line is PART of the spec)
### #1 · T1 — The map payoff lands true — clear-map callout + one honest rhythm · median 9.35 · ★ DO-FIRST · signature ★
**WHERE:** src/components/PlatformMap.web.tsx (Popup ~:430, container :759; theme-flip tile swap :646–657) · src/components/PlatformMap.tsx (native showCallout path) · src/screens/MapScreen.tsx (overlayTopGroup onLayout measure; :159–170 retryShowCallout ladder; :2483 Nearby select, :1387 handleDetailViewOnMap; overlay :1416/:2686 untouched as law).
**Resolves:** F2-01 (CRITICAL), F3-04, F3-05, F3-06 · **Effort:** M · **Tier:** Signature ★ · **NEEDS-SKY-DEVICE:** yes
*Merge of the signature cluster's F2-01 occlusion fix and the touch-motion cluster's F3-04/05/06 continuity family — one surface (the map callout), one moment (the payoff beat). The CRITICAL leads; rank #1 regardless of tier sort.*
1. **WHAT** — Opening any pin callout always lands the WHOLE callout — title, "SEVERITY 4 OF 5 · SIGNIFICANT · VERIFIED", "Reported Jun 4, 2026" — in clear map, in one designed rhythm, every tap. (occlusion) Web: MapScreen measures the top chrome cluster (onLayout on overlayTopGroup) and threads that height through PlatformMap's shared imperative interface; the Leaflet Popup gains autoPanPaddingTopLeft = measured-cluster-height + margin, so autoPan clears the chrome, not just container edges. Under Reduce Motion the suppressed pan becomes an instant setView cut (animate:false) to the same clear position. Native biases animateTo below the chrome band. (rhythm) Three continuity mechanics join it: wire the existing canceller at the two careless retryShowCallout sites (ref-stored, cancel on next selection/close) so rapid Nearby A→B can never pop A's callout on a map centered on B (last-tap-wins); add a 0ms first rung before the 250/400/550/700 ladder so the common case — and RM's instant jump — collapses to same-frame payoff while the retries still cover re-clustering; and the theme-flip tile swap mounts the incoming layer before removing the outgoing (or setUrl in place) so the map never blanks mid-flip. We pan the map and schedule beats; we never restack z-order.
2. **WHY** — Resolves F2-01 — the merged ladder's only CRITICAL, and the slate's #1. The Leaflet popup pane can never escape the zIndex-0 map container while chrome rides the zIndex-10 sibling overlay, so any top-third callout composites under glass — and the occluded text is the severity grammar itself, at the S3 trust doorway. THESIS: one material (chrome and callout stop smearing into one illegible plane), one voice (the grammar — the actor — is the exact text being silenced). The fix direction is the finding's own: pan padding plus an RM-safe instant equivalent. Beauty was costing honesty here; this buys it back for free. And the continuity legs (F3-04/05/06, three LOWs, one family): an explicitly selected flag answered with a different flag's callout is a wrong-answer moment on the trust surface; RM users receiving a fixed ≥250ms dead beat are the motion system's one inequity imposed on exactly the users it elsewhere protects; the map is the only surface that visibly rebuilds instead of transforming during the theme moment. THESIS: one material (chrome and callout stop smearing into one illegible plane; the floor never blanks), one voice (the grammar — the actor — is the exact text being silenced), one hand (the payoff lands with one rhythm, every tap, by intent). Signature by definition: the S3 doorway on the app's richest surface becomes flawless at the moment it is most seen.
3. **PERSONA IMPACT** — R7 FIRST and gains MOST: under Reduce Transparency the rail is engineered-opaque, so today's ghosting becomes total occlusion — this guarantees RT users full visibility of trust text, first-class by construction. R1: "sloppiness reads as 'maybe the data is sloppy too'" — the seam closes at the credibility moment. R5's "worst frame in the pack" and his occlusion-pass precondition for premium: answered. R3: meta readable at default size, and clear-map placement survives zoom. R6: the trust text is legible at the exact judging moment. R2: SR path unchanged (marker alt already speaks the full sentence). R4: the count pill stops overlapping tappable callout area. On the rhythm legs: R4, likeliest to fire the rapid A→B race with imprecise re-taps, gains guaranteed correctness; RM users (whom the ladder currently short-changes) gain a same-frame callout; R7 running RT gains that plus theme-flip continuity — the map keeps its floor while the world re-inks; R3 gains a dark-flip without a blinding blank.
4. **TRUST + COHESION** — This is the S3 doorway — the surface Round 1 built so a wheelchair user can judge a report before routing around it. At HEAD the app's most trust-sensitive text renders half-ghosted under its own chrome in both themes; R1 read it as data sloppiness, R5 as a craft disqualifier. After: the material system reads as one stage that reserves space for its actor. The one-material claim becomes literally true at the app's most-seen collision point. The luck-vs-intent tells close too: a payoff landing anywhere in a 450ms window reads as luck; a callout occasionally answering the previous question reads as sloppiness — R1's exact chain. Tasks→Map, deep-link, Nearby, and detail handoffs read as one governed camera with one rhythm and one honest floor.
5. **A11Y + RESTRAINT** — Restores legibility and honesty; costs nothing — finding's inline RESTRAINT: PASS carried. RM contract preserved exactly: no animated pan under RM (S12's intent), replaced by an instant cut, consistent with the designed-stillness law (end states present, presence at t150 n/a for a cut). No target, ink, or floor changes; WCAG untouched-to-improved (occluded text is a de facto legibility failure today). The rhythm legs add zero motion and subtract defects: the 0ms rung deletes the RM dead beat; cancellation removes stray late-callout state; the tile overlap replaces a blank with continuity (labels legible throughout). RESTRAINT: PASS — restores legibility and honesty, costs nothing.
6. **DEPENDENCIES / CONFLICTS** — Preserves PROTECT box-none overlay law (MapScreen.tsx:1416 pointerEvents untouched; cluster height read via onLayout only). Preserves S12 RM-gating intent (no animated pan under RM) and the cross-platform camera grammar (reliability extensions only). Preserves the SR nested-detail continuity pattern — only the sighted-path row select (:2483) is touched; the SR path (:2461–2472) stays byte-identical. Preserves S6 (zoom control stays removed). PlatformMap interface change coordinated across .tsx/.web.tsx per gotcha #3. F3-05 is swap MECHANICS only — the tile family is Sky's open Fork 7 eye-candidate, never re-litigated. Shares MapScreen.tsx with T11/T13's map hunks and the T5-grammar chip re-ink — serialize commits within the phase. Nothing re-opens S6/S12.
7. **VERIFICATION** — Occlusion-regression: re-shoot the finding's capture pair (base/map__light__390__pin-callout.png + dark twin) with a top-third pin — zero overlap between callout bounds and pill/rail bounds, both themes. Layout guards (jest fake timers): web Popup receives autoPanPaddingTopLeft = measured cluster height + margin; the RM branch asserts instant setView (animate:false); rapid Nearby A→B yields exactly B's callout with A's 550/700 rungs cancelled; leaving the Map tab mid-ladder cancels cleanly; the ladder fires a same-tick 0ms attempt; under RM showCallout invokes in the same frame as the instant jump; theme flip banked as a t-series (old tiles persist beneath the incoming layer). No ink change → NO arbiter. Blur budget unchanged (no pane added; worst simultaneous map state stays at count + the invisible tab-bar pane). SKY DEVICE (native Callout is the same collision class, code-inferred only): open callouts on top-third pins both themes, RM on/off, VoiceOver pass on the callout→details path — callout sits fully below the pill/rail; rapid Nearby A→B yields B only; flip Appearance mid-session — the map never blanks.


**Skeptic verdict: FIX (absorbed).** measure the PERSISTENT top-chrome band (mapHeaderRow:1429 + topRow:1451), NOT the filterPanel-nesting overlayTopGroup:1423, and CLAMP the autoPan inset to ~45% of map height; set `autoPanPaddingTopLeft` as a declarative/open-time Popup option (correct the 'imperative interface' wording); optional single shared canceller ref across all four callout sites (:1178/:1264/:1387/:2483) to also kill cross-path races.

### Commit plan (one commit per item, this order)
1. **(no-edit) THE VERSION HANDSHAKE** — record branch + HEAD; verify the three baseline markers (`PHOTO_MAX_DIMENSION` in `src/lib/flags.ts` + its test · `src/components/LiveStatusRegion.tsx` exists · `variant="bulk"` in `src/screens/NearbyFlagsModal.tsx` ~:202); run typecheck/jest/lint at base and confirm 0 / 1857-green / 0-err (any red at base = STOP); if HEAD ≠ `a8549ff`, diff the files THIS TRAIN touches and judge drift — meaningful drift → STOP to Sky. Append the result to DECISIONS §P before any edit.
2. Web occlusion fix: MapScreen measures the PERSISTENT top-chrome band (`mapHeaderRow` :1429 + `topRow` :1451 — NOT the filterPanel-nesting `overlayTopGroup` :1423) via onLayout into a ref; PlatformMap.web gains a declarative `chromeInsetTop` prop (or a `showCallout(id,{paddingTop})` open-time option) that sets the Leaflet Popup's `autoPanPaddingTopLeft` = insets.top + 16 + measured band + margin, CLAMPED to ~45% of map-container height; under Reduce Motion the pan is replaced by an instant `setView(..., {animate:false})` cut to the same clear position.
3. Native leg: the marker-press/showCallout path biases `animateTo` below the chrome band (same shared imperative handle — gotcha #3: both PlatformMap variants expose `{animateTo, showCallout}`).
4. Rhythm legs: wire the EXISTING canceller at the two careless retryShowCallout sites (:1387 `handleDetailViewOnMap`, :2483 Nearby sighted select) via a ref (optionally ONE shared canceller ref across all four scheduling sites :1178/:1264/:1387/:2483 — the skeptic's hardening); add a 0ms first rung before the 250/400/550/700 ladder (:159–170); theme-flip tile swap mounts the incoming layer before removing the outgoing (or setUrl in place) at PlatformMap.web.tsx:646–657.
5. Guards + evidence: jest fake-timer suite (autoPanPaddingTopLeft receives measured height; RM branch asserts animate:false; rapid Nearby A→B yields exactly B with A's rungs cancelled; leaving Map mid-ladder cancels; same-tick 0ms attempt; RM same-frame showCallout); re-shoot the occlusion capture pair (top-third pin, both themes) + a theme-flip t-series; save PROTECT before/afters.

### Phase-specific laws
- **This phase creates the shared instant-camera path (`animate:false` cut) that BP13/T7 REUSES — build it as a clean, reusable branch of the imperative handle, not an inline special case.** (Master §7 dependency.)
- F3-05 is swap MECHANICS only — the tile family/appearance is Sky's open eye-candidate (Fork-7-adjacent bench discovery); never re-litigate which tiles.
- Nothing re-opens S6 (zoom control stays removed) or S12 (no animated pan under RM — the instant cut is a cut, not an animation; the map already does `flyTo(animate:false)` under RM at PlatformMap.web.tsx:742).

## 3 · RAILS (adapted to this repo's discovered infrastructure — master §3)
- The report is the design AUTHORITY. Execute to its recorded taste; never invent a decision it already made; never re-litigate a standing decision (DECISIONS §S) or a settled device read (D9/D10).
- Fork discipline: UI/read halves only. Forks 1-13 are Sky's (proximity/geo · points economy · auth-wall/guest · k-anonymity · trust-model scope · product name · stagePoolB · dark saved-place chips · ui/Button · M-12/M-13 swap · bulk mechanism lever · M-37 locked rows · lightbox blacks). A decision half you brush against becomes a DECISIONS §F question, never a build.
- Honesty fences: no invented copy, claims, prices, or numbers. Every NEW user-facing string is PROPOSED (before/after in your evidence report) unless DECISIONS §A already carries Sky's pick; proposed status strings use the em-dash grammar ("state — next step").
- Hard territory (never crossed): `src/components/ui/GlassSurface.tsx` DO-NOT-EDIT (thread the primitive, never fork it) · MapScreen's `pointerEvents="box-none"` overlay law untouched · the blur budget never exceeded (assert at the worst SIMULTANEOUS state, +1 for the tab-bar pane invisible to `__getLiveBlurPaneCount`) · the RM designed-stillness contract + the 220ms delay-gate/pulse-token discipline · RN reality (no CSS-only tricks; expo-blur intensities 12/24 only; virtualization law) · zero Supabase/Storage writes · never merge, push, build, deploy, or send anything external — those are Sky's hands, always.
- Evidence honesty: tag every claim verified / web-approximated / code-inferred / NEEDS-SKY-DEVICE. Web captures come from the audit's static-export rig (`node design-reviews/r2-audit/tools/probe-export.mjs` pattern serves the production bundle on :8082 — the dev server crashes Map/Tasks on the lucide lazy boundary; pre-existing, not yours to fix).
- **PROTECT ACTIVE for this phase:** the box-none overlay law (MapScreen :1413/:1416/:1423 untouched — onLayout is passive) · the SR nested-detail continuity pattern (:2461–2472 stays byte-identical; only the sighted path :2483 is touched — diff-prove it) · the cross-platform camera grammar (reliability extensions only) · S12 RM intent — re-render each touched PROTECT surface BEFORE/AFTER and save both frames to `design-reviews/r2-audit/build-plan/evidence/BP01/`; diff-verify the byte-preserved ones your spec names.

## 4 · GATES (all hard — the phase does not stop until every one passes)
- `npm run typecheck` → 0 errors.
- `npm test` → green; RECORD the count in your evidence report (baseline **1857** at `a8549ff`; expect growth from guards you add; any unrelated red = STOP, do not paper over).
- `npm run lint` → 0 errors, no new warnings.
- The 7 immutable prior stacks files untouched (diff-check): `tools/audit-stacks.json` · `p2-material-stacks.json` · `bench3-material-stacks.json` · `shipped-stacks.json` · `wave1-stacks.json` · `wave2-stacks.json` · `map-stacks.json` (real paths at `a8549ff`: `audit`/`p2-material`/`bench3-material` under `design-reviews/fable-audit/tools/`; `shipped`/`wave1`/`wave2`/`map` under `qa-reports/assets/2026-07-03_tasks_glass/` · `_glass_w1/` · `_glass_w2/` · `2026-07-04_glass_map/` — diff-check by exact filename; only `map-stacks.json` is git-tracked at the base, so for the untracked six the check = never edit them).
- Every commit-plan item CLOSED or explicitly forked with a written reason — nothing silently dropped.
- **Arbiter:** NO arbiter sibling needed — this phase changes no color/floor/ink pair. If any edit turns out to touch an ink pair, STOP and declare a named sibling first under `design-reviews/r2-audit/tools/` and run it through `node ~/AccessMap-material-lab/2026-07-02/shared/contrast-check.mjs` to exit 0 (the rail: measured, never asserted).
- **Diff scope = intended files only:** `src/components/PlatformMap.web.tsx`, `src/components/PlatformMap.tsx`, `src/screens/MapScreen.tsx` (+ their test files), plus this phase's own untracked-side artifacts — its named `design-reviews/r2-audit/tools/r2-*-stacks.json` sibling(s) where mandated, its `build-plan/evidence/BP*` output, and the DECISIONS.md append (all inside the untracked `design-reviews/` tree, so they never appear in the tracked diff) — and any guard/test files its commit plan adds or updates. Anything else in the TRACKED diff = a defect; revert it.
- Occlusion regression gate: the re-shot capture pair shows ZERO overlap between callout bounds and pill/rail bounds, both themes (this is the CRITICAL's close-out proof).
- Blur-pane count unchanged at the worst simultaneous map state (no pane added; Leaflet popup is plain HTML chrome).

## 5 · EVIDENCE + LEDGER DUTIES (before stopping)
- Write `design-reviews/r2-audit/build-plan/evidence/BP01-verification-evidence.md`: what shipped per commit-plan item · per-gate results (with the jest count) · honest tags on every claim · the PROTECT before/after pairs · any PROPOSED strings (before/after table) · anything Sky must eyeball.
- Append to DECISIONS.md — §P: `BP1: base <sha> → tip <sha> · <date> · gates green (jest <count>)` · §D device items contributed: R2-D12 — native Callout on top-third pins both themes, RM on/off; rapid Nearby A→B yields B only; theme-flip continuity; VoiceOver callout→details path. · §L launch items: none. · §F any fork question surfaced · §A a "default applied (BP1)" line for each §S/§O default you exercised.

## 6 · STOP
Built + green + **STOPPED on `r2/bp1-callout-true`**. Never merge, push, build, or deploy — Sky's hands. One phase per fresh window. **RESUME RULE:** if this phase stalls, re-fire this same file in a fresh window; reconcile the branch's existing commits against the commit plan and continue from the FIRST gap — never redo a landed item, never renumber.
