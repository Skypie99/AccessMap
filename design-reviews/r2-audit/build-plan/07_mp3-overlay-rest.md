# BP7 — MP3 — the overlay remainder (T2)

**Fire this file ALONE in a FRESH window.** Model law: **EXECUTE on Opus 4.8 ultracode MAX EFFORT, all sub-agents max effort** (Sky's direction for this train). Authored on Claude Fable 5 max effort, 2026-07-15, in the same window that authored the synthesis — provenance is disclosed, never silent; tag it in your phase report.

**Branch:** `r2/mp3-overlay-rest` — cut from the tip of ``r2/mp2-profile-lists`` (the prior phase; its tip SHA is in DECISIONS §P — **if that entry is absent, the prior phase has not run: STOP**). Record your base SHA as this phase's rollback anchor.
**Carries (conservation):** T2 — advances T2 (phase 4 of 6): M-37 Achievements · M-22 Leaderboard · M-42 NotificationPrefsModal · M-24 NotificationPreferencesScreen.
**Effort:** one Opus window (4 files, same recipe; Leaderboard densest, still material-only).
**Purpose:** The remainder of the opaque overlay tier joins the one material — including the two notification surfaces that split Settings' hallway (RB7) — with the Leaderboard's shipped drift re-inks absorbed.

## 0 · READ FIRST (in order — no edit before all of these)
1. `design-reviews/r2-audit/build-plan/DECISIONS.md` — apply every §S standing decision and §O/§A entry named in §1 below; take your base tip from §P.
2. **THE REPORT (design authority):** `/Users/skypie/AccessMap/design-reviews/r2-audit/2026-07-10_AccessMap_R2_Design_Review.md` — your inlined block(s) below are copied from it verbatim (`### #2 · T2 —`); also read §4 (the PROTECT list + fork registry + R2-D ledger) and §5's phase spine.
3. **ALONGSIDE (the train's atomic edits live here, not in the report):** `design-reviews/r2-audit/04_material_migration_spec.md` — §3's shared sheet recipe (lines ~299–321 at authoring) + **MP3 blocks (lines ~508–559 at authoring — verify-first: re-locate by the `### MP3 block` heading, line numbers may drift)** + §5 dependency graph + §7 the train table. Also `design-reviews/r2-audit/tools/r2-material-stacks.json` (the ink declarations your blocks cite). Plus the master: `design-reviews/r2-audit/build-plan/00_master.md` (§6 ordering laws, §13 PROTECT spots).
4. **VERIFY-FIRST:** every `file:line` in the spec was true at plan-authoring (audit HEAD `a8549ff`). Re-grep each cited anchor BEFORE editing; small drift → adapt and note it in your evidence report; meaningful drift (a mechanism moved or is absent) → **STOP** and surface to Sky (evidence report + DECISIONS §P note). Never build against drift.

## 1 · STANDING DECISIONS THAT BIND THIS PHASE (inlined — self-containedness includes decisions)
- **S-11 (the train law):** linear MP0→MP5, each branch from the prior MP tip; built+green+STOPPED per phase; zero `theme.ts` edits on MP branches; `GlassSurface.tsx` never touched; the 7 immutable stacks files diff-checked; RT+C-lite as specced; Sky merges; Sky owns D-gates.
- **S-5:** ratified tiers stand — you migrate ONLY your phase's M-ids; every ratified-stays row you touch for an in-tier ink repair stays classified ratified.
- **S-10:** model law (Opus 4.8 ultracode max; provenance tagged).
- **Fork-12 default (per the MP3 block + §F):** Achievements' locked-row idiom ships ACCEPT-AND-RECORD (rowDimmed opacity 0.7 kept; the pre-existing ≈3.0:1 locked rowDesc recorded, SR-mitigated). The re-spec half stays Sky's fork — record 'default applied' in §A.

## 2 · THE WORK — the spec, inlined VERBATIM from the report (the "Skeptic verdict: FIX (absorbed)" line is PART of the spec)
### #2 · T2 — One Material World — the MP0–MP5 migration train (mounted whole) · median 8.8 · ★ DO-FIRST · signature ★
**WHERE:** 19 files across six phases: App.tsx, MapScreen.tsx, UpdateBanner.tsx, PhotoLightboxModal.tsx, LiveStatusRegion.tsx, ProfileScreen.tsx + SettingsScreen.tsx (comments only) · HomeScreen.tsx · MyReportsModal/MyWatchedModal/ActivityFeedModal.tsx · AchievementsModal.tsx, LeaderboardScreen.tsx, NotificationPrefsModal.tsx, NotificationPreferencesScreen.tsx · FlagDetailModal.tsx, StatusHistoryModal.tsx · AdminScreen.tsx + RootNavigator.tsx. theme.ts and GlassSurface.tsx untouched.
**Resolves:** RB1,RB2,RB3,RB4,RB5,RB6,RB7,RB8,RB10,RB11,RB14,M-06,M-22,M-23,M-24,M-36,M-37,M-38,M-39,M-40,M-41,M-42,M-49,M-52,M-55,M-56 · **Effort:** L · **Tier:** Signature · **NEEDS-SKY-DEVICE:** yes
*The train is 04 §7 VERBATIM — 6 phases MP0…MP5, wrapped, never redesigned; skeptic FIXes route back as noted amendments against 04.*
1. **WHAT** — ONE Deep Field world after onboarding, delivered as 04 §7's Sky-gated linear train — every phase built + green + STOPPED on its branch; Sky merges; Sky owns D-gates. MP0 `r2/mp0-first-frame` (cut from the tree Sky designates; spec authored at a8549ff) — M-56 boot frame → themed wash, M-55 map empty-card → row+wash family, M-52 UpdateBanner → banner tier; rides the Stage-6 in-tier ink repairs (M-46 lightbox chrome → textOnBrand; M-51 chip/info-fill) + M-43/Settings comment hygiene: 7 files, five S/XS mechanical blocks + 2 comments — the highest feel-per-line phase. MP1 `r2/mp1-home-stage` — M-06: 1 file, every mount and re-ink enumerated, no data paths. MP2 `r2/mp2-profile-lists` — M-39·M-40·M-38: 3 files, one shared B4-engineered recipe applied three times. MP3 `r2/mp3-overlay-rest` — M-37·M-22·M-42·M-24: 4 files, same recipe; Leaderboard densest, still material-only; absorbs the retry/segment→ctaFill and rankTop→brandText drift re-inks. MP4 `r2/mp4-trust-ledger` — M-36·M-41: 2 files, one 82 KB; the ink map + pre-passed union declaration keep it single-window; hardest last; the reopen-button 2.07 drift re-inks to brandText (⚑ Sky may veto). MP5 `r2/mp5-admin-editorial` — M-23·M-49, optional (§6.7): skipping records B14 as deliberate and conservation reads 13/15. Partition 15/15; zero theme.ts edits; GlassSurface.tsx never touched — the primitive is threaded, never forked.
2. **WHY** — Resolves the material HIGH band nearly whole: RB1 two-worlds tab switch, RB2 leftover legacy pane, RB3 glass-over-paper, RB4/RB5 the trust-ledger holdout at every S3 doorway, RB6/RB7 the Profile and Settings hallway splits, RB8 the dark-launch boot strobe — plus RB10/RB11/RB14 and all 15 MIGRATE rows. This is the THESIS's ONE MATERIAL leg executed literally, and ONE HAND: every sheet becomes the same recipe from the same primitive. ONE VOICE holds by gate — strings byte-identical. Signature tier earned: the Deep Field material is the taste anchor's hero, and the moment every doorway opens onto the house material is the most ownable moment Round 2 can ship — access-first by construction, not garnish.
3. **PERSONA IMPACT** — R7 (Reduce Transparency) gains the most, first-class: today RT users see TWO sheet materials (glass sheets render the designed 0.97 overlay + borderStrong; opaque sheets render plain surface) — after the train every sheet renders the ONE designed RT state, and C-lite ships engineered-literal by construction, a specced gate, not a fallback. R6 stops changing worlds at tab switches and doorways — fewer where-am-I moments. R3 gains six shipped-AA-drift repairs (M-46's functionally invisible dark lightbox chrome, M-52's 3.42 View button, M-51's chip, M-22's rankTop, M-36's 2.07 reopen) plus ≥500 on-glass body weight. R2: strings, roles, focus byte-identical by diff-gate. R5 gets the loudest break (B1, on the default tab) gone. R1's trust ledger keeps its meaning, elevated in material; R4: zero targets move.
4. **TRUST + COHESION** — The app's most important honesty surface — the trust ledger — stops wearing its least-considered material; every S3 doorway (map callout, Tasks card, Profile row) lands in one world. The first painted frame stops strobing white at dark-mode users. GLASS §8's aspirational Home line becomes shipped truth. Credibility mechanics: six live AA drifts move from the trials file's exit-1 findings record to repaired code, so the paper record and the pixels agree — the audit's one-material claim becomes checkable fact rather than intent.
5. **A11Y + RESTRAINT** — Floor preserved and raised: every re-ink maps to an existing arbitrated token — zero new tokens, zero eye-tuned floors, the arbiter decides (80 pairs, exit 0 at spec time). RT + C-lite implemented as specced is a hard gate, not a deferral. Blur budget: the whole train adds ZERO live panes anywhere (engineered-first rule); Tasks' shipped worst 12 is never exceeded — the deepest stack (Tasks 12 → FlagDetail → StatusHistory) is still 12. Zero targets move; zero motion added (modal RM gates untouched). RESTRAINT: PASS — this train deletes entropy. The one affordance trade (reopen orange → brandText, 2.07 → 4.95/5.42) buys AA with flair, not legibility, and carries an explicit Sky veto flag.
6. **DEPENDENCIES / CONFLICTS** — Linear MP0→MP5; no two phases share a file (§5 verified); MP2→MP3→MP4 apply one recipe in rising complexity — hardest (82 KB FlagDetail) last. Preserves PROTECT-1 (Nearby byte-identical) · preserves PROTECT-3 (ReportFlag untouched) · preserves PROTECT-4 (severity badges keep arbitrated fills + textOnColor) · preserves PROTECT-7 (modal RM gates unchanged) · preserves PROTECT-8 (POINTS single-source + the anonymity honesty set — the FlagDetailModal Reported-anonymously string byte-preserved, MP4); honest-arrival is preserved SEPARATELY (localized banner strings untouched) · preserves PROTECT-10/S17 (Home peek pointer-inert + suppressAttribution, diff-checked) · preserves the MapScreen box-none law (M-55 is render-only) · preserves the S3 read half byte-identical; Fork 5's DECISION half never crossed (no count, no guest flag-as-wrong). M-14 untouched (Fork 8 boundary); mechanism-uniformity lever stays Sky's (§6.3). Deliberately excludes theme.ts — the M-48-ink candidate is the out-of-train complement; the MyWatched prop gap is carried byte-identically and closed by its own candidate.
7. **VERIFICATION** — Per-phase hard gates: typecheck 0 · jest green, count recorded in the phase report · lint 0 errors, no new warnings · the phase's arbiter declaration exit 0 — the spec-level tools/r2-material-stacks.json re-run verbatim, with sibling tools/r2-mp{n}-stacks.json ONLY on drift (drift = re-declare == re-run) · §3 PROTECT statements re-verified by diff · RT + C-lite as specced, not deferred · the seven immutable prior stacks files untouched (diff-check). Blur-pane count asserted at each screen's worst simultaneous state per §2f (+1 manual tab-bar pane, invisible to __getLiveBlurPaneCount). Sky device gates: dark-launch first frame (M-56); Home stage in both palettes; frost/perf feel of the engineered sheet tier over live content; an RT sweep (OS + Settings toggle); a VoiceOver walk of FlagDetail post-MP4.


**Skeptic verdict: FIX (absorbed).** [FIX routes as noted amendments against 04 — the train BODY is unchanged.] WHERE reads 19 files not '18'; field-6 correct PROTECT-8 to the ANONYMITY SET (state honest-arrival preservation separately); add the MapScreen.tsx serialization note (MP0's M-55 hunk shares the file with T1/T7/T9, lines disjoint).

### Commit plan (one commit per item, this order)
1. M-37 AchievementsModal per its block (incl. the locked-row accept-and-record + the goldChip 4.15 correction as specced).
2. M-22 LeaderboardScreen per its block — absorbs the drift re-inks (retry/segment → `ctaFill`, rankTop → `brandText`, stateHint textSubtle re-ink) + the empty-state Trophy `goldAccent` (`:375`) `_doc` register entry.
3. M-42 NotificationPrefsModal per its block (incl. the spinner ink).
4. M-24 NotificationPreferencesScreen per its block — with the unreachability honesty line (hidden behind `PUSH_NOTIF_TYPES_ENABLED=false` at the shipped default; the break is latent there, live via M-42).
5. Arbiter re-run + captures + PROTECT diffs.

### THE TRAIN'S OWN GATES (04 §7, verbatim in spirit — these are IN ADDITION to §4 below)
- The phase's arbiter declaration at exit 0: re-run `tools/r2-material-stacks.json` VERBATIM through `node ~/AccessMap-material-lab/2026-07-02/shared/contrast-check.mjs` — add a sibling `tools/r2-mp3-stacks.json` ONLY if your shipped values drift from spec (drift = re-declare == re-run). The banked spec-time proof is `design-reviews/r2-audit/assets/arbiter/r2-material.txt` (exit 0, 80 pairs).
- The §3 PROTECT statements for THIS phase's blocks re-verified by diff.
- RT + C-lite implemented AS SPECCED, not deferred (RT = the variant's designed state; C-lite = engineered-literal by construction).
- **Zero `theme.ts` edits on this branch** (diff-check) · `GlassSurface.tsx` never touched · the 15/15 migrate partition holds (your M-ids appear in exactly this phase).
- Blur budget: this train adds ZERO live panes anywhere (engineered-first rule); assert the worst simultaneous state is not increased (+1 invisible tab-bar pane).

## 3 · RAILS (adapted to this repo's discovered infrastructure — master §3)
- The report is the design AUTHORITY. Execute to its recorded taste; never invent a decision it already made; never re-litigate a standing decision (DECISIONS §S) or a settled device read (D9/D10).
- Fork discipline: UI/read halves only. Forks 1-13 are Sky's (proximity/geo · points economy · auth-wall/guest · k-anonymity · trust-model scope · product name · stagePoolB · dark saved-place chips · ui/Button · M-12/M-13 swap · bulk mechanism lever · M-37 locked rows · lightbox blacks). A decision half you brush against becomes a DECISIONS §F question, never a build.
- Honesty fences: no invented copy, claims, prices, or numbers. Every NEW user-facing string is PROPOSED (before/after in your evidence report) unless DECISIONS §A already carries Sky's pick; proposed status strings use the em-dash grammar ("state — next step").
- Hard territory (never crossed): `src/components/ui/GlassSurface.tsx` DO-NOT-EDIT (thread the primitive, never fork it) · MapScreen's `pointerEvents="box-none"` overlay law untouched · the blur budget never exceeded (assert at the worst SIMULTANEOUS state, +1 for the tab-bar pane invisible to `__getLiveBlurPaneCount`) · the RM designed-stillness contract + the 220ms delay-gate/pulse-token discipline · RN reality (no CSS-only tricks; expo-blur intensities 12/24 only; virtualization law) · zero Supabase/Storage writes · never merge, push, build, deploy, or send anything external — those are Sky's hands, always.
- Evidence honesty: tag every claim verified / web-approximated / code-inferred / NEEDS-SKY-DEVICE. Web captures come from the audit's static-export rig (`node design-reviews/r2-audit/tools/probe-export.mjs` pattern serves the production bundle on :8082 — the dev server crashes Map/Tasks on the lucide lazy boundary; pre-existing, not yours to fix).
- **PROTECT ACTIVE for this phase:** the train's §3 PROTECT statements for THIS phase's blocks (re-verify by diff) · PROTECT-21 the bulk tier (newly-migrating sheets JOIN the recipe) · PROTECT-27 ThemeContext's total default (never a throwing hook) · PROTECT-5 the arbitration system (script decides, never the eye) · reward gold never wears the disc (Leaderboard's Trophy/metals stay Lucide/metal — no numbered-disc costume) — re-render each touched PROTECT surface BEFORE/AFTER and save both frames to `design-reviews/r2-audit/build-plan/evidence/BP07/`; diff-verify the byte-preserved ones your spec names.

## 4 · GATES (all hard — the phase does not stop until every one passes)
- `npm run typecheck` → 0 errors.
- `npm test` → green; RECORD the count in your evidence report (baseline **1857** at `a8549ff`; expect growth from guards you add; any unrelated red = STOP, do not paper over).
- `npm run lint` → 0 errors, no new warnings.
- The 7 immutable prior stacks files untouched (diff-check): `tools/audit-stacks.json` · `p2-material-stacks.json` · `bench3-material-stacks.json` · `shipped-stacks.json` · `wave1-stacks.json` · `wave2-stacks.json` · `map-stacks.json` (real paths at `a8549ff`: `audit`/`p2-material`/`bench3-material` under `design-reviews/fable-audit/tools/`; `shipped`/`wave1`/`wave2`/`map` under `qa-reports/assets/2026-07-03_tasks_glass/` · `_glass_w1/` · `_glass_w2/` · `2026-07-04_glass_map/` — diff-check by exact filename; only `map-stacks.json` is git-tracked at the base, so for the untracked six the check = never edit them).
- Every commit-plan item CLOSED or explicitly forked with a written reason — nothing silently dropped.
- **Arbiter:** the train's own: re-run `tools/r2-material-stacks.json` verbatim → exit 0 (sibling `tools/r2-mp3-stacks.json` ONLY on drift).
- **Diff scope = intended files only:** `src/components/AchievementsModal.tsx`, `src/screens/LeaderboardScreen.tsx`, `src/components/NotificationPrefsModal.tsx`, `src/screens/NotificationPreferencesScreen.tsx` (+ their test files), plus this phase's own untracked-side artifacts — its named `design-reviews/r2-audit/tools/r2-*-stacks.json` sibling(s) where mandated, its `build-plan/evidence/BP*` output, and the DECISIONS.md append (all inside the untracked `design-reviews/` tree, so they never appear in the tracked diff) — and any guard/test files its commit plan adds or updates. Anything else in the TRACKED diff = a defect; revert it.

## 5 · EVIDENCE + LEDGER DUTIES (before stopping)
- Write `design-reviews/r2-audit/build-plan/evidence/BP07-verification-evidence.md`: what shipped per commit-plan item · per-gate results (with the jest count) · honest tags on every claim · the PROTECT before/after pairs · any PROPOSED strings (before/after table) · anything Sky must eyeball.
- Append to DECISIONS.md — §P: `BP7: base <sha> → tip <sha> · <date> · gates green (jest <count>)` · §D device items contributed: contributes to R2-D14 — RT sweep across the newly-migrated tier. · §L launch items: none. · §F any fork question surfaced · §A a "default applied (BP7)" line for each §S/§O default you exercised.

## 6 · STOP
Built + green + **STOPPED on `r2/mp3-overlay-rest`**. Never merge, push, build, or deploy — Sky's hands. One phase per fresh window. **RESUME RULE:** if this phase stalls, re-fire this same file in a fresh window; reconcile the branch's existing commits against the commit plan and continue from the FIRST gap — never redo a landed item, never renumber.
