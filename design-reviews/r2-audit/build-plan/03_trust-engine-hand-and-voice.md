# BP3 — The trust engine — the Report sheet answers the hand, the triage layer speaks one voice

**Fire this file ALONE in a FRESH window.** Model law: **EXECUTE on Opus 4.8 ultracode MAX EFFORT, all sub-agents max effort** (Sky's direction for this train). Authored on Claude Fable 5 max effort, 2026-07-15, in the same window that authored the synthesis — provenance is disclosed, never silent; tag it in your phase report.

**Branch:** `r2/bp3-trust-hand` — cut from the tip of ``r2/bp2-perception-floor`` (the prior phase; its tip SHA is in DECISIONS §P — **if that entry is absent, the prior phase has not run: STOP**). Record your base SHA as this phase's rollback anchor.
**Carries (conservation):** T4, T8 — closes T4 (F1-01 + F1-08) and T8 (F4-02, F4-08 mechanics-half, F4-10, F4-17, F4-01 badge-doubling leg). T8's VISIBLE verb flip ('Resolved'→'Resolve', card + FlagDetail :1436) is PROPOSED here and APPLIED at BP16's copy gate — an explicit deferred-string, not a drop.
**Effort:** M + M.
**Purpose:** Make the app's two working moments feel designed: the report sheet acknowledges the finger (AA-safely), triage commits with the right haptic words, and the FlagCard's spoken layer stops double-speaking and orphaning its actions.

## 0 · READ FIRST (in order — no edit before all of these)
1. `design-reviews/r2-audit/build-plan/DECISIONS.md` — apply every §S standing decision and §O/§A entry named in §1 below; take your base tip from §P.
2. **THE REPORT (design authority):** `/Users/skypie/AccessMap/design-reviews/r2-audit/2026-07-10_AccessMap_R2_Design_Review.md` — your inlined block(s) below are copied from it verbatim (`### #9 · T4 —` + `### #5 · T8 —`); also read §4 (the PROTECT list + fork registry + R2-D ledger) and §5's phase spine.
3. The master: `design-reviews/r2-audit/build-plan/00_master.md` — §6 ordering laws that name this phase, §13 PROTECT spot list.
4. **VERIFY-FIRST:** every `file:line` in the spec was true at plan-authoring (audit HEAD `a8549ff`). Re-grep each cited anchor BEFORE editing; small drift → adapt and note it in your evidence report; meaningful drift (a mechanism moved or is absent) → **STOP** and surface to Sky (evidence report + DECISIONS §P note). Never build against drift.

## 1 · STANDING DECISIONS THAT BIND THIS PHASE (inlined — self-containedness includes decisions)
- **S-8:** strings are PROPOSED-only — the verb flip and any new spoken wording go to your evidence table and BP16's consolidated gate; mechanics ship now.
- **S-10:** model law.

## 2 · THE WORK — the spec, inlined VERBATIM from the report (the "Skeptic verdict: FIX (absorbed)" line is PART of the spec)
### #9 · T4 — The Report sheet and triage answer the hand · median 7.6
**WHERE:** src/screens/ReportFlagModal.tsx — all 10 Pressables (category chips :635, severity circles :664, template chips :593, 'Use my location' retry :532, anon-banner Sign in :564, seasonal/disability/context tag chips :804/:861/:991, Cancel :1052, Submit :1062) + press-in haptic timing; src/screens/TasksScreen.tsx — renderAction haptic word (:1576) + applyStatusChange outcome notify. haptics.ts untouched (existing API only).
**Resolves:** F1-01,F1-08 · **Effort:** M · **Tier:** Meaningful · **NEEDS-SKY-DEVICE:** yes
1. **WHAT** — The app's core contribution flow acknowledges the finger, and the trust engine speaks the right haptic words. All 10 ReportFlagModal Pressables gain the static pressed dim PhotoGallery already uses in-sheet (thumbPressed/removeBtnPressed — the in-sheet inconsistency closes from inside the sheet's own vocabulary); hapticSelection moves to finger-down on the category/severity pickers instead of release. At Tasks triage, Verify/Resolve/Reject commit with impact('medium') and land with notify('success'|'error') alongside the existing flash banner; selection remains the pickers-and-filters word per haptics.ts's three-word vocabulary. Submit keeps its shipped notify('success'). No layout, color-role, or copy changes — acknowledgment only.
2. **WHY** — Resolves F1-01 (HIGH — 'utilitarian-bare at the app's emotional center is the precise thing the R2 thesis exists to refuse'; skeptic-confirmed bitwise-identical pressed frames) and F1-08 (MED — the vocabulary deployed inverted: real state commits on someone's barrier report get the lightest word and no outcome haptic while opening a form gets impact). ONE HAND: the severity picker is the signature control and must answer the finger the way the drawer row does. ONE VOICE, felt: picks feel like picks, commits like commits, outcomes like outcomes. The mission's two working moments become the best-feeling surfaces in the app — craft exactly where the thesis says it belongs.
3. **PERSONA IMPACT** — R4: the surface that asks the most of a tremor user stops demanding blind faith — landing confirms under the finger, re-press anxiety at Submit dies. R2, triaging screen-off: the only press channel that survives on native without vision becomes semantic — picking a filter and resolving a barrier finally feel different. R7 gains full first-class acknowledgment on the form — including the anonymous/guest form, today feedback-dead without exception per the skeptic — via RT-native static dims, no transparency or motion involved. R6 gains submit confidence at the moment of civic contribution; R1 gains a crafted signature control; R5 reads the emotional center as finished.
4. **TRUST + COHESION** — The severity grammar is the brand (R5's unprompted signature read); a silent signature control undercuts it on every touch. Acknowledgment at the emotional center says the app takes the report as seriously as it renders the result. In-sheet consistency closes a visible seam (PhotoGallery dims, everything else dead), and correct haptic semantics make the trust engine feel like a trust engine, not a form.
5. **A11Y + RESTRAINT** — Static dims are motionless and RM-safe by construction; press-in haptics ride haptics.ts's PROTECT'd RM-independence (OS-level haptic settings still govern). No blur, no motion, no new strings; the double-submit guard (disabled={submitting || !location}) is untouched. Pressed ink pairs arbiter-gated. RESTRAINT: PASS — pure acknowledgment at zero legibility/reachability/honesty cost; the confirm gate on Reject keeps its net, so the costliest mis-tap still costs a dialog, not data.
6. **DEPENDENCIES / CONFLICTS** — Independent of the PressableScale candidate (these are raw Pressables) — can land FIRST as the highest-value surface. Preserves the confirm gate on Reject (commit haptic fires after confirmation, never bypasses it — extensions only). Preserves haptics.ts RM-independence stance. Preserves the FlagCard acknowledgment stack (triage buttons keep their static truth; only the haptic word changes). Preserves the severity live-region hint (sevHint speaks today per the F1-03 skeptic — byte-untouched here). Shares ReportFlagModal.tsx with the chip-semantics candidate — separate commits: styles here, semantics there. Fork 5 (trust-model scope) untouched: zero status/points behavior change, feel only.
7. **VERIFICATION** — Arbiter sibling tools/r2-report-ack-stacks.json — pressed severity circles and every chip family, label ink vs dimmed fill, both schemes; exit 0 gates. Re-capture press/report-category and press/report-severity pressed frames — must show nonzero diff vs base (today 0 changed pixels). Guard tests: pressed-style presence on all 10 Pressables; a haptic call-site census test pinning selection-at-press-in on pickers, impact('medium') at triage commit, notify on outcome; the confirm-gate test stays green. Sky device: finger-down tick on severity circles; Verify = medium thunk then success notify with the flash banner; Reject still confirms first; RM on — dims and haptics both still answer.


**Skeptic verdict: FIX (absorbed).** do NOT apply group-opacity-0.75 to any text-bearing control (it collapses label-vs-fill contrast below AA) — use a fill-composited pressed treatment that keeps label ink at full opacity; fire hapticImpact('medium') at the commit point INSIDE applyStatusChange (set the three commit actions' haptic='none', Details stays 'selection'); fire hapticNotify('error') in setStatus's catch (~:696), 'success' in applyStatusChange.

### #5 · T8 — One spoken voice for the triage layer · median 8.1 · ★ DO-FIRST
**WHERE:** TasksScreen.tsx (FlagCard baseLabel + action a11yLabels + visible verb), HomeScreen.tsx (recent-row label branch), ReportFlagModal.tsx (photo-nudge string), LegendModal.tsx (badge decorativeProps adoption). a11yText.ts imported, never changed.
**Resolves:** F4-02,F4-08,F4-10,F4-17,F4-01 · **Effort:** M · **Tier:** Meaningful · **NEEDS-SKY-DEVICE:** yes
1. **WHAT** — Make the spoken app as designed as the seen one on the triage path. (a) The Tasks card label routes through the helpers — before: "No ramp, severity 3, open. Tap to view on map."; after sketch: "No ramp, severity 3 of 5, Moderate, status Open. Tap to view on map." (b) Each action names its flag — a11yLabel sketch: "Verify this flag — No ramp, 639 m"; visible "Verify" unchanged. (c) The visible verb unifies: card/detail "Resolved" becomes "Resolve" (a11y "Mark this flag resolved" kept). (d) Home rows keep status when distance appears: "…, status Open, 297 m away." (e) The severity-4 photo nudge interpolates SEVERITY_LABELS, retiring the invented "major." (f) The Legend badge adopts the repo's own decorativeProps so the web digit stops doubling.
2. **WHY** — Resolves F4-02 (HIGH — the exact drift a11yText.ts:22-28 exists to prevent, on the surface where a blind verifier decides) with F4-08, F4-10, F4-17, and F4-01 riding as one family: the spoken layer drifting where the visible layer never would. ONE VOICE, literally: the severity grammar is the actor, and it must speak its taught words to SR users too. ONE HAND: S1 fixed this family on Home's visible rows; this finishes the adoption S1 started (skeptic-confirmed not-closed), and the Legend leg deploys the S9 primitive built precisely for RN-web's dropped hiding props.
3. **PERSONA IMPACT** — R2 is principal: "I hear every flag twice, slightly differently" and "'Verify this flag' six times in a row with no idea which flag" both die, and the app's dictionary stops stuttering digits on web. R5's verb/state pick-one lands. R3 gains: the shorter "Resolve" eases the action-row overflow R3 feared. R1/R6: steadier vocabulary, unchanged visuals. R4: no target changes. R7 first-class: RT users gain the visible verb fix outright — the state-word-on-a-button misreading R7 flagged disappears — and RT+VoiceOver pairings gain the entire unified voice at zero visual spend.
4. **TRUST + COHESION** — The severity grammar is the signature; today it is fluent in ink and broken in speech on the doing-surface. After, one grammar spans channels: what the Legend teaches, the card speaks, in the helpers' canonical words. For SR users, parity is credibility itself — the app stops sounding like a port of its visual self exactly where the community-verification loop happens. The verb unification removes the last state/verb ambiguity from the triage buttons.
5. **A11Y + RESTRAINT** — Pure floor improvement — label quality (WCAG 1.3.1/4.1.2-adjacent), unique action names, no visual spend beyond one shortening verb. Preserves the spoken-brevity twins (visible-warm/spoken-plain pairs are designed — only drifted labels change); preserves PROTECT-1 (NearbyFlagsModal's frozen labels untouched — census-only there); never compresses the taught severity scale (helpers interpolate the canonical words); PROTECT-12 honored — S9 engine adoption, not redesign. RESTRAINT: PASS — spoken-only plus one shorter verb; nothing traded.
6. **DEPENDENCIES / CONFLICTS** — Preserves PROTECT-1, PROTECT-12, the taught-scale strings, and the S13 card structure — D1's settled device subject is not re-opened; only label composition changes atop the byte-same structure. GlassSurface and a11yText.ts untouched (import-side adoption). Shared files: TasksScreen.tsx (status-ledge and sweep candidates), HomeScreen.tsx (failure-voice candidate) — disjoint regions, coordinate merge order. No fork's DECISION half is approached; Fork 5's data-content question (BUMBAKLOT) is explicitly not this — that stays Sky's.
7. **VERIFICATION** — Jest guards: baseLabel composed via severityA11y/statusA11y (string-match); action a11yLabels contain category + distance; a regression regex proves no raw-enum composition (/severity \d, (open|verified)/) survives in TasksScreen; Home row label includes statusA11y when distance renders. Re-run the banked CDP probes (tools/probe-f401-skeptic.mjs, probe-f401-detail.mjs): badge digit no longer doubles; a11y-tree re-walk on tasks/legend/home. No ink change, so no arbiter. Sky device (D1-adjacent, the audit's highest-stakes gate): VoiceOver on Tasks — each card reads once in the taught grammar and each action names its flag uniquely; Legend rows read one utterance each.


**Skeptic verdict: FIX (absorbed).** scope to F4-01's badge-doubling leg only (PARK-with-direction the color-name-vocabulary SR leg); pin decorativeProps to the inner sevDot <View> (LegendModal:79-83), NEVER the row View carrying the authored label; ALSO route the selection-mode label (TasksScreen:1511) through severityA11y; make the action-label distance conditional (null when no location → category-only); mark 'Resolved'→'Resolve' PROPOSED + update getByText tests; add FlagDetailModal:1436 to WHERE (+ the T2/T5 FlagDetailModal coordination).

### Commit plan (one commit per item, this order)
1. T4/report-sheet: static pressed dims on all 10 ReportFlagModal Pressables using a FILL-COMPOSITED treatment that keeps label ink at full opacity — NEVER group opacity on text-bearing controls (the absorbed FIX: 0.75 group opacity collapses a 5.5:1 pair to ~2:1); `hapticSelection` moves to finger-down (onPressIn) on the category/severity pickers; Submit keeps its shipped notify('success').
2. T4/triage haptics: `hapticImpact('medium')` fires at the COMMIT POINT inside `applyStatusChange` (TasksScreen ~:637), landing AFTER the Reject confirm; the three commit actions' PressableScale get `haptic="none"` (Details stays 'selection'); `hapticNotify('success')` in applyStatusChange, `hapticNotify('error')` in setStatus's catch (~:696) covering both error branches; import the helpers by exact name (the bare `notify` symbol is already bound).
3. T8/spoken recompose: FlagCard single coherent a11yLabel routed through `severityA11y` — INCLUDING the selection-mode label (TasksScreen:1511, the absorbed FIX); action a11yLabels carry the flag's category (+ distance only when userLocation exists — degrade to category-only); `decorativeProps` lands on the inner sevDot View (LegendModal.tsx:79–83), NEVER the row View carrying the authored label; Home recent-row labels keep `statusA11y` when distance appears (HomeScreen.tsx recent-row label branch ~:354 — T8 WHAT leg d); the severity-4 photo nudge interpolates SEVERITY_LABELS, retiring the invented "major" (ReportFlagModal.tsx photo nudge — T8 WHAT leg e).
4. PROPOSED strings table (ships nothing): the 'Resolved'→'Resolve' verb alignment (TasksScreen :1543 + FlagDetailModal :1436) with the getByText test-lockstep list (TasksScreenFlagCard.test.tsx:128/157/170) — recorded for BP16's gate.
5. Guards + evidence: pressed-frame captures now DIFFER from rest (the audit's bitwise-identical pairs re-shot); jest asserts the haptic call sites + orders (impact after confirm; error in catch); a11y-tree re-walk of Tasks (no double-speak; actions carry subjects); guard regex covers the selection-mode branch; jest guard: the Home row label includes `statusA11y` output when distance renders (T8 §7).

### Phase-specific laws
- F4-01's OTHER verified leg (the color-name vocabulary on RN-web role-generic rows) is PARK-with-direction per the absorbed FIX — record it in your evidence report as parked, not dropped.
- This phase LANDS BEFORE MP4 by ordering law L4 — your FlagDetail/a11y-label edits settle the text baseline the material train will diff against.
- The in-sheet PhotoGallery dims (thumbPressed/removeBtnPressed) are the sheet's OWN vocabulary — extend it, don't invent a new one.

## 3 · RAILS (adapted to this repo's discovered infrastructure — master §3)
- The report is the design AUTHORITY. Execute to its recorded taste; never invent a decision it already made; never re-litigate a standing decision (DECISIONS §S) or a settled device read (D9/D10).
- Fork discipline: UI/read halves only. Forks 1-13 are Sky's (proximity/geo · points economy · auth-wall/guest · k-anonymity · trust-model scope · product name · stagePoolB · dark saved-place chips · ui/Button · M-12/M-13 swap · bulk mechanism lever · M-37 locked rows · lightbox blacks). A decision half you brush against becomes a DECISIONS §F question, never a build.
- Honesty fences: no invented copy, claims, prices, or numbers. Every NEW user-facing string is PROPOSED (before/after in your evidence report) unless DECISIONS §A already carries Sky's pick; proposed status strings use the em-dash grammar ("state — next step").
- Hard territory (never crossed): `src/components/ui/GlassSurface.tsx` DO-NOT-EDIT (thread the primitive, never fork it) · MapScreen's `pointerEvents="box-none"` overlay law untouched · the blur budget never exceeded (assert at the worst SIMULTANEOUS state, +1 for the tab-bar pane invisible to `__getLiveBlurPaneCount`) · the RM designed-stillness contract + the 220ms delay-gate/pulse-token discipline · RN reality (no CSS-only tricks; expo-blur intensities 12/24 only; virtualization law) · zero Supabase/Storage writes · never merge, push, build, deploy, or send anything external — those are Sky's hands, always.
- Evidence honesty: tag every claim verified / web-approximated / code-inferred / NEEDS-SKY-DEVICE. Web captures come from the audit's static-export rig (`node design-reviews/r2-audit/tools/probe-export.mjs` pattern serves the production bundle on :8082 — the dev server crashes Map/Tasks on the lucide lazy boundary; pre-existing, not yours to fix).
- **PROTECT ACTIVE for this phase:** PROTECT-3 the ReportFlagModal sheet architecture (no footer moves, no button replacement — dims only) · haptics.ts RM-independence stance (extensions only; OS governs) · the confirm gate on Reject (the impact lands POST-confirm by construction) · PROTECT-18 LiveStatusRegion announce law (untouched) · PROTECT-1 Nearby row labels (frozen — not in scope) — re-render each touched PROTECT surface BEFORE/AFTER and save both frames to `design-reviews/r2-audit/build-plan/evidence/BP03/`; diff-verify the byte-preserved ones your spec names.

## 4 · GATES (all hard — the phase does not stop until every one passes)
- `npm run typecheck` → 0 errors.
- `npm test` → green; RECORD the count in your evidence report (baseline **1857** at `a8549ff`; expect growth from guards you add; any unrelated red = STOP, do not paper over).
- `npm run lint` → 0 errors, no new warnings.
- The 7 immutable prior stacks files untouched (diff-check): `tools/audit-stacks.json` · `p2-material-stacks.json` · `bench3-material-stacks.json` · `shipped-stacks.json` · `wave1-stacks.json` · `wave2-stacks.json` · `map-stacks.json` (real paths at `a8549ff`: `audit`/`p2-material`/`bench3-material` under `design-reviews/fable-audit/tools/`; `shipped`/`wave1`/`wave2`/`map` under `qa-reports/assets/2026-07-03_tasks_glass/` · `_glass_w1/` · `_glass_w2/` · `2026-07-04_glass_map/` — diff-check by exact filename; only `map-stacks.json` is git-tracked at the base, so for the untracked six the check = never edit them).
- Every commit-plan item CLOSED or explicitly forked with a written reason — nothing silently dropped.
- **Arbiter:** REQUIRED for T4 (its pressed dims create new label-ink-vs-dimmed-fill pairs — the inlined spec's own §7): declare `design-reviews/r2-audit/tools/r2-report-ack-stacks.json` covering the pressed severity circles + every chip family (label ink vs dimmed fill, both schemes) and gate on exit 0 via `node ~/AccessMap-material-lab/2026-07-02/shared/contrast-check.mjs` — measured, never asserted. T8 changes no ink — no second sibling.
- **Diff scope = intended files only:** `src/screens/ReportFlagModal.tsx`, `src/screens/TasksScreen.tsx`, `src/screens/HomeScreen.tsx` (recent-row label branch), `src/screens/LegendModal.tsx` (sevDot decorativeProps), `src/lib/haptics.ts` (READ-only — existing API) (+ their test files), plus this phase's own untracked-side artifacts — its named `design-reviews/r2-audit/tools/r2-*-stacks.json` sibling(s) where mandated, its `build-plan/evidence/BP*` output, and the DECISIONS.md append (all inside the untracked `design-reviews/` tree, so they never appear in the tracked diff) — and any guard/test files its commit plan adds or updates. Anything else in the TRACKED diff = a defect; revert it.

## 5 · EVIDENCE + LEDGER DUTIES (before stopping)
- Write `design-reviews/r2-audit/build-plan/evidence/BP03-verification-evidence.md`: what shipped per commit-plan item · per-gate results (with the jest count) · honest tags on every claim · the PROTECT before/after pairs · any PROPOSED strings (before/after table) · anything Sky must eyeball.
- Append to DECISIONS.md — §P: `BP3: base <sha> → tip <sha> · <date> · gates green (jest <count>)` · §D device items contributed: press-in haptic feel on the pickers; commit/outcome haptic distinctness at triage; VoiceOver FlagCard walk (rides R2-D1 — the highest-stakes check). · §L launch items: none. · §F any fork question surfaced · §A a "default applied (BP3)" line for each §S/§O default you exercised.

## 6 · STOP
Built + green + **STOPPED on `r2/bp3-trust-hand`**. Never merge, push, build, or deploy — Sky's hands. One phase per fresh window. **RESUME RULE:** if this phase stalls, re-fire this same file in a fresh window; reconcile the branch's existing commits against the commit plan and continue from the FIRST gap — never redo a landed item, never renumber.
