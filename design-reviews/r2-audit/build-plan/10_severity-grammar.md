# BP10 — The actor completed — the severity grammar speaks everywhere severity renders

**Fire this file ALONE in a FRESH window.** Model law: **EXECUTE on Opus 4.8 ultracode MAX EFFORT, all sub-agents max effort** (Sky's direction for this train). Authored on Claude Fable 5 max effort, 2026-07-15, in the same window that authored the synthesis — provenance is disclosed, never silent; tag it in your phase report.

**Branch:** `r2/bp10-severity-grammar` — cut from the tip of `r2/mp5-admin-editorial` — or `r2/mp4-trust-ledger` if O-1 = SKIP (check DECISIONS §P for the actual prior tip; its tip SHA is in DECISIONS §P — **if that entry is absent, the prior phase has not run: STOP**). Record your base SHA as this phase's rollback anchor.
**Carries (conservation):** T5 — closes T5 (F6-01..F6-06) in full — eight sub-items including the SeverityDisc primitive and the onboarding teaching row.
**Effort:** L.
**Purpose:** Every surface rendering a real severity speaks the full canonical grammar — number + word + stake — from one primitive, and the scale introduces itself on day zero.

## 0 · READ FIRST (in order — no edit before all of these)
1. `design-reviews/r2-audit/build-plan/DECISIONS.md` — apply every §S standing decision and §O/§A entry named in §1 below; take your base tip from §P.
2. **THE REPORT (design authority):** `/Users/skypie/AccessMap/design-reviews/r2-audit/2026-07-10_AccessMap_R2_Design_Review.md` — your inlined block(s) below are copied from it verbatim (`### #3 · T5 —`); also read §4 (the PROTECT list + fork registry + R2-D ledger) and §5's phase spine.
3. The master: `design-reviews/r2-audit/build-plan/00_master.md` — §6 ordering laws that name this phase, §13 PROTECT spot list.
4. **VERIFY-FIRST:** every `file:line` in the spec was true at plan-authoring (audit HEAD `a8549ff`). Re-grep each cited anchor BEFORE editing; small drift → adapt and note it in your evidence report; meaningful drift (a mechanism moved or is absent) → **STOP** and surface to Sky (evidence report + DECISIONS §P note). Never build against drift.

## 1 · STANDING DECISIONS THAT BIND THIS PHASE (inlined — self-containedness includes decisions)
- **S-2:** T5 is signature-★; execute at signature care.
- Zero-visual-delta rule (absorbed FIX): the SeverityDisc primitive PRESERVES each adoption site's existing `maxFontSizeMultiplier` cap (true byte-delta-zero at default AND large type) — do not silently harden caps.
- **S-10:** model law.

## 2 · THE WORK — the spec, inlined VERBATIM from the report (the "Skeptic verdict: FIX (absorbed)" line is PART of the spec)
### #3 · T5 — The severity grammar speaks everywhere severity renders · median 8.7 · ★ DO-FIRST · signature ★
**WHERE:** src/components/FlagDetailModal.tsx:849 · src/screens/HomeScreen.tsx:358/:512 · src/screens/ProfileScreen.tsx:1246 · src/components/MyReportsModal.tsx:252 · src/components/MyWatchedModal.tsx:231 · src/screens/TasksScreen.tsx:1136 · new src/components/SeverityDisc.tsx + adoption at LegendModal:266, NearbyFlagsModal:399, ActivityFeedModal:451, RecentlyViewedRow:194 · src/components/OnboardingCards.tsx:107–108 (slide-2 illustration).
**Resolves:** F6-01 (HIGH), F6-02, F6-03, F6-04, F6-05, F6-06 · **Effort:** L · **Tier:** Signature ★ · **NEEDS-SKY-DEVICE:** yes
*Merge of the signature suite (F6-01/02/04/05/06) and the F6-03 onboarding teaching row — one signature system: the brand's atom completed AND introduced.*
1. **WHAT** — Every surface rendering a real severity speaks the full canonical grammar. Seven sub-items: (1) FlagDetailModal chip "Severity 4" → "Severity 4 · Significant" with accessibilityLabel pinned to severityA11y so the "of 5" anchor cannot regress (skeptic-adjusted direction), plus the stake line — SEVERITY_DESCRIPTIONS[n], e.g. "Hard or unsafe for most users." — as one quiet line under the meta row. (2) Home Recent 11px dot → 20–24px numbered mini-disc (RecentlyViewedRow recipe: digit ≥11pt, textOnColor ink, maxFontSizeMultiplier 1.3, SR-hidden — row label already speaks). (3) Profile pill "severity 3" → "Severity 3 · Moderate". (4) MyReports "•" → "·" + word added. (5) MyWatched unnumbered dot → numbered disc (sighted channel gained). (6) Tasks pill → "Nearest open barrier · No ramp · Severity 3 · 639 m" + matching a11y label. (7) Extract SeverityDisc primitive (sm/md/lg, digit + ink fork baked in, decorative escape hatch); adopt at the four numbered sites with zero visual delta. Pins excluded — four channels, at capacity. And onboarding front-loads it: slide 2's illustration circle swaps the generic Lucide map glyph for the five numbered severity discs in one quiet static row — the Legend in miniature, Deep Field styling, digits and ink fork intact, rendered with the SeverityDisc primitive (or RecentlyViewedRow's inline recipe if the suite hasn't landed) — above the existing "…then rate how bad it is." One accessible group, PROPOSED label from the wording spine (before: decorative icon; after: "Severity scale — 1 Minor to 5 Severe", derived from SEVERITY_LABELS). No severity-colored chrome, no animation: a static teaching image that happens to be true.
2. **WHY** — Resolves F6-01 (HIGH), F6-02(≡F2-09), F6-04, F6-05, F6-06. ONE VOICE is what this IS: blinded readers named the severity grammar as the brand unprompted (R5: "a civic reporting tool whose brand is its severity scale"), yet at HEAD it decays exactly where stakes peak — the decision sheet speaks one of three parts, the signed-in mirror surfaces drop word or digit, the first screen wears a degraded atom. theme.ts's own law ("color is ALWAYS paired with a number + a word") indicts every gap. ONE HAND: the primitive ends identity-by-discipline — F6-02/F6-04 are the drift already showing. Signature tier by definition: completion of the ownable thing, not garnish; the lens's killed candidates (digit-on-pin, tinted picker, toast tones) stay dead. F6-03 adds the day-zero leg: the one moment the app explains its core loop currently illustrates itself with a stock glyph while the app's most distinctive asset sits out its own introduction — slide 1 already got this right with the LogoMark (B3). ONE VOICE from minute one: users meet 1–5 warm, before the report form must carry the whole load.
3. **PERSONA IMPACT** — R7 first-class: the grammar sits on her designed-for-me list; discs are opaque ink-on-fill so RT renders them identically by construction, and MyWatched gains a visible severity channel she currently lacks entirely. R3's literal ask lands ("put the numbers in those dots too"). R1 gets number + word + stake at the routing decision. R2's inverted gap closes upward — the severityA11y pin guarantees SR strings stay richer-or-equal, never regressed. R6: the stake sentence says what a 4 MEANS at the only moment it matters. R4: no target changes. R5: "execution hygiene 5" rises — one atom, structurally enforced. On onboarding: R6 meets the grammar before the form instead of cold at it; R2 hears one grouped spine-derived label, not five colour swatches; R3 gets legible numbered discs where a glyph told her nothing; R7 first-class — opaque ink-on-fill discs render identically under RT by construction.
4. **TRUST + COHESION** — The grammar stops relaxing when the audience becomes the contributor — the one-hand read extends behind the auth fence. The deepest trust surface finally speaks the same sentence as every surface that leads to it, ending the landslide R2's worst-strings list quotes. The brand's atom becomes structure: future surfaces inherit the disc instead of re-deciding size, digit, and ink — which is exactly how the Home dot happened. And introducing the grammar WITH the grammar is the strongest one-voice statement available on day zero — it kills the stock-glyph tell that R5-class reviewers read as template DNA; a user who saw the scale on slide 2 recognizes the legend, the callouts, and the report form as one continuous system.
5. **A11Y + RESTRAINT** — Floor improves: Home and MyWatched gain redundant sighted channels; every disc ink is the arbitrated theme.ts:541–545 pair; digit holds the 11pt caption floor; below an 18pt disc the direction dies (F2-09's own caveat honored). SR strictly richer or equal, guard-tested. All five findings carry inline RESTRAINT: PASS — words and structure, no new color meaning, no motion. Nothing traded. Onboarding leg: grouped SR label derives from SEVERITY_LABELS (spine extension); digits hold the arbitrated textOnColor inks; static row = zero motion, RM identical by construction. RESTRAINT: PASS — words and structure, no new colour meaning, no motion; the finding's own conditions (static row, no severity-colored chrome, existing copy anchors meaning) honored.
6. **DEPENDENCIES / CONFLICTS** — COMPOSES with the material cluster's RB4/M-36 (same FlagDetailModal sheet): sub-item 1 lands as its own commit in a phase distinct from the sheet's re-material — sequence it after the material T where both ship (text/token change, composes either order; ids stay distinct per reconcile). Preserves PROTECT: ramp mode-independence (discs byte-identical both themes); the ink fork as ONE system (primitive bakes dark-ink-1–4/white-5 — never uniformed); the pin's four-channel capacity (primitive never applied to pins); the wording spine (labels/stakes derive from theme.ts:533 — extensions only); outcome beats stay severity-free (FlashBanner/LiveStatusRegion untouched); Legend row rhythm and Nearby card grammar (zero-delta adoption). Fork 3: presentation-only on already-signed-in surfaces, no gating change. Fork 5 untouched. §6.9 (MyWatched accessibilityViewIsModal) is a different prop — no collision. The onboarding row prefers the SeverityDisc primitive (sequence after it where both ship) but degrades to the inline recipe — no hard dependency; OnboardingCards.tsx touched by no other candidate; B3's slide-1 mark untouched. Killed-by-lens options stay dead: digit-on-pin (four channels at capacity — also PROTECT), tinted picker buttons, severity-toned toasts, grammar-speaking zero states (the PROTECT'd "Any severity" chip is the answer).
7. **VERIFICATION** — Named arbiter sibling tools/r2-severity-disc-stacks.json — all five fill×ink pairs at sm/md/lg, both themes. Zero-visual-delta: style-equality guard tests at the four existing disc sites (primitive's resolved styles === prior literals). Guard tests: chip accessibilityLabel contains "of 5" (severityA11y pin); chip text matches "Severity {n} · {word}"; Tasks pill visible + a11y strings. A11y-tree re-walk: Home, Nearby, detail sheet. Captures: home at-rest pair, flagdetail pair. SKY DEVICE: Profile pill, MyReports, MyWatched are auth-fenced (harness never signed in) — Sky signs in, walks all three surfaces both themes, VoiceOver pass on the detail chip and Tasks pill. Onboarding: capture pair onboarding slide-2 light/dark (discs byte-identical across themes — mode-independence PROTECT check); a11y-tree shows exactly one group node with the spine-derived label, no per-disc SR noise; disc inks covered by tools/r2-severity-disc-stacks.json (or tools/r2-onboarding-disc-stacks.json if it ships alone); render-state check at fontScale 1.3.


**Skeptic verdict: FIX (absorbed).** reword the R7 MyWatched line — it gains a NUMBERED channel (a colour dot already exists at :231); stakes derive from SEVERITY_DESCRIPTIONS (flags.ts:1235, comment-aligned to the ramp), only labels derive from theme.ts:533; ADD the blur-budget-unchanged assertion (zero panes); scope 'zero-visual-delta' to default Dynamic Type (preserve each site's maxFontSizeMultiplier cap, or record the 1.3 cap as intended large-type hardening).

### Commit plan (one commit per item, this order)
1. `src/components/SeverityDisc.tsx` primitive (sm/md/lg; digit + the designed ink fork BAKED — dark ink 1–4 / white 5, never uniformed; decorative escape hatch) + zero-delta adoption at the four numbered sites (LegendModal:266, NearbyFlagsModal:399, ActivityFeedModal:451, RecentlyViewedRow:194) with style-equality guards.
2. FlagDetail chip: 'Severity 4' → 'Severity 4 · Significant' + the stake line (SEVERITY_DESCRIPTIONS — flags.ts:1235, comment-aligned, NOT derived from theme.ts; say so in code comment) + accessibilityLabel pinned to `severityA11y` (the 'of 5' anchor cannot regress). Lands ON the MP4-materialed sheet (L5).
3. Home Recent 11px dot → 20–24pt numbered mini-disc (RecentlyViewedRow recipe; SR-hidden — the row label already speaks).
4. The signed-in mirror surfaces: Profile pill 'Severity 3 · Moderate' · MyReports '·' + word · MyWatched numbered disc (it gains the NUMBERED channel — a colour dot exists today; phrase it honestly) · Tasks nearest pill full grammar + matched a11y label. Auth-fenced: unit-verified + code-inferred; the eye pass is R2-D15.
5. Onboarding slide 2: the five-disc static teaching row (Deep Field, digits + ink fork intact) + ONE grouped spine-derived label — 'Severity scale — 1 Minor to 5 Severe' (PROPOSED string derived from SEVERITY_LABELS; it derives, so it ships with the derivation cited — no invented words).
6. Arbiter sibling `tools/r2-severity-disc-stacks.json` (all five fill×ink pairs at sm/md/lg, both themes) exit 0 + captures (home, nearby, flagdetail, onboarding light/dark byte-identical discs) + a11y-tree re-walks + the blur-budget-unchanged assertion (zero panes added — state it explicitly per the absorbed FIX).

### Phase-specific laws
- PINS ARE EXCLUDED — the pin's four channels are AT CAPACITY (PROTECT): no digit, no fifth channel, the primitive never applies to pins.
- Outcome beats stay severity-free (FlashBanner/LiveStatusRegion untouched — PROTECT).
- Killed-by-lens options STAY DEAD: digit-on-pin, tinted picker buttons, severity-toned toasts, grammar-speaking zero states (the PROTECT'd "Any severity" chip is the answer).

## 3 · RAILS (adapted to this repo's discovered infrastructure — master §3)
- The report is the design AUTHORITY. Execute to its recorded taste; never invent a decision it already made; never re-litigate a standing decision (DECISIONS §S) or a settled device read (D9/D10).
- Fork discipline: UI/read halves only. Forks 1-13 are Sky's (proximity/geo · points economy · auth-wall/guest · k-anonymity · trust-model scope · product name · stagePoolB · dark saved-place chips · ui/Button · M-12/M-13 swap · bulk mechanism lever · M-37 locked rows · lightbox blacks). A decision half you brush against becomes a DECISIONS §F question, never a build.
- Honesty fences: no invented copy, claims, prices, or numbers. Every NEW user-facing string is PROPOSED (before/after in your evidence report) unless DECISIONS §A already carries Sky's pick; proposed status strings use the em-dash grammar ("state — next step").
- Hard territory (never crossed): `src/components/ui/GlassSurface.tsx` DO-NOT-EDIT (thread the primitive, never fork it) · MapScreen's `pointerEvents="box-none"` overlay law untouched · the blur budget never exceeded (assert at the worst SIMULTANEOUS state, +1 for the tab-bar pane invisible to `__getLiveBlurPaneCount`) · the RM designed-stillness contract + the 220ms delay-gate/pulse-token discipline · RN reality (no CSS-only tricks; expo-blur intensities 12/24 only; virtualization law) · zero Supabase/Storage writes · never merge, push, build, deploy, or send anything external — those are Sky's hands, always.
- Evidence honesty: tag every claim verified / web-approximated / code-inferred / NEEDS-SKY-DEVICE. Web captures come from the audit's static-export rig (`node design-reviews/r2-audit/tools/probe-export.mjs` pattern serves the production bundle on :8082 — the dev server crashes Map/Tasks on the lucide lazy boundary; pre-existing, not yours to fix).
- **PROTECT ACTIVE for this phase:** PROTECT-4 the severity grammar (this phase EXTENDS the law) · PROTECT-25 the wording spine (labels derive; stakes = SEVERITY_DESCRIPTIONS, cite honestly) · the ink fork as ONE system (designed asymmetry — never 'fix' to uniform) · ramp mode-independence (discs byte-identical both themes — capture-prove) · the Legend severity-row rhythm + Nearby card grammar (zero-delta adoption) · Fork 3 boundary (presentation-only on signed-in surfaces) · Fork 5 boundary (no verified-count, no guest-write) — re-render each touched PROTECT surface BEFORE/AFTER and save both frames to `design-reviews/r2-audit/build-plan/evidence/BP10/`; diff-verify the byte-preserved ones your spec names.

## 4 · GATES (all hard — the phase does not stop until every one passes)
- `npm run typecheck` → 0 errors.
- `npm test` → green; RECORD the count in your evidence report (baseline **1857** at `a8549ff`; expect growth from guards you add; any unrelated red = STOP, do not paper over).
- `npm run lint` → 0 errors, no new warnings.
- The 7 immutable prior stacks files untouched (diff-check): `tools/audit-stacks.json` · `p2-material-stacks.json` · `bench3-material-stacks.json` · `shipped-stacks.json` · `wave1-stacks.json` · `wave2-stacks.json` · `map-stacks.json` (real paths at `a8549ff`: `audit`/`p2-material`/`bench3-material` under `design-reviews/fable-audit/tools/`; `shipped`/`wave1`/`wave2`/`map` under `qa-reports/assets/2026-07-03_tasks_glass/` · `_glass_w1/` · `_glass_w2/` · `2026-07-04_glass_map/` — diff-check by exact filename; only `map-stacks.json` is git-tracked at the base, so for the untracked six the check = never edit them).
- Every commit-plan item CLOSED or explicitly forked with a written reason — nothing silently dropped.
- **Arbiter:** `design-reviews/r2-audit/tools/r2-severity-disc-stacks.json` (sibling to the existing `r2-material-stacks.json`) — five fill×ink pairs at rendered sizes, both themes; run through `node ~/AccessMap-material-lab/2026-07-02/shared/contrast-check.mjs`; exit 0 gates.
- **Diff scope = intended files only:** new `src/components/SeverityDisc.tsx`; `src/components/FlagDetailModal.tsx`, `src/screens/HomeScreen.tsx`, `src/screens/ProfileScreen.tsx`, `src/components/MyReportsModal.tsx`, `src/components/MyWatchedModal.tsx`, `src/screens/TasksScreen.tsx`, `src/screens/LegendModal.tsx`, `src/screens/NearbyFlagsModal.tsx`, `src/components/ActivityFeedModal.tsx`, `src/components/RecentlyViewedRow.tsx`, `src/components/OnboardingCards.tsx` (+ their test files), plus this phase's own untracked-side artifacts — its named `design-reviews/r2-audit/tools/r2-*-stacks.json` sibling(s) where mandated, its `build-plan/evidence/BP*` output, and the DECISIONS.md append (all inside the untracked `design-reviews/` tree, so they never appear in the tracked diff) — and any guard/test files its commit plan adds or updates. Anything else in the TRACKED diff = a defect; revert it.

## 5 · EVIDENCE + LEDGER DUTIES (before stopping)
- Write `design-reviews/r2-audit/build-plan/evidence/BP10-verification-evidence.md`: what shipped per commit-plan item · per-gate results (with the jest count) · honest tags on every claim · the PROTECT before/after pairs · any PROPOSED strings (before/after table) · anything Sky must eyeball.
- Append to DECISIONS.md — §P: `BP10: base <sha> → tip <sha> · <date> · gates green (jest <count>)` · §D device items contributed: R2-D15 — Sky signs in: Profile pill, MyReports, MyWatched, the detail chip, the Tasks pill; VoiceOver pass; both themes. · §L launch items: none. · §F any fork question surfaced · §A a "default applied (BP10)" line for each §S/§O default you exercised.

## 6 · STOP
Built + green + **STOPPED on `r2/bp10-severity-grammar`**. Never merge, push, build, or deploy — Sky's hands. One phase per fresh window. **RESUME RULE:** if this phase stalls, re-fire this same file in a fresh window; reconcile the branch's existing commits against the commit plan and continue from the FIRST gap — never redo a landed item, never renumber.
