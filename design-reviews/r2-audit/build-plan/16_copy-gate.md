# BP16 — THE COPY GATE — one voice, proposed then applied

**Fire this file ALONE in a FRESH window.** Model law: **EXECUTE on Opus 4.8 ultracode MAX EFFORT, all sub-agents max effort** (Sky's direction for this train). Authored on Claude Fable 5 max effort, 2026-07-15, in the same window that authored the synthesis — provenance is disclosed, never silent; tag it in your phase report.

**Branch:** `r2/bp16-copy-gate` — cut from the tip of ``r2/bp15-drawer-guest`` (the prior phase; its tip SHA is in DECISIONS §P — **if that entry is absent, the prior phase has not run: STOP**). Record your base SHA as this phase's rollback anchor.
**Carries (conservation):** T17, T18 — closes T18 (F4-04, F4-11) and T17 (F4-06, F4-07, F4-09, F4-13..16, F4-18..20, F5-10) — mechanics immediately; STRINGS only after Sky's §A picks. Also APPLIES the deferred strings parked by earlier phases (BP3's verb flip; BP13's proposed wordings; BP15's optional guest line).
**Effort:** S + S (+ the STOP for Sky).
**Purpose:** The register stops disagreeing with itself: mechanics ship now (the tutorial announce gate, the Bell glyph); every string in the round lands in ONE before/after table for Sky; the resume applies exactly the picks.

## 0 · READ FIRST (in order — no edit before all of these)
1. `design-reviews/r2-audit/build-plan/DECISIONS.md` — apply every §S standing decision and §O/§A entry named in §1 below; take your base tip from §P.
2. **THE REPORT (design authority):** `/Users/skypie/AccessMap/design-reviews/r2-audit/2026-07-10_AccessMap_R2_Design_Review.md` — your inlined block(s) below are copied from it verbatim (`### #11 · T17 —` + `### #14 · T18 —`); also read §4 (the PROTECT list + fork registry + R2-D ledger) and §5's phase spine.
3. The master: `design-reviews/r2-audit/build-plan/00_master.md` — §6 ordering laws that name this phase, §13 PROTECT spot list.
4. **VERIFY-FIRST:** every `file:line` in the spec was true at plan-authoring (audit HEAD `a8549ff`). Re-grep each cited anchor BEFORE editing; small drift → adapt and note it in your evidence report; meaningful drift (a mechanism moved or is absent) → **STOP** and surface to Sky (evidence report + DECISIONS §P note). Never build against drift.

## 1 · STANDING DECISIONS THAT BIND THIS PHASE (inlined — self-containedness includes decisions)
- **S-8 + O-5 (BINDING):** nothing textual ships unproposed. The table carries BOTH channels (visible + spoken) for every row. The k≥3 privacy caveat rewording carries an explicit **Jordan Art. 7 semantic-equivalence sign-off line** (it IS the privacy disclaimer; DEFAULT_K_FLOOR stays the single numeric source — Fork 4 boundary).
- F4-20's Settings retitle and the two register rewrites are Sky TASTE PICKS — table rows, never defaults.
- **S-10:** model law.

## 2 · THE WORK — the spec, inlined VERBATIM from the report (the "Skeptic verdict: FIX (absorbed)" line is PART of the spec)
### #11 · T17 — One voice — the convergence sweep + the honest-completeness promise · median 7.25
**WHERE:** copy.ts (new single-source caveat export beside offlineBannerText) · MapScreen.tsx (heat disclaimer + heat-toggle hint + FAB label) · LegendModal.tsx + HelpModal.tsx (caveat import; dialect; query echo) · TasksScreen.tsx (subtitle, end-of-list footer + matched SR label, search empty) · NearbyFlagsModal.tsx (search placeholder + empty strings only) · ReportFlagModal.tsx (sheet title, partial-success titles, capability alert) · HamburgerDrawer.tsx + ProfileScreen.tsx + FlagDetailModal.tsx (casing) · SettingsScreen.tsx (banner sentence, header retitle) · HomeScreen.tsx (genuine-empty clause) · UpdateBanner.tsx (glyph half only).
**Resolves:** F4-06, F4-07, F4-09, F4-13, F4-14, F4-15, F4-16, F4-18, F4-19, F4-20, F5-10 · **Effort:** S · **Tier:** QuickWin · **NEEDS-SKY-DEVICE:** no
*Merge of the one-privacy-promise/honest-completeness pair (F4-06/09) and the convergence sweep (the register's residual drift) — one copy pass; every string PROPOSED via before/after table; nothing ships.*
1. **WHAT** — (promise) The app's completeness honesty speaks once, exactly. (1) The k≥3 privacy caveat — currently three wordings: "reported" / "submitted" / "reports are shown" — converges on the map disclaimer's anchor wording and becomes one exported string beside offlineBannerText in copy.ts, keeping the DEFAULT_K_FLOOR interpolation, imported by all three surfaces. (2) The Tasks end-of-list footer drops its two unearned claims — before: "That's everything nearby — you're up to date" (SR: "You have seen all flags nearby"); after sketch, em-dash grammar: "That's every report loaded — more appear as neighbours flag them." with the SR label matched. Both PROPOSED via before/after table; nothing ships without Sky. (sweep) And one pass retires the register's residual drift: noun canon (signed-in sheet title and Map FAB adopt "Report a barrier (here)"; the Tasks subtitle retires "reports"); the two bureaucrat sentences rewritten per the findings' sketches; Nearby search converges on Tasks' praised wording; partial-success "Report filed — context tags not saved."; dialect standardized ("colour", Canadian majority); casing finishes S20's sentence-case sweep; Help echoes the failing query and Home's genuine-empty gains one inviting clause toward the FAB; the Settings header stutter (eyebrow "SETTINGS" over title "Settings") retitled to Sky's pick; the surviving 🔔 becomes Lucide Bell per the B2 pattern.
2. **WHY** — Resolves F4-06 + F4-09 (both MEDIUM, one register: completeness claims). PROTECT-11 names the k≥3 caveat part of the trust moat — the one sentence the app should say identically every time; three wordings of a privacy promise invite the reading that none is exact. The footer contradicts the heat caveat's honest-completeness register at the exact moment a verifier decides they're done (R1: "absence of a flag is not absence of a barrier"). ONE VOICE: the app's most ethically load-bearing sentences adopt the offlineBannerText one-true-string discipline — the app's own model, named in its own docstring. And the sweep resolves nine LOW/POLISH drifts plus F4-07: F4's lens verdict names dispersion as "the register disagreeing with itself" — each item small, together the audible difference between one writer and a committee. ONE VOICE by name; S15/S20/B2 each get their completion clause (cited as shipped-fix-incomplete under the regression clause, not re-opens).
3. **PERSONA IMPACT** — R1's core stake is the footer: the completeness claim stops overreaching. R6 — who kept the app because it is honest about its limits — gets the same honesty at list-end that won them at submit. R2 hears one identical privacy rule on every surface; repetition builds authority in a spoken-first session. R5's dispersion diagnosis gets its cleanest counterexample. R7 first-class: these strings render on solid banners and footers, identical in RT — R7 gains one memorizable rule sentence (cognitive load down) instead of three near-misses. R3/R4: no layout, target, or type changes. On the sweep: R6's Round-1 "four-name whiplash" gets its last leak sealed at the highest-traffic moment; R5's cold-read catches (casing drift, register slips) are the sweep's literal checklist; R2 hears consistent nouns and echoed queries; R7 first-class — gains the Bell inside the Lucide iconography she called designed-for-me, on solid surfaces RT renders identically.
4. **TRUST + COHESION** — A privacy promise worded three ways reads as unverified paperwork; worded once, it reads as law. The end-of-list beat stops claiming a census a crowdsourced model cannot have, aligning Tasks' warmest closing moment with the heat caveat's honesty register. Single-sourcing hardens the future: the next surface that needs the caveat imports it and cannot drift — the same mechanism that ended the offline banner's three-way drift. And dispersion is how a one-writer voice becomes a committee; this sweep is the committee resigning — none of it individually visible, all of it together why the app reads as one hand.
5. **A11Y + RESTRAINT** — Zero visual change beyond string content; SR labels updated in lockstep so visible and spoken stay paired. Honesty strictly improves — two unearned claims removed, zero warmth lost (the replacement stays inviting). The k threshold, heat mechanics, and the Jordan Art. 7 disclaimer's required visibility are untouched — wording convergence only. Em-dash grammar held in both proposed strings. RESTRAINT: PASS — words only. Sweep: strings and one decorative glyph only; every SR label updates in lockstep; the designed spoken-brevity pairs stay designed; the five-way Tasks empty fork (PROTECT) and AddressSearch's failure≠no-matches split (PROTECT) untouched; em-dash grammar held in every proposed status string; the Bell inherits the emoji's decorative hiding. RESTRAINT: PASS — register repaired, nothing traded.
6. **DEPENDENCIES / CONFLICTS** — Fork 4 (k-anonymity) adjacency: the rule's DECISION half is Sky's — this converges the wording of the existing promise, never the number or mechanics; DEFAULT_K_FLOOR stays the single numeric source. Extends PROTECT-11 and the offlineBannerText discipline (PROTECT). Preserves the five-way Tasks empty fork (PROTECT) — the footer is the pagination end-line, not the empty fork. Shared files: MapScreen.tsx and TasksScreen.tsx overlap other candidates — disjoint lines. "Neighbours" spelling aligns with the sweep candidate's F4-13 dialect call — land together or sweep-first. Coordinate F4-19 with T2's RB11 (same UpdateBanner.tsx): this ships only the glyph/string half; the merge lead may fold it into T2/MP0. PROTECT-1: Nearby's frozen row labels untouched (only search placeholder/empty converge). Fork 6 (product name) untouched by the noun canon; Fork 3 untouched (the auth-variant title is a string change). F4-20's retitle is a Sky taste pick. Fork 4 (k-anonymity) adjacency: converges the WORDING of the existing k≥3 promise, never the number or mechanics (DEFAULT_K_FLOOR stays the single numeric source).
7. **VERIFICATION** — Grep/jest single-source guard: the caveat string has exactly one definition and three import sites; zero other "at least 3" user-facing variants in src. Snapshot tests on all three render sites emitting the anchor wording with the interpolated floor. Footer test: visible string and SR label both updated ("You have seen all flags nearby" retired). Before/after copy table to Sky — strings are PROPOSED. No ink/floor change, so no arbiter. A11y-tree re-walk of legend and Tasks end-of-list confirms spoken parity. Sweep: before/after copy table (every string, both channels) for Sky sign-off; jest string/snapshot tests per site; grep guards (zero "Report a flag" variants, zero user-facing "color", zero title-case strays in the named rows, the BENCH-1 emoji census extended to unit-render UpdateBanner behind the fence); a11y-tree re-walk on report-open, tasks, legend, settings. No colour/floor/ink change → NO arbiter, except if the Bell's ink pair is new rather than inherited, run tools/r2-voice-sweep-stacks.json.


**Skeptic verdict: FIX (absorbed).** search-convergence — do NOT port Tasks' verbatim placeholder to Nearby (both use searchFlags matching description+category+STATUS); converge the register but keep the TRUE 3-field list ('…description, category, or status…') on both; the F4-07 noun flip breaks ReportFlagModal.test.tsx:427/430 + :399/402 + qaMergeConsolidation.test.ts:35 — name + update them in the same commit; the k≥3 caveat IS the Jordan Art. 7 disclaimer — the before/after table carries an explicit Jordan semantic-equivalence sign-off, not only Sky's taste read.

### #14 · T18 — The tutorial tells the present truth · median 6.9
**WHERE:** OnboardingModal.tsx (announce gate + deck copy source), OnboardingCards.tsx (shared CARDS source or copy export), SettingsScreen.tsx (replay row promise).
**Resolves:** F4-04,F4-11 · **Effort:** S · **Tier:** QuickWin · **NEEDS-SKY-DEVICE:** no
1. **WHAT** — Two repairs to the replay tutorial. (1) Gate the position announce on visible — the useEffect([index]) fires on closed mount today, publishing a phantom "Step 1 of 3" into Settings' live region (and straight to VoiceOver on native). (2) End the two-generation copy split: the replay deck ports S15's rewritten copy (canon "barrier," the photo-needs-account honesty, the current slide-2 sentence) and adopts one position grammar ("Card N of M," matching OnboardingCards) — preferably by sharing a single CARDS source with a digest flag rather than duplicating strings; the Settings row's "3-card" promise updates to whichever count ships. Consent slides are unaffected on replay. Both decks' copy PROPOSED side-by-side for Sky.
2. **WHY** — Resolves F4-04 + F4-11 (both MEDIUM, one root: the replay deck is a fossil). ONE VOICE head-on — the lens verdict's sharpest line is that the app "remembers its own old accent and replays it on request": a user asking to be re-taught receives pre-uplift vocabulary that disagrees with every surface they'll then touch. The phantom announce is spoken noise that erodes trust in every real announcement, and the announce channel is the mission cohort's primary channel — its credibility is a floor asset, not polish.
3. **PERSONA IMPACT** — R2 first: the stray "Step 1 of 3… referring to nothing" leaves Settings, and every future announcement regains authority. R6 — the first-timer under cognitive load, exactly who replays a tutorial — relearns the app in the words it actually uses now, including the canon noun and the photo honesty sentence. R1: the taught vocabulary matches the severity grammar everywhere else. R7 first-class: the replayed deck teaches the app R7 called 85% first-class, not its pre-uplift ancestor — the digest R7 might lean on under RT stays current. R3/R4/R5: consistency at zero layout cost.
4. **TRUST + COHESION** — A tutorial is the app explaining itself; when it speaks last year's voice, every canon string it contradicts loses a little authority. After: one deck lineage, one position grammar, one noun canon from first-run through replay — and a live region that only ever speaks the truth, which is the cheapest trust asset the spoken app owns. The Settings row's promise ("3-card") stops disagreeing with what opens.
5. **A11Y + RESTRAINT** — Strictly improves the floor: removes a false status message (WCAG 4.1.3 quality) while keeping the position announce for real navigation; keeps "Back. Disabled on first card." (PROTECT-17, reaffirmed by this cluster's rail) intact; adds the photo-needs-account honesty the replay currently omits. No motion change: the modal mount stays RM-ternaried as shipped and the announce remains motion-decoupled. RESTRAINT: PASS — removes noise, adds truth, decorates nothing.
6. **DEPENDENCIES / CONFLICTS** — Preserves PROTECT-17 and the settled S19 platform fork — consent/permission slides untouched on replay (the shared-source-with-digest-flag option avoids re-running consent entirely; recommend it over replaying the raw 5-card deck). Shared file: SettingsScreen.tsx also touched by the sweep candidate (F4-20) — disjoint rows. No fork adjacency, no material, no inks, no blur. Sequencing: fully independent; a safe first merge for the cluster.
7. **VERIFICATION** — Jest: mounting OnboardingModal with visible=false fires zero announcements (mock announceForAccessibility + live-region text assert); opening announces "Card 1 of N" exactly once; a copy-parity test pins replay strings equal to the S15 source (structural if single-sourced); the Settings row promise matches CARDS.length. A11y-tree re-walk of Settings — the orphan text node at line 69 is gone. Capture the replay deck (opens from Settings; capturable per the finding). No ink change, so no arbiter. Native VoiceOver spot-check may ride any existing Sky device session but is not gating — the web tree plus jest prove the fix.


**Skeptic verdict: FIX (absorbed).** harden the announce gate — add `visible` to the effect deps AND guarantee exactly one post-reset 'Card 1 of N' (coordinate with the reset-to-0 effect so no stale pre-reset index fires first); extend the jest case to reopen-after-navigating-to-a-later-card; make the copy-PORT path (canon 'barrier' + slide-2 sentence + photo-needs-account line + 'Step'→'Card' + Settings:579 count) the primary Effort-S plan, CARDS de-dup optional; PROTECT-17 lives in OnboardingCards:417 (the replay Back is conditionally rendered — legit).

### Commit plan (one commit per item, this order)
1. T18/mechanics: the replay-tutorial announce gate — `visible` joins the effect deps AND exactly ONE post-reset 'Card 1 of N' announce on open (coordinate with the reset-to-0 effect :81–86 so no stale pre-reset index fires); jest covers fresh-open AND reopen-after-navigating-to-card-3; the copy-PORT path is primary (canon copy + slide-2 sentence + the photo-needs-account line + 'Step'→'Card' at :74/:231/:248 + SettingsScreen:579 tracks CARDS.length) — the ported strings are TABLE rows, the mechanism ships.
2. T17/glyph: the surviving 🔔 (UpdateBanner.tsx:61) → Lucide Bell per the B2 pattern, decorative/SR-hidden, INHERITED ink (if the ink pair is new: STOP, declare `tools/r2-voice-sweep-stacks.json` first); ADD the emoji-census guard test (BENCH-1's census was a survey — no jest guard exists at `a8549ff`, so CREATE it) unit-rendering UpdateBanner (zero emoji StaticText, truly app-wide).
3. THE TABLE (ships nothing): every T17 string with before/after in both channels — the noun canon ('Report a barrier (here)'), the k≥3 single-source caveat (+ Jordan line), the search twins converged on the TRUE 3-field list ('description, category, or status' — searchFlags matches all three; NEVER port Tasks' 2-field wording verbatim, the absorbed FIX), partial-success 'Report filed — context tags not saved.', dialect (colour), casing rows, Help query echo, Home genuine-empty clause, Tasks end-of-list footer + SR twin, F4-20 retitle PAIR, the two register rewrites — PLUS the parked strings from earlier phases (BP3 verb flip incl. FlagDetail :1436; BP13 arrival/failure wordings; BP15 guest line). Written to your evidence report AND appended to DECISIONS §O-5. **THEN STOP.**
4. ON RESUME (after §A carries picks): apply EXACTLY the picked strings with test-lockstep — ReportFlagModal.test.tsx:427/430 + :399/402, qaMergeConsolidation.test.ts:35, the getByText('Resolved') sites (TasksScreenFlagCard.test.tsx:128/157/170), the grep guards (zero 'Report a flag' variants, zero user-facing 'color', zero title-case strays in the named rows); SR labels update in lockstep with visible twins; a11y-tree re-walks (report-open, tasks, legend, settings).

### Phase-specific laws
- PROTECT-1: Nearby's frozen row labels untouched — only search placeholder/empty strings converge.
- The spoken-brevity twins are DESIGNED (visible warm / spoken plain) — paired rows in the table keep the pairing; the five-way Tasks empty fork and AddressSearch's failure≠no-matches split are PROTECT (the footer is the pagination end-line, not the empty fork).
- PROTECT-17's replay-deck Back button nuance per the absorbed T18 FIX: conditional RENDER is the legitimate alternative; 'keeps PROTECT-17 intact' = OnboardingCards:417 undisturbed.

## 3 · RAILS (adapted to this repo's discovered infrastructure — master §3)
- The report is the design AUTHORITY. Execute to its recorded taste; never invent a decision it already made; never re-litigate a standing decision (DECISIONS §S) or a settled device read (D9/D10).
- Fork discipline: UI/read halves only. Forks 1-13 are Sky's (proximity/geo · points economy · auth-wall/guest · k-anonymity · trust-model scope · product name · stagePoolB · dark saved-place chips · ui/Button · M-12/M-13 swap · bulk mechanism lever · M-37 locked rows · lightbox blacks). A decision half you brush against becomes a DECISIONS §F question, never a build.
- Honesty fences: no invented copy, claims, prices, or numbers. Every NEW user-facing string is PROPOSED (before/after in your evidence report) unless DECISIONS §A already carries Sky's pick; proposed status strings use the em-dash grammar ("state — next step").
- Hard territory (never crossed): `src/components/ui/GlassSurface.tsx` DO-NOT-EDIT (thread the primitive, never fork it) · MapScreen's `pointerEvents="box-none"` overlay law untouched · the blur budget never exceeded (assert at the worst SIMULTANEOUS state, +1 for the tab-bar pane invisible to `__getLiveBlurPaneCount`) · the RM designed-stillness contract + the 220ms delay-gate/pulse-token discipline · RN reality (no CSS-only tricks; expo-blur intensities 12/24 only; virtualization law) · zero Supabase/Storage writes · never merge, push, build, deploy, or send anything external — those are Sky's hands, always.
- Evidence honesty: tag every claim verified / web-approximated / code-inferred / NEEDS-SKY-DEVICE. Web captures come from the audit's static-export rig (`node design-reviews/r2-audit/tools/probe-export.mjs` pattern serves the production bundle on :8082 — the dev server crashes Map/Tasks on the lucide lazy boundary; pre-existing, not yours to fix).
- **PROTECT ACTIVE for this phase:** PROTECT-1 (Nearby labels frozen) · PROTECT-11 (the privacy voice — corrections move TOWARD it) · PROTECT-19 (em-dash grammar in every proposed status string) · PROTECT-24 (the Lucide house style — the Bell completes it) · the taught severity scale + submit-moment honesty sentence (never compressed, never touched) — re-render each touched PROTECT surface BEFORE/AFTER and save both frames to `design-reviews/r2-audit/build-plan/evidence/BP16/`; diff-verify the byte-preserved ones your spec names.

## 4 · GATES (all hard — the phase does not stop until every one passes)
- `npm run typecheck` → 0 errors.
- `npm test` → green; RECORD the count in your evidence report (baseline **1857** at `a8549ff`; expect growth from guards you add; any unrelated red = STOP, do not paper over).
- `npm run lint` → 0 errors, no new warnings.
- The 7 immutable prior stacks files untouched (diff-check): `tools/audit-stacks.json` · `p2-material-stacks.json` · `bench3-material-stacks.json` · `shipped-stacks.json` · `wave1-stacks.json` · `wave2-stacks.json` · `map-stacks.json` (real paths at `a8549ff`: `audit`/`p2-material`/`bench3-material` under `design-reviews/fable-audit/tools/`; `shipped`/`wave1`/`wave2`/`map` under `qa-reports/assets/2026-07-03_tasks_glass/` · `_glass_w1/` · `_glass_w2/` · `2026-07-04_glass_map/` — diff-check by exact filename; only `map-stacks.json` is git-tracked at the base, so for the untracked six the check = never edit them).
- Every commit-plan item CLOSED or explicitly forked with a written reason — nothing silently dropped.
- **Arbiter:** NO arbiter sibling needed — this phase changes no color/floor/ink pair. If any edit turns out to touch an ink pair, STOP and declare a named sibling first under `design-reviews/r2-audit/tools/` and run it through `node ~/AccessMap-material-lab/2026-07-02/shared/contrast-check.mjs` to exit 0 (the rail: measured, never asserted).
- **Diff scope = intended files only:** `src/screens/OnboardingModal.tsx`, `src/components/OnboardingCards.tsx` (read-mostly), `src/screens/SettingsScreen.tsx`, `src/components/UpdateBanner.tsx` (+ their test files, incl. the NEW census guard and the announce-gate tests); ON RESUME: additionally the string sites named in the table — including the four stage-1 files above (SettingsScreen retitle/count rows, the Onboarding deck ports) — (ReportFlagModal, MapScreen, TasksScreen, NearbyFlagsModal, LegendModal, HelpModal, HamburgerDrawer, ProfileScreen, FlagDetailModal, HomeScreen, `src/lib/copy.ts`) (+ their test files), plus this phase's own untracked-side artifacts — its named `design-reviews/r2-audit/tools/r2-*-stacks.json` sibling(s) where mandated, its `build-plan/evidence/BP*` output, and the DECISIONS.md append (all inside the untracked `design-reviews/` tree, so they never appear in the tracked diff) — and any guard/test files its commit plan adds or updates. Anything else in the TRACKED diff = a defect; revert it.

## 5 · EVIDENCE + LEDGER DUTIES (before stopping)
- Write `design-reviews/r2-audit/build-plan/evidence/BP16-verification-evidence.md`: what shipped per commit-plan item · per-gate results (with the jest count) · honest tags on every claim · the PROTECT before/after pairs · any PROPOSED strings (before/after table) · anything Sky must eyeball.
- Append to DECISIONS.md — §P: `BP16: base <sha> → tip <sha> · <date> · gates green (jest <count>)` · §D device items contributed: none new — strings/glyph verify on web + unit; VoiceOver spot-check of changed spoken labels folds into R2-D3. · §L launch items: none. · §F any fork question surfaced · §A a "default applied (BP16)" line for each §S/§O default you exercised.

## 6 · STOP
Built + green + **STOPPED on `r2/bp16-copy-gate`**. Never merge, push, build, or deploy — Sky's hands. One phase per fresh window. **RESUME RULE:** if this phase stalls, re-fire this same file in a fresh window; reconcile the branch's existing commits against the commit plan and continue from the FIRST gap — never redo a landed item, never renumber.
