# BP11 — One press vocabulary — every control answers the hand

**Fire this file ALONE in a FRESH window.** Model law: **EXECUTE on Opus 4.8 ultracode MAX EFFORT, all sub-agents max effort** (Sky's direction for this train). Authored on Claude Fable 5 max effort, 2026-07-15, in the same window that authored the synthesis — provenance is disclosed, never silent; tag it in your phase report.

**Branch:** `r2/bp11-press-vocab` — cut from the tip of ``r2/bp10-severity-grammar`` (the prior phase; its tip SHA is in DECISIONS §P — **if that entry is absent, the prior phase has not run: STOP**). Record your base SHA as this phase's rollback anchor.
**Carries (conservation):** T3 — closes T3 (F1-02, F1-05, F1-06, F1-07, F1-10, F1-11, F1-12, F1-13, F1-14) in full.
**Effort:** L.
**Purpose:** One press dialect app-wide: the static-truth dim (motion-free, blur-free, RT-native) with the spring as garnish — the ~20-control primitive estate, the tab bar, the filter language, the modal closes, the web callout button.

## 0 · READ FIRST (in order — no edit before all of these)
1. `design-reviews/r2-audit/build-plan/DECISIONS.md` — apply every §S standing decision and §O/§A entry named in §1 below; take your base tip from §P.
2. **THE REPORT (design authority):** `/Users/skypie/AccessMap/design-reviews/r2-audit/2026-07-10_AccessMap_R2_Design_Review.md` — your inlined block(s) below are copied from it verbatim (`### #6 · T3 —`); also read §4 (the PROTECT list + fork registry + R2-D ledger) and §5's phase spine.
3. The master: `design-reviews/r2-audit/build-plan/00_master.md` — §6 ordering laws that name this phase, §13 PROTECT spot list.
4. **VERIFY-FIRST:** every `file:line` in the spec was true at plan-authoring (audit HEAD `a8549ff`). Re-grep each cited anchor BEFORE editing; small drift → adapt and note it in your evidence report; meaningful drift (a mechanism moved or is absent) → **STOP** and surface to Sky (evidence report + DECISIONS §P note). Never build against drift.

## 1 · STANDING DECISIONS THAT BIND THIS PHASE (inlined — self-containedness includes decisions)
- **S-2:** T3 is signature-★.
- Absorbed-FIX law: the internal tint borrows ONLY `opacity: 0.85` from FlagCard cardPressed (TasksScreen ~:2103) — never its scale (the spring stays the sole owner of transform).
- **S-10:** model law.

## 2 · THE WORK — the spec, inlined VERBATIM from the report (the "Skeptic verdict: FIX (absorbed)" line is PART of the spec)
### #6 · T3 — One press vocabulary — every control answers the hand · median 8.1 · signature ★
**WHERE:** src/components/ui/PressableScale.tsx (internal static dim + hover); RootNavigator.tsx (custom tabBarButton); MapScreen.tsx filter panel (collapse header, S16 Clear, saved-set pills, category/severity/status/affects chips, '+ Save current'); TasksScreen.tsx cat chips + sort tabs; FlagDetailModal.tsx (~15 uncovered controls incl. Close, Directions/Share/History, edit/save/cancel, reopen); Sheet.tsx close + bespoke closes in LegendModal/NearbyFlagsModal/FeedbackModal/MyFeedbackModal/StatusHistoryModal/AchievementsModal/NotificationPrefsModal; HomeScreen.tsx search pill + mapPeekHint chip + list paddingBottom; PlatformMap.web.tsx:471–488 callout button. GlassSurface.tsx never touched.
**Resolves:** F1-02,F1-05,F1-06,F1-07,F1-10,F1-11,F1-12,F1-13,F1-14 · **Effort:** L · **Tier:** Signature · **NEEDS-SKY-DEVICE:** yes
1. **WHAT** — One press dialect app-wide: the static-truth dim. PressableScale itself gains an always-on ({pressed}) tint layer — reusing the FlagCard cardPressed values — with the 0.97 spring kept strictly as motion-allowed garnish and a hovered tint riding the same internal style; one file upgrades ~20 controls (Map's 7 tools, zoom, both FABs, Home locate/refresh/retry/recents/pill, Tasks triage). The tab bar gets a custom tabBarButton on the repaired primitive + hapticSelection. The filter language adopts the panel's own presetBtnPressed dim; FlagDetail's silent controls and the modal-family closes finish their surfaces (one dim in Sheet.tsx cascades; four bespoke closes hand-fixed); Home's pill and peek-hint chip take a pressed tint (plus a few pt bottom list padding — the F1-10 rider); the web callout 'Open details' button pads to ≥44px with the ctaFill pressed/hover/focus treatment. All values token-bound, reused, never invented.
2. **WHY** — Resolves F1-02 (HIGH — the primitive's RM feedback hole institutionalized in its own doc comment), F1-05/06/07 (MED), F1-10/11/12/14 (LOW), F1-13 (POLISH). This is the thesis's ONE HAND leg made literal: the app speaks three press dialects, its most-pressed chrome is silent, and R4's begged-for fix #1 is exactly this. It also serves ONE MATERIAL — material that visibly answers pressure behaves like matter. Taste anchor honored: acknowledgment in stillness, a dim not a dance, always legible. Signature claim: in an app built for tremor users, no press anywhere goes unanswered — and the answer never depends on motion, platform, or ability. The two PROTECT'd patterns are the grown seed, not new invention.
3. **PERSONA IMPACT** — R4 lives in the during-channel ('the moment-of-touch layer is missing almost everywhere, and that's the layer I live in') — re-press-from-uncertainty disappears at the filter language, tab bar, closes, and FABs. R7 is first-class by construction: the static dim IS the truth layer — blur-free, motion-free, opaque-friendly — so RT/C-lite users get the identical answer as everyone; their explicit ask ('that's not reduced motion, that's removed feedback') closes. R3 gains a luminance-based state change that reads at low vision and large type. R1 and R6 gain confidence at the filtering language they plan routes with. R2's audible loop is untouched; R5's execution-hygiene score rises where it was cheapest to lose.
4. **TRUST + COHESION** — The 'many hands' tell — sibling controls on one pane answering differently (preset buttons dim, chips dead) — disappears; drawer, tab bar, filter chip, and close button finally speak one dialect. A press that answers instantly reads as an app that is listening. The one sub-floor new control in the app (web callout button) joining the 44pt floor closes the last new-chrome exception, so the S3 doorway's first press feels governed by the same hand as everything Round 2 praises.
5. **A11Y + RESTRAINT** — Floor preserved and raised: F1-14 brings the callout button to ≥44px (2.5.8 floor held everywhere new); static dims survive RM by construction — zero motion added, B5's stillness tests keep passing per the skeptic's own note; haptics stay OS-governed. Pressed ink pairs are arbiter-gated, never eye-tuned. Delight (spring, hover) stays layered above truth per the FlagCard model. RESTRAINT: PASS — strictly additive acknowledgment; no legibility, reachability, or honesty cost anywhere in the estate.
6. **DEPENDENCIES / CONFLICTS** — Preserves the drawer-row pressed dim (spread, never dilute) and the FlagCard static-truth + gated-delight layering (the internal model for the primitive repair). Preserves haptics.ts RM-independence (tab haptic is an extension). Preserves new-chrome target discipline (44pt everywhere; dims shrink nothing). Preserves the S17 pointer-inert peek (tint lives on the mapPeekHint chip, never the tiles) and the audible filter-acknowledgment loop (untouched). Fork 8 boundary: saved-set pills get the dim only — zero dark-color restyle. Fork 9 boundary: Button.tsx untouched. Threads PressableScale, never forks GlassSurface. Shares MapScreen/TasksScreen with T11 (chip SR-state) and T1 (callout) and RootNavigator with T12 (drawer passage) and T16 (badge) — separate hunks; land after or with the Report-sheet candidate so core flow leads.
7. **VERIFICATION** — Arbiter sibling tools/r2-press-vocab-stacks.json — every adopted pressed pair (label ink vs dimmed fill) plus the callout button's ctaFill pressed/hover/focus states, both schemes; ships only on exit 0. Guard tests extend the B5 RM net: pressed-style presence asserted per family; existing RM spring-skip tests must stay green. Re-capture the twin pairs — press/clear-all rest-vs-pressed and press/tabbar rest-vs-pressed must now differ (today bitwise-identical). Blur-pane count unchanged at the worst simultaneous state (+1 invisible tab-bar pane). Sky device: press-and-hold a drawer row, a Map tool, a tab, a filter chip — one felt dialect; tab press haptic fires; RM on — dims still answer.


**Skeptic verdict: FIX (absorbed).** the custom tabBarButton MUST forward every injected a11y prop (role/state.selected/label/style/children) onto the wrapped PressableScale + assert the tab AX node keeps selected — the dim/haptic are strictly additive; reconcile the close-button count to 7 (naming which inherit the Sheet.tsx:64 cascade vs hand-fixed); borrow opacity-0.85 ONLY from cardPressed (leave scale to the spring); remap stale C-ids→T-ids; add the box-none preservation line for the filter chips.

### Commit plan (one commit per item, this order)
1. PressableScale internal always-on `({pressed})` static tint (opacity-only) + a hovered tint riding the same internal style — one file upgrades the ~20-control estate; B5's RM spring-skip tests stay green (the dim survives RM by construction).
2. The tab bar: custom `tabBarButton` on the repaired primitive + `hapticSelection` — FORWARDING EVERY injected prop (accessibilityRole, accessibilityState.selected, accessibilityLabel, style, children) with a regression assertion that the tab AX node still exposes selected (the load-bearing absorbed FIX).
3. The filter language adopts the panel's own presetBtnPressed dim (collapse header :1738, S16 Clear :1757, saved-set pills :1804, '+ Save current' :1845, the chip families) — style-only; pointerEvents untouched (box-none preservation line per the absorbed FIX).
4. FlagDetail's ~15 silent controls + the modal-family closes: Sheet.tsx:64 dim cascades to its inheritors; the SEVEN bespoke closes reconciled per the absorbed FIX — name in your evidence which inherit vs which are hand-fixed (Legend, Nearby, Feedback, MyFeedback, StatusHistory, Achievements, NotificationPrefs).
5. Home: search pill + mapPeekHint chip pressed tints + the F1-10 rider (a few pt bottom list padding); web hover (F1-13) rides the primitive's hovered tint.
6. Web callout 'Open details' (PlatformMap.web.tsx:471–488): pad to ≥44px + the ctaFill pressed/hover/focus treatment — the last new-chrome 44pt exception closes.
7. Arbiter sibling `tools/r2-press-vocab-stacks.json` (every adopted pressed pair label-ink vs dimmed-fill + the callout button states, both schemes) exit 0; re-capture the twin pairs (press/clear-all rest-vs-pressed, press/tabbar rest-vs-pressed — today bitwise-identical, must now differ); guards per family; blur-pane count unchanged.

### Phase-specific laws
- The two PROTECT'd patterns are the SEED (drawer-row dim; FlagCard static-truth + gated-delight layering) — spread, never dilute; delight stays layered above truth.
- Chip containers were touched by BP2 (semantics) — compose style hunks cleanly on top; remap note: shared files with BP1 (MapScreen) and BP2 already landed below you in the stack.

## 3 · RAILS (adapted to this repo's discovered infrastructure — master §3)
- The report is the design AUTHORITY. Execute to its recorded taste; never invent a decision it already made; never re-litigate a standing decision (DECISIONS §S) or a settled device read (D9/D10).
- Fork discipline: UI/read halves only. Forks 1-13 are Sky's (proximity/geo · points economy · auth-wall/guest · k-anonymity · trust-model scope · product name · stagePoolB · dark saved-place chips · ui/Button · M-12/M-13 swap · bulk mechanism lever · M-37 locked rows · lightbox blacks). A decision half you brush against becomes a DECISIONS §F question, never a build.
- Honesty fences: no invented copy, claims, prices, or numbers. Every NEW user-facing string is PROPOSED (before/after in your evidence report) unless DECISIONS §A already carries Sky's pick; proposed status strings use the em-dash grammar ("state — next step").
- Hard territory (never crossed): `src/components/ui/GlassSurface.tsx` DO-NOT-EDIT (thread the primitive, never fork it) · MapScreen's `pointerEvents="box-none"` overlay law untouched · the blur budget never exceeded (assert at the worst SIMULTANEOUS state, +1 for the tab-bar pane invisible to `__getLiveBlurPaneCount`) · the RM designed-stillness contract + the 220ms delay-gate/pulse-token discipline · RN reality (no CSS-only tricks; expo-blur intensities 12/24 only; virtualization law) · zero Supabase/Storage writes · never merge, push, build, deploy, or send anything external — those are Sky's hands, always.
- Evidence honesty: tag every claim verified / web-approximated / code-inferred / NEEDS-SKY-DEVICE. Web captures come from the audit's static-export rig (`node design-reviews/r2-audit/tools/probe-export.mjs` pattern serves the production bundle on :8082 — the dev server crashes Map/Tasks on the lucide lazy boundary; pre-existing, not yours to fix).
- **PROTECT ACTIVE for this phase:** the drawer-row pressed dim (the benchmark press — spread it) · the FlagCard acknowledgment stack (the layering model) · haptics RM-independence · new-chrome target discipline (44pt everywhere; dims shrink nothing) · the S17 pointer-inert peek (tint on the chip, never the tiles) · the audible filter-acknowledgment loop (untouched) · Fork 8 (pills get the dim only, zero dark restyle) · Fork 9 (Button.tsx untouched) — re-render each touched PROTECT surface BEFORE/AFTER and save both frames to `design-reviews/r2-audit/build-plan/evidence/BP11/`; diff-verify the byte-preserved ones your spec names.

## 4 · GATES (all hard — the phase does not stop until every one passes)
- `npm run typecheck` → 0 errors.
- `npm test` → green; RECORD the count in your evidence report (baseline **1857** at `a8549ff`; expect growth from guards you add; any unrelated red = STOP, do not paper over).
- `npm run lint` → 0 errors, no new warnings.
- The 7 immutable prior stacks files untouched (diff-check): `tools/audit-stacks.json` · `p2-material-stacks.json` · `bench3-material-stacks.json` · `shipped-stacks.json` · `wave1-stacks.json` · `wave2-stacks.json` · `map-stacks.json` (real paths at `a8549ff`: `audit`/`p2-material`/`bench3-material` under `design-reviews/fable-audit/tools/`; `shipped`/`wave1`/`wave2`/`map` under `qa-reports/assets/2026-07-03_tasks_glass/` · `_glass_w1/` · `_glass_w2/` · `2026-07-04_glass_map/` — diff-check by exact filename; only `map-stacks.json` is git-tracked at the base, so for the untracked six the check = never edit them).
- Every commit-plan item CLOSED or explicitly forked with a written reason — nothing silently dropped.
- **Arbiter:** `design-reviews/r2-audit/tools/r2-press-vocab-stacks.json` (sibling beside `r2-material-stacks.json`) — pressed pairs + callout states, both schemes; run through `node ~/AccessMap-material-lab/2026-07-02/shared/contrast-check.mjs`; exit 0 gates.
- **Diff scope = intended files only:** `src/components/ui/PressableScale.tsx`, `src/navigation/RootNavigator.tsx`, `src/screens/MapScreen.tsx`, `src/screens/TasksScreen.tsx`, `src/components/FlagDetailModal.tsx`, `src/components/ui/Sheet.tsx` + the seven named modal files, `src/screens/HomeScreen.tsx`, `src/components/PlatformMap.web.tsx` (+ their test files), plus this phase's own untracked-side artifacts — its named `design-reviews/r2-audit/tools/r2-*-stacks.json` sibling(s) where mandated, its `build-plan/evidence/BP*` output, and the DECISIONS.md append (all inside the untracked `design-reviews/` tree, so they never appear in the tracked diff) — and any guard/test files its commit plan adds or updates. Anything else in the TRACKED diff = a defect; revert it.

## 5 · EVIDENCE + LEDGER DUTIES (before stopping)
- Write `design-reviews/r2-audit/build-plan/evidence/BP11-verification-evidence.md`: what shipped per commit-plan item · per-gate results (with the jest count) · honest tags on every claim · the PROTECT before/after pairs · any PROPOSED strings (before/after table) · anything Sky must eyeball.
- Append to DECISIONS.md — §P: `BP11: base <sha> → tip <sha> · <date> · gates green (jest <count>)` · §D device items contributed: R2-D13 — one felt dialect across drawer row / Map tool / tab / filter chip; tab haptic; RM-on dims still answer. · §L launch items: none. · §F any fork question surfaced · §A a "default applied (BP11)" line for each §S/§O default you exercised.

## 6 · STOP
Built + green + **STOPPED on `r2/bp11-press-vocab`**. Never merge, push, build, or deploy — Sky's hands. One phase per fresh window. **RESUME RULE:** if this phase stalls, re-fire this same file in a fresh window; reconcile the branch's existing commits against the commit plan and continue from the FIRST gap — never redo a landed item, never renumber.
