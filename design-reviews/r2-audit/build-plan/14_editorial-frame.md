# BP14 — The editorial frame holds — type at 200%, true insets, overflow scent

**Fire this file ALONE in a FRESH window.** Model law: **EXECUTE on Opus 4.8 ultracode MAX EFFORT, all sub-agents max effort** (Sky's direction for this train). Authored on Claude Fable 5 max effort, 2026-07-15, in the same window that authored the synthesis — provenance is disclosed, never silent; tag it in your phase report.

**Branch:** `r2/bp14-editorial-frame` — cut from the tip of ``r2/bp13-arrival-waits`` (the prior phase; its tip SHA is in DECISIONS §P — **if that entry is absent, the prior phase has not run: STOP**). Record your base SHA as this phase's rollback anchor.
**Carries (conservation):** T13, T14 — closes T13 (F2-02, F2-05, F2-06, F2-11 as device-register entry, F2-12 touched-files share, F2-13) and T14 (F2-07, F2-08).
**Effort:** S–M + M.
**Purpose:** Magnification stops eating the type (wrap at the floor, unbreakable number-unit), the header family loses its double-inset and stranded circle (cheap-win scope), and every chip rail earns its overflow scent on the shipped S16 contract.

## 0 · READ FIRST (in order — no edit before all of these)
1. `design-reviews/r2-audit/build-plan/DECISIONS.md` — apply every §S standing decision and §O/§A entry named in §1 below; take your base tip from §P.
2. **THE REPORT (design authority):** `/Users/skypie/AccessMap/design-reviews/r2-audit/2026-07-10_AccessMap_R2_Design_Review.md` — your inlined block(s) below are copied from it verbatim (`### #8 · T13 —` + `### #17 · T14 —`); also read §4 (the PROTECT list + fork registry + R2-D ledger) and §5's phase spine.
3. The master: `design-reviews/r2-audit/build-plan/00_master.md` — §6 ordering laws that name this phase, §13 PROTECT spot list.
4. **VERIFY-FIRST:** every `file:line` in the spec was true at plan-authoring (audit HEAD `a8549ff`). Re-grep each cited anchor BEFORE editing; small drift → adapt and note it in your evidence report; meaningful drift (a mechanism moved or is absent) → **STOP** and surface to Sky (evidence report + DECISIONS §P note). Never build against drift.

## 1 · STANDING DECISIONS THAT BIND THIS PHASE (inlined — self-containedness includes decisions)
- **S-3 (BINDING):** the cheap-win inset scope — Settings header-zero + Map HeaderActions box-none regroup (+ optional Map overlay 16→20). NO single-20 family law; PROFILE IS OUT of the edit set. The eyebrow verification target is 'aligned with its own section rows at the container inset', not a forced 20.
- **S-10:** model law.

## 2 · THE WORK — the spec, inlined VERBATIM from the report (the "Skeptic verdict: FIX (absorbed)" line is PART of the spec)
### #8 · T13 — The editorial frame holds at every size and inset · median 7.7
**WHERE:** src/components/ScreenHeader.tsx:40–49/104–113/128 (floor+overflow wrap) + inset law · distance formatter (formatDistance) · src/screens/MapScreen.tsx:1429–1447/:2695–2702 (mapHeaderRow regroup) · src/screens/SettingsScreen.tsx:456–477 (double-inset) · src/screens/ProfileScreen.tsx:2053–2057 (reference convention) · device-gate register entry for TasksScreen.tsx:119–121.
**Resolves:** F2-02 (HIGH), F2-05, F2-06, F2-11, F2-12, F2-13 · **Effort:** S–M · **Tier:** Meaningful · **NEEDS-SKY-DEVICE:** yes
*Merge of the type-stress fixes (F2-02/11/13) and the alignment/inset fixes (F2-05/06/12) — one editorial frame: the family's shared header object, held true at magnification and at rest.*
1. **WHAT** — (magnification) Magnification stops eating the type. (a) ScreenHeader: when M18's auto-fit hits its 0.6 floor AND still overflows, the title wraps to two lines instead of ellipsizing — at-rest behavior byte-identical; "9 barriers" survives 200% as two dignified lines, never "9 b…". (b) formatDistance emits U+00A0 between value and unit so "297 m" can never orphan across lines. (c) F2-11's residual seam — a 376–390pt device at fontScale 1.10–1.14 sits between M16's compact-stack triggers — is recorded as harness-artifact-plus-window and registered on Sky's device gate, exactly per that finding's no-code-change direction; R3's read is not double-counted. (alignment) And the header family aligns to one law: Map's HeaderActions two circles regroup into one right-pinned pair (container gap spacing.xs, pointerEvents box-none) so the space-between row stops stranding the menu circle mid-air; Settings adopts Profile's convention (zero the header's own horizontal padding under the padded container so the eyebrow aligns with its own section rows at the container inset, killing the 44pt double-inset); and per F2-12's own direction, off-scale strays in the touched styles snap to tokens (mapHeaderRow 10, mapHeaderChip 14), the app-wide census remainder staying opportunistic PARK.
2. **WHY** — Resolves F2-02 (HIGH — WCAG 1.4.4 is the hard floor and the web export ships), F2-13, F2-11. THESIS: beauty never costs legibility — the editorial composition loosens one beat at 200% so the screen's anchor datum itself survives; ONE VOICE: the headline keeps speaking at every size instead of truncating to gibberish. The wrap direction extends the PROTECT'd ScreenHeader rhythm exactly as the F2 PROTECT nomination demands — extend, never loosen at-rest. Round 1's own skeptic canon already settled the methodology ("1.4.4's test IS 200% on the viewport you ship"). And F2-05/06/12: ONE HAND — the editorial header is the family's one shared object, and the hero surface dissolves the signature circle pair into what reads as a layout accident while Settings double-insets its eyebrow. This candidate takes the CHEAP, honest win — the Map regroup + the Settings double-inset kill (+ optionally nudging the Map overlay inset 16→20 for local consistency) — and deliberately does NOT declare a single 20 across every screen (that would reflow Profile's + Settings' whole card stacks 24→20, a spend the skeptic flagged; Profile stays out of the edit set). S8's shipped header rhythm holds; alignment is the cheapest luxury in the system.
3. **PERSONA IMPACT** — R3 owns all three finds — "the single most important number on the screen truncates into gibberish," the orphaned unit, and "'Resolved' overflows its own button… exactly the damage I fear," which gets a device-gated answer rather than a shrug. R1 and R6 keep the count that anchors orientation and comprehension. R2: visible text finally matches what SR always spoke. R4: M16's compact-stack defense respected, not re-tuned. R5: hygiene score rises. R7 first-class: wrap behavior is material-independent — RT users get the identical datum-survival guarantee over opaque panels, nothing derived, nothing lesser. On alignment: R5 — the alignment auditor whose "execution hygiene 5" this buys back — finally reads the family as one hand; R7 first-class (geometry-only, renders identically under engineered materials; cold-read cohesion climbs on the surface she sees most); R4 gains a predictable reach zone (circles always the top-right adjacent pair, 44pt held); R3's scan start stops moving between screens.
4. **TRUST + COHESION** — A headline that dies at the first accessibility stop reads as "not tested for me" — the precise opposite of this app's promise. Wrapping at the floor reads as designed-for-me. The unbreakable number-unit pair is the last 2% of the card R3 called "the best survivor in the whole set" — finishing it converts praise into proof. And four insets and a stranded circle read as four authors; one inset and one paired cluster read as one — the subliminal layer where "one hand" is either true or merely claimed, the S8 promise kept on the most-seen surface.
5. **A11Y + RESTRAINT** — Pure floor work: 1.4.4 at 200% restored on the shipping web surface; M18's 0.6 floor unchanged; M16 thresholds unchanged; at-rest rhythm guard-pinned byte-identical. F2-11 proposes no code, so nothing can be traded there. RESTRAINT: PASS per both active findings' inline verdicts — legibility outranks one beat of editorial tightness, and only at the extreme. Alignment leg: 44pt targets unchanged; the new pair container carries pointerEvents box-none so map gestures pass through its gap; type rhythm untouched (insets only); SR order re-verified post-regroup. RESTRAINT: PASS — pure regrouping and alignment; nothing spent.
6. **DEPENDENCIES / CONFLICTS** — Preserves PROTECT ScreenHeader type rhythm (wrap fires only at floor+overflow; eyebrow/display/subtitle micro-margins untouched) and preserves M18 auto-fit + M16 compact-stack contracts (no threshold moves). ScreenHeader.tsx is family-shared — coordinate commit order with the header-inset candidate (different lines, same phase acceptable, serialize commits). No fork adjacency. Does not re-open S18's closed 200% legs — that scope never included Home zoom-2 (per the skeptic rationale). Alignment leg preserves PROTECT box-none overlay law (circles stay discrete Pressables; container box-none) and the ScreenHeader type rhythm (horizontal insets only). ScreenHeader.tsx and MapScreen.tsx are shared across both legs and with T1 — different lines, serialize commits within the phase. Fork 9 untouched (HeaderActions is not ui/Button). Does not re-open S18's closed 200% legs (Home zoom-2 was never in that scope).
7. **VERIFICATION** — Guard jest: starved-width render (M18 floor reached, still overflowing) wraps to two lines with the full string present, zero ellipsis; at-rest snapshot pin proves rhythm unchanged. Re-shoot states/home__light__390__zoom200 (headline whole) and states/nearby__light__390__zoom200 ("297 m" unbroken). No color/ink/floor change → no arbiter sibling required. SKY DEVICE: Home headline at Dynamic Type 1.3 (confirms the code-inferred ~39pt no-truncation math) + the F2-11 window — Tasks action row on a 376–390pt device at fontScale 1.10–1.14, checking "Resolved" stays inside its pill. Alignment: capture diff at rest, both edited screens both themes (Map's menu circle right-pinned adjacent to feedback within gap tolerance of Home's; Settings eyebrow now at the container inset — i.e. aligned with its own section rows, double-inset gone — not a forced 20); layout guards (Settings header's OWN horizontal padding zeroed under the padded container; pair container present with gap spacing.xs + box-none); a11y-tree re-walk of Map top cluster and Settings header. The formatDistance U+00A0 change updates distance.test.ts's space-literal equalities and injects NBSP into the visible label only — confirm speakDistance (the SR path) stays byte-identical so VoiceOver distance wording is unaffected. No ink change → NO arbiter; blur budget untouched.


**Skeptic verdict: FIX (absorbed).** resolve the inset-law contradiction — scope to the CHEAP win (Settings header-zero + Map HeaderActions box-none regroup + optional Map overlay 16→20), DROP the 'one 20 inset across every screen' claim AND drop Profile; OR, if Sky wants the true single inset, state the full Profile+Settings container reflow (24→20 moves every card) and re-rate RESTRAINT + add device coverage; fix the self-contradictory verification (24 vs 20 — pick one); the formatDistance U+00A0 breaks distance.test.ts + injects NBSP into the a11y label — update tests, confirm speakDistance stays byte-identical.

### #17 · T14 — Every chip rail earns its overflow scent · median 6.7
**WHERE:** src/screens/MapScreen.tsx:1795/:1860 (+S16 fade :383–416/:1614–1628 efficacy) · src/components/NearbyFlagsModal.tsx:242 · src/screens/TasksScreen.tsx:980 · src/screens/ReportFlagModal.tsx:583/626
**Resolves:** F2-07,F2-08 · **Effort:** M · **Tier:** Meaningful · **NEEDS-SKY-DEVICE:** no
1. **WHAT** — The one shipped overflow affordance becomes the house dialect. Extend S16's fade edge — pointer-inert, a11y-hidden, static gradient over the clipped edge — to all five silent chip scrollers: filter-panel categories, filter-panel saved sets, Nearby category chips, Tasks category strip, Report sheet category chips; same measurement contract (onLayout vs onContentSizeChange), reused not forked. Then the F2-08 efficacy leg: live-probe the shipped action-bar fade on a truly overflowing rail — if measurement fires but the cue doesn't read, raise the light-theme gradient from rgba(15,27,45,0.22) until it registers over Positron tiles; if measurement fails, fix the measurement. One interaction, one dialect, everywhere chips overflow.
2. **WHY** — Resolves F2-07 + F2-08 (both MEDIUM, one mechanism). Both findings' own framing: an EXTENSION of S16 and its regression/efficacy check at HEAD — nothing closed is re-opened. ONE HAND / ONE MATERIAL: the same gesture (horizontal chip overflow) currently speaks two dialects, and the silent one clips mid-glyph like an accident ("Blocked path 1", "Steep g…", "M…"). Mission-level stake: a category chip the user can't see is a filter they don't know they have — this is comprehension of one's own controls, not chrome.
3. **PERSONA IMPACT** — R4 named the clip ("chip rows get clipped at the edge"); R7 named the missing hint ("clips the third category chip at the screen edge with no hint there's more") — and RT gains doubly: the fade is verified against her engineered-opaque C-lite panel fills, not just glass, so the scent works first-class under Reduce Transparency. R3 gets the affordance S16 promised her toolbar — the efficacy leg makes it actually fire at the moment she loses the crosshair. R6 discovers filters she'd "never guess" existed. R2 unaffected by design (fade a11y-hidden; scrollers announce as before). R1/R5 read one system where five accidents stood.
4. **TRUST + COHESION** — Five rails that hard-clip with zero scent read as five accidents by five authors; one fade grammar reads as one designed material behavior. The S16 investment finally pays across the whole app instead of on a single rail — and the efficacy check converts a shipped promise into a verified one, which is the difference between claiming craft and proving it.
5. **A11Y + RESTRAINT** — Fades are decorative, pointer-inert, a11y-hidden per the shipped S16 pattern — zero target, legibility, or SR cost. No fully-visible chip glyph may sit under the fade footprint (arbiter-checked). RM contract untouched: static gradients are not motion; no mounts, no springs. RESTRAINT: PASS per F2-07's verdict — and if any strengthened gradient ever muddies chip text, that strength dies, never the floor.
6. **DEPENDENCIES / CONFLICTS** — Preserves PROTECT box-none overlay law (map rails stay inside the existing overlay structure; fades pointer-inert) and S16's shipped measurement contract (reuse the mechanism, never fork it). Preserves S17's pointer-inert discipline. Shares MapScreen.tsx with the callout and header candidates and NearbyFlagsModal/TasksScreen/ReportFlagModal with the grammar suite's meta-text sub-items — different lines; serialize commits within the phase. No fork adjacency. GlassSurface untouched (gradients, not material panes).
7. **VERIFICATION** — Named arbiter sibling tools/r2-chip-fade-stacks.json — strengthened light gradient vs Positron tile luminance, fade over engineered-opaque C-lite panel fills, and the no-fully-visible-glyph-under-fade assertion, both themes. Per-rail guard tests: fade mounts iff contentWidth > layoutWidth. Forced-overflow captures per rail, both themes, 390 + 375. Live preview probe answers F2-08's two-hypothesis question (measurement vs gradient strength) before code moves. Blur budget untouched — gradients, not BlurViews; worst-state pane count unchanged including the tab-bar pane invisible to __getLiveBlurPaneCount.


**Skeptic verdict: FIX (absorbed).** WHERE path is src/screens/NearbyFlagsModal.tsx:242 (no components/ copy); match the SHIPPED S16 measurement contract `overflow>1 && !atEnd` wiring onScroll (scrollEventThrottle=16) to HIDE the fade at end-of-scroll — not merely onLayout/onContentSizeChange; label :583 as the templates rail (6 scrollers across 5 surfaces).

### Commit plan (one commit per item, this order)
1. T13/wrap: ScreenHeader — when M18's auto-fit hits its 0.6 floor AND still overflows, wrap to two lines instead of ellipsizing; at-rest rhythm byte-identical (snapshot pin).
2. T13/unit: `formatDistance` emits U+00A0 between value and unit; UPDATE distance.test.ts's space-literal equalities; prove `speakDistance` (the SR path) stays byte-identical (the NBSP lives in visible text only).
3. T13/insets (S-3 scope): Map HeaderActions regroup into one right-pinned pair (container gap spacing.xs, pointerEvents box-none) + Settings header's own horizontal padding zeroed under the padded container; touched-file token snaps (mapHeaderRow 10, mapHeaderChip 14); F2-11 = NO CODE — register the fontScale 1.10–1.14 / 376–390pt window on the device list.
4. T14/scent: the shipped S16 measurement contract (`overflow > 1 && !atEnd`, onScroll wired at scrollEventThrottle=16, fade HIDES at end-of-scroll) extended to the 6 silent scrollers across 5 surfaces — filter-panel categories + saved sets (MapScreen :1860/:1794), Report templates :583 + categories :626, Tasks cat chips, Nearby tabs (src/screens/NearbyFlagsModal.tsx:242 — the CORRECT path; add its expo-linear-gradient import) — using the shipped `rgba(15,27,45,0.22)` fade recipe.
5. Guards + captures: starved-width wrap test (two lines, full string, zero ellipsis) + at-rest snapshot; re-shoot home zoom200 + nearby zoom200; capture-diff the two edited header screens both themes; fade-contract guards (mounts iff overflow, hides at end); a11y re-walks.

## 3 · RAILS (adapted to this repo's discovered infrastructure — master §3)
- The report is the design AUTHORITY. Execute to its recorded taste; never invent a decision it already made; never re-litigate a standing decision (DECISIONS §S) or a settled device read (D9/D10).
- Fork discipline: UI/read halves only. Forks 1-13 are Sky's (proximity/geo · points economy · auth-wall/guest · k-anonymity · trust-model scope · product name · stagePoolB · dark saved-place chips · ui/Button · M-12/M-13 swap · bulk mechanism lever · M-37 locked rows · lightbox blacks). A decision half you brush against becomes a DECISIONS §F question, never a build.
- Honesty fences: no invented copy, claims, prices, or numbers. Every NEW user-facing string is PROPOSED (before/after in your evidence report) unless DECISIONS §A already carries Sky's pick; proposed status strings use the em-dash grammar ("state — next step").
- Hard territory (never crossed): `src/components/ui/GlassSurface.tsx` DO-NOT-EDIT (thread the primitive, never fork it) · MapScreen's `pointerEvents="box-none"` overlay law untouched · the blur budget never exceeded (assert at the worst SIMULTANEOUS state, +1 for the tab-bar pane invisible to `__getLiveBlurPaneCount`) · the RM designed-stillness contract + the 220ms delay-gate/pulse-token discipline · RN reality (no CSS-only tricks; expo-blur intensities 12/24 only; virtualization law) · zero Supabase/Storage writes · never merge, push, build, deploy, or send anything external — those are Sky's hands, always.
- Evidence honesty: tag every claim verified / web-approximated / code-inferred / NEEDS-SKY-DEVICE. Web captures come from the audit's static-export rig (`node design-reviews/r2-audit/tools/probe-export.mjs` pattern serves the production bundle on :8082 — the dev server crashes Map/Tasks on the lucide lazy boundary; pre-existing, not yours to fix).
- **PROTECT ACTIVE for this phase:** the ScreenHeader type rhythm + M18 auto-fit floor + M16 compact-stack contracts (no threshold moves; wrap fires only at floor+overflow) · the box-none law (the pair container carries it) · Fork 9 (HeaderActions is not ui/Button) · S18's closed 200% legs stay closed (Home zoom-2 was never in that scope) — re-render each touched PROTECT surface BEFORE/AFTER and save both frames to `design-reviews/r2-audit/build-plan/evidence/BP14/`; diff-verify the byte-preserved ones your spec names.

## 4 · GATES (all hard — the phase does not stop until every one passes)
- `npm run typecheck` → 0 errors.
- `npm test` → green; RECORD the count in your evidence report (baseline **1857** at `a8549ff`; expect growth from guards you add; any unrelated red = STOP, do not paper over).
- `npm run lint` → 0 errors, no new warnings.
- The 7 immutable prior stacks files untouched (diff-check): `tools/audit-stacks.json` · `p2-material-stacks.json` · `bench3-material-stacks.json` · `shipped-stacks.json` · `wave1-stacks.json` · `wave2-stacks.json` · `map-stacks.json` (real paths at `a8549ff`: `audit`/`p2-material`/`bench3-material` under `design-reviews/fable-audit/tools/`; `shipped`/`wave1`/`wave2`/`map` under `qa-reports/assets/2026-07-03_tasks_glass/` · `_glass_w1/` · `_glass_w2/` · `2026-07-04_glass_map/` — diff-check by exact filename; only `map-stacks.json` is git-tracked at the base, so for the untracked six the check = never edit them).
- Every commit-plan item CLOSED or explicitly forked with a written reason — nothing silently dropped.
- **Arbiter:** conditional on the F2-08 answer. If the shipped S16 `rgba(15,27,45,0.22)` survives unchanged (no NEW ink pair): no arbiter RUN is required, but the spec's unconditional assertions still close via the forced-overflow captures — no fully-visible chip glyph under any fade footprint, and the fade verified over the engineered-opaque C-lite fills, both themes. If the efficacy leg STRENGTHENS the gradient (any new ink value): declare + run `design-reviews/r2-audit/tools/r2-chip-fade-stacks.json` (runner `node ~/AccessMap-material-lab/2026-07-02/shared/contrast-check.mjs`) to exit 0 carrying all three spec checks (strengthened gradient vs Positron tiles · fade over C-lite fills · the no-fully-visible-glyph assertion). T13 changes no ink — no arbiter for it.
- **Diff scope = intended files only:** `src/components/ui/ScreenHeader.tsx` (the verified disk path at `a8549ff`; the spec's WHERE says `src/components/ScreenHeader.tsx` — report-verbatim text, not the disk path), `src/lib/distance.ts` (`formatDistance` :53 / `speakDistance` :76; test `src/lib/__tests__/distance.test.ts`), `src/screens/MapScreen.tsx`, `src/screens/SettingsScreen.tsx`, `src/screens/ReportFlagModal.tsx`, `src/screens/TasksScreen.tsx`, `src/screens/NearbyFlagsModal.tsx` (+ their test files), plus this phase's own untracked-side artifacts — its named `design-reviews/r2-audit/tools/r2-*-stacks.json` sibling(s) where mandated, its `build-plan/evidence/BP*` output, and the DECISIONS.md append (all inside the untracked `design-reviews/` tree, so they never appear in the tracked diff) — and any guard/test files its commit plan adds or updates. Anything else in the TRACKED diff = a defect; revert it.

## 5 · EVIDENCE + LEDGER DUTIES (before stopping)
- Write `design-reviews/r2-audit/build-plan/evidence/BP14-verification-evidence.md`: what shipped per commit-plan item · per-gate results (with the jest count) · honest tags on every claim · the PROTECT before/after pairs · any PROPOSED strings (before/after table) · anything Sky must eyeball.
- Append to DECISIONS.md — §P: `BP14: base <sha> → tip <sha> · <date> · gates green (jest <count>)` · §D device items contributed: the F2-11 window (Tasks action row, 376–390pt @ fontScale 1.10–1.14 — 'Resolved' stays inside its pill) + Home headline at real DT 1.3 (R2-D7 lineage) + T14's overflow-scent felt check (R2-D11 lineage). · §L launch items: none. · §F any fork question surfaced · §A a "default applied (BP14)" line for each §S/§O default you exercised.

## 6 · STOP
Built + green + **STOPPED on `r2/bp14-editorial-frame`**. Never merge, push, build, or deploy — Sky's hands. One phase per fresh window. **RESUME RULE:** if this phase stalls, re-fire this same file in a fresh window; reconcile the branch's existing commits against the commit plan and continue from the FIRST gap — never redo a landed item, never renumber.
