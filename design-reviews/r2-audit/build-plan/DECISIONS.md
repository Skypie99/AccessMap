# R2 BUILD — DECISIONS LEDGER (cross-window state; every phase READS this FIRST and APPENDS its results)

Rules: append-only per section; newest entry wins on conflict; a pick made in any window lives HERE, never in chat history. Phases copy their relevant subset into their own report for the record. Sky is the only author of decision ANSWERS; executors record questions, defaults-applied, and results.

## §S STANDING DECISIONS (seeded from the report + audit record, 2026-07-15 — already made; never re-litigate)

- **S-1 · Device reads closed:** D9 (the ≥500 on-glass weight reads fine — Sky's direct device read 2026-07-09; the report §4 is the paper record) and D10 (the B6 light bulk sheet reads clean — BENCH-3 §B6). Do not re-open.
- **S-2 · T6 tier:** down-tiered Signature → Meaningful (Panel B demotion + skeptic concur). The ★ set is T1 · T2 · T3 · T5.
- **S-3 · T13 scope:** resolved to the CHEAP WIN — Settings header-zero + Map HeaderActions box-none regroup (+ optional Map overlay 16→20). **No single-20 family-inset law; Profile is OUT of the edit set.** (Cold-read fix, applied to the report + slate.)
- **S-4 · M-48:** the glass-mechanism adoption is KILLED (trial record, 4/4 AA failures on the true chrome floor) — NO-ACTION forever unless Sky re-opens; its shipped light-inactive INK drift (3.17/4.04) is T15's scope only.
- **S-5 · Ratified tiers stand** (04 §4): the four-dialog tier, the drawer, the media-world lightboxes, the map legacy always-light pair (M-12/M-13), the semantic/system solids, M-57 boot chrome. Craft-deltas inside ratified tiers are T20's scope; ratification itself is settled.
- **S-6 · T6 vehicle path DEFAULT = minimal arbitration/stacking** (offset the second vehicle below the first; text-carrier pill goes pointerEvents box-none). Full migration of TasksScreen's flash onto the region channel is Sky-OPTIONAL (Fork-2-adjacent strings never move either way). Sky may override here.
- **S-7 · T16 badge meaning DEFAULT = open-only** (writer 1's definition, matching the visible OPEN header), and the badge stays GLOBAL/unfiltered — never filter-aware. Sky may override here.
- **S-8 · BP16 copy discipline:** every string is PROPOSED via the before/after table; NOTHING ships until the pick lands in §A below. The k≥3 caveat rewording additionally requires a **Jordan Art. 7 semantic-equivalence sign-off** (it IS the privacy disclaimer).
- **S-9 · The base designation default:** the audited tip `a8549ff` (`bench/4-quality`). If Sky merges bench→main before firing BP1, the base is main (BP1 verifies the three markers there). Recorded properly in §P by BP1.
- **S-10 · Model law for this train:** authored Fable 5 max (2026-07-15); execution Opus 4.8 ultracode max effort, all sub-agents max — per Sky's direction on this train. Provenance disclosed in every phase report.
- **S-11 · MP train law (04 §7, restated):** linear MP0→MP5, each branch from the prior MP tip, built+green+STOPPED per phase, zero `theme.ts` edits on MP branches, `GlassSurface.tsx` never touched, the 7 immutable prior stacks files diff-checked, RT + C-lite implemented as specced (never deferred), Sky merges, Sky owns D-gates.

## §O OPEN — SKY'S CALLS (executors check before the named phase; unanswered where marked → STOP)

- **O-1 · MP5 include/skip** (§6.7) — checked by BP9 BEFORE any edit; **unanswered → BP9 stops having changed nothing** and BP10 cuts from the MP4 tip; a skip is recorded by BP17's paper leg as the B14 deliberate-exception line. → *Sky writes: INCLUDE / SKIP.*
- **O-2 · MP4 reopen-button re-ink ⚑** — 04's block re-inks the FlagDetail reopen action accentOrange→`brandText` (fixes a live 2.07 AA fail; arbiter-proven 4.95/5.42). DEFAULT = proceed per spec. → *Sky may write: VETO (keep orange; the AA fail then routes to a fork note).* Checked by BP8 before that commit.
- **O-3 · GLASS.md doc-refresh ownership** (§6.5) — the T20 paper leg SPECS the edits; whether the executor lands them or Sky's hand does is Sky's one-liner. DEFAULT = executor lands them as the caboose commit. → *Sky may write: MY HAND.*
- **O-4 · T20 dialog-tier: UNIFY (fade + e3 four-of-four — the block default) vs RECORD-ONLY** (ship shadow, note the entrance split as deliberate). Checked by BP17. → *Sky writes: UNIFY / RECORD.*
- **O-5 · BP16 string picks** — the phase STOPS after producing the table; Sky's per-string picks (incl. F4-20 Settings retitle pair, the colour/color dialect, casing rows) land in §A; the resume applies exactly those.

## §F FORKS (boundaries — never built by any phase; questions accrue here for Sky)

Inherited Forks 1–9 (Round-1 report §5) + new Forks 10–13 (R2 report §4): 10 M-12/M-13 keep-vs-engineered-swap · 11 bulk-tier mechanism lever (B4 flag #2) · 12 M-37 locked-row idiom (MP3 ships the accept-and-record DEFAULT per its block; re-spec is the fork half) · 13 lightbox blacks tokenize-vs-record. Phases may ADD fork-question entries here; no phase answers one.

## §P PHASE TIPS + HANDSHAKE RECORD (append per phase: base SHA → tip SHA, date, gate results one-liner)

- *BP1 appends the version-handshake result here first (base designation + marker checks + jest/typecheck/lint at base).*
- **BP1 HANDSHAKE: 2026-07-16 · base = `a8549ff` (`bench/4-quality` tip, S-9 default applied — bench NOT merged; main `01f7392`) · branch `r2/bp1-callout-true` cut · all 3 markers verified (flags.ts:52 PHOTO_MAX_DIMENSION + 11 test refs · LiveStatusRegion.tsx present · NearbyFlagsModal.tsx:202 variant="bulk") · gates at base: typecheck 0 · lint 0 err/77 warn (baseline) · jest run 1 = 2 flaky fails (MyReportsModal initialStatus describe — async waitFor timing under parallel load; passes in isolation), run 2 = 1857/0 CLEAN = exact baseline → base confirmed green, flake recorded honestly (not drift, not touched) · pre-existing working-tree dirt noted: ` D .claude/launch.json` (tracked deletion, predates BP1, never staged by this train) · executed on Opus 4.8 ultracode max (S-10; authored Fable 5 max 2026-07-15).**
- **BP1: base `a8549ff` → tip `3d13c8b` · 2026-07-16 · gates green (typecheck 0 · lint 0/77 · jest 1913/0; baseline 1857 + 56 guards). T1 CLOSED — F2-01 (CRITICAL) + F3-04/05/06. 6 commits (handshake + web occlusion + native bias + rhythm + guards/evidence + adversarial-verify hardening). Diff scope = 8 files exactly (3 src + 5 tests). PROTECT byte-proofs held (box-none law + SR nested-detail block = 0 diff lines). Occlusion close-out web-verified zeroOverlap TRUE both themes + RM (evidence/BP01/). Adversarial verify: web-leaflet-internals lens 0 findings; 2 LOW fixed, 1 LOW (native cold-start+large-type) documented → R2-D12. NOT merged/pushed/built — STOPPED on branch (Sky's hands). ✅ BP2 may now re-fire.**
- BP2: fired 2026-07-16 — STOPPED at §0 entry gate (no BP1 entry in §P; no r2/bp1-callout-true branch; repo at bench/4-quality a8549ff). Zero edits. Re-fire 02_perception-floor.md after BP1 lands its §P line.
- BP3: fired 2026-07-16 — STOPPED at §0 entry gate (no BP2 entry in §P; no r2/* branches at all — BP1 also unrun; repo at bench/4-quality a8549ff). Zero edits. Re-fire 03_trust-engine-hand-and-voice.md after BP2 lands its §P line.
- **BP2: base `3d13c8b` → tip `373c582` · 2026-07-17 · gates green (typecheck 0 · lint 0/77 no-new-warn · jest 1929/0 = baseline 1913 + 16 guards · arbiter `r2-focus-net-stacks.json` exit 0). T11 CLOSED (F1-03 SR-state + F1-04 focus) + T16 CLOSED (F3-03 badge). 4 commits: SR-state a11yToggle `pressed` + migrate 9 chips · focus/inert · single badge writer · guards+evidence. Diff = 5 tracked src/test files + new `perceptionHelpers.ts` (testability extraction — RootNavigator can't import into jest: react-native-map-clustering; noted §A/evidence) + untracked design-reviews artifacts. CDP AX probe (`tools/probe-bp2-perception.mjs`): aria-selected DROPPED / aria-pressed KEPT on role=button proven first-hand; 5 chips now expose `pressed`. Inert: leak 0 (13 occluded Home focusables inert-protected — react-native-screens does NOT display:none tab siblings here, so aria-hidden alone left the leak; probe corrected the static inference). Ring: 0 defect-clips on the named filter/report strips both screens (arbiter light 3.70/dark 3.24 — ring stays OUTSIDE because active chips are brand-blue-filled). First parallel run showed 2 flaky fails = the known MyReportsModal waitFor flake (0 on clean re-run + isolation). PROTECT held (announce loop + shim untouched; Fork 8/9; S17 peek inert = no visible change). NOT merged/pushed/built — STOPPED on `r2/bp2-perception-floor` (Sky's hands). ✅ BP3 may now re-fire (its §0 gate now passes).**
- **BP3: base `373c582` → tip `db61189` · 2026-07-17 · gates green (typecheck 0 · lint 0/77 no-new-warn · jest 1944/0 = baseline 1929 + 15 guards · arbiter `r2-report-ack-stacks.json` exit 0, ALL pairs AA both schemes: light neutral 9.78/brand 5.42/submit 5.09, dark 8.37/5.07/7.17/5.09). T4 CLOSED (F1-01 pressed dims on all 10 Report Pressables via the house `borderPressed` FILL-swap — never group opacity; gated `!active` so an active brand fill is never dimmed under white text; Submit = gradient scrim. F1-08 selection tick moved to `onPressIn` on the category+severity pickers + `hapticImpact('medium')`+`hapticNotify('success')` at the shared `applyStatusChange` commit point, `hapticNotify('error')` in the `setStatus` catch, 3 commit actions `haptic='none'`/Details `'selection'`). T8 CLOSED (F4-02 FlagCard baseLabel+selection via `severityA11y`/`statusA11y`; F4-08 each action names its flag, distance null-guarded on `distanceInfo`; F4-10 Home status-with-distance; F4-01 badge-doubling leg via `decorativeProps` on the Legend sevDot — the only sibling with a text glyph; F4-17 photo nudge interpolates `SEVERITY_LABELS`, retiring 'major'). 4 commits (report-sheet · triage haptics · spoken recompose · guards+arbiter); commit-plan item 4 = the PROPOSED strings table, ships nothing (evidence §4). Diff = 6 tracked files (4 src + 2 tests); haptics.ts READ-only. Web: all 4 touched screens RENDER on the static export (`probe-export.mjs`, no regression; `findNodeHandle` pageerror pre-existing, not in the diff). Adversarial verify (4 skeptics, Opus 4.8 max effort): **4/4 UPHELD, 0 refutations, high confidence** — 1 out-of-scope nit → §PARKING-LOT. PROTECT held (haptics.ts/GlassSurface/NearbyFlagsModal not in diff; confirm-gate-on-Reject + PROTECT-18 announce + Submit `disabled` guard byte-intact; PROTECT-3 sheet architecture = dims-only). Provenance: authored Fable 5 max (2026-07-15), executed Opus 4.8 ultracode max (S-10). NOT merged/pushed/built — STOPPED on `r2/bp3-trust-hand` (Sky's hands). ✅ BP4 (MP0) may cut from this tip per ordering law L4 (BP3's text baseline now settled).**
- **BP4 (MP0): base `db61189` → tip `c4d484f` · 2026-07-17 · gates green (typecheck 0 · lint 0/77 no-new-warn · jest 1944/0 = baseline unchanged, no guards needed · arbiter `r2-material-stacks.json` verbatim EXIT 0, RESULT ALL PASS, 81 verdicts — MP0 stacks: M-46 textOnBrand 7.86/10.41/15.08, M-51 white-on-0.25-black chip 7.31 successStrong / 7.97 ctaFill). T2 phase 1 of 6.** M-56 CLOSED (boot frame `#fff`→`surfaceMuted` via `useColor()` in FirstLaunchGate — B8 dark strobe). M-55 CLOSED (map empty-card → `GlassSurface variant=row forceEngineered` + `glassMapWash`; body→`color.text`, icon→`inkGlassMuted`; solid fill/border dropped — B10). M-52 CLOSED (UpdateBanner → `variant=banner forceEngineered`; text/dismiss→`brandOnSoft`, View button `brand`→`ctaFill` killing the 3.42 dark drift — B11; `View` import dropped, GlassSurface added). In-tier ink repairs: M-46 (3 lightbox `color.surface`→`textOnBrand`, ratified row stays), M-51 (`pillInfo`→`ctaFill`, Retry chip `rgba(255,255,255,0.22)`→`rgba(0,0,0,0.25)`). M-43 comment hygiene ×2 (Profile stale AboutScreen ref, Settings stale dark-chrome note). **5 source commits, one per item.** Diff = 7 tracked src files (+ the pre-existing `.claude/launch.json` deletion, NOT staged); zero `theme.ts` edits; `GlassSurface.tsx` untouched; 7 immutable stacks files untouched; no sibling `r2-mp0-stacks.json` (shipped values == spec declarations). Blur budget: ZERO new live panes (`forceEngineered` → engineered material, no BlurView). PROTECT held (M-55 alert role + label byte-identical, box-none law 0 diff lines; M-52 announce-once + hint byte-identical; PROTECT-27 `useColor` NOT made throwing). Provenance: authored Fable 5 max (2026-07-15), executed Opus 4.8 ultracode max (S-10, Sky's in-chat direction). NOT merged/pushed/built — STOPPED on `r2/mp0-first-frame` (Sky's hands). ✅ BP5 (MP1) may cut from this tip.**
- **BP5 (MP1): base `c4d484f` → tip `31086fd` · 2026-07-17 · gates green (typecheck 0 · lint 0/77 no-new-warn, HomeScreen clean · jest 1944/0 = baseline unchanged, no guards needed · arbiter `r2-material-stacks.json` verbatim EXIT 0, RESULT ALL PASS — all 20 M-06 Home verdicts PASS across light/dark × blur/C-lite/RT, worst 5.44 light search placeholder `glassPlaceholder`). T2 phase 2 of 6.** M-06 CLOSED — HomeScreen joins the stage: root `surfaceMuted`→`stage1` + `<ScreenStage/>` first child; search pill legacy `intensity={20}`→`variant=row forceEngineered` (engineered-literal, retires the last legacy pane outside the map — B2); list card opaque `surface` View → ONE `variant=row forceEngineered={glassLite}` GlassSurface across all 4 state branches (inner `sep`/`SkeletonRow` kept); `styles.search` + `styles.listCard` border/fill deleted (row variant paints its own; `overflow:hidden`+radius kept); ScreenHeader `eyebrowColor`/`subtitleColor`→`inkOnStage` + sectionLabel `textSubtle`→`inkOnStage`; `rowMeta`+`searchText` gain ≥500 on-glass weight (`font.family.bodyMedium`); `rowMeta`/chevron/search icon/`errorText`/`emptyText`→`inkGlassMuted`, search placeholder→`glassPlaceholder`, search-active stays `textStrong`. **1 source commit.** Diff = 1 tracked file (`HomeScreen.tsx`, 24/23); zero `theme.ts`; `GlassSurface.tsx`/`ScreenStage.tsx` untouched; 7 immutable stacks untouched; no sibling `r2-mp1-stacks.json` (shipped hexes == declarations). Blur budget net-zero 2→2 (pill legacy-blur→engineered = 0; list card 0→blur-when-full = 1; `useBlurPaneBudget` gated on `material==='blur'`, pill unconditionally engineered so pill+card never blur simultaneously). PROTECT held (firsthand diff = 0 lines): PROTECT-10/S17 peek `pointerEvents`/`suppressAttribution`, row `away` a11y label (locked bp3TrustEngineGuards:102), B9b banner, NEARBY/LATEST honesty. Adversarial verify (4 skeptics, Opus 4.8 max: correctness/PROTECT · diff-scope · ink+budget · independent gate re-run) → **4/4 UPHELD, 0 refutations**. D1 (clear-X→inkGlassMuted, GLASS §7.4) + D2 (no hydrateGlassMode — read-only) applied as defaults, logged §A. Provenance: authored Fable 5 max (2026-07-15), executed Opus 4.8 ultracode max (S-10). NOT merged/pushed/built — STOPPED on `r2/mp1-home-stage` (Sky's hands). ✅ BP6 (MP2) may cut from this tip.**
- **BP6 (MP2): base `31086fd` → tip `828f736` · 2026-07-17 · gates green (typecheck 0 · lint 0/77 no-new-warn · jest 1944/0 = baseline unchanged, no guards · arbiter `r2-material-stacks.json` verbatim EXIT 0, ALL PASS, 80 pairs — profileSheet MP2 inks 11.24/8.93/6.24/4.95 light, 11.14/8.94/6.51/5.42 dark). T2 phase 3 of 6.** M-39/M-40/M-38 CLOSED — the three Profile list sheets join the shared B4-engineered bulk recipe: container → `cardWrap` (up-shadow) + `<GlassSurface variant="bulk" borderRadius={0} forceEngineered>`; card/sheet drops `backgroundColor` + gains `overflow:hidden`. On-glass re-inks by ROLE: muted/meta/counters/dates (`textMuted`/`textMutedAlt`/`textSubtle`-class) → `inkGlassMuted`, on-glass body +≥500 (`font.family.bodyMedium`); titles stay `textStrong`, body stays `color.text`; opaque inner row cards (M-39/M-38) + semantic pills keep their tokens. **M-40 error repair:** MyWatched's full-screen load-error was BARE `color.error` on the now-glass sheet → adopts the sibling `errorBanner` (self-contained `errorBg` pin, no new arbiter pair). **3 source commits** (`dc581d0` M-39 · `19cc058` M-40 · `828f736` M-38). Diff = 3 tracked files (107+/22-); zero `theme.ts`; `GlassSurface.tsx` untouched; 7 immutable stacks untouched; no sibling `r2-mp2-stacks.json` (shipped hexes == declarations). Blur budget **+0** (`forceEngineered` = engineered, no BlurView). PROTECT held (firsthand diff): the **MyWatched `accessibilityViewIsModal` gap CARRIED byte-identically** (S-11 L7 — only a comment names it, no prop added); all a11yLabels/hints/roles/testIDs + the `a11yToggle` chips byte-identical; opaque inner cards + semantic banners kept. Defaults (revertible, §A): the cardWrap up-shadow (the sheets had none; matches the 4 shipped bulk siblings) + `forceEngineered` (engineered-literal per §2a/§2f). Provenance: authored Fable 5 max (2026-07-15), executed Opus 4.8 ultracode max (S-10). NOT merged/pushed/built — STOPPED on `r2/mp2-profile-lists` (Sky's hands). ✅ BP7 (MP3) may cut from this tip.**
- **BP7 (MP3): base `828f736` → tip `db59980` · 2026-07-17 · gates green (typecheck 0 · lint 0/77 no-new-warn · jest 1944/0 = baseline unchanged, no guards · arbiter `r2-material-stacks.json` verbatim EXIT 0, ALL PASS, 80 pairs). T2 phase 4 of 6.** M-37/M-22/M-42/M-24 CLOSED — the remainder of the opaque-overlay tier joins the shared B4-engineered bulk recipe: container → `cardWrap` (up-shadow) + `<GlassSurface variant="bulk" borderRadius={0} forceEngineered>`; card drops `backgroundColor` (M-37 also drops the `surfaceMuted` wash — one material) + gains `overflow:hidden`. On-glass re-inks by ROLE: muted/meta/hints (`textMuted`/`textMutedAlt`/`textSubtle`-class) → `inkGlassMuted`, on-glass body +≥500 (`font.family.bodyMedium`, 6 rules; xs-meta stateHint/footers ink-only per the `sectionHeaderCount` precedent); titles stay `textStrong`, body stays `color.text`; opaque inner rows/pills + semantic notices keep tokens. **M-22 drift re-inks** (M-52 class, file in scope): `segBtnActive`+`retryBtn` fills `brand`→`ctaFill` (kills the recorded 3.42 dark FAIL); `rankTop` `brand`→`brandText` (was ≈4.45 on dark podiumGold). **M-42 spinner:** `ActivityIndicator`→`color.text` (was brand 3.3:1). **Fork-12 default:** M-37 locked rows ship accept-and-record (`rowDimmed {opacity:0.7}` kept; ≈3.0:1 locked rowDesc recorded, SR-mitigated). **goldChip/rankTop drift resolved verify-first** (goldChip = arbiter surface name, earned-icon `goldDark`/`achievementEarnedBg` unchanged; rankTop = local style key, not a token). **4 source commits** (`6322b98` M-37 · `6727722` M-22 · `32d671c` M-42 · `db59980` M-24). Diff = 4 tracked files (126+/25-); zero `theme.ts`; `GlassSurface.tsx` untouched; 7 immutable stacks untouched; no sibling `r2-mp3-stacks.json` (shipped hexes == declarations). Blur budget **+0** (`forceEngineered` = engineered, no BlurView). PROTECT held (firsthand diff): shared sign-in string, all a11y/testIDs/`accessibilityViewIsModal`, toggle semantics, podium order+tier washes, `removeClippedSubviews`/`initialNumToRender`, semantic notices — byte-identical; NotificationPreferencesScreen render test 15/15. **4-skeptic adversarial verify 4/4 confirm** (byte-identity skeptic's lone flag = the recipe-mandated `bodyMedium` bump, resolved as wording — spec §3 + MP2 precedent). Provenance: authored Fable 5 max (2026-07-15), executed Opus 4.8 ultracode max (S-10). NOT merged/pushed/built — STOPPED on `r2/mp3-overlay-rest` (Sky's hands). ✅ BP8 (MP4) may cut from this tip.**
- **BP8 (MP4): base `db59980` → tip `205108c` · 2026-07-17 · gates green (typecheck 0 · lint 0/77 no-new-warn · jest 1944/0 = baseline unchanged, no guards · arbiter `r2-material-stacks.json` verbatim EXIT 0, ALL PASS, 80 pairs — detailSheet inks 11.24/8.93/6.24/4.95 light, 11.14/8.94/6.51/5.42 dark, reopen re-ink 4.95/5.42, coordsCopyGlyph pin 3.70/3.56). T2 phase 5 of 6 (hardest last).** M-36/M-41 CLOSED — the trust ledger (FlagDetail + StatusHistory) joins the shared B4-engineered bulk recipe: container → `<GlassSurface variant="bulk" borderRadius={0} forceEngineered>` (**NO cardWrap** — these two cards had no up-shadow at HEAD, nothing to relocate; the one place MP4 diverges from BP6/BP7); `accessibilityViewIsModal` moved onto GlassSurface (forwarded via `...rest` in all 3 material branches); card drops `backgroundColor` + gains `overflow:hidden`. On-glass re-inks by ROLE: muted/meta/captions/counters/hints (`textMuted`/`textSubtle`-class) → `inkGlassMuted` (10 sites in M-36 incl. the empty-state MessageCircle icon + the `textSubtle` mono char-counter; 2 in M-41), on-glass clean-body +≥500 (`font.family.bodyMedium`: M-36 `commentsEmptyLabel`; M-41 `loadingText`+`emptyBody`); titles + `description` + `entryLine` stay `textStrong` (no down-ink); body stays `color.text`; italic hints re-ink color-only (italic preserved). **Reopen re-ink (O-2 default, no §A veto):** `reopenBtn` border + `reopenBtnText` `accentOrange`→`brandText` — retires the live 2.07:1 AA drift (arbiter-banked 4.95 light / 5.42 dark). **Pins:** `coordsCopyGlyph` `color.brand` (icon floor, SR-labeled); severity/status badges keep arbitrated fills+`textOnColor` (PROTECT-4); Star watch glyph + `watchBtnActive` border stay `accentOrange` (amber affordance); inner-opaque inputs/chips/bubbles keep tokens; anonymity set byte-preserved (PROTECT-8). **2 source commits** (`243a0c6` M-36 · `205108c` M-41). Diff = 2 tracked files (47+/20-); zero `theme.ts`; `GlassSurface.tsx`/`ThemeContext.tsx` untouched; 7 immutable stacks untouched; no sibling `r2-mp4-stacks.json` (shipped hexes == declarations). Blur budget **+0** (`forceEngineered`=engineered, no BlurView; deepest stack Tasks 12→FlagDetail→StatusHistory stays 12). StatusHistoryModal render test 6/6. **4-skeptic adversarial verify 4/4 UPHELD, 0 refutations** (classification / recipe-integrity / arbiter-drift / byte-identity). PROTECT held (firsthand diff): anonymity strings, RM `animationType` gate, Fork 5 read-half (`statusDotColor`/connector/row labels), all a11y/testIDs byte-identical; Fork 5 decision half never crossed (no count, no guest flag-as-wrong). Provenance: authored Fable 5 max (2026-07-15), executed Opus 4.8 ultracode max (S-10). NOT merged/pushed/built — STOPPED on `r2/mp4-trust-ledger` (Sky's hands). ✅ BP9 (MP5, optional per §6.7) may cut from this tip.**
- **BP9 (MP5): base `205108c` → tip `8a190a3` · 2026-07-17 · gates green (typecheck 0 · lint 0/77 no-new-warn · jest 1944/0 = baseline unchanged, no guards · arbiter `r2-material-stacks.json` verbatim EXIT 0, RESULT ALL PASS — no sibling `r2-mp5-stacks.json`: MP5 introduces ZERO new token+backdrop pairs; every ink lands on an already-banked pair — inkGlassMuted-on-row 8.01/8.32 (r2 M-06), inkOnStage-on-stage 4.83 light/6.29 dark (immutable wave2), color.text@≥500-on-row (wave2)). O-1 = Sky INCLUDE (§A). T2 phase 6 of 6 — the OPTIONAL CABOOSE — CLOSED.** M-23 CLOSED (AdminScreen → editorial stage family: root `surfaceMuted`→`stage1` + `<ScreenStage/>`; the S8 `<ScreenHeader>` (ADMIN/Admin) + `<HeaderActions>` menu/Feedback cluster mounted as the FlatList `ListHeaderComponent` — the recorded Stage-6 grammar exception, scroll-away, blur budget 1→1 — AND above the loading/denied centered states so the drawer escape is reachable in all three; section cards `<Card>`→`<GlassSurface variant="row" forceEngineered>`; 5 stage-direct muted → `inkOnStage` (Lock icon/denied body/list-header meta/empty Inbox/empty body), on-card coord meta → `inkGlassMuted`, description gains `font.family.bodyMedium`). M-49 CLOSED (Admin `headerLeft:renderMenuButton`→`headerShown:false`, now mirrors FullMap/Settings — the shared nav header retires app-wide; dead code removed with 0 dangling refs: `renderMenuButton` + the orphaned `const drawer = useDrawer()` in NavInner + the `MenuIcon` lucide import + the `icon` theme import + the `hamburgerBtn`/`hamburgerBtnPressed` styles). **2 source commits** (`91f47f2` M-23 · `8a190a3` M-49). Diff = 2 tracked files; zero `theme.ts`; `GlassSurface.tsx`/`ScreenStage.tsx`/`ScreenHeader.tsx`/`HeaderActions.tsx`/`ThemeContext.tsx` untouched; 7 immutable stacks untouched; ` D .claude/launch.json` pre-existing deletion NOT staged. Blur budget **1→1** (`forceEngineered` unconditional = no BlurView; ScreenStage adds 0 live panes). PROTECT held (firsthand diff): admin GATE (`isAdmin` logic byte-identical + `src/lib/admin.ts` 0-diff) + denied/empty COPY + moderation handlers (F18 guard, F53 CAS) + sevPill (PROTECT-4) + `accessibilityRole="list"` + `{sev.label} · {item.severity}` all byte-identical; lockstep `qaMergeConsolidation` green. **4-skeptic adversarial verify (Opus 4.8 max) 4/4 UPHELD, 0 refutations** (PROTECT byte-identity / diff-scope+dead-code / ink+material+budget / independent gate re-run). Transparency note (declared, not a regression): the a11y TREE is intentionally restructured — menu/Feedback/title move OS-nav-header → in-screen ScreenHeader (+`role=header` title + ADMIN eyebrow); equivalent affordances, same labels, no logic/data change. **Conservation 15/15 — the T2 material train (MP0→MP5) is COMPLETE.** Provenance: authored Fable 5 max (2026-07-15), executed Opus 4.8 ultracode max (S-10). NOT merged/pushed/built — STOPPED on `r2/mp5-admin-editorial` (Sky's hands). Device close-out folds under R2-D14 (§D).**
- **BP10 (T5): base `8a190a3` → tip `d0ed1b1` · 2026-07-17 · gates green (typecheck 0 · lint 0 err / 0 NEW warn — HEAD-vs-current equal per touched file · jest 1966/0 = baseline 1944 + 22 BP10 guards · arbiter `r2-severity-disc-stacks.json` exit 0, ALL PASS both modes — sev1 11.03 / sev2 8.05 / sev3 6.21 / sev4 ink 4.79 / sev5 white 4.83, all ≥4.5). T5 CLOSED (F6-01 HIGH + F6-02/03/04/05/06) — the severity grammar's atom completed AND introduced.** O-1 = Sky INCLUDE (§A) → cut from the MP5 tip (NOT MP4). New `src/components/SeverityDisc.tsx` (size/digitSize/mfm/decorative; ink fork 1–4 `#0F1B2D` / 5 `#ffffff` baked, NO override prop; `borderRadius: radius.circle`; decorative-by-default via house `decorativeProps`; PINS EXCLUDED). **6 commits, one per plan item:** (1) primitive + 4 zero-delta adoptions (`bb3b93f` — Legend 32/14, Nearby 32/13, ActivityFeed 28/12, RecentlyViewed 24/12+1.3; each dropped its private `sevDot`/`sevDotText` + dead `severity`/`severityColor` imports; Nearby `importantForAccessibility="no"`→house spread = a11y equal-or-better; bp3 F4-01 guard RELOCATED to the primitive); (2) FlagDetail chip `Severity {n}`→`{n} · {word}` + quiet stake line (`SEVERITY_DESCRIPTIONS`, flags.ts, NOT theme) — a11y stays `severityA11y` ("of 5") (`7dbede6`); (3) Home 11px dot → `<SeverityDisc size=24>` + `sep` marginLeft 40→52 realign (`d87ec05`); (4) signed-in mirror surfaces — Profile `· Severity {n} · {word}`, MyReports `•`→`·`+word, MyWatched colour dot → numbered disc, Tasks pill `· Severity {n} ·` (number only, at capacity); Profile+MyReports a11y → `severityA11y` (Sky Q2=sync); MyReports test flags-mock gained `SEVERITY_LABELS` (`9dadf23`); (5) Onboarding slide 2 `icon:MapIcon` → unframed 5-disc row (32/14) on the gradient, ONE `role=image` group, spine-derived label "Severity scale — 1 Minor to 5 Severe" (Sky Q1=unframed) (`b3ba544`); (6) arbiter + blur guard + evidence (`d0ed1b1`). Diff = tracked `src/**` + their tests ONLY; arbiter JSON + evidence + this append UNTRACKED (`design-reviews/`); zero `theme.ts`/`GlassSurface.tsx`; 7 immutable stacks untouched; ` D .claude/launch.json` pre-existing, NOT staged. Blur budget +0 (SeverityDisc = opaque View, guard-enforced). Mode-independence + zero-delta proven BY CONSTRUCTION (primitive reads no theme-forked token; arbiter both modes identical; render style-equality vs theme-independent constants) — stronger than a screenshot. Captures NOT produced this session (auth-fenced surfaces uncapturable unsigned; visual pass folds into R2-D15) — declared honestly, not papered over. **4-skeptic adversarial verify (Opus 4.8 max) 4/4 UPHELD, 0 refutations** (zero-visual-delta / SR-richer-or-equal / PROTECT 9-of-9 / arbiter+gates honesty; ratios independently recomputed sev4 4.7897 / sev5 4.8314; two un-enriched spoken-label holdouts surfaced → §PARKING-LOT, both EQUAL to pre-BP10). PROTECT held: PROTECT-4 grammar (EXTENDED), PROTECT-25 spine, ink-fork asymmetry, ramp mode-independence, Legend/Nearby geometry, pin four-channel capacity, outcome beats severity-free, Fork 3 presentation-only / Fork 5 untouched. Provenance: authored Fable 5 max (2026-07-15), executed Opus 4.8 ultracode max (S-10, Sky-initiated + plan-approved via ExitPlanMode). NOT merged/pushed/built — STOPPED on `r2/bp10-severity-grammar` (Sky's hands).

- **BP11 (T3): base `d0ed1b1` → tip `8acb184` · 2026-07-17 · gates green (typecheck 0 · lint 0 err / 0 NEW warn — per-file HEAD-vs-current equal, new files 0; the only touched-file warnings are pre-existing hook-dep lines NearbyFlags:182 + FlagDetail:210 · jest **1979/0 SERIAL deterministic** = baseline 1966 + 13 BP11 guards [tab-a11y 5 + press-vocab 8]; the parallel run's 3–4 fails are pre-existing worker-teardown flakes in UNTOUCHED files, all green in isolation 54/54 · arbiter `r2-press-vocab-stacks.json` exit 0 ALL PASS both schemes). T3 CLOSED — F1-02/05/06/07/10/11/12/13/14 — the ONE PRESS VOCABULARY.** Mechanism = static **FILL-SWAP** dim (backgroundColor → house token, label/icon ink stays full opacity), never a group opacity on a text-bearing control — **S-1's `opacity: 0.85` wording SUPERSEDED** (Sky AskUserQuestion #1 = "Fill-swap (house idiom)"; the phase doc predated BP3's test-enforced fill-swap by 2 days; justified by the arbiter's ink-vs-fill framing + both PROTECT seeds being fill-swaps + AA-safety). **9 commits:** (1) `5ebe5cf` PressableScale internal pressed/hover dim via `useState` (no Animated → survives RM by construction) + props `dimOnPress`/`pressedTint` + JSDoc rewrite + ~11 brand/active call-site opt-outs + dead `fabPressed` removed [F1-02]; (2) `1a917dd` tab bar (→ final at the fix commit); (3) `0fed1eb` **new token `ctaFillPressed` = `#0F53BE`, MODE-INDEPENDENT** — the pressed companion to ctaFill (deepening white-on-brand to `brandText` breaks dark mode, where brandText is a LIGHTER blue; an AA-mechanics necessity, reuses the brand deep-end, mirrors borderPressed/successStrong); (4) `67706c5` filter language (Map panel + Tasks chips/sort) — inactive→borderPressed, brand→ctaFillPressed, `!active` guard; converts enumerated same-file opacity dims (presetBtn/emptyCardBtn→ctaFillPressed, placeChip literal-minus-opacity); +savedAddPillText brand→brandText (sibling-consistent AA) [F1-05/06/07]; (5) `3b8b0bd` Sheet close (cascades to Sheet consumers) + 7 named modal closes + FlagDetail ~19 controls by family (neutral→borderPressed / brand→ctaFillPressed / Resolve→successStrong / Delete→error) + its 4 same-file opacity dims [F1-11/12]; (6) `9af7373` Home search pill + clear-search + map-peek chip-only tint via children-render-prop (S17 tiles stay inert) + F1-10 paddingBottom 96→108 + web hover rides the primitive [F1-10/13]; (7) `18feda9` web callout minHeight 44 + `.am-callout-btn` id-guarded `<style>` hover/active/focus-visible [F1-14]; (8) `3adb974` arbiter + `bp11PressVocabGuards` + two arbiter-caught AA fixes (**tab → HAPTIC-ONLY**: `headerBtnBgPressed` on the near-floor tab labels measured 3.81 light-inactive / 4.03 dark-active / 4.37 dark-inactive < 4.5 — a visible dim and legible labels can't coexist; **Tasks Details → `dimOnPress=false`**: inkDetailsGhost is #1466E0 light = 4.03 on borderPressed); **(9/fix) `9d1ff85`** — the 4-skeptic adversarial verify (Opus 4.8 max) caught a CONFIRMED BLOCKER: the custom tabBarButton dropped v7's `aria-selected` (the screen-reader active-tab signal — v7 `BottomTabItem.js:142` sends it as aria-selected, NOT accessibilityState), and its guard test was falsely green (it hand-fed accessibilityState, a shape v7 never emits). Rebuilt on RN Navigation's own `PlatformPressable` + `{...rest}` (still haptic-only via `pressOpacity={1}` + `pressColor="transparent"`); test rewritten to the real aria-selected shape (now catches the drop; passes the fix). Diff = **20 tracked files** (18 src + 2 tests); `GlassSurface.tsx` / `src/lib/haptics.ts` / git-tracked `map-stacks.json` = **0 diff-lines**; MapScreen `box-none` wrappers ×5 intact; blur budget **+0** (fill-swaps + PlatformPressable add no BlurView). **Adversarial verdict: 3 UPHELD (AA-coverage · RM-survival · active-never-greys) + 1 REFUTED→FIXED (the tab a11y blocker); +4 arbiter completeness pairs added (inkGlassMuted / textStrong / warningFg / placeChip-glyph — all PASS).** Exceptions (documented, §PARKING-LOT below): MapScreen `errorBanner` keeps its opacity + FlagDetail comment-Retry ×2 + Submit-reopen stay silent — red/orange filled CTAs with no darker companion token (deepen is the only AA-safe direction; converting cleanly needs an invented colour value or a BP3-style scrim restructure); `cardPressed` left as the borrow-model; 5 newly-found Tasks residual opacity dims (suggestedRow / loadMore / selectEntry / bulk / Tasks-errorBanner) NOT converted (beyond the enumerated scope Sky approved — flagged for a follow-up). **Completion pass `8acb184` (Sky-delegated "I trust your recommendation"):** the same-file residuals answer in-dialect — Tasks suggestedRow/loadMore/selectEntry → borderPressed; the error-load banners (Map+Tasks) + FlagDetail comment Retry ×2 → new mode-independent **`errorPressed`** (#9e2a1e, white 7.49:1). STILL flagged (genuine design decisions): the bulk-select bar (mixed-colour, successStrong/accentPurple no darker token) + Submit-reopen (PRE-EXISTING rest AA fail — white on light amber #f1a520 ≈1.8:1). **⚠️ CONCURRENCY:** a concurrent **BP12 agent** (`r2/bp12-status-ledge`) switched this shared working tree and the completion commit `eafd20e` landed on ITS branch; recovered via an isolated worktree cherry-pick onto `r2/bp11-press-vocab` (tip `8acb184`, typecheck 0) without disturbing the BP12 agent. **Left for Sky:** drop the duplicate from bp12 — `git rebase --onto 7de1f34 eafd20e r2/bp12-status-ledge` (when the BP12 agent is idle). Provenance: authored Fable 5 max (2026-07-15), executed Opus 4.8 ultracode max (S-10, Sky-initiated + plan-approved via ExitPlanMode). NOT merged/pushed/built — STOPPED on `r2/bp11-press-vocab` (Sky's hands). Evidence: `build-plan/evidence/BP11-verification-evidence.md`.
- **BP12 (T6): base `9d1ff85` → tip `705a798` · 2026-07-17 · gates green (typecheck 0 · lint 0 err / 0 warn — test helpers `any`→`unknown` to hold the no-new-warn floor · jest **2000/0** clean full run at HEAD `705a798`, 136/136 suites = 1979 baseline (`9d1ff85`) + 19 BP12 guards + ≈3 from the interleaved `eafd20e` · **NO arbiter** — position-only, no ink/color/floor change (report §7) · blur budget **+0**). T6 CLOSED (F2-03 HIGH, canonical for F5-04 + F5-08) — the status voice gets ONE designed podium.** New `src/lib/statusLedge.ts` pub-sub (house pattern: module state + `Set` listeners + `useSyncExternalStore`, **scalar hooks only**): pure `computeLedgeTop(insetTop, headerHeight, slot)` collapses to today's `Math.max(insetTop,56)` when headerHeight null + slot 0 (Map / guest-native / provider-less tests **byte-identical**); owner-guarded `clearHeaderHeight` (tab-switch clobber-proof); occupants rank by fixed priority (LIVE_STATUS above FLASH_BANNER → the toast stacks below the voice). `ScreenHeader` publishes its outer-container **HEIGHT** (onLayout, guard `h<=0`, scroll-invariant) while focused via **non-throwing `useContext(NavigationContext)`** (seeds from `isFocused()` on mount, `publishLedge` opt-out prop). **4 commits:** (1) `e413b78` store + header publish; (2) `8c2c264` LiveStatusRegion docks — **PROTECT-18 byte-preserved**: ONLY the wrapper `top` expression changed (the sole removed line; diff-proven `evidence/BP12/PROTECT-18_LiveStatusRegion.diff`), the native announce effect + web text-mutation carrier + zero-width-space parity untouched (the 6 announce tests stay green = runtime proof); (3) `7de1f34` vehicle arbitration (FlashBanner lower-priority occupant → offsets below the voice, never in the same z-50 slot) + inner text-carrier pill `pointerEvents="box-none"` (only Retry hittable; the menu circle beneath is freed; FlashBanner dismiss Pressable untouched) + Tasks `publishLedge={false}`; (4) `705a798` 19 guards (statusLedge 11 + LiveStatusRegion +3 + ScreenHeader 5). Diff (mine) = **exactly the 8 intended files** (verified `git show --stat e413b78 8c2c264 7de1f34 705a798`). Web render-regression (fresh export + `probe-export.mjs`): Tasks/Map/Nearby/Report **all RENDER** (no ErrorBoundary; `findNodeHandle` pageerror pre-existing in `src/lib/accessibility.ts`, not mine); Home header frames light+dark show the "9 barriers" headline **un-decapitated**. Docked-pill + two-vehicle-stack visuals are status-triggered (submit forbidden by the rig; no debug hook added) → **NEEDS-SKY-DEVICE R2-D16**. **⚠️ CONCURRENCY:** the BP11 agent's completion commit **`eafd20e`** landed on this checked-out branch (between my C3 and C4); its content is **safely preserved on bp11 `8acb184`** (cherry-pick — `errorPressed`/`borderPressed` verified there), so it's a **duplicate** here. Documented drop-remediation `git rebase --onto 7de1f34 eafd20e r2/bp12-status-ledge` **left for Sky** — I did NOT rewrite it (history rewrite + file-sourced instruction). Provenance: authored Fable 5 max (2026-07-15), executed Opus 4.8 ultracode max (S-10, Sky-initiated + plan-approved via ExitPlanMode). NOT merged/pushed/built — STOPPED on `r2/bp12-status-ledge` (Sky's hands). Evidence: `build-plan/evidence/BP12-verification-evidence.md`.
- **BP13 (T7+T9): base `705a798` → tip `6e8e636` · 2026-07-18 · gates green (typecheck 0 · lint 0 err/77 warn = BP1 baseline, no new · jest 2014/0 CLEAN full run, 137/137 suites = baseline 2000 + 14 guards · NO arbiter — no ink/color/floor change, spec §4). Sky-authorized IN-WINDOW completion of a STRANDED/interrupted BP13 run** (a prior run had left T7 partially built + uncommitted; reconciled per the RESUME RULE — item 1 verified+kept, item 2's web impl + all MapScreen wiring built, items 3-6 built). **T7 CLOSED (F4-03 + F5-03):** the undetermined no-location arrival gets ONE polite voice (`role="text"`, `live="polite"`, `NO_LOCATION_HINT`, gated on the RAW status via `arrivalPermissionDenied` — never "off"; web-announced via the S9 shim; mutually exclusive with the byte-identical assertive DENIED banner) + a TRUE frame (`regionForFlags` fits the loaded rows; `snapToRegion` — a new `PlatformMapHandle` method in BOTH variants reusing BP1's instant path, native `animateToRegion(_,0)` / web `instantCut`+`deltaToZoom` — cuts to it ONCE, instantly, guarded off a resolved location + a CAMERA-MOVING intent arrival (focusFlag/deep-link, retired BEFORE the flags gate so the deep-link's ~800ms flagId self-clear can't re-fire it — the adversarial catch); openReport deliberately NOT retired (no camera → still earns the honest fit); empty→DEFAULT, singleton→fixed zoom). **T9 CLOSED (F5-02 HIGH + F5-01/05/09):** no settled failure renders a false zero (Home headline + Map pill's new 4th arm both gated on `error && flags.length===0`, so the SWR stale path keeps its cached count; S11 loading/count strings byte-identical), worded waits ("Loading…"/"Loading flags…"), ONE failure recipe single-sourced in `copy.ts` (`RETRY_VERB` + `failureBannerText`, de-duped; Tasks + Map adopt; NOT a shared component), submit keeps words ("Filing your report…"). **5 commits:** `ee79bb1` (T7 voice+frame) · `877c9ab` (T7 guards) · `5d98df8` (T9) · `01eb35c` (T7 regionForFlags noUncheckedIndexedAccess fix) · `6e8e636` (T7 race fix: deep-link retire + openReport exclusion). Diff = exactly **9 tracked files** (7 src/lib + 2 tests); 7 immutable stacks untouched; blur budget **+0** (no BlurView). PROTECT held (git-verified): DENIED banner byte-identical, S11 ladder strings/thresholds byte-identical (4th arm inserted around them; +2-space re-indent only), Home recovery card untouched, box-none footprint unchanged (retry verb appended to the EXISTING AppText), Home honesty (fit = camera only, no fabricated distances). Drift adapted+noted: `currentRegionRef` is written only on the location-sync in this tree (never on pan), so the spec's "no-gesture proxy `=== DEFAULT_REGION`" is equivalent to "location still null". Process note (honest): a `regionForFlags` typecheck slip rode into the T7 commit because an early background typecheck's trailing `echo` masked tsc's real exit; the full gate caught it, fixed forward in `01eb35c`. **Adversarial verify (Opus high, S-10): 4 parallel skeptics → 3/4 UPHELD (high) + arrival-frame REFUTED→FIXED (a MEDIUM viewport race: the null-location fit stomped a deep-link ~800ms after arrival) + a re-verify NEW-BUG→FIXED (openReport over-retire) → Round-3 confirm CLEAN. The pass earned its keep — caught a real race the type/unit gates could not.** All new strings PROPOSED (S-8) — tabled in evidence for Sky/BP16. Static-export render/captures declared NEEDS-SKY-DEVICE (status-triggered frames uncapturable by the granted-geo probe rig; render-no-regression covered by typecheck 0 + jest 2014/0 + reused primitives). Provenance: authored Fable 5 max (2026-07-15), executed Opus 4.8 ultracode max (S-10, Sky-initiated in-window completion). NOT merged/pushed/built — STOPPED on `r2/bp13-arrival-waits` (Sky's hands). Evidence: `build-plan/evidence/BP13-verification-evidence.md`. ✅ BP14 may now cut from this tip (its §0 gate now passes).
- **BP14 (T13+T14): base `6e8e636` → tip `edfcb08` · 2026-07-18 · gates green (typecheck 0 · lint 0 err / 77 warn = BP13 baseline, **0 NEW** · jest **2029/0** CLEAN full run, 139/139 suites = baseline 2014 + 15 guards [wrap 2 + distance 2 + Settings-inset 1 + Map-pair 3 + fade-pure-fn 7]; the 2 parallel-run fails [StatusHistoryModal, MyReportsModal] are pre-existing worker-teardown timeout flakes in UNTOUCHED files — green in isolation 10/10, zero refs to any BP14 module · **NO arbiter** — T13 changes no ink, T14 REUSES the shipped S16 ink `rgba(15,27,45,0.22)`/`rgba(0,0,0,0.38)` UNCHANGED so the conditional arbiter gate never fires · blur budget **+0**, gradients not BlurViews). T13 CLOSED (F2-02 HIGH + F2-05/06/11/12/13) — the editorial frame holds at size + inset; T14 CLOSED (F2-07/08) — one overflow-scent grammar.** **4 commits:** (1) `ed406f0` T13/wrap — ScreenHeader `numberOfLines={1}→{2}`: at the M18 0.6 floor a still-overflowing title WRAPS instead of tail-ellipsizing (WCAG 1.4.4 on the web export); at-rest byte-identical (the estimate/floor/`adjustsFontSizeToFit` untouched — numberOfLines is the only functional line) + at-rest structural pin + starved-width wrap guard. (2) `3e49c39` T13/unit — `formatDistance` joins value+unit with U+00A0 (grep-visible ` ` escape, **0 raw NBSP**) so "297 m" can't orphan; `speakDistance` **BYTE-IDENTICAL** ASCII (VoiceOver unchanged); `formatWalkingEta` parked; 8 assertions updated + NBSP/ASCII guards. (3) `37616f3` T13/insets (S-3 cheap-win) — Map `HeaderActions` regrouped into ONE right-pinned **box-none** pair (`gap spacing.xs`) so the space-between row can't strand the menu circle; Settings header `paddingHorizontal:0` kills the 44→24 double-inset (Profile's convention, **horizontal-only**; **Profile OUT**, no single-20 law); chip `paddingHorizontal 14→spacing.md` (width-only, T1-safe); **`MAP_HEADER_ROW_MARGIN_BOTTOM=10` PARKED** (T1 callout-clearance coupling — §A); F2-11 NO-CODE→§D. (4) `edfcb08` T14/scent — extracted the shipped S16 fade into `useHorizontalOverflowFade()` (pure `computeOverflowHasMore`; the exact `overflow>1 && !atEnd` / throttle-16 / hide-at-end contract) + `OverflowFade` (single-source ink, `edge`/`width` props); grafted onto the **6 silent chip rails / 5 surfaces** (Map filter Categories+Saved-sets, Report templates+categories, Tasks strip, Nearby tablist), each in a `position:relative` wrapper (shared `filterRow`/`row`/`chipScroll` untouched; **Saved-Places landmine avoided**); action bar RETROFITTED to the same OverflowFade+pure-fn (dead inline LinearGradient + `actionBarFade` style + now-unused LinearGradient import retired, 0 refs); 7-case pure-fn guard covers all 7 sites. Diff = **exactly 14 tracked files** (10 src + 4 tests, `--name-only` verified); 7 immutable stacks **0-diff**; `.claude/launch.json` deletion in **NO** commit. **F2-08 decision — SHIP UNCHANGED, no arbiter:** the ink is the already-shipped, ratified S16 affordance reused with zero value change, and every graft backdrop is ≥ the action bar's floored engineered-glass over Positron → strengthen not expected; the empirical live-probe could not run here (Playwright absent; the rig is a Playwright client) → code-inferred + device-gated (§D-d). **Captures DEFERRED (honest):** the capture rig needs Playwright (absent) and the on-disk `dist/` predates BP14 → the home/nearby zoom200 + Settings + chip-fade frames fold into Sky's device/eye gate; code proven by typecheck 0 + jest 2029/0 + 15 structural guards. **Adversarial verify (Opus max, S-10): 3 parallel skeptics (T14-grafts · T13+PROTECT · scope/completeness) → 3/3 UPHELD, 0 defects** (skeptic ran an independent `tsc` clean; confirmed 1:1:1 hook→spread→fade with no Rules-of-Hooks violation, at-rest byte-identical, box-none/T1/Profile preserved, scope = 14 in-set files, no dead code, no new user-facing strings). Provenance: authored Fable 5 max (2026-07-15), executed Opus 4.8 ultracode max (S-10, Sky-initiated + plan-approved via ExitPlanMode). NOT merged/pushed/built — STOPPED on `r2/bp14-editorial-frame` (Sky's hands). Evidence: `build-plan/evidence/BP14-verification-evidence.md`. ✅ BP15 may now cut from this tip (its §0 gate now passes).
- **BP15 (T12+T10): base `edfcb08` → tip `c0ee449` · 2026-07-18 · gates green (typecheck 0 · lint 0 err / 77 warn = BP14 baseline, **0 NEW** · jest **2040/0**, 142/142 suites = baseline 2029 + 11 guards [exitLatch 4 + ScreenFallback 2 + GuestProfile 5] · arbiter `r2-guest-profile-stacks.json` exit 0 ALL PASS both schemes — guest eyebrow/subtitle inkOnStage 4.83 light / 6.29 dark, title textStrong 11.35/13.40, byte-pairing the shipped signed-in recipe). T12 CLOSED (F3-01 HIGH + F3-02 + F3-07) — the drawer passage whole in both directions; T10 CLOSED (F2-04 HIGH) — the signed-out Profile joins the family.** **4 commits:** (1) `94f4171` T12/warm+dress — `DrawerHost` preloads the Settings (+Admin gated `isAdmin===true`) lazy chunk fire-and-forget `.catch(()=>{})` on drawer-open (extends the shipped Suspense-fallback-null warm-chunk pattern to the nav layer; import cached so repeat opens no-op); `ScreenFallback` dressed = `stage1` root → `ScreenStage` wash → header-shaped static `Skeleton` (opaque only → keeps M-54 opaque-system tier, F3-01's sanctioned exception, NOT a material-train migration; ErrorBoundary crash fallback untouched); the 'Opening Settings — one moment' line PROPOSED-only → BP16, so `r2-settings-arrival-stacks.json` **SKIPPED**. (2) `9fb637d` T12/exit — local `rendered` latch drives Modal visibility (was `visible={open}`); mount on open, stay mounted while the EXISTING exit `Animated.parallel` plays (slide `base`/180 + fade `fast`/120 byte-intact), flip closed in `.start(({finished})=>!open&&finished&&setRendered(false))`; RM path `setValue` snap + `setRendered(false)` same-tick, no timer; sub-screens are independent Modals (latch never touches them). **SAME COMMIT (absorbed-FIX law):** `navigate()` `220` literal → `motion.duration.base` (retires the transition layer's last off-scale literal; RM stays 0) + `reduceMotion.drawer.test.tsx:55` `toContain(220)`→`180`, RM=0 assertions byte-intact. (3) `a5535f8` T10/guest — `!user` branch renders the S8 family (`ScreenHeader` eyebrow PROFILE / title "Your profile" / subtitle + `HeaderActions` menu+feedback) above the CTA on the existing `ScreenStage`; TRUE render-parity (eyebrow/subtitle = `inkOnStage` per the :900 AA law, title default `textStrong` — **no `titleColor` prop exists**, spec over-specified; +`insets.top` notch clearance, `profileHeader` zero-pad); **NO new copy** (all 3 strings pre-existed — md5-verified); authLoading skeleton leg (F5-06) **DROPPED** — runtime-unreachable (App.tsx Gate returns null while loading; auth.tsx flips loading→false once, never back), code-inferred; `drawer`+`setSharedModal` already component-scoped (zero new wiring). (4) `c0ee449` guards+arbiter — 3 suites (exitLatch/ScreenFallback/GuestProfile) + the MANDATED `r2-guest-profile-stacks.json` (exit 0); 2 testability extractions (`ScreenFallback` → own module; guest state → `GuestProfile.tsx`, same rendered tree relocated, guest-only styles moved, shared `profileHeader` stays). **Deferred (forked with reason):** the drawer-open import-once guard — dynamic `import()` call-counting isn't cleanly mockable under jest-expo without an invasive seam; typechecked + device-observable (warm path shows no interstitial). Diff = **exactly 9 tracked files** (`git diff edfcb08 HEAD --stat`); 7 immutable stacks **0-diff** (`map-stacks.json` unchanged); ` D .claude/launch.json` pre-existing, in **NO** commit; blur budget **+0** (opaque primitives, no BlurView). Captures device-deferred (Playwright absent) → profile-signedout + drawer-dismissal frames fold into Sky's device gate. **Adversarial verify (Opus 4.8 max, S-10): 5-skeptic fan-out → 5/5 UPHELD, 0 refutations** (exit-latch correctness [7 failure-modes, no defect] · arbiter honesty [conservative bound, re-run exit 0] · RM stillness preserved · authLoading unreachable · no-new-copy md5-verified; the exit-latch skeptic's Workflow schema attempt hit a StructuredOutput retry cap — tooling, no verdict — re-run as a plain-text pass, UPHELD high). Documented unreachable dev-nit (no anim-cleanup on unmount → §PARKING-LOT; `DrawerHost` is a permanent mount). PROTECT held: presentation grammar (drawer stays own hand-animated class, `modalGate` guard green), welded arrival→departure, Suspense-fallback-null warm chunks (cited not forked), 220ms delay-gate+pulse-token discipline (completed), PROTECT-17 (reference), GlassSurface/blur untouched. Provenance: authored Fable 5 max (2026-07-15), executed Opus 4.8 ultracode max (S-10, Sky-initiated + plan-approved via ExitPlanMode). NOT merged/pushed/built — STOPPED on `r2/bp15-drawer-guest` (Sky's hands). Evidence: `build-plan/evidence/BP15-verification-evidence.md`. ✅ BP16 may now cut from this tip — BUT BP16 additionally requires Sky's §A per-string picks (S-8/O-5) + the Jordan Art. 7 k≥3 sign-off before it can ship strings (it STOPS at the table otherwise).
- **BP16 (T17+T18): base `c0ee449` → tip `8adb4d4` · 2026-07-18 · gates green (typecheck 0 · lint 0 err / 77 warn = BP15 baseline, **0 NEW** · jest **2047/0, 144/144 suites** = baseline 2040 + 7 guards [OnboardingModal announce-gate 4 + UpdateBanner emoji-census 3] · **NO arbiter** — no ink/floor/colour pair changed). MECHANICS shipped, table PROPOSED, phase STOPPED at the table (S-8/O-5: §A carried no BP16 pick + no Jordan sign-off at build time → ZERO strings shipped).** **2 source commits:** `d9d3887` T18 announce gate — OnboardingModal's position announce becomes a single `visible`-gated owner (refs `wasVisible`/`prevIndex`); kills the phantom "Step 1 of 3" that fired on the always-mounted-CLOSED Settings modal, hard-codes the open-edge announce to position 1 so no stale pre-reset index ("Step 3 of 3") leaks, still announces each navigated position; wording verbatim "Step N of M" (the 'Step'→'Card' flip = deferred table row). `8adb4d4` T17 Bell glyph — UpdateBanner 🔔 → Lucide `<Bell size=18 color=brandOnSoft strokeWidth=2.2 accessibilityElementsHidden importantForAccessibility="no">`; reuses the already-arbitrated banner ink (NO new pair → no arbiter, no stacks sibling); dead `icon` style dropped. **Commit-plan item 3 (THE TABLE — 38 PROPOSED rows) + this evidence + this append are UNTRACKED (`design-reviews/`) → no third git commit (nothing tracked changed).** Tracked diff = **exactly 4 files** (2 src + 2 new tests), `git diff --stat c0ee449..8adb4d4`; 7 immutable stacks 0-diff; ` D .claude/launch.json` pre-existing, NOT staged; blur budget +0. **Adversarial verify (Workflow `wf_cd1067b7-42c`, 8 sub-agents ALL `claude-opus-4-8` max/high): 5 skeptics → 5/5 UPHELD, 0 refutations, high confidence** (M1 timing-and-effect-ordering · M1 a11y-voiceover · M1 regression-deps-and-blast-radius · M2 ink-token-no-arbiter · M2 census-validity-and-a11y-hiding); the run was READ-ONLY (HEAD unchanged at `8adb4d4` after). Table assembled (38 rows) + completeness-critic **COMPLETE** (one soft gap: `MapScreen:1217` second no-location wording → fold into `parked-bp13-noloc`) + **Jordan Art. 7 k≥3 SIGNED OFF** (semantic-equivalent=YES, ADVISORY — needs professional legal review; one non-blocking advisory: fold "to protect reporters" into the single-source string). Provenance: authored Fable 5 max (2026-07-15), executed Opus 4.8 ultracode max (S-10, Sky-initiated + plan-approved via ExitPlanMode). NOT merged/pushed/built — STOPPED on `r2/bp16-copy-gate` (Sky's hands). Evidence: `build-plan/evidence/BP16-verification-evidence.md` (+ `evidence/BP16/PROTECT-24.md`). **RESUME (item 4) GATED on Sky's §A per-string picks (S-8/O-5) + a recorded Jordan sign-off; the resume applies EXACTLY the picks with test-lockstep (ReportFlagModal.test 399/402/427/430 · qaMergeConsolidation.test:35 · TasksScreenFlagCard.test 128/157/170 · bp13FailureVoice RETRY_VERB) — never merged/built by any agent.**
- **BP17 (T19+T15+T20): base `8adb4d4` → tip `d43f867` · 2026-07-19 · gates green (typecheck 0 · lint 0 err / 77 warn = BP16 baseline, **0 NEW** · jest **2057/0, 146/146 suites** = baseline 2047 + 10 guards [T19 notification-parity 2 · T15 ink 2 · MyWatched containment 1 · dialogTier 5] · arbiters `r2-brand-cameo-stacks.json` exit 0 [accentOrange fill 7.68:1; Profile cameo 3.74 light / 14.61 dark] + `r2-tabbar-ink-stacks.json` exit 0 [inactive ink #515964 = 4.65:1 on the 0.82/#000 floor, 5.92 on the 0.92 RT/web; dark regression 5.17/5.87]; M-48 trials still exit-1-by-design). T19 CLOSED (F6-07 comment-only + F6-08 marks + F6-09 notification), T15 CLOSED (M-48-ink drift), T20 CLOSED (§6.9 containment + RB12 dialog tier + M-57 paper) — the caboose; the whole R2 train is built.** **6 commits:** (1) `b11cd6f` T19 — accentOrange UNCHANGED #f1a520 (Sky: comment-only); stale theme.ts:136 comment corrected + theme.test guard re-pointed severity4→severity2 (the real sev-2 near-twin #F0A030); app.json notification #1a4fa3→#1466E0 + new 3-way parity guard; LogoMark seated signed-out Profile (~56pt) + About title (22pt, SR-hidden), both theme-aware. **Recon correction:** About heroBadge was already a Lucide icon, NOT an emoji (stale comment) → no PROTECT-24 gap; the About mark is additive. (2) `0ab983a` T15 — light tabBarInactiveTint #6B7280→#515964 (arbiter-decided, lightest clearing 4.5 w/ margin; dark untouched, separate token; no consumption edit); :186 comment corrected; guard pins hex + AA on #D1D1D1. (3) `36586d4` T20/containment — MyWatched sheet CONTENT GlassSurface gains accessibilityViewIsModal (mirrors MyReports, never backdrop); render guard. (4) `e9e89c9` T20/dialog UNIFY (Sky: O-4=UNIFY) — nameCard +shadow.e3 (four-of-four depth), ProfileScreen tier-explainer + delete-account slide→fade (RM ternary preserved); dialogTier guard 5/5 + modalGate green; out-of-scope sign-in modal untouched. (5) `bd11d7d` T20/paper — GLASS §8 Home chrome→row (shipped truth), tab-bar cleanup line → M-48 kill as self-contained prose, drawer + dialog-tier ratification notes; index.html keep-in-sync hex comment (byte-identical, 15+/0−). O-1=INCLUDE→no B14 line; O-4=UNIFY→no record-note. (6) `d43f867` post-verify — tighten the T20 containment guard from existential to content-placement (testID-scoped) after the adversarial pass; test-only, containment code unchanged. **Consolidation (untracked, no git commit):** `build-plan/DEPLOY-CHECKLIST.md` — merge tip `d43f867` (pure-ff + bp12 dup-rebase flag) · the one testflight build cmd · R2-D0…R2-D18 verbatim ordered D1→D4→D14 + BP1–16 §D roll-up + R2-D18 · open Sky items · evidence index. Tracked diff = **exactly 12 files** (`git diff 8adb4d4 HEAD --name-only`; **ThemeContext.tsx UNTOUCHED** — hue unchanged); 7 immutable stacks 0-diff; ` D .claude/launch.json` pre-existing, NOT staged; blur budget +0 (LogoMark is SVG, no BlurView; GlassSurface untouched). **Adversarial verify (Workflow `wf_5cdc7937-5ed`, 6 skeptics ALL Opus 4.8 high, S-10): 5 UPHELD + 1 minor DEFECT [containment guard over-claim — FIXED `d43f867`] + 1 nit [luminance figure ~0.0995→~0.098 in untracked _doc/evidence — FIXED]; 0 blocker/major; the completeness critic independently re-ran all gates + confirmed scope/PROTECT.** Provenance: authored/planned Fable 5 max (2026-07-15), executed Opus 4.8 ultracode max (S-10, Sky-initiated + plan-approved via ExitPlanMode). NOT merged/pushed/built — STOPPED on `r2/bp17-hygiene-deploy` (Sky's hands). Evidence: `build-plan/evidence/BP17-verification-evidence.md`. ✅ **This is the caboose — the whole R2 train is built; next = Sky fires `build-plan/DEPLOY-CHECKLIST.md`.**

## §D DEVICE-GATE CONTRIBUTIONS (accrues; BP17 consolidates into the final checklist with the report's R2-D0…R2-D18)

- *Seed: the full R2-D0…R2-D18 ledger lives in the report §4 — phases append ONLY additions/refinements here, tagged by phase.*
- **BP1 → R2-D12 (refined):** on-device, both themes, RM on/off — (a) native Callout on a top-third pin sits fully below the header chip / status pill / action rail; (b) rapid Nearby A→B yields B's callout only; (c) theme-flip continuity (the map never blanks — web-approximated PASS here, native tile handling differs); (d) VoiceOver callout→details path (S3 wiring untouched by BP1); (e) **NEW native edge:** cold-start straight into a `focusFlag` under LARGE Dynamic Type (Map's first-ever mount before onLayout measures the chrome band → the one native path with no web-style self-heal; adversarial finding 1); (f) inherited B5 caveat — whether `animateToRegion` duration 0 is truly instant on-device.
- **BP2 → R2-D3 (contributes):** iOS VoiceOver, both themes — (a) Report severity row + Map category/status/affects chips announce role + state ("… selected") on focus, where the web build now exposes `aria-pressed`; (b) the already-working severity live-region hint speaks ONCE on change — no double-speak with the chip's own state; (c) toggle a chip off, re-focus, hear the state flip. Native path = the mirrored `accessibilityState.selected` (mechanism unchanged) — this device leg confirms the `{selected}`→`{pressed}` migration didn't regress native VoiceOver. The focus-ring + inert legs are web-only (native focus is OS-drawn) — no device gate for those.
- **BP3 → R2-D1 (contributes — the audit's highest-stakes gate):** iOS device, both themes, RM on/off — (a) finger-DOWN selection tick on the category + severity pickers (the signature control answers the press, not the release); (b) Verify = a **medium thunk THEN a success notify** landing with the flash banner; the failure path an **error notify**; the commit/outcome haptics feel distinct from the picker's selection tick; (c) Reject still **confirms first** — the commit haptic fires only post-confirmation; (d) RM ON → the static pressed dims AND the haptics both still answer (dims are motionless, haptics ride the OS setting, both independent of reduce-motion); (e) **VoiceOver FlagCard walk:** each card reads **once** in the taught grammar ("No ramp, severity 4 of 5, Significant, status Open. Tap to view on map."), each action names its flag uniquely ("Verify this flag — No ramp, 639 m"), and Legend rows read one utterance each (no doubled severity digit).
- **BP4 → R2-D14 (contributes — the phase headline):** cold-launch **dark-mode first frame** (M-56) — on an OS-dark device, the FirstLaunchGate loading frame paints `surfaceMuted` (dark), not white, so there is no white boot strobe before the app/sign-in screen mounts. Also on the device gate: (a) the map empty-filters card + UpdateBanner read as the house Deep Field material in **both palettes** (M-55/M-52); (b) frost/perf feel of the engineered banner/row tier over live map content; (c) an **RT sweep** (OS Reduce Transparency + Settings toggle) confirming both new surfaces render the *designed opaque state* (banner → brandSofter+brand border; row → overlay+borderStrong), never a low-contrast smear; (d) lightbox close-X/caption/empty text now legible in dark (M-46). Residual noted: an OS-light / app-dark user still gets one light frame during the ~50 ms storage read (out of MP0 scope).
- **BP5 → R2-D14 (contributes — the Home-stage leg):** iOS device, **both palettes** — (a) the Home tab (default landing surface) reads as the house Deep Field stage: root wash + the six-row list card as ONE row-glass pane + the search pill as the engineered row tier — no longer a different material world from Tasks/Profile; (b) frost/perf feel of the row-tier card + pill over the live stage (the card is the one live blur pane in full mode; the pill is engineered-literal, budget-free); (c) an **RT sweep** (OS Reduce Transparency + Settings toggle) confirming the card + pill render the *designed opaque state* (overlay 0.97 + borderStrong), never a low-contrast smear, and the stage itself STAYS under RT (GLASS §6 — only glass goes opaque); (d) a VoiceOver pass — the search pill + rows read exactly as before (a11y props byte-identical), and eyebrow/subtitle/section-label are legible on the stage in both palettes (`inkOnStage`, arbitrated above the 4.10 `textMuted` default). C-lite note: long-press the Tasks header to flip glass→lite and confirm Home's card drops its blur for the engineered `*Lite` gradient (per D2, Home READS the shared store but does not itself hydrate the persisted pref on cold launch, so land on Tasks/Profile once first).
- **BP6 → R2-D14 (contributes — the Profile-sheets leg):** iOS device, **both palettes** — (a) the My Reports / My Watched / Recent Activity sheets read as the house engineered bulk material, matching the About/Feedback sheet they sit beside (B6 — Profile's hallway no longer opens two buildings); (b) frost/perf feel of the engineered bulk sheet tier over live Profile content; (c) an **RT sweep** (OS Reduce Transparency + Settings toggle) confirming all three render the *designed opaque state* (overlay 0.97 + `borderStrong` top edge), never a low-contrast smear; (d) a VoiceOver pass — each sheet reads exactly as before (a11y byte-identical; MyWatched still correctly LACKS `accessibilityViewIsModal` — that containment gap is BP17's leg, not an MP2 regression); (e) confirm the MyWatched load-error now shows on a solid `errorBg` banner, not bare on glass.
- **BP7 → R2-D14 (contributes — the overlay-remainder leg):** iOS device, **both palettes** — (a) the Achievements / Leaderboard / Notifications / Notification-Preferences sheets read as the house engineered bulk material, matching the sheets they sit beside (RB7 — Settings' hallway no longer splits on the two notification surfaces); (b) frost/perf feel of the engineered bulk sheet tier over live content; (c) an **RT sweep** (OS Reduce Transparency + Settings toggle) confirming all four render the *designed opaque state* (overlay 0.97 + `borderStrong` top edge), never a low-contrast smear; (d) a VoiceOver pass — each sheet reads exactly as before (a11y byte-identical); (e) confirm the Leaderboard `ctaFill` retry/segment fills + `brandText` rank-1 row read cleanly in dark. **M-24 caveat:** unreachable in production at HEAD (`PUSH_NOTIF_TYPES_ENABLED=false`); its identical sheet is live via M-42, so the read covers both.
- **BP8 → R2-D14 (contributes — the trust-ledger leg + the train's named close-out):** iOS device, **both palettes**, RM on/off — (a) the **VoiceOver walk of FlagDetail post-material** (the train's named device close-out): each section + action reads exactly as before (a11y byte-identical), the trust-receipt read half unchanged, the `accessibilityViewIsModal` boundary now carried by GlassSurface still contains focus; (b) FlagDetail + StatusHistory read as the house engineered bulk material — every S3 doorway (map callout, Tasks card, Profile row) now opens onto ONE world; (c) frost/perf feel of the engineered sheet over the **live moving map** AND over the Tasks list (the deepest simultaneous stack, still 12 blur panes); (d) an **RT sweep** (OS Reduce Transparency + Settings toggle) confirming both sheets render the *designed opaque state* (overlay 0.97 + `borderStrong` top edge), never a low-contrast smear; (e) confirm the reopen button's new `brandText` (was the 2.07 orange) + the re-inked ledger meta/char-counter read cleanly in both palettes.
- **BP9 → R2-D14 (contributes — the Admin editorial leg + the T2 train's final device leg):** iOS device, **both palettes**, RM on/off — (a) all three Admin states (loading / access-denied / loaded+empty) read as the house Deep Field stage, and the moderation cards read as the engineered row material; (b) the in-screen **menu (drawer) is reachable in EACH of the three states** — the escape affordance after M-49 retired the nav header (bottom-tab headers never had a back button; the drawer + tab bar are the escape); (c) an **RT sweep** (OS Reduce Transparency + Settings toggle) confirming the cards render the *designed opaque state* (row overlay + border), never a low-contrast smear, and the stage stays under RT; (d) a **VoiceOver walk** — the Remove/Dismiss moderation buttons still reachable (the card is not collapsed), the admin gate + denied copy unchanged, and the new `role=header` title + "ADMIN" eyebrow read cleanly. NOTE: reaching Admin requires an authenticated *admin* session, so there is no web/static-export capture (auth wall + credentials) — this leg is device-only, gated/low-traffic. **This is the FINAL leg of the T2 train (MP0→MP5 complete, conservation 15/15).**
- **BP10 → R2-D15 (contributes — the auth-fenced grammar surfaces, the phase's named device gate):** Sky signs in, both palettes, VoiceOver — walk (a) the **Profile** nearest-unresolved pill ("· Severity 3 · Moderate"); (b) **MyReports** meta ("Severity 3 · Moderate · date") + its now-spine a11y; (c) **MyWatched** — the colour dot is now a NUMBERED disc (new sighted channel); (d) the **FlagDetail** chip ("Severity 4 · Significant") + the quiet stake line below it — VoiceOver hears "severity 4 of 5, Significant" then the stake sentence; (e) the **Tasks** nearest-open-barrier pill ("· Severity 3 ·") + its a11y via `severityA11y`/`speakDistance`. Also (unauth, but no harness capture this session): (f) **Onboarding slide 2** — the five-disc "Legend in miniature" row on the dark gradient reads well and announces as ONE node "Severity scale — 1 Minor to 5 Severe"; (g) **Home Recent** — the 24px numbered disc + the realigned separator (marginLeft 52). RT leg folds under R2-D6. Ratify the PROPOSED strings (evidence table) into §A.

- **BP11 → R2-D13 (contributes — the one felt press dialect):** iOS device, both palettes, RM on/off — press-and-hold each family and confirm ONE felt dialect: (a) a **drawer row**, a **Map tool** (Search/Legend/zoom/FAB), a **filter chip** (Map panel + Tasks), a **modal close**, a **FlagDetail action** — each answers the finger with the static fill-swap dim (motion-free), the label/icon staying legible on the dimmed fill; (b) a **tab press** fires the selection haptic — tabs are **HAPTIC-ONLY** by design (no visual dim; the near-floor labels can't take one), but VoiceOver must still announce the active tab as *selected* (the PlatformPressable a11y-forwarding fix — confirm on device that the current tab reads "selected"); (c) **RM ON** → the dims still answer (backgroundColor swaps, zero motion) while the scale spring is skipped; (d) active/brand controls deepen (`ctaFillPressed`) and never grey; (e) on **web** (browser, not the RN device): the callout "Open details" reaches 44px and answers `:hover`/`:active` (#0F53BE) + a `:focus-visible` ring, and PressableScale controls show the hover tint. Transient press-state feel is inherently device/browser-gated (per BP3). Confirm the `ctaFillPressed` token + the colored-CTA exceptions + the 5 parked Tasks residual dims (§PARKING-LOT).

- **BP12 → R2-D16 (contributes — the status ledge on device):** iPhone VoiceOver, both palettes, RM on/off. Trigger a status beat (submit a report → "Report filed — thanks for flagging this barrier"; or stall connectivity → "Still trying — check your signal") and confirm: (a) on a header-bearing tab (**Home** — the `9 barriers` headline · Profile · Settings · Admin) the pill **docks below** the editorial header, not over it; (b) it announces **once** — no double/dropped announce — and the reposition preserves SR announce timing; (c) the **menu circle is fully tappable** through the pill (inner pill box-none); (d) on **Tasks** (opted out) and **Map** (no header) the pill keeps today's placement; (e) two-vehicle case — if the away-points toast and a "still trying" pill coincide, they **stack** (voice on top, toast below), never superimposed; (f) scroll a docked pill's screen — it stays put (scroll-invariant HEIGHT, not screen-Y). Placement/occlusion is inherently device-gated (native insets).
- **BP13 → R2-D17 (contributes — the honest no-location arrival):** native "Not now" (undetermined) arrival shows the polite no-location hint; native REAL-denial arrival fires the assertive S4 banner; VoiceOver hears ONE arrival announcement (reconciled with the auto-open Nearby sheet); the no-location arrival shows REAL flags in-viewport (a TRUE bounds-fit frame), not the empty San-Francisco default. Closes 02 honesty-ledger #16 (the denied-banner render question). Native first-run deny→arrival feel (D11 lineage). Device-only because the granted-geo static-export rig can't reach undetermined/null-location. **+ T9 (contributes):** Home headline reads "Loading…" (first load) / "—" (settled failure) / "N barriers" at Dynamic Type; the F5-02 settled-failure frame (Home + Map pill "Couldn't load flags") is unphotographed → eyeball; submit "Filing your report…" beside the spinner. The deep-link no-stomp fix + openReport-still-fits are code-inferred + adversarially-verified → a felt check on device is welcome (open a deep-link with location off; tap the Report pill with location off).
- **BP14 → R2-D7 + R2-D11 (contributes — the frame holds at size + the overflow scent):** iPhone, both palettes, RM on/off. (a) **F2-02/13 magnification** — set Dynamic Type ~1.3 (or Larger Text 200%) and open Home/Tasks/Profile/Settings: the display headline (e.g. "9 barriers") WRAPS to two dignified lines at the auto-fit floor, never truncates to "9 b…"; the Nearby "297 m" distance never orphans its unit across a wrap (R2-D7 lineage). (b) **F2-11 window** — on a 376–390pt device at fontScale 1.10–1.14 (the gap between M16's ≤375 and ≥1.15), open Tasks and confirm the per-card "Resolved" action label stays inside its pill (no code change; `isCompactLayout` threshold PROTECTED — this window is the registered watch). (c) **F2-05/06 header family** — Map's menu + Feedback circles read as one right-pinned pair (menu no longer floats mid-air) and the map still pans/zooms through the gap between them; Settings' eyebrow now aligns with its section rows at the single container inset (double-inset gone); both themes. (d) **F2-07/08 overflow scent (R2-D11 lineage)** — on a narrow width / large type, each overflowing chip rail shows the right-edge fade cue and it HIDES when scrolled to the end: Map filter Categories + Saved-sets, Report templates + categories, Tasks category strip, Nearby tablist, and the Map action bar; **confirm the light-theme fade actually reads over the (near-white Positron) action bar** — the F2-08 efficacy eye check the Playwright rig couldn't run here.

- **BP15 → R2-D (contributes; folds toward R2-D3/D11/D14):** on-device, both themes, RM on/off — (a) **drawer open→close (motion):** the panel and scrim leave together as one welded object (~180ms `motion.duration.base`), no white flash, and the sub-screen (Resources/How-to-help/About) appears right as the drawer finishes closing (the exact native risk the latch fixes — spring-vs-unmounted node, reopen start-position); (b) **immediate reopen** after a close starts clean (panel from off-screen, no half-state); (c) **RM on:** drawer close is instant snap, no dead wait before a sub-screen; (d) **warm path (the deferred-guard proxy):** open the drawer → tap Settings → no interstitial on a normal network (the dressed skeleton shows only on a cold/slow load) — the device-observable stand-in for the deferred drawer-open import-once jest guard; (e) **guest Profile** (sign out / web guest): the header family matches Home/Tasks (eyebrow PROFILE, title, subtitle, menu + feedback circles), reads correctly in **both** light and dark, and the sign-in CTA sits below — an eye-pass. Native VoiceOver spot-check of the guest header's eyebrow/title/subtitle + the two 44pt actions folds into R2-D3.

- **BP16 → R2-D3 (contributes — the announce-gate spoken leg):** iOS VoiceOver, on the **Settings** screen (which keeps the replay modal permanently mounted) — (a) with the tutorial CLOSED, **no phantom "Step 1 of 3"** is spoken as Settings renders (the mounted-but-closed modal is now silent); (b) tap "Replay tutorial" → hear **"Step 1 of 3" exactly once** on open; swipe forward → "Step 2 of 3" / "Step 3 of 3" each once; close and reopen → "Step 1 of 3" once (**never** a stale "Step 3 of 3"). Web+unit already prove it (jest 4/4 + 3 skeptics UPHELD); the native VoiceOver spot-check is the device confirmation. Also eye-only (any device pass): the **UpdateBanner** bell now renders as the Lucide line-Bell — the banner is auth+data-gated and uncapturable by the read-only rig, so its pixel look folds into a device glance (evidence/BP16/PROTECT-24.md). **No new copy shipped** → no string device leg (the T17/T18 strings ride to Sky's §A picks).
- **BP17 → R2-D18 (NEW — the containment leg) + R2-D15/D14/D6 (contributes — the hygiene eyeball):** on-device — (a) **R2-D18 (T20 containment):** VoiceOver on Profile → My Watched, swipe-explore stays inside the sheet until close, side-by-side with MyReports (jest proves the prop; device proves containment). (b) **R2-D15 (T19 marks):** the signed-out Profile brand LogoMark (~56pt, colour pin light / white knockout dark) + the small About-title mark read right both themes; the auth-fenced MyWatched orange-adjacency row — one amber Star ("watched") never reads as a sev-2 disc (structurally defused by the numbered discs); Android notification tint shows #1466E0 if an Android device is available, else code-verified hex parity. (c) **R2-D14 (T15 ink, a glance):** the light inactive tab labels read cleanly over dark photos / dark web tiles (the composites that failed AA pre-fix; arbiter-verified). (d) **R2-D6/D5 (T20 dialog fade):** the tier-explainer + delete-account dialogs fade in (were slide), RM on = no entrance motion — simulator-verifiable, a device feel is a bonus. **BP17 is the consolidator: the full R2-D0…R2-D18 gate + this leg + the BP1–16 §D bucket roll-up now live in `build-plan/DEPLOY-CHECKLIST.md`.**

## §L LAUNCH-SEQUENCING LEDGER (accrues; BP17 consolidates — see master §11 for the seed)

- *Seed: merge-by-prefix (Sky) → the ONE EAS TestFlight build (Sky) → the consolidated device gate. No web deploy, no DNS, no store submission.*
- **BP9: no new launch item** — but a milestone for the seed: with MP5 landed, the **T2 material train (MP0→MP5) is COMPLETE (15/15)**. The whole MP stack (`r2/mp0-first-frame`…`r2/mp5-admin-editorial`) is now ready for Sky's merge-by-prefix. BP17 consolidates the final launch ledger + device list.

## §PARKING-LOT (ideas that occur during building — recorded, NEVER scheduled; Sky promotes or kills)

- *Empty at authoring, by discipline.*
- **BP2 — the ~90 other `a11yToggle({ selected })` role=button sites app-wide** (MyReportsModal filter chips, Tasks mineOnly/catChips, etc.) still carry the Chromium-dropped `aria-selected` channel on web — same F1-03 defect class, OUT of this phase's named scope (the F1-03 core = the 5 Map families + Report). A future systemic sweep could migrate them to `pressed`.
- **BP2 — Tasks catChip (`TasksScreen.tsx:1005`) native double-speak:** it bakes ", selected, tap to deselect" into the label AND passes `a11yToggle({ selected })`, so iOS VoiceOver says "selected" twice. Pre-existing; a cleanup would migrate it to `pressed` (or web-gate the suffix).
- **BP2 — map action-bar + saved-places ring clip:** `actionBarScroll` (S16 fade) + `placesRowScroll` horizontal scrollers clip the focus ring to L/R edges (a visible indicator remains → meets the Map "visible indicator on an identifiable control" end-state). Full four-edged ring there = a follow-up (same headroom recipe, but the action bar's tuned S16 fade + onScroll metrics want care).
- **BP3 — FlagDetailModal error-haptic asymmetry:** the modal's `runStatusChange` catch shows a visible `notify()` dialog on failure but does NOT fire `hapticNotify('error')`, unlike the Tasks card's `setStatus` catch (which BP3 gave the error haptic). Surfaced by the BP3 adversarial haptic-skeptic; OUT of BP3's diff scope (FlagDetailModal.tsx is not edited this phase). A one-line follow-up would add the error haptic there for cross-surface parity.
- **BP4 — PhotoLightboxModal dead `closeBtnText` style:** `:155` still carries `color.surface` (the ~1.3:1 dark drift M-46 fixed on the live sites) but is unused — the close button renders the `<X>` icon. Left untouched (not a named M-46 site, not an arbiter stack). A hygiene pass could delete the dead style or re-ink it to `textOnBrand` for consistency.
- **BP5 — Home "Try again" retry button, white on `color.brand` = 3.42:1 (dark):** surfaced by the BP5 ink-skeptic. Pre-existing + byte-identical (`retryBtn`/`retryText` are context-only in the MP1 diff) — a self-contained error-state CTA whose contrast is against its OWN brand fill (independent of the now-glass card behind it), so OUT of MP1's material-only scope, and the spec keeps self-contained CTAs on their opaque token as a pin. Same latent `color.brand`+white pairing MP0 fixed on the M-52 View button (`brand`→`ctaFill`, killing a 3.42 drift). A future CTA-token sweep could re-ink Home's retry to `ctaFill`; recorded, not scheduled.
- **BP7 — Leaderboard close-`X` icon (`color.textMuted`) vs the other three modals' `X` (`color.text`):** a pre-existing cross-file chrome inconsistency; the X sits inside the opaque `surfaceNeutral` `closeBtn` circle (not on-glass sheet text), so OUT of MP3's on-glass scope. A future chrome-icon sweep could align it.
- **BP7 — Leaderboard `closeBtnText` dead style:** unused (the close button renders the `<X>` icon). Pre-existing; a hygiene pass could delete it.
- **BP8 — FlagDetailModal `reopenSubmitBtn` amber fill (`:~1992`):** `backgroundColor: color.accentOrange` with white `textOnBrand` label (≈1.9:1) — a SEPARATE white-on-amber case the MP4 spec deliberately did NOT name (its reopen re-ink covers only the outlined trigger's border + text `:1943/:1946`). Left as-is (material-only + spec-authority discipline). A future AA sweep could re-ink the submit fill to `ctaFill` (or make the button outlined-brand) for parity with the now-`brandText` trigger; recorded, not scheduled.
- **BP8 — FlagDetailModal dead styles `commentsEmptyText` + `watchBtnGlyph`:** unreferenced in JSX (the empty state renders `commentsEmptyLabel`; the watch icon is the lucide `<Star>`, `accessibilityElementsHidden`). Left untouched (not on-glass, not live). A hygiene pass could delete them.
- **BP8 — the BP3 FlagDetailModal error-haptic follow-up STAYS parked:** MP4 edited FlagDetailModal but MATERIAL-ONLY, so the `runStatusChange` catch still lacks `hapticNotify('error')` (see the BP3 parking-lot entry above). Out of MP4's scope; still open for a behavior-layer phase.
- **BP9 — the now-inert shared nav-header block in RootNavigator:** after M-49 every tab is `headerShown:false`, so the `screenOptions` header config (`headerStyle`/`headerTitleStyle`/`headerTintColor`/`headerTitleAlign`/`headerRight`) + `renderHeaderRight` (the Feedback button — now lives in-screen via `HeaderActions` on every editorial screen) never render. Left in place — `renderHeaderRight` is still *referenced* in `screenOptions` so it is not lint-dead, and deleting the block is a larger change MP5's material-only scope does not mandate. A future chrome-hygiene pass could remove the dead nav-header config + `renderHeaderRight` + the `feedbackBtn`/`feedbackBtnText` styles + the now-orphaned `headerBg`/`headerFg`/`headerBorder`/`headerBtnBg` token references.
- **BP10 — two un-enriched SPOKEN-label holdouts in the severity spine** (adversarial-skeptic surfaced; EQUAL to pre-BP10, NOT a regression): `NearbyFlagsModal.tsx:131` the card's `accessibilityLabel` hardcodes bare `severity ${item.severity}` (no "of 5", no word), and `LegendModal`'s row label is a bespoke shape — neither routes through `severityA11y`. BP10 swapped their VISUAL discs (in the WHERE list) but left these labels byte-identical (out of scope). A future spine sweep could route both through `severityA11y` for one spoken voice. Recorded, not scheduled.

- **BP15 — HamburgerDrawer animation effect has no cleanup to stop an in-flight close animation** (adversarial-skeptic surfaced; UNREACHABLE, not a leak): if the drawer unmounted *mid-close*, the exit `Animated.parallel` completion callback firing `setRendered(false)` afterward would log a dev-only "setState on unmounted component" warning. Unreachable in practice — `DrawerHost` (RootNavigator) is a permanent single mount that never unmounts during app life — and it is a transient dev warning, not a leak (the animation/callback is GC'd within ≤180ms). A future robustness pass could add `return () => { slideAnim.stopAnimation(); fadeAnim.stopAnimation(); }` to the effect. Recorded, not scheduled.
- **BP16 — stale comment at `SettingsScreen.tsx:644`** (adversarial regression-skeptic surfaced; PRE-EXISTING, not introduced by BP16, zero runtime effect): the comment says "App.tsx mounts OnboardingModal on first launch" but App.tsx actually mounts **OnboardingCards** (the 5-card first-run deck) — OnboardingModal is the Settings *replay* modal only. A future doc-hygiene pass could correct the comment. Recorded, not scheduled.

## §A ANSWERS + APPLIED-PICK LOG (Sky's answers land here; executors log "default applied" entries with phase + date)

- *Empty at authoring.*
- **default applied (BP1, 2026-07-16):** S-9 base default exercised — base = `a8549ff` (`bench/4-quality`; bench NOT merged to main, so the default holds; three markers verified there). · S-10 model law honored — executed Opus 4.8 ultracode max effort, all sub-agents max (incl. the 3-lens adversarial verify); phase authored Fable 5 max — provenance disclosed in the evidence report.
- **default applied (BP2, 2026-07-17):** S-7 exercised — the Tasks badge stays open-only + GLOBAL/unfiltered (writer 1 = `computeTasksBadge`; writer 2 deleted); NOT made filter-aware. · S-10 honored — executed Opus 4.8 ultracode max; phase authored Fable 5 max, provenance disclosed.
- **executor decision (BP2, 2026-07-17) — VETOABLE:** exclusive pickers (Report severity/category, Map min-severity/distance/saved-set) use `a11yToggle({ pressed })`, NOT the spec's literal "tab/checked OR label-baked" menu. Rationale (red-team-backed): `pressed` keeps the native `selected` trait (existing severity `.accessibilityState.selected` tests stay green), gives web a real cross-browser state, avoids the Firefox `aria-selected`+baked-label double-announce, needs no `Platform.OS` branch, and one mechanism across multi+exclusive is more "one voice"; a bare role=tab/radio carries the same roving-tabindex/keyboard-mismatch the skeptic rejected for radio. → Sky may write: use label-baked/tab instead (would re-touch the 3 exclusive families).
- **default applied (BP3, 2026-07-17):** S-8 exercised — every new/changed user-facing string is PROPOSED-only (evidence §4): the `'Resolved'→'Resolve'` verb flip at `TasksScreen.tsx:1549` + `FlagDetailModal.tsx:1436`, with the getByText test-lockstep list (`TasksScreenFlagCard.test.tsx:128/157/170`) — recorded for BP16's copy gate, ships nothing here. · S-9 base confirmed — cut from `r2/bp2-perception-floor` tip `373c582` (== main == origin; Sky ff-merged the off-main stack). · S-10 honored — executed Opus 4.8 ultracode max, all sub-agents max (incl. the 4-skeptic adversarial verify); authored Fable 5 max — provenance disclosed.
- **executor note (BP3, 2026-07-17) — the ONE shipped visible string:** the severity-4 photo nudge changed **"major"→"significant"** (`ReportFlagModal.tsx:953` + the two a11y announce sites). This is NOT new copy — it retires an invented word ("major" is absent from the taught `SEVERITY_LABELS`) for the app's own severity-4 label, shipped as a mechanic per the report (commit-plan item 3). Severity-5 ("severe") unchanged. Flagged for Sky's eye.
- **default applied (BP3, 2026-07-17):** F4-01 scoped to the badge-doubling leg only — `decorativeProps` on the Legend `sevDot` (the only sibling carrying a text digit; the 3 icon/dot swatches have no glyph to double). The color-name-vocabulary SR leg is **PARK-with-direction** per the absorbed FIX (evidence §4) — parked, not dropped.
- **executor pressed-idiom note (BP3, 2026-07-17):** the pressed dim uses the app's own `color.borderPressed` fill-swap (FilterPresetsModal/MyReportsModal/LeaderboardScreen), NOT the PhotoGallery `thumbPressed` the spec named alongside `removeBtnPressed` — `thumbPressed` is `opacity:0.75` (group opacity), the exact thing the absorbed FIX forbids on a text-bearing control. Arbiter-proven AA both schemes.
- **default applied (BP4, 2026-07-17):** S-11 train law honored — cut from the BP3 tip `db61189` (per §P clearance + ordering law L4); linear MP0, built+green+STOPPED on `r2/mp0-first-frame`; zero `theme.ts` edits; `GlassSurface.tsx` untouched; 7 immutable stacks diff-checked; RT + C-lite shipped as specced (`forceEngineered` = engineered-by-construction, `variant` opaque state = the designed RT state); Sky merges; Sky owns D-gates. · S-5 honored — M-46/M-51 ratified rows migrated ink-only, classification unchanged. · S-10 honored — executed Opus 4.8 ultracode max; authored Fable 5 max — provenance disclosed.
- **executor note (BP4, 2026-07-17) — dead style left out of scope:** `PhotoLightboxModal.closeBtnText` (`:155`) still carries `color.surface` but is **unused** (the close button renders the `<X>` icon). Not one of M-46's three named live sites, not an arbiter-declared stack — migrating it would be an unrequested change. → §PARKING-LOT candidate for a future hygiene pass.
- **default applied (BP5, 2026-07-17):** S-11 train law honored — cut from the BP4/MP0 tip `c4d484f` (per §P clearance); linear MP1, built+green+STOPPED on `r2/mp1-home-stage`; zero `theme.ts` edits; `GlassSurface.tsx` + `ScreenStage.tsx` untouched; 7 immutable stacks diff-checked; RT + C-lite shipped as specced (`forceEngineered` = engineered-by-construction on the pill/card, `variant` opaque state = the designed RT state, the stage stays under RT); Sky merges; Sky owns D-gates. · S-5 honored — Home was a MIGRATE row, no ratified-tier reclassification; the CTAs/pins it contains keep their opaque tokens. · S-10 honored — executed Opus 4.8 ultracode max, all sub-agents max (incl. the 4-skeptic adversarial verify); authored Fable 5 max — provenance disclosed.
- **executor decision (BP5, 2026-07-17) — D1, REVERTIBLE:** the search pill's clear-`X` icon re-inked `textMuted`→`inkGlassMuted` alongside the enumerated leading Search icon (GLASS §7.4 bans muted faces on glass; both sit in the now-glass pill). Not a separate arbiter pair (same token+backdrop as the leading icon, 8.01/8.32). → Sky may revert the one line if the X should stay `textMuted`.
- **executor decision (BP5, 2026-07-17) — D2, the no-hydrate call:** Home threads `useGlassMode()` READ-only (`glassLite`) and does NOT call `hydrateGlassMode` — the spec's "no data paths" rule. Consequence: a lite-mode user who cold-launches straight to Home sees the list card in full-blur until Tasks/Profile hydrates the shared module store. Ephemeral A/B preference (hidden long-press, slated for later removal). → Sky may add a one-line mount effect if Home should honor the persisted lite pref on first paint (deferred as out-of-material-scope).
- **default applied (BP6, 2026-07-17):** S-11 train law honored — cut from the BP5/MP1 tip `31086fd` (per §P clearance); linear MP2, built+green+STOPPED on `r2/mp2-profile-lists`; zero `theme.ts` edits; `GlassSurface.tsx` untouched; 7 immutable stacks diff-checked; RT + C-lite shipped as specced (variant opaque state + `forceEngineered` engineered-literal); Sky merges; Sky owns D-gates. · S-5 honored — the three sheets were MIGRATE rows; their opaque inner cards + semantic pills keep their tokens (no ratified-tier reclassification). · S-10 honored — executed Opus 4.8 ultracode max; authored Fable 5 max — provenance disclosed.
- **executor decision (BP6, 2026-07-17) — cardWrap up-shadow, REVERTIBLE:** the three sheets carried NO shadow pre-migration; the §3 recipe text + all four shipped bulk siblings (About/Feedback/Help/AddressSearch) carry an up-shadow on the wrapper, so it was applied verbatim (`HelpModal.tsx:224` values) to complete the convergence on the About/Feedback material — a visible change beyond pure ink. → Sky may revert per sheet (delete `cardWrap`, restore the top-radii on `card`).
- **executor note (BP6, 2026-07-17) — forceEngineered:** MP2 sheets ship engineered-literal (unlike the round-1 blurred B4 siblings) per §2a/§2f so Profile's blur budget stays +0; identical declared AA floors — the frost-feel delta over live content is the R2-D14 device read.
- **default applied (BP7, 2026-07-17):** S-11 train law honored — cut from the BP6/MP2 tip `828f736` (per §P clearance + ordering law L3); linear MP3, built+green+STOPPED on `r2/mp3-overlay-rest`; zero `theme.ts` edits; `GlassSurface.tsx` untouched; 7 immutable stacks diff-checked; RT + C-lite shipped as specced (variant opaque state + `forceEngineered` engineered-literal); Sky merges; Sky owns D-gates. · S-5 honored — the four sheets were MIGRATE rows; opaque inner rows/pills + semantic notices keep tokens (no ratified-tier reclassification). · S-10 honored — executed Opus 4.8 ultracode max, all sub-agents max (incl. the 4-skeptic adversarial verify); authored Fable 5 max — provenance disclosed.
- **default applied (BP7, 2026-07-17) — Fork-12:** M-37 locked-row idiom ships the **accept-and-record DEFAULT** per its block — `rowDimmed {opacity:0.7}` KEPT; the pre-existing ≈3.0:1 locked `rowDesc` (textMuted on the opaque inner row) recorded, SR-mitigated by the row-level `accessibilityLabel`. The re-spec half stays Sky's fork — never built.
- **default applied (BP7, 2026-07-17) — cardWrap up-shadow + forceEngineered, REVERTIBLE:** same two revertible defaults as BP6 — the sheets carried an up-shadow via `shadow.eN` on the card (M-22 `e2`, M-24 `e3`) or none (M-37/M-42); the recipe moves/adds the up-shadow to `cardWrap` to match the shipped bulk siblings, and ships engineered-literal (`forceEngineered`) so the blur budget stays +0. → Sky may revert per sheet.
- **executor note (BP7, 2026-07-17) — the on-glass ≥500 weight bump:** the diff adds `fontFamily: font.family.bodyMedium` to 6 on-glass body styles (M-37 subtitle; M-42 subtitle; M-22 subtitle/name/stateText; M-24 titleSubtitle). This is the recipe's third documented axis (spec §3, MP2 precedent — ActivityFeed subtitle/emptyBody), NOT out-of-scope typography; the adversarial byte-identity skeptic flagged it against a literal "material+ink only" reading and itself confirmed it admissible. xs-meta (stateHint, both footers) gets `inkGlassMuted` ink-only (no weight), matching MP2's `sectionHeaderCount`.
- **default applied (BP8, 2026-07-17):** S-11 train law honored — cut from the BP7/MP3 tip `db59980` (per §P clearance + ordering laws L3/L4 — the recipe was proven on 6 simpler sheets before the 82 KB FlagDetail, and BP3's a11y-label settlement is in the lineage); linear MP4, built+green+STOPPED on `r2/mp4-trust-ledger`; zero `theme.ts` edits; `GlassSurface.tsx`/`ThemeContext.tsx` untouched; 7 immutable stacks diff-checked; RT + C-lite shipped as specced (variant opaque state + `forceEngineered` engineered-literal); Sky merges; Sky owns D-gates. · S-5 honored — the two modals were MIGRATE rows; inner-opaque inputs/chips/bubbles + PROTECT-4 severity/status badges keep their tokens (no ratified-tier reclassification). · S-10 honored — executed Opus 4.8 ultracode max, all sub-agents max (incl. the 4-skeptic adversarial verify); authored Fable 5 max — provenance disclosed.
- **default applied (BP8, 2026-07-17) — O-2 (⚑ reopen re-ink):** §A carried NO veto → the DEFAULT proceeded per the block — FlagDetail `reopenBtn` border + `reopenBtnText` re-ink `accentOrange`→`brandText`, retiring the live 2.07:1 AA drift (arbiter-banked 4.95 light / 5.42 dark on detailSheet). If Sky prefers to keep the orange affordance, this is a one-commit revert (2 lines) and the 2.07 AA fail returns as a standing §F fork note.
- **executor note (BP8, 2026-07-17) — NO cardWrap (divergence from BP6/BP7):** unlike the MP2/MP3 sheets (which relocated an existing up-shadow to a new `cardWrap`), FlagDetail + StatusHistory carried NO shadow at HEAD, so per the spec §8 ("no shadow to relocate — the card has none at HEAD") the GlassSurface sits directly in the `backdrop` with no wrapper. Not a revertible default — it's the absence of an unneeded element.
- **executor note (BP8, 2026-07-17) — the on-glass ≥500 weight bump (scope):** `fontFamily: font.family.bodyMedium` added to only the 3 clean non-italic body-variant muted lines being re-inked (M-36 `commentsEmptyLabel`; M-41 `loadingText`+`emptyBody`), per the recipe's third axis (spec §3, MP0–MP3 precedent). The 2 italic hint lines (`commentsSoonText`, `reopenMessage`) got color-only re-inks to PRESERVE their italic (a custom Medium family would risk dropping the faux-italic); label-variant sites (already ≥500 semibold/bold) + the mono char-counter got color-only. `description`/`metaValue` (non-muted) left unweighted. The byte-identity skeptic confirmed the bump admissible under the recipe.
- **Sky answer (O-1 MP5, 2026-07-17): INCLUDE — build MP5.** Given by Sky in-window (this build session); transcribed here per the ledger-authorship rule (Sky is the sole author of decision ANSWERS; the executor records the answer verbatim). The O-1 gate (§O-1 / 00_master §6.7) therefore PASSES — BP9 proceeds with the M-23 + M-49 build, conservation targets 15/15. The prior UNANSWERED state (which would have stopped BP9 changed-nothing) is resolved.
- **default applied (BP9, 2026-07-17):** S-11 train law honored — cut from the BP8/MP4 tip `205108c` (per §P clearance + ordering law L3, the MP sub-train's contiguous final phase); linear MP5, built+green+STOPPED on `r2/mp5-admin-editorial`; zero `theme.ts` edits; `GlassSurface.tsx`/`ScreenStage.tsx`/`ScreenHeader.tsx`/`HeaderActions.tsx`/`ThemeContext.tsx` untouched; 7 immutable stacks diff-checked; RT + C-lite shipped as specced (`variant` opaque state = the designed RT state; `forceEngineered` = engineered-literal by construction); Sky merges; Sky owns D-gates. · S-5 honored — Admin was a MIGRATE row (M-23/M-49); the sevPill (PROTECT-4) + Remove/Dismiss semantic solids + spinners keep their tokens (no ratified-tier reclassification). · S-10 honored — executed Opus 4.8 ultracode max, all sub-agents max (incl. the 4-skeptic adversarial verify); authored Fable 5 max — provenance disclosed.
- **executor decisions (BP9, 2026-07-17) — REVERTIBLE / verify-first:** (a) the ScreenHeader mounts as the FlatList `ListHeaderComponent` per the spec's explicit Stage-6 grammar choice (scroll-away, budget 1→1, no third fixed-View-above-list); (b) section cards self-inset via `marginHorizontal: spacing.xl` to align under the header, replacing the old container `padding: spacing.lg` (net card content ~16px narrower — revertible); (c) the empty state renders the header at top with the "all caught up" message centered below (`emptyInner` flex:1); (d) `:170` coord meta → `inkGlassMuted` — the one on-card muted site the spec's 5-item stage-direct list does not name explicitly; it sits on the row material, so it follows the §3 recipe + MP1 row precedent (arbiter-confirmed on the banked M-06 pair); (e) description → `font.family.bodyMedium` (the recipe's ≥500 on-glass-body third axis, MP0–MP4 precedent); (f) `onFeedback` wired to `setOpen('feedback')` via `useSharedModals` (HeaderActions requires BOTH handlers; preserves Admin's prior nav-header Feedback affordance). → Sky may revert any layout/ink call.
- **executor note (BP9, 2026-07-17) — a11y tree restructure (transparency, skeptic-surfaced, not a refutation):** the menu/Feedback/title move from the OS nav-bar header into the in-screen `ScreenHeader`, which adds an `accessibilityRole="header"` title + an "ADMIN" eyebrow. This IS the declared M-23/M-49 change; the same menu label ("Open navigation menu"), the same Feedback control ("Send feedback"), and the screen title survive with equivalent affordances. No logic/behavior/data path changed; the admin gate + denied copy + moderation buttons are byte-identical (PROTECT held).
- **default applied (BP10, 2026-07-17):** S-2 honored — T5 is signature-★, executed at signature care (the ownable atom completed AND introduced, not garnish). · base per O-1=INCLUDE — cut from the MP5 tip `8a190a3` (the "cuts from the MP4 tip" clause fires only on the unanswered/SKIP path, which did not happen). · S-10 honored — executed Opus 4.8 ultracode max, all sub-agents max (incl. the 4-skeptic adversarial verify); authored Fable 5 max — provenance disclosed.
- **default applied (BP11, 2026-07-17):** S-2 honored — T3 is signature-★, executed at signature care (the ONE HAND leg made literal). · **S-1 REINTERPRETED — Sky-approved at plan time (AskUserQuestion #1 = "Fill-swap (house idiom)"):** the doc's "borrow `opacity: 0.85`" wording was consciously superseded by the FILL-SWAP (backgroundColor swap, ink full-opacity) — justified by the arbiter's ink-vs-fill framing + both PROTECT seeds being fill-swaps + BP3's test-enforced idiom + AA-safety; recorded so no reviewer rejects against literal 0.85 and no later hand reverts it to opacity. · **Scope = "unify within edited files" (AskUserQuestion #2):** converted the enumerated MapScreen-3 + FlagDetail-4 same-file opacity dims + presetBtn; left `cardPressed` (the borrow-model); 5 newly-discovered Tasks residual dims surfaced but NOT converted (beyond the enumeration — §PARKING-LOT). · **New token `ctaFillPressed` (`#0F53BE`, mode-independent)** — an AA-mechanics default, not a taste pick (no existing token deepens a mode-independent brand fill without breaking white-on-blue in dark mode; reuses the brand deep-end, mirrors borderPressed/successStrong); flagged for Sky confirm/revert (1 line each in theme.ts + ThemeContext.tsx). · **Tab = HAPTIC-ONLY** (arbiter-proven the near-floor tab labels can't take a dim) built on PlatformPressable after the **adversarial-caught `aria-selected` drop** (a real screen-reader blocker; fixed `9d1ff85`). · **Tasks Details `dimOnPress=false`** (inkDetailsGhost #1466E0 light = 4.03 on borderPressed). · savedAddPillText brand→brandText (sibling-consistent AA). · S-10 honored — executed Opus 4.8 ultracode max, all sub-agents max (incl. the 4-skeptic adversarial verify that caught the tab blocker); authored Fable 5 max — provenance disclosed.
- **default applied (BP12, 2026-07-17):** **S-6 honored** — the MINIMAL arbitration/stacking path (placement pub-sub + offset-below when both vehicles are live + inner-pill `box-none`); the full migration of TasksScreen's flash onto the region channel was NOT taken (Sky-optional, not this phase). Fork-2-coupled points strings never moved. · **Tasks opts out** (`publishLedge={false}`) — Sky AskUserQuestion #1 at plan time: on Tasks the top chrome is the composite glass pane (title+search+chips+sort, measured as `chromeHeight`), not the nested `ScreenHeader`, so publishing its title-block height would mis-dock the pill into the middle of the chrome; instead the pill keeps today's placement on Tasks and the inline points flash stays byte-identical. The ledge fix lands on Home (the `${flags.length} barriers` headline personas R1/R5/R6 cite) + Profile/Settings/Admin. · **Inline points flash left byte-identical** — Sky AskUserQuestion #2: it's a third vehicle at z-60 + `pointerEvents:none`, so it can never enter the z-50 slot; arbitrate only the two true z-50 colliders (LiveStatusRegion + FlashBanner). Strings/channel/timer/z untouched. · **S-2 honored** — T6 built Meaningful (well, not bigger). · **PROTECT-18** announce law byte-preserved (diff-proven — the sole removed line is the wrapper `top`). · **S-10 honored** — executed Opus 4.8 ultracode max; authored Fable 5 max — provenance disclosed. · **⚠️ concurrency (informational):** the BP11 agent's `eafd20e` duplicated onto this branch (content safe on bp11 `8acb184` via cherry-pick); the documented drop-remediation (`git rebase --onto 7de1f34 eafd20e r2/bp12-status-ledge`) is **left for Sky** — I did not rewrite it. See §P + `evidence/BP12-verification-evidence.md`.
- **Sky answer (BP10 Q1, onboarding treatment, 2026-07-17): UNFRAMED disc row on the gradient.** Given by Sky in-window during planning (AskUserQuestion). Slide 2 drops the translucent glass icon-circle; five ~32px discs (the Legend disc) sit as one row on the Deep Field gradient (`severityScaleRow` keeps the 112pt footprint so the slide's rhythm matches the framed siblings). Recorded per the ledger-authorship rule (Sky is the sole author of ANSWERS).
- **Sky answer (BP10 Q2, a11y-label scope, 2026-07-17): SYNC to the spine.** Sky chose to route the Profile + MyReports `accessibilityLabel`s through `severityA11y` (were lowercase/wordless) so SR hears the same grammar the eye now sees — extends PROTECT-25, uses the existing helper, no invented copy.
- **executor decision (BP10, 2026-07-17) — sep realign, REVERTIBLE:** HomeScreen `sep` divider `marginLeft` 40→52 so the separator sits under the row text now that the leading glyph grew 11→24px (paddingH 12 + disc 24 + gap 12 = 48). → Sky may revert the one value if the old inset is preferred.
- **executor note (BP10, 2026-07-17) — captures deviation, HONEST:** the static-export visual before/after re-renders were NOT produced this session. Rationale: the five load-bearing surfaces (Profile/MyReports/MyWatched/detail chip/Tasks pill) are auth-fenced and uncapturable unsigned (the spec assigns their eye pass to R2-D15); the rig skips onboarding; and mode-independence + zero-delta are proven MORE STRONGLY by construction (the primitive reads no theme-forked token; arbiter both-modes; render style-equality vs theme-independent constants) than by a screenshot. The visual pass folds into R2-D15. Declared, not papered over.
- **executor note (BP10, 2026-07-17) — the one tracked test co-change beyond the src-list:** `bp3TrustEngineGuards.test.ts` F4-01 assertion RELOCATED — it pinned Legend's now-removed inline `styles.sevDot` + `{...decorativeProps}`; rewritten to assert Legend renders `<SeverityDisc>` (decorative-by-default, guarded in the bp10 suite) with no `styles.sevDot`. Invariant preserved, mechanism moved — a legitimate refactor co-change, not scope creep.
- **default applied (BP13, 2026-07-18):** S-8 honored — every new string PROPOSED + tabled (`NO_LOCATION_HINT`, "Loading…", "Couldn't load flags", `RETRY_VERB`/"Tap to retry.", "Filing your report…"); none pre-carried a §A pick → all ride to the evidence table + BP16. · S-10 honored — executed Opus 4.8 ultracode max (Sky-initiated in-window completion of the stranded run); the adversarial verify ran on Opus high effort (4 parallel skeptics + 2 re-verify rounds; provenance disclosed). · Fork 1 boundary respected — the bounds-fit is presentation over already-fetched rows, no proximity query. · The "reports" vs "flags" term in `NO_LOCATION_HINT` resolved to "flags" (matches the shipped DENIED banner voice) — a PROPOSED default; Sky's final at BP16. · **openReport excluded from the fit-retire** (adversarial follow-up, REVERTIBLE): it moves no camera, so a null-location Report-pill arrival still earns the honest fit — logged here.
- **executor note (BP13, 2026-07-18) — the stranded-run reconciliation, HONEST:** this phase was NOT a clean fresh-window run — a prior BP13 window had left T7 item 1 (voice) complete + item 2 (frame) half-built (native `snapToRegion` primitive only) uncommitted in the working tree. Per the RESUME RULE the stranded work was verified + kept (never redone), the missing halves (web `snapToRegion` impl + all MapScreen fit wiring) + items 3-6 built, and the whole reconciled into 5 clean commits. Sky authorized the in-window completion (Morgan-briefed, then executed here).
- **default applied (BP14, 2026-07-18):** S-3 honored — the cheap-win inset scope only (Settings header-zero + Map HeaderActions box-none regroup + Map chip token snap); **Profile OUT of the edit set** (`git diff 6e8e636 HEAD -- ProfileScreen.tsx` empty), NO single-20 family law. · S-10 honored — executed Opus 4.8 ultracode max (Sky-initiated + plan-approved); the adversarial verify ran 3 parallel skeptics on Opus max (provenance disclosed). · S-8 honored — BP14 introduces **no new user-facing copy** (the NBSP swaps the space INSIDE the existing "N m"/"N.N km" strings; visible glyphs identical; `speakDistance` byte-identical) → nothing to table. · **`MAP_HEADER_ROW_MARGIN_BOTTOM = 10` PARKED** (executor decision, REVERTIBLE): the F2-12 commit-plan named both `mapHeaderChip 14` and `mapHeaderRow 10` for snapping, but the `10` is a module const load-bearing in T1's chrome-band / callout-clearance math (`MapScreen:534`) with no clean token (equidistant sm↔md); snapping is self-consistent but pure churn against a calibrated value and would entangle this commit with T1 — parked with a written reason (**not** silently dropped); if Sky wants it, `spacing.sm` (8) with a T1 re-verify. · **F2-08 = ship the shipped `rgba(15,27,45,0.22)` UNCHANGED, no arbiter** (executor decision): the ink is the ratified S16 affordance reused unchanged and every graft backdrop is ≥ the action bar's floored engineered-glass over Positron; the live efficacy probe couldn't run (Playwright absent) → code-inferred + device-gated (§D-d). · **Settings inset zeroed HORIZONTAL-ONLY** (not Profile's all-three-zero): keeps the header's vertical rhythm (paddingTop sm / paddingBottom md) intact; the "adopt Profile's convention" comment means the zero-own-padding idea, horizontal only. · **Captures deferred** — the Playwright capture rig is unavailable in this environment and the on-disk `dist/` predates BP14; the home/nearby zoom200 + Settings + chip-fade frames fold into Sky's device/eye gate (§D); code verified by the gates + 15 structural guards + 3/3 adversarial UPHELD.
- **default applied (BP15, 2026-07-18):** S-8 honored — BP15 ships **no new user-facing copy** (the 5 guest strings — eyebrow "PROFILE", title "Your profile", subtitle, aria-label "Sign in to your account", button "Sign in" — all pre-existed VERBATIM at `edfcb08`, md5-verified; the title is the signed-in header's reachable display-name fallback) → nothing to table. · **O-5 respected** — the T12 optional status line 'Opening Settings — one moment' is PROPOSED-only, NOT shipped → rides to the BP16 table, so `r2-settings-arrival-stacks.json` was skipped this phase. · S-10 honored — executed Opus 4.8 ultracode max (Sky-initiated + plan-approved via ExitPlanMode); the adversarial verify ran 5 parallel skeptics on Opus max (one re-run as a plain-text pass after a StructuredOutput tooling cap; provenance disclosed). · **Absorbed-FIX laws applied:** T12 — the `220`→`motion.duration.base` collapse shipped in the SAME commit as the exit-latch (`9fb637d`), never a bare collapse; the RM=0 test lines byte-intact. T10 — the F5-06 authLoading skeleton leg was DROPPED (runtime-unreachable — adversarially confirmed, code-inferred), and the guest header overrides only `eyebrowColor`/`subtitleColor` (no `titleColor` prop exists — spec over-specified). · **Fork 3 (auth-wall/guest) boundary respected** — BP15 designed the guest STATE render only; same capability, same button, same modal, no new gate (the decision half stays Sky's). · **Two testability extractions** (`ScreenFallback` → own module; guest state → `GuestProfile.tsx`) — behaviour-preserving (same rendered tree relocated), logged as an executor decision. · **One guard deferred with reason** (drawer-open import-once — dynamic `import()` not cleanly mockable under jest-expo; typechecked + device-observable), NOT silently dropped.
- **default applied (BP16, 2026-07-18):** **S-8 honored** — every T17/T18 string is PROPOSED via the before/after table (38 rows, `evidence/BP16-verification-evidence.md` §THE TABLE); NOTHING shipped — the phase STOPPED at the table (§A carried no BP16 pick + no Jordan sign-off was recorded at build time → zero strings shipped). · **O-5 honored** — the table is produced; Sky's per-string picks + a recorded Jordan Art. 7 sign-off unlock the resume, which applies EXACTLY the picks with test-lockstep. · **S-10 honored** — executed Opus 4.8 ultracode max; the adversarial-verify + table Workflow ran 8 sub-agents ALL `claude-opus-4-8` (5 skeptics → 5/5 UPHELD, 0 refutations; table-assembler + completeness-critic + Jordan); authored Fable 5 max — provenance disclosed.
- **executor note (BP16, 2026-07-18) — the two MECHANICS that DID ship** (behaviour/glyph, NOT strings, so not §A-gated): **T18 announce gate** (OnboardingModal — kills the phantom closed-mount "Step 1 of 3" on the always-mounted Settings modal; single `visible`-gated announce owner via `wasVisible`/`prevIndex`; keeps "Step N of M" wording) and **T17 Bell glyph** (UpdateBanner 🔔→Lucide Bell — decorative, reuses `brandOnSoft`, no new ink pair). Both adversarially verified 5/5 UPHELD. The 'Step'→'Card' wording flip is table row `step-card`, deferred with the rest of the copy.
- **Jordan Art. 7 (BP16, 2026-07-18) — k≥3 caveat, ADVISORY sign-off recorded for Sky's ratification:** semantic-equivalent = **YES** ("submitted"→"reported" is verb-canon; the k-floor count is status-agnostic so the counted set is identical; the dropped "To protect reporters" is rationale, not the guarantee; `DEFAULT_K_FLOOR` + k-mechanics untouched — Fork 4 WORDING lane only). **Non-blocking advisory:** consider folding a short "to protect reporters" clause into the single-source string so every surface GAINS the purpose (a strict strengthening). **Jordan is NOT a lawyer — this sign-off is advisory and requires professional legal review.** → For the resume to ship the k-caveat rows, Sky records this sign-off (or her own) in §A.
- **default applied (BP17, 2026-07-19):** **O-1 = INCLUDE** honored — MP5 built (conservation 15/15), so the T20 paper leg wrote **NO B14 deliberate-exception line** (the skip antecedent is false). **O-3 = DEFAULT** honored — the executor landed the GLASS.md/index.html paper commit as the caboose (`bd11d7d`); no "MY HAND" on record. **S-8 honored** — BP17 ships **no new user-facing copy** (LogoMarks add no text — About mark SR-hidden, Profile mark reuses LogoMark's baked "AccessMap" label; T15/T19 = token/comment; T20 = mechanics + docs) → nothing to table. **S-10 honored** — executed Opus 4.8 ultracode max (incl. the 6-skeptic adversarial verify); authored/planned Fable 5 max — provenance disclosed. **T15 ink arbiter-decided** (#515964, lightest clearing 4.5 w/ margin, no eye-tuning); **M-48 kill preserved** (mechanism untouched, only the ink repaired; trials file still exit-1-by-design). **PROTECT held** — GlassSurface untouched (diff-check), RM ternaries preserved (dialogTier + modalGate guards), no decorative emoji added (LogoMark is SVG).
- **Sky answer (O-4 dialog tier, 2026-07-19): UNIFY.** Given by Sky in-window (this build session, AskUserQuestion). The four-dialog tier unifies to the RM-gated FADE entrance + `shadow.e3` four-of-four (the ProfileScreen tier-explainer + delete-account migrate slide→fade; every RM ternary preserved). Recorded per the ledger-authorship rule (Sky is the sole author of ANSWERS). The RECORD-only fallback was NOT taken.
- **Sky answer (T19 accentOrange, 2026-07-19): COMMENT-ONLY.** Given by Sky in-window (AskUserQuestion). accentOrange stays `#f1a520`; the shared amber band with severity[2] (#F0A030 "Mild") is accepted (defused structurally by the BP10 disc grammar); the stale comment was corrected honest. No hue moved → no mockup gate fired. If Sky ever wants the hue-nudge instead, it is a fresh fork with an arbiter mockup.
- **BP16 — SKY'S PICKS LAND BELOW THIS LINE** (the resume reads them; full row detail = `evidence/BP16-verification-evidence.md` §THE TABLE): the 30 **mechanical** rows may be a blanket "apply all mechanical" or per-row; the **3 TASTE** rows need an explicit choice — **settings-retitle** (A: title→"Preferences" / B: eyebrow→"YOUR APP"), **register-rewrite-tasks-loadmore** (A plain / B em-dash), **register-rewrite-flagdetail-date** (A "Reported" / B "Reported on" / keep "Date"); confirm the **parked-bp13-filing** keep-exemption; decide the **Jordan advisory** (fold the purpose clause in, or not); and the critic gap **MapScreen:1217** (fold its second no-location wording into `parked-bp13-noloc`). → *Sky writes her picks here.*
