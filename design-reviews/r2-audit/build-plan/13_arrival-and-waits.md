# BP13 — The honest arrival + word every wait

**Fire this file ALONE in a FRESH window.** Model law: **EXECUTE on Opus 4.8 ultracode MAX EFFORT, all sub-agents max effort** (Sky's direction for this train). Authored on Claude Fable 5 max effort, 2026-07-15, in the same window that authored the synthesis — provenance is disclosed, never silent; tag it in your phase report.

**Branch:** `r2/bp13-arrival-waits` — cut from the tip of ``r2/bp12-status-ledge`` (the prior phase; its tip SHA is in DECISIONS §P — **if that entry is absent, the prior phase has not run: STOP**). Record your base SHA as this phase's rollback anchor.
**Carries (conservation):** T7, T9 — closes T7 (F4-03 + F5-03) and T9 (F5-02, F5-01, F5-05, F5-09). T7's device row closes 02 honesty-ledger #16 (the denied-banner render question) via R2-D17.
**Effort:** M + M.
**Purpose:** The no-location arrival speaks one voice over a viewport that shows real data (never a silent San-Francisco frame), and every wait has words — no settled failure ever claims a zero.

## 0 · READ FIRST (in order — no edit before all of these)
1. `design-reviews/r2-audit/build-plan/DECISIONS.md` — apply every §S standing decision and §O/§A entry named in §1 below; take your base tip from §P.
2. **THE REPORT (design authority):** `/Users/skypie/AccessMap/design-reviews/r2-audit/2026-07-10_AccessMap_R2_Design_Review.md` — your inlined block(s) below are copied from it verbatim (`### #10 · T7 —` + `### #7 · T9 —`); also read §4 (the PROTECT list + fork registry + R2-D ledger) and §5's phase spine.
3. The master: `design-reviews/r2-audit/build-plan/00_master.md` — §6 ordering laws that name this phase, §13 PROTECT spot list.
4. **VERIFY-FIRST:** every `file:line` in the spec was true at plan-authoring (audit HEAD `a8549ff`). Re-grep each cited anchor BEFORE editing; small drift → adapt and note it in your evidence report; meaningful drift (a mechanism moved or is absent) → **STOP** and surface to Sky (evidence report + DECISIONS §P note). Never build against drift.

## 1 · STANDING DECISIONS THAT BIND THIS PHASE (inlined — self-containedness includes decisions)
- **S-8:** new strings PROPOSED (the undetermined-arrival line + failure-recipe wordings go to your table + BP16 unless copy.ts strings already carry Sky's pick in §A).
- **S-10:** model law.

## 2 · THE WORK — the spec, inlined VERBATIM from the report (the "Skeptic verdict: FIX (absorbed)" line is PART of the spec)
### #10 · T7 — Finish the honest arrival — the undetermined voice · median 7.45
**WHERE:** MapScreen.tsx only (arrival effect region + banner overlay region + initial-region computation). location.ts, NearbyFlagsModal.tsx, OnboardingCards.tsx untouched.
**Resolves:** F4-03,F5-03 · **Effort:** M · **Tier:** Meaningful · **NEEDS-SKY-DEVICE:** yes
1. **WHAT** — Complete S4's honesty for the arrival S4 deliberately left silent: the undetermined no-location arrival (never-asked web guests — the default first-run state — and native "Not now" decliners). Two moves. (1) One honest, non-accusing line through the existing banner recipe, polite not assertive since nothing was denied; PROPOSED sketch, em-dash grammar: before = silence; after = "Location isn't on yet — showing the most recent reports, not ones near you." (never "is off": the S4 gate's own comment forbids telling a never-asked user access is off). (2) A true frame: with no usable location at arrival, fit the initial viewport to the loaded flags (bounds over already-fetched rows, instant initial region, no fly-in — RM-clean) instead of hardcoded San Francisco, so "Showing 9 flags" becomes visibly true.
2. **WHY** — Resolves F4-03 + F5-03 (both HIGH, skeptic-hardened: probes proved the banked "denied" captures depict the undetermined state, and that state is genuinely wordless once the auto-opened Nearby sheet closes). ONE VOICE: the arrival stops splitting into an honest suffix inside one sheet and silence on the map beneath it. Honesty is the thesis's hard floor — a confident count over a city the user isn't in is the inverted-honesty shape Round 1's S4/S6 existed to kill, still live on the trust mission's front door.
3. **PERSONA IMPACT** — R6 ("I don't live in San Francisco. Just say so") gets told, in the app's warm register. R1's "technically true, functionally misleading" count becomes functionally true — the nine flags are on screen. R2: the line rides the existing banner's live region, so the spoken arrival finally carries location context (today's tree has zero words about it). R3/R4: no layout or target changes. R7 first-class: the banner recipe is a solid surface, identical in RT — R7 gains an arrival whose frame makes sense at a glance instead of demanding pin-hunting across an empty default city.
4. **TRUST + COHESION** — The first sixty seconds of the largest cohort (guest web) currently show a false frame under a confident pill. After: frame true, words honest, one register from arrival onward. No new vehicle — the existing honesty banner learns one more true sentence, so the one-material read is untouched and the one-voice read strengthens exactly where first impressions form. The honest no-location suffix in Nearby is corroborated by the map instead of contradicted by it.
5. **A11Y + RESTRAINT** — Adds honesty and orientation; costs nothing — no target moves, no new pane, existing arbitrated banner stack. Preserves the honest no-location suffix (PROTECT: extend, never replace) and PROTECT-6's locating ladder. Bounds-fit uses an instant initial region — no animation, so the RM designed-stillness contract is honored by construction. The denied-arrival S4 banner remains byte-identical, assertive, and gated exactly as shipped. RESTRAINT: PASS — replaces a false frame with a true one; zero decoration.
6. **DEPENDENCIES / CONFLICTS** — Fork 1 (proximity/geo) adjacency: bounds-fit runs over already-fetched rows — presentation only; it must not introduce proximity querying or cross Fork 1's DECISION half. Fork 3 adjacency: no permission-structure change; S19's platform fork and the OS prompt flow untouched. Preserves MapScreen's box-none overlay law (banner lives in the existing overlay region). Shared file: MapScreen.tsx also touched by the failure-voice candidate and the map cluster's F2-01 callout work — sequence with the merge lead. String is PROPOSED; Sky signs the final wording.
7. **VERIFICATION** — Re-run the banked read-only probes (tools/probe-f403-denied-real.mjs, tools/probe-f503-skeptic.mjs) against the build: undetermined arrival renders the new line with flags in viewport; true-denied still fires the S4 banner (regression watch). Re-label the map__*__permission-denied captures as undetermined per the skeptic. A11y-tree re-walk: arrival tree contains the honest line, announced politely once. No ink change (existing banner stack), so no arbiter; if any tint shifts, run tools/r2-honest-arrival-stacks.json. Sky device row (this closes 02 honesty-ledger #16 in the paper record): native "Not now" arrival shows the line; native real-denial arrival fires the S4 banner; VoiceOver hears one arrival announcement.


**Skeptic verdict: FIX (absorbed).** the bounds-fit needs PlatformMap.tsx + .web.tsx (gotcha #3) — reuse T1's instant setView path, do not fork it; rows load ASYNC so seed DEFAULT_REGION at mount and fire a ONE-TIME unconditional instant fit on the FIRST flags-load while location is still null; guard it on no-gesture + plain no-param arrival (never stomp focusFlag/deep-link/openReport cameras) + empty/singleton fallback to DEFAULT_REGION; the undetermined banner is a NEW polite element reusing styles.banner INK only (the assertive denied banner stays byte-identical) — verify it announces on web or route through announce.ts; re-run S4's undetermined guard.

### #7 · T9 — Word every wait; never claim a zero · median 7.85
**WHERE:** HomeScreen.tsx (headline gate + error-card message source), MapScreen.tsx (status-pill fourth arm + error-banner visible retry verb), TasksScreen.tsx (shared failure recipe adoption), ReportFlagModal.tsx (submit-button wording), copy.ts (single-source failure presentation strings).
**Resolves:** F5-02,F5-01,F5-05,F5-09 · **Effort:** M · **Tier:** Meaningful · **NEEDS-SKY-DEVICE:** no
1. **WHAT** — The failure register gets the empty register's hand. (1) Honest counts: gate both census renderers on error — Home's headline never computes "0 barriers" on a settled failure; the Map pill's three-way ternary grows a fourth honest arm, sketch: "Couldn't load flags" (retry stays on the co-present banner). (2) Worded waits: Home's first-load headline says "Loading…" instead of the bare display-size em-dash (Map already words this wait); the submit button keeps words beside its spinner, sketch: "Filing your report…". (3) One failure recipe: Home/Tasks/Map route the same read failure through one presentation contract — icon + provider message + visible retry verb, sketch appending Tasks' "Tap to retry." — single-sourced beside offlineBannerText in copy.ts, the app's own one-true-string model. All strings PROPOSED via before/after table.
2. **WHY** — Resolves F5-02 (HIGH, skeptic-CONFIRMED: a false "zero barriers" census announced politely to SR users during failure is a direct trust-mission hit) with F5-01, F5-05, F5-09 riding naturally. ONE VOICE: one failure currently speaks three registers — Home generic, Tasks instructive, Map mute about its own tappability. ONE HAND: the F5 census shows the empty states got the master and the failure states got the apprentice; this is the apprentice's work redone by the same hand, in the em-dash grammar the app already owns.
3. **PERSONA IMPACT** — R1 (silent Home failure was a top-3 trust-reducer "right next to a 'still trying' state that proves the team knows how to do it right") and R6 ("the one screen I'd call 'maybe broken'") get worded waits and honest failure on Home. R2: polite live regions stop announcing a false census, and Map's banner finally says visibly what its a11y hint promises. R3: display-size "Loading…" beats a cryptic glyph at large type. R4: Map's failure banner gains a visible retry verb — reach parity with Tasks. R7 first-class: every failure vehicle is a toned solid, identical in RT — R7 gains worded waits above the static skeletons R7 called the app's warmest asset.
4. **TRUST + COHESION** — A barriers app asserting "0 barriers" at display size beside its own failure banner is self-contradiction — removing it is the cheapest credibility repair in this cluster. Converging three failure dialects onto one recipe makes failure a designed moment: same icon, same register, same verb, so failure stops being where the app audibly changes writers. The count pill becomes honest across all four states it can actually inhabit, matching the S11 ladder's honesty on the states around it.
5. **A11Y + RESTRAINT** — Improves the floor: no false polite announcements (WCAG 4.1.3 quality), a visible retry affordance on Map (reachability), words during every wait (cognition). Preserves the S11 read ladder as a unit — thresholds, strings, Retry byte-untouched; this candidate words the states around it. Preserves the Skeleton contract, PROTECT-2's recovery card, the submit-moment honesty sentence, and offlineBannerText discipline (extended, not altered). The F5-01 skeptic's library-retry posture (~7–9s window) is worded over, not re-engineered — no .retry() changes. RESTRAINT: PASS — words and honesty only.
6. **DEPENDENCIES / CONFLICTS** — Preserves the S11 ladder + its write-half twin (PROTECT) and the em-dash grammar in every proposed string. The Map pill is a GlassSurface consumer — threaded, never forked; forceEngineered tint law untouched, no intensity changes. Shared files: MapScreen.tsx (honest-arrival candidate + map cluster's F2-01), HomeScreen.tsx (spoken-voice candidate), TasksScreen.tsx (three candidates) — regions disjoint; merge-lead sequencing. No fork adjacency: no DB, no auth, no points values. Fence respected: submit-path wording is verified component-level, never by live submit.
7. **VERIFICATION** — Jest render-state matrix over {loading, error, offline, settled} × {Home headline, Map pill}: never "0 barriers"/"Showing 0 flags" while error && empty; "Loading…" during showFirstLoad; fourth pill arm exact-string; submit button renders "Filing your report…" while pending (fence-safe component test). Live-region assertion: the pill announces the honest arm politely. Grep guard: the failure recipe imports from one source at all three call sites. New strings ride existing arbitrated inks — no arbiter; if any new text-on-tone pair appears, run tools/r2-failure-voice-stacks.json. Re-capture home load-error plus the settled-failure frame F5-02 notes is unphotographed.


**Skeptic verdict: FIX (absorbed).** gate BOTH the Home headline and the Map error arm on `error && flags.length===0` (not `error` alone) so the SWR stale path keeps its cached rows; implement the Map banner's retry verb by APPENDING to the existing single AppText (mirror Tasks' '{msg}. Tap to retry.'), not a new child (preserves the M10 space-between + box-none footprint); the 'one recipe' is string/token single-sourcing in copy.ts, NOT a shared component.

### Commit plan (one commit per item, this order)
1. T7/voice: the undetermined-arrival banner as a NEW polite element (`accessibilityLiveRegion='polite'`, role='text') reusing `styles.banner` INK only — the shipped assertive role='alert' DENIED banner stays byte-identical; verify the polite line actually announces on web arrival (static aria-live often won't speak initial content in Chromium — route through the S9 announce shim `src/lib/announce.ts` or mount-empty-then-set if it doesn't); reconcile with the auto-open Nearby sheet for ONE arrival announcement.
2. T7/frame: seed DEFAULT_REGION at mount; on the FIRST flags-load while location is still null, ONE unconditional instant bounds-fit (animate:false — deliberately not RM-gated: it replaces the initial paint, it is not motion) — guarded: only a plain no-param arrival (never focusFlag :1165–1188 / flagId deep-link :1215–1280 / openReport :1193–1203), only if the user hasn't gestured (currentRegionRef still == DEFAULT_REGION), empty→DEFAULT_REGION fallback, singleton→fixed sane zoom. REUSES BP1's instant-camera path in BOTH PlatformMap variants (gotcha #3) — never forks it.
3. T7/guards: re-run S4's undetermined guard (updated to expect the new status-neutral line, still never 'access is off'); re-cite the banked probes at `design-reviews/r2-audit/tools/probe-f403-denied-real.mjs` + `probe-f503-skeptic.mjs`.
4. T9/gates: pin the Home headline (HomeScreen.tsx:185) AND the Map pill's new error arm (MapScreen.tsx:1463 ternary) to `error && flags.length === 0` (mirroring :315 / :1022) — the SWR stale path keeps its cached rows; no settled failure renders '0 barriers' or an em-dash-as-sentence.
5. T9/retry-verb: the Map error banner's visible retry verb lands by APPENDING to the EXISTING single AppText (mirror Tasks' `${msg}. Tap to retry.` :624–630) — no new child (preserves the M10 grouping + box-none footprint); keep accessibilityLabel/Hint wiring.
6. T9/one-recipe: string/token single-sourcing in `src/lib/copy.ts` (provider message + the retry verb + shared icon token) — NOT a shared component; Home/Tasks/Map keep their containers. F5-09's submit-verb keep-words per its block. PROPOSED wordings tabled.
7. Captures: arrival (undetermined, denied, granted) + settled-failure states, both themes; PROTECT before/afters.

### Phase-specific laws
- Fork 1 boundary: the bounds-fit is PRESENTATION over already-fetched rows — the proximity/geo-query DECISION stays Sky's; Fork 3 boundary: no permission-structure change; Fork 4 untouched.
- F5-01's library-retry posture (postgrest silent retry) stays a Sky-note — worded over, never re-engineered (the audit's own call).

## 3 · RAILS (adapted to this repo's discovered infrastructure — master §3)
- The report is the design AUTHORITY. Execute to its recorded taste; never invent a decision it already made; never re-litigate a standing decision (DECISIONS §S) or a settled device read (D9/D10).
- Fork discipline: UI/read halves only. Forks 1-13 are Sky's (proximity/geo · points economy · auth-wall/guest · k-anonymity · trust-model scope · product name · stagePoolB · dark saved-place chips · ui/Button · M-12/M-13 swap · bulk mechanism lever · M-37 locked rows · lightbox blacks). A decision half you brush against becomes a DECISIONS §F question, never a build.
- Honesty fences: no invented copy, claims, prices, or numbers. Every NEW user-facing string is PROPOSED (before/after in your evidence report) unless DECISIONS §A already carries Sky's pick; proposed status strings use the em-dash grammar ("state — next step").
- Hard territory (never crossed): `src/components/ui/GlassSurface.tsx` DO-NOT-EDIT (thread the primitive, never fork it) · MapScreen's `pointerEvents="box-none"` overlay law untouched · the blur budget never exceeded (assert at the worst SIMULTANEOUS state, +1 for the tab-bar pane invisible to `__getLiveBlurPaneCount`) · the RM designed-stillness contract + the 220ms delay-gate/pulse-token discipline · RN reality (no CSS-only tricks; expo-blur intensities 12/24 only; virtualization law) · zero Supabase/Storage writes · never merge, push, build, deploy, or send anything external — those are Sky's hands, always.
- Evidence honesty: tag every claim verified / web-approximated / code-inferred / NEEDS-SKY-DEVICE. Web captures come from the audit's static-export rig (`node design-reviews/r2-audit/tools/probe-export.mjs` pattern serves the production bundle on :8082 — the dev server crashes Map/Tasks on the lucide lazy boundary; pre-existing, not yours to fix).
- **PROTECT ACTIVE for this phase:** PROTECT-2 the recovery card (untouched — T9's gates are the OTHER failure surfaces) · the S11 read ladder + write-half twin (extended wording only) · the honest no-location suffix (EXTENDED, never replaced) · Home's honesty law (no fabricated distances — the fit changes the CAMERA, never distance rows) · PROTECT-19 em-dash grammar on every proposed string — re-render each touched PROTECT surface BEFORE/AFTER and save both frames to `design-reviews/r2-audit/build-plan/evidence/BP13/`; diff-verify the byte-preserved ones your spec names.

## 4 · GATES (all hard — the phase does not stop until every one passes)
- `npm run typecheck` → 0 errors.
- `npm test` → green; RECORD the count in your evidence report (baseline **1857** at `a8549ff`; expect growth from guards you add; any unrelated red = STOP, do not paper over).
- `npm run lint` → 0 errors, no new warnings.
- The 7 immutable prior stacks files untouched (diff-check): `tools/audit-stacks.json` · `p2-material-stacks.json` · `bench3-material-stacks.json` · `shipped-stacks.json` · `wave1-stacks.json` · `wave2-stacks.json` · `map-stacks.json` (real paths at `a8549ff`: `audit`/`p2-material`/`bench3-material` under `design-reviews/fable-audit/tools/`; `shipped`/`wave1`/`wave2`/`map` under `qa-reports/assets/2026-07-03_tasks_glass/` · `_glass_w1/` · `_glass_w2/` · `2026-07-04_glass_map/` — diff-check by exact filename; only `map-stacks.json` is git-tracked at the base, so for the untracked six the check = never edit them).
- Every commit-plan item CLOSED or explicitly forked with a written reason — nothing silently dropped.
- **Arbiter:** NO arbiter sibling needed — this phase changes no color/floor/ink pair. If any edit turns out to touch an ink pair, STOP and declare a named sibling first under `design-reviews/r2-audit/tools/` and run it through `node ~/AccessMap-material-lab/2026-07-02/shared/contrast-check.mjs` to exit 0 (the rail: measured, never asserted).
- **Diff scope = intended files only:** `src/screens/MapScreen.tsx`, `src/screens/HomeScreen.tsx`, `src/screens/TasksScreen.tsx` (failure-recipe adoption), `src/screens/ReportFlagModal.tsx` (submit-verb keep-words), `src/components/PlatformMap.tsx`, `src/components/PlatformMap.web.tsx`, `src/lib/copy.ts`, (`src/lib/announce.ts` consumed, not edited) (+ their test files), plus this phase's own untracked-side artifacts — its named `design-reviews/r2-audit/tools/r2-*-stacks.json` sibling(s) where mandated, its `build-plan/evidence/BP*` output, and the DECISIONS.md append (all inside the untracked `design-reviews/` tree, so they never appear in the tracked diff) — and any guard/test files its commit plan adds or updates. Anything else in the TRACKED diff = a defect; revert it.

## 5 · EVIDENCE + LEDGER DUTIES (before stopping)
- Write `design-reviews/r2-audit/build-plan/evidence/BP13-verification-evidence.md`: what shipped per commit-plan item · per-gate results (with the jest count) · honest tags on every claim · the PROTECT before/after pairs · any PROPOSED strings (before/after table) · anything Sky must eyeball.
- Append to DECISIONS.md — §P: `BP13: base <sha> → tip <sha> · <date> · gates green (jest <count>)` · §D device items contributed: R2-D17 — the native denied-arrival render question (02 honesty-ledger #16) + the no-location single voice; native first-run deny→arrival feel (D11 lineage). · §L launch items: none. · §F any fork question surfaced · §A a "default applied (BP13)" line for each §S/§O default you exercised.

## 6 · STOP
Built + green + **STOPPED on `r2/bp13-arrival-waits`**. Never merge, push, build, or deploy — Sky's hands. One phase per fresh window. **RESUME RULE:** if this phase stalls, re-fire this same file in a fresh window; reconcile the branch's existing commits against the commit plan and continue from the FIRST gap — never redo a landed item, never renumber.
