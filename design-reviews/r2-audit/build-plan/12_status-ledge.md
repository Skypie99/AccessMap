# BP12 — The status ledge — one podium for the status voice

**Fire this file ALONE in a FRESH window.** Model law: **EXECUTE on Opus 4.8 ultracode MAX EFFORT, all sub-agents max effort** (Sky's direction for this train). Authored on Claude Fable 5 max effort, 2026-07-15, in the same window that authored the synthesis — provenance is disclosed, never silent; tag it in your phase report.

**Branch:** `r2/bp12-status-ledge` — cut from the tip of ``r2/bp11-press-vocab`` (the prior phase; its tip SHA is in DECISIONS §P — **if that entry is absent, the prior phase has not run: STOP**). Record your base SHA as this phase's rollback anchor.
**Carries (conservation):** T6 — closes T6 (F2-03, canonical for F5-04 + F5-08).
**Effort:** M.
**Purpose:** The app's status voice gets one designed podium: the pill docks below the editorial header instead of beheading it, and the two vehicles stop superimposing — via the MINIMAL arbitration path (DECISIONS default).

## 0 · READ FIRST (in order — no edit before all of these)
1. `design-reviews/r2-audit/build-plan/DECISIONS.md` — apply every §S standing decision and §O/§A entry named in §1 below; take your base tip from §P.
2. **THE REPORT (design authority):** `/Users/skypie/AccessMap/design-reviews/r2-audit/2026-07-10_AccessMap_R2_Design_Review.md` — your inlined block(s) below are copied from it verbatim (`### #12 · T6 —`); also read §4 (the PROTECT list + fork registry + R2-D ledger) and §5's phase spine.
3. The master: `design-reviews/r2-audit/build-plan/00_master.md` — §6 ordering laws that name this phase, §13 PROTECT spot list.
4. **VERIFY-FIRST:** every `file:line` in the spec was true at plan-authoring (audit HEAD `a8549ff`). Re-grep each cited anchor BEFORE editing; small drift → adapt and note it in your evidence report; meaningful drift (a mechanism moved or is absent) → **STOP** and surface to Sky (evidence report + DECISIONS §P note). Never build against drift.

## 1 · STANDING DECISIONS THAT BIND THIS PHASE (inlined — self-containedness includes decisions)
- **S-6 (the pinned path):** MINIMAL arbitration/stacking — placement pub-sub + offset-below when both vehicles are live. Full migration of TasksScreen's flash onto the region channel is Sky-OPTIONAL (NOT this phase unless §A says so). Fork-2 boundary either way: the trigger-coupled points strings NEVER move.
- **S-2:** T6 is Meaningful (down-tiered — build it well, not bigger).
- **S-10:** model law.

## 2 · THE WORK — the spec, inlined VERBATIM from the report (the "Skeptic verdict: FIX (absorbed)" line is PART of the spec)
### #12 · T6 — The status ledge — one podium for the status voice · median 7.1
**WHERE:** LiveStatusRegion.tsx (placement math), FlashBanner.tsx (channel migration or stacking arbitration), App.tsx root mount, TasksScreen.tsx points-flash call site; ScreenHeader.tsx only if a reserved slot is exposed. No MapScreen overlay edits, no GlassSurface edits.
**Resolves:** F2-03 · **Effort:** M · **Tier:** Meaningful (down-tiered from Signature — Panel B demotion + skeptic concur; a placement/arbitration fix, however well-voiced, is not the ownable signature move T1/T2/T3/T5 are) · **NEEDS-SKY-DEVICE:** yes
1. **WHAT** — Give the app's one status voice one designed podium. On tab screens the resting top moves from Math.max(insets.top, 56) to below the editorial header band (insets.top + measured ScreenHeader clearance, or a reserved ledge slot ScreenHeader exposes), so "Still trying — check your signal" docks under the headline it currently beheads; on Map the pill keeps clearing chrome exactly as today. FlashBanner's points flash migrates onto the liveStatus channel (which already has tones, auto-dismiss, and the persistent-mount announce) or, minimally, gains arbitration: when both vehicles are live the second offsets below the first — never superimposed in the same zIndex-50 slot. Both pills stay toned solids; zero new material, zero string changes.
2. **WHY** — Resolves F2-03 (HIGH; canonical for the F5-04 placement read and the F5-08 two-vehicles collision). The em-dash status grammar is the app's audible signature (PROTECT nomination); a signature voice deserves a stage that never upstages what it reports on — that is why this is signature-class: it deploys the app's own signature into space, ownably. ONE VOICE: every status beat lands in one predictable place. ONE HAND: R5's "nothing reserves space for anything" collision family gets its system answer, not a nudge. ONE MATERIAL: placement only — Deep Field stays the hero, the header gets its breathing room back.
3. **PERSONA IMPACT** — R1 ("sits sloppily on top of the header text") and R5 ("the toast decapitates the '9 barriers' headline") get the occlusion pass R5 demanded before calling the app premium. R6's "black smudge" first-load frame becomes headline plus pill, both whole. R2 hears identical announcements — the text-mutation announce law is byte-preserved. R4 regains the menu circle's full tap area the pill currently intercepts. R7 first-class: both pills are already engineered solids, so RT renders the ledge identically — R7 gains a status system that never covers the editorial headers R7 named designed-for-me, plus placement predictability under C-lite.
4. **TRUST + COHESION** — The one designed escalation moment — the proof the app is alive — currently reads as a collision at the exact anxious moment, on a ~12–30s recurring window per retry. Docked, it turns "sloppy toast" into "the app has a place where it talks to you." Two status vehicles can no longer fight for one slot, so the S10/S11 single-status-channel promise becomes visibly true, not just architecturally true. Status text never again ghosts over a headline, which is precisely the sloppiness-reads-as-sloppy-data risk R1 named.
5. **A11Y + RESTRAINT** — Floor improves on all three axes: legibility (no text-over-text), reachability (44pt menu circle un-intercepted), honesty (zero string changes). PROTECT-preserving by construction: preserves the LiveStatusRegion persistent-mount text-mutation ANNOUNCE LAW (P5-shipped) byte-for-byte — placement moves, mechanism untouched; preserves PROTECT-7 (announce decoupled from motion; entrance stays RM-gated); no new motion added, honoring the 220ms/pulse discipline by not touching it. RESTRAINT: PASS — position-only; nothing decorates, nothing is traded.
6. **DEPENDENCIES / CONFLICTS** — Preserves the persistent-mount announce law (PROTECT) and PROTECT-7. Preserves MapScreen's pointerEvents="box-none" overlay law — Map placement unchanged, MapScreen untouched. GlassSurface never edited. Points-flash migration keeps the Fork-2-coupled strings byte-same (vehicle changes, words don't — Fork 2's DECISION half untouched). Shared files: TasksScreen.tsx also touched by the spoken-triage and sweep candidates — disjoint regions, either order. If ScreenHeader grows a reserved slot, coordinate with the rhythm cluster's F2-06 inset work.
7. **VERIFICATION** — Jest guards: wrapper stays persistent-mounted + box-none; announce still fires via text mutation at the new offset; arbitration test — both vehicles live renders stacked pills, never one top value twice. A11y-tree re-walk of the still-trying state: headline nodes unoccluded, one announcement. Re-capture states/app__*__still-trying.png. Blur-pane count at the worst simultaneous state (Map + still-trying + points flash, +1 tab-bar pane): unchanged — both pills non-blur. No ink change, so no arbiter; if any ledge tint appears, run tools/r2-status-ledge-stacks.json. Sky device: iPhone VoiceOver — pill announces once below the header; menu circle fully tappable (native insets math per the F2-03 skeptic shows occlusion is inherent on device).


**Skeptic verdict: FIX (absorbed).** [TIER: Signature→Meaningful, panel + skeptic concur.] WHERE is a placement pub-sub (new lib, e.g. statusLedge.ts) + header-HEIGHT focus-publish (onLayout, scroll-invariant — not a screen-Y measure) + a LiveStatusRegion subscription DEFAULTING to today's Map placement; ScreenHeader lives at src/components/ui/ScreenHeader.tsx; pin ONE path — arbitration/stacking across the three vehicles (App-root LiveStatusRegion + App-root FlashBanner + TasksScreen's inline flash) OR full migration of the inline flash; keep the text-carrier pill pointerEvents='box-none' (it is NOT today and could sit over a live control); assert no double/dropped announce.

### Commit plan (one commit per item, this order)
1. Placement pub-sub (new `src/lib/statusLedge.ts`) + header-HEIGHT publish from `src/components/ui/ScreenHeader.tsx` (onLayout, scroll-invariant — a HEIGHT, never a screen-Y; the absorbed FIX names the exact file path) per focused tab screen.
2. LiveStatusRegion subscribes; DEFAULT placement = today's `Math.max(insets.top, 56)` (Map keeps exactly today's clearance); on tab screens with a published header, dock below it. The persistent-mount text-mutation ANNOUNCE LAW is byte-preserved — placement only (PROTECT-18 diff-prove).
3. Vehicle arbitration: when both vehicles are live the second offsets below the first (shared occupied-slot signal across the App-root LiveStatusRegion + App-root FlashBanner + TasksScreen's inline flash); the region's inner text-carrier pill goes `pointerEvents="box-none"` (it is NOT today — the absorbed FIX) so only Retry is hittable; per-tab clearance verified.
4. Announce-safety guards: no double-announce, no dropped announce (the TasksScreen call sites keep their own channel under arbitration — nothing migrates); jest + a11y-tree evidence.
5. Captures at the collision states the audit shot (status pill + header, flash + pill simultaneous), both themes; PROTECT before/afters.

## 3 · RAILS (adapted to this repo's discovered infrastructure — master §3)
- The report is the design AUTHORITY. Execute to its recorded taste; never invent a decision it already made; never re-litigate a standing decision (DECISIONS §S) or a settled device read (D9/D10).
- Fork discipline: UI/read halves only. Forks 1-13 are Sky's (proximity/geo · points economy · auth-wall/guest · k-anonymity · trust-model scope · product name · stagePoolB · dark saved-place chips · ui/Button · M-12/M-13 swap · bulk mechanism lever · M-37 locked rows · lightbox blacks). A decision half you brush against becomes a DECISIONS §F question, never a build.
- Honesty fences: no invented copy, claims, prices, or numbers. Every NEW user-facing string is PROPOSED (before/after in your evidence report) unless DECISIONS §A already carries Sky's pick; proposed status strings use the em-dash grammar ("state — next step").
- Hard territory (never crossed): `src/components/ui/GlassSurface.tsx` DO-NOT-EDIT (thread the primitive, never fork it) · MapScreen's `pointerEvents="box-none"` overlay law untouched · the blur budget never exceeded (assert at the worst SIMULTANEOUS state, +1 for the tab-bar pane invisible to `__getLiveBlurPaneCount`) · the RM designed-stillness contract + the 220ms delay-gate/pulse-token discipline · RN reality (no CSS-only tricks; expo-blur intensities 12/24 only; virtualization law) · zero Supabase/Storage writes · never merge, push, build, deploy, or send anything external — those are Sky's hands, always.
- Evidence honesty: tag every claim verified / web-approximated / code-inferred / NEEDS-SKY-DEVICE. Web captures come from the audit's static-export rig (`node design-reviews/r2-audit/tools/probe-export.mjs` pattern serves the production bundle on :8082 — the dev server crashes Map/Tasks on the lucide lazy boundary; pre-existing, not yours to fix).
- **PROTECT ACTIVE for this phase:** PROTECT-18 (the announce mechanism byte-preserved — the diff is the proof) · PROTECT-19 (any proposed wording in em-dash grammar — though this phase ships zero strings) · the box-none law extended to the new carrier — re-render each touched PROTECT surface BEFORE/AFTER and save both frames to `design-reviews/r2-audit/build-plan/evidence/BP12/`; diff-verify the byte-preserved ones your spec names.

## 4 · GATES (all hard — the phase does not stop until every one passes)
- `npm run typecheck` → 0 errors.
- `npm test` → green; RECORD the count in your evidence report (baseline **1857** at `a8549ff`; expect growth from guards you add; any unrelated red = STOP, do not paper over).
- `npm run lint` → 0 errors, no new warnings.
- The 7 immutable prior stacks files untouched (diff-check): `tools/audit-stacks.json` · `p2-material-stacks.json` · `bench3-material-stacks.json` · `shipped-stacks.json` · `wave1-stacks.json` · `wave2-stacks.json` · `map-stacks.json` (real paths at `a8549ff`: `audit`/`p2-material`/`bench3-material` under `design-reviews/fable-audit/tools/`; `shipped`/`wave1`/`wave2`/`map` under `qa-reports/assets/2026-07-03_tasks_glass/` · `_glass_w1/` · `_glass_w2/` · `2026-07-04_glass_map/` — diff-check by exact filename; only `map-stacks.json` is git-tracked at the base, so for the untracked six the check = never edit them).
- Every commit-plan item CLOSED or explicitly forked with a written reason — nothing silently dropped.
- **Arbiter:** NO arbiter sibling needed — this phase changes no color/floor/ink pair. If any edit turns out to touch an ink pair, STOP and declare a named sibling first under `design-reviews/r2-audit/tools/` and run it through `node ~/AccessMap-material-lab/2026-07-02/shared/contrast-check.mjs` to exit 0 (the rail: measured, never asserted).
- **Diff scope = intended files only:** new `src/lib/statusLedge.ts`; `src/components/ui/ScreenHeader.tsx`, `src/components/LiveStatusRegion.tsx`, `src/components/FlashBanner.tsx`, `src/screens/TasksScreen.tsx` (slot signal only), `App.tsx` (mount wiring if needed) (+ their test files), plus this phase's own untracked-side artifacts — its named `design-reviews/r2-audit/tools/r2-*-stacks.json` sibling(s) where mandated, its `build-plan/evidence/BP*` output, and the DECISIONS.md append (all inside the untracked `design-reviews/` tree, so they never appear in the tracked diff) — and any guard/test files its commit plan adds or updates. Anything else in the TRACKED diff = a defect; revert it.

## 5 · EVIDENCE + LEDGER DUTIES (before stopping)
- Write `design-reviews/r2-audit/build-plan/evidence/BP12-verification-evidence.md`: what shipped per commit-plan item · per-gate results (with the jest count) · honest tags on every claim · the PROTECT before/after pairs · any PROPOSED strings (before/after table) · anything Sky must eyeball.
- Append to DECISIONS.md — §P: `BP12: base <sha> → tip <sha> · <date> · gates green (jest <count>)` · §D device items contributed: R2-D16 — ledge placement on device; SR announce timing preserved through the reposition. · §L launch items: none. · §F any fork question surfaced · §A a "default applied (BP12)" line for each §S/§O default you exercised.

## 6 · STOP
Built + green + **STOPPED on `r2/bp12-status-ledge`**. Never merge, push, build, or deploy — Sky's hands. One phase per fresh window. **RESUME RULE:** if this phase stalls, re-fire this same file in a fresh window; reconcile the branch's existing commits against the commit plan and continue from the FIRST gap — never redo a landed item, never renumber.
