I have everything I need — captures walked, code truth run down, census complete. Assembling the lens document.

## F5 — States as designed moments

**Lens verdict.** States are where this app's care is most visible and most uneven at once. The PROTECT-2 bar survived the entire uplift intact and got *better* — the recovery card now offers a per-axis one-tap chip plus ctaFill reset, and two surfaces (Tasks' five-way empty fork with its gold-only-when-true rule, AddressSearch's failure≠no-matches split) now genuinely *exceed* the bar. The S11 waiting ladder (cold "Loading flags…" / "Updating…" split → 12s "Still trying" with Retry → 30s ceiling) is a designed act of honesty almost no map app performs, and R1, R6, and R7 each independently praised a state moment unprompted. But the failure register trails the empty register badly: Home's first-load failure presents as a mute em-dash over anonymous skeletons (both cold readers called it "maybe broken"), a settled failure asserts a count it doesn't have ("0 barriers" / "Showing 0 flags"), and the no-location web arrival — the default first-run state for the guest-web cohort — drops the user in San Francisco with a pill claiming nine flags nobody can see. One hand built the empty states; the failure states got the apprentice.

### The census (surface · state · what renders · meets the PROTECT-2 bar? · evidence)

| # | Surface | State | What renders | Meets bar? | Evidence |
|---|---|---|---|---|---|
| 1 | Map | Empty-filters (the bar itself) | "Nothing here right now" + "Your filters are hiding everything. Clear just the one in the way, or reset them all." + per-axis chip ("All categories") + ctaFill "Reset all filters" + honest "0 of 9 shown" pill | **IS the bar — intact + extended** (per-axis chips, MapScreen.tsx:1037-1053) | `states/map__{light,dark}__390__empty-filters-true.png`, `voice/empty-filters-true__light__390__crop.png` [web-approximated]; MapScreen.tsx:2150-2189 [code-inferred] |
| 2 | Map | Cold load | "Loading flags…" pill in the count slot, live-region'd | Meets (worded, calm, placed) | `states/app__light__390__loading-cold.png` [web-approximated]; MapScreen.tsx:1462-1471 [code-inferred] |
| 3 | Map | Revalidate over data | "Updating…" pill (S11 split) | Meets — honest split; but see F5-02 for what it settles into on failure | `states/map__light__390__offline-refresh.png` (caught mid-flight) [web-approximated]; MapScreen.tsx:1463-1468 [code-inferred] |
| 4 | Map | Refresh/load failure settled | Solid error banner, tap-to-retry, busy "Retrying…" swap | Meets mechanically; voice + count leaks (F5-02, F5-05) | MapScreen.tsx:2115-2141 [code-inferred]; settled frame not banked [web-approximated gap] |
| 5 | Map | Offline w/ cache | B9a banner stating data AGE | Meets [code-inferred; offlineBannerText, copy.ts:21-25]. Guest never sees it — cache is user-scoped (Fork 4, Sky's — referenced, not re-litigated) |
| 6 | Map | Permission-denied arrival (S4) | "Location is off, so the map shows the most recent flags…" alert banner | Meets when it fires — banner confirmed rendering on the denied locate path; arrival-fire on web UNRESOLVED (F5-03) | `states/map__light__390__locate-failure.png` shows the banner [web-approximated]; MapScreen.tsx:2218-2229, 1148-1154 [code-inferred] |
| 7 | Map | Undetermined-location arrival | DEFAULT_REGION (San Francisco) viewport + "Showing 9 flags" + **no words** | **FAILS** (F5-03) | `states/map__{light,dark}__390__permission-denied.png`, `a11y-tree/denied-map__light__390.txt` [web-approximated] |
| 8 | Map | Locating | Glass "Finding your location…" banner, spinner + words, 15s bound | Meets (PROTECT-6 held: bounded, worded, recoverable) | `states/map__light__390__locating-hang.png` [web-approximated]; location.ts:44-58 [code-inferred] |
| 9 | Map | Locate failure (B10, web) | LiveStatusRegion pill + self-clearing Retry | Meets by code; the banked "locate-failure" frame actually shows the denied-banner path, so B10's pill remains uncaptured | MapScreen.tsx:194, 1112-1121, 1060-1065 [code-inferred] |
| 10 | Map | Heat on / no qualifying zones | k≥3 rule banner (captured) + "No heat zones qualify yet — coverage grows as more reports come in." companion | Meets; companion line did not fire in the banked frame (zones qualified) | `base/map__light__390__heatmap-on.png`, `voice/heat-caveat__light__390__crop.png` [web-approximated]; MapScreen.tsx:2255-2266 [code-inferred, B7 tests test-inferred] |
| 11 | Home | First load | Title = literal "—" + 4 AT-hidden SkeletonRows | **FAILS** (F5-01) | `states/home__{light,dark}__390__load-error.png` [web-approximated]; HomeScreen.tsx:161, 185, 330-338 [code-inferred] |
| 12 | Home | Load failure settled, no data | "Couldn't load barriers." + ctaFill "Try again" card — but headline computes "0 barriers" | Partial — card meets, headline lies (F5-02) | HomeScreen.tsx:315-329 vs :185 [code-inferred] |
| 13 | Home | Refresh fail w/ data (B9b) | "Couldn't refresh — showing older data. Tap to try again." live pressable notice | Meets (explains + one-tap fix + honest) | HomeScreen.tsx:294-311 [code-inferred; capture infeasible per brief] |
| 14 | Home | Offline w/ cache | Age-stating banner | Meets | HomeScreen.tsx:285-292 [code-inferred] |
| 15 | Home | Genuine empty | "No barriers reported yet." — one line, no next step | Borderline (F5-10) | HomeScreen.tsx:339-342 [code-inferred] |
| 16 | Tasks | Loading | Full FlagCard-anatomy skeletons (chip row, lines, summary, 3 action pills); static 0.5 under RM; AT-hidden | **Exceeds** — R7: "exactly what I want… almost nobody gives it to me" | `states/tasks__{light,dark}__390__skeletons-slowdata.png`, `tasks__light__390__rm-skeletons.png` [web-approximated]; Skeleton.tsx:38-53 [code-inferred + B5 net test-inferred] |
| 17 | Tasks | Empty ×5 (error / category / search / more-pages / done) | Five-way fork; gold Sparkles + "All caught up — nice work!" **only** when genuinely empty and fully loaded (F40/F41) | **Exceeds** | `states/tasks__{light,dark}__390__empty-search.png` [web-approximated]; TasksScreen.tsx:1181-1233 [code-inferred] |
| 18 | Tasks | Load failure | Tap-to-retry banner, appends "Tap to retry.", busy swap | Meets | TasksScreen.tsx:622-630, 855-881 [code-inferred] |
| 19 | Tasks | Pagination | "Load more" w/ labeled busy spinner; end line "That's everything nearby — you're up to date" | Meets — the end-of-list is a designed beat | TasksScreen.tsx:1236-1275 [code-inferred] |
| 20 | Nearby | Empty ×3 (search echo / named category / genuine) | Explains + names the widening move; genuine variant explains where flags come from | Meets | NearbyFlagsModal.tsx:288-306 [code-inferred; live data non-empty, uncapturable] |
| 21 | Report | Ready-submit boundary | Selected severity (ring+check+ink), definition line, counter, both honesty sentences, enabled gradient Submit | Meets — a designed boundary | `states/report__{light,dark}__390__ready-submit.png` [web-approximated] |
| 22 | Report | Submitting / stalled | Form-wide disable + button spinner; 12s in-sheet "Still trying — check your signal" overlay; write never aborted | Meets (spinner-verb nit → F5-09) | ReportFlagModal.tsx:313-325, 1040-1049, 1086-1090 [code-inferred; fence] |
| 23 | Report | Success (S10) | "Report filed — thanks for flagging this barrier" green pill, announced, 4s auto-dismiss, RM-gated entrance, honest EXIF-strip variant, hapticNotify('success') | Meets — visible + spoken + timed + felt | ReportFlagModal.tsx:364-375, 453-471; LiveStatusRegion.tsx [code-inferred — success is fenced by law, orientation §4.8; haptic half NEEDS-SKY-DEVICE] |
| 24 | Report | Anon rate-limit | Web-visible worded notice (F46 fix held) | Meets | ReportFlagModal.tsx:335-351 [code-inferred] |
| 25 | AddressSearch | Error vs no-matches | "Couldn't search" + Retry card, **distinct from** "No matches" + three widening strategies; recents list | **Exceeds** — "worth retrying, not rephrasing" | AddressSearchModal.tsx:61-67, 302-346 [code-inferred]; `base/address-search__{light,dark}` [web-approximated] |
| 26 | Help | Empty search | One italic line, re-inked for glass (B4b) | Adequate; no query echo (F5-10) | HelpModal.tsx:94, 147-151 [code-inferred] |
| 27 | MyFeedback | Empty ×2 (filtered / genuine) | Named filter + "Tap 'All' above"; genuine explains destination ("lands here and in the maintainer's inbox"), B2-i icons | Meets | MyFeedbackModal.tsx:206-232 [code-inferred]; `base/myfeedback-modal__{light,dark}` [web-approximated] |
| 28 | Profile | Loading | Bare centered ActivityIndicator on the stage, wordless | **FAILS** (F5-06) | ProfileScreen.tsx:812-817 [code-inferred] |
| 29 | Profile | Signed-out | One sentence + CTA in a large void — R7: "calm… but unfurnished" | Borderline — designed-minimal; census note only (composition → F2 territory) | `base/profile-signedout__{light,dark}` [web-approximated] |
| 30 | Drawer | Sub-screen swap pause | Blank pane + unlabeled spinner at t120 | **FAILS** (F5-07) | `transitions/drawer__light__390__subswap-t120.png` [web-approximated] |
| 31 | App-level | Still-trying (12s read) | Brand-blue pill + embedded 44pt Retry, persists until settle, announced via text mutation | Meets, minus placement (F5-04) | `states/app__light__390__still-trying.png` [web-approximated]; flagsStore.tsx:303-310 [code-inferred] |
| 32 | FlagDetail | Comments loading / empty / failed | Labeled small spinner; worded empty; Retry on failure | Meets | FlagDetailModal.tsx:1282-1294 [code-inferred] |
| 33 | Tasks | Points flash (auth) | "Verified! +N points" FlashBanner | KNOWN B1/Fork-2 honesty gap — cited, not re-found | TasksScreen.tsx:647-651 [code-inferred; auth-fenced] |

Skeleton-vs-spinner census (grep at HEAD, in-control busy spinners excluded as legitimate): **skeletons** on Tasks, Home, Leaderboard; **bare surface spinners** on Profile and the drawer pause; **worded/labeled spinners** (acceptable) in FlagDetail comments, MyFeedback, load-more, submit buttons. The skeleton language stops at the tab bar's third tab.

---

**F5-01** · **Where:** HomeScreen first-load/failure window — HomeScreen.tsx:161, 185, 315-338; captures `states/home__{light,dark}__390__load-error.png` · **What:** During any first load the headline renders a literal em-dash ("—") at display size above AT-hidden skeletons, and when the load has *already hard-failed* the presentation doesn't change: at t≈2.5s after a fully aborted Supabase route, the production bundle still showed the skeleton state — no words, no retry, nothing distinguishable from loading [web-approximated]. The designed error card ("Couldn't load barriers." + Try again, HomeScreen.tsx:315-329 [code-inferred]) did not present within the capture window, and by the ladder's own constants the first guaranteed words on a stalled read arrive at 12s (still-trying pill — captured firing, `states/app__light__390__still-trying.png` [web-approximated]) with the actionable card gated behind settle (ceiling 30s, flagsStore.tsx:31-37 [code-inferred]). Why an already-rejected read left `loading` standing at 2.5s is an open engine question this read-only pass could not settle — two independent capture recipes (abort + offline) both show it — and deserves the skeptic's first swing. · **Why it matters:** Two blinded readers independently hit this exact wall: R1 felt "I'd sit there thirty seconds before realizing the app had failed me… a failure state indistinguishable from loading, right next to a 'still trying' state that proves the team knows how to do it right"; R6 felt "the one screen I'd call 'maybe broken.'" For the mission cohort, a mute failure over barrier data is a stranding risk, and it sits beside the app's best-in-class waiting ladder — the contrast is what makes it read as neglect. · **Direction:** word the wait ("Loading…" in the headline slot instead of the bare glyph — the Map already does exactly this with its pill), and make the error card present the moment the read rejects rather than only at settle; run the loading-stuck-at-2.5s engine question to ground. RESTRAINT: PASS — every part adds words and honesty; nothing decorates. · **Severity: HIGH**

> ⟢ **SKEPTIC VERDICT: ADJUSTED.** Severity HIGH → MEDIUM, and rewrite the failure model: (1) DELETE the engine-bug framing — the "loading stuck at 2.5s after an already-rejected read" is SETTLED, not open: @supabase/postgrest-js 2.106.2 retries failed idempotent requests 3× with 1s/2s/4s exponential backoff by default (dist/index.mjs:5,13,255ff), so the read genuinely had not rejected at 2.5s; `loading` was honest. (2) CORRECT the hard-fail timeline: on an aborted or offline read the designed error card ("Couldn't load barriers." + Try again) presents at ≈7–9s after boot — probe-verified live (tools/probe-f501-skeptic.mjs: errorCard=false at 1.6s/4.2s, true at 9.2s, persistent through 35s) — NOT at 12s, NOT gated behind the 30s settle; the 12s pill and 30s ceiling govern stalled reads only, and the still-trying pill never fires on a hard fail. Re-tag both load-error captures "mid-retry window (t≈3.7s of a ~7-9s library retry ladder)". (3) Weigh R1/R6 as reactions to a frozen mid-retry frame, not a persistent state — the "thirty seconds before realizing it failed" extrapolation is factually wrong. (4) KEEP, as the surviving finding: the display-size "—" headline with zero words during any first load (HomeScreen.tsx:161,185, AT-hidden skeletons confirmed) and the wordless ~7–9s hard-fail window — word the wait ("Loading…" in the headline slot, as the Map does). (5) CUT the direction "make the error card present the moment the read rejects" — it already does; the actionable variant is deciding whether the library's ~7s first-load retry ladder is the wanted posture (e.g. .retry(false) on cold start) or simply wording the wait over it.
> *Rationale:* I re-ran the finding's own recipe live with network/DOM instrumentation and read the installed client library. The central alarm — a hard-failed read that stays mute and indistinguishable from loading, with first words at 12s and the actionable card at 30s — is factually wrong: postgrest-js 2.106.2 silently retries the failed GET for ~7s (1s/2s/4s backoff, on by default), after which the rejection surfaces and HomeScreen's designed error card renders at ≈7–9s and persists. The captures are genuine but frozen inside that retry window, and the two persona reads extrapolated permanence from the frozen frame. What survives verification is smaller and real: the headline renders a bare display-size "—" over AT-hidden skeletons with no words during every first load (Map words the same wait; Home doesn't), leaving a wordless ~7–9s worst-case window. That is a voice/cohesion defect worth fixing, not a stranding-risk access failure, so it calibrates MEDIUM, not HIGH.


**F5-02** · **Where:** HomeScreen.tsx:185; MapScreen.tsx:1463-1471 · **What:** On a *settled* failure with no rows (`error` set, `flags.length === 0`, `loading` false), Home's headline computes `"0 barriers"` and Map's status pill computes `"Showing 0 flags"` — both assert a census the app does not have, directly above/beside their own failure banners saying the load failed [code-inferred; the banked failure captures caught the pre-settle window, so this state is unphotographed]. · **Why it matters:** "0 barriers" in a disability-barriers app is the best possible news — rendering it as the largest text on screen during a failure is the exact inverted-honesty shape Round 1's S4/S6 work existed to kill. The thesis says failure states are design surfaces; a design surface must not state unknowns as zeros. · **Direction:** gate both count renderers on `error` (Home: keep "—" or "Couldn't load"; Map: let the pill say "Couldn't load flags" — the pill already has a three-way ternary, this is a fourth honest arm). RESTRAINT: PASS — pure honesty, no material or reach cost. · **Severity: HIGH**

> ⟢ **SKEPTIC VERDICT: CONFIRMED.**
> *Rationale:* I attacked reachability hardest: flagsStore.tsx:353–378 shows the cache fallback requires a signed-in userId, so a guest (or first-launch user) with a failed network read lands exactly in the settled triple — setError at :376, flags stays [], loading cleared in finally at :386. Both cited renderers then behave precisely as claimed: HomeScreen.tsx:185 has only a showFirstLoad (loading) gate, so it prints "0 barriers" above the :315 "Couldn't load barriers." card; MapScreen.tsx:1463–1471's three-way ternary never consults loadError, so it prints "Showing 0 flags" — through a polite accessibilityLiveRegion, meaning SR users are announced the false census — while the :2115 error banner shows in the same overlay. The delta digest confirms S4/S11/B9b touched adjacent honesty surfaces but never gated the counts, and the finding is on no never-relitigate fork. Severity HIGH is calibrated right: not CRITICAL because the honest failure banner is co-present on both screens, but a false "zero barriers" census in a barriers app is a direct trust-mission hit. The proposed fix direction is mechanically sound (the ternary structure exists as described).


**F5-03** · **Where:** Map arrival with no usable location — MapScreen.tsx:138-141 (DEFAULT_REGION = 37.7749 San Francisco), 1287-1298, 1136-1161; captures `states/map__{light,dark}__390__permission-denied.png`; `a11y-tree/denied-map__light__390.txt` · **What:** When no location exists at arrival, the map opens on the hardcoded San Francisco default with the pill asserting "Showing 9 flags" while zero of the nine (all in Kelowna) are in the viewport, and — in the undetermined case — no banner explains anything [web-approximated]. The S4 banner deliberately covers only a *real prior denial* (arrivalPermissionDenied, location.ts:93-97 [code-inferred]) — a correct honesty fork — but that leaves the undetermined arrival, which on web is the **default first-run state** (the web onboarding consent advances via "Continue" and the declines are native-only, OnboardingCards.tsx:222 per the render index), with no designed moment at all. Sub-note, honestly flagged: the banked denied-arrival frames show no S4 banner even though the harness set a CDP deny; the same context's locate-tap denial *did* produce the banner (`states/map__light__390__locate-failure.png` [web-approximated]), so either the CDP deny never bound to the capture context (harness artifact — likely, and consistent with expo-location's correct web mapping, node_modules/expo-location/build/ExpoLocation.web.js:47-63 [code-inferred]) or S4's arrival probe misses on web. Unresolved read-only; needs one probe or NEEDS-SKY-DEVICE. · **Why it matters:** R6 felt "the app silently dropped me in San Francisco… I don't live in San Francisco. Just say so." R1 felt the denied map's count was "technically true, functionally misleading." This is the honest-arrival thesis (S4) with its largest cohort — first-run guest web — left uncovered. · **Direction:** when location is unknown at arrival, fit the initial viewport to the loaded flags (a bounds-fit over the fetched rows is presentation-only — it does not touch Fork 1's proximity/geo-query question), and/or give the undetermined arrival one honest line that doesn't accuse ("Showing the most recent reports" — the Nearby sheet already speaks exactly this suffix, NearbyFlagsModal.tsx:68). RESTRAINT: PASS — replaces a false frame with a true one; no legibility cost. · **Severity: HIGH**

> ⟢ **SKEPTIC VERDICT: ADJUSTED.** (1) Cut the over-claim "with no designed moment at all": on web the undetermined arrival DOES get one designed honest moment — the Nearby sheet auto-opens for every web user (RN-web's isScreenReaderEnabled resolves true unconditionally, node_modules/react-native-web/dist/exports/AccessibilityInfo/index.js:14-17; MapScreen.tsx:418-424) carrying the visible line "Allow location access to sort flags by distance. Showing the most recent first." (NearbyFlagsModal.tsx:217-223) — the capture harness itself dismissed it ("clickOpt":"Close", manifests/states.json) before shooting. The genuinely uncovered surfaces are (a) the map itself once that sheet closes — SF frame + "Showing 9 flags" pill with zero in view, no explanation on the map — and (b) NATIVE undetermined arrivals (onboarding "Not now" decliners, no screen reader → no auto-open), which get nothing at all. (2) Sub-note RESOLVED by probe (tools/probe-f503-skeptic.mjs, run 2026-07-10 vs :8082): harness replica — the page-scoped CDP Browser.setPermission deny never bound (in-page permissions.query='prompt'; no banner = correct undetermined behavior); true-denied context (permissions.query→'denied') — the S4 arrival banner RENDERS on web. Strike "Unresolved read-only; needs one probe or NEEDS-SKY-DEVICE"; the banked map__*__permission-denied captures depict the UNDETERMINED state and should be relabeled — which makes them primary evidence for this finding's core claim; R6's "when I denied location" read is evidence about the undetermined arrival (genuine deniers do get the banner). (3) Evidence nit: the Nearby suffix at NearbyFlagsModal.tsx:68 is ". Showing the most recent first." (visible sibling at :220), not "Showing the most recent reports". Severity stays HIGH; DEFAULT_REGION is :138-143.
> *Rationale:* I attacked the finding's weakest joint — the unresolved capture sub-note — by running the one read-only probe it said it needed, and the result cuts both ways: the CDP deny provably never bound (so the cited "denied" captures are artifacts), but they are artifacts that depict exactly the undetermined state the finding is actually about, so the core claim gets STRONGER while the denied-state framing and R6's quote need re-attribution. Separately, S4's web arrival banner was verified working under a real denial, killing the finding's "or S4 misses on web" alternative. The one real over-claim is "no designed moment at all": the audit's own honesty ledger treats the Nearby auto-open as web app truth, that sheet visibly says "Allow location access... Showing the most recent first." on every web arrival, and the harness closed it before shooting — so the web cohort has a transient honest moment, though the map surface beneath (SF frame + misleading "Showing 9 flags" pill) stays false after close and native Not-now decliners truly get nothing. The core defect survives every attack with code, capture, and verbatim persona evidence intact, and the trust-mission harm keeps it HIGH.


**F5-04** · **Where:** LiveStatusRegion placement — LiveStatusRegion.tsx:109, 143-149 (absolute, `top: max(insets.top, 56)`, zIndex 50); capture `states/app__light__390__still-trying.png` · **What:** The still-trying pill lands directly on Home's header block — in the capture it covers the headline slot and beheads "Most recent barriers" ("M" visible) [web-approximated]. It persists until the read settles (no auto-dismiss, by design), so the occlusion is minutes-long on a bad connection, not a 4s flash. · **Why it matters:** R1 felt it "sits sloppily on top of the header text"; R5 called the same pattern out as part of "occlusion is a pattern, not an incident… the 'Still trying' toast decapitates the '9 barriers' headline." The one designed escalation moment — the thing that proves the app is alive — reads as a collision on the exact screen where the user is anxious. · **Direction:** drop the region's resting `top` below the editorial header band on tab screens (or give ScreenHeader a reserved status slot); keep the overlay behavior on Map where it clears the chrome. RESTRAINT: PASS — placement change only; announcement mechanism, tones, and persistence untouched (PROTECT: the persistent-mount announce law is sacred). · **Severity: MEDIUM**

**F5-05** · **Where:** The same read failure speaks three dialects — HomeScreen.tsx:318 ("Couldn't load barriers." — fixed string, provider message discarded), TasksScreen.tsx:622-630 (provider string + appended "Tap to retry."), MapScreen.tsx:2137-2138 (raw provider string, no visible retry cue — the retry affordance lives only in the a11y hint at :2126) · **What:** One failure, three registers: Home generic, Tasks instructive, Map mute-about-its-own-tappability [all code-inferred]. The provider's own crafted line ("Loading barriers is taking longer than usual. Check your signal and try again.", flagsStore.tsx:315) reaches Map and Tasks but never Home. · **Why it matters:** "One voice" is a third of the Round-2 thesis; the failure register is where a stressed user most needs the same hand. Map's banner being a Pressable that never *says* it's tappable is also a quiet reach cost for the sighted-motor cohort. · **Direction:** route all three through one shared failure-banner recipe (errors.ts already centralizes the message; centralize the presentation contract too — icon + message + visible retry verb). RESTRAINT: PASS — consolidation adds legibility and reach. · **Severity: MEDIUM**

**F5-06** · **Where:** ProfileScreen.tsx:812-817 · **What:** Profile's loading state is a bare centered ActivityIndicator on the stage — no words, no shape — while Tasks and Home render content-shaped skeletons and Leaderboard has SkeletonRows (census above) [code-inferred]. The third tab is the only core surface still speaking spinner. · **Why it matters:** The skeleton contract (content-shaped, RM-static, AT-hidden) is one of the app's most praised feel assets — R7: "It just sits there and tells me the shape of what's coming… almost nobody gives it to me." Breaking it on a tab breaks "one hand." · **Direction:** a stats-card-shaped skeleton pair (points tile + list rows) using the existing primitives. RESTRAINT: PASS — extends a shipped pattern; no motion added (RM-static inherited). · **Severity: MEDIUM**

**F5-07** · **Where:** Drawer sub-screen swap pause — capture `transitions/drawer__light__390__subswap-t120.png`; HamburgerDrawer.tsx:110-122 · **What:** Inside the designed 220ms pause between drawer close and sub-screen mount, the app presents a blank pale pane with a single small unlabeled spinner — no chrome, no shape, no words [web-approximated]. The pause itself is deliberate (and correctly 0 under RM, B5); the *state shown during it* is the emptiest frame in the entire capture bank. Engine cause of what exactly renders there → F3 territory; the state presentation is judged here. · **Why it matters:** R5 felt it precisely: "the one genuine mid-state captured is… a blank white page with a lonely spinner… That's the motion story: instant everywhere except the one place you get a white flash." Against the bar, this is the app's only bare-spinner-on-blank. · **Direction:** during the gate, keep the underlying screen (or the destination's stage + ScreenHeader skeleton) visible instead of a blank pane. RESTRAINT: PASS — removes a flash, adds continuity; the RM=0 path is untouched. · **Severity: MEDIUM**

**F5-08** · **Where:** FlashBanner.tsx:119, 140-147 and LiveStatusRegion.tsx:109, 143-149 · **What:** Both status vehicles independently mount at `top: max(insets.top, 56)`, zIndex 50, with no arbitration — TasksScreen mounts its own FlashBanner (points flash) while the app root mounts LiveStatusRegion (still-trying / report-filed), so a verify-triggered "+3 points" flash and a concurrently stalled read's persistent pill can superimpose in the same slot [code-inferred; auth-gated coincidence, unphotographable behind the fence]. · **Why it matters:** Two pills fighting for one slot at the same z-index is exactly the layered-collision family R5 flagged as the app's execution-hygiene weakness; the S10/S11 region was built to be the *single* status channel and FlashBanner predates it. · **Direction:** when both are live, offset the second pill below the first (or migrate the points flash onto the liveStatus channel, which already has tones and auto-dismiss). RESTRAINT: PASS — no announcement semantics change. · **Severity: LOW**

**F5-09** · **Where:** ReportFlagModal.tsx:1086-1090 · **What:** During submit, the button's verb is replaced by a bare spinner — the moment loses its words (a11y `busy` is set, so the spoken channel survives; the visual channel goes wordless) [code-inferred; fence — never pressed]. The 12s stall overlay then *re-words* the wait, which proves the intent. · **Why it matters:** The submit moment is the app's most consequential beat and everything around it (S15 sentence, S18 label, S10 confirmation) is worded; ten seconds of anonymous spinner is the one silent stretch. · **Direction:** "Filing your report…" beside (or instead of) the spinner. RESTRAINT: PASS — one word of voice, footer layout already fits a two-line label at 200% (S18 proof). · **Severity: POLISH**

**F5-10** · **Where:** Empty-state echo/next-step drift — HelpModal.tsx:149 vs TasksScreen.tsx:1228 and NearbyFlagsModal.tsx:300; HomeScreen.tsx:341 · **What:** Tasks and Nearby empties echo the failing query in quotes ("Nothing matches \"zzzz…\""); Help's does not ("No FAQ matches that search."). Home's genuine-empty ("No barriers reported yet.") is the only genuine-empty in the app with no next step — Tasks celebrates, Nearby explains where flags come from, MyFeedback points at the Feedback button; Home could invite the first report (the FAB is on-screen) [code-inferred; `states/tasks__light__390__empty-search.png` web-approximated]. · **Why it matters:** Small, but the empty-state family is the app's signature care register — R6 specifically credited the echo ("polite and clear"); drift here is drift in the strongest voice. · **Direction:** echo the query in Help; one inviting clause on Home's genuine-empty. RESTRAINT: PASS — words only. · **Severity: POLISH**

### PROTECT nominations

- **The extended recovery card** (MapScreen.tsx:2150-2189 + emptyResetChips :1037-1053): the per-axis one-tap clear ("All categories" / "Any severity" / "Any distance" / "All access needs") is a genuine extension of PROTECT-2 — freeze the *pattern* (explain + surgical fix + full reset + honest count), not just the copy.
- **Tasks' five-way empty fork with the F40/F41 gold rule** (TasksScreen.tsx:1181-1233): celebration only for a genuinely empty, fully-loaded list; error/filter/search/more-pages each get their own honest words. This is the state machine every future list should copy.
- **AddressSearch's failure≠no-matches split** (AddressSearchModal.tsx:61-67, 302-346): "worth retrying, not rephrasing" — the distinction itself is the design asset.
- **The S11 read ladder as a unit** (flagsStore.tsx:31-37, 297-318): cold/updating split + 12s worded escalation with Retry + 30s ceiling, reads stall and never silently abort — plus its write-half twin (the in-sheet stall overlay, same phrase, ReportFlagModal.tsx:315-325).
- **The Skeleton contract** (Skeleton.tsx:38-58): content-shaped, AT-hidden, static at 0.5 under RM — R7's single warmest praise.
- **LiveStatusRegion's persistent-mount text-mutation announce law** (LiveStatusRegion.tsx:16-24, 106-109): the mechanism that makes success and escalation audible to the guest-web cohort; never regress to mount-with-text.
- **The locating ladder** (MapScreen.tsx:2194-2216 + location.ts:44-58): worded glass banner, 15s bound, retryable — PROTECT-6 verified held at HEAD.

### Copy observations

- "Your filters are hiding everything. Clear just the one in the way, or reset them all." — survived byte-intact; still the app's best sentence (R5 agrees).
- The recovery card's spoken label ("No flags match your active filters. Try clearing one…", MapScreen.tsx:2155) is a different dialect from its visible copy — one sentence should serve both channels (→ F4).
- "Still trying — check your signal" repeats verbatim across the read pill and the submit overlay — one-voice discipline, keep.
- Home's error card discards the provider's warmer "taking longer than usual" line for generic "Couldn't load barriers." (→ F5-05).
- Map's error banner never visibly says "Tap to retry"; Tasks' does — same banner, two contracts.
- "All caught up — nice work! … Pull down to refresh anytime." — the only celebration in the app and it's earned honestly; keep the gold gate.
- "Showing saved data from {age} — connect for the latest." — the age-stating offline line is quiet mission-grade honesty.
- "No heat zones qualify yet — coverage grows as more reports come in." — companion line reads well in code; its tuning is Sky's open bench discovery (referenced, not re-litigated).
- "Report filed — thanks for flagging this barrier. Location data was removed from your photos." — the EXIF-strip variant is the most trustworthy sentence in the app; guard it with S10's honesty proof chain.
- Points flash strings ("Verified! +N points") remain coupled to the schema trigger values — the KNOWN B1/Fork-2 honesty gap, cited only.
