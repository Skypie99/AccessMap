FABLE AUDIT ROUND 2 — ACCESSMAP — PART 1 of 3: THE FEEL AUDIT (READ-ONLY)
(file 1 of 3 — fire each part alone in a FRESH window, in order; siblings: r2_part2_material.md · r2_part3_synthesis.md · operator note: 00_run_order.md. Authored 2026-07-09 on Fable 5 max effort.)

Purpose: judge the CRAFT of a now-correct app — micro-interactions, rhythm, transitions, voice, states-as-moments, and where the signature can deepen — screen by screen, with blinded persona reads (including Reduce Transparency and VoiceOver) proving that beauty survives every access posture. Round 1 asked "is it honest and reachable?"; Part 1 of Round 2 asks "does every press, pause, and sentence feel like one careful hand made it?" Deliverable: a banked evidence-plus-findings base (`02_feel_findings.md` + orientation, index, persona reads, captures) complete enough that Part 3 builds the slate from it alone, in a fresh window. State lives on disk at `~/AccessMap/design-reviews/r2-audit/` — nothing carries between parts except files.

★ SUBJECT BLOCK (identical in all three parts; swap this block to reuse the system on future rounds):
- SUBJECT: AccessMap — Sky's flagship, a civil-benefit accessibility product, and her first-impression project for career-critical audiences. Expo SDK 54 / React Native 0.81 / React 19.1 / TypeScript strict / Supabase (auth + Postgres + RLS + Storage) MOBILE app; react-native-maps native, react-leaflet 5 + OSM/CartoDB tiles on web. Repo root `~/AccessMap` — verify by GLASS.md at root + `src/screens/{MapScreen,TasksScreen,ProfileScreen}.tsx`.
- AUDIT BASELINE (Round 2 audits the post-uplift, post-bench tree): Round 1's slate is FULLY CLOSED — S1–S20 shipped via the uplift train P0–P5 (ledger: `design-reviews/fable-audit/uplift-assets/P0..P5-verification-evidence.md`), B1–B11 resolved via the bench tier BENCH 1–4 (ledger: `design-reviews/fable-audit/bench-assets/BENCH-1..4-verification-evidence.md`; authored tip `bench/4-quality` @ `a8549ff`, stacked on main `01f7392`). Audit HEAD exactly as Sky left it — record branch + hash, NEVER switch branches — and verify the tree truly contains the whole baseline by three markers spanning the trains: `PHOTO_MAX_DIMENSION` greps in the photo pipeline (BENCH-4/B8) · `src/components/LiveStatusRegion.tsx` exists (uplift P5/S10) · `variant="bulk"` inside `src/screens/NearbyFlagsModal.tsx` (BENCH-3/B4e). Any marker missing → STOP AND REPORT (the premise — auditing the post-bench baseline — is wrong; Sky decides, you don't).
- THE ROUND-2 THESIS (given, not discovered — every judgment serves it): "DISABLED USERS DESERVE BEAUTIFUL SOFTWARE — one material, one voice, one hand." Round 1 made the app CORRECT (honest, reachable, coherent, safe); Round 2 makes it LOVELY — because utilitarian-bare is what disabled users are always handed, and refusing that is both the mission and the differentiator. One material = a single material world after intro/onboarding (Part 2's question). One voice = the same warm, civically-serious editorial voice in every small copy moment. One hand = every corner feels made by ONE careful hand (craft coherence) — and stays usable WITH one hand (reachability never regresses).
- STAKES + RESTRAINT (both true at once): beauty here NEVER costs legibility, reachability, or honesty. Round 1's enemy — clutter, occlusion, anything that costs legibility — still stands; WCAG 2.2 AA remains the HARD FLOOR. Glass is the STAGE; the severity grammar stays the ACTOR (Round 1's signature thesis, intact). Reduce Transparency users get a FIRST-CLASS experience, not a fallback. THE RESTRAINT CHECK on every finding-with-a-direction and every proposal: does it cost legibility, reachability, or honesty? Then it dies, however pretty.
- THE MISSION (unchanged — hold it in every judgment): AccessMap maps physical-world accessibility barriers (6 categories, severity 1–5, status open/verified/resolved) so people with mobility, vision, or other disabilities can find and trust navigable routes and contribute what they encounter. Real disabled people depend on this. DESIGN.md's own law: "Beauty that excludes people is a defect — especially here."
- SKY'S TASTE ANCHOR (judge against THIS, not generic mobile convention): the Deep Field material system — material depth as hero, used with RESTRAINT and breathing room; EDITORIAL typography; ALWAYS LEGIBLE. Distinctiveness comes FROM the ethos, never by abandoning it. Deviations from Sky's OWN ethos are the most valuable findings.
- OUTPUT HOME: `design-reviews/r2-audit/` inside `~/AccessMap` (these prompts live there; the audit banks everything else there too — `partials/`, `assets/`, `tools/`). The repo's `qa-reports/` convention is for role reports; audits keep their own folder, like Round 1's `fable-audit/`.

★ BASELINE-LEDGER BLOCK (identical in all three parts — what is closed, what is Sky's, what is settled):
- DO NOT RE-FIND (the new baseline): all 20 Round-1 proposals S1–S20 (closed P0–P5) and all 11 bench items B1–B11 (9 built · B6 closed-no-fix · B1 forked to Sky). The close-out ledgers under `design-reviews/fable-audit/uplift-assets/` + `bench-assets/` are the authority — read them before judging anything they touch. Re-finding a closed item without new evidence is a calibration failure. If a closed item's SHIPPED FIX looks wrong at HEAD, that IS a legitimate new finding — cite the ledger AND the regression evidence.
- NEVER RE-LITIGATE (Sky's open either/ors — frame, never prescribe; their UI/read halves may be referenced, their decision halves are Sky's alone): Fork 1 proximity/geo-query architecture · Fork 2 points-economy honesty (migration + CLAUDE.md doc drift) · Fork 3 auth-wall & guest contract · Fork 4 k-anonymity / guest cache-scope · Fork 5 trust-model scope (verifier COUNT + guest flag-as-wrong; S3 shipped the read half only) · Fork 6 product-name collision · Fork 7 `stagePoolB` keep/kill · Fork 8 dark saved-place chips · Fork 9 `ui/Button` adopt-or-remove. Full wording: Round-1 report §5. Also open-for-Sky bench discoveries (flag-adjacent if relevant, never re-derive): the `blocked_path` per-template icon collision (⛔/🚗) · `CATEGORY_ICONS` dead export · `searchClearText` dead style · heat "no zones in view" copy tuning · OnboardingCards' local RM detection.
- DEVICE READS IN HAND (Sky's word, 2026-07-09 — both settled; do not re-open): D10 — the B6 light bulk sheet reads clean as-is (recorded: `bench-assets/BENCH-3-verification-evidence.md` §B6). D9 — the ≥500 on-glass weight reads fine on device (Sky's direct read; it POST-DATES the bench docs, which still say NEEDS-SKY-DEVICE — the paper record is stale, the read stands). Part 3 records the D9 closure in the refreshed device-gate ledger so the record catches up. All OTHER Round-1 device gates (D1–D8, D11) are verify-first: read the ledgers' current state; fold what is still open into Round 2's fresh list — never assume a gate cleared.
- PROTECT (the law Round 2 extends, never regresses): the 17-item merged PROTECT list — Round-1 report §4 / `design-reviews/fable-audit/partials/protect-merged.md`. Load-bearing spot checks: `GlassSurface.tsx` is DO-NOT-EDIT (the freeze lives in the governance docs; the code carries the arbiter contract in its docstring, no literal banner) · the map overlay's `pointerEvents="box-none"` gesture law (`MapScreen.tsx` ~:1421 — comment-enforced, NO unit test guards it) · the anonymity/honesty overlays stay byte-preserved (PROTECT-1/3/7/8 markers in source) · every arbitrated floor/ink is script-decided, never eye-tuned. Part 3 UPDATES the list: everything shipped in P0–P5 + BENCH 1–4 joins the protected baseline.
- Known-fixed, not a finding: the `/ago$/` TasksScreenFlagCard fixture flake (fixed on main `01f7392`, "make meta-line assertion time-stable").

★ SAFETY BLOCK (verbatim in every part — non-negotiable):
- Standing model directive (Sky): this ENTIRE session and ALL sub-agents on Claude Fable 5 at MAX EFFORT; do not downgrade. Thoroughness > tokens. If Fable 5 is unavailable (out of credits, hard errors), STOP AND REPORT the halt inside your deliverable plus a DECISIONS-FOR-SKY note — the fallback model is Sky's call, never yours (precedent: the Dashboard audit halted exactly here and Sky chose; Round 1 finished on Opus 4.8 at Sky's explicit direction). Model provenance is disclosed, never silent — this Round-2 prompt set was authored on Fable 5 max effort (2026-07-09); write every artifact as a durable design authority a different model could execute to.
- STRICTLY READ-ONLY: no code changes, no fixes, no builds (no EAS, no native builds), no deploys. Per Sky's Claude Corp norms: NO database changes, NO external sends, NO autonomous mutations of any kind. Never `git add`/commit/push; never switch branches or move HEAD. `git status` at the end must equal the pre-flight baseline plus ONLY `design-reviews/r2-audit/` additions. (`.claude/launch.json` shows as an uncommitted deletion in the baseline — that is Sky's workspace state; leave it exactly as found and drive the dev server from the shell.)
- NETWORK FENCE: the local web app + map-tile fetches + Supabase ANON READS through the app's own guest path are the wave-era read-only precedent — allowed. NEVER sign in to any account, NEVER submit/verify/resolve/reject a flag, NEVER upload — zero Supabase writes, zero Storage writes. The CONTRIBUTE flow is exercised up to the submit affordance and no further; post-submit states are code-inferred.
- PRIVACY IS SACRED (this app holds disabled users' location data): never expose, export, or compromise real user data or the heatmap's k-anonymity protection; judge the UI's privacy posture without ever undermining it. Real user content visible in captures stays inside `design-reviews/r2-audit/` and is never quoted beyond what the UI itself shows.
- No copy rewrites shipped: Round 2 PROPOSES voice (short before/after sketches inside findings + the copy appendix); it never edits a string.

★ ENGINE-TRUTH BLOCK (shared in every part — how truth is tagged and where the web can't see; only the LUCIDE and MATERIAL bullets may end with a one-sentence part-scoped scope note):
- HONEST EVIDENCE TAGS on every claim — Round 1's six: `web-approximated` (expo web in Chromium; RN-web ≠ iOS/Android — this is the tag for what the Round-2 brief calls "web-verified") · `code-inferred` · `test-inferred` (a jest guard proves it) · `arbiter-measured` (contrast-check.mjs output) · `lab-mockup` (Material Lab HTML — design intent, not app truth) · `NEEDS-SKY-DEVICE`. State the engine truth wherever it matters: true blur feel, scroll smoothness, VoiceOver/TalkBack, haptics, real Dynamic Type, and Reduce Transparency are device-only.
- ★ THE LUCIDE DEV-PREVIEW BOUNDARY (pre-existing, NOT a finding — first recorded in `design-reviews/fable-audit/uplift-assets/P1-verification-evidence.md`): the expo-web DEV server (Metro lazy=true) crashes the Map and Tasks screens — and the lazy heavy-lucide modals reached from them (ReportFlagModal, FlagDetailModal, NearbyFlagsModal) — into the ErrorBoundary with `createLucideIcon.default is not a function` / "Requiring unknown module". It reproduces on the pristine base; it is a dev-bundler limitation, not an app bug. CONSEQUENCE: Home, Profile, Settings, the drawer and its sub-screens render live; Map/Tasks-family live evidence is code-inferred / test-inferred / NEEDS-SKY-DEVICE — say which, per finding. PERMITTED WORKAROUND (try once, cheaply): a static export (`npx expo export --platform web --output-dir <scratchpad>/web-export` — NEVER into `~/AccessMap/dist/`) bundles without dev-lazy and MAY render Map/Tasks; if it does, the boundary lifts (record that it did, re-tag accordingly); if not, record the attempt and move on.
- DEVICE SIZES, not breakpoints: 375×667 · 390×844 · 430×932 · 834×1194, DPR 2, both themes (the wave-era 375×812 / 320×568 remain valid for apples-to-apples with Round-1 shots). Landscape: app.json orientation truth first.
- THEMES: the app's own toggle (Settings / useThemeMode) or colorScheme emulation — record which. Tiles ≠ theme (GLASS.md §12): web CartoDB dark_all is always dark; iOS Apple tiles follow the OS — never assume they match.
- DYNAMIC TYPE: RN-web ignores browser font settings — zoom/font-stress is a layout proxy (`web-approximated`); code-read allowFontScaling / maxFontSizeMultiplier / the DT guards (`test-inferred`); true ×2 is NEEDS-SKY-DEVICE.
- NATIVE A11Y, not web ARIA: accessibilityLabel/Role/State, accessibilityViewIsModal, grouping, focus order — audit via source + the RN-web-emitted ARIA tree (dump snapshots as text evidence); VoiceOver/TalkBack truth stays NEEDS-SKY-DEVICE.
- MATERIAL: per surface — right GLASS.md tier + tokens; blur budget at the worst SIMULTANEOUS state (+ the tab-bar pane, invisible to `__getLiveBlurPaneCount`); C-lite parity (`src/lib/glassMode.ts` — the Tasks-header long-press or the store/localStorage flip); Reduce Transparency designed states (iOS-only API — code/test-inferred, never renderable on web); reduced-motion parity (emulate `prefers-reduced-motion`); contrast over each surface's REAL worst-case backdrops — the arbiter decides, not the eye (Part 2 carries the full arbiter law; Part 1 cites `arbiter-measured` only from existing banked outputs or a fresh run it logs).
- MOTOR + COGNITIVE ACCESS are core findings, never nitpicks: 44pt HARD floor, gesture alternatives, one-handed reach per size class, cognitive load.
- CAPTURE CONSTRAINT (the auth carve): the guest path opens Map/Tasks browsing; signed-in states are UNREACHABLE by law — capture what the guest web allows, CODE-READ the rest, tag honestly, never fake a state.
— end of the shared blocks —

═══ PRE-FLIGHT (verify, then bank the baseline — STOP conditions are hard) ═══
1. Repo: `~/AccessMap` exists; GLASS.md at root; `src/screens/` contains MapScreen.tsx + TasksScreen.tsx + ProfileScreen.tsx. Any miss → STOP AND REPORT (wrong root or wrong machine).
2. Baseline: run the three marker checks from the SUBJECT block; record branch + `git rev-parse HEAD`. Markers missing → STOP AND REPORT.
3. Bank the fence baseline into `01_feel_orientation.md` §0: HEAD hash · branch · the full `git status --porcelain` output · date · model · serve mode. (The repo carries pre-existing untracked `qa-reports/` files, `design-reviews/`, and the launch.json deletion — they are IN the baseline, not yours.) The end-of-part fence check diffs against THIS, not against "clean."
4. Ground in the Round-1 authority (read, don't skim): the report `design-reviews/fable-audit/2026-07-04_AccessMap_Design_Review.md` §1 + §4 (PROTECT) + §5 (slate + forks + device gates) · the P0–P5 and BENCH-1..4 close-out ledgers (know exactly what closed, so you never re-find it) · GLASS.md §2/§3/§6/§7/§8/§12 · DESIGN.md's voice + severity sections.
5. Harness reality: Round 1's tools live at `design-reviews/fable-audit/tools/` (capture + a11y-tree + verify patterns) and the Material Lab at `~/AccessMap-material-lab/2026-07-02/`. Adapt copies into `design-reviews/r2-audit/tools/` — originals untouched. Playwright: reuse an existing install if present (lab or prior audit); otherwise install OUTSIDE the repo (scratchpad / PW_PREFIX pattern). Never npm-install into `~/AccessMap`.
6. `.env` present (Supabase anon reads power guest browsing). If missing: NEVER create or fill it — capture the app's real failure state instead, lean on code-read, record it in the honesty ledger.
7. Serve: `npm run web` → http://localhost:8081 (Metro writes only caches; the repo stays clean). Expect the lucide boundary on Map/Tasks; optionally attempt the static-export workaround ONCE per the ENGINE-TRUTH block.

RESUME RULE: if `design-reviews/r2-audit/` already holds partial Part-1 output — read `01_feel_render-index.md` (last row per filename wins), verify banked captures exist on disk, check which orientation/persona/lens sections are filled, keep everything verified, and continue from the first gap. Never recapture what is banked and verified; never redo a finished persona read or lens. Log the resume as a note row in the index.

PLAN-MODE NOTE: Sky runs AccessMap work in plan mode. If you are in plan mode, the plan = Stages 1–5 below with this pre-flight verbatim as step 0; after approval, execute exactly as approved — pre-flight STOP conditions still bind mid-execution (a failed check after approval means stop and report, never improvise).

═══ STAGE 1 — ORIENT (write 01_feel_orientation.md FIRST, before any capture) ═══
Sections, in order:
- §0 BASELINE (from pre-flight 3).
- §1 THE DELTA DIGEST: what P0–P5 + BENCH 1–4 actually changed on each surface, from the ledgers, spot-verified by grep at HEAD (e.g. the 8 emoji→Lucide swaps, the 9 bulk sheets, the motion-hygiene gates, LiveStatusRegion, the Clear≥44pt bar). Round 1's capture bank (`fable-audit/assets/`) remains citable ONLY for surfaces the delta digest shows untouched — every reuse says so.
- §2 THE FEEL INVENTORY (the audit's raw nerves, each with path): press-feedback primitives — `src/components/ui/PressableScale.tsx` (adopted) · `src/components/ui/Button.tsx` (zero call sites; Fork 9 — census it, never re-litigate adopt-or-remove) · `src/lib/haptics.ts` (selection/impact/notify) · pressed-style patterns in raw Pressables. Motion — `motion` tokens (`src/theme.ts` ~:462, duration/easing/spring incl. pulse=700) · the drawer's 220ms RM-gated sub-screen delay (`HamburgerDrawer.tsx`) · `HamburgerDrawer` `animationType="none"` · sheet/modal animationTypes across all mounts · `useReducedMotion()` (`src/lib/accessibility.ts`). Voice — `src/lib/copy.ts` (shared strings) · `src/lib/a11yText.ts` + `src/lib/announce.ts` (SR strings) · the rest is inline JSX per component (that dispersion is itself feel-relevant). States — `LiveStatusRegion.tsx` · `FlashBanner.tsx` · `ui/Skeleton.tsx` (static at RM/RT/C-lite) · the empty-filters recovery card (`MapScreen.tsx` ~:1929, PROTECT-2 — "the app's best moment", the bar every state is judged against) · ErrorBoundary variants · offline banners (`copy.ts`). Signature — the severity grammar single-source (`src/theme.ts` ~:533 + `SeverityBadge.tsx` + `severityColor()`), the Wayfinder mark + `ctaFill`, the bespoke CategoryIcon set (PROTECT-16).
- §3 HOW-TO-REACH LEDGER: reuse Round 1's orientation §5 ledger (`fable-audit/01_orientation.md`) — verify a 6-row sample still reproduces at HEAD, record drift, extend for any new state Part 1 needs (C-lite flip via `@accessmap/glass_mode_v1`; onboarding seed `@accessmap/onboarded_v1`; the wave coords 49.8874,-119.4925).
- §4 HONESTY LEDGER: engine truth, unreachable states, capture caveats — started here, appended throughout the run.

═══ STAGE 2 — CAPTURE THE FEEL EVIDENCE (delta-first; append index rows as you go; a stall must bank progress) ═══
Fresh captures go to `assets/<group>/` with rows in `01_feel_render-index.md` (`filename · screen · theme · width · state · tag · note`; resumable skip-if-exists; verify-script PASS pasted at the tail — adapt Round 1's verify pattern). Groups: `base/ press/ transitions/ states/ voice/ rm/ glassmode/ a11y-tree/ probes/ annotated/`. The matrix (light + dark; standard size everywhere, SE + Pro Max where layout-sensitive):
1. BASE: every reachable screen and sheet at rest (the bench moved icons + sheets — Round-1 base shots are stale for those; recapture).
2. PRESS: pressed/hover/focus-visible states of the primary controls per screen (tab bar, FAB, chips, triage buttons, drawer rows, form controls) — paired rest/pressed shots where the web can hold the state; otherwise code-read the pressed style and say so.
3. TRANSITIONS: before/during/after frames of sheet presentations, drawer open (+ its 220ms sub-screen swap), tab switches, modal dismissals — cheap frame pulls, not video; RM-emulated pass for each (the RM variant is a designed state, capture it as one).
4. STATES: every empty/loading/error/offline/success moment reachable as guest — empty filters (the PROTECT-2 card), zero-results lists, offline banners (CDP offline), locate-denied, skeletons mid-load, the submit-enabled boundary (never press submit), LiveStatusRegion moments if triggerable read-only.
5. VOICE: close-crop captures of small-copy moments (buttons, empties, banners, confirmations, section headers, hints) — the F4 reading set.
6. RM + GLASSMODE: the motion-bearing moments under `prefers-reduced-motion`; full vs C-lite on Tasks + Profile + one sheet (localStorage flip) — both themes, standard size.
7. A11Y TREES: per-screen RN-web ARIA snapshots for every surface a lens or persona will read (`a11y-tree/<screen>__<theme>__<width>.txt`).
Map/Tasks-family surfaces under the lucide boundary: attempt via the static-export workaround once; otherwise their rows enter the index as UNREACHABLE-ledger entries with code-read pointers — never faked, never silently skipped.

═══ STAGE 3 — BLINDED PERSONA READS (01_feel_persona-reads.md) ═══
Fresh parallel agents, ZERO project context, given ONLY capture files (+ a flow-ordered file list). Each prompt contains: the persona brief, the exact file paths, and the blinding fence ("you know nothing about this product; do not read source, docs, or anything else on disk"). Their unprimed reactions are first-class evidence — bank them verbatim, one section per reader, appended as each lands. The Round-2 question they all answer: does this feel CARED FOR — designed, warm, one hand — or utilitarian-bare? Plus per-persona:
- R1 — wheelchair user planning a route (FIND-flow + state shots): does the craft help or decorate? What feels considered vs bolted-on?
- R2 — blind, VoiceOver-primary (a11y-tree dumps FIRST, then PNGs): is the SPOKEN experience also designed — labels as prose, announced moments, rhythm of the traversal? Where is the spoken app blunter than the visual one?
- R3 — low-vision, large-type (DT-proxy + standard pairs): does the craft survive magnification, or is beauty only at 100%?
- R4 — one-handed / limited-dexterity (size-labeled base + press shots): do presses acknowledge instantly and legibly? Anything feel dead to the hand?
- R5 — senior mobile craft reviewer (full base set, both themes): rhythm, alignment, transitions, restraint — where does craft sing, where does it crack? Would this portfolio-impress?
- R6 — first-time user under cognitive load (onboarding + FIND + CONTRIBUTE sequences): does the voice welcome or process me? Do waiting/empty/error moments reassure?
- R7 — Reduce Transparency user (NEW this round; gets the C-lite/engineered + RT designed-state captures and ONLY those): judge MY version of the app on its own — does it feel first-class, deliberate, complete? Name every place it reads as "the budget fallback."
Then ONE adversarial completeness critic (context-free, given only the index + the Stage-2 matrix spec): what's missing, thin, or contradictory? → run the top-up captures → append rows → record the critique PASS at the index tail.

═══ STAGE 4 — THE SIX FEEL LENSES (parallel agents; each banks to partials/F{n}.md the moment it completes) ═══
Each lens agent gets: its brief below + `01_feel_orientation.md` + its asset groups + the persona reads + read-only repo access. FINDING FORMAT (Round 1's): `F{n}-{seq}` · Where (screen/component/file:line) · What · Why it matters (mission- and thesis-weighted) · Evidence (capture filenames + code refs + [tag]) · Severity. SEVERITY SCALE (Round 1's, verbatim): CRITICAL = an access failure on a core flow, a trust-breaking defect, or overlap/clip/occlusion at any size · HIGH = materially impairs a disabled user's job or the cohesion/trust mission · MEDIUM · LOW · POLISH. At equal tier, access failures outrank aesthetic ones. Every finding-with-a-direction carries the RESTRAINT CHECK verdict inline. Every lens section ends with: PROTECT nominations + copy observations.
F1 — TOUCH & ACKNOWLEDGMENT. Census EVERY interactive element per screen: does the press acknowledge — visually (scale/opacity/highlight), haptically (which vocabulary word), audibly (SR state change)? Map the PressableScale / pressed-style / dead-to-the-hand distribution; is the haptic vocabulary ONE vocabulary (selection vs impact vs notify used semantically) or ad-hoc? Web hover/focus-visible parity. Feedback must survive RM honestly — instant acknowledgment, never absent. The 44pt floor stays a floor, not a finding-quota.
F2 — RHYTHM & OPTICAL ALIGNMENT. The spacing scale in practice: token discipline vs ad-hoc numbers per screen; vertical rhythm down each scroll; optical alignment (icon-to-text baselines, badge centering, chip rows, card grids); breathing room as the ethos' restraint; where density fights the editorial read. Grid the severity disc/badge alignment everywhere it appears.
F3 — TRANSITIONS & CONTINUITY. Inventory every screen/sheet/drawer/tab transition: trigger · curve/duration vs the motion tokens · native-driver? · RM parity (the RM variant must be a DESIGNED stillness, not an amputation). Judge the drawer's `animationType="none"` and its 220ms sub-screen delay as designed moments — deliberate stillness or dead air? Does bulk glass ARRIVE like material (sheet presentation quality)? Cross-screen handoffs (Tasks card → Map focus; callout → detail) — continuity of the one material world through motion.
F4 — VOICE & MICROCOPY. Read every small copy moment (the Stage-2 voice set + a full inline-strings sweep): buttons, empties, errors, banners, confirmations, headers, hints, a11y labels AS PROSE. Is it one voice — warm, human, civically serious? Where is it utilitarian-bare, bureaucratic, or inconsistent (flag vs report vs barrier; sentence case; contractions)? The privacy-forward trust voice (PROTECT #11) is the anchor register — proposals correct TOWARD it. Before/after sketches allowed inside findings; nothing ships.
F5 — STATES AS DESIGNED MOMENTS. Census every empty/loading/error/offline/success state per surface against the PROTECT-2 bar (explains + offers the fix + stays honest): which states meet it, which are bare spinners/dead ends? Skeleton coverage + honesty (static under RM/RT/C-lite); waiting moments (does the app hold your hand or leave you hanging?); success moments (submit confirmation, points flash, "still trying" / "Report filed" LiveStatusRegion beats) — do they land as designed beats? Failure states are design surfaces, not plumbing.
F6 — SIGNATURE DEEPENING (the thesis lens — weight it highest with F1). Round 1 named the signature: the severity grammar (numbered amber→red disc + plain word + one-line stake). Where else can it live WITHOUT decaying into decoration — candidates to judge, not presume: app icon/splash posture, onboarding's first lesson, empty/zero states, Profile's stats, Leaderboard, Achievements, notification/banner moments, the Wayfinder mark's cameos. What would make AccessMap visually unmistakable at a glance — grounded in the EXISTING signature, never a new gimmick? HARD RULE: the grammar means real-world severity; any placement that dilutes that meaning (ornamental discs, severity-colored chrome) FAILS the restraint check by definition. Also judge the inverse: places the signature should appear and doesn't (grammar gaps at decision surfaces).

═══ STAGE 5 — ADVERSARIAL VERIFICATION + ASSEMBLY ═══
- Every CRITICAL and HIGH finding gets an independent skeptic agent (fresh context; the finding + its cited evidence + read-only repo access) charged to REFUTE it: re-read the code, re-check the capture, attack alternatives (capture artifact? web-only delta? already shipped in P0–P5/BENCH — check the ledgers? taste-not-defect?). Verdicts: CONFIRMED / ADJUSTED (surgical correction stated) / REFUTED (dies or demotes — record why). Lens text is amended to post-verification canonical BEFORE assembly.
- ANNOTATED CAPTURES: every surviving CRITICAL/HIGH gets one annotated image in `assets/annotated/` (arrows + a one-line caption) where a capture exists; code-only findings get the code excerpt instead.
- Assemble `02_feel_findings.md` incrementally: header (subject/date/HEAD/inputs/engine caveat/method/tags/severity scale/finding format) → §Calibration & verification (verdict table · dedup/cross-ref map — one defect counted once · the FINAL FEEL LADDER Part 3 will walk) → F1–F6 sections → §Persona digest (verbatim highlights, credited, incl. R7's first-class-or-fallback verdict) → §Copy-observations index → §Probe log → §Honesty ledger.

═══ VERIFICATION + WIN CONDITION ═══
- `01_feel_orientation.md` §0–§4 filled; `01_feel_render-index.md` verify PASS at tail (0 missing / 0 failed / 0 orphan); every UNREACHABLE state has a ledger entry + code-read pointer.
- `01_feel_persona-reads.md` holds all SEVEN reads + the completeness-critique PASS. All six lens sections present; every CRITICAL/HIGH carries a skeptic verdict (+ annotated capture or code excerpt); the ladder + dedup map complete.
- Nothing in `02_feel_findings.md` re-finds a closed S/B item without regression evidence; every direction carries its restraint-check verdict; the lucide boundary is named wherever it constrained evidence.
- FENCE: `git status --porcelain` == the §0 baseline + `design-reviews/r2-audit/` additions only; `git diff --stat` empty (no tracked file touched).
- THE HANDOFF TEST: a fresh window given ONLY `design-reviews/r2-audit/` could run Part 3's feel half — the orientation names every file group and how to re-probe.
WIN: the feel of the whole app is judged surface by surface — every press, pause, transition, sentence, and state — through seven unprimed personas (RT and VoiceOver included), every CRITICAL/HIGH skeptic-survived, the signature-deepening candidates ranked with restraint verdicts, nothing closed re-found, nothing open re-litigated. Close by printing the absolute paths of the four deliverables + `assets/`, the counts (findings by severity · verdicts · captures), and this line: **STOP — Part 1 ends here. Do NOT begin Part 2 (r2_part2_material.md runs in its own fresh window).**
