# R2 Part 3 — THE ROUND-2 SLATE (working file + process log)

Subject: AccessMap @ `bench/4-quality` `a8549ff3d6d15ed4410b71d803d50a130613d3d0` · Part 3 of 3 (synthesis) · strictly read-only (writes only inside `design-reviews/r2-audit/`).
Model provenance (disclosed, not silent): Part 3 STARTED on `claude-fable-5` max (Sky-pinned via `/model`) — Stage 1 reconcile + the Stage-2 drafter Workflow (`wf_d48560db-407`) both ran on Fable 5. **Mid-Part-3, at the Stage-2 merge boundary, Sky switched the session to Opus 4.8** (`/model claude-opus-4-8`) — Sky directly operating the fallback, the exact Round-1 precedent ("Round 1 finished on Opus 4.8 at Sky's explicit direction"; the fallback is Sky's call, never the agent's). From the Stage-2 merge onward (this merge · Stage 3 skeptics + panels · Stage 4 assembly · Stage 5 cold-read) runs on **Opus 4.8 at max effort at Sky's direction**; remaining sub-agents pinned `opus`/max. No degraded model anywhere; the split is recorded in the report's front-matter.
T-ids are Round 2's series — disjoint from Round 1's S1–S20 by design; reading-test breaks cited as RB1…RB14 (declared alias of `04` §1's B1–B14; collision guard vs bench B1–B11).

## PROCESS LOG

1. **Step 0 pre-flight — PASS** (2026-07-10): markers 3/3 · branch/HEAD match (`bench/4-quality` @ `a8549ff`, == Parts 1–2 §0, no drift) · all required inputs present and structurally complete · banked arbiter header re-read (never trusted): `EXIT CODE: 0 … RESULT: ALL PASS`, 80 pairs · fence baseline banked (scratchpad `r2p3/fence-baseline.txt`; `design-reviews/` wholly untracked, so end-fence = no NEW porcelain line + `git diff --stat` empty). Judgment call logged: the reading-test break list lives in `04` §1 rather than inside `03` — nothing absent on disk; proceeded.
2. **Stage 1 RECONCILE — COMPLETE** → `partials/r2p3-stage1-reconcile.md`. One merged ladder (1 CRIT · 22 HIGH · 29 MED · 19 LOW · 7 POLISH = 78 defect-class items; RB12/RB13 ratified-tier rows, not defects). Cross-part dedup: zero true duplicates; 4 facet cross-refs recorded (RB4↔F6-01 · RB11↔F4-19 · RB10↔F5-PROTECT recovery card · RB2↔F1-11). Conservation prelim buckets assigned for all 65 F + 57 M + 14 RB + 12 §6-register ids; independent no-orphan check scheduled against the FINAL table (post-Stage-3).
3. **Stage 2 DRAFT — COMPLETE**: Workflow `wf_d48560db-407` (4 drafters, fable/max, schema-forced, 0 errors; raw pool banked at `partials/r2p3-draft-pool.json`). 28 candidates returned with full cluster-ladder coverage (each cluster's notes verify per-id conservation; F1-09 deliberately ceded to PARK-with-direction). Merge lead composed 28 → **20 T-proposals**: 13 pass-throughs + 7 merges (T1 = F2-01 callout + F3-04/05/06 continuity family, same surface/moment · T5 = grammar suite + F6-03 onboarding teaching row, one signature system · T11 = F1-03 SR state + F1-04 focus visibility, the non-touch perception layer · T12 = F3-01 warm/dressed arrival + F3-02/07 exit symmetry, one drawer passage · T13 = F2-02/11/13 type stress + F2-05/06/12 alignment, one editorial frame · T17 = F4 sweep + F4-06/09 promise strings, one copy pass · T20 = §6.9 + §6.11 + §6.5/§6.6, three independent one-commit hygiene branches grouped for slate economy). Re-keys for conservation purity: T15 resolves the M-48 INK DRIFT only (§6.12 — the mechanism-kill NO-ACTION stands); T20's containment leg keys to §6.9 (M-40's census row stays with T2). Killed-by-lens candidates stay dead (digit-on-pin, tinted picker buttons, severity-toned toasts, grammar-speaking zero states).

## THE SLATE — 20 proposals (T1…T20)

### T1 — The map payoff lands true — clear-map callout + one honest rhythm ★
**WHERE:** src/components/PlatformMap.web.tsx (Popup ~:430, container :759; theme-flip tile swap :646–657) · src/components/PlatformMap.tsx (native showCallout path) · src/screens/MapScreen.tsx (overlayTopGroup onLayout measure; :159–170 retryShowCallout ladder; :2483 Nearby select, :1387 handleDetailViewOnMap; overlay :1416/:2686 untouched as law).
**Resolves:** F2-01 (CRITICAL), F3-04, F3-05, F3-06 · **Effort:** M · **Tier:** Signature ★ · **NEEDS-SKY-DEVICE:** yes
*Merge of the signature cluster's F2-01 occlusion fix and the touch-motion cluster's F3-04/05/06 continuity family — one surface (the map callout), one moment (the payoff beat). The CRITICAL leads; rank #1 regardless of tier sort.*
1. **WHAT** — Opening any pin callout always lands the WHOLE callout — title, "SEVERITY 4 OF 5 · SIGNIFICANT · VERIFIED", "Reported Jun 4, 2026" — in clear map, in one designed rhythm, every tap. (occlusion) Web: MapScreen measures the top chrome cluster (onLayout on overlayTopGroup) and threads that height through PlatformMap's shared imperative interface; the Leaflet Popup gains autoPanPaddingTopLeft = measured-cluster-height + margin, so autoPan clears the chrome, not just container edges. Under Reduce Motion the suppressed pan becomes an instant setView cut (animate:false) to the same clear position. Native biases animateTo below the chrome band. (rhythm) Three continuity mechanics join it: wire the existing canceller at the two careless retryShowCallout sites (ref-stored, cancel on next selection/close) so rapid Nearby A→B can never pop A's callout on a map centered on B (last-tap-wins); add a 0ms first rung before the 250/400/550/700 ladder so the common case — and RM's instant jump — collapses to same-frame payoff while the retries still cover re-clustering; and the theme-flip tile swap mounts the incoming layer before removing the outgoing (or setUrl in place) so the map never blanks mid-flip. We pan the map and schedule beats; we never restack z-order.
2. **WHY** — Resolves F2-01 — the merged ladder's only CRITICAL, and the slate's #1. The Leaflet popup pane can never escape the zIndex-0 map container while chrome rides the zIndex-10 sibling overlay, so any top-third callout composites under glass — and the occluded text is the severity grammar itself, at the S3 trust doorway. THESIS: one material (chrome and callout stop smearing into one illegible plane), one voice (the grammar — the actor — is the exact text being silenced). The fix direction is the finding's own: pan padding plus an RM-safe instant equivalent. Beauty was costing honesty here; this buys it back for free. And the continuity legs (F3-04/05/06, three LOWs, one family): an explicitly selected flag answered with a different flag's callout is a wrong-answer moment on the trust surface; RM users receiving a fixed ≥250ms dead beat are the motion system's one inequity imposed on exactly the users it elsewhere protects; the map is the only surface that visibly rebuilds instead of transforming during the theme moment. THESIS: one material (chrome and callout stop smearing into one illegible plane; the floor never blanks), one voice (the grammar — the actor — is the exact text being silenced), one hand (the payoff lands with one rhythm, every tap, by intent). Signature by definition: the S3 doorway on the app's richest surface becomes flawless at the moment it is most seen.
3. **PERSONA IMPACT** — R7 FIRST and gains MOST: under Reduce Transparency the rail is engineered-opaque, so today's ghosting becomes total occlusion — this guarantees RT users full visibility of trust text, first-class by construction. R1: "sloppiness reads as 'maybe the data is sloppy too'" — the seam closes at the credibility moment. R5's "worst frame in the pack" and his occlusion-pass precondition for premium: answered. R3: meta readable at default size, and clear-map placement survives zoom. R6: the trust text is legible at the exact judging moment. R2: SR path unchanged (marker alt already speaks the full sentence). R4: the count pill stops overlapping tappable callout area. On the rhythm legs: R4, likeliest to fire the rapid A→B race with imprecise re-taps, gains guaranteed correctness; RM users (whom the ladder currently short-changes) gain a same-frame callout; R7 running RT gains that plus theme-flip continuity — the map keeps its floor while the world re-inks; R3 gains a dark-flip without a blinding blank.
4. **TRUST + COHESION** — This is the S3 doorway — the surface Round 1 built so a wheelchair user can judge a report before routing around it. At HEAD the app's most trust-sensitive text renders half-ghosted under its own chrome in both themes; R1 read it as data sloppiness, R5 as a craft disqualifier. After: the material system reads as one stage that reserves space for its actor. The one-material claim becomes literally true at the app's most-seen collision point. The luck-vs-intent tells close too: a payoff landing anywhere in a 450ms window reads as luck; a callout occasionally answering the previous question reads as sloppiness — R1's exact chain. Tasks→Map, deep-link, Nearby, and detail handoffs read as one governed camera with one rhythm and one honest floor.
5. **A11Y + RESTRAINT** — Restores legibility and honesty; costs nothing — finding's inline RESTRAINT: PASS carried. RM contract preserved exactly: no animated pan under RM (S12's intent), replaced by an instant cut, consistent with the designed-stillness law (end states present, presence at t150 n/a for a cut). No target, ink, or floor changes; WCAG untouched-to-improved (occluded text is a de facto legibility failure today). The rhythm legs add zero motion and subtract defects: the 0ms rung deletes the RM dead beat; cancellation removes stray late-callout state; the tile overlap replaces a blank with continuity (labels legible throughout). RESTRAINT: PASS — restores legibility and honesty, costs nothing.
6. **DEPENDENCIES / CONFLICTS** — Preserves PROTECT box-none overlay law (MapScreen.tsx:1416 pointerEvents untouched; cluster height read via onLayout only). Preserves S12 RM-gating intent (no animated pan under RM) and the cross-platform camera grammar (reliability extensions only). Preserves the SR nested-detail continuity pattern — only the sighted-path row select (:2483) is touched; the SR path (:2461–2472) stays byte-identical. Preserves S6 (zoom control stays removed). PlatformMap interface change coordinated across .tsx/.web.tsx per gotcha #3. F3-05 is swap MECHANICS only — the tile family is Sky's open Fork 7 eye-candidate, never re-litigated. Shares MapScreen.tsx with T11/T13's map hunks and the T5-grammar chip re-ink — serialize commits within the phase. Nothing re-opens S6/S12.
7. **VERIFICATION** — Occlusion-regression: re-shoot the finding's capture pair (base/map__light__390__pin-callout.png + dark twin) with a top-third pin — zero overlap between callout bounds and pill/rail bounds, both themes. Layout guards (jest fake timers): web Popup receives autoPanPaddingTopLeft = measured cluster height + margin; the RM branch asserts instant setView (animate:false); rapid Nearby A→B yields exactly B's callout with A's 550/700 rungs cancelled; leaving the Map tab mid-ladder cancels cleanly; the ladder fires a same-tick 0ms attempt; under RM showCallout invokes in the same frame as the instant jump; theme flip banked as a t-series (old tiles persist beneath the incoming layer). No ink change → NO arbiter. Blur budget unchanged (no pane added; worst simultaneous map state stays at count + the invisible tab-bar pane). SKY DEVICE (native Callout is the same collision class, code-inferred only): open callouts on top-third pins both themes, RM on/off, VoiceOver pass on the callout→details path — callout sits fully below the pill/rail; rapid Nearby A→B yields B only; flip Appearance mid-session — the map never blanks.


**Skeptic verdict: FIX (absorbed).** measure the PERSISTENT top-chrome band (mapHeaderRow:1429 + topRow:1451), NOT the filterPanel-nesting overlayTopGroup:1423, and CLAMP the autoPan inset to ~45% of map height; set `autoPanPaddingTopLeft` as a declarative/open-time Popup option (correct the 'imperative interface' wording); optional single shared canceller ref across all four callout sites (:1178/:1264/:1387/:2483) to also kill cross-path races.
### T2 — One Material World — the MP0–MP5 migration train (mounted whole)
**WHERE:** 19 files across six phases: App.tsx, MapScreen.tsx, UpdateBanner.tsx, PhotoLightboxModal.tsx, LiveStatusRegion.tsx, ProfileScreen.tsx + SettingsScreen.tsx (comments only) · HomeScreen.tsx · MyReportsModal/MyWatchedModal/ActivityFeedModal.tsx · AchievementsModal.tsx, LeaderboardScreen.tsx, NotificationPrefsModal.tsx, NotificationPreferencesScreen.tsx · FlagDetailModal.tsx, StatusHistoryModal.tsx · AdminScreen.tsx + RootNavigator.tsx. theme.ts and GlassSurface.tsx untouched.
**Resolves:** RB1,RB2,RB3,RB4,RB5,RB6,RB7,RB8,RB10,RB11,RB14,M-06,M-22,M-23,M-24,M-36,M-37,M-38,M-39,M-40,M-41,M-42,M-49,M-52,M-55,M-56 · **Effort:** L · **Tier:** Signature · **NEEDS-SKY-DEVICE:** yes
*The train is 04 §7 VERBATIM — 6 phases MP0…MP5, wrapped, never redesigned; skeptic FIXes route back as noted amendments against 04.*
1. **WHAT** — ONE Deep Field world after onboarding, delivered as 04 §7's Sky-gated linear train — every phase built + green + STOPPED on its branch; Sky merges; Sky owns D-gates. MP0 `r2/mp0-first-frame` (cut from the tree Sky designates; spec authored at a8549ff) — M-56 boot frame → themed wash, M-55 map empty-card → row+wash family, M-52 UpdateBanner → banner tier; rides the Stage-6 in-tier ink repairs (M-46 lightbox chrome → textOnBrand; M-51 chip/info-fill) + M-43/Settings comment hygiene: 7 files, five S/XS mechanical blocks + 2 comments — the highest feel-per-line phase. MP1 `r2/mp1-home-stage` — M-06: 1 file, every mount and re-ink enumerated, no data paths. MP2 `r2/mp2-profile-lists` — M-39·M-40·M-38: 3 files, one shared B4-engineered recipe applied three times. MP3 `r2/mp3-overlay-rest` — M-37·M-22·M-42·M-24: 4 files, same recipe; Leaderboard densest, still material-only; absorbs the retry/segment→ctaFill and rankTop→brandText drift re-inks. MP4 `r2/mp4-trust-ledger` — M-36·M-41: 2 files, one 82 KB; the ink map + pre-passed union declaration keep it single-window; hardest last; the reopen-button 2.07 drift re-inks to brandText (⚑ Sky may veto). MP5 `r2/mp5-admin-editorial` — M-23·M-49, optional (§6.7): skipping records B14 as deliberate and conservation reads 13/15. Partition 15/15; zero theme.ts edits; GlassSurface.tsx never touched — the primitive is threaded, never forked.
2. **WHY** — Resolves the material HIGH band nearly whole: RB1 two-worlds tab switch, RB2 leftover legacy pane, RB3 glass-over-paper, RB4/RB5 the trust-ledger holdout at every S3 doorway, RB6/RB7 the Profile and Settings hallway splits, RB8 the dark-launch boot strobe — plus RB10/RB11/RB14 and all 15 MIGRATE rows. This is the THESIS's ONE MATERIAL leg executed literally, and ONE HAND: every sheet becomes the same recipe from the same primitive. ONE VOICE holds by gate — strings byte-identical. Signature tier earned: the Deep Field material is the taste anchor's hero, and the moment every doorway opens onto the house material is the most ownable moment Round 2 can ship — access-first by construction, not garnish.
3. **PERSONA IMPACT** — R7 (Reduce Transparency) gains the most, first-class: today RT users see TWO sheet materials (glass sheets render the designed 0.97 overlay + borderStrong; opaque sheets render plain surface) — after the train every sheet renders the ONE designed RT state, and C-lite ships engineered-literal by construction, a specced gate, not a fallback. R6 stops changing worlds at tab switches and doorways — fewer where-am-I moments. R3 gains six shipped-AA-drift repairs (M-46's functionally invisible dark lightbox chrome, M-52's 3.42 View button, M-51's chip, M-22's rankTop, M-36's 2.07 reopen) plus ≥500 on-glass body weight. R2: strings, roles, focus byte-identical by diff-gate. R5 gets the loudest break (B1, on the default tab) gone. R1's trust ledger keeps its meaning, elevated in material; R4: zero targets move.
4. **TRUST + COHESION** — The app's most important honesty surface — the trust ledger — stops wearing its least-considered material; every S3 doorway (map callout, Tasks card, Profile row) lands in one world. The first painted frame stops strobing white at dark-mode users. GLASS §8's aspirational Home line becomes shipped truth. Credibility mechanics: six live AA drifts move from the trials file's exit-1 findings record to repaired code, so the paper record and the pixels agree — the audit's one-material claim becomes checkable fact rather than intent.
5. **A11Y + RESTRAINT** — Floor preserved and raised: every re-ink maps to an existing arbitrated token — zero new tokens, zero eye-tuned floors, the arbiter decides (80 pairs, exit 0 at spec time). RT + C-lite implemented as specced is a hard gate, not a deferral. Blur budget: the whole train adds ZERO live panes anywhere (engineered-first rule); Tasks' shipped worst 12 is never exceeded — the deepest stack (Tasks 12 → FlagDetail → StatusHistory) is still 12. Zero targets move; zero motion added (modal RM gates untouched). RESTRAINT: PASS — this train deletes entropy. The one affordance trade (reopen orange → brandText, 2.07 → 4.95/5.42) buys AA with flair, not legibility, and carries an explicit Sky veto flag.
6. **DEPENDENCIES / CONFLICTS** — Linear MP0→MP5; no two phases share a file (§5 verified); MP2→MP3→MP4 apply one recipe in rising complexity — hardest (82 KB FlagDetail) last. Preserves PROTECT-1 (Nearby byte-identical) · preserves PROTECT-3 (ReportFlag untouched) · preserves PROTECT-4 (severity badges keep arbitrated fills + textOnColor) · preserves PROTECT-7 (modal RM gates unchanged) · preserves PROTECT-8 (POINTS single-source + the anonymity honesty set — the FlagDetailModal Reported-anonymously string byte-preserved, MP4); honest-arrival is preserved SEPARATELY (localized banner strings untouched) · preserves PROTECT-10/S17 (Home peek pointer-inert + suppressAttribution, diff-checked) · preserves the MapScreen box-none law (M-55 is render-only) · preserves the S3 read half byte-identical; Fork 5's DECISION half never crossed (no count, no guest flag-as-wrong). M-14 untouched (Fork 8 boundary); mechanism-uniformity lever stays Sky's (§6.3). Deliberately excludes theme.ts — the M-48-ink candidate is the out-of-train complement; the MyWatched prop gap is carried byte-identically and closed by its own candidate.
7. **VERIFICATION** — Per-phase hard gates: typecheck 0 · jest green, count recorded in the phase report · lint 0 errors, no new warnings · the phase's arbiter declaration exit 0 — the spec-level tools/r2-material-stacks.json re-run verbatim, with sibling tools/r2-mp{n}-stacks.json ONLY on drift (drift = re-declare == re-run) · §3 PROTECT statements re-verified by diff · RT + C-lite as specced, not deferred · the seven immutable prior stacks files untouched (diff-check). Blur-pane count asserted at each screen's worst simultaneous state per §2f (+1 manual tab-bar pane, invisible to __getLiveBlurPaneCount). Sky device gates: dark-launch first frame (M-56); Home stage in both palettes; frost/perf feel of the engineered sheet tier over live content; an RT sweep (OS + Settings toggle); a VoiceOver walk of FlagDetail post-MP4.


**Skeptic verdict: FIX (absorbed).** [FIX routes as noted amendments against 04 — the train BODY is unchanged.] WHERE reads 19 files not '18'; field-6 correct PROTECT-8 to the ANONYMITY SET (state honest-arrival preservation separately); add the MapScreen.tsx serialization note (MP0's M-55 hunk shares the file with T1/T7/T9, lines disjoint).
### T3 — One press vocabulary — every control answers the hand
**WHERE:** src/components/ui/PressableScale.tsx (internal static dim + hover); RootNavigator.tsx (custom tabBarButton); MapScreen.tsx filter panel (collapse header, S16 Clear, saved-set pills, category/severity/status/affects chips, '+ Save current'); TasksScreen.tsx cat chips + sort tabs; FlagDetailModal.tsx (~15 uncovered controls incl. Close, Directions/Share/History, edit/save/cancel, reopen); Sheet.tsx close + bespoke closes in LegendModal/NearbyFlagsModal/FeedbackModal/MyFeedbackModal/StatusHistoryModal/AchievementsModal/NotificationPrefsModal; HomeScreen.tsx search pill + mapPeekHint chip + list paddingBottom; PlatformMap.web.tsx:471–488 callout button. GlassSurface.tsx never touched.
**Resolves:** F1-02,F1-05,F1-06,F1-07,F1-10,F1-11,F1-12,F1-13,F1-14 · **Effort:** L · **Tier:** Signature · **NEEDS-SKY-DEVICE:** yes
1. **WHAT** — One press dialect app-wide: the static-truth dim. PressableScale itself gains an always-on ({pressed}) tint layer — reusing the FlagCard cardPressed values — with the 0.97 spring kept strictly as motion-allowed garnish and a hovered tint riding the same internal style; one file upgrades ~20 controls (Map's 7 tools, zoom, both FABs, Home locate/refresh/retry/recents/pill, Tasks triage). The tab bar gets a custom tabBarButton on the repaired primitive + hapticSelection. The filter language adopts the panel's own presetBtnPressed dim; FlagDetail's silent controls and the modal-family closes finish their surfaces (one dim in Sheet.tsx cascades; four bespoke closes hand-fixed); Home's pill and peek-hint chip take a pressed tint (plus a few pt bottom list padding — the F1-10 rider); the web callout 'Open details' button pads to ≥44px with the ctaFill pressed/hover/focus treatment. All values token-bound, reused, never invented.
2. **WHY** — Resolves F1-02 (HIGH — the primitive's RM feedback hole institutionalized in its own doc comment), F1-05/06/07 (MED), F1-10/11/12/14 (LOW), F1-13 (POLISH). This is the thesis's ONE HAND leg made literal: the app speaks three press dialects, its most-pressed chrome is silent, and R4's begged-for fix #1 is exactly this. It also serves ONE MATERIAL — material that visibly answers pressure behaves like matter. Taste anchor honored: acknowledgment in stillness, a dim not a dance, always legible. Signature claim: in an app built for tremor users, no press anywhere goes unanswered — and the answer never depends on motion, platform, or ability. The two PROTECT'd patterns are the grown seed, not new invention.
3. **PERSONA IMPACT** — R4 lives in the during-channel ('the moment-of-touch layer is missing almost everywhere, and that's the layer I live in') — re-press-from-uncertainty disappears at the filter language, tab bar, closes, and FABs. R7 is first-class by construction: the static dim IS the truth layer — blur-free, motion-free, opaque-friendly — so RT/C-lite users get the identical answer as everyone; their explicit ask ('that's not reduced motion, that's removed feedback') closes. R3 gains a luminance-based state change that reads at low vision and large type. R1 and R6 gain confidence at the filtering language they plan routes with. R2's audible loop is untouched; R5's execution-hygiene score rises where it was cheapest to lose.
4. **TRUST + COHESION** — The 'many hands' tell — sibling controls on one pane answering differently (preset buttons dim, chips dead) — disappears; drawer, tab bar, filter chip, and close button finally speak one dialect. A press that answers instantly reads as an app that is listening. The one sub-floor new control in the app (web callout button) joining the 44pt floor closes the last new-chrome exception, so the S3 doorway's first press feels governed by the same hand as everything Round 2 praises.
5. **A11Y + RESTRAINT** — Floor preserved and raised: F1-14 brings the callout button to ≥44px (2.5.8 floor held everywhere new); static dims survive RM by construction — zero motion added, B5's stillness tests keep passing per the skeptic's own note; haptics stay OS-governed. Pressed ink pairs are arbiter-gated, never eye-tuned. Delight (spring, hover) stays layered above truth per the FlagCard model. RESTRAINT: PASS — strictly additive acknowledgment; no legibility, reachability, or honesty cost anywhere in the estate.
6. **DEPENDENCIES / CONFLICTS** — Preserves the drawer-row pressed dim (spread, never dilute) and the FlagCard static-truth + gated-delight layering (the internal model for the primitive repair). Preserves haptics.ts RM-independence (tab haptic is an extension). Preserves new-chrome target discipline (44pt everywhere; dims shrink nothing). Preserves the S17 pointer-inert peek (tint lives on the mapPeekHint chip, never the tiles) and the audible filter-acknowledgment loop (untouched). Fork 8 boundary: saved-set pills get the dim only — zero dark-color restyle. Fork 9 boundary: Button.tsx untouched. Threads PressableScale, never forks GlassSurface. Shares MapScreen/TasksScreen with T11 (chip SR-state) and T1 (callout) and RootNavigator with T12 (drawer passage) and T16 (badge) — separate hunks; land after or with the Report-sheet candidate so core flow leads.
7. **VERIFICATION** — Arbiter sibling tools/r2-press-vocab-stacks.json — every adopted pressed pair (label ink vs dimmed fill) plus the callout button's ctaFill pressed/hover/focus states, both schemes; ships only on exit 0. Guard tests extend the B5 RM net: pressed-style presence asserted per family; existing RM spring-skip tests must stay green. Re-capture the twin pairs — press/clear-all rest-vs-pressed and press/tabbar rest-vs-pressed must now differ (today bitwise-identical). Blur-pane count unchanged at the worst simultaneous state (+1 invisible tab-bar pane). Sky device: press-and-hold a drawer row, a Map tool, a tab, a filter chip — one felt dialect; tab press haptic fires; RM on — dims still answer.


**Skeptic verdict: FIX (absorbed).** the custom tabBarButton MUST forward every injected a11y prop (role/state.selected/label/style/children) onto the wrapped PressableScale + assert the tab AX node keeps selected — the dim/haptic are strictly additive; reconcile the close-button count to 7 (naming which inherit the Sheet.tsx:64 cascade vs hand-fixed); borrow opacity-0.85 ONLY from cardPressed (leave scale to the spring); remap stale C-ids→T-ids; add the box-none preservation line for the filter chips.
### T4 — The Report sheet and triage answer the hand
**WHERE:** src/screens/ReportFlagModal.tsx — all 10 Pressables (category chips :635, severity circles :664, template chips :593, 'Use my location' retry :532, anon-banner Sign in :564, seasonal/disability/context tag chips :804/:861/:991, Cancel :1052, Submit :1062) + press-in haptic timing; src/screens/TasksScreen.tsx — renderAction haptic word (:1576) + applyStatusChange outcome notify. haptics.ts untouched (existing API only).
**Resolves:** F1-01,F1-08 · **Effort:** M · **Tier:** Meaningful · **NEEDS-SKY-DEVICE:** yes
1. **WHAT** — The app's core contribution flow acknowledges the finger, and the trust engine speaks the right haptic words. All 10 ReportFlagModal Pressables gain the static pressed dim PhotoGallery already uses in-sheet (thumbPressed/removeBtnPressed — the in-sheet inconsistency closes from inside the sheet's own vocabulary); hapticSelection moves to finger-down on the category/severity pickers instead of release. At Tasks triage, Verify/Resolve/Reject commit with impact('medium') and land with notify('success'|'error') alongside the existing flash banner; selection remains the pickers-and-filters word per haptics.ts's three-word vocabulary. Submit keeps its shipped notify('success'). No layout, color-role, or copy changes — acknowledgment only.
2. **WHY** — Resolves F1-01 (HIGH — 'utilitarian-bare at the app's emotional center is the precise thing the R2 thesis exists to refuse'; skeptic-confirmed bitwise-identical pressed frames) and F1-08 (MED — the vocabulary deployed inverted: real state commits on someone's barrier report get the lightest word and no outcome haptic while opening a form gets impact). ONE HAND: the severity picker is the signature control and must answer the finger the way the drawer row does. ONE VOICE, felt: picks feel like picks, commits like commits, outcomes like outcomes. The mission's two working moments become the best-feeling surfaces in the app — craft exactly where the thesis says it belongs.
3. **PERSONA IMPACT** — R4: the surface that asks the most of a tremor user stops demanding blind faith — landing confirms under the finger, re-press anxiety at Submit dies. R2, triaging screen-off: the only press channel that survives on native without vision becomes semantic — picking a filter and resolving a barrier finally feel different. R7 gains full first-class acknowledgment on the form — including the anonymous/guest form, today feedback-dead without exception per the skeptic — via RT-native static dims, no transparency or motion involved. R6 gains submit confidence at the moment of civic contribution; R1 gains a crafted signature control; R5 reads the emotional center as finished.
4. **TRUST + COHESION** — The severity grammar is the brand (R5's unprompted signature read); a silent signature control undercuts it on every touch. Acknowledgment at the emotional center says the app takes the report as seriously as it renders the result. In-sheet consistency closes a visible seam (PhotoGallery dims, everything else dead), and correct haptic semantics make the trust engine feel like a trust engine, not a form.
5. **A11Y + RESTRAINT** — Static dims are motionless and RM-safe by construction; press-in haptics ride haptics.ts's PROTECT'd RM-independence (OS-level haptic settings still govern). No blur, no motion, no new strings; the double-submit guard (disabled={submitting || !location}) is untouched. Pressed ink pairs arbiter-gated. RESTRAINT: PASS — pure acknowledgment at zero legibility/reachability/honesty cost; the confirm gate on Reject keeps its net, so the costliest mis-tap still costs a dialog, not data.
6. **DEPENDENCIES / CONFLICTS** — Independent of the PressableScale candidate (these are raw Pressables) — can land FIRST as the highest-value surface. Preserves the confirm gate on Reject (commit haptic fires after confirmation, never bypasses it — extensions only). Preserves haptics.ts RM-independence stance. Preserves the FlagCard acknowledgment stack (triage buttons keep their static truth; only the haptic word changes). Preserves the severity live-region hint (sevHint speaks today per the F1-03 skeptic — byte-untouched here). Shares ReportFlagModal.tsx with the chip-semantics candidate — separate commits: styles here, semantics there. Fork 5 (trust-model scope) untouched: zero status/points behavior change, feel only.
7. **VERIFICATION** — Arbiter sibling tools/r2-report-ack-stacks.json — pressed severity circles and every chip family, label ink vs dimmed fill, both schemes; exit 0 gates. Re-capture press/report-category and press/report-severity pressed frames — must show nonzero diff vs base (today 0 changed pixels). Guard tests: pressed-style presence on all 10 Pressables; a haptic call-site census test pinning selection-at-press-in on pickers, impact('medium') at triage commit, notify on outcome; the confirm-gate test stays green. Sky device: finger-down tick on severity circles; Verify = medium thunk then success notify with the flash banner; Reject still confirms first; RM on — dims and haptics both still answer.


**Skeptic verdict: FIX (absorbed).** do NOT apply group-opacity-0.75 to any text-bearing control (it collapses label-vs-fill contrast below AA) — use a fill-composited pressed treatment that keeps label ink at full opacity; fire hapticImpact('medium') at the commit point INSIDE applyStatusChange (set the three commit actions' haptic='none', Details stays 'selection'); fire hapticNotify('error') in setStatus's catch (~:696), 'success' in applyStatusChange.
### T5 — The severity grammar speaks everywhere severity renders ★
**WHERE:** src/components/FlagDetailModal.tsx:849 · src/screens/HomeScreen.tsx:358/:512 · src/screens/ProfileScreen.tsx:1246 · src/components/MyReportsModal.tsx:252 · src/components/MyWatchedModal.tsx:231 · src/screens/TasksScreen.tsx:1136 · new src/components/SeverityDisc.tsx + adoption at LegendModal:266, NearbyFlagsModal:399, ActivityFeedModal:451, RecentlyViewedRow:194 · src/components/OnboardingCards.tsx:107–108 (slide-2 illustration).
**Resolves:** F6-01 (HIGH), F6-02, F6-03, F6-04, F6-05, F6-06 · **Effort:** L · **Tier:** Signature ★ · **NEEDS-SKY-DEVICE:** yes
*Merge of the signature suite (F6-01/02/04/05/06) and the F6-03 onboarding teaching row — one signature system: the brand's atom completed AND introduced.*
1. **WHAT** — Every surface rendering a real severity speaks the full canonical grammar. Seven sub-items: (1) FlagDetailModal chip "Severity 4" → "Severity 4 · Significant" with accessibilityLabel pinned to severityA11y so the "of 5" anchor cannot regress (skeptic-adjusted direction), plus the stake line — SEVERITY_DESCRIPTIONS[n], e.g. "Hard or unsafe for most users." — as one quiet line under the meta row. (2) Home Recent 11px dot → 20–24px numbered mini-disc (RecentlyViewedRow recipe: digit ≥11pt, textOnColor ink, maxFontSizeMultiplier 1.3, SR-hidden — row label already speaks). (3) Profile pill "severity 3" → "Severity 3 · Moderate". (4) MyReports "•" → "·" + word added. (5) MyWatched unnumbered dot → numbered disc (sighted channel gained). (6) Tasks pill → "Nearest open barrier · No ramp · Severity 3 · 639 m" + matching a11y label. (7) Extract SeverityDisc primitive (sm/md/lg, digit + ink fork baked in, decorative escape hatch); adopt at the four numbered sites with zero visual delta. Pins excluded — four channels, at capacity. And onboarding front-loads it: slide 2's illustration circle swaps the generic Lucide map glyph for the five numbered severity discs in one quiet static row — the Legend in miniature, Deep Field styling, digits and ink fork intact, rendered with the SeverityDisc primitive (or RecentlyViewedRow's inline recipe if the suite hasn't landed) — above the existing "…then rate how bad it is." One accessible group, PROPOSED label from the wording spine (before: decorative icon; after: "Severity scale — 1 Minor to 5 Severe", derived from SEVERITY_LABELS). No severity-colored chrome, no animation: a static teaching image that happens to be true.
2. **WHY** — Resolves F6-01 (HIGH), F6-02(≡F2-09), F6-04, F6-05, F6-06. ONE VOICE is what this IS: blinded readers named the severity grammar as the brand unprompted (R5: "a civic reporting tool whose brand is its severity scale"), yet at HEAD it decays exactly where stakes peak — the decision sheet speaks one of three parts, the signed-in mirror surfaces drop word or digit, the first screen wears a degraded atom. theme.ts's own law ("color is ALWAYS paired with a number + a word") indicts every gap. ONE HAND: the primitive ends identity-by-discipline — F6-02/F6-04 are the drift already showing. Signature tier by definition: completion of the ownable thing, not garnish; the lens's killed candidates (digit-on-pin, tinted picker, toast tones) stay dead. F6-03 adds the day-zero leg: the one moment the app explains its core loop currently illustrates itself with a stock glyph while the app's most distinctive asset sits out its own introduction — slide 1 already got this right with the LogoMark (B3). ONE VOICE from minute one: users meet 1–5 warm, before the report form must carry the whole load.
3. **PERSONA IMPACT** — R7 first-class: the grammar sits on her designed-for-me list; discs are opaque ink-on-fill so RT renders them identically by construction, and MyWatched gains a visible severity channel she currently lacks entirely. R3's literal ask lands ("put the numbers in those dots too"). R1 gets number + word + stake at the routing decision. R2's inverted gap closes upward — the severityA11y pin guarantees SR strings stay richer-or-equal, never regressed. R6: the stake sentence says what a 4 MEANS at the only moment it matters. R4: no target changes. R5: "execution hygiene 5" rises — one atom, structurally enforced. On onboarding: R6 meets the grammar before the form instead of cold at it; R2 hears one grouped spine-derived label, not five colour swatches; R3 gets legible numbered discs where a glyph told her nothing; R7 first-class — opaque ink-on-fill discs render identically under RT by construction.
4. **TRUST + COHESION** — The grammar stops relaxing when the audience becomes the contributor — the one-hand read extends behind the auth fence. The deepest trust surface finally speaks the same sentence as every surface that leads to it, ending the landslide R2's worst-strings list quotes. The brand's atom becomes structure: future surfaces inherit the disc instead of re-deciding size, digit, and ink — which is exactly how the Home dot happened. And introducing the grammar WITH the grammar is the strongest one-voice statement available on day zero — it kills the stock-glyph tell that R5-class reviewers read as template DNA; a user who saw the scale on slide 2 recognizes the legend, the callouts, and the report form as one continuous system.
5. **A11Y + RESTRAINT** — Floor improves: Home and MyWatched gain redundant sighted channels; every disc ink is the arbitrated theme.ts:541–545 pair; digit holds the 11pt caption floor; below an 18pt disc the direction dies (F2-09's own caveat honored). SR strictly richer or equal, guard-tested. All five findings carry inline RESTRAINT: PASS — words and structure, no new color meaning, no motion. Nothing traded. Onboarding leg: grouped SR label derives from SEVERITY_LABELS (spine extension); digits hold the arbitrated textOnColor inks; static row = zero motion, RM identical by construction. RESTRAINT: PASS — words and structure, no new colour meaning, no motion; the finding's own conditions (static row, no severity-colored chrome, existing copy anchors meaning) honored.
6. **DEPENDENCIES / CONFLICTS** — COMPOSES with the material cluster's RB4/M-36 (same FlagDetailModal sheet): sub-item 1 lands as its own commit in a phase distinct from the sheet's re-material — sequence it after the material T where both ship (text/token change, composes either order; ids stay distinct per reconcile). Preserves PROTECT: ramp mode-independence (discs byte-identical both themes); the ink fork as ONE system (primitive bakes dark-ink-1–4/white-5 — never uniformed); the pin's four-channel capacity (primitive never applied to pins); the wording spine (labels/stakes derive from theme.ts:533 — extensions only); outcome beats stay severity-free (FlashBanner/LiveStatusRegion untouched); Legend row rhythm and Nearby card grammar (zero-delta adoption). Fork 3: presentation-only on already-signed-in surfaces, no gating change. Fork 5 untouched. §6.9 (MyWatched accessibilityViewIsModal) is a different prop — no collision. The onboarding row prefers the SeverityDisc primitive (sequence after it where both ship) but degrades to the inline recipe — no hard dependency; OnboardingCards.tsx touched by no other candidate; B3's slide-1 mark untouched. Killed-by-lens options stay dead: digit-on-pin (four channels at capacity — also PROTECT), tinted picker buttons, severity-toned toasts, grammar-speaking zero states (the PROTECT'd "Any severity" chip is the answer).
7. **VERIFICATION** — Named arbiter sibling tools/r2-severity-disc-stacks.json — all five fill×ink pairs at sm/md/lg, both themes. Zero-visual-delta: style-equality guard tests at the four existing disc sites (primitive's resolved styles === prior literals). Guard tests: chip accessibilityLabel contains "of 5" (severityA11y pin); chip text matches "Severity {n} · {word}"; Tasks pill visible + a11y strings. A11y-tree re-walk: Home, Nearby, detail sheet. Captures: home at-rest pair, flagdetail pair. SKY DEVICE: Profile pill, MyReports, MyWatched are auth-fenced (harness never signed in) — Sky signs in, walks all three surfaces both themes, VoiceOver pass on the detail chip and Tasks pill. Onboarding: capture pair onboarding slide-2 light/dark (discs byte-identical across themes — mode-independence PROTECT check); a11y-tree shows exactly one group node with the spine-derived label, no per-disc SR noise; disc inks covered by tools/r2-severity-disc-stacks.json (or tools/r2-onboarding-disc-stacks.json if it ships alone); render-state check at fontScale 1.3.


**Skeptic verdict: FIX (absorbed).** reword the R7 MyWatched line — it gains a NUMBERED channel (a colour dot already exists at :231); stakes derive from SEVERITY_DESCRIPTIONS (flags.ts:1235, comment-aligned to the ramp), only labels derive from theme.ts:533; ADD the blur-budget-unchanged assertion (zero panes); scope 'zero-visual-delta' to default Dynamic Type (preserve each site's maxFontSizeMultiplier cap, or record the 1.3 cap as intended large-type hardening).
### T6 — The status ledge — one podium for the status voice
**WHERE:** LiveStatusRegion.tsx (placement math), FlashBanner.tsx (channel migration or stacking arbitration), App.tsx root mount, TasksScreen.tsx points-flash call site; ScreenHeader.tsx only if a reserved slot is exposed. No MapScreen overlay edits, no GlassSurface edits.
**Resolves:** F2-03 · **Effort:** M · **Tier:** Meaningful (down-tiered from Signature — Panel B demotion + skeptic concur; a placement/arbitration fix, however well-voiced, is not the ownable signature move T1/T2/T3/T5 are) · **NEEDS-SKY-DEVICE:** yes
1. **WHAT** — Give the app's one status voice one designed podium. On tab screens the resting top moves from Math.max(insets.top, 56) to below the editorial header band (insets.top + measured ScreenHeader clearance, or a reserved ledge slot ScreenHeader exposes), so "Still trying — check your signal" docks under the headline it currently beheads; on Map the pill keeps clearing chrome exactly as today. FlashBanner's points flash migrates onto the liveStatus channel (which already has tones, auto-dismiss, and the persistent-mount announce) or, minimally, gains arbitration: when both vehicles are live the second offsets below the first — never superimposed in the same zIndex-50 slot. Both pills stay toned solids; zero new material, zero string changes.
2. **WHY** — Resolves F2-03 (HIGH; canonical for the F5-04 placement read and the F5-08 two-vehicles collision). The em-dash status grammar is the app's audible signature (PROTECT nomination); a signature voice deserves a stage that never upstages what it reports on — that is why this is signature-class: it deploys the app's own signature into space, ownably. ONE VOICE: every status beat lands in one predictable place. ONE HAND: R5's "nothing reserves space for anything" collision family gets its system answer, not a nudge. ONE MATERIAL: placement only — Deep Field stays the hero, the header gets its breathing room back.
3. **PERSONA IMPACT** — R1 ("sits sloppily on top of the header text") and R5 ("the toast decapitates the '9 barriers' headline") get the occlusion pass R5 demanded before calling the app premium. R6's "black smudge" first-load frame becomes headline plus pill, both whole. R2 hears identical announcements — the text-mutation announce law is byte-preserved. R4 regains the menu circle's full tap area the pill currently intercepts. R7 first-class: both pills are already engineered solids, so RT renders the ledge identically — R7 gains a status system that never covers the editorial headers R7 named designed-for-me, plus placement predictability under C-lite.
4. **TRUST + COHESION** — The one designed escalation moment — the proof the app is alive — currently reads as a collision at the exact anxious moment, on a ~12–30s recurring window per retry. Docked, it turns "sloppy toast" into "the app has a place where it talks to you." Two status vehicles can no longer fight for one slot, so the S10/S11 single-status-channel promise becomes visibly true, not just architecturally true. Status text never again ghosts over a headline, which is precisely the sloppiness-reads-as-sloppy-data risk R1 named.
5. **A11Y + RESTRAINT** — Floor improves on all three axes: legibility (no text-over-text), reachability (44pt menu circle un-intercepted), honesty (zero string changes). PROTECT-preserving by construction: preserves the LiveStatusRegion persistent-mount text-mutation ANNOUNCE LAW (P5-shipped) byte-for-byte — placement moves, mechanism untouched; preserves PROTECT-7 (announce decoupled from motion; entrance stays RM-gated); no new motion added, honoring the 220ms/pulse discipline by not touching it. RESTRAINT: PASS — position-only; nothing decorates, nothing is traded.
6. **DEPENDENCIES / CONFLICTS** — Preserves the persistent-mount announce law (PROTECT) and PROTECT-7. Preserves MapScreen's pointerEvents="box-none" overlay law — Map placement unchanged, MapScreen untouched. GlassSurface never edited. Points-flash migration keeps the Fork-2-coupled strings byte-same (vehicle changes, words don't — Fork 2's DECISION half untouched). Shared files: TasksScreen.tsx also touched by the spoken-triage and sweep candidates — disjoint regions, either order. If ScreenHeader grows a reserved slot, coordinate with the rhythm cluster's F2-06 inset work.
7. **VERIFICATION** — Jest guards: wrapper stays persistent-mounted + box-none; announce still fires via text mutation at the new offset; arbitration test — both vehicles live renders stacked pills, never one top value twice. A11y-tree re-walk of the still-trying state: headline nodes unoccluded, one announcement. Re-capture states/app__*__still-trying.png. Blur-pane count at the worst simultaneous state (Map + still-trying + points flash, +1 tab-bar pane): unchanged — both pills non-blur. No ink change, so no arbiter; if any ledge tint appears, run tools/r2-status-ledge-stacks.json. Sky device: iPhone VoiceOver — pill announces once below the header; menu circle fully tappable (native insets math per the F2-03 skeptic shows occlusion is inherent on device).


**Skeptic verdict: FIX (absorbed).** [TIER: Signature→Meaningful, panel + skeptic concur.] WHERE is a placement pub-sub (new lib, e.g. statusLedge.ts) + header-HEIGHT focus-publish (onLayout, scroll-invariant — not a screen-Y measure) + a LiveStatusRegion subscription DEFAULTING to today's Map placement; ScreenHeader lives at src/components/ui/ScreenHeader.tsx; pin ONE path — arbitration/stacking across the three vehicles (App-root LiveStatusRegion + App-root FlashBanner + TasksScreen's inline flash) OR full migration of the inline flash; keep the text-carrier pill pointerEvents='box-none' (it is NOT today and could sit over a live control); assert no double/dropped announce.
### T7 — Finish the honest arrival — the undetermined voice
**WHERE:** MapScreen.tsx only (arrival effect region + banner overlay region + initial-region computation). location.ts, NearbyFlagsModal.tsx, OnboardingCards.tsx untouched.
**Resolves:** F4-03,F5-03 · **Effort:** M · **Tier:** Meaningful · **NEEDS-SKY-DEVICE:** yes
1. **WHAT** — Complete S4's honesty for the arrival S4 deliberately left silent: the undetermined no-location arrival (never-asked web guests — the default first-run state — and native "Not now" decliners). Two moves. (1) One honest, non-accusing line through the existing banner recipe, polite not assertive since nothing was denied; PROPOSED sketch, em-dash grammar: before = silence; after = "Location isn't on yet — showing the most recent reports, not ones near you." (never "is off": the S4 gate's own comment forbids telling a never-asked user access is off). (2) A true frame: with no usable location at arrival, fit the initial viewport to the loaded flags (bounds over already-fetched rows, instant initial region, no fly-in — RM-clean) instead of hardcoded San Francisco, so "Showing 9 flags" becomes visibly true.
2. **WHY** — Resolves F4-03 + F5-03 (both HIGH, skeptic-hardened: probes proved the banked "denied" captures depict the undetermined state, and that state is genuinely wordless once the auto-opened Nearby sheet closes). ONE VOICE: the arrival stops splitting into an honest suffix inside one sheet and silence on the map beneath it. Honesty is the thesis's hard floor — a confident count over a city the user isn't in is the inverted-honesty shape Round 1's S4/S6 existed to kill, still live on the trust mission's front door.
3. **PERSONA IMPACT** — R6 ("I don't live in San Francisco. Just say so") gets told, in the app's warm register. R1's "technically true, functionally misleading" count becomes functionally true — the nine flags are on screen. R2: the line rides the existing banner's live region, so the spoken arrival finally carries location context (today's tree has zero words about it). R3/R4: no layout or target changes. R7 first-class: the banner recipe is a solid surface, identical in RT — R7 gains an arrival whose frame makes sense at a glance instead of demanding pin-hunting across an empty default city.
4. **TRUST + COHESION** — The first sixty seconds of the largest cohort (guest web) currently show a false frame under a confident pill. After: frame true, words honest, one register from arrival onward. No new vehicle — the existing honesty banner learns one more true sentence, so the one-material read is untouched and the one-voice read strengthens exactly where first impressions form. The honest no-location suffix in Nearby is corroborated by the map instead of contradicted by it.
5. **A11Y + RESTRAINT** — Adds honesty and orientation; costs nothing — no target moves, no new pane, existing arbitrated banner stack. Preserves the honest no-location suffix (PROTECT: extend, never replace) and PROTECT-6's locating ladder. Bounds-fit uses an instant initial region — no animation, so the RM designed-stillness contract is honored by construction. The denied-arrival S4 banner remains byte-identical, assertive, and gated exactly as shipped. RESTRAINT: PASS — replaces a false frame with a true one; zero decoration.
6. **DEPENDENCIES / CONFLICTS** — Fork 1 (proximity/geo) adjacency: bounds-fit runs over already-fetched rows — presentation only; it must not introduce proximity querying or cross Fork 1's DECISION half. Fork 3 adjacency: no permission-structure change; S19's platform fork and the OS prompt flow untouched. Preserves MapScreen's box-none overlay law (banner lives in the existing overlay region). Shared file: MapScreen.tsx also touched by the failure-voice candidate and the map cluster's F2-01 callout work — sequence with the merge lead. String is PROPOSED; Sky signs the final wording.
7. **VERIFICATION** — Re-run the banked read-only probes (tools/probe-f403-denied-real.mjs, tools/probe-f503-skeptic.mjs) against the build: undetermined arrival renders the new line with flags in viewport; true-denied still fires the S4 banner (regression watch). Re-label the map__*__permission-denied captures as undetermined per the skeptic. A11y-tree re-walk: arrival tree contains the honest line, announced politely once. No ink change (existing banner stack), so no arbiter; if any tint shifts, run tools/r2-honest-arrival-stacks.json. Sky device row (this closes 02 honesty-ledger #16 in the paper record): native "Not now" arrival shows the line; native real-denial arrival fires the S4 banner; VoiceOver hears one arrival announcement.


**Skeptic verdict: FIX (absorbed).** the bounds-fit needs PlatformMap.tsx + .web.tsx (gotcha #3) — reuse T1's instant setView path, do not fork it; rows load ASYNC so seed DEFAULT_REGION at mount and fire a ONE-TIME unconditional instant fit on the FIRST flags-load while location is still null; guard it on no-gesture + plain no-param arrival (never stomp focusFlag/deep-link/openReport cameras) + empty/singleton fallback to DEFAULT_REGION; the undetermined banner is a NEW polite element reusing styles.banner INK only (the assertive denied banner stays byte-identical) — verify it announces on web or route through announce.ts; re-run S4's undetermined guard.
### T8 — One spoken voice for the triage layer
**WHERE:** TasksScreen.tsx (FlagCard baseLabel + action a11yLabels + visible verb), HomeScreen.tsx (recent-row label branch), ReportFlagModal.tsx (photo-nudge string), LegendModal.tsx (badge decorativeProps adoption). a11yText.ts imported, never changed.
**Resolves:** F4-02,F4-08,F4-10,F4-17,F4-01 · **Effort:** M · **Tier:** Meaningful · **NEEDS-SKY-DEVICE:** yes
1. **WHAT** — Make the spoken app as designed as the seen one on the triage path. (a) The Tasks card label routes through the helpers — before: "No ramp, severity 3, open. Tap to view on map."; after sketch: "No ramp, severity 3 of 5, Moderate, status Open. Tap to view on map." (b) Each action names its flag — a11yLabel sketch: "Verify this flag — No ramp, 639 m"; visible "Verify" unchanged. (c) The visible verb unifies: card/detail "Resolved" becomes "Resolve" (a11y "Mark this flag resolved" kept). (d) Home rows keep status when distance appears: "…, status Open, 297 m away." (e) The severity-4 photo nudge interpolates SEVERITY_LABELS, retiring the invented "major." (f) The Legend badge adopts the repo's own decorativeProps so the web digit stops doubling.
2. **WHY** — Resolves F4-02 (HIGH — the exact drift a11yText.ts:22-28 exists to prevent, on the surface where a blind verifier decides) with F4-08, F4-10, F4-17, and F4-01 riding as one family: the spoken layer drifting where the visible layer never would. ONE VOICE, literally: the severity grammar is the actor, and it must speak its taught words to SR users too. ONE HAND: S1 fixed this family on Home's visible rows; this finishes the adoption S1 started (skeptic-confirmed not-closed), and the Legend leg deploys the S9 primitive built precisely for RN-web's dropped hiding props.
3. **PERSONA IMPACT** — R2 is principal: "I hear every flag twice, slightly differently" and "'Verify this flag' six times in a row with no idea which flag" both die, and the app's dictionary stops stuttering digits on web. R5's verb/state pick-one lands. R3 gains: the shorter "Resolve" eases the action-row overflow R3 feared. R1/R6: steadier vocabulary, unchanged visuals. R4: no target changes. R7 first-class: RT users gain the visible verb fix outright — the state-word-on-a-button misreading R7 flagged disappears — and RT+VoiceOver pairings gain the entire unified voice at zero visual spend.
4. **TRUST + COHESION** — The severity grammar is the signature; today it is fluent in ink and broken in speech on the doing-surface. After, one grammar spans channels: what the Legend teaches, the card speaks, in the helpers' canonical words. For SR users, parity is credibility itself — the app stops sounding like a port of its visual self exactly where the community-verification loop happens. The verb unification removes the last state/verb ambiguity from the triage buttons.
5. **A11Y + RESTRAINT** — Pure floor improvement — label quality (WCAG 1.3.1/4.1.2-adjacent), unique action names, no visual spend beyond one shortening verb. Preserves the spoken-brevity twins (visible-warm/spoken-plain pairs are designed — only drifted labels change); preserves PROTECT-1 (NearbyFlagsModal's frozen labels untouched — census-only there); never compresses the taught severity scale (helpers interpolate the canonical words); PROTECT-12 honored — S9 engine adoption, not redesign. RESTRAINT: PASS — spoken-only plus one shorter verb; nothing traded.
6. **DEPENDENCIES / CONFLICTS** — Preserves PROTECT-1, PROTECT-12, the taught-scale strings, and the S13 card structure — D1's settled device subject is not re-opened; only label composition changes atop the byte-same structure. GlassSurface and a11yText.ts untouched (import-side adoption). Shared files: TasksScreen.tsx (status-ledge and sweep candidates), HomeScreen.tsx (failure-voice candidate) — disjoint regions, coordinate merge order. No fork's DECISION half is approached; Fork 5's data-content question (BUMBAKLOT) is explicitly not this — that stays Sky's.
7. **VERIFICATION** — Jest guards: baseLabel composed via severityA11y/statusA11y (string-match); action a11yLabels contain category + distance; a regression regex proves no raw-enum composition (/severity \d, (open|verified)/) survives in TasksScreen; Home row label includes statusA11y when distance renders. Re-run the banked CDP probes (tools/probe-f401-skeptic.mjs, probe-f401-detail.mjs): badge digit no longer doubles; a11y-tree re-walk on tasks/legend/home. No ink change, so no arbiter. Sky device (D1-adjacent, the audit's highest-stakes gate): VoiceOver on Tasks — each card reads once in the taught grammar and each action names its flag uniquely; Legend rows read one utterance each.


**Skeptic verdict: FIX (absorbed).** scope to F4-01's badge-doubling leg only (PARK-with-direction the color-name-vocabulary SR leg); pin decorativeProps to the inner sevDot <View> (LegendModal:79-83), NEVER the row View carrying the authored label; ALSO route the selection-mode label (TasksScreen:1511) through severityA11y; make the action-label distance conditional (null when no location → category-only); mark 'Resolved'→'Resolve' PROPOSED + update getByText tests; add FlagDetailModal:1436 to WHERE (+ the T2/T5 FlagDetailModal coordination).
### T9 — Word every wait; never claim a zero
**WHERE:** HomeScreen.tsx (headline gate + error-card message source), MapScreen.tsx (status-pill fourth arm + error-banner visible retry verb), TasksScreen.tsx (shared failure recipe adoption), ReportFlagModal.tsx (submit-button wording), copy.ts (single-source failure presentation strings).
**Resolves:** F5-02,F5-01,F5-05,F5-09 · **Effort:** M · **Tier:** Meaningful · **NEEDS-SKY-DEVICE:** no
1. **WHAT** — The failure register gets the empty register's hand. (1) Honest counts: gate both census renderers on error — Home's headline never computes "0 barriers" on a settled failure; the Map pill's three-way ternary grows a fourth honest arm, sketch: "Couldn't load flags" (retry stays on the co-present banner). (2) Worded waits: Home's first-load headline says "Loading…" instead of the bare display-size em-dash (Map already words this wait); the submit button keeps words beside its spinner, sketch: "Filing your report…". (3) One failure recipe: Home/Tasks/Map route the same read failure through one presentation contract — icon + provider message + visible retry verb, sketch appending Tasks' "Tap to retry." — single-sourced beside offlineBannerText in copy.ts, the app's own one-true-string model. All strings PROPOSED via before/after table.
2. **WHY** — Resolves F5-02 (HIGH, skeptic-CONFIRMED: a false "zero barriers" census announced politely to SR users during failure is a direct trust-mission hit) with F5-01, F5-05, F5-09 riding naturally. ONE VOICE: one failure currently speaks three registers — Home generic, Tasks instructive, Map mute about its own tappability. ONE HAND: the F5 census shows the empty states got the master and the failure states got the apprentice; this is the apprentice's work redone by the same hand, in the em-dash grammar the app already owns.
3. **PERSONA IMPACT** — R1 (silent Home failure was a top-3 trust-reducer "right next to a 'still trying' state that proves the team knows how to do it right") and R6 ("the one screen I'd call 'maybe broken'") get worded waits and honest failure on Home. R2: polite live regions stop announcing a false census, and Map's banner finally says visibly what its a11y hint promises. R3: display-size "Loading…" beats a cryptic glyph at large type. R4: Map's failure banner gains a visible retry verb — reach parity with Tasks. R7 first-class: every failure vehicle is a toned solid, identical in RT — R7 gains worded waits above the static skeletons R7 called the app's warmest asset.
4. **TRUST + COHESION** — A barriers app asserting "0 barriers" at display size beside its own failure banner is self-contradiction — removing it is the cheapest credibility repair in this cluster. Converging three failure dialects onto one recipe makes failure a designed moment: same icon, same register, same verb, so failure stops being where the app audibly changes writers. The count pill becomes honest across all four states it can actually inhabit, matching the S11 ladder's honesty on the states around it.
5. **A11Y + RESTRAINT** — Improves the floor: no false polite announcements (WCAG 4.1.3 quality), a visible retry affordance on Map (reachability), words during every wait (cognition). Preserves the S11 read ladder as a unit — thresholds, strings, Retry byte-untouched; this candidate words the states around it. Preserves the Skeleton contract, PROTECT-2's recovery card, the submit-moment honesty sentence, and offlineBannerText discipline (extended, not altered). The F5-01 skeptic's library-retry posture (~7–9s window) is worded over, not re-engineered — no .retry() changes. RESTRAINT: PASS — words and honesty only.
6. **DEPENDENCIES / CONFLICTS** — Preserves the S11 ladder + its write-half twin (PROTECT) and the em-dash grammar in every proposed string. The Map pill is a GlassSurface consumer — threaded, never forked; forceEngineered tint law untouched, no intensity changes. Shared files: MapScreen.tsx (honest-arrival candidate + map cluster's F2-01), HomeScreen.tsx (spoken-voice candidate), TasksScreen.tsx (three candidates) — regions disjoint; merge-lead sequencing. No fork adjacency: no DB, no auth, no points values. Fence respected: submit-path wording is verified component-level, never by live submit.
7. **VERIFICATION** — Jest render-state matrix over {loading, error, offline, settled} × {Home headline, Map pill}: never "0 barriers"/"Showing 0 flags" while error && empty; "Loading…" during showFirstLoad; fourth pill arm exact-string; submit button renders "Filing your report…" while pending (fence-safe component test). Live-region assertion: the pill announces the honest arm politely. Grep guard: the failure recipe imports from one source at all three call sites. New strings ride existing arbitrated inks — no arbiter; if any new text-on-tone pair appears, run tools/r2-failure-voice-stacks.json. Re-capture home load-error plus the settled-failure frame F5-02 notes is unphotographed.


**Skeptic verdict: FIX (absorbed).** gate BOTH the Home headline and the Map error arm on `error && flags.length===0` (not `error` alone) so the SWR stale path keeps its cached rows; implement the Map banner's retry verb by APPENDING to the existing single AppText (mirror Tasks' '{msg}. Tap to retry.'), not a new child (preserves the M10 space-between + box-none footprint); the 'one recipe' is string/token single-sourcing in copy.ts, NOT a shared component.
### T10 — The guest Profile joins the family
**WHERE:** ProfileScreen.tsx only (the !user early-return branch + the authLoading branch); ScreenHeader, HeaderActions, Skeleton threaded as shipped primitives — no primitive edits.
**Resolves:** F2-04 · **Effort:** M · **Tier:** Meaningful · **NEEDS-SKY-DEVICE:** no
1. **WHAT** — The signed-out Profile — the default state of every web session and native guest — renders the same S8 editorial family as the other four tabs: eyebrow PROFILE, display title, subtitle, and the HeaderActions pair (menu + feedback circles) above the existing sign-in prompt on the existing ScreenStage. Copy sketch (PROPOSED): eyebrow "PROFILE" · title "Your profile" · subtitle promotes the current sentence "Sign in to see your stats, badges, and reports."; the CTA and sign-in modal stay byte-same. The authLoading branch swaps its bare centered ActivityIndicator for a stats-shaped Skeleton pair (points tile + list rows) from the shipped primitives — the absorbed F5-06 leg. Fork 3's wall is not touched: same capabilities, same button, same modal.
2. **WHY** — Resolves F2-04 (HIGH — a cohesion break plus a true S8 REGRESSION per the skeptic: pre-S8 the nav header served both auth states; headerShown:false removed it and only the signed-in branch got the replacement). ONE HAND: four tabs wear the family; the fifth doesn't, on its default state. ONE MATERIAL: the header is ink on the existing stage — no new pane, no blur. The skeleton leg extends the app's most-praised state pattern to the last core surface still speaking bare spinner, closing the census's "skeleton language stops at the third tab."
3. **PERSONA IMPACT** — R7 verbatim ("the one screen that feels unfurnished rather than minimal") — R7 first-class gains the full editorial family in RT: header inks on the solid stage, zero transparency dependence, plus the content-shaped skeleton R7 called "exactly what I want… almost nobody gives it to me" now on Profile. R4 regains one-thumb reach: drawer and feedback without tabbing away. R2 gains heading structure on a tree that currently has no header nodes. R5's one-hand read strengthens on the exact surface that broke it. R1/R3/R6: consistency at zero cost.
4. **TRUST + COHESION** — A guest deciding whether to sign in currently decides on the app's least-designed screen — the pitch surface is the shabbiest room in the house. With the family header, the guest state reads as a designed state, not an afterthought; the one-material read completes (five tabs, one stage, one header voice), and Profile's waiting moment joins the app's single waiting language instead of a wordless spinner.
5. **A11Y + RESTRAINT** — Adds reachability (two 44pt header actions restored to the tab), structure (heading order for SR), and legibility via existing arbitrated header inks. The skeleton inherits the shipped contract — content-shaped, AT-hidden, static at 0.5 under RM — so no motion is added; the sign-in modal's RM-ternaried mount is untouched. Nothing is removed from the guest render; honesty unchanged. RESTRAINT: PASS — adds cohesion and reach; nothing traded, nothing decorated.
6. **DEPENDENCIES / CONFLICTS** — Fork 3 (auth-wall/guest) — the DECISION half is Sky's and untouched: this designs the guest STATE render only, granting no new capability (drawer/feedback are already guest-reachable elsewhere; the skeptic verified header parity prescribes neither Fork-3 half). Preserves the ScreenHeader type rhythm (PROTECT — threaded, never loosened) and Profile's zero-header-padding convention the rhythm cluster cites as the correct F2-06 pattern — coordinate if they unify insets. GlassSurface untouched. Sequencing: independent; the cluster's cleanest structural first merge.
7. **VERIFICATION** — Re-capture base/profile-signedout__{light,dark}__390 — header present, family composition matches Home/Tasks. A11y-tree re-walk: eyebrow/title/menu/feedback nodes exist with sane heading order. Jest: !user renders ScreenHeader + HeaderActions; authLoading renders the Skeleton pair (AT-hidden, RM-static — extend the B5 regression net's pattern); drawer and feedback modal open from guest Profile. Ink proof via named arbiter sibling tools/r2-guest-profile-stacks.json: eyebrow/title/subtitle inks over the guest stage pass AA deterministically (the pairing exists signed-in; prove the guest branch byte-pairs). Blur budget unchanged — header, stage, and skeleton are all non-blur.


**Skeptic verdict: FIX (absorbed).** drop/reframe the F5-06 skeleton leg — ProfileScreen's authLoading branch is runtime-unreachable (Gate returns null until loading is false); spec TRUE render-parity with the signed-in header (guest ScreenHeader carries eyebrow/subtitle = inkOnStage per the :898-899 AA comment + a safe-area, not a bare primitive drop-in); retag the 'S8 regression' as code-inferred from the S8 comments, not a skeptic claim.
### T11 — The perception layer — chips speak their state, focus you can see
**WHERE:** src/lib/accessibility.ts:50–60 (a11yToggle, repaired at source) + its button-role consumers (ReportFlagModal :649/:682/:597; MapScreen saved-set pills :1816, category :1875, severity :1907, status/affects :1965/:1999) · public/index.html:54–64 (the shipped :focus-visible net, un-defeated) · chip-scroller containers (ReportFlagModal / MapScreen / TasksScreen) · web navigation screen stack (RootNavigator / container level, inert on occluded screens). Button.tsx untouched.
**Resolves:** F1-03 (HIGH), F1-04 (HIGH) · **Effort:** M · **Tier:** Meaningful · **NEEDS-SKY-DEVICE:** yes
*Merge of F1-03 (SR state on chips) and F1-04 (visible/truthful keyboard focus) — the two non-touch perception channels, both AA-floor repairs of shipped intent.*
1. **WHAT** — (SR state) Every chip that holds state reports it to assistive tech. Exclusive pickers (Report severity, panel min-severity, sort where applicable) adopt honest single-select semantics — role radio with accessibilityState.checked, or tab where the visual already says tab; multi-select toggles (Report categories/templates/tag chips, Map category/status/affects chips, saved-set pills) adopt accessibilityState.pressed — aria-pressed IS exposed on role=button per the skeptic's CDP probe; the Tasks catChips label-baked ', selected, tap to deselect' pattern is the fallback wherever a role change would lie. a11yToggle is repaired at the source so the fix is systemic, not per-site. Zero-to-minimal new strings — the engine speaks the state; before/after AX sketches accompany each family (before: 'Severity 3, button'; after: 'Severity 3, radio button, checked, 3 of 5'). (Visible focus) Keyboard focus becomes visible and truthful on the two most interactive screens: the shipped brand :focus-visible net stops being structurally defeated — chip scrollers get vertical headroom (or rings draw inside via the Card/Button ring-overlay recipe at -focusRingOffset) so the ring renders whole instead of two clipped slivers; and the web screen stack marks inactive fully-occluded screens inert/aria-hidden so Tab lands on the Map screen's real controls instead of leaking into the still-mounted Home beneath the map card. End-state: tab-walking Report shows a complete four-edged ring on every chip; tab-walking Map shows a visible indicator on an identifiable on-screen control at every stop.
2. **WHY** — Resolves F1-03 (HIGH, skeptic-corrected mechanism: Chromium drops aria-selected from the AX tree on role=button, so selected STATE is invisible on focus/re-query on every cited chip, and the MapScreen filter panel's five chip families announce nothing at all — a blind guest's core filtering job on the only surface a guest has). ONE VOICE: the app must say the same true thing in every channel, and the accessibility tree is a channel. S9's flat-aria leg gets un-defeated exactly where selection matters most — honesty plumbing in service of the thesis's hard floor. And F1-04 (HIGH, skeptic-adjusted: the focus SYSTEM exists — a deliberate WCAG-cited net in public/index.html — but is defeated by same-height overflow clipping and by focus escaping into a non-inert occluded screen: 2.4.7 plus 2.4.3). Both serve the thesis's hard floor: the accessibility tree and the focus ring are channels, and the app must say the same true thing in every channel. ONE VOICE (state), ONE HAND (a beautiful ring that reaches the chips and the Map, not just search).
3. **PERSONA IMPACT** — R2 is the principal: state-on-focus works everywhere, filter picks confirm audibly, and the already-working severity live-region hint is joined by queryable state — 'no selected announced anywhere' ends. R6 using SR regains re-orientation: re-focusing a chip re-tells its state under cognitive load. R7's information floor rises first-class: when RT flattens material cues, the reported state — not a subtle fill — carries the truth, so RT+SR combinations get certainty from the honest channel. R4/R1/R3/R5 see zero visual change and lose nothing — this candidate spends nothing visible to buy honesty. On focus: R4 named it — "the ring only draws its left and right edges — it looks broken" — and regains place-keeping on Report and Map; R2 gains a truthful tab order (focus never parks inside occluded Home); R3 gains a high-visibility indicator that survives zoom; R7 first-class — a hard-ink transparency-independent indicator that renders identically on flat RT panels, plus inert stacking so RT keyboard users never focus invisible content.
4. **TRUST + COHESION** — Today the app answers 'which filters are on?' differently by role — tabs tell the truth, buttons stay mute. One repaired helper makes the whole chip estate speak one dialect in the invisible channel, which is the one-voice read where it counts most: credibility with AT users is binary, and an app that won't confirm state won't be trusted with barrier reports. And a ring that draws as two orphaned bars reads as broken software; an invisible focus position reads as lost software. Repairing both makes the web build — the guest and AT front door — feel governed by the same hand as native.
5. **A11Y + RESTRAINT** — This IS floor work: WCAG 4.1.2 name/role/value (state) + 2.4.7 visible focus + 2.4.3 focus order. No visual deltas from the state leg; rings show only on keyboard focus (zero cost to touch users); headroom shrinks no 44pt target; inert applies only to fully-occluded inactive screens, never the live one or the map overlay tree; native accessibilityState.selected stays intact. The PROTECT'd audible filter-acknowledgment loop is layered under, not competed with. RESTRAINT: PASS — pure honesty + reachability gain, zero cost in any channel.
6. **DEPENDENCIES / CONFLICTS** — Preserves the audible filter-acknowledgment loop (count pill + cycle-tool announcements untouched; no competing announce strings). Shares ReportFlagModal.tsx with the Report-sheet candidate and MapScreen.tsx with the press-vocabulary candidate — semantics commits kept separate from style commits. Fork 8 boundary: saved-set pills gain state semantics only, zero dark-mode restyle. Any label-baked strings reuse the shipped Tasks catChips wording verbatim; genuinely new wording (if any family needs it) is PROPOSED with before/after and lands via the voice cluster's rail. Role-change table reviewed against native VoiceOver behavior before merge. Focus leg is web-layer only (native focus indication is OS-drawn — hence the device gate is for the SR-state leg's VoiceOver behavior, not focus). Shares chip containers with T3 (press-vocabulary) — coordinate headroom + pressed styles into coherent hunks. Fork 9 boundary: Button.tsx's dormant ring is pattern-reference only. Preserves the S17 peek (Home going inert under Map changes nothing visible). GlassSurface.tsx untouched.
7. **VERIFICATION** — (state) No ink change — no arbiter. CDP AX-tree re-probe (the skeptic's own method): report-open tree shows checked/pressed on active chips where today it shows nothing; tasks tree keeps tab [selected]. Banked a11y-tree re-walks for report-open, the Map filter panel, and Tasks. Guard tests assert accessibilityState props per family and the a11yToggle source repair. Sky device: VoiceOver on the severity row and Map category chips — hears role + state on focus, live-region hint speaks once (no double-speak), toggles a chip off, re-focuses, hears the state flip. (focus) Arbiter sibling tools/r2-focus-net-stacks.json — ring ink vs chip fills and page grounds, both schemes, ≥3:1 non-text; exit 0 gates. Re-run the tab-walk captures: focus-report tab2 shows a whole ring; focus-map tab4 shows a visible indicator on an identifiable Map control. Scripted walk asserts document.activeElement is never inside an inert/aria-hidden subtree and elementFromPoint hits the focused element at every stop; guard test asserts inactive stacked containers carry inert on web. Preview-is-Chromium caveat noted for a later Safari pass — not a gate here.


**Skeptic verdict: FIX (absorbed).** exclusive pickers must NOT ship a bare role=radio (needs a radiogroup + roving-tabindex/arrow-keys, else SR-announces-radio / keyboard-behaves-button mismatch) — default to the cross-platform tab/checked pattern; pin the occluded-screen fix to `inert` (aria-hidden ALONE leaves tab stops — insufficient for the 2.4.3 leak); prefer the ring-overlay-INSIDE recipe over added headroom (keeps 'zero visual cost' literally true); extend the coordination note to T14's overflow-fade.
### T12 — The drawer passage — dressed arrival, symmetric exit
**WHERE:** src/navigation/RootNavigator.tsx:41–54 (ScreenFallback redesign) + drawer-open preload hook · src/components/HamburgerDrawer.tsx:86–104 (exit animation), :110–122 (220ms delay), :119 (off-scale literal), :143–150 (Modal visible/animationType="none") · RootNavigator.tsx:427–447 (close paths).
**Resolves:** F3-01 (HIGH), F3-02, F3-07 · **Effort:** S–M · **Tier:** Meaningful · **NEEDS-SKY-DEVICE:** yes
*Merge of F3-01 (Settings arrives dressed) and F3-02/F3-07 (drawer departs like it arrives) — one passage: the app's hand-animated menu handoff, made whole in both directions.*
1. **WHAT** — (arrival) The drawer→Settings handoff stops being three unrelated worlds. Two moves: (1) WARM — when the drawer opens, fire the Settings (and Admin, when visible) chunk import fire-and-forget, adopting the PROTECT'd Suspense-fallback-null warm-chunk pattern from MapScreen/Tasks/Profile, so the void simply never exists except on cold, slow networks; (2) DRESS — ScreenFallback renders the destination's stage instead of a bare surfaceMuted spinner: the Settings gradient wash plus an editorial-header-shaped static skeleton built from the shipped Skeleton primitives — no BlurView, no new pane, no new recipe. Optional single line, PROPOSED in the em-dash status grammar: 'Opening Settings — one moment' (final wording is the voice cluster's rail; the skeleton ships without it). (exit) And the drawer departs as it arrives: latch visibility locally so on close the Modal stays mounted while the existing-but-dead exit animation actually plays — panel slides out and backdrop fades as one welded object over motion.duration.base (180ms) — then flips closed via the timing callback; under RM today's instant snap stays (the setValue path at :81–85 already exists). With a real exit, the 220ms sub-screen delay earns its own written premise and collapses to motion.duration.base — one token, retiring the transition layer's last off-scale literal per B5's pulse-documentation standard. Dismissal input still fires immediately.
2. **WHY** — Resolves F3-01 (HIGH — 'the single worst tear in the one-material thesis': a raw, structureless interstitial between two fully-dressed surfaces, playing on every user's FIRST Settings visit per session — every first-run, every cold start). ONE MATERIAL, by name: the material world must not drop its floor between rooms. R5 flagged it unprimed as the lone exception in the motion story; the app's own lazy modals prove the better pattern in four files. For a civil-benefit app, the first Settings walk is a first impression — and it is currently the app's only undressed frame. And F3-02/F3-07: the drawer is the app's one hand-animated moment and half of it is silently missing — it arrives like glass and vanishes like a light switch, while the code's comment promises a visual close that never renders. ONE MATERIAL (matter doesn't teleport), ONE HAND (one clock — every hand-animated duration speaks token). Closing the intent-vs-render mismatch is exactly the seam a one-careful-hand audit exists to catch.
3. **PERSONA IMPACT** — R6 (cognitive load): the where-am-I blank mid-navigation disappears — the gap keeps the destination's shape and color, so orientation never resets. R5: the 'white flash' exception closes, the cheapest point on the hygiene score. R7 gains a first-class gap: static skeletons are on R7's designed-for-me list ('it just sits there and tells me the shape of what's coming'), and the dressed fallback is opaque wash plus static bones — RT/C-lite-native by construction, zero transparency involved. R3 gains a sane luminance path (no white slab between dark drawer and washed Settings). R1/R2/R4: same routes, arrivals that feel governed. On exit: R5 is the principal witness — the asymmetric material is precisely the hygiene-tell behind "execution hygiene is 5"; a symmetric drawer is visible craft. R6 gains rhythm coherence (the Resources 220ms pause stops being dead air). R4 keeps immediate dismissal. R7 first-class two ways: RM path byte-identical instant snap, and in RT the near-opaque panel behaves like matter in both directions.
4. **TRUST + COHESION** — The interstitial is the one place the costume slips and the bare stage shows. Never showing it — or dressing it in the destination's own stage when networks are cold — makes the material continuous from drawer to Settings, which is precisely the one-material read. An app that never shows scaffolding is an app whose polish reads as integrity. And arrive-like-glass / vanish-like-a-light-switch is the two-buildings feeling in time instead of space. Exit symmetry plus one token makes the navigation heart read as a single crafted object; landed together, the whole drawer handoff family (Resources, Settings, About) shares one rhythm, and dead code that claims a behavior it never renders quietly exits.
5. **A11Y + RESTRAINT** — Zero motion added: static wash + static skeleton; the RM path is already instant and stays byte-identical (RM designed-stillness contract preserved). Preloading is invisible. No blur pane — budget unchanged. Busy-state semantics for SR preserved via the skeleton's existing treatment. If the text line ships, its ink is arbiter-gated on the wash, never eye-tuned. RESTRAINT: PASS — pure continuity gain; nothing traded, nothing louder. Exit leg: a ≤200ms token-bound exit plays only when motion is allowed; RM keeps designed stillness (instant snap, no timers); the drawer stays the sanctioned hand-animated class (no new Modal mounts; the RM-ternary law and presence-at-t150 regime untouched); dismissal fires on press with zero added latency. RESTRAINT: PASS — removes a dishonesty, trades nothing.
6. **DEPENDENCIES / CONFLICTS** — Adopts (and thereby preserves) Suspense-fallback-null warm chunks — the PROTECT'd pattern, extended to the nav layer. Preserves the 220ms delay-gate discipline: the Settings path has no delay today and gains none. Recommend landing in the same train as the drawer-exit candidate — real exit + no void is the complete felt repair of this handoff. The PROPOSED line's wording is the voice cluster's call; skeleton-first, line optional. RootNavigator.tsx shared with the tab-bar/badge/drawer candidates — trivial separate hunks. No fork adjacency. Exit leg preserves the presentation grammar (25/5 zero exceptions — drawer remains its own hand-animated class), preserves the welded arrival and extends welding to departure, and completes the 220ms delay-gate + pulse-token discipline (literal → motion.duration.base or pulse-style token doc). The finding's native risk (exit spring vs unmounted node; reopen start-position luck) is exactly what the latch fixes — device-verify it. HamburgerDrawer.tsx / RootNavigator.tsx shared between the two legs — land as one felt train, separate hunks.
7. **VERIFICATION** — Guard tests: drawer-open fires the dynamic import exactly once (mocked import); ScreenFallback snapshot renders wash + skeleton with no spinner-only branch; RM render identical. Web throttled cold-load: the dressed stage appears instead of the void; warm path shows no interstitial at all — re-capture the subswap-t120 frame and expect Settings or the dressed stage, never a bare spinner (both standard and rm variants). Blur-pane count unchanged at worst simultaneous state. Arbiter sibling tools/r2-settings-arrival-stacks.json runs only if the text line ships (line ink vs wash, both schemes). Web-provable; native inherits the same dressed fallback. Exit guards (fake timers): onClose keeps the Modal mounted until the 180ms timing completes, then visible flips; RM path closes same-tick with no timers; reopen always starts from the fully-reset off-screen value; the 220 literal is gone or token-documented. Bank the missing drawer-dismissal capture set (closing t60/t120, standard + rm) the audit noted as a gap. SKY DEVICE: open then close — panel and scrim leave together, no flash; immediate reopen starts clean; RM on — close is instant.


**Skeptic verdict: FIX (absorbed).** MANDATORY: the 220→motion.duration.base collapse breaks reduceMotion.drawer.test.tsx:55 — update the assertion to 180, keep the RM=0 lines byte-intact; the collapse MUST ship in the SAME commit as the F3-02 exit-latch or take F3-07's documented-exception fallback (never a bare collapse — else dead air returns); drop the 'PROTECT'd' qualifier on warm-chunks (real pattern, not on the 17-item list); reword 'zero motion' (the RM-gated pulse skeleton replaces the un-gated spinner — a net-calmer swap); cite M-54 (ScreenFallback stays opaque system tier); the preload must `.catch(()=>{})` + gate the Admin chunk on isAdmin.
### T13 — The editorial frame holds at every size and inset
**WHERE:** src/components/ScreenHeader.tsx:40–49/104–113/128 (floor+overflow wrap) + inset law · distance formatter (formatDistance) · src/screens/MapScreen.tsx:1429–1447/:2695–2702 (mapHeaderRow regroup) · src/screens/SettingsScreen.tsx:456–477 (double-inset) · src/screens/ProfileScreen.tsx:2053–2057 (reference convention) · device-gate register entry for TasksScreen.tsx:119–121.
**Resolves:** F2-02 (HIGH), F2-05, F2-06, F2-11, F2-12, F2-13 · **Effort:** S–M · **Tier:** Meaningful · **NEEDS-SKY-DEVICE:** yes
*Merge of the type-stress fixes (F2-02/11/13) and the alignment/inset fixes (F2-05/06/12) — one editorial frame: the family's shared header object, held true at magnification and at rest.*
1. **WHAT** — (magnification) Magnification stops eating the type. (a) ScreenHeader: when M18's auto-fit hits its 0.6 floor AND still overflows, the title wraps to two lines instead of ellipsizing — at-rest behavior byte-identical; "9 barriers" survives 200% as two dignified lines, never "9 b…". (b) formatDistance emits U+00A0 between value and unit so "297 m" can never orphan across lines. (c) F2-11's residual seam — a 376–390pt device at fontScale 1.10–1.14 sits between M16's compact-stack triggers — is recorded as harness-artifact-plus-window and registered on Sky's device gate, exactly per that finding's no-code-change direction; R3's read is not double-counted. (alignment) And the header family aligns to one law: Map's HeaderActions two circles regroup into one right-pinned pair (container gap spacing.xs, pointerEvents box-none) so the space-between row stops stranding the menu circle mid-air; Settings adopts Profile's convention (zero the header's own horizontal padding under the padded container so the eyebrow aligns with its own section rows at the container inset, killing the 44pt double-inset); and per F2-12's own direction, off-scale strays in the touched styles snap to tokens (mapHeaderRow 10, mapHeaderChip 14), the app-wide census remainder staying opportunistic PARK.
2. **WHY** — Resolves F2-02 (HIGH — WCAG 1.4.4 is the hard floor and the web export ships), F2-13, F2-11. THESIS: beauty never costs legibility — the editorial composition loosens one beat at 200% so the screen's anchor datum itself survives; ONE VOICE: the headline keeps speaking at every size instead of truncating to gibberish. The wrap direction extends the PROTECT'd ScreenHeader rhythm exactly as the F2 PROTECT nomination demands — extend, never loosen at-rest. Round 1's own skeptic canon already settled the methodology ("1.4.4's test IS 200% on the viewport you ship"). And F2-05/06/12: ONE HAND — the editorial header is the family's one shared object, and the hero surface dissolves the signature circle pair into what reads as a layout accident while Settings double-insets its eyebrow. This candidate takes the CHEAP, honest win — the Map regroup + the Settings double-inset kill (+ optionally nudging the Map overlay inset 16→20 for local consistency) — and deliberately does NOT declare a single 20 across every screen (that would reflow Profile's + Settings' whole card stacks 24→20, a spend the skeptic flagged; Profile stays out of the edit set). S8's shipped header rhythm holds; alignment is the cheapest luxury in the system.
3. **PERSONA IMPACT** — R3 owns all three finds — "the single most important number on the screen truncates into gibberish," the orphaned unit, and "'Resolved' overflows its own button… exactly the damage I fear," which gets a device-gated answer rather than a shrug. R1 and R6 keep the count that anchors orientation and comprehension. R2: visible text finally matches what SR always spoke. R4: M16's compact-stack defense respected, not re-tuned. R5: hygiene score rises. R7 first-class: wrap behavior is material-independent — RT users get the identical datum-survival guarantee over opaque panels, nothing derived, nothing lesser. On alignment: R5 — the alignment auditor whose "execution hygiene 5" this buys back — finally reads the family as one hand; R7 first-class (geometry-only, renders identically under engineered materials; cold-read cohesion climbs on the surface she sees most); R4 gains a predictable reach zone (circles always the top-right adjacent pair, 44pt held); R3's scan start stops moving between screens.
4. **TRUST + COHESION** — A headline that dies at the first accessibility stop reads as "not tested for me" — the precise opposite of this app's promise. Wrapping at the floor reads as designed-for-me. The unbreakable number-unit pair is the last 2% of the card R3 called "the best survivor in the whole set" — finishing it converts praise into proof. And four insets and a stranded circle read as four authors; one inset and one paired cluster read as one — the subliminal layer where "one hand" is either true or merely claimed, the S8 promise kept on the most-seen surface.
5. **A11Y + RESTRAINT** — Pure floor work: 1.4.4 at 200% restored on the shipping web surface; M18's 0.6 floor unchanged; M16 thresholds unchanged; at-rest rhythm guard-pinned byte-identical. F2-11 proposes no code, so nothing can be traded there. RESTRAINT: PASS per both active findings' inline verdicts — legibility outranks one beat of editorial tightness, and only at the extreme. Alignment leg: 44pt targets unchanged; the new pair container carries pointerEvents box-none so map gestures pass through its gap; type rhythm untouched (insets only); SR order re-verified post-regroup. RESTRAINT: PASS — pure regrouping and alignment; nothing spent.
6. **DEPENDENCIES / CONFLICTS** — Preserves PROTECT ScreenHeader type rhythm (wrap fires only at floor+overflow; eyebrow/display/subtitle micro-margins untouched) and preserves M18 auto-fit + M16 compact-stack contracts (no threshold moves). ScreenHeader.tsx is family-shared — coordinate commit order with the header-inset candidate (different lines, same phase acceptable, serialize commits). No fork adjacency. Does not re-open S18's closed 200% legs — that scope never included Home zoom-2 (per the skeptic rationale). Alignment leg preserves PROTECT box-none overlay law (circles stay discrete Pressables; container box-none) and the ScreenHeader type rhythm (horizontal insets only). ScreenHeader.tsx and MapScreen.tsx are shared across both legs and with T1 — different lines, serialize commits within the phase. Fork 9 untouched (HeaderActions is not ui/Button). Does not re-open S18's closed 200% legs (Home zoom-2 was never in that scope).
7. **VERIFICATION** — Guard jest: starved-width render (M18 floor reached, still overflowing) wraps to two lines with the full string present, zero ellipsis; at-rest snapshot pin proves rhythm unchanged. Re-shoot states/home__light__390__zoom200 (headline whole) and states/nearby__light__390__zoom200 ("297 m" unbroken). No color/ink/floor change → no arbiter sibling required. SKY DEVICE: Home headline at Dynamic Type 1.3 (confirms the code-inferred ~39pt no-truncation math) + the F2-11 window — Tasks action row on a 376–390pt device at fontScale 1.10–1.14, checking "Resolved" stays inside its pill. Alignment: capture diff at rest, both edited screens both themes (Map's menu circle right-pinned adjacent to feedback within gap tolerance of Home's; Settings eyebrow now at the container inset — i.e. aligned with its own section rows, double-inset gone — not a forced 20); layout guards (Settings header's OWN horizontal padding zeroed under the padded container; pair container present with gap spacing.xs + box-none); a11y-tree re-walk of Map top cluster and Settings header. The formatDistance U+00A0 change updates distance.test.ts's space-literal equalities and injects NBSP into the visible label only — confirm speakDistance (the SR path) stays byte-identical so VoiceOver distance wording is unaffected. No ink change → NO arbiter; blur budget untouched.


**Skeptic verdict: FIX (absorbed).** resolve the inset-law contradiction — scope to the CHEAP win (Settings header-zero + Map HeaderActions box-none regroup + optional Map overlay 16→20), DROP the 'one 20 inset across every screen' claim AND drop Profile; OR, if Sky wants the true single inset, state the full Profile+Settings container reflow (24→20 moves every card) and re-rate RESTRAINT + add device coverage; fix the self-contradictory verification (24 vs 20 — pick one); the formatDistance U+00A0 breaks distance.test.ts + injects NBSP into the a11y label — update tests, confirm speakDistance stays byte-identical.
### T14 — Every chip rail earns its overflow scent
**WHERE:** src/screens/MapScreen.tsx:1795/:1860 (+S16 fade :383–416/:1614–1628 efficacy) · src/components/NearbyFlagsModal.tsx:242 · src/screens/TasksScreen.tsx:980 · src/screens/ReportFlagModal.tsx:583/626
**Resolves:** F2-07,F2-08 · **Effort:** M · **Tier:** Meaningful · **NEEDS-SKY-DEVICE:** no
1. **WHAT** — The one shipped overflow affordance becomes the house dialect. Extend S16's fade edge — pointer-inert, a11y-hidden, static gradient over the clipped edge — to all five silent chip scrollers: filter-panel categories, filter-panel saved sets, Nearby category chips, Tasks category strip, Report sheet category chips; same measurement contract (onLayout vs onContentSizeChange), reused not forked. Then the F2-08 efficacy leg: live-probe the shipped action-bar fade on a truly overflowing rail — if measurement fires but the cue doesn't read, raise the light-theme gradient from rgba(15,27,45,0.22) until it registers over Positron tiles; if measurement fails, fix the measurement. One interaction, one dialect, everywhere chips overflow.
2. **WHY** — Resolves F2-07 + F2-08 (both MEDIUM, one mechanism). Both findings' own framing: an EXTENSION of S16 and its regression/efficacy check at HEAD — nothing closed is re-opened. ONE HAND / ONE MATERIAL: the same gesture (horizontal chip overflow) currently speaks two dialects, and the silent one clips mid-glyph like an accident ("Blocked path 1", "Steep g…", "M…"). Mission-level stake: a category chip the user can't see is a filter they don't know they have — this is comprehension of one's own controls, not chrome.
3. **PERSONA IMPACT** — R4 named the clip ("chip rows get clipped at the edge"); R7 named the missing hint ("clips the third category chip at the screen edge with no hint there's more") — and RT gains doubly: the fade is verified against her engineered-opaque C-lite panel fills, not just glass, so the scent works first-class under Reduce Transparency. R3 gets the affordance S16 promised her toolbar — the efficacy leg makes it actually fire at the moment she loses the crosshair. R6 discovers filters she'd "never guess" existed. R2 unaffected by design (fade a11y-hidden; scrollers announce as before). R1/R5 read one system where five accidents stood.
4. **TRUST + COHESION** — Five rails that hard-clip with zero scent read as five accidents by five authors; one fade grammar reads as one designed material behavior. The S16 investment finally pays across the whole app instead of on a single rail — and the efficacy check converts a shipped promise into a verified one, which is the difference between claiming craft and proving it.
5. **A11Y + RESTRAINT** — Fades are decorative, pointer-inert, a11y-hidden per the shipped S16 pattern — zero target, legibility, or SR cost. No fully-visible chip glyph may sit under the fade footprint (arbiter-checked). RM contract untouched: static gradients are not motion; no mounts, no springs. RESTRAINT: PASS per F2-07's verdict — and if any strengthened gradient ever muddies chip text, that strength dies, never the floor.
6. **DEPENDENCIES / CONFLICTS** — Preserves PROTECT box-none overlay law (map rails stay inside the existing overlay structure; fades pointer-inert) and S16's shipped measurement contract (reuse the mechanism, never fork it). Preserves S17's pointer-inert discipline. Shares MapScreen.tsx with the callout and header candidates and NearbyFlagsModal/TasksScreen/ReportFlagModal with the grammar suite's meta-text sub-items — different lines; serialize commits within the phase. No fork adjacency. GlassSurface untouched (gradients, not material panes).
7. **VERIFICATION** — Named arbiter sibling tools/r2-chip-fade-stacks.json — strengthened light gradient vs Positron tile luminance, fade over engineered-opaque C-lite panel fills, and the no-fully-visible-glyph-under-fade assertion, both themes. Per-rail guard tests: fade mounts iff contentWidth > layoutWidth. Forced-overflow captures per rail, both themes, 390 + 375. Live preview probe answers F2-08's two-hypothesis question (measurement vs gradient strength) before code moves. Blur budget untouched — gradients, not BlurViews; worst-state pane count unchanged including the tab-bar pane invisible to __getLiveBlurPaneCount.


**Skeptic verdict: FIX (absorbed).** WHERE path is src/screens/NearbyFlagsModal.tsx:242 (no components/ copy); match the SHIPPED S16 measurement contract `overflow>1 && !atEnd` wiring onScroll (scrollEventThrottle=16) to HIDE the fade at end-of-scroll — not merely onLayout/onContentSizeChange; label :583 as the templates rail (6 scrollers across 5 surfaces).
### T15 — Tab bar light-inactive ink meets AA everywhere
**WHERE:** src/theme.ts (one light-mode inactive tab-label ink token; + the RootNavigator.tsx tabBar consumption line only if the site doesn't already read a dedicated token) · new arbiter sibling tools/r2-tabbar-ink-stacks.json.
**Resolves:** M-48-ink (§6.12 shipped-truth drift; the mechanism-kill NO-ACTION stands) · **Effort:** S · **Tier:** QuickWin · **NEEDS-SKY-DEVICE:** no
1. **WHAT** — One darker light-mode inactive tab-label ink in theme.ts — the #3F4854-class value the M-48 kill record already proves survives even the 0.82 chrome floor — replacing the shipped #6B7280 for the 12px inactive labels; dark mode untouched (passes everywhere today). Deliberately OUTSIDE the migration train: the train's zero-theme-edits rule is why this candidate exists (04 §6.12 — 'Part 3 slates it'). Diff shape: one token (plus one consumption line if needed), and theme.ts:181's stale '~4.8 on white' comment corrected to the translucent-state truth. No layout, no mechanism, no motion — TabBarGlass's ratified BlurView/RT architecture stays byte-identical, and the mechanism-adoption kill stays dead.
2. **WHY** — Resolves M-48's shipped ink drift — carried at HIGH on the merged ladder, and the one Stage-6 drift family the train cannot repair. The light inactive label fails AA on every translucent state the bar actually reaches: 3.17 on the 0.82 tabBarGlassFloor, 4.04 on the 0.92 RT/web surface, over the live #000 worst case (dark photos under the bar; the always-dark web tiles). The THESIS's hard floor says beauty never costs legibility — and this is the app's most persistent chrome, the frame around every screen. ONE VOICE: the navigation frame must speak as clearly as the content it frames.
3. **PERSONA IMPACT** — R3 (low-vision, large-type) is the primary beneficiary — inactive tab labels become readable over exactly the content that breaks them today (dark photos, dark web tiles). R7 first-class: the RT/web 0.92 surface FAILS today too (4.04) — the new ink passes there by declaration, so Reduce Transparency users gain a compliant bar in their own designed state, not a hand-me-down from the blur path. R6 gains steadier wayfinding chrome at a glance. R2: labels and roles unchanged. R1/R4: zero target or gesture change. R5 reads it as invisible craft — changed only where it was broken.
4. **TRUST + COHESION** — A navigation frame that quietly fails AA under real content contradicts 'disabled users deserve beautiful software' at the app's front door. The drift is already RECORDED in the trials file (exit-1-by-design findings record) — repairing it converts a documented failure into a documented pass, keeping the audit's honesty mechanism intact: shipped truth, declared truth, and pixels agree. It also protects the M-48 kill's integrity: the kill stands on shipped inks; this fixes the ink without reopening the mechanism question.
5. **A11Y + RESTRAINT** — Pure floor repair — the exact class the thesis calls the hard floor. No eye-tuned floors: the token ships only if the named arbiter sibling passes 4.5:1 on the true worst composites (0.82 glass floor over #000 and #FFF; the 0.92 RT/web surface; dark floors as regression sanity). No layout, motion, target, or mechanism change; the visible delta is a slightly darker inactive gray in light mode only. RESTRAINT: PASS — one token, arbitrated, invisible except where it was illegible. Nothing is traded; this is the floor asserting itself.
6. **DEPENDENCIES / CONFLICTS** — Independent of the train and deliberately so — the cluster's ONLY theme.ts edit; must never ride an MP branch (the train's zero-theme-edits gate is diff-checked). If a RootNavigator consumption line is needed, land before MP5's branch cut or after its merge (MP5 touches RootNavigator :371–:381 — different lines, but sequencing keeps diffs clean). Preserves the ratified M-48 kill record (mechanism adoption stays dead; TabBarGlass byte-identical). No PROTECT surfaces touched; no fork adjacency; no strings proposed.
7. **VERIFICATION** — NAMED arbiter sibling tools/r2-tabbar-ink-stacks.json: the new inactive ink declared over tabBarGlassFloor 0.82 light (bases #000/#FFF), the 0.92 tabBarBg RT/web surface, and the dark floors — exit 0 required before ship; cross-check r2-trials-stacks.json so the M-48 light-inactive family reads repaired while the kill rows stay exit-1-by-design. Guard test pinning the inactive label token at the consumption site; typecheck/lint/jest green. No device read: the failing states ARE the declared worst-case composites — the arbiter decides, per the no-eye-tuned-floors rail.


**Skeptic verdict: FIX (absorbed).** attribution — the #3F4854-class value is NAMED by 04 §6.12, not 'proved' by the §4 kill record; target the LIGHTEST ink clearing 4.5:1 with margin on the 0.82/#000 worst composite (NOT the darkest #3F4854, which overshoots at 6.07) to preserve luminance separation from the active #0F53BE tint; drop the 'plus one consumption line' hedge — RootNavigator:281 already reads color.tabBarInactiveTint, so it is one theme.ts token + the :181 comment fix.
### T16 — One badge, one writer, one meaning
**WHERE:** src/navigation/RootNavigator.tsx:219–221 (kept: the store-derived single writer) + src/screens/TasksScreen.tsx:615–620 (deleted: the navigation.setOptions writer).
**Resolves:** F3-03 · **Effort:** S · **Tier:** QuickWin · **NEEDS-SKY-DEVICE:** no
1. **WHAT** — The Tasks badge gets one writer and one definition for the app's whole life. Structural fix: delete the TasksScreen setOptions writer and let the RootNavigator store-derived writer be the badge's single source — identical before, during, and after every tab cut, never re-defined by a screen mounting. Recommended default meaning: open-only, matching the list header's visible 'OPEN n' and the pre-mount value users meet first; the alternative (open+verified) is a one-line swap inside the single writer. Which meaning the badge carries is Sky's one-line call — this candidate builds the structure so either answer is true everywhere, forever.
2. **WHY** — Resolves F3-03 (MED; Round-1 L8-9 lineage, never slated into S1–S20 so legitimately open, now photographed mid-transition: badge 6 on Home, 9 at t100 in the same frame as a header reading OPEN 6). ONE VOICE, structurally: a number that changes meaning at the exact moment you cross the transition it decorates is the app contradicting itself on its loudest call-to-action. Continuity of state across cuts is what makes three tabs read as one world. The copy half (≡F4-05 — what the badge should say it counts) stays with the voice-states cluster; this is the plumbing that makes any wording true.
3. **PERSONA IMPACT** — R7 hit it cold — 'the Tasks tab badge says 9 while the list header says Open · 6 — which number do I believe?' — and gains a badge that can never disagree with the header; RT users, leaning harder on text-and-number channels than material cues, get numeric self-consistency as a first-class trust primitive. R6 ('I apparently owe this app six things') gains a stable obligation count under cognitive load. R2 gains one spoken badge value that matches the list summary. R1 and R5 see the small-systematic-leak class of defect closed at the source. R3/R4 neutral — nothing visual moves but the wrong number.
4. **TRUST + COHESION** — Two blinded R2 readers flagged it fresh — a live, systematic trust leak on the tab users are most invited to press. One writer means badge, header, and SR summary can never diverge by construction rather than by luck. Cheap structural credibility: the fix class that makes 'Verified' and every other number in the app easier to believe.
5. **A11Y + RESTRAINT** — No visual, motion, color, or target change — a definition consolidation. SR badge announcements become consistent with visible counts: an honesty gain in the spoken channel at zero cost anywhere. Nothing arbiter-relevant, nothing near the floor, nothing repainted. RESTRAINT: PASS — pure consistency; there is no polish here to trade access against.
6. **DEPENDENCIES / CONFLICTS** — Frames, never decides: the badge's MEANING (open vs open+verified) is Sky's one-line pick inside the single writer; the structure ships correctly under either. Not adjacent to any numbered fork — Fork 2 (points economy) untouched, no point values involved. Copy half stays with the voice-states cluster (≡F4-05); any label/tooltip wording lands after the single writer exists. RootNavigator.tsx shared with the Settings-arrival and drawer-exit candidates — trivial separate hunks. No PROTECT touched.
7. **VERIFICATION** — Guard tests: assertion that TasksScreen sets no tabBarBadge via setOptions (grep-style test); unit test of the single formula from the shared store across pre-mount, post-mount, and post-triage states; a triage action updates badge and header from the same state commit. Re-capture tabswitch t0-before/t100: the badge shows the same number in both frames and agrees with the visible header. No arbiter, no device gate — jest plus Chromium frames suffice.


**Skeptic verdict: FIX (absorbed).** scope the self-consistency claims to the UNFILTERED/default arrival + the tab cut (writer 2's flags.length is filter-affected — badge and header CAN diverge under a category filter by design); add an explicit guard note that writer 1 must STAY global/unfiltered (do NOT 'fix' this by making the badge filter-aware).
### T17 — One voice — the convergence sweep + the honest-completeness promise
**WHERE:** copy.ts (new single-source caveat export beside offlineBannerText) · MapScreen.tsx (heat disclaimer + heat-toggle hint + FAB label) · LegendModal.tsx + HelpModal.tsx (caveat import; dialect; query echo) · TasksScreen.tsx (subtitle, end-of-list footer + matched SR label, search empty) · NearbyFlagsModal.tsx (search placeholder + empty strings only) · ReportFlagModal.tsx (sheet title, partial-success titles, capability alert) · HamburgerDrawer.tsx + ProfileScreen.tsx + FlagDetailModal.tsx (casing) · SettingsScreen.tsx (banner sentence, header retitle) · HomeScreen.tsx (genuine-empty clause) · UpdateBanner.tsx (glyph half only).
**Resolves:** F4-06, F4-07, F4-09, F4-13, F4-14, F4-15, F4-16, F4-18, F4-19, F4-20, F5-10 · **Effort:** S · **Tier:** QuickWin · **NEEDS-SKY-DEVICE:** no
*Merge of the one-privacy-promise/honest-completeness pair (F4-06/09) and the convergence sweep (the register's residual drift) — one copy pass; every string PROPOSED via before/after table; nothing ships.*
1. **WHAT** — (promise) The app's completeness honesty speaks once, exactly. (1) The k≥3 privacy caveat — currently three wordings: "reported" / "submitted" / "reports are shown" — converges on the map disclaimer's anchor wording and becomes one exported string beside offlineBannerText in copy.ts, keeping the DEFAULT_K_FLOOR interpolation, imported by all three surfaces. (2) The Tasks end-of-list footer drops its two unearned claims — before: "That's everything nearby — you're up to date" (SR: "You have seen all flags nearby"); after sketch, em-dash grammar: "That's every report loaded — more appear as neighbours flag them." with the SR label matched. Both PROPOSED via before/after table; nothing ships without Sky. (sweep) And one pass retires the register's residual drift: noun canon (signed-in sheet title and Map FAB adopt "Report a barrier (here)"; the Tasks subtitle retires "reports"); the two bureaucrat sentences rewritten per the findings' sketches; Nearby search converges on Tasks' praised wording; partial-success "Report filed — context tags not saved."; dialect standardized ("colour", Canadian majority); casing finishes S20's sentence-case sweep; Help echoes the failing query and Home's genuine-empty gains one inviting clause toward the FAB; the Settings header stutter (eyebrow "SETTINGS" over title "Settings") retitled to Sky's pick; the surviving 🔔 becomes Lucide Bell per the B2 pattern.
2. **WHY** — Resolves F4-06 + F4-09 (both MEDIUM, one register: completeness claims). PROTECT-11 names the k≥3 caveat part of the trust moat — the one sentence the app should say identically every time; three wordings of a privacy promise invite the reading that none is exact. The footer contradicts the heat caveat's honest-completeness register at the exact moment a verifier decides they're done (R1: "absence of a flag is not absence of a barrier"). ONE VOICE: the app's most ethically load-bearing sentences adopt the offlineBannerText one-true-string discipline — the app's own model, named in its own docstring. And the sweep resolves nine LOW/POLISH drifts plus F4-07: F4's lens verdict names dispersion as "the register disagreeing with itself" — each item small, together the audible difference between one writer and a committee. ONE VOICE by name; S15/S20/B2 each get their completion clause (cited as shipped-fix-incomplete under the regression clause, not re-opens).
3. **PERSONA IMPACT** — R1's core stake is the footer: the completeness claim stops overreaching. R6 — who kept the app because it is honest about its limits — gets the same honesty at list-end that won them at submit. R2 hears one identical privacy rule on every surface; repetition builds authority in a spoken-first session. R5's dispersion diagnosis gets its cleanest counterexample. R7 first-class: these strings render on solid banners and footers, identical in RT — R7 gains one memorizable rule sentence (cognitive load down) instead of three near-misses. R3/R4: no layout, target, or type changes. On the sweep: R6's Round-1 "four-name whiplash" gets its last leak sealed at the highest-traffic moment; R5's cold-read catches (casing drift, register slips) are the sweep's literal checklist; R2 hears consistent nouns and echoed queries; R7 first-class — gains the Bell inside the Lucide iconography she called designed-for-me, on solid surfaces RT renders identically.
4. **TRUST + COHESION** — A privacy promise worded three ways reads as unverified paperwork; worded once, it reads as law. The end-of-list beat stops claiming a census a crowdsourced model cannot have, aligning Tasks' warmest closing moment with the heat caveat's honesty register. Single-sourcing hardens the future: the next surface that needs the caveat imports it and cannot drift — the same mechanism that ended the offline banner's three-way drift. And dispersion is how a one-writer voice becomes a committee; this sweep is the committee resigning — none of it individually visible, all of it together why the app reads as one hand.
5. **A11Y + RESTRAINT** — Zero visual change beyond string content; SR labels updated in lockstep so visible and spoken stay paired. Honesty strictly improves — two unearned claims removed, zero warmth lost (the replacement stays inviting). The k threshold, heat mechanics, and the Jordan Art. 7 disclaimer's required visibility are untouched — wording convergence only. Em-dash grammar held in both proposed strings. RESTRAINT: PASS — words only. Sweep: strings and one decorative glyph only; every SR label updates in lockstep; the designed spoken-brevity pairs stay designed; the five-way Tasks empty fork (PROTECT) and AddressSearch's failure≠no-matches split (PROTECT) untouched; em-dash grammar held in every proposed status string; the Bell inherits the emoji's decorative hiding. RESTRAINT: PASS — register repaired, nothing traded.
6. **DEPENDENCIES / CONFLICTS** — Fork 4 (k-anonymity) adjacency: the rule's DECISION half is Sky's — this converges the wording of the existing promise, never the number or mechanics; DEFAULT_K_FLOOR stays the single numeric source. Extends PROTECT-11 and the offlineBannerText discipline (PROTECT). Preserves the five-way Tasks empty fork (PROTECT) — the footer is the pagination end-line, not the empty fork. Shared files: MapScreen.tsx and TasksScreen.tsx overlap other candidates — disjoint lines. "Neighbours" spelling aligns with the sweep candidate's F4-13 dialect call — land together or sweep-first. Coordinate F4-19 with T2's RB11 (same UpdateBanner.tsx): this ships only the glyph/string half; the merge lead may fold it into T2/MP0. PROTECT-1: Nearby's frozen row labels untouched (only search placeholder/empty converge). Fork 6 (product name) untouched by the noun canon; Fork 3 untouched (the auth-variant title is a string change). F4-20's retitle is a Sky taste pick. Fork 4 (k-anonymity) adjacency: converges the WORDING of the existing k≥3 promise, never the number or mechanics (DEFAULT_K_FLOOR stays the single numeric source).
7. **VERIFICATION** — Grep/jest single-source guard: the caveat string has exactly one definition and three import sites; zero other "at least 3" user-facing variants in src. Snapshot tests on all three render sites emitting the anchor wording with the interpolated floor. Footer test: visible string and SR label both updated ("You have seen all flags nearby" retired). Before/after copy table to Sky — strings are PROPOSED. No ink/floor change, so no arbiter. A11y-tree re-walk of legend and Tasks end-of-list confirms spoken parity. Sweep: before/after copy table (every string, both channels) for Sky sign-off; jest string/snapshot tests per site; grep guards (zero "Report a flag" variants, zero user-facing "color", zero title-case strays in the named rows, the BENCH-1 emoji census extended to unit-render UpdateBanner behind the fence); a11y-tree re-walk on report-open, tasks, legend, settings. No colour/floor/ink change → NO arbiter, except if the Bell's ink pair is new rather than inherited, run tools/r2-voice-sweep-stacks.json.


**Skeptic verdict: FIX (absorbed).** search-convergence — do NOT port Tasks' verbatim placeholder to Nearby (both use searchFlags matching description+category+STATUS); converge the register but keep the TRUE 3-field list ('…description, category, or status…') on both; the F4-07 noun flip breaks ReportFlagModal.test.tsx:427/430 + :399/402 + qaMergeConsolidation.test.ts:35 — name + update them in the same commit; the k≥3 caveat IS the Jordan Art. 7 disclaimer — the before/after table carries an explicit Jordan semantic-equivalence sign-off, not only Sky's taste read.
### T18 — The tutorial tells the present truth
**WHERE:** OnboardingModal.tsx (announce gate + deck copy source), OnboardingCards.tsx (shared CARDS source or copy export), SettingsScreen.tsx (replay row promise).
**Resolves:** F4-04,F4-11 · **Effort:** S · **Tier:** QuickWin · **NEEDS-SKY-DEVICE:** no
1. **WHAT** — Two repairs to the replay tutorial. (1) Gate the position announce on visible — the useEffect([index]) fires on closed mount today, publishing a phantom "Step 1 of 3" into Settings' live region (and straight to VoiceOver on native). (2) End the two-generation copy split: the replay deck ports S15's rewritten copy (canon "barrier," the photo-needs-account honesty, the current slide-2 sentence) and adopts one position grammar ("Card N of M," matching OnboardingCards) — preferably by sharing a single CARDS source with a digest flag rather than duplicating strings; the Settings row's "3-card" promise updates to whichever count ships. Consent slides are unaffected on replay. Both decks' copy PROPOSED side-by-side for Sky.
2. **WHY** — Resolves F4-04 + F4-11 (both MEDIUM, one root: the replay deck is a fossil). ONE VOICE head-on — the lens verdict's sharpest line is that the app "remembers its own old accent and replays it on request": a user asking to be re-taught receives pre-uplift vocabulary that disagrees with every surface they'll then touch. The phantom announce is spoken noise that erodes trust in every real announcement, and the announce channel is the mission cohort's primary channel — its credibility is a floor asset, not polish.
3. **PERSONA IMPACT** — R2 first: the stray "Step 1 of 3… referring to nothing" leaves Settings, and every future announcement regains authority. R6 — the first-timer under cognitive load, exactly who replays a tutorial — relearns the app in the words it actually uses now, including the canon noun and the photo honesty sentence. R1: the taught vocabulary matches the severity grammar everywhere else. R7 first-class: the replayed deck teaches the app R7 called 85% first-class, not its pre-uplift ancestor — the digest R7 might lean on under RT stays current. R3/R4/R5: consistency at zero layout cost.
4. **TRUST + COHESION** — A tutorial is the app explaining itself; when it speaks last year's voice, every canon string it contradicts loses a little authority. After: one deck lineage, one position grammar, one noun canon from first-run through replay — and a live region that only ever speaks the truth, which is the cheapest trust asset the spoken app owns. The Settings row's promise ("3-card") stops disagreeing with what opens.
5. **A11Y + RESTRAINT** — Strictly improves the floor: removes a false status message (WCAG 4.1.3 quality) while keeping the position announce for real navigation; keeps "Back. Disabled on first card." (PROTECT-17, reaffirmed by this cluster's rail) intact; adds the photo-needs-account honesty the replay currently omits. No motion change: the modal mount stays RM-ternaried as shipped and the announce remains motion-decoupled. RESTRAINT: PASS — removes noise, adds truth, decorates nothing.
6. **DEPENDENCIES / CONFLICTS** — Preserves PROTECT-17 and the settled S19 platform fork — consent/permission slides untouched on replay (the shared-source-with-digest-flag option avoids re-running consent entirely; recommend it over replaying the raw 5-card deck). Shared file: SettingsScreen.tsx also touched by the sweep candidate (F4-20) — disjoint rows. No fork adjacency, no material, no inks, no blur. Sequencing: fully independent; a safe first merge for the cluster.
7. **VERIFICATION** — Jest: mounting OnboardingModal with visible=false fires zero announcements (mock announceForAccessibility + live-region text assert); opening announces "Card 1 of N" exactly once; a copy-parity test pins replay strings equal to the S15 source (structural if single-sourced); the Settings row promise matches CARDS.length. A11y-tree re-walk of Settings — the orphan text node at line 69 is gone. Capture the replay deck (opens from Settings; capturable per the finding). No ink change, so no arbiter. Native VoiceOver spot-check may ride any existing Sky device session but is not gating — the web tree plus jest prove the fix.


**Skeptic verdict: FIX (absorbed).** harden the announce gate — add `visible` to the effect deps AND guarantee exactly one post-reset 'Card 1 of N' (coordinate with the reset-to-0 effect so no stale pre-reset index fires first); extend the jest case to reopen-after-navigating-to-a-later-card; make the copy-PORT path (canon 'barrier' + slide-2 sentence + photo-needs-account line + 'Step'→'Card' + Settings:579 count) the primary Effort-S plan, CARDS de-dup optional; PROTECT-17 lives in OnboardingCards:417 (the replay Back is conditionally rendered — legit).
### T19 — Retire ghost hues; seat the missing marks
**WHERE:** src/theme.ts:131 + src/theme/ThemeContext.tsx:97 (accentOrange) · src/screens/ProfileScreen.tsx !user block + src/screens/AboutScreen.tsx (LogoMark) · app.json:56 (notification color)
**Resolves:** F6-07,F6-08,F6-09 · **Effort:** S · **Tier:** QuickWin · **NEEDS-SKY-DEVICE:** yes
1. **WHAT** — Three strikes against the ghosts. (a) F6-07: re-audit accentOrange (#f1a520) against the current ramp where it near-twins sev-2 #F0A030 — nudge it out of the sev-2 band if a hue holds its recorded 6.3:1 dark-text duty (ThemeContext.tsx:47), else accept the hue-share explicitly and correct the stale theme.ts:131 comment so the guarantee is honest; the grammar suite's numbered discs defuse the adjacency structurally either way. (b) F6-08: the color LogoMark, ~48–64pt, above signed-out Profile's sign-in sentence — the sign-in screen's own posture at lower volume — and a small mark beside "About AccessMap"; one mark each, no copy inflation. (c) F6-09: app.json notification color #1a4fa3 → #1466E0 — the last front-door value on a dead palette.
2. **WHY** — Resolves F6-07, F6-08, F6-09 — the LOW tail as one hygiene pass. ONE HAND extends to the OS-level face and the grammar's hue band: chrome spectrally indistinguishable from severity ink leaks the signature (three small orange marks in one MyWatched row, one meaning danger, two meaning "watched"); the account doorway R7 called "unfurnished rather than minimal" gets its brand moment; the notification tint stops wearing a blue whose retirement is on record in the test suite. Restraint IS the taste here: marks land only in the two bare rooms — everywhere else the mark's absence is correct and stays.
3. **PERSONA IMPACT** — R7's own cold-read answered — and first-class under RT: the mark sits on the gradient with arbiter-recorded contrast, identical under engineered materials, so her doorway gains the same furniture, not a derived version. R3's three-orange adjacency becomes disambiguable structurally (digits) and, if the hue moves, spectrally — no more squinting orange-vs-orange. R5's dead-palette find closes. R1/R6 get brand presence at the account system's trust doorway. R2: marks decorative or labeled once — zero SR noise added. R4: no targets change.
4. **TRUST + COHESION** — The signature's hue band stops leaking into chrome — severity color goes back to meaning exactly one thing. The app's OS-level surfaces (notification tint, splash, favicon, web themeColor) finally speak with one hand. Two bare rooms stop reading as unfinished. Small strikes, but they are the difference between "one hand" and "mostly one hand" — and R7 measures that gap at 15%.
5. **A11Y + RESTRAINT** — Any accentOrange recolor must hold every recorded contrast duty or it does not move — the arbiter decides, never an eye-tuned floor. The LogoMark carries no severity meaning, so no grammar dilution is possible (the finding's own logic); marks add zero copy, zero targets, and preserve each screen's calm. The stale-comment correction is pure honesty. RESTRAINT: PASS on all three, per their inline verdicts.
6. **DEPENDENCIES / CONFLICTS** — COORDINATION (flag for merge lead): F6-08's Profile mark shares the ProfileScreen !user block with voice-states' ceded F2-04 (guest header restoration) — compose, don't collide: the mark lands with or after that header fix, distinct commits, never the same phase-file collision. F6-07's structural defusal sequences after the grammar suite, or ships standalone as hue-nudge/comment-fix. Preserves the reward-gold PROTECT (no reward element gains disc form — untouched) and Civic Gold itself (#FBB024 not in scope). No fork adjacency.
7. **VERIFICATION** — Named arbiter sibling tools/r2-brand-cameo-stacks.json — candidate accentOrange hue × its real surfaces both themes (FlagDetailModal watch, MyWatched Star/unwatch glyph, SavedPlaces, NotificationPrefs, streak Flame) holding every recorded ratio, plus LogoMark-on-gradient pairs for both themes. Unit test pinning app.json notification.color === splash/web themeColor (#1466E0), sitting beside the existing retirement test (theme.test.ts:115). Captures: profile-signedout pair + about pair with marks seated. SKY DEVICE: the auth-fenced MyWatched orange-adjacency row on device; Android notification tint if an Android device is available — otherwise it ships code-verified as hex parity with the front-door set.


**Skeptic verdict: FIX (absorbed).** LOAD-BEARING — add theme.test.ts:163-167 to WHERE+VERIFICATION (branch A updates the accentOrange hex assertion in lockstep AND re-points the stale `.not.toBe(severity4Color())` guard to the real sev-2 #F0A030 adjacency); expand the arbiter sibling to cover EVERY accentOrange duty before any hue moves (dark-text-on-fill at the reopen button, accentOrange-as-text); rename 'ceded F2-04' → 'T10 (F2-04 guest-header restoration)'; optional: accessibilityElementsHidden on the About-screen LogoMark (it double-speaks 'AccessMap').
### T20 — Three invisible-hand hygiene branches (containment · dialog tier · paper-matches-pixels)
**WHERE:** src/components/MyWatchedModal.tsx (sheet container ~:273, one prop) · MapScreen.tsx (shared nameCard shadow.e3 — M-20/M-21) + ProfileScreen.tsx (M-43/M-44 animationType slide→fade) · GLASS.md §7/§8 + public/index.html splash style block (comment only).
**Resolves:** §6.9 (MyWatched accessibilityViewIsModal), RB12 (dialog-tier craft delta / §6.11), M-57 (§6.5/§6.6 doc sync) · **Effort:** S · **Tier:** QuickWin · **NEEDS-SKY-DEVICE:** yes (containment leg only)
*Grouped for slate economy: three independent one-commit branches, each the invisible hand finishing an already-ratified thing. Each lands in whatever window Sky picks; NONE re-opens a ratified material row.*
1. **WHAT** — (containment) MyWatchedModal's sheet container gains accessibilityViewIsModal — the one prop the app-wide SR-containment blanket misses (census M-40; 04 §6.9: the ONE sheet missing it; ReportFlagModal's census row shows the shipped pattern). A one-prop, zero-pixel diff. Deliberately NOT folded into MP2: the train's byte-identical rule carries the gap forward by design, so this candidate is the complement, not a duplicate — it lands as its own one-commit branch in whichever window Sky picks; §6.9 says the landing spot is Sky's call, and this candidate frames the two clean windows (before MP2's branch cut, or after MP2 merges) without choosing. (dialog tier) The ratified four-dialog tier (M-20/M-21 map name prompts, M-43 tier explainer, M-44 delete-account) closes its two recorded intra-tier deltas at smallest honest scope. (1) Shadow: shadow.e3 extends to the Map name prompts — one shared nameCard style object — making depth four-of-four; the cards are solid surface, so the transparent-bg shadow suppression doesn't apply. (2) Entrance: unify on FADE — the Map pair's existing entrance — by switching the Profile pair's slide to fade, preserving every mount's RM ternary (RM still gets no entrance motion). Explicit fallback (the 'record' half of §6.11's unify-or-record): if Sky prefers zero motion churn, ship shadow-only and record the fade/slide split as a deliberate tier note riding the doc-sync candidate. (paper) One documentation commit, sequenced after MP1 (ideally the train's caboose): (1) public/index.html's splash styles gain a 'keep in sync with theme.ts' comment block naming the 10 tracked hexes + app.json's eleventh (M-57's maintenance note, §6.6) — comment-only, zero rendered change; (2) GLASS.md §8's Home line corrected chrome→row for the search pill, making §8 checkably true the day MP1 merges; (3) §8's tab-bar 'later cleanup' line gains a pointer to the M-48 kill table (4/4 AA failures on the true chrome floor) so the cleanup is never naively re-attempted; (4) one-line §7/§8 notes recording the drawer and dialog-tier ratifications. The commit's HAND — executor caboose vs Sky — is §6.5's open question: specced here, decided by Sky.
2. **WHY** — (containment) Resolves the M-40 containment facet. ONE VOICE: for a screen-reader user, ten sheets that contain focus and one that leaks it into the host Profile screen speak two interaction dialects — containment is part of the app's voice, not decoration. The audit's access-first ethic says hygiene gaps this small don't wait for a redesign train; the mission (a civil-benefit accessibility app) makes an SR-containment hole on a user's own watched-flags list a small but genuine credibility wound. Serves the taste anchor by omission: restraint includes fixing the invisible layer without touching a pixel. (dialog tier) Resolves RB12's craft-delta remainder (04 §6.11): the tier is ratified 'consistent where it matters' — fill, radius, centering, scrim four-for-four — but shadow and entrance still split two-and-two. ONE HAND by name: a tier is a hand only when its four members land identically, and these are the app's decision moments — decision moments deserve one grammar, including the destructive one. Taste anchor: unifying DOWN to fade is restraint — less motion, calmer arrivals — and e3 four-of-four is Deep Field depth discipline applied consistently rather than decoratively. (paper) Resolves M-57's maintenance-note obligation and closes the paper loop on the census's ratifications. This audit itself was slowed by GLASS §8's drift — the census had to flag the Home line as aspirational before it could proceed. For a solo maintainer running agent executors, the law files ARE part of the material system: ONE VOICE means the paper record and the pixels tell the same story, or every future audit and every future agent re-derives — or worse, re-'fixes' — settled ground. The M-48 pointer specifically guards a proven AA cliff.
3. **PERSONA IMPACT** — (containment) R2 (blind, VoiceOver-primary) is the direct beneficiary: swipe-explore inside My Watched stops escaping into ProfileScreen content behind the sheet — the same containment R2 already gets in every sibling sheet. R7 gains identical containment in RT and C-lite: the prop is mechanism-independent, so the Reduce Transparency experience improves in lockstep, not later. R6 loses phantom focus targets behind an open sheet — less disorientation under load. R1/R3/R4/R5: zero visible change, which is the point — a zero-pixel access fix is the thesis in miniature. (dialog tier) R6 gains one learned pattern for every decision moment; R5 reads the tier as finished; R7 first-class (glass-free tier — RT/C-lite identical unified shadow + entrance); R3 gains e3 figure-ground on the Map prompts. (paper) R7 is the persona this protects hardest — the M-48 kill pointer documents WHY the tab bar's mechanism must never be "cleaned up" into glass (4/4 AA failures on the true floor), the regression class that would hit RT and low-vision users first.
4. **TRUST + COHESION** — (containment) closes the last gap in an app-wide invariant — "every sheet contains screen-reader focus" becomes a true sentence, not ten-of-eleven; ONE HAND includes the non-visual hand. (dialog tier) the ratification's own sentence — "this one is a tier, not a break" — becomes checkable: four fills, four radii, four scrims, four shadows, one entrance. (paper) after this commit GLASS §8 is checkably true for the first time since the Home line was written; the audit's no-silent-fixes discipline extends to documentation.
5. **A11Y + RESTRAINT** — (containment) pure floor addition, zero visual/motion/target/string cost — RESTRAINT: PASS trivially; the care point is sequencing (land before MP2's cut or after its merge, never between). (dialog tier) zero floor movement; motion decreases or holds (slide→fade is strictly gentler; every mount's RM ternary preserved); NO arbiter (no colour pair changes) — RESTRAINT: PASS. (paper) zero pixels/floors/strings — comment and doc text only; the M-48 pointer is itself a restraint mechanism preventing a proven-4/4-failing mechanism swap — RESTRAINT: PASS by construction.
6. **DEPENDENCIES / CONFLICTS** — (containment) shares MyWatchedModal.tsx with T2/MP2 — land strictly BEFORE MP2's branch cut or AFTER MP2 merges (preserves MP2's material-only diff); landing spot is Sky's call per §6.9; stands alone even if the train is deferred; M-40's material row stays with T2 (facet-disjoint). (dialog tier) shares ProfileScreen.tsx and MapScreen.tsx with T2/MP0 — cut AFTER MP0 lands; preserves PROTECT-7 / the RM designed-stillness contract and the tier's ratified §2b reasons (text inputs stay solid; destructive stays solid); explicit record-only fallback (ship shadow-only, record the split as a tier note on the paper branch) if Sky prefers zero motion churn. (paper) hard-sequenced AFTER MP1 merges (else §8's Home correction documents an unshipped state); §6.5's executor-vs-Sky ownership half stays Sky's; if Sky picks record-not-unify on the dialog tier, that note lands here; if MP5 is skipped, the B14 deliberate-exception line lands here too.
7. **VERIFICATION** — (containment) jest guard asserting the prop; a11y-tree re-walk on the web export; SKY DEVICE — VoiceOver iOS, Profile → My Watched, swipe-explore forward/back, focus stays inside until close (native-SR-only; jest proves presence, device proves containment). (dialog tier) jest RM-ternary guards on all four mounts + snapshot asserting shadow.e3 on all four cards and fade at standard motion; render states all four dialogs light+dark, standard+RM; NO arbiter; NO device gate (simulator-verifiable). (paper) diff-check public/index.html byte-identical outside the new comment block; grep proves GLASS §8's Home line matches HomeScreen.tsx's shipped mounts; the M-48 pointer resolves to the banked trials-file path; NO arbiter, NO jest surface, NO device — legible in a diff reader.


**Skeptic verdict: FIX (absorbed).** containment — pin accessibilityViewIsModal to the sheet CONTENT view (styles.sheet ~:275, mirroring MyReports:265), NEVER styles.backdrop; write the GLASS.md §8 M-48 pointer as self-contained PROSE stating the finding inline (not a path into untracked design-reviews/); ENUMERATE the actual index.html hexes (11 distinct + 3 app.json literals), not a bare '10'.
## CONSERVATION MAP (id → T; verified no-orphan, no double-assignment)

Signature-class ★: **T1 · T2 · T3 · T5 · T6** (5 ★, exceeds ≥3). Full tier mix below.

**F-ids (65 canon; the 8 absorbed twins ride their canonical):** F1-01→T4 · F1-02→T3 · F1-03→T11 · F1-04→T11 · F1-05→T3 · F1-06→T3 · F1-07→T3 · **F1-09→PARK-with-direction** (Dani-gated rail-zoning; kill-condition built in — see conservation table) · F1-08→T4 · F1-10→T3 · F1-11→T3 · F1-12→T3 · F1-13→T3 · F1-14→T3 · F2-01→T1 · F2-02→T13 · F2-03→T6 · F2-04→T10 · F2-05→T13 · F2-06→T13 · F2-07→T14 · F2-08→T14 · F2-11→T13 · F2-12→T13 · F2-13→T13 · F3-01→T12 · F3-02→T12 · F3-03→T16 (voice twin ≡F4-05 rides it) · F3-04→T1 · F3-05→T1 · F3-06→T1 · F3-07→T12 · F4-01→T8 · F4-02→T8 · F4-03→T7 · F4-04→T18 · F4-06→T17 · F4-07→T17 · F4-08→T8 · F4-09→T17 · F4-10→T8 · F4-11→T18 · F4-13→T17 · F4-14→T17 · F4-15→T17 · F4-16→T17 · F4-17→T8 · F4-18→T17 · F4-19→T17 (string half) + T2/RB11 (material half) · F4-20→T17 · F5-01→T9 · F5-02→T9 · F5-03→T7 · F5-05→T9 · F5-09→T9 · F5-10→T17 · F6-01→T5 · F6-02→T5 · F6-03→T5 · F6-04→T5 · F6-05→T5 · F6-06→T5 · F6-07→T19 · F6-08→T19 · F6-09→T19. Absorbed (canonical in parens): F5-04/F5-08(F2-03→T6) · F5-06(F2-04→T10) · F5-07(F3-01→T12) · F4-05(F3-03→T16) · F4-12(F5-01→T9) · F2-09(F6-02→T5) · F2-10(F1-10→T3). **All 65 routed; F1-09 the sole PARK.**

**M-ids (57):** 15 MIGRATE→T2 (M-06/22/23/24/36/37/38/39/40/41/42/49/52/55/56) · M-48 mechanism-kill→NO-ACTION (04 §4 trial record) + M-48 ink-drift→T15 · M-40 has a facet split: material row→T2, `accessibilityViewIsModal` containment (§6.9)→T20 · remaining 41 ratified→NO-ACTION (04 §4 written reasons).

**RB1–14:** RB1/2/3/4/5/6/7/8/10/11/14→T2 · RB9→NO-ACTION (recorded glass-family delta) · RB12→T20 (craft-delta remainder; ratified tier stays) · RB13→NO-ACTION + Fork(§6.2).

**04 §6 register (never silently resolved):** §6.1→Fork 8 · §6.2→NEW FORK (M-12/M-13 keep vs engineered swap) · §6.3→Sky-note (bulk mechanism lever, inherited B4 flag #2) · §6.4→Fork-9-adjacent note (ui/Sheet) · §6.5→T20 (doc ownership Sky's half framed) · §6.6→T20 (M-57 hex-sync) · §6.7→T2 note (MP5 skip) · §6.8→NEW FORK/note (M-37 locked-row idiom) · §6.9→T20 · §6.10→NEW FORK/note (lightbox blacks) · §6.11→T20 · §6.12→T15.

**Carried to PROTECT update (Part-2 flag):** ThemeContext's TOTAL lightColor default (ThemeContext.tsx:218) is load-bearing for the error-boundary safety net (04 §2b/M-54; S9 B.2) — `useColor` must never become a throwing hook.

## STAGE-3 SKEPTIC VERDICTS — COMPLETE

Workflow `wf_35247f08-206` (20 skeptics + 2 panels, Opus 4.8 max, 0 errors, ~3.0M tokens). **Every proposal survived: 0 KILL, 0 clean-KEEP, 20 FIX** — i.e. all 20 hold with conditions absorbed (the adversarial pass produced refinements — measurement precision, test-lockstep, AA-during-press guards, mis-citation and scope-honesty corrections — not rejections). Each proposal's absorbed conditions are appended to its block above as **"Skeptic verdict: FIX (absorbed)."**; the raw verdicts (attack log + railCheck + fidelityCheck + full fixConditions) are banked at `partials/r2p3-stage3-raw.json`. For **T2 (the migration train)** the FIX routes as noted amendments against `04` — the train BODY is unchanged (3 conditions: WHERE reads 19 files; PROTECT-8 citation corrected to the anonymity set; a MapScreen serialization note).

| T | verdict | # fix | one-line |
|---|---|---|---|
| T1 | FIX | 3 | All cited ids say what the proposal claims. F2-01 (final CRITICAL, ladder-confirmed) — direction is verbatim: … |
| T2 | FIX | 3 | Findings faithfully cited with two nits. RB claims all match 04 §1 verbatim: RB1 two-worlds tab switch, RB2 le… |
| T3 | FIX | 5 | Strong — no material over-claims. Every cited severity is exact against 02_feel_findings.md: F1-02 HIGH, F1-05… |
| T4 | FIX | 4 | Mostly faithful. Confirmed accurate: F1-01 is HIGH, skeptic verdict ADJUSTED (not CRITICAL — "cohesion-and-con… |
| T5 | FIX | 4 | All six cited findings verified in code and match. F6-01 (HIGH, confirmed HIGH in reconcile line 22): FlagDeta… |
| T6 | FIX | 6 | FAITHFUL on the load-bearing claims; two soft imprecisions, no mis-citation. F2-03 is HIGH (feel_findings:47) … |
| T7 | FIX | 9 | F4-03 and F5-03 are cited faithfully; both are final HIGH (verified table lines 52/55) so 'both HIGH' is accur… |
| T8 | FIX | 6 | Four of five cites CONFIRMED at source, byte-exact: F4-02 (TasksScreen.tsx:1509 inline `severity ${flag.severi… |
| T9 | FIX | 3 | Faithful overall. F5-02 (02_feel_findings.md:446, skeptic ledger:54) is CONFIRMED HIGH and code-verified on BO… |
| T10 | FIX | 3 | F2-04 (02:189, HIGH) — accurate and code-matched: guest branch (:821-846) renders no ScreenHeader/HeaderAction… |
| T11 | FIX | 3 | Excellent fidelity — T11 faithfully absorbs BOTH ADJUSTED corrections rather than citing the stale finding bod… |
| T12 | FIX | 6 | Findings are faithfully represented with one qualifier over-claim. F3-01 (HIGH): T12's 'dress the fallback in … |
| T13 | FIX | 4 | F2-02/05/11/12/13 cited faithfully against source: F2-02 direction ('wrap at floor+overflow') matches; F2-05 (… |
| T14 | FIX | 3 | Strong, with two mis-citations named. Cited ids verify: F2-07 and F2-08 are BOTH MEDIUM (02_feel_findings.md:8… |
| T15 | FIX | 3 | Core claims verified TRUE: 3.17/4.04 drift (04 §4:700; r2-trials.txt:18-19; r2-trials-stacks.json:8; my comput… |
| T16 | FIX | 2 | F3-03/F4-05 cited accurately. Writer 1 = open-only, capped 99 (RootNavigator.tsx:220-221, consumed at :335) — … |
| T17 | FIX | 4 | Copy directions match sources cleanly: F4-06 (three k-caveat wordings 'reported'/'submitted'/'reports are show… |
| T18 | FIX | 5 | Cited ids say what the proposal claims — high fidelity. F4-04 (MED, 02_feel_findings.md:331): ungated useEffec… |
| T19 | FIX | 4 | Findings are faithfully cited — every code anchor verified against source. F6-07: theme.ts:131 accentOrange #f… |
| T20 | FIX | 3 | Cited ids verified against source, all faithful. §6.9 (spec:772-774) = 'the one sheet missing accessibilityVie… |

## STAGE-3 PANEL SCORES + FINAL RANKING — COMPLETE

**Panel A** ranked: T1 9.4 > T5 8.6 ≈ T11 8.5 > T13/T2 > … (impact-led). **Panel B** ranked: T1 9.3 > T5 8.8 > T2 8.6 > T11 8.3 > T3 8.1 > T8 8.0 …, and made the slate's one tier demotion — **T6 Signature→Meaningful** ('overreaches a placement bugfix'; the ★ set T1/T2/T3/T5 holds). Both panels put impact ahead of every tie (floor-work T11 outranks signature-tier T3; T4/T7/T11/T14's highest-risk FIXes are composite drag, not axis penalties). FINAL RANKING = median across both panels; effort tiebreak only. **All 20 KEEP-class — 0 KILL.**

### Tier decision (logged): **T6 down-tiered Signature → Meaningful.** Two authorities concur — Panel B's explicit demotion and the T6 skeptic's soft down-tier note (a HIGH+MED+LOW placement/arbitration fix, defensible via "deploys the app's own status voice into space" but not the ownable signature move). Signature-★ set = **T1 · T2 · T3 · T5** (4 ★, ≥3 held).

### FINAL RANKED SLATE (median across both panels; effort tiebreak only)

| Rank | T-id | Title | Effort | Tier | Median | Top-5 |
|---|---|---|---|---|---|---|
| 1 | T1 | The map payoff lands true — clear-map callout + one honest rhythm | M | Signature ★ ★ | 9.35 | ★ DO FIRST |
| 2 | T2 | One Material World — the MP0–MP5 migration train (mounted whole) | L | Signature ★ | 8.8 | ★ DO FIRST |
| 3 | T5 | The severity grammar speaks everywhere severity renders | L | Signature ★ ★ | 8.7 | ★ DO FIRST |
| 4 | T11 | The perception layer — chips speak their state, focus you can see | M | Meaningful | 8.4 | ★ DO FIRST |
| 5 | T8 | One spoken voice for the triage layer | M | Meaningful | 8.1 | ★ DO FIRST |
| 6 | T3 | One press vocabulary — every control answers the hand | L | Signature ★ | 8.1 |  |
| 7 | T9 | Word every wait; never claim a zero | M | Meaningful | 7.85 |  |
| 8 | T13 | The editorial frame holds at every size and inset | S–M | Meaningful | 7.7 |  |
| 9 | T4 | The Report sheet and triage answer the hand | M | Meaningful | 7.6 |  |
| 10 | T7 | Finish the honest arrival — the undetermined voice | M | Meaningful | 7.45 |  |
| 11 | T17 | One voice — the convergence sweep + the honest-completeness promise | S | QuickWin | 7.25 |  |
| 12 | T6 | The status ledge — one podium for the status voice | M | Meaningful | 7.1 |  |
| 13 | T12 | The drawer passage — dressed arrival, symmetric exit | S–M | Meaningful | 6.95 |  |
| 14 | T18 | The tutorial tells the present truth | S | QuickWin | 6.9 |  |
| 15 | T10 | The guest Profile joins the family | M | Meaningful | 6.85 |  |
| 16 | T16 | One badge, one writer, one meaning | S | QuickWin | 6.75 |  |
| 17 | T14 | Every chip rail earns its overflow scent | M | Meaningful | 6.7 |  |
| 18 | T15 | Tab bar light-inactive ink meets AA everywhere | S | QuickWin | 6.6 |  |
| 19 | T20 | Three invisible-hand hygiene branches (containment · dialog tier · paper-matches-pixels) | S | QuickWin | 6.3 |  |
| 20 | T19 | Retire ghost hues; seat the missing marks | S | QuickWin | 5.95 |  |

**TOP-5 "do first":** T1, T2, T5, T11, T8 — the sole CRITICAL (T1), the two signature systems (T2 material train, T5 the completed actor), the WCAG-floor perception layer (T11), and the trust-engine spoken recompose (T8).

## STAGE-5 COLD-READ VERDICT — SHIP (after fixes)

Fresh Opus-4.8/max reviewer, ZERO context, read ONLY the dated report. First pass = **FIX** with an 8-item editorial punch-list (all consistency/precision, zero design re-derivation): (1) §4 ledger heading D20→D18; (2) T3 stale C-ids→T-ids; (3) T2 WHERE 18→19 files; (4) T13 header-inset self-contradiction (24-vs-20) resolved to the cheap-win scope, Profile dropped; (5) T2 PROTECT-8 label corrected (POINTS + anonymity set; honest-arrival stated separately); (6) §1 exec summary compressed to one screen (grade + one clause per persona; full quotes stay in §2); (7) conservation table stated as FACET-level (M-48/M-40 splits); (8) §5 train intro states plainly that MP0–MP5 execution opens `04` alongside. All 8 applied to BOTH the report and this slate, grep-verified; a self-caught double-star/signature-glyph inconsistency in the ranked headers was also normalized. **Second confirmation cold-read (fresh Opus/max, zero context): all 8 RESOLVED, no new breakage → VERDICT: SHIP.**

### FENCE (final)
`git diff --stat` shows ONLY the pre-existing `.claude/launch.json` deletion — captured in the step-0 baseline, explicitly carved out by the prompt as Sky's workspace state (left as found). Zero tracked-file changes introduced. `git status --porcelain` == the step-0 baseline + `design-reviews/r2-audit/` additions only (the whole `design-reviews/` tree was already one untracked porcelain line, unchanged). No commits, no pushes, no branch moved, zero Supabase writes.

### WIN-CONDITION CHECK (all pass)
Traceability: every T carries all 7 fields (20/20 × 7). Tier mix: QuickWins + Meaningful + Larger + **4 Signature ★ (T1·T2·T3·T5, ≥3 held)**; impact-led ranking, effort tiebreak only; TOP-5 marked (T1·T2·T5·T11·T8). Migration train INTACT: slate phase count == `04` §7 phase count == **6 (MP0–MP5)**. Conservation: no orphans (65 F + 57 M + 14 RB + 12 §6-register all routed; F1-09 the sole PARK; facet-splits declared). PROTECT additions numbered **PROTECT-18…27**. R2-D ledger: **D9 + D10 recorded closed**, every open Round-1 gate folded once (R2-D1…D11 + new R2-D12…D18). Forks 1–9 appear as boundaries only; new Forks 10–13 framed never decided. Every on-disk evidence link resolves (the `tools/r2-*-stacks.json` are prospective arbiter siblings each proposal creates on ship — correct for a spec). Exec summary = 25 lines (one screen). Every `S\d`/`B\d` reference is baseline context (P0–P5/S1–S20, BENCH B-items, PROTECT/fork/dependency prose) — none re-opens a closed item.

**STOP — Round 2 is authored and judged.**
