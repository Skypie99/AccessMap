FABLE AUDIT ROUND 2 — ACCESSMAP — PART 2 of 3: THE MATERIAL CONTINUITY AUDIT (READ-ONLY)
(file 2 of 3 — fire each part alone in a FRESH window, in order; siblings: r2_part1_feel.md · r2_part3_synthesis.md · operator note: 00_run_order.md. Authored 2026-07-09 on Fable 5 max effort.)

Purpose: answer Sky's mandate — does the app read as ONE MATERIAL WORLD after intro/onboarding? Census EVERY post-onboarding surface (BENCH-3 unified the modal SHEETS; the SCREENS were never scoped, and the bench close-out itself flags "secondary list-modal tier left opaque — later 'whole overlay' pass": THIS is that pass's design authority). Classify each surface, design the unified material system (glass stage / deliberate-opaque tier / map-world composition), and write the FULL migration spec — surface by surface, arbiter-proven at spec time, packaged as its own phased build train that Opus can execute later, phase by phase, Sky-gated. Deliverables: `03_material_census.md` + `04_material_migration_spec.md` + `tools/r2-material-stacks.json` (+ banked arbiter output), complete enough that Part 3 slates from them alone and a future builder executes from them alone. This part produces reports + specs, never code changes.

★ SUBJECT BLOCK (identical in all three parts; swap this block to reuse the system on future rounds):
- SUBJECT: AccessMap — Sky's flagship, a civil-benefit accessibility product, and her first-impression project for career-critical audiences. Expo SDK 54 / React Native 0.81 / React 19.1 / TypeScript strict / Supabase (auth + Postgres + RLS + Storage) MOBILE app; react-native-maps native, react-leaflet 5 + OSM/CartoDB tiles on web. Repo root `~/AccessMap` — verify by GLASS.md at root + `src/screens/{MapScreen,TasksScreen,ProfileScreen}.tsx`.
- AUDIT BASELINE (Round 2 audits the post-uplift, post-bench tree): Round 1's slate is FULLY CLOSED — S1–S20 shipped via the uplift train P0–P5 (ledger: `design-reviews/fable-audit/uplift-assets/P0..P5-verification-evidence.md`), B1–B11 resolved via the bench tier BENCH 1–4 (ledger: `design-reviews/fable-audit/bench-assets/BENCH-1..4-verification-evidence.md`; authored tip `bench/4-quality` @ `a8549ff`, stacked on main `01f7392`). Audit HEAD exactly as Sky left it — record branch + hash, NEVER switch branches — and verify the tree truly contains the whole baseline by three markers spanning the trains: `PHOTO_MAX_DIMENSION` greps in the photo pipeline (BENCH-4/B8) · `src/components/LiveStatusRegion.tsx` exists (uplift P5/S10) · `variant="bulk"` inside `src/screens/NearbyFlagsModal.tsx` (BENCH-3/B4e). Any marker missing → STOP AND REPORT (the premise — auditing the post-bench baseline — is wrong; Sky decides, you don't).
- THE ROUND-2 THESIS (given, not discovered — every judgment serves it): "DISABLED USERS DESERVE BEAUTIFUL SOFTWARE — one material, one voice, one hand." Round 1 made the app CORRECT (honest, reachable, coherent, safe); Round 2 makes it LOVELY — because utilitarian-bare is what disabled users are always handed, and refusing that is both the mission and the differentiator. One material = a single material world after intro/onboarding (Part 2's question). One voice = the same warm, civically-serious editorial voice in every small copy moment. One hand = every corner feels made by ONE careful hand (craft coherence) — and stays usable WITH one hand (reachability never regresses).
- STAKES + RESTRAINT (both true at once): beauty here NEVER costs legibility, reachability, or honesty. Round 1's enemy — clutter, occlusion, anything that costs legibility — still stands; WCAG 2.2 AA remains the HARD FLOOR. Glass is the STAGE; the severity grammar stays the ACTOR (Round 1's signature thesis, intact). Reduce Transparency users get a FIRST-CLASS experience, not a fallback. THE RESTRAINT CHECK on every finding-with-a-direction and every proposal: does it cost legibility, reachability, or honesty? Then it dies, however pretty.
- THE MISSION (unchanged — hold it in every judgment): AccessMap maps physical-world accessibility barriers (6 categories, severity 1–5, status open/verified/resolved) so people with mobility, vision, or other disabilities can find and trust navigable routes and contribute what they encounter. Real disabled people depend on this. DESIGN.md's own law: "Beauty that excludes people is a defect — especially here."
- SKY'S TASTE ANCHOR (judge against THIS, not generic mobile convention): the Deep Field material system — material depth as hero, used with RESTRAINT and breathing room; EDITORIAL typography; ALWAYS LEGIBLE. Distinctiveness comes FROM the ethos, never by abandoning it. Deviations from Sky's OWN ethos are the most valuable findings.
- OUTPUT HOME: `design-reviews/r2-audit/` inside `~/AccessMap` (these prompts live there; the audit banks everything else there too — `partials/`, `assets/`, `tools/`). The repo's `qa-reports/` convention is for role reports; audits keep their own folder, like Round 1's `fable-audit/`.

★ BASELINE-LEDGER BLOCK (identical in all three parts — what is closed, what is Sky's, what is settled):
- DO NOT RE-FIND (the new baseline): all 20 Round-1 proposals S1–S20 (closed P0–P5) and all 11 bench items B1–B11 (9 built · B6 closed-no-fix · B1 forked to Sky). The close-out ledgers under `design-reviews/fable-audit/uplift-assets/` + `bench-assets/` are the authority — read them before judging anything they touch. Re-finding a closed item without new evidence is a calibration failure. If a closed item's SHIPPED FIX looks wrong at HEAD, that IS a legitimate new finding — cite the ledger AND the regression evidence.
- NEVER RE-LITIGATE (Sky's open either/ors — frame, never prescribe; their UI/read halves may be referenced, their decision halves are Sky's alone): Fork 1 proximity/geo-query architecture · Fork 2 points-economy honesty (migration + CLAUDE.md doc drift) · Fork 3 auth-wall & guest contract · Fork 4 k-anonymity / guest cache-scope · Fork 5 trust-model scope (verifier COUNT + guest flag-as-wrong; S3 shipped the read half only) · Fork 6 product-name collision · Fork 7 `stagePoolB` keep/kill · Fork 8 dark saved-place chips · Fork 9 `ui/Button` adopt-or-remove. Full wording: Round-1 report §5. Also open-for-Sky bench discoveries (flag-adjacent if relevant, never re-derive): the `blocked_path` per-template icon collision (⛔/🚗) · `CATEGORY_ICONS` dead export · `searchClearText` dead style · heat "no zones in view" copy tuning · OnboardingCards' local RM detection.
- DEVICE READS IN HAND (Sky's word, 2026-07-09 — both settled; do not re-open): D10 — the B6 light bulk sheet reads clean as-is (recorded: `bench-assets/BENCH-3-verification-evidence.md` §B6). D9 — the ≥500 on-glass weight reads fine on device (Sky's direct read; it POST-DATES the bench docs, which still say NEEDS-SKY-DEVICE — the paper record is stale, the read stands). Part 3 records the D9 closure in the refreshed device-gate ledger so the record catches up. All OTHER Round-1 device gates (D1–D8, D11) are verify-first: read the ledgers' current state; fold what is still open into Round 2's fresh list — never assume a gate cleared.
- PROTECT (the law Round 2 extends, never regresses): the 17-item merged PROTECT list — Round-1 report §4 / `design-reviews/fable-audit/partials/protect-merged.md`. Load-bearing spot checks: `GlassSurface.tsx` is DO-NOT-EDIT (the freeze lives in the governance docs; the code carries the arbiter contract in its docstring, no literal banner) · the map overlay's `pointerEvents="box-none"` gesture law (`MapScreen.tsx` ~:1421 — comment-enforced, NO unit test guards it) · the anonymity/honesty overlays stay byte-preserved (PROTECT-1/3/7/8 markers in source) · every arbitrated floor/ink is script-decided, never eye-tuned. Part 3 UPDATES the list: everything shipped in P0–P5 + BENCH 1–4 joins the protected baseline.
- Known-fixed, not a finding: the `/ago$/` TasksScreenFlagCard fixture flake (fixed on main `01f7392`, "make meta-line assertion time-stable").

★ SAFETY BLOCK (verbatim in every part — non-negotiable):
- Standing model directive (Sky): this ENTIRE session and ALL sub-agents on Claude Fable 5 at MAX EFFORT; do not downgrade. Thoroughness > tokens. If Fable 5 is unavailable (out of credits, hard errors), STOP AND REPORT the halt inside your deliverable plus a DECISIONS-FOR-SKY note — the fallback model is Sky's call, never yours (precedent: the Dashboard audit halted exactly here and Sky chose; Round 1 finished on Opus 4.8 at Sky's explicit direction). Model provenance is disclosed, never silent — this Round-2 prompt set was authored on Fable 5 max effort (2026-07-09); write every artifact as a durable design authority a different model could execute to.
- STRICTLY READ-ONLY: no code changes, no fixes, no builds (no EAS, no native builds), no deploys. Per Sky's Claude Corp norms: NO database changes, NO external sends, NO autonomous mutations of any kind. Never `git add`/commit/push; never switch branches or move HEAD. `git status` at the end must equal the pre-flight baseline plus ONLY `design-reviews/r2-audit/` additions. (`.claude/launch.json` shows as an uncommitted deletion in the baseline — that is Sky's workspace state; leave it exactly as found and drive the dev server from the shell.)
- NETWORK FENCE: the local web app + map-tile fetches + Supabase ANON READS through the app's own guest path are the wave-era read-only precedent — allowed. NEVER sign in to any account, NEVER submit/verify/resolve/reject a flag, NEVER upload — zero Supabase writes, zero Storage writes. The CONTRIBUTE flow is exercised up to the submit affordance and no further; post-submit states are code-inferred.
- PRIVACY IS SACRED (this app holds disabled users' location data): never expose, export, or compromise real user data or the heatmap's k-anonymity protection; judge the UI's privacy posture without ever undermining it. Real user content visible in captures stays inside `design-reviews/r2-audit/` and is never quoted beyond what the UI itself shows.
- No copy rewrites shipped: Round 2 PROPOSES voice (short before/after sketches inside findings + the copy appendix); it never edits a string.

★ ENGINE-TRUTH BLOCK (shared in every part — how truth is tagged and where the web can't see; only the LUCIDE and MATERIAL bullets may end with a one-sentence part-scoped scope note):
- HONEST EVIDENCE TAGS on every claim — Round 1's six: `web-approximated` (expo web in Chromium; RN-web ≠ iOS/Android — this is the tag for what the Round-2 brief calls "web-verified") · `code-inferred` · `test-inferred` (a jest guard proves it) · `arbiter-measured` (contrast-check.mjs output) · `lab-mockup` (Material Lab HTML — design intent, not app truth) · `NEEDS-SKY-DEVICE`. State the engine truth wherever it matters: true blur feel, scroll smoothness, VoiceOver/TalkBack, haptics, real Dynamic Type, and Reduce Transparency are device-only.
- ★ THE LUCIDE DEV-PREVIEW BOUNDARY (pre-existing, NOT a finding — first recorded in `design-reviews/fable-audit/uplift-assets/P1-verification-evidence.md`): the expo-web DEV server (Metro lazy=true) crashes the Map and Tasks screens — and the lazy heavy-lucide modals reached from them (ReportFlagModal, FlagDetailModal, NearbyFlagsModal) — into the ErrorBoundary with `createLucideIcon.default is not a function` / "Requiring unknown module". It reproduces on the pristine base; it is a dev-bundler limitation, not an app bug. CONSEQUENCE: Home, Profile, Settings, the drawer and its sub-screens render live; Map/Tasks-family live evidence is code-inferred / test-inferred / NEEDS-SKY-DEVICE — say which, per finding. PERMITTED WORKAROUND (try once, cheaply): a static export (`npx expo export --platform web --output-dir <scratchpad>/web-export` — NEVER into `~/AccessMap/dist/`) bundles without dev-lazy and MAY render Map/Tasks; if it does, the boundary lifts (record that it did, re-tag accordingly); if not, record the attempt and move on. For THIS part the boundary is survivable by design: a material census is primarily a code-and-arbiter exercise — the classification of every surface is provable from source; live renders are corroboration, not the spine.
- DEVICE SIZES, not breakpoints: 375×667 · 390×844 · 430×932 · 834×1194, DPR 2, both themes (the wave-era 375×812 / 320×568 remain valid for apples-to-apples with Round-1 shots). Landscape: app.json orientation truth first.
- THEMES: the app's own toggle (Settings / useThemeMode) or colorScheme emulation — record which. Tiles ≠ theme (GLASS.md §12): web CartoDB dark_all is always dark; iOS Apple tiles follow the OS — never assume they match.
- DYNAMIC TYPE: RN-web ignores browser font settings — zoom/font-stress is a layout proxy (`web-approximated`); code-read allowFontScaling / maxFontSizeMultiplier / the DT guards (`test-inferred`); true ×2 is NEEDS-SKY-DEVICE.
- NATIVE A11Y, not web ARIA: accessibilityLabel/Role/State, accessibilityViewIsModal, grouping, focus order — audit via source + the RN-web-emitted ARIA tree (dump snapshots as text evidence); VoiceOver/TalkBack truth stays NEEDS-SKY-DEVICE.
- MATERIAL: per surface — right GLASS.md tier + tokens; blur budget at the worst SIMULTANEOUS state (+ the tab-bar pane, invisible to `__getLiveBlurPaneCount`); C-lite parity (`src/lib/glassMode.ts` — the Tasks-header long-press or the store/localStorage flip); Reduce Transparency designed states (iOS-only API — code/test-inferred, never renderable on web); reduced-motion parity (emulate `prefers-reduced-motion`); contrast over each surface's REAL worst-case backdrops — the arbiter decides, not the eye.
- MOTOR + COGNITIVE ACCESS are core findings, never nitpicks: 44pt HARD floor, gesture alternatives, one-handed reach per size class, cognitive load.
- CAPTURE CONSTRAINT (the auth carve): the guest path opens Map/Tasks browsing; signed-in states are UNREACHABLE by law — capture what the guest web allows, CODE-READ the rest, tag honestly, never fake a state.
— end of the shared blocks —

═══ PRE-FLIGHT (independent of Part 1 — reuse it where banked, never require it) ═══
1. Repo + baseline: run the SUBJECT-block checks (root, three markers, record branch + HEAD). Any miss → STOP AND REPORT.
2. Fence baseline: if Part 1's `01_feel_orientation.md` §0 exists and its HEAD matches yours, adopt it (log the reuse); otherwise bank your own §0 block at the top of `03_material_census.md` (HEAD · branch · full `git status --porcelain` · date · model · serve mode).
3. Ground in the LAW (read in full, in this order): `~/AccessMap/GLASS.md` — especially §2 tokens/tiers, §3 blur budget, §6 Reduce-Transparency designed states, §7 do/don'ts, §8 application map (NOTE: §8's Home line is ASPIRATIONAL — shipped Home is not on the stage; treat the drift as census input, not doc-truth), §9 rollout recipe (the migration phases will follow its shape), §12 live-backdrop addendum · the Material Lab's law files `~/AccessMap-material-lab/2026-07-02/briefs/common-law.md` + `briefs/gates.md` ("the contrast script decides, not the eye") · `src/theme.ts` (stage/glass/ink tokens ~:198–:254, `export const glass` ~:448, severity ~:533) + `src/theme/ThemeContext.tsx` (dark mirror) + `src/components/ui/GlassSurface.tsx` docstring (the byte-stability + arbiter contract) + `src/lib/glassMode.ts` · `bench-assets/BENCH-3-verification-evidence.md` (the 9-sheet unification + the "secondary list-modal tier left opaque" flag — this part's mandate seed) · `uplift-assets/P2-verification-evidence.md` §arbiter (the sibling-file "laundering" precedent) · the shipped + audit stack declarations (paths in THE ARBITER LAW below).
4. Harness (optional for this part): captures corroborate; the census spine is code. If capturing, adapt Round 1's tools into `r2-audit/tools/` (originals untouched; Playwright never installed into `~/AccessMap`); serve `npm run web` :8081; the lucide boundary applies.
5. `.env` rule: if missing, NEVER create or fill it — code-read instead and record in the honesty ledger.

RESUME RULE: if `03_material_census.md` / `04_material_migration_spec.md` / `tools/r2-material-stacks.json` already exist partially — verify banked sections (census rows resolve to real files at the same lines ±drift; arbiter outputs match the JSON), keep them, and continue from the first gap (census → system → spec → arbiter → train). Never re-census a verified surface.

PLAN-MODE NOTE: Sky runs AccessMap work in plan mode. If you are in plan mode, the plan = Stages 1–6 below with this pre-flight verbatim as step 0; after approval, execute exactly as approved — STOP conditions still bind mid-execution.

═══ THE ARBITER LAW (mandatory for every material claim and every proposed stack) ═══
- THE TOOL: `node ~/AccessMap-material-lab/2026-07-02/shared/contrast-check.mjs <stacks.json>` — single positional arg, no flags. Exit 0 = ALL PASS · exit 1 = at least one pair below floor (the per-mode markdown table is the evidence — quote it verbatim) · exit 2 = usage/parse error. Light/dark are `modes` keys inside the JSON; Reduce-Transparency states are declared as their own surfaces (the RT code path's real paint), never a CLI switch.
- THE SCHEMA (per mode): `surfaces.<name> = { stack: [translucent layers, painted base→top], base: [worst-case OPAQUE backdrops — the script rejects alpha<1] }` · `pairs = [{ text, surface, label, min (default 4.5) }]`. Every pair is composited over EVERY base of its surface; the worst ratio gates. Working example: `~/AccessMap-material-lab/2026-07-02/shared/stacks-example.json`.
- WORST-CASE BACKDROP DISCIPLINE (GLASS.md §12): deterministic surfaces (rows/banner over the stage) → the stage's darkest reachable stops; chaotic surfaces (chrome/bulk over photos or live tiles) → `#000` + `#FFF` + the documented domain saturants (the 5 heat-ramp colors, severity fills); the Window rule (§12.3) decides when extremes suffice vs when every base must be probed; tiles ≠ theme — regime-decompose (darkRegime/lightRegime) exactly like the shipped map set. Copy the discipline from `qa-reports/assets/2026-07-04_glass_map/map-stacks.json` `_doc`.
- THE SIBLING RULE (the P2 "laundering" precedent): `design-reviews/fable-audit/tools/audit-stacks.json` is the IMMUTABLE Round-1 findings record — never edit it (or ANY prior shipped/audit set: `qa-reports/assets/2026-07-03_tasks_glass/shipped-stacks.json` · `…/2026-07-03_glass_w1/wave1-stacks.json` · `…/2026-07-03_glass_w2/wave2-stacks.json` · `…/2026-07-04_glass_map/map-stacks.json` · `fable-audit/tools/p2-material-stacks.json` · `fable-audit/tools/bench3-material-stacks.json`) to make anything pass. Every Round-2 declaration goes in the NEW sibling `design-reviews/r2-audit/tools/r2-material-stacks.json`; bank the run output (exit code + command + date header, R1 convention) to `design-reviews/r2-audit/assets/arbiter/`.
- DECLARED == SHIPPED: any stack cited as CURRENT truth gets its layer + ink values cross-checked against HEAD's `src/theme.ts` + `src/theme/ThemeContext.tsx`; drift is itself a finding.
- NO HOLLOW CONTRAST CLAIMS: a proposed glass surface without a named worst-case backdrop set and an arbiter-run declaration does not enter the spec, the train, or the slate. "The arbiter decides, not the eye" (GLASS.md §7.1).

═══ STAGE 1 — THE CENSUS (03_material_census.md — every post-onboarding surface, no exceptions) ═══
Enumerate EVERY surface a user can see after intro/onboarding: screens AND sheets AND Settings AND Profile AND Tasks AND drawers/menus AND transient chrome (tab bar, nav headers, banners, toasts, status regions, lightbox). Walk `App.tsx` → `src/navigation/RootNavigator.tsx` (3 visible tabs Home/Tasks/Profile + hidden routes FullMap/Settings/Admin + `SharedModalsHost` + `DrawerHost`) and every Modal mount in `src/components/` + `src/screens/`. BOUNDARY: `FirstLaunchGate`/`Gate` — OnboardingCards, OnboardingModal, SignInScreen are OUT of scope (name them in a one-line boundary appendix so the fence is explicit).
CENSUS ROW (one per surface, stable IDs `M-01…M-nn`): id · surface · file (+line of the material mount, verified at HEAD) · entry paths · current material (exact: which GlassSurface variant / legacy intensity / opaque token / map-world) · CLASSIFICATION: liquid-glass(variant) | opaque | mixed | map-world | media-world · worst-case backdrop family (stage-deterministic / photo-chaotic / tile-chaotic) · PROTECT touches · notes.
★ THE SEED (authored 2026-07-09 from a verified read at `a8549ff` — VERIFY EVERY ROW at your HEAD; lines drift, the bench moved things; a seed row you can't confirm is a discrepancy to investigate, not to copy):
- Deep Field screens (ScreenStage + variant panes): TasksScreen · ProfileScreen (~20 row panes) · SettingsScreen · ResourcesScreen · HowToHelpScreen.
- Map-world: MapScreen — live tiles as stage; chrome/action-bar `variant="row" forceEngineered`; ONE true-blur pane (the filter panel threading `forceEngineered={glassLite}`); legacy ALWAYS-LIGHT stragglers: locate banner · HeatmapLegend · saved-place chips (Fork 8 owns their dark variant — census them, don't decide them).
- MIXED (the headline drift): HomeScreen — opaque `surfaceMuted` editorial + ONE legacy `GlassSurface intensity={20}` search pill over the map peek; GLASS.md §8 claims the stage, shipped truth says otherwise.
- Opaque screens: LeaderboardScreen · AdminScreen · NotificationPreferencesScreen · the Settings/Admin nav header (`color.headerBg` light editorial while sibling screens wear Deep Field chrome).
- Bulk-glass sheets (11): ChangelogModal (the ONLY consumer of `ui/Sheet`'s glass path) · HelpModal · MyFeedbackModal · LegendModal · AddressSearchModal · SavedPlacesModal · FilterPresetsModal · ReportFlagModal (PROTECT-3) · NearbyFlagsModal (PROTECT-1) · FeedbackModal · AboutScreen. All hand-roll `<Modal>`+`GlassSurface variant="bulk"` except Changelog — the scaffold split is census-relevant architecture.
- The OPAQUE MODAL TIER (the bench-flagged "secondary list-modal tier" — this part's core subject): FlagDetailModal (82 KB, entered from Map + Tasks + Profile — the biggest holdout) · AchievementsModal · ActivityFeedModal · MyReportsModal · MyWatchedModal · StatusHistoryModal · NotificationPrefsModal.
- Deliberate-opaque CANDIDATES (ratify or reclassify, never assume): HamburgerDrawer (explicit code comment ~:167 "NO GlassSurface/BlurView by design") · PhotoLightboxModal (black immersive media-world).
- Glass chrome: the bottom tab bar (`TabBarGlass`, RootNavigator ~:133 — frosted, RT-aware).
- Transient: FlashBanner · LiveStatusRegion · UpdateBanner · offline banners.
COMPLETENESS CRITIC: one fresh context-free agent, given only the census table + the navigation entry points, hunts missed surfaces (grep `<Modal`, `Portal`, `position:'absolute'` overlays, navigator routes, banner mounts). The census is complete only when the critic finds nothing new — bank its PASS in the census file.

═══ STAGE 2 — THE UNIFIED MATERIAL SYSTEM (the design — one world, deliberately tiered) ═══
- THE READING TEST first: walk the app as a user (live where the web renders; code-walk under the lucide boundary) and name every moment the material world BREAKS — glass sheet over opaque screen, opaque modal over glass screen, editorial header over Deep Field body, the Home↔Tasks tab switch. Each break: `M-`ref + a one-line "what it feels like" (this is the evidence the thesis stands on).
- THEN THE SYSTEM — assign every census row a TARGET tier, and justify the tier not the surface: (a) THE GLASS WORLD — which surfaces should wear which variant (row/chrome/banner/bulk) and why, per GLASS.md §2/§8 logic extended to the never-scoped surfaces; (b) THE DELIBERATE-OPAQUE TIER — a deliberate tier is design, an accidental one is drift: ratify-or-reclassify HamburgerDrawer (the comment says by-design — test the rationale, state it, keep or challenge it) and PhotoLightbox (media-world: content IS the surface); any OTHER surface staying opaque needs its rationale written or it's drift by definition; (c) MAP-WORLD COMPOSITION — the tiles are the stage's live twin; how the glass chrome + the legacy always-light trio compose with the Deep Field world around them (Fork 8's decision half stays Sky's); (d) HOME, RESOLVED AS A DESIGN DECISION — GLASS.md §8 says stage, shipped says opaque editorial + one legacy pill: decide what Home SHOULD be (stage-adopt / deliberately-editorial / hybrid), argue it from the thesis and the reading test, and flag the GLASS.md §8 doc-drift either way; (e) THE SCAFFOLD QUESTION — one bulk recipe, two architectures (Sheet's glass path vs 10 hand-rolls): unify-or-ratify, as spec work not code.
- EVERY "should" carries: the restraint check · the blur-budget consequence (the ≤12 live-pane budget, `glass.maxLivePanes`; intensities are fixed 12/24 — expo-blur law; Android always engineered; chips never blur; count the worst SIMULTANEOUS state per screen + the tab-bar pane; the Map's engineered-cut precedent shows restraint CAN mean fewer live panes) · the RT + C-lite designed states (GLASS.md §6 — "never a low-contrast smear"; R7's first-class experience is a rail, not a hope).

═══ STAGE 3 — THE MIGRATION SPEC (04_material_migration_spec.md — surface by surface, executable later without you) ═══
One spec block per census row whose TARGET ≠ current (and a one-line RATIFIED entry for every row staying put, so conservation is provable). Each block:
1. `M-`id + surface + file(s) · target variant/tier · why (reading-test refs).
2. REAL WORST-CASE BACKDROPS, NAMED (§12 discipline): the exact base set this surface composites over (stage stops / `#000`+`#FFF`+saturants / regime-decomposed tiles / the photo case for media-adjacent surfaces) — these are the arbiter inputs, not prose.
3. ON-GLASS INKS → ARBITRATED TOKENS: map every text/icon color the surface will carry to an EXISTING arbitrated token (`inkGlassMuted`/`inkOnStage`/`inkSelect`/`ctaFill`/`glassChipInk`/severity `textOnColor`…). A NEW token is allowed ONLY with its own arbitration entry + an explicit SKY-FLAG line (new tokens touch `theme.ts`, which the executor may edit ONLY for Sky-flagged, arbiter-passed additions — `GlassSurface.tsx` stays DO-NOT-EDIT absolutely; consumers thread the primitive, never fork it).
4. REDUCE-TRANSPARENCY BEHAVIOR: the designed opaque state (which fills/hairlines per GLASS §6), stated per surface — R7 gets design, not fallback.
5. C-LITE BEHAVIOR: which panes drop to engineered `*Lite` gradients, which keep blur (chrome+bulk keep blur under lite — current law; state any deviation).
6. BLUR-BUDGET DELTA: worst simultaneous live-pane count before → after, per screen the surface appears on; the budget never exceeds 12 and SHOULD fall or hold, citing the Map precedent.
7. PROTECT CONSTRAINTS: name every PROTECT item the surface touches and write the preservation as a testable statement (e.g. "FlagDetailModal migration preserves PROTECT-1's SR row labels byte-identical; the box-none law untouched; StatusHistory's ledger content unrestyled in meaning").
8. EFFORT S/M/L · RISK + the rollback anchor (the branch-tip hash convention — each phase is independently revertable).
Also in the spec file: the DEPENDENCY GRAPH between blocks (shared files, shared backdrop families, scaffold-first ordering) and the OPEN-QUESTION register (anything that is genuinely Sky's — new tokens, Home's direction if you rate it a coin-flip, Fork-adjacent edges — framed as either/ors, never decided).

═══ STAGE 4 — ARBITER AT SPEC TIME (no hollow claims — run it NOW, read-only) ═══
Build `tools/r2-material-stacks.json` covering EVERY proposed stack × ink pairing in the spec (and any CURRENT-truth stack the census disputes — declared==shipped). Run the arbiter; bank output to `assets/arbiter/` with the R1-style header (exit code · command · date). Iterate the SPEC (floors within existing token values, ink choices, backdrop honesty) until the proposed world is exit 0 — or a surface's glass ambition DIES/PARKS with the failing table quoted (an honest kill is a result, not a failure). The sibling rule is absolute; the immutable Round-1 records keep their FAILs.

═══ STAGE 5 — THE PHASED SUB-TRAIN (the migration as its own build train — sized for Opus, gated for Sky) ═══
Package the spec as an execution train Part 3 will mount whole into the slate. Phases `MP0…MPn` (suggested spine — derive the real one from YOUR dependency graph): MP0 = cheapest coherence wins + any scaffold groundwork · then group by shared files + backdrop family + risk, hardest/riskiest LAST. Each phase declares:
- SCOPE: the `M-`ids it migrates (a phase is one reviewable idea, not a grab bag).
- BRANCH: `r2/mp{n}-<slug>`, cut from the prior phase's tip (linear train, Round-1 uplift convention).
- GATES (all hard, per phase): typecheck 0 · jest green (count recorded) · lint clean · its OWN arbiter sibling declaration exit 0 (`tools/r2-mp{n}-stacks.json` if the phase's stacks differ from the spec set) · the PROTECT preservation statements from Stage 3 re-verified · RT + C-lite states implemented as specced, not deferred.
- SIZE: executable in ONE Opus window (state the rationale — file count, test surface); if it can't be, split it.
- STOP-ON-BRANCH: every phase ends built + green + STOPPED — not merged, not pushed, not built for device. Sky merges; Sky owns D-gates. Model note: execution may run on Opus 4.8 at Sky's direction — the spec + train must be complete enough that Opus executes without re-deriving any design decision (that completeness is THIS part's win condition).
- The train NEVER executes in this window. Round 2 is read-only; the train is the deliverable.

═══ STAGE 6 — ADVERSARIAL VERIFICATION ═══
One skeptic agent per Stage-2 tier call (each SHOULD-glass and each stays-opaque ratification) and per train phase, fresh context, charged to REFUTE: rail violations (budget math wrong? RT state hand-waved? a PROTECT statement untestable?), RN/Expo feasibility (expo-blur 12/24 only; engineered-tint law for sub-row surfaces; the virtualization law — glass never defeats list virtualization; no CSS-only tricks), census fidelity (does the seed row match HEAD?), and hidden access regressions. Verdicts CONFIRMED / ADJUSTED / REFUTED applied to the census + spec BEFORE the win check; log all verdicts in the spec's process log.

═══ VERIFICATION + WIN CONDITION ═══
- Census: every row verified at HEAD with file:line; completeness-critic PASS banked; boundary appendix present; the reading test's break list references only census ids.
- System + spec: every census row has a target (migrate or RATIFIED-stays) — conservation provable; every migrating block carries all 8 fields; every stays-opaque carries a written rationale; the open-question register frames, never decides.
- Arbiter: `tools/r2-material-stacks.json` exists, covers every proposed pairing, and its banked run is exit 0 (or the killed/parked surfaces quote their failing tables); no prior stacks file modified (diff-check them).
- Train: phases cover every migrating `M-`id exactly once; gates + sizes + branches declared; dependency graph acyclic and stated.
- FENCE: `git status --porcelain` == the §0 baseline + `design-reviews/r2-audit/` additions only; `git diff --stat` empty.
- THE HANDOFF TEST (double): Part 3 can slate from `03` + `04` alone; a future Opus builder can execute MP-phase-by-MP-phase from `04` + the stacks JSON alone, with zero design re-derivation.
WIN: Sky's question answered with evidence — where the one-material world holds, where it breaks, what it should be — every surface censused and classified, the deliberate-opaque tier ratified as DESIGN with reasons, every proposed pane arbiter-proven over its real worst-case backdrops at spec time, and the whole migration packaged as a Sky-gated, Opus-executable, phase-gated train. Close by printing the absolute paths of `03_material_census.md` + `04_material_migration_spec.md` + `tools/r2-material-stacks.json` + the arbiter output, the counts (surfaces censused · migrating · ratified-opaque · killed/parked · phases), and this line: **STOP — Part 2 ends here. Do NOT begin Part 3 (r2_part3_synthesis.md runs in its own fresh window), and NEVER execute the train.**
