# F6 — SIGNATURE DEEPENING (the thesis lens — weight it highest, with F1)

Round 1 named the signature: **the severity grammar** — the numbered amber→red disc + plain word
+ one-line stake (theme.ts:533 + SeverityBadge + severityColor; ramp #F7C948→#D92D20; labels
Minor→Severe; "Impassable. Needs a detour."). The uplift already made the app wear it more
(S1: callout/Nearby/Home meta + Legend Status block; S2: legible digits; S14: hairline union).
Your job: where ELSE can the signature live WITHOUT decaying into decoration — and where does it
still fail to appear at a decision surface?

**The HARD RULE (a definition, not a preference):** the grammar means REAL-WORLD SEVERITY. Any
placement that dilutes that meaning — ornamental discs, severity-colored chrome, ramp colors used
as mood — FAILS the restraint check by definition. Judge candidates, never presume:
- app icon / splash posture (assets/ + app.json — code-read; does the app's own front door wear
  the Wayfinder mark + grammar, or a generic look?)
- onboarding's first lesson (slide set — does the grammar teach itself early, or does a new user
  meet severity cold on the form?)
- empty/zero states (can a zero state speak the grammar honestly — "no severity-5 barriers here"
  — without decorating?)
- Profile's stats (signed-out + code-read signed-in: points/counts — could contributions speak in
  grammar terms without gamifying severity?)
- Leaderboard / Achievements (auth-gated, code-read — is Civic Gold kept clean of the ramp?)
- notification/banner moments (LiveStatusRegion tones, FlashBanner — should outcome beats carry
  category/severity, or stay neutral?)
- the Wayfinder mark's cameos (B3 wore it on 3 intro surfaces — where else does the mark belong;
  where would it be noise?)

Also judge the INVERSE — grammar gaps at decision surfaces: anywhere severity is spoken without
number+word+stake, or status without its defined meaning, at a moment a user DECIDES (route
choice, triage, trust). And judge the grammar's own internal craft at HEAD: is the disc drawn
identically everywhere (size, ring, digit weight — F2 grids alignment; you judge IDENTITY)? Is
the ink fork (dark 1–4 / white 5) reading as ONE deliberate system or as inconsistency (the P2
ledger flags this exact question)?

What would make AccessMap visually unmistakable at a glance — grounded in the EXISTING signature
(grammar + Wayfinder mark + Deep Field restraint), never a new gimmick? Rank your candidates:
each carries its restraint verdict inline; kill freely — a short list of survivors beats a long
list of maybes. Part 3 walks your ladder into the slate.

Your asset groups: ALL of `base/`, `voice/` crops, `states/`; the repo (LogoMark, CategoryIcon,
TierIcon, app.json, assets/images). Personas' unprimed "what does this app look like" reactions
are your ground truth for distinctiveness.
