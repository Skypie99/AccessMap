FABLE AUDIT ROUND 2 — ACCESSMAP — PART 3 of 3: SYNTHESIS — THE ROUND-2 SLATE + FINAL REPORT (READ-ONLY)
(file 3 of 3 — fire each part alone in a FRESH window, in order; siblings: r2_part1_feel.md · r2_part2_material.md · operator note: 00_run_order.md. Authored 2026-07-09 on Fable 5 max effort.)

Purpose: transform Parts 1–2's verified findings into ONE prioritized Round-2 slate — with the material-continuity migration mounted whole as its own phased sub-train — and assemble the single dated report Sky opens, in the Round-1 report's format (it worked). The report carries the thesis through, refreshes the PROTECT law and the device-gate ledger (recording D9's closure), holds every fork boundary, and stands as a durable design authority a different model can execute to. This is the deliverable the whole round exists for. NEVER invent findings here — proposals trace to Part-1/Part-2 ids or they don't exist.

★ SUBJECT BLOCK (identical in all three parts; swap this block to reuse the system on future rounds):
- SUBJECT: AccessMap — Sky's flagship, a civil-benefit accessibility product, and her first-impression project for career-critical audiences. Expo SDK 54 / React Native 0.81 / React 19.1 / TypeScript strict / Supabase (auth + Postgres + RLS + Storage) MOBILE app; react-native-maps native, react-leaflet 5 + OSM/CartoDB tiles on web. Repo root `~/AccessMap` — verify by GLASS.md at root + `src/screens/{MapScreen,TasksScreen,ProfileScreen}.tsx`.
- AUDIT BASELINE (Round 2 audits the post-uplift, post-bench tree): Round 1's slate is FULLY CLOSED — S1–S20 shipped via the uplift train P0–P5 (ledger: `design-reviews/fable-audit/uplift-assets/P0..P5-verification-evidence.md`), B1–B11 resolved via the bench tier BENCH 1–4 (ledger: `design-reviews/fable-audit/bench-assets/BENCH-1..4-verification-evidence.md`; authored tip `bench/4-quality` @ `a8549ff`, stacked on main `01f7392`). Audit HEAD exactly as Sky left it — record branch + hash, NEVER switch branches — and verify the tree truly contains the whole baseline by three markers spanning the trains: `PHOTO_MAX_DIMENSION` greps in the photo pipeline (BENCH-4/B8) · `src/components/LiveStatusRegion.tsx` exists (uplift P5/S10) · `variant="bulk"` inside `src/screens/NearbyFlagsModal.tsx` (BENCH-3/B4e). Any marker missing → STOP AND REPORT (the premise — auditing the post-bench baseline — is wrong; Sky decides, you don't).
- THE ROUND-2 THESIS (given, not discovered — every judgment serves it): "DISABLED USERS DESERVE BEAUTIFUL SOFTWARE — one material, one voice, one hand." Round 1 made the app CORRECT (honest, reachable, coherent, safe); Round 2 makes it LOVELY — because utilitarian-bare is what disabled users are always handed, and refusing that is both the mission and the differentiator. One material = a single material world after intro/onboarding (Part 2's question). One voice = the same warm, civically-serious editorial voice in every small copy moment. One hand = every corner feels made by ONE careful hand (craft coherence) — and stays usable WITH one hand (reachability never regresses).
- STAKES + RESTRAINT (both true at once): beauty here NEVER costs legibility, reachability, or honesty. Round 1's enemy — clutter, occlusion, anything that costs legibility — still stands; WCAG 2.2 AA remains the HARD FLOOR. Glass is the STAGE; the severity grammar stays the ACTOR (Round 1's signature thesis, intact). Reduce Transparency users get a FIRST-CLASS experience, not a fallback. THE RESTRAINT CHECK on every finding-with-a-direction and every proposal: does it cost legibility, reachability, or honesty? Then it dies, however pretty.
- THE MISSION (unchanged — hold it in every judgment): AccessMap maps physical-world accessibility barriers (6 categories, severity 1–5, status open/verified/resolved) so people with mobility, vision, or other disabilities can find and trust navigable routes and contribute what they encounter. Real disabled people depend on this. DESIGN.md's own law: "Beauty that excludes people is a defect — especially here."
- SKY'S TASTE ANCHOR (judge against THIS, not generic mobile convention): the Deep Field material system — material depth as hero, used with RESTRAINT and breathing room; EDITORIAL typography; ALWAYS LEGIBLE. Distinctiveness comes FROM the ethos, never by abandoning it. Deviations from Sky's OWN ethos are the most valuable findings.
- OUTPUT HOME: `design-reviews/r2-audit/` inside `~/AccessMap` (these prompts live there; the audit banks everything else there too — `partials/`, `assets/`, `tools/`). The repo's `qa-reports/` convention is for role reports; audits keep their own folder, like Round 1's `fable-audit/`.

★ BASELINE-LEDGER BLOCK (identical in all three parts — what is closed, what is Sky's, what is settled):
- DO NOT RE-FIND (the new baseline): all 20 Round-1 proposals S1–S20 (closed P0–P5) and all 11 bench items B1–B11 (9 built · B6 closed-no-fix · B1 forked to Sky). The close-out ledgers under `design-reviews/fable-audit/uplift-assets/` + `bench-assets/` are the authority — read them before judging anything they touch. Re-finding a closed item without new evidence is a calibration failure. If a closed item's SHIPPED FIX looks wrong at HEAD, that IS a legitimate new finding — cite the ledger AND the regression evidence.
- NEVER RE-LITIGATE (Sky's open either/ors — frame, never prescribe; their UI/read halves may be referenced, their decision halves are Sky's alone): Fork 1 proximity/geo-query architecture · Fork 2 points-economy honesty (migration + CLAUDE.md doc drift) · Fork 3 auth-wall & guest contract · Fork 4 k-anonymity / guest cache-scope · Fork 5 trust-model scope (verifier COUNT + guest flag-as-wrong; S3 shipped the read half only) · Fork 6 product-name collision · Fork 7 `stagePoolB` keep/kill · Fork 8 dark saved-place chips · Fork 9 `ui/Button` adopt-or-remove. Full wording: Round-1 report §5. Also open-for-Sky bench discoveries (flag-adjacent if relevant, never re-derive): the `blocked_path` per-template icon collision (⛔/🚗) · `CATEGORY_ICONS` dead export · `searchClearText` dead style · heat "no zones in view" copy tuning · OnboardingCards' local RM detection.
- DEVICE READS IN HAND (Sky's word, 2026-07-09 — both settled; do not re-open): D10 — the B6 light bulk sheet reads clean as-is (recorded: `bench-assets/BENCH-3-verification-evidence.md` §B6). D9 — the ≥500 on-glass weight reads fine on device (Sky's direct read; it POST-DATES the bench docs, which still say NEEDS-SKY-DEVICE — the paper record is stale, the read stands). Part 3 records the D9 closure in the refreshed device-gate ledger so the record catches up. All OTHER Round-1 device gates (D1–D8, D11) are verify-first: read the ledgers' current state; fold what is still open into Round 2's fresh list — never assume a gate cleared.
- PROTECT (the law Round 2 extends, never regresses): the 17-item merged PROTECT list — Round-1 report §4 / `design-reviews/fable-audit/partials/protect-merged.md`. Load-bearing spot checks: `GlassSurface.tsx` is DO-NOT-EDIT (the freeze lives in the governance docs; the code carries the arbiter contract in its docstring, no literal banner) · the map overlay's `pointerEvents="box-none"` gesture law (`MapScreen.tsx` ~:1421 — comment-enforced, NO unit test guards it) · the anonymity/honesty overlays stay byte-preserved (PROTECT-1/3/7/8 markers in source) · every arbitrated floor/ink is script-decided, never eye-tuned. Part 3 UPDATES the list: everything shipped in P0–P5 + BENCH 1–4 joins the protected baseline.
- Known-fixed, not a finding: the `/ago$/` TasksScreenFlagCard fixture flake (fixed on main `01f7392`, "make meta-line assertion time-stable").

★ SAFETY BLOCK (verbatim in every part — non-negotiable):
- Standing model directive (Sky): this ENTIRE session and ALL sub-agents on Claude Fable 5 at MAX EFFORT; do not downgrade. Thoroughness > tokens. If Fable 5 is unavailable (out of credits, hard errors), STOP AND REPORT the halt inside your deliverable plus a DECISIONS-FOR-SKY note — the fallback model is Sky's call, never yours (precedent: the Dashboard audit halted exactly here and Sky chose; Round 1 finished on Opus 4.8 at Sky's explicit direction). Model provenance is disclosed, never silent — this Round-2 prompt set was authored on Fable 5 max effort (2026-07-09); write every artifact as a durable design authority a different model could execute to.
- STRICTLY READ-ONLY: no code changes, no fixes, no builds (no EAS, no native builds), no deploys. Per Sky's Claude Corp norms: NO database changes, NO external sends, NO autonomous mutations of any kind. Never `git add`/commit/push; never switch branches or move HEAD. `git status` at the end must equal the pre-flight baseline plus ONLY `design-reviews/r2-audit/` additions. (`.claude/launch.json` shows as an uncommitted deletion in the baseline — that is Sky's workspace state; leave it exactly as found and drive the dev server from the shell.)
- NETWORK FENCE: the local web app + map-tile fetches + Supabase ANON READS through the app's own guest path are the wave-era read-only precedent — allowed. NEVER sign in to any account, NEVER submit/verify/resolve/reject a flag, NEVER upload — zero Supabase writes, zero Storage writes. The CONTRIBUTE flow is exercised up to the submit affordance and no further; post-submit states are code-inferred.
- PRIVACY IS SACRED (this app holds disabled users' location data): never expose, export, or compromise real user data or the heatmap's k-anonymity protection; judge the UI's privacy posture without ever undermining it. Real user content visible in captures stays inside `design-reviews/r2-audit/` and is never quoted beyond what the UI itself shows.
- No copy rewrites shipped: Round 2 PROPOSES voice (short before/after sketches inside findings + the copy appendix); it never edits a string.

★ ENGINE-TRUTH BLOCK (shared in every part — how truth is tagged and where the web can't see; only the LUCIDE and MATERIAL bullets may end with a one-sentence part-scoped scope note):
- HONEST EVIDENCE TAGS on every claim — Round 1's six: `web-approximated` (expo web in Chromium; RN-web ≠ iOS/Android — this is the tag for what the Round-2 brief calls "web-verified") · `code-inferred` · `test-inferred` (a jest guard proves it) · `arbiter-measured` (contrast-check.mjs output) · `lab-mockup` (Material Lab HTML — design intent, not app truth) · `NEEDS-SKY-DEVICE`. State the engine truth wherever it matters: true blur feel, scroll smoothness, VoiceOver/TalkBack, haptics, real Dynamic Type, and Reduce Transparency are device-only.
- ★ THE LUCIDE DEV-PREVIEW BOUNDARY (pre-existing, NOT a finding — first recorded in `design-reviews/fable-audit/uplift-assets/P1-verification-evidence.md`): the expo-web DEV server (Metro lazy=true) crashes the Map and Tasks screens — and the lazy heavy-lucide modals reached from them (ReportFlagModal, FlagDetailModal, NearbyFlagsModal) — into the ErrorBoundary with `createLucideIcon.default is not a function` / "Requiring unknown module". It reproduces on the pristine base; it is a dev-bundler limitation, not an app bug. CONSEQUENCE: Home, Profile, Settings, the drawer and its sub-screens render live; Map/Tasks-family live evidence is code-inferred / test-inferred / NEEDS-SKY-DEVICE — say which, per finding. PERMITTED WORKAROUND (try once, cheaply): a static export (`npx expo export --platform web --output-dir <scratchpad>/web-export` — NEVER into `~/AccessMap/dist/`) bundles without dev-lazy and MAY render Map/Tasks; if it does, the boundary lifts (record that it did, re-tag accordingly); if not, record the attempt and move on. For THIS part: Parts 1–2 already banked the evidence — Part 3 inherits their tags and re-probes only when a skeptic or judge demands it.
- DEVICE SIZES, not breakpoints: 375×667 · 390×844 · 430×932 · 834×1194, DPR 2, both themes (the wave-era 375×812 / 320×568 remain valid for apples-to-apples with Round-1 shots). Landscape: app.json orientation truth first.
- THEMES: the app's own toggle (Settings / useThemeMode) or colorScheme emulation — record which. Tiles ≠ theme (GLASS.md §12): web CartoDB dark_all is always dark; iOS Apple tiles follow the OS — never assume they match.
- DYNAMIC TYPE: RN-web ignores browser font settings — zoom/font-stress is a layout proxy (`web-approximated`); code-read allowFontScaling / maxFontSizeMultiplier / the DT guards (`test-inferred`); true ×2 is NEEDS-SKY-DEVICE.
- NATIVE A11Y, not web ARIA: accessibilityLabel/Role/State, accessibilityViewIsModal, grouping, focus order — audit via source + the RN-web-emitted ARIA tree (dump snapshots as text evidence); VoiceOver/TalkBack truth stays NEEDS-SKY-DEVICE.
- MATERIAL: per surface — right GLASS.md tier + tokens; blur budget at the worst SIMULTANEOUS state (+ the tab-bar pane, invisible to `__getLiveBlurPaneCount`); C-lite parity (`src/lib/glassMode.ts` — the Tasks-header long-press or the store/localStorage flip); Reduce Transparency designed states (iOS-only API — code/test-inferred, never renderable on web); reduced-motion parity (emulate `prefers-reduced-motion`); contrast over each surface's REAL worst-case backdrops — the arbiter decides, not the eye.
- MOTOR + COGNITIVE ACCESS are core findings, never nitpicks: 44pt HARD floor, gesture alternatives, one-handed reach per size class, cognitive load.
- CAPTURE CONSTRAINT (the auth carve): the guest path opens Map/Tasks browsing; signed-in states are UNREACHABLE by law — capture what the guest web allows, CODE-READ the rest, tag honestly, never fake a state.
— end of the shared blocks —

═══ PRE-FLIGHT (hard gates — this part synthesizes, it never re-audits) ═══
1. Repo + baseline: run the SUBJECT-block checks (root, three markers, record branch + HEAD). Any miss → STOP AND REPORT. If HEAD differs from the hash recorded in Parts 1–2's §0 blocks, STOP AND REPORT the drift (Sky decides whether the banked evidence still binds — you don't).
2. REQUIRED INPUTS, all on disk under `design-reviews/r2-audit/` — read them fully: `02_feel_findings.md` (must contain §Calibration with verdict table + dedup map + the final feel ladder, all six F-lens sections, the persona digest incl. R7) · `03_material_census.md` (census rows + completeness-critic PASS + the reading-test break list) · `04_material_migration_spec.md` (per-surface blocks + ratified-opaque rationales + the MP-phase train + the open-question register) · `tools/r2-material-stacks.json` + its banked arbiter output (exit status verified by re-reading the banked header, not by trust). Also `01_feel_orientation.md` + `01_feel_render-index.md` + `01_feel_persona-reads.md`. ANYTHING missing or incomplete → STOP AND REPORT exactly what's absent; do NOT re-run Parts 1–2 yourself and do NOT backfill their gaps with fresh judgment.
3. Ground in the Round-1 report (`design-reviews/fable-audit/2026-07-04_AccessMap_Design_Review.md`) — you are replicating its FORMAT (§1 exec summary → §2 digests → §3 findings by lens → §4 reference tables → §5 slate → §6 copy appendix → §7 honest coverage) and extending its LAW (§4 PROTECT, §5 forks + device gates). Read the P0–P5 + BENCH ledgers enough to assemble the updated PROTECT additions accurately.

RESUME RULE: if `05_r2-slate.md` or the dated report already exist partially — keep verified stages (draft → merge → skeptics → panels → report → cold read; the process log in `05_r2-slate.md` records which completed), and continue from the first gap. Never re-run a completed panel; never renumber existing T-ids.

PLAN-MODE NOTE: Sky runs AccessMap work in plan mode. If you are in plan mode, the plan = Stages 1–5 below with this pre-flight verbatim as step 0; after approval, execute exactly as approved — STOP conditions still bind mid-execution.

═══ STAGE 1 — RECONCILE (one ladder, provable conservation) ═══
- Merge Part 1's F-ladder and Part 2's material findings (census discrepancies, reading-test breaks, killed/parked ambitions) into ONE severity ladder. Dedup across parts (a Home material break found by F2 and by the census is ONE canonical item — cross-ref map, Round-1 style).
- THE CONSERVATION TABLE (the discipline that lets the eventual uplift prove closure): every F-id and every M-id lands in exactly one bucket — PROPOSAL (which T-id) · FORK (which fork, new or inherited) · PARK (why, revisit-when) · PROTECT (it's a strength — nominate it) · NO-ACTION (stated reason). No orphans; the table lives in the report.

═══ STAGE 2 — THE ROUND-2 SLATE (05_r2-slate.md is the working file with the full process log) ═══
- DRAFT: four drafter agents by cluster — touch & motion (F1+F3) · voice & states (F4+F5) · signature (F6 + F2's grammar-adjacent finds) · material continuity (the census/spec, mounted) — each drafting candidates from the ladder → merge/dedup to 12–20 proposals with stable IDs `T1…Tn` (T-ids are Round 2's series, disjoint from Round 1's S1–S20 by design; never reuse an S-id).
- ★ THE MIGRATION MOUNTS WHOLE: the material-continuity migration enters as ONE proposal (expect it near the top) whose body IS Part 2's phased sub-train — the T-id wraps MP0…MPn intact (phases, gates, branches, sizes, stop-on-branch, Sky merges). Do not shred the train into per-surface proposals; do not redesign it here — skeptics may attack it, and FIX verdicts route back as amendments noted against `04`, never silent rewrites.
- EACH PROPOSAL — header line: TITLE + WHERE (screens/components/files) · Resolves (F-/M-ids) · Effort S/M/L · Tier (QuickWin / Meaningful / Larger / Signature ★) · NEEDS-SKY-DEVICE? — then the SEVEN FIELDS (Round 1's, adapted):
  1. WHAT, vividly — the end-state, concrete enough to mockup; material work follows the GLASS §9 recipe shape.
  2. WHY — which findings it resolves + how it serves the mission, the taste anchor, and the ROUND-2 THESIS by name (one material / one voice / one hand).
  3. PERSONA IMPACT — named R1–R7; R7 (Reduce Transparency) is a first-class column now: state what the RT/C-lite experience gains, not just "unaffected."
  4. TRUST + COHESION — what changes in credibility and the one-material/one-voice read.
  5. A11Y (mandatory) — how it preserves or improves the floor; a proposal trading access for polish is invalid by definition; the RESTRAINT CHECK verdict inline.
  6. DEPENDENCIES / CONFLICTS — sequencing, PROTECT interactions (written as "preserves PROTECT-N …" statements), shared-file coordination, fork adjacency (which fork's DECISION half it must not cross).
  7. VERIFICATION — how "done right" is checked: the arbiter sibling to extend for ANY color/floor/ink change (named file), guard tests, blur-budget count at worst state, a11y-tree/SR re-walk, render states, or the device check.
- RAILS (hard): every proposal traces to F-/M-ids · WCAG 2.2 AA never traded · GLASS.md law respected — no eye-tuned floors (the arbiter decides or it doesn't ship), the blur budget never exceeded, `box-none` untouched, `GlassSurface.tsx` never edited (thread the primitive, never fork it) · privacy-sensitive / product-scope / DB-data halves become SKY-DECISION FORKS, not slate entries (UI/read halves only — the Round-1 fork discipline verbatim) · nothing re-opens a closed S/B item or a settled device read (D9/D10) · the open-question register from `04` surfaces as forks or Sky-notes, never silently resolved.
- AMBITION-TIER MIX (required): QuickWins (S) · Meaningful (M/L) · ≥3 SIGNATURE-class moves (memorable, ownable, still-in-ethos, access-first — for this product a signature that isn't accessible is a contradiction in terms). A slate of safe tweaks fails a flagship civic product — and so does a slate that mistakes decoration for beauty; the thesis is craft, not garnish.
- (Workflow-runtime tips, Round-1 precedent: `args` can arrive as an unparsed JSON string — `const A = typeof args==='string' ? JSON.parse(args) : args;` — and hard-pin the model in each agent's OPTS.)

═══ STAGE 3 — ADVERSARIAL VERIFY + JUDGE PANELS ═══
- SKEPTICS: one per proposal — KEEP / FIX / KILL — attacking rail violations, RN/Expo feasibility (expo-blur 12/24 only; engineered-tint law; the virtualization law; no CSS-only tricks), finding fidelity (does the cited F-/M-id actually say that?), restraint-check honesty, and hidden access regressions. Apply FIX conditions in place; log all verdicts in the process log; the migration proposal's skeptic additionally re-checks the train's budget math + PROTECT statements against `04`.
- JUDGE PANELS: TWO independent 3-judge panels — a disabled-user advocate (lived-experience proxy; weights the disabled-user test AND the R7 first-class rail) · a senior mobile product-craft reviewer · Sky's taste guardian (ethos + restraint). Both score every proposal on disabled-user impact + trust/cohesion + ethos fit; FINAL RANKING = the median across both panels; effort is the TIEBREAKER only — cheap never outranks mission-critical. Mark the TOP-5 "do first."

═══ STAGE 4 — ASSEMBLE THE FINAL REPORT ═══
Write `design-reviews/r2-audit/<today's ISO date>_AccessMap_R2_Design_Review.md`, Round-1 shape:
- Front-matter: Subject / Date / MODEL PROVENANCE (disclosed, not silent — authored 2026-07-09 on Fable 5 max effort; state what each part actually RAN on, including any Sky-directed fallback; note the slate's execution may land on Opus 4.8 at Sky's direction, so the report is written to be executed by a different model without re-derivation) / Stack + the read-only fence statement.
- §1 EXECUTIVE SUMMARY — ONE SCREEN, readable in 60 seconds: THE LOVELINESS VERDICT per persona (R1–R7, honest grades — does the app FEEL cared-for to each of them today?) + THE ONE-MATERIAL VERDICT (does it read as one world? where does it break?) + the top-5 + the thesis carried through in one paragraph (severity grammar still the actor; what Round 2 adds to the signature).
- §2 Orientation digest · render-index reference · persona-read digest (verbatim highlights, credited, R7's first-class-or-fallback verdict quoted).
- §3 FINDINGS carried through — the F-lens sections' canonical CRITICAL/HIGH + roll-ups, then the material findings (reading-test breaks, census headlines); every item keeps its id + evidence tags.
- §4 REFERENCE TABLES — ★ THE UPDATED PROTECT LIST: Round 1's 17 restated + the Round-2 additions (everything shipped in P0–P5 and BENCH 1–4 joins the protected baseline — name the load-bearing ones as testable statements: LiveStatusRegion's announce discipline, the Clear≥44pt bar, the bulk-sheet unification, the motion delay-gate + pulse token, the resize-on-ingest pipeline, the Lucide house style…; number them PROTECT-18+ so future rounds cite them) · THE FORK REGISTRY: Forks 1–9 inherited verbatim-by-reference + any NEW forks from this round (each a crisp either/or, framed never prescribed) · ★ THE REFRESHED DEVICE-GATE LEDGER: new ids `R2-D0…R2-Dn` — R2-D0 stays the ONE build gate (Sky's build, Sky's merge, never auto-run); RECORD THE CLOSURES: D10 closed (Sky device read, BENCH-3 §B6) and D9 closed (Sky's direct device read, 2026-07-09 — this report is the paper record catching up; cite it as exactly that); fold every still-open Round-1 gate (verify-first from the ledgers: expect D1–D8, D11 unless a ledger says otherwise) + every NEEDS-SKY-DEVICE item Parts 1–2 raised, deduped, one list that survives the round · the parked/discovery dispositions (the five open bench discoveries: dispositioned, not re-derived) · THE CONSERVATION TABLE from Stage 1.
- §5 THE ROUND-2 SLATE — ranked table (rank | T-id | title | effort | tier | score | top5) · the phase spine (which T-ids ride together; the migration train's MP0…MPn shown intact as its own sub-train inside the slate, gates and all) · per-proposal seven-field blocks · SKY-DECISION FORKS (new + touched-inherited) · the consolidated NEEDS-SKY-DEVICE pointer into §4's ledger.
- §6 COPY-OBSERVATIONS APPENDIX (from F4 + the persona reads; proposals reference it; nothing ships).
- §7 HONEST COVERAGE STATEMENT: what was web-approximated vs code-inferred vs arbiter-measured vs device-only; the lucide boundary's real cost this round (which surfaces never rendered live and what that means for confidence); unreached states; anything thinner than it should be — said plainly.
- Closing provenance line, Round-1 style.

═══ STAGE 5 — COLD-READ GATE ═══
A fresh agent with ZERO context reads ONLY the final report and answers: does it stand alone? is the exec summary truly one screen? do the two verdicts actually answer the thesis (lovely?) and the mandate (one material?)? could Opus execute the slate — especially the migration train — without asking a single design question? would Sky know exactly what to do Monday morning? Verdict SHIP or FIX-list → apply the fixes → re-verify. Record the verdict in the process log.

═══ VERIFICATION + WIN CONDITION ═══
- Every proposal traces to F-/M-ids and carries all seven fields incl. the A11Y+restraint field; tier mix + ranking rules hold (≥3 signature; impact leads; effort tiebreaks only); top-5 marked; the migration train appears INTACT (phase count in the slate == phase count in `04`).
- The conservation table has no orphan ids; the updated PROTECT list numbers its additions; the R2-D ledger records D9 + D10 as closed and folds every open gate exactly once; forks 1–9 appear as boundaries only; no closed S/B item re-opened anywhere (grep the report for `S\d` / `B\d` refs and check each is baseline context).
- Every asset/report link resolves on disk; the exec summary fits one screen; cold read = SHIP.
- FENCE: `git status --porcelain` == the §0 baseline + `design-reviews/r2-audit/` additions only; `git diff --stat` empty.
WIN: Sky opens ONE dated report and can SEE whether AccessMap is beautiful the way the thesis demands — judged surface by surface and persona by persona (RT and VoiceOver first-class), the one-material question answered with a census and an executable train, the excellent parts newly protected, every fork held, every device gate consolidated, nothing invented and nothing lost — a durable design authority the next window (any model) can execute to, exactly like Round 1. Close by printing the report's absolute path + the slate counts (total · tier mix · top-5 · migration phase count) + the cold-read verdict, and this line: **STOP — Round 2 is authored and judged. Nothing executes until Sky fires the uplift; the material train (04 + the slate) is its own Sky-gated build train.**
