# PROJECT_STATE — accessmap
_Last updated: 2026-06-19 — Opus 4.8 presentation/UX/a11y overhaul COMPLETE: Phases 1–6 MERGED to main `ae81f15` + pushed (Rory, Sky-authorized); stale branches pruned; Sky's single EAS/TestFlight device pass is the only remaining gate_

## Current Status
**Presentation/UX/a11y overhaul (Opus 4.8) — COMPLETE: Phases 1–6 MERGED + pushed (Sky-authorized via Rory, 2026-06-19):** main is `ae81f15`, synced with origin. The only remaining gate is Sky's single EAS/TestFlight device pass (the a11y-gauntlet verification — one-build checklist in `qa-reports/2026-06-19_DesignOverhaul_Phase6_A11yGauntlet.md`). A read-only 9-lens critique (`qa-reports/2026-06-19_DesignOverhaul_Phase0_Plan.md`) drove four phases, all behind a hard data/privacy fence:
- **Phase 1** (live earlier today) — branded web cold open: instant-paint splash + brand pin, dark-mode `prefers-color-scheme` harmony, reduced-motion loading cue, installable PWA manifest.
- **Phase 2** — dark-mode card elevation fixed (lighter card than its wash + soft cool glow), OnboardingCards → brand palette, severity-label scale unified to one derived source, Button 44pt floor, DESIGN.md/CLAUDE.md reconcile (dropped deleted Pill/PointsChip).
- **Phase 3** — Hero 2 report-a-flag **privacy trust signal** (the senior-grade EXIF/GPS stripping is now visible + confirmed on success — stripping logic untouched), dead heatmap-label + dangling-pronoun fixes, native-callout brand fonts, ResourcesScreen seeded (URLs are `TODO(Sky)`, info-cards until then).
- **Phase 4** — **points-display correctness (now shows the live trigger 10/3/15/7)** — resolves the long-standing points-drift carry-over; reward FlashBanner entrance/exit animation (reduced-motion gated); triage reward pill AA fix.

Gates: typecheck 0 · lint 0 errors / 92 warnings · 107 suites / **1,721 tests** · **fence intact** (zero data/auth/EXIF/RLS/RPC/points-trigger changes; the only `flags.ts` touch was the editable `SEVERITY_LABELS` export). Per-phase reports + `2026-06-19_Rory_Phases2-4_Release.md` in `qa-reports/`. Rollback: revert the 3 merge commits (`173602d` `a1e5d71` `9bfff30`) or `git reset --hard 186c471 && git push --force-with-lease`. **Remaining gate is Sky-side: the EAS/TestFlight device pass** — dark-mode elevation feel, the privacy moment, the reward animation 60fps, VoiceOver/TalkBack — plus privacy-copy sign-off + ResourcesScreen links. **Phase 5** (motion: `PressableScale` press language + reward-pill animation) also merged (`78eae3d`). **Phase 6** (a11y gauntlet: reduced-motion sweep across all 18 modals, the textSubtle tertiary token darkened to AA in both palettes, web `:focus-visible` ring) also merged (`ae81f15`) — every text token now clears WCAG AA and zero ungated modal animations remain. **The 6-phase overhaul is COMPLETE on main.** **Housekeeping:** the overhaul branches + 30 stale pre-overhaul local branches were pruned (tip SHAs recorded in the Rory release records for ~90-day reflog recovery); only `main` remains local.


**Main is green and build-ready.** The re-sweep branch (`audit/accessmap-resweep-2026-06-09`: 43 audit fixes F28–F65 + all of Sky's 2026-06-09 decisions + all 15 remaining medium/low items) has been merged and pushed on Sky's pre-approval. Main is now `9f0a792`, synced with `origin/main`. typecheck 0 · 103 suites / 1,680 tests · lint 0 errors / 165 warnings. Remaining gate is Sky-side: re-confirm EAS env vars, then run the two EAS build + submit commands. (Carry-over: reviewer-password rotation + points-value drift decision.)

**Overhaul MERGED + pushed (Sky-approved 2026-06-17):** the expressive UI/UX overhaul is on `main` at `a94cf25` (merge --no-ff, pushed to origin), plus a hardening sweep (`ui/overhaul-hardening-2026-06-17`: +60 tests → **1,740**, visual-enforcement, adversarial-review fixes). typecheck 0 · lint 0 errors · 108 suites/1,740 tests · fences untouched. Audit + plan: `qa-reports/2026-06-17_AccessMap_Creative_UIUX_Audit.md`. Fence-feature proposals: `qa-reports/2026-06-17_AccessMap_FenceFeature_Proposals.md`. **Code-verified, NOT eye-verified** — Sky will see it live on the next EAS build (frosted-glass look + large-text wrapping + new banner placement are unseen; frosted glass has one RN unknown: iOS shadow on a translucent panel). Rollback: `git revert -m 1 a94cf25 && git push`. **Pending Sky decisions:** 3 fence features (#5 photos / #7 voice / #8 leaderboard windows) + one deferred LOW (shared-modal reduced-motion gate).

## Context Snapshot
Sky directed a whole-app UI/UX elevation of AccessMap (Expo/RN). The audit found the design system was already premium (3 prior passes), so the work was reframed as **elevation + adoption + the bolder direction**, not a rebuild. Sky explicitly chose the **MORE EXPRESSIVE** aesthetic (gradients, soft glows, celebratory gamification beats) and **run-end-to-end** (no mid checkpoint), held to a WCAG AA / reduced-motion / 60fps floor. After the UI pass merged, Sky asked for the flagged brand-font cleanup + build prep; both done. Sky then interrupted with `/new-window` to compress before starting a separate **Prompt Library Tool** UI pass in a fresh window.

## Recent Outcomes
- **Expressive UI/UX overhaul — branch (2026-06-17, NOT merged):** Sky-directed "evolve & enforce the expressive system + bolder + frosted glass + all 10 UX ideas." Read-only audit first (`2026-06-17_AccessMap_Creative_UIUX_Audit.md`) — corrected the multi-agent walk's over-grading (severity-row/PointsChip/map-rows over-claims), verdict: established design language, evolve-not-rebuild. Then 11 gated commits on `ui/expressive-overhaul-2026-06-17`: design-system foundation (nav tokens, icon scale, brand-hex doc reconcile #2f80ed→#1466E0), a11y (reduced-motion modal gates, comment-timestamp contrast, status-change SR announcements, sub-44pt targets, `numberOfLines={1}` title de-caps), and the **frosted-glass `GlassSurface`** primitive (blur + AA contrast floor + Reduce-Transparency fallback) adopted on the map filter panel + legend. **6 of 10 net-new features built** (#1 filter counts, #2 smart empty-state, #3 nearest-open suggestion, #6 verification timeline, #9 profile status-pill filters, #10 severity non-color cue); **#4 swipe-to-triage CUT** (redundant — Tasks cards already have inline Verify/Resolve/Reject). **3 features need Sky decisions / cross fences** (#5 before/after photos → photo-upload/EXIF path [recommended]; #7 voice search → mic permission [defer]; #8 leaderboard windows → schema/trigger + weekly-grind ethics call) — proposals in `2026-06-17_AccessMap_FenceFeature_Proposals.md`. Code-verified, not eye-verified. Rollback: delete branch.
- **Re-sweep fix pass + merge (2026-06-10):** Sky decided all 8 re-sweep decision items on 2026-06-09; all decisions executed + all 15 verified-not-fixed items (M1–M5, L1–L10) fixed in 13 gated commits; branch merged --no-ff to main **`9f0a792`** + pushed (Sky pre-approved). Highlights: notification toggles hidden behind `PUSH_NOTIF_TYPES_ENABLED` flag, storage-orphan cleanup on failed submit, fresh GPS on report FAB, friendly error mapping, deep-link share URLs + markers, web-safe filter-set menu, full submit-disable sweep, warm deep links while signed out, catalog-derived milestones. 3-lens review PASS×3. +95 tests / +7 suites. Status-guard SQL remains **file only** (Decision 4). Report: `qa-reports/2026-06-09_AccessMap_ReSweep_Fixes.md`. Rollback: `git revert -m 1 9f0a792 && git push`.
- **Re-sweep audit complete (2026-06-09):** `audit/accessmap-resweep-2026-06-09`. 43 new verified findings fixed (F28–F65): PNG-upload false-positive privacy gate, post-sign-out tile resurrection, silent push opt-out failure, silent offline sign-out, stale-snapshot status overwrites (now CAS), all-silent web failure feedback, flaky baseline test suite. Report: `qa-reports/summaries/2026-06-09_AccessMap_ReSweep_Report.md`.
- **Deep-audit merged + pushed (2026-06-07):** `cbf9a3b → c6298df` (no-ff). 27 verified fixes (F1–F27: EXIF/GPS privacy, double-submit guards, broken flows, races, leaks). 97 suites / 1,575 tests / typecheck 0 / lint 0 errors. Orphaned `onboarding.ts` removed. Docs reconciled. schema.sql synced to live. See `qa-reports/summaries/2026-06-07_AccessMap_PreBuild_StepA_Report.md`.
- **Backend security gate — DONE LIVE 2026-06-03:** Duplicate points trigger dropped, flag-photo INSERT guard tightened, RLS `search_path` hardened, webhook secret moved to Vault, `is_admin` bug fixed. Applied to live DB by Sky via Cowork; verified read-only by Morgan/Gary. These are NOT pending — they are done. Cite: `qa-reports/summaries/2026-06-07_AccessMap_PreBuild_Checklist.md §D1`.
- **UI pass merged + pushed (2026-06-03):** `df02ca1 → 7018bd5` (no-ff). Design-system elevation. App now 100% Lucide/SVG.
- **Brand-font cleanup merged + pushed (2026-06-04):** `7018bd5 → f499fc8` (no-ff). ~180 `<Text>`→`<AppText>` across 24 screens/modals.
- **Gates green throughout:** typecheck 0; 97 suites / 1,575 tests; lint 0 errors. `expo export --platform ios` bundles clean.
- **Build readiness verified:** eas.json (`appVersionSource: remote`, testflight `distribution: store` + `environment: production`, submit `ascAppId 6774709116`/team `S78F8ZA8QU`) ✓; app.json (bundle `com.accessmap.app`, version 3.0.0, usage strings, `ITSAppUsesNonExemptEncryption: false`) ✓; privacy-policy URL live ✓.

## Next Actions (Sky-side only — all agent work is done)
1. **Sky** — `npx eas-cli env:list --environment production` → re-confirm `EXPO_PUBLIC_SUPABASE_URL` + `EXPO_PUBLIC_SUPABASE_ANON_KEY` present (env can change between sessions; missing = app launches blank).
2. **Sky** — from `~/AccessMap`: `npx eas-cli build --platform ios --profile testflight --non-interactive` (~15–20 min) then `npx eas-cli submit --platform ios --profile production --latest --non-interactive`. Agents CANNOT run EAS/App Store.
3. **Sky** — carry-over: rotate `reviewer@accessmap.com` password before App Store (old value in git history); decide points-value drift (live 10/3/15/7 vs docs 5/2/10/5).

## Open Risks
- **EAS Supabase env unverified this session** — if `EXPO_PUBLIC_SUPABASE_*` are missing from EAS prod, the build launches blank/crashes. Sky must verify before/at build.
- **Live-but-unverified UI** — the expressive UI + brand fonts + the 2026-06-10 re-sweep merge are LIVE on main/origin but NOT yet device-verified; RN gradients/shadows/haptics only fully render on-device. On-device VoiceOver/TalkBack + visual pass still pending on a TestFlight build (manual checklist in `qa-reports/2026-06-09_AccessMap_ReSweep_Fixes.md §7`). Rollback: `git revert -m 1 9f0a792` (re-sweep) / `git revert -m 1 f499fc8` (brand fonts) / `git revert -m 1 7018bd5` (UI pass) `&& git push`.
- **POINTS-VALUES-DRIFT** (carry-over) — live `handle_flag_status_change` awards 10/3/15/7; schema.sql/CLAUDE.md say 5/2/10/5. Sky decision pending. Trust live catalog over schema.sql.
- **Reviewer password** (carry-over) — rotate before App Store (old value in public git history).
