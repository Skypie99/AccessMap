{
  "cli": {
    "version": ">= 10.0.0",
    "appVersionSource": "local",
    "// appVersionSource": "local = app.json `version` is the source of truth for the marketing version. iOS buildNumber auto-increments per profile (autoIncrement below) so successive uploads don't collide."
  },

  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal",
      "ios": {
        "simulator": false
      },
      "env": {
        "APP_ENV": "development"
      }
    },

    "preview": {
      "distribution": "internal",
      "autoIncrement": true,
      "ios": {
        "simulator": false
      },
      "env": {
        "APP_ENV": "preview",
        "SENTRY_DISABLE_AUTO_UPLOAD": "true"
      }
    },

    "preview2": {
      "// description": "Parallel internal TestFlight channel. Inherits everything from preview; only APP_ENV differs. Second concurrent build slot (e.g. a separate tester group).",
      "extends": "preview",
      "env": {
        "APP_ENV": "preview2"
      }
    },

    "preview3": {
      "// description": "Third parallel internal TestFlight channel. Inherits from preview; only APP_ENV differs.",
      "extends": "preview",
      "env": {
        "APP_ENV": "preview3"
      }
    },

    "production": {
      "// PRIVACY GATE": "Do NOT run this profile until the D8 EXIF GPS leak (see PROJECT_STATE.md) is closed. App Store submission of location data while that leak is open is a pre-launch blocker.",
      "distribution": "store",
      "autoIncrement": true,
      "ios": {
        "simulator": false
      },
      "env": {
        "APP_ENV": "production"
      }
    }
  },

  "submit": {
    "production": {
      "ios": {
        "appleId": "$EXPO_APPLE_ID",
        "ascAppId": "$EXPO_ASC_APP_ID",
        "appleTeamId": "$EXPO_APPLE_TEAM_ID"
      },
      "android": {
        "serviceAccountKeyPath": "TODO_PATH_TO_GOOGLE_SERVICE_ACCOUNT_KEY.json",
        "track": "internal"
      }
    }
  }
}
